# small-vault-ambiguous

Exercises D-20 core roster: small vault (≤6 pages, <10 classifier clusters)
for the TTY small-batch review-typing path (cluster count below the N=20
threshold per RESEARCH Q3).  All four pages are freshly bootstrapped with
`bootstrap_stage: bootstrapped`, empty `type:`, and varied filename
conventions (kebab-case and PascalCase) + H1 shapes to surface a realistic
mix of classifier signals.

Expected outputs live in `expected/`; input is frozen — do not edit without
regenerating `expected/` (see tests/phase-11/fixtures/README.md for the
regeneration recipe).
