# already-tagged-vault

Exercises D-20 core roster: pages where every TL;DR + Key Facts bullet
already carries `[epistemic:: sourced]` or `[epistemic:: inferred]`.
Proves `02-provenance-bootstrap` is idempotent (no double-tagging on
already-tagged bullets) and reports honestly via
`provenance-bootstrap-report.yaml` / applied.log summary when no eligible
bullets remain.

Expected outputs live in `expected/`; input is frozen — do not edit without
regenerating `expected/` (see tests/phase-11/fixtures/README.md).
