---
phase: 01-schema-structure-conventions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AGENTS.md
  - sources/.gitkeep
  - wiki/entities/.gitkeep
  - wiki/concepts/.gitkeep
  - wiki/sources/.gitkeep
  - wiki/comparisons/.gitkeep
  - wiki/overviews/.gitkeep
  - wiki/index.md
  - wiki/log.md
  - schema/templates/.gitkeep
autonomous: true
requirements:
  - DIRS-01
  - DIRS-02
  - DIRS-03
  - DIRS-04
  - SCHM-01
  - SCHM-03
  - SCHM-04
  - OBSD-01
  - OBSD-02
  - OBSD-03
  - OBSD-04
  - PROG-01
  - PROG-02
  - PROG-03

must_haves:
  truths:
    - "An LLM agent can read AGENTS.md sections 1-8 and understand how to create wiki pages with correct frontmatter, section structure, wikilinks, and provenance syntax"
    - "The directory structure exists with clear separation between sources/, wiki/, and schema/ layers"
    - "All wiki page conventions specify valid Obsidian wikilinks, Dataview-compatible YAML frontmatter, and graph-friendly structure"
    - "The schema documents progressive disclosure structure and instructs agents to navigate shallow-first"
    - "Each page type has a fully worked example showing frontmatter, section structure, wikilinks, and provenance markers in context"
    - "Negative constraints tell agents what NOT to do, preventing common mistakes"
  artifacts:
    - path: "AGENTS.md"
      provides: "Agent-agnostic schema document sections 1-8 with worked examples"
      min_lines: 500
    - path: "sources/.gitkeep"
      provides: "Raw sources directory marker"
    - path: "wiki/entities/.gitkeep"
      provides: "Entity pages directory"
    - path: "wiki/concepts/.gitkeep"
      provides: "Concept pages directory"
    - path: "wiki/sources/.gitkeep"
      provides: "Source summary pages directory"
    - path: "wiki/comparisons/.gitkeep"
      provides: "Comparison pages directory"
    - path: "wiki/overviews/.gitkeep"
      provides: "Overview pages directory"
    - path: "wiki/index.md"
      provides: "Empty content index scaffold"
    - path: "wiki/log.md"
      provides: "Empty activity log scaffold"
    - path: "schema/templates/.gitkeep"
      provides: "Templates directory for page type templates"
  key_links:
    - from: "AGENTS.md"
      to: "wiki/"
      via: "Section 2 directory structure references"
      pattern: "wiki/(entities|concepts|sources|comparisons|overviews)"
    - from: "AGENTS.md"
      to: "sources/"
      via: "Section 2 directory structure references"
      pattern: "sources/YYYY"
---

<objective>
Create the directory skeleton and the first half of AGENTS.md (sections 1-8): Overview, Directory Structure, Global Rules, Page Types & Templates, Frontmatter Schema, Provenance/Epistemics/Staleness, Progressive Disclosure, and Wikilink & Graph Conventions. Each page type includes a fully worked example page. Negative constraints are included to tell agents what NOT to do.

Purpose: Establishes the physical vault structure and the foundational specification sections that define how pages are created, structured, and linked. Addresses review feedback: worked examples per page type, negative constraints, and concrete schema consistency.
Output: Directory tree with all wiki subdirectories, stub index/log files, and AGENTS.md with sections 1-8 complete including worked examples.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-structure-conventions/01-CONTEXT.md
@.planning/phases/01-schema-structure-conventions/01-RESEARCH.md
@idea.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create directory skeleton with stub files</name>
  <files>
    sources/.gitkeep,
    wiki/entities/.gitkeep,
    wiki/concepts/.gitkeep,
    wiki/sources/.gitkeep,
    wiki/comparisons/.gitkeep,
    wiki/overviews/.gitkeep,
    wiki/index.md,
    wiki/log.md,
    schema/templates/.gitkeep
  </files>
  <read_first>
    .planning/phases/01-schema-structure-conventions/01-CONTEXT.md
  </read_first>
  <action>
    Create the directory structure per D-05, D-06, D-07, D-08:

    1. Create `sources/.gitkeep` (empty file to track the directory in git, per D-05 and DIRS-01)
    2. Create `wiki/entities/.gitkeep` (per D-06 and DIRS-02)
    3. Create `wiki/concepts/.gitkeep` (per D-06)
    4. Create `wiki/sources/.gitkeep` (per D-06)
    5. Create `wiki/comparisons/.gitkeep` (per D-06)
    6. Create `wiki/overviews/.gitkeep` (per D-06)
    7. Create `schema/templates/.gitkeep` (per D-05 optional schema/ directory)

    8. Create `wiki/index.md` (per D-07) with this exact content:
    ```markdown
    ---
    id: index
    title: Content Index
    type: overview
    status: active
    summary: "Master catalog of all wiki pages organized by type."
    created_at: 2026-04-09
    updated_at: 2026-04-09
    sources: []
    epistemic_status: sourced
    tags:
      - meta
      - navigation
    domains: []
    supersedes:
    superseded_by:
    privacy: cloud_safe
    ---

    # Content Index

    ## Entities

    _No entity pages yet._

    ## Concepts

    _No concept pages yet._

    ## Sources

    _No source summary pages yet._

    ## Comparisons

    _No comparison pages yet._

    ## Overviews

    _No overview pages yet._
    ```

    9. Create `wiki/log.md` (per D-07) with this exact content:
    ```markdown
    ---
    id: log
    title: Activity Log
    type: overview
    status: active
    summary: "Chronological record of all wiki operations."
    created_at: 2026-04-09
    updated_at: 2026-04-09
    sources: []
    epistemic_status: sourced
    tags:
      - meta
      - operations
    domains: []
    supersedes:
    superseded_by:
    privacy: cloud_safe
    ---

    # Activity Log

    _No operations recorded yet._
    ```

    10. Initialize git tracking: run `git add sources/ wiki/ schema/` to stage the new directories.
  </action>
  <verify>
    <automated>test -d sources && test -d wiki/entities && test -d wiki/concepts && test -d wiki/sources && test -d wiki/comparisons && test -d wiki/overviews && test -d schema/templates && test -f wiki/index.md && test -f wiki/log.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `sources/.gitkeep` exists (empty file)
    - `wiki/entities/.gitkeep` exists
    - `wiki/concepts/.gitkeep` exists
    - `wiki/sources/.gitkeep` exists
    - `wiki/comparisons/.gitkeep` exists
    - `wiki/overviews/.gitkeep` exists
    - `schema/templates/.gitkeep` exists
    - `wiki/index.md` contains `id: index` in frontmatter
    - `wiki/index.md` contains `## Entities` section heading
    - `wiki/log.md` contains `id: log` in frontmatter
    - `wiki/log.md` contains `# Activity Log` heading
  </acceptance_criteria>
  <done>All 7 directories exist with .gitkeep files, index.md and log.md contain valid Dataview-compatible YAML frontmatter and section scaffolds</done>
</task>

<task type="auto">
  <name>Task 2: Write AGENTS.md sections 1-8 with worked examples and negative constraints</name>
  <files>AGENTS.md</files>
  <read_first>
    .planning/phases/01-schema-structure-conventions/01-CONTEXT.md,
    .planning/phases/01-schema-structure-conventions/01-RESEARCH.md,
    idea.md
  </read_first>
  <action>
    Create `AGENTS.md` at the repository root (per D-01, D-02) with sections 1 through 8 of the 16-section outline (per D-35). The file must be self-contained enough that an LLM with no prior context can read it and operate correctly (per SCHM-01).

    **CRITICAL:** This file is the sole authoritative specification. It must be prescriptive and step-by-step (per D-03). Use concrete examples, not abstract descriptions. Addresses review feedback: every page type gets a fully worked example, and negative constraints are included throughout.

    Write the following sections:

    **Section 1: Overview and Principles** (~30 lines)
    - State that this is the sole authoritative schema for the LLM Wiki Compiler
    - Three layers: raw sources (immutable), wiki (LLM-maintained), schema (this file + templates)
    - Core principle: the wiki is a persistent, compounding artifact
    - The LLM writes and maintains the wiki; the human curates sources and asks questions
    - List the four operations: ingest, query, lint, reflect
    - State that workflows are defined later in this document (Section 11)

    **Section 2: Directory Structure** (~50 lines)
    Per D-05, D-06, D-07, D-08, D-09:
    - Show the exact directory tree (copy from research Architecture Patterns section)
    - Explain `sources/` uses chronological nesting: `YYYY/YYYY-MM/YYYY-MM-DD-slug/`
    - Bundle folders when assets exist: `source.md` + attachments co-located
    - Single `.md` file for text-only sources (no bundle directory needed)
    - `wiki/` organized by page type: entities/, concepts/, sources/, comparisons/, overviews/
    - `wiki/index.md` and `wiki/log.md` live inside wiki/ (per D-07)
    - `schema/` is optional, for templates and examples
    - Source metadata (type, topic, privacy) stored in frontmatter, not directory structure (per D-09)

    **Section 3: Global Rules** (~60 lines)
    - All dates use ISO 8601 format: `YYYY-MM-DD` or `YYYY-MM-DDTHH:mm:ss`
    - All frontmatter field names use `snake_case` (Dataview compatibility per research Pitfall 2)
    - Commit conventions (per D-25, D-26): conventional commit format with types `ingest(source-slug):`, `query(topic):`, `lint(scope):`, `reflect(scope):`, `schema:`
    - One commit per logical operation, not per file
    - LLM navigation rule: always read `wiki/index.md` first, then drill into specific pages. Read shallow sections (TL;DR, Key Facts) before Detail sections. (per PROG-03)
    - Red links are allowed and intentional -- they signal knowledge gaps for the lint workflow (per D-33)

    **IMPORTANT -- Include a "DO NOT" subsection (addresses Gemini review feedback on negative constraints):**
    ```
    ### What Agents Must NOT Do
    - DO NOT create topic-based directories (e.g., `wiki/machine-learning/`). Use frontmatter `domains` field and Dataview queries instead.
    - DO NOT put conventions or rules in any file other than AGENTS.md. This is the sole source of truth.
    - DO NOT put wikilinks in YAML frontmatter. Use string IDs in frontmatter, wikilinks in body text.
    - DO NOT use display aliases in wikilinks: write `[[Attention Mechanism]]` not `[[Attention Mechanism|attention]]`.
    - DO NOT put provenance blobs, relation arrays, or decay settings in base frontmatter. Those belong in type-specific fields.
    - DO NOT delete or move files when archiving. Set `status: archived` and remove from index active listings.
    - DO NOT read the entire wiki when answering a query. Read index first, then TL;DR/Key Facts of relevant pages, then Detail only when needed.
    - DO NOT create multiple commits for a single logical operation. One ingest = one commit, even if it touches 15 files.
    - DO NOT send `local_only` content to cloud LLM APIs under any circumstances.
    - DO NOT link to the same page more than once in a single page body. Link on first mention only.
    ```

    **Section 4: Page Types and Templates** (~150 lines)
    Per D-06, SCHM-03, D-20 through D-24:
    Define five page types with their purpose, when to use each, and a FULLY WORKED EXAMPLE for each (addresses Codex HIGH review feedback).

    1. **Entity** (`type: entity`) -- People, tools, organizations, specific named things
       Sections: TL;DR -> Key Facts -> Detail -> Related Pages -> Sources (per D-20)
       Include a complete worked example: "Geoffrey Hinton" entity page with full frontmatter, TL;DR, Key Facts with provenance markers, Detail, Related Pages with wikilinks, Sources section. Copy the example from the RESEARCH.md Code Examples section.

    2. **Concept** (`type: concept`) -- Ideas, theories, frameworks, abstract topics
       Sections: TL;DR -> Key Facts -> Detail -> Related Pages -> Sources (per D-20)
       Include a complete worked example: "Attention Mechanism" concept page with full frontmatter (including `aliases: [Attention, Self-Attention]`), TL;DR referencing `[[Transformer Architecture]]`, Key Facts with `[prov:...]` markers, Detail section, Related Pages, Sources.

    3. **Source Summary** (`type: source`) -- One per ingested source document
       Sections: TL;DR -> Key Takeaways -> Extracted Claims -> Notes -> Source Metadata (per D-21)
       Additional frontmatter: `path`, `url`, `content_hash`, `ingested_at`, `source_type`
       Include a complete worked example: source summary for "Vaswani et al. - Attention Is All You Need" with full frontmatter (including `source_type: paper`, `content_hash`, `ingested_at`, `path`), TL;DR, Key Takeaways, Extracted Claims each with `[prov:...]` markers including locators, Notes, Source Metadata section.

    4. **Comparison** (`type: comparison`) -- Contrasting sources, viewpoints, or approaches
       Sections: TL;DR -> Bottom Line -> Comparison Table -> Detailed Comparison -> Sources (per D-22)
       Include a complete worked example: "RNNs vs Transformers" comparison page with full frontmatter, TL;DR, Bottom Line, a markdown comparison table with columns for Architecture/Strengths/Weaknesses/Best For, Detailed Comparison section, Sources.

    5. **Overview** (`type: overview`) -- High-level topic summaries, synthesis pages
       Sections: TL;DR -> Key Facts -> Detail -> Related Pages -> Sources (same as entity/concept)
       Include a complete worked example: "Deep Learning" overview page with full frontmatter, TL;DR synthesizing across sources, Key Facts with multiple provenance markers, Detail, Related Pages, Sources.

    **Section 5: Frontmatter Schema** (~80 lines)
    Per D-10, D-11, D-12, SCHM-04, OBSD-02:

    Define the base frontmatter fields required on EVERY wiki page:
    ```yaml
    ---
    id: slug-style-identifier        # Unique page ID, kebab-case
    title: "Human Readable Title"     # Canonical page title
    type: entity|concept|source|comparison|overview
    status: active|stale|superseded|archived
    summary: "One-sentence description for index scanning."
    created_at: YYYY-MM-DD           # ISO 8601
    updated_at: YYYY-MM-DD           # ISO 8601
    sources:                          # List of source IDs (strings, NOT wikilinks)
      - src-YYYY-MM-DD-slug
    epistemic_status: sourced|mixed|tentative|stale
    tags:                             # Flat list for Dataview queries
      - tag-name
    domains:                          # Topic/category classification
      - domain-name
    supersedes:                       # ID of page this replaces (if any)
    superseded_by:                    # ID of page that replaces this (if any)
    privacy: local_only|cloud_safe    # Privacy routing tier
    aliases:                          # Alternative names for Obsidian resolution
      - Alternate Name
    ---
    ```

    Explain each field's purpose and constraints. Include a "Frontmatter Validation Checklist" subsection:
    - All 16 fields present (id, title, type, status, summary, created_at, updated_at, sources, epistemic_status, tags, domains, supersedes, superseded_by, privacy, aliases -- plus any type-specific fields)
    - `type` is one of: entity, concept, source, comparison, overview
    - `status` is one of: active, stale, superseded, archived
    - `epistemic_status` is one of: sourced, mixed, tentative, stale
    - `privacy` is one of: local_only, cloud_safe
    - `created_at` and `updated_at` match ISO 8601 pattern `YYYY-MM-DD`
    - `sources` is a YAML list of string IDs, NOT wikilinks
    - `tags` and `domains` are YAML lists of lowercase kebab-case strings
    - `summary` is a single quoted string, not multi-line

    Note:
    - `sources` contains string IDs, NOT wikilinks (per research Pattern 3: Body Links, Frontmatter IDs)
    - `epistemic_status` values: `sourced` (directly from source), `mixed` (some sourced + some inferred), `tentative` (weak evidence), `stale` (likely outdated) (per D-11)
    - No `confidence` field -- epistemic_status is sufficient (per D-11)
    - Type-specific fields (provenance blobs, decay settings, comparison dimensions) go in type-specific sections, not base frontmatter (per D-12)

    Define type-specific additional fields for source summary pages:
    ```yaml
    path: sources/YYYY/YYYY-MM/YYYY-MM-DD-slug/source.md
    url: "https://..."                 # Original URL if applicable
    content_hash: "sha256:abc123..."   # For staleness detection
    ingested_at: YYYY-MM-DD           # When source was processed
    source_type: article|paper|transcript|journal|data|image
    ```

    **Section 6: Provenance, Epistemics, and Staleness** (~70 lines)
    Per D-13 through D-18, OBSD-01:

    Define inline provenance syntax:
    - Basic: `[prov:<source_id>#<locator>]`
    - Extended: `[prov:<source_id>#<locator>|<support_type>|<checked_at>]`

    Locator types (per D-14):
    - `#p12-14` -- page range
    - `#sec:introduction` -- section name
    - `#para3` -- paragraph number
    - `#t00:12:10-00:12:48` -- timestamp range (for transcripts)
    - `#img2` -- image reference

    Support types (per D-15): `direct`, `inferred`, `tentative`, `derived`

    `checked_at` (per D-16): ISO 8601 date when provenance link was last verified.

    Include multiple examples in context showing different locator types:
    ```markdown
    - Attention mechanisms allow models to focus on relevant input tokens [prov:src-2026-03-15-vaswani-attention#sec:introduction|direct|2026-04-08]
    - The model achieves 28.4 BLEU on WMT 2014 English-to-German [prov:src-2026-03-15-vaswani-attention#p8|direct|2026-04-08]
    - Hinton expressed concerns about AI safety risks [prov:src-2026-03-20-hinton-interview#t00:12:10-00:12:48|direct|2026-04-08]
    ```

    Include a "bad vs. good" example (addresses Codex review feedback):
    ```
    BAD:  Attention is important.
    GOOD: Attention mechanisms allow models to focus on relevant input tokens [prov:src-2026-03-15-vaswani-attention#sec:introduction|direct|2026-04-08]

    BAD:  [prov:vaswani] (missing locator)
    GOOD: [prov:src-2026-03-15-vaswani-attention#sec:introduction]

    BAD:  [prov:src-2026-03-15-vaswani-attention#page8] (invalid locator format)
    GOOD: [prov:src-2026-03-15-vaswani-attention#p8] (correct: #p prefix for pages)
    ```

    Source registry (per D-17): Each source summary page in `wiki/sources/` serves as the registry entry for that source. Its frontmatter contains: `id` (source_id), `path`, `title`, `source_type`, `url`, `content_hash`, `ingested_at`. This is the Dataview-native approach -- query source metadata with `TABLE source_type, ingested_at FROM "wiki/sources"`.

    Validation rules (per D-18):
    - Every `[prov:...]` reference MUST resolve to a known source ID in `wiki/sources/`
    - Every locator MUST be syntactically valid (matches one of the defined patterns)
    - If a source's `content_hash` has changed since `checked_at`, dependent claims SHOULD be marked with `epistemic_status: stale`

    **Section 7: Progressive Disclosure** (~40 lines)
    Per D-19, D-24, PROG-01, PROG-02, PROG-03:

    State the principle: all pages are structured shallow-to-deep. Top is optimized for fast LLM scanning; bottom is for human verification and deep reading.

    Rules for LLM agents:
    1. When searching for information, read `wiki/index.md` FIRST
    2. Scan TL;DR and Key Facts sections of relevant pages BEFORE reading Detail sections
    3. Only read Detail and Sources sections when shallow sections are insufficient
    4. When creating pages, TL;DR MUST be 1 short paragraph or 2-4 bullets (per D-24)
    5. Key Facts MUST be compact bullets with inline provenance markers
    6. Detail section contains full narrative, synthesis, caveats, nuance
    7. Sources section at the bottom lists human-readable source references

    Reference the per-type section orderings defined in Section 4.

    **Section 8: Wikilink and Graph Conventions** (~40 lines)
    Per D-32, D-33, D-34, OBSD-01, OBSD-03:

    Rules:
    - Use `[[Exact Page Title]]` for all cross-references in page body (per D-34)
    - Link on FIRST mention only per page; subsequent mentions are plain text (per D-32)
    - DO NOT use display aliases in wikilinks: `[[Attention Mechanism]]` not `[[Attention Mechanism|attention]]` (per D-34)
    - Use `aliases` frontmatter field for alternate names Obsidian resolves automatically
    - Red links (links to non-existent pages) are ALLOWED and intentional (per D-33) -- they signal knowledge gaps
    - DO NOT put wikilinks in YAML frontmatter -- use string IDs in frontmatter, wikilinks in body (per research Pitfall 1)
    - The `## Related Pages` section lists explicit wikilinks to connected pages
    - The `## Sources` section in page body lists human-readable source references with wikilinks to source summary pages

    Include a wikilink "bad vs. good" example:
    ```
    BAD:  sources: ["[[Vaswani et al]]"]           (wikilink in frontmatter)
    GOOD: sources: [src-2026-03-15-vaswani-attention]  (string ID in frontmatter)

    BAD:  [[Attention Mechanism|attention]]         (display alias)
    GOOD: [[Attention Mechanism]]                   (exact title match)

    BAD:  ...the [[Attention Mechanism]] uses [[Attention Mechanism]] weights...  (linked twice)
    GOOD: ...the [[Attention Mechanism]] uses attention weights...  (linked once, plain text after)
    ```

    The document should end with a comment: `<!-- Sections 9-16 follow below -->` to indicate continuation.

    **TARGET LENGTH:** 500-700 lines total for sections 1-8. The increase from original 400-600 is due to the fully worked examples per page type (addresses review feedback). Be prescriptive and concrete but not padded.
  </action>
  <verify>
    <automated>test -f AGENTS.md && grep -c "^## " AGENTS.md | xargs -I{} test {} -ge 8 && grep -q "Directory Structure" AGENTS.md && grep -q "Frontmatter Schema" AGENTS.md && grep -q "Progressive Disclosure" AGENTS.md && grep -q "Wikilink" AGENTS.md && grep -q "prov:" AGENTS.md && grep -q "DO NOT" AGENTS.md && grep -q "BAD:" AGENTS.md && grep -q "GOOD:" AGENTS.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `AGENTS.md` exists at repository root
    - File contains at least 8 `## ` level-2 headings
    - Contains string "Directory Structure" (Section 2)
    - Contains string "Page Types" (Section 4)
    - Contains string "Frontmatter Schema" (Section 5)
    - Contains string `id:` and `title:` and `type:` and `epistemic_status:` in the frontmatter schema section
    - Contains string `snake_case` (field naming convention)
    - Contains string `prov:` (provenance syntax)
    - Contains string `#sec:` and `#p` and `#para` (locator types)
    - Contains string "Progressive Disclosure" (Section 7)
    - Contains string `wiki/index.md` (navigation instruction per PROG-03)
    - Contains string "Wikilink" (Section 8)
    - Contains string `[[` (wikilink syntax examples)
    - Contains string `aliases` (frontmatter field for Obsidian resolution)
    - Contains string `local_only` and `cloud_safe` (privacy field values)
    - Contains string `ISO 8601` (date format rule)
    - Contains "What Agents Must NOT Do" or "DO NOT" subsection (negative constraints)
    - Contains at least 5 worked examples (one per page type) indicated by `---` frontmatter delimiters inside code blocks
    - Contains "BAD:" and "GOOD:" comparison examples for provenance and wikilinks
    - Contains "Validation Checklist" for frontmatter fields
    - File has at least 500 lines: `wc -l AGENTS.md` >= 500
  </acceptance_criteria>
  <done>AGENTS.md exists at repo root with sections 1-8 complete, covering directory structure, global rules (including negative constraints), page types (each with a fully worked example page), frontmatter schema with validation checklist, provenance syntax with bad/good examples, progressive disclosure, and wikilink conventions with bad/good examples.</done>
</task>

</tasks>

<verification>
1. Directory structure: `find sources/ wiki/ schema/ -type f | sort` shows all expected files
2. AGENTS.md: `wc -l AGENTS.md` shows 500+ lines
3. AGENTS.md sections: `grep "^## " AGENTS.md` shows at least 8 section headings
4. Frontmatter conventions: AGENTS.md defines all 16 base fields from D-10 (including aliases)
5. Provenance syntax: AGENTS.md includes `[prov:source_id#locator]` format with multiple examples
6. Progressive disclosure: AGENTS.md instructs LLMs to read index first, scan shallow sections
7. Wikilink rules: AGENTS.md specifies first-mention linking, exact title match, no frontmatter wikilinks
8. Worked examples: each page type has a complete example page (entity, concept, source, comparison, overview)
9. Negative constraints: "What Agents Must NOT Do" subsection exists with at least 8 prohibitions
10. Bad/good examples exist for both provenance syntax and wikilink conventions
</verification>

<success_criteria>
- Directory skeleton exists with all 7 directories (sources/, wiki/entities/, wiki/concepts/, wiki/sources/, wiki/comparisons/, wiki/overviews/, schema/templates/)
- wiki/index.md and wiki/log.md exist with valid Dataview-compatible YAML frontmatter
- AGENTS.md sections 1-8 are complete, prescriptive, and self-contained
- Every page type has a fully worked example showing frontmatter + all sections + provenance + wikilinks
- Negative constraints section explicitly tells agents what NOT to do
- Frontmatter validation checklist provides verifiable field rules
- An LLM reading only AGENTS.md can determine: what directories to use, what frontmatter fields to set, what section order to follow, how to write provenance markers, how to create wikilinks, and what NOT to do
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-structure-conventions/01-01-SUMMARY.md`
</output>
