---
phase: 02-page-types-examples-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - schema/templates/entity.md
  - schema/templates/concept.md
  - schema/templates/source-summary.md
  - schema/templates/comparison.md
  - schema/templates/overview.md
autonomous: true
requirements:
  - PAGE-01
  - PAGE-02
  - PAGE-03
  - PAGE-04
  - PAGE-05
  - PAGE-06
  - PAGE-07
must_haves:
  truths:
    - "Each of the five page types has a template file in schema/templates/"
    - "Every template has complete frontmatter with all base fields plus type-specific fields"
    - "Every template uses progressive disclosure section ordering matching AGENTS.md section 4"
    - "Templates are operational skeletons with constraint comments, no example content"
    - "Templates include comments warning against forbidden patterns (wikilinks in frontmatter, display aliases)"
  artifacts:
    - path: "schema/templates/entity.md"
      provides: "Entity page template"
      contains: "type: entity"
    - path: "schema/templates/concept.md"
      provides: "Concept page template"
      contains: "type: concept"
    - path: "schema/templates/source-summary.md"
      provides: "Source summary page template"
      contains: "type: source"
    - path: "schema/templates/comparison.md"
      provides: "Comparison page template"
      contains: "type: comparison"
    - path: "schema/templates/overview.md"
      provides: "Overview page template"
      contains: "type: overview"
  key_links:
    - from: "schema/templates/*.md"
      to: "AGENTS.md sections 4.1-4.5"
      via: "Section ordering and frontmatter fields match spec"
      pattern: "TL;DR"
---

<objective>
Create five operational page type templates in schema/templates/ that agents can copy-fill-done for each page type.

Purpose: Templates are the operational interface between the schema specification (AGENTS.md) and actual wiki pages. They must be copy-paste ready with all required frontmatter fields and section headings. (Addresses review concern: templates must prevent forbidden patterns, not just provide correct structure.)
Output: Five template files in schema/templates/ (entity.md, concept.md, source-summary.md, comparison.md, overview.md)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-page-types-examples-navigation/02-CONTEXT.md
@.planning/phases/02-page-types-examples-navigation/02-RESEARCH.md
@AGENTS.md

<interfaces>
From AGENTS.md section 5 -- Base frontmatter fields (all 16):
id, title, type, status, summary, created_at, updated_at, sources, epistemic_status, tags, domains, supersedes, superseded_by, privacy, aliases

Note on field count: AGENTS.md section 5 defines these fields. Cross-check against the actual section 5 content when implementing -- use schema-derived field names, not a hardcoded count. (Addresses review concern: "16 base fields" may drift from actual schema.)

Source summary additional fields (section 5):
path, url, content_hash, ingested_at, source_type

Section orderings from AGENTS.md section 4:
Entity (4.1): TL;DR -> Key Facts -> Detail -> Related Pages -> Sources
Concept (4.2): TL;DR -> Key Facts -> Detail -> Related Pages -> Sources
Source Summary (4.3): TL;DR -> Key Takeaways -> Extracted Claims -> Notes -> Source Metadata
Comparison (4.4): TL;DR -> Bottom Line -> Comparison Table -> Detailed Comparison -> Sources
Overview (4.5): TL;DR -> Key Facts -> Detail -> Related Pages -> Sources

Forbidden patterns from AGENTS.md section 3:
- No wikilinks in frontmatter (use string IDs only)
- No display aliases in wikilinks (write [[Page Title]] not [[Page Title|alias]])
- Link on first mention only per page body
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create entity, concept, and overview templates</name>
  <files>schema/templates/entity.md, schema/templates/concept.md, schema/templates/overview.md</files>
  <read_first>AGENTS.md (sections 4.1, 4.2, 4.5, and 5 for frontmatter schema, and section 3 for forbidden patterns)</read_first>
  <action>
Create three template files. All three share the same section ordering (TL;DR -> Key Facts -> Detail -> Related Pages -> Sources) but differ in `type` field and constraint comments. Per D-13, templates are operational skeletons: section headings + placeholder frontmatter + brief HTML comments explaining constraints. NO example content.

Each template MUST have YAML frontmatter delimited by triple-dash lines. Read AGENTS.md section 5 to extract the exact field names and include ALL of them. Based on current schema, these are: id (empty), title (empty), type (set to entity/concept/overview respectively), status (set to active), summary (empty quoted string ""), created_at (empty), updated_at (empty), sources (empty list []), epistemic_status (empty), tags (empty list []), domains (empty list []), supersedes (empty), superseded_by (empty), privacy (empty), aliases (empty list []).

After the frontmatter, include these section headings with HTML constraint comments:

- `## TL;DR` with comment: 1 short paragraph or 2-4 bullets. Must be scannable in seconds.
- `## Key Facts` with comment: Compact bullets with inline provenance and epistemic markers. Format: - Claim text [prov:source_id#locator|support_type] [epistemic:: status]
- `## Detail` with comment: Full narrative, synthesis, caveats. Long-form prose goes here.
- `## Related Pages` with comment: Wikilinks to connected pages. Link on first mention only. Use [[Page Title]] format. Do NOT use display aliases like [[Page|Alias]].
- `## Sources` with comment: Human-readable source list with wikilinks to source summary pages. Format: - [[src-YYYY-MM-DD-slug]]: "Title" (date)

Type-specific constraint comments to add in each template after the `## TL;DR` heading:
- **entity.md**: `<!-- Subject is a proper-named thing (person, tool, organization). Summarize what it is and why it matters. -->`
- **concept.md**: `<!-- Subject is an abstract idea, theory, methodology, or framework. Summarize the core idea. -->`
- **overview.md**: `<!-- High-level synthesis across multiple sources and pages. Summarize the landscape. -->`

Add a FORBIDDEN PATTERNS comment block at the top of each template (after frontmatter, before first section). This addresses Codex review concern about preventing agent errors:

```html
<!-- FORBIDDEN PATTERNS (see AGENTS.md section 3):
     - No wikilinks in frontmatter (use plain string IDs in sources[], supersedes, etc.)
     - No display aliases: write [[Page Title]] not [[Page Title|Alias]]
     - Link each page only on first mention in the body
     - No example content in this template -- fill with real content when using -->
```
  </action>
  <verify>
    <automated>for f in schema/templates/entity.md schema/templates/concept.md schema/templates/overview.md; do echo "=== $f ===" && grep -c "^---" "$f" && grep "^type:" "$f" && grep -c "## TL;DR" "$f" && grep -c "FORBIDDEN PATTERNS" "$f" && grep "^id:" "$f" && grep "^epistemic_status:" "$f" && grep "^aliases:" "$f"; done</automated>
  </verify>
  <acceptance_criteria>
    - schema/templates/entity.md contains `type: entity`
    - schema/templates/concept.md contains `type: concept`
    - schema/templates/overview.md contains `type: overview`
    - Each file has exactly 2 lines matching `^---` (frontmatter delimiters)
    - Each file contains all base frontmatter fields from AGENTS.md section 5: id, title, type, status, summary, created_at, updated_at, sources, epistemic_status, tags, domains, supersedes, superseded_by, privacy, aliases
    - Each file has sections: ## TL;DR, ## Key Facts, ## Detail, ## Related Pages, ## Sources
    - Each file contains an HTML comment block with "FORBIDDEN PATTERNS" warning
    - Each file contains HTML comments with constraints but NO example content (no wikilinks, no provenance markers, no factual claims in body)
  </acceptance_criteria>
  <done>Three template files exist with correct frontmatter matching AGENTS.md section 5, section ordering, constraint comments, and forbidden pattern warnings. No example content present.</done>
</task>

<task type="auto">
  <name>Task 2: Create source-summary and comparison templates</name>
  <files>schema/templates/source-summary.md, schema/templates/comparison.md</files>
  <read_first>AGENTS.md (sections 4.3, 4.4, and 5 for additional source fields, and section 3 for forbidden patterns)</read_first>
  <action>
Create two template files with type-specific sections and frontmatter. Per D-13, operational skeletons only.

**source-summary.md** -- Frontmatter includes all base fields PLUS additional source fields (per AGENTS.md section 5). Read section 5 to extract the exact additional field names. Based on current schema, the additional fields are: path (empty), url (empty quoted string ""), content_hash (empty quoted string ""), ingested_at (empty), source_type (empty). All placed after aliases in the frontmatter.

Section ordering (per AGENTS.md 4.3) with constraint comments:
- `## TL;DR` with comment: One summary page per ingested source. Summarize the source's main contribution in 1-3 sentences.
- `## Key Takeaways` with comment: Major findings/arguments from the source. Compact bullets with provenance. Format: - Takeaway [prov:this_source_id#locator|support_type] [epistemic:: sourced]
- `## Extracted Claims` with comment: Specific quotable claims pulled from the source, with precise locators. Format: - "Verbatim or close-paraphrase claim" [prov:this_source_id#locator|direct]
- `## Notes` with comment: Context, commentary, connections to other wiki pages. Wikilinks go here. This is where the source connects to the broader wiki knowledge graph.
- `## Source Metadata` with comment: Human-readable metadata. Fields: Source type, Authors, Published, Path, URL.

**comparison.md** -- Standard base-field frontmatter with `type: comparison`.

Section ordering (per AGENTS.md 4.4) with constraint comments:
- `## TL;DR` with comment: Contrasting two or more subjects. State what is being compared and the key difference in 1-2 sentences.
- `## Bottom Line` with comment: Actionable recommendation or key conclusion from the comparison.
- `## Comparison Table` with comment: Structured side-by-side table. Use markdown table with columns: Dimension | Subject A | Subject B (add more subject columns as needed).
- `## Detailed Comparison` with comment: Prose analysis organized by dimension or theme. Include provenance for factual claims. Format: Claim [prov:source_id#locator|support_type] [epistemic:: status]
- `## Sources` with comment: Human-readable source list with wikilinks to source summary pages.

Both templates MUST include the FORBIDDEN PATTERNS comment block (same as Task 1).
  </action>
  <verify>
    <automated>for f in schema/templates/source-summary.md schema/templates/comparison.md; do echo "=== $f ===" && grep -c "^---" "$f" && grep "^type:" "$f" && grep -c "FORBIDDEN PATTERNS" "$f"; done && grep "source_type:" schema/templates/source-summary.md && grep "content_hash:" schema/templates/source-summary.md && grep "ingested_at:" schema/templates/source-summary.md && grep "## Comparison Table" schema/templates/comparison.md && grep "## Bottom Line" schema/templates/comparison.md && grep "## Key Takeaways" schema/templates/source-summary.md && grep "## Extracted Claims" schema/templates/source-summary.md</automated>
  </verify>
  <acceptance_criteria>
    - schema/templates/source-summary.md contains `type: source`
    - schema/templates/source-summary.md contains additional fields: path, url, content_hash, ingested_at, source_type
    - schema/templates/source-summary.md has sections: ## TL;DR, ## Key Takeaways, ## Extracted Claims, ## Notes, ## Source Metadata
    - schema/templates/comparison.md contains `type: comparison`
    - schema/templates/comparison.md has sections: ## TL;DR, ## Bottom Line, ## Comparison Table, ## Detailed Comparison, ## Sources
    - Both files have exactly 2 lines matching `^---`
    - Both files contain FORBIDDEN PATTERNS comment block
    - Both files contain constraint comments but NO example content
    - source-summary.md's ## Key Takeaways comment includes the combined inline pattern: `[prov:...|support_type] [epistemic:: sourced]`
    - comparison.md's ## Comparison Table comment shows the markdown table format
  </acceptance_criteria>
  <done>Source-summary and comparison templates exist with correct type-specific frontmatter, section ordering per AGENTS.md 4.3/4.4, constraint comments, and forbidden pattern warnings.</done>
</task>

</tasks>

<verification>
All five template files exist in schema/templates/:
- `ls schema/templates/` shows entity.md, concept.md, source-summary.md, comparison.md, overview.md
- Each has valid YAML frontmatter (2 `---` delimiters)
- Each has `type:` matching the template purpose
- Section ordering matches AGENTS.md section 4 per-type specification
- No template contains example content (wikilinks, provenance markers, or factual claims)
- source-summary.md has additional frontmatter fields beyond the base set
- All templates include FORBIDDEN PATTERNS comment block
</verification>

<success_criteria>
Five operational template files exist that an agent can copy-fill-done. Templates match AGENTS.md specifications exactly. Progressive disclosure ordering is correct per page type. Source summary template includes additional frontmatter fields. All templates warn against forbidden patterns.
</success_criteria>

<output>
After completion, create `.planning/phases/02-page-types-examples-navigation/02-01-SUMMARY.md`
</output>
