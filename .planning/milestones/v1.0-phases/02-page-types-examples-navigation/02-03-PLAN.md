---
phase: 02-page-types-examples-navigation
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - wiki/index.md
  - wiki/log.md
  - AGENTS.md
autonomous: true
requirements:
  - EXMP-05
  - INDX-01
  - INDX-02
  - INDX-03
  - LOG-01
  - LOG-02
  - LOG-03
  - EPST-03
  - PAGE-06

must_haves:
  truths:
    - "index.md catalogs all five example pages with wikilinks, summaries, and metadata"
    - "index.md is organized by category sections matching page types"
    - "log.md has strictly formatted entries with ISO timestamps, operation types, and structured bodies"
    - "Log entries use the exact schema defined in AGENTS.md section 12"
    - "Log entries are parseable by the regex pattern: ^## \\[YYYY-MM-DD\\] <operation_type> \\| <description>"
    - "AGENTS.md section 6 documents the mixed inline grammar with rationale"
    - "Epistemic inline syntax is documented alongside provenance syntax"
    - "AGENTS.md additions are narrowly scoped and do not duplicate existing section 6 material"
  artifacts:
    - path: "wiki/index.md"
      provides: "Populated content index"
      contains: "[[Daniel Kahneman]]"
    - path: "wiki/log.md"
      provides: "Populated activity log"
      contains: "## [2026-"
    - path: "AGENTS.md"
      provides: "Updated section 6 with epistemic inline syntax"
      contains: "[epistemic:: sourced]"
  key_links:
    - from: "wiki/index.md"
      to: "all five example pages"
      via: "wikilinks in category sections"
      pattern: "\\[\\[Daniel Kahneman\\]\\].*--"
    - from: "wiki/log.md"
      to: "operational record"
      via: "strictly formatted log entries"
      pattern: "^## \\[2026-04-09\\] (schema|ingest)"
    - from: "AGENTS.md section 6"
      to: "epistemic conventions"
      via: "documented inline syntax"
      pattern: "\\[epistemic:: "
---

<objective>
Populate index.md and log.md with entries for all example pages, and update AGENTS.md section 6 to document the mixed inline grammar (provenance + epistemic syntax).

Purpose: Complete the navigation layer (index + log) and the schema documentation for epistemic markers, making the wiki ready for Phase 3 ingestion. Addresses review concerns: log format is strictly specified with parseable schema, AGENTS.md additions are narrowly scoped to avoid duplication.
Output: Populated index.md, populated log.md, updated AGENTS.md section 6.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-page-types-examples-navigation/02-CONTEXT.md
@.planning/phases/02-page-types-examples-navigation/02-RESEARCH.md
@AGENTS.md
@.planning/phases/02-page-types-examples-navigation/02-02-SUMMARY.md

<interfaces>
<!-- Index entry format (AGENTS.md section 12, D-11): -->
- [[Page Title]] -- <one-line summary> (<epistemic_status>, <updated_at>)

<!-- Index category sections (D-11): -->
## Entities
## Concepts
## Sources
## Comparisons
## Overviews

<!-- STRICT Log entry schema (AGENTS.md section 12) -- addresses HIGH review concern about underspecification:
     
     HEADER LINE (parseable by grep):
       ## [YYYY-MM-DD] <operation_type> | <description>
     
     BODY (free-form but must include):
       - Which pages were affected (wikilinks or filenames)
       - What was done (brief summary)
       - Rationale (why this operation was performed)
     
     REGEX for header: ^## \[\d{4}-\d{2}-\d{2}\] (ingest|query|lint|reflect|update|merge|supersede|archive|schema) \| .+$
     
     Valid operation types (from AGENTS.md section 12):
       ingest, query, lint, reflect, update, merge, supersede, archive
       Also: "schema" for initial setup operations (template/example creation)
     
     Ordering: newest entries at the bottom (append-only) per AGENTS.md section 12
     Parseable with: grep "^## \[" wiki/log.md | tail -5
-->

<!-- Example page IDs and titles for index entries: -->
daniel-kahneman -> "Daniel Kahneman" (entity, epistemic_status: sourced)
cognitive-biases -> "Cognitive Biases" (concept, epistemic_status: mixed)
src-2026-04-09-thinking-fast-and-slow-part1 -> "Kahneman - Thinking, Fast and Slow Part 1" (source, epistemic_status: sourced)
system-1-vs-system-2 -> "System 1 vs System 2" (comparison, epistemic_status: sourced)
decision-making -> "Decision Making" (overview, epistemic_status: mixed)

<!-- AGENTS.md section 6 current content: provenance syntax, locator types, support types, validation rules -->
<!-- New subsection to add: epistemic inline markers, page-vs-claim distinction, mixed grammar rationale -->
<!-- NARROW SCOPE (addresses MEDIUM review concern): Only add what doesn't exist. Do NOT rephrase or duplicate
     existing provenance documentation. Section 6 already covers [prov:] syntax fully. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate index.md and log.md</name>
  <files>wiki/index.md, wiki/log.md</files>
  <read_first>wiki/index.md, wiki/log.md, AGENTS.md (section 12 for format conventions), wiki/entities/daniel-kahneman.md (read frontmatter for epistemic_status and summary), wiki/concepts/cognitive-biases.md (read frontmatter), wiki/sources/src-2026-04-09-thinking-fast-and-slow-part1.md (read frontmatter), wiki/comparisons/system-1-vs-system-2.md (read frontmatter), wiki/overviews/decision-making.md (read frontmatter)</read_first>
  <action>
**Update wiki/index.md:** Replace the placeholder text in each category section with actual entries. Keep the existing frontmatter unchanged. Per D-09, this is a manual curated list (no Dataview queries). Per D-11, each entry uses format: `- [[Page Title]] -- <one-line summary> (<epistemic_status>, <updated_at>)`.

Read the actual `summary` and `epistemic_status` fields from each example page's frontmatter to construct accurate index entries. Do NOT use hardcoded values -- pull them from the actual files created in Plan 02-02.

The index body should contain these sections with entries:

```
## Entities
- [[Daniel Kahneman]] -- <summary from frontmatter> (sourced, 2026-04-09)

## Concepts
- [[Cognitive Biases]] -- <summary from frontmatter> (mixed, 2026-04-09)

## Sources
- [[src-2026-04-09-thinking-fast-and-slow-part1]] -- <summary from frontmatter> (sourced, 2026-04-09)

## Comparisons
- [[System 1 vs System 2]] -- <summary from frontmatter> (sourced, 2026-04-09)

## Overviews
- [[Decision Making]] -- <summary from frontmatter> (mixed, 2026-04-09)
```

**Update wiki/log.md:** Replace the placeholder text with log entries. Keep existing frontmatter unchanged. Per AGENTS.md section 12, newest entries at the bottom (append-only).

STRICT LOG FORMAT (addresses HIGH review concern about underspecification):

Each log entry MUST follow this exact structure:
1. Header line: `## [YYYY-MM-DD] <operation_type> | <description>` (single line, no line break)
2. Blank line
3. Body: What was done, which pages were affected, brief rationale. 1-3 sentences.

The header line MUST be parseable by the regex: `^## \[\d{4}-\d{2}-\d{2}\] (ingest|query|lint|reflect|update|merge|supersede|archive|schema) \| .+$`

Create exactly 2 log entries:

Entry 1:
```
## [2026-04-09] schema | Create page type templates

Created 5 page type templates in schema/templates/: entity.md, concept.md, source-summary.md, comparison.md, overview.md. Templates provide operational skeletons with frontmatter schema and progressive disclosure section ordering per AGENTS.md section 4.
```

Entry 2:
```
## [2026-04-09] schema | Create example pages and populate navigation

Created 5 example pages: Daniel Kahneman (entity), Cognitive Biases (concept), Thinking Fast and Slow Part 1 (source summary), System 1 vs System 2 (comparison), Decision Making (overview). Pages form a connected graph cluster demonstrating cross-references, provenance chains, and epistemic markers. Populated index.md and log.md. Updated AGENTS.md section 6 with epistemic inline syntax documentation.
```

NOTE on log ordering: CONTEXT.md D-16 says "most recent first" but AGENTS.md section 12 says "newest entries at the bottom." Following AGENTS.md as the authoritative spec. The grep command `grep "^## \[" wiki/log.md | tail -5` in section 12 also assumes newest-at-bottom.
  </action>
  <verify>
    <automated>echo "=== Index entries ===" && grep -P "^\- \[\[.+\]\] -- " wiki/index.md | wc -l && echo "=== Index categories ===" && grep "^## " wiki/index.md && echo "=== Index wikilinks ===" && grep -oP "\[\[[^\]]+\]\]" wiki/index.md && echo "=== No placeholders ===" && grep -c "No.*pages yet\|No.*recorded yet" wiki/index.md wiki/log.md || true && echo "=== Log entry count ===" && grep -c "^## \[" wiki/log.md && echo "=== Log format validation ===" && grep -P "^## \[\d{4}-\d{2}-\d{2}\] (ingest|query|lint|reflect|update|merge|supersede|archive|schema) \| " wiki/log.md && echo "=== Frontmatter preserved ===" && grep "^id:" wiki/index.md && grep "^id:" wiki/log.md</automated>
  </verify>
  <acceptance_criteria>
    - wiki/index.md contains exactly 5 entries matching pattern `- [[...]] -- ...`
    - wiki/index.md has category sections: `## Entities`, `## Concepts`, `## Sources`, `## Comparisons`, `## Overviews`
    - wiki/index.md contains wikilinks: `[[Daniel Kahneman]]`, `[[Cognitive Biases]]`, `[[src-2026-04-09-thinking-fast-and-slow-part1]]`, `[[System 1 vs System 2]]`, `[[Decision Making]]`
    - wiki/index.md does NOT contain placeholder text ("No entity pages yet" or similar)
    - wiki/log.md contains exactly 2 entries matching `^## \[2026-`
    - EVERY log header line matches the regex: `^## \[\d{4}-\d{2}-\d{2}\] (ingest|query|lint|reflect|update|merge|supersede|archive|schema) \| .+$`
    - wiki/log.md entries are in chronological order (newest at bottom)
    - wiki/log.md does NOT contain placeholder text ("No operations recorded yet")
    - wiki/index.md frontmatter is preserved (id, type fields unchanged)
    - wiki/log.md frontmatter is preserved (id, type fields unchanged)
    - Each index entry includes epistemic_status and date in parentheses at the end
  </acceptance_criteria>
  <done>index.md lists all 5 example pages organized by category with wikilinks, summaries, and metadata. log.md records operations with strictly formatted, grep-parseable entries following the exact AGENTS.md section 12 schema.</done>
</task>

<task type="auto">
  <name>Task 2: Update AGENTS.md section 6 with epistemic inline syntax documentation</name>
  <files>AGENTS.md</files>
  <read_first>AGENTS.md (section 6 in full -- find the exact line range from "## 6." to "## 7." to understand what already exists and avoid duplication)</read_first>
  <action>
Per D-06, add a new subsection to AGENTS.md section 6 documenting the mixed inline grammar. Insert AFTER the existing "Provenance Validation Rules" subsection and BEFORE section 7 "Progressive Disclosure".

NARROW SCOPE (addresses MEDIUM review concern about AGENTS.md growing large and risking duplication):
- Do NOT re-explain provenance syntax -- section 6 already covers it fully
- Do NOT add lengthy rationale paragraphs -- keep it to the minimum an agent needs
- DO add: epistemic syntax, valid values, combined pattern, page-vs-claim distinction
- Total addition should be ~60-80 lines, not 100+

Add these new subsections to section 6:

### Inline Epistemic Markers

Per-claim epistemic status uses Dataview inline field syntax, separate from provenance markers.

**Syntax:** `[epistemic:: <status>]`

**Valid statuses:**

| Status | Meaning | When to use |
|--------|---------|-------------|
| `sourced` | Directly from a source | Verbatim or close paraphrase with provenance |
| `inferred` | Synthesized from source(s) | Logical conclusion not explicitly stated in any single source |
| `tentative` | Weak or contested evidence | Claim may not hold; flag for review |
| `stale` | Likely outdated | Source has changed, finding superseded, or claim is time-sensitive |

Queryable via Dataview: `TABLE file.name FROM "wiki" FLATTEN file.lists.text as item WHERE contains(item, "[epistemic:: tentative]")`

### Page-Level vs Claim-Level Epistemic Status

- **Page-level:** `epistemic_status` frontmatter field (Section 5). Reflects overall page evidence quality: `sourced`, `mixed`, `tentative`, or `stale`.
- **Claim-level:** Inline `[epistemic:: <status>]` in body text. Applies to individual claims within a page.

A page with `epistemic_status: sourced` may contain individual `[epistemic:: inferred]` claims if the majority is directly sourced. Use `mixed` when the page has a significant proportion of non-sourced claims.

### Mixed Inline Grammar

Two inline syntaxes coexist intentionally in wiki page bodies. Do NOT normalize to a single syntax.

| Syntax | Purpose | Tool |
|--------|---------|------|
| `[prov:source_id#locator\|support_type]` | Traceability | grep, scripts |
| `[epistemic:: status]` | Confidence discovery | Dataview |

**Combined pattern:** `Claim text. [prov:source_id#locator|support_type] [epistemic:: status]`

Not every claim needs both markers. Provenance is omitted when there is no specific source. Epistemic status is recommended on all factual claims.

---

IMPORTANT: Insert this content between the existing "### Provenance Validation Rules" subsection and "## 7. Progressive Disclosure". Do NOT modify any existing content in section 6 -- only ADD the new subsections. Verify section 7 still follows immediately after.
  </action>
  <verify>
    <automated>echo "=== New subsections exist ===" && grep "### Inline Epistemic Markers" AGENTS.md && grep "### Page-Level vs Claim-Level" AGENTS.md && grep "### Mixed Inline Grammar" AGENTS.md && echo "=== Key content ===" && grep "\[epistemic:: sourced\]" AGENTS.md && grep "\[epistemic:: tentative\]" AGENTS.md && grep "Do NOT normalize" AGENTS.md && echo "=== Section ordering preserved ===" && grep -n "## 6\.\|## 7\." AGENTS.md && echo "=== Addition size (should be 60-80 lines) ===" && sed -n '/### Inline Epistemic Markers/,/## 7\./p' AGENTS.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md contains new subsection `### Inline Epistemic Markers` in section 6
    - AGENTS.md contains new subsection `### Page-Level vs Claim-Level Epistemic Status` in section 6
    - AGENTS.md contains new subsection `### Mixed Inline Grammar` in section 6
    - AGENTS.md documents all four valid epistemic statuses: sourced, inferred, tentative, stale (in a table)
    - AGENTS.md contains the combined inline pattern: `Claim text. [prov:source_id#locator|support_type] [epistemic:: status]`
    - AGENTS.md contains explicit "Do NOT normalize" instruction
    - New content is between "### Provenance Validation Rules" and "## 7. Progressive Disclosure"
    - Existing section 6 content (Inline Provenance Syntax, Locator Types, Support Types, etc.) is unchanged
    - Addition is narrowly scoped (~60-80 lines, not 100+)
    - No duplication of existing provenance syntax documentation
  </acceptance_criteria>
  <done>AGENTS.md section 6 documents epistemic inline syntax, page-vs-claim distinction, and mixed grammar design. Additions are narrowly scoped and do not duplicate existing provenance documentation.</done>
</task>

</tasks>

<verification>
1. Index populated: `grep -P "^\- \[\[.+\]\] --" wiki/index.md | wc -l` returns 5
2. Index categories: `grep "^## " wiki/index.md` shows Entities, Concepts, Sources, Comparisons, Overviews
3. Log entries: `grep "^## \[" wiki/log.md` returns exactly 2 entries
4. Log strictly parseable: `grep -P "^## \[\d{4}-\d{2}-\d{2}\] (ingest|query|lint|reflect|update|merge|supersede|archive|schema) \| " wiki/log.md | wc -l` returns 2 (ALL entries match)
5. AGENTS.md updated: `grep -c "epistemic::" AGENTS.md` shows new matches
6. Mixed grammar documented: `grep "Do NOT normalize" AGENTS.md` finds the design instruction
7. AGENTS.md section ordering: `grep -n "## 6\.\|## 7\." AGENTS.md` confirms section 7 follows section 6
</verification>

<success_criteria>
index.md catalogs all 5 example pages by category with consistent entry format. log.md has strictly formatted, regex-parseable operational entries following the exact AGENTS.md section 12 schema. AGENTS.md section 6 documents epistemic inline syntax with narrow scope and no duplication. The wiki is ready for Phase 3 ingestion.
</success_criteria>

<output>
After completion, create `.planning/phases/02-page-types-examples-navigation/02-03-SUMMARY.md`
</output>
