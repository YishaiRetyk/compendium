---
phase: 03-ingestion-provenance-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AGENTS.md
  - wiki/sources/src-2026-04-09-thinking-fast-and-slow-part1.md
autonomous: true
requirements:
  - CMPL-01
  - CMPL-02
  - CMPL-03
  - CMPL-04
  - CMPL-05
  - CMPL-06
  - CMPL-07
  - PROV-05
  - INGST-01
  - INGST-02

must_haves:
  truths:
    - "AGENTS.md section 10 Pass 2 contains adaptive claim granularity rules keyed by source type"
    - "AGENTS.md section 10 Pass 3 contains append-then-synthesize incremental update policy"
    - "AGENTS.md section 10 Pass 0 enum contains book-chapter as the canonical book source type"
    - "The existing src-2026-04-09-thinking-fast-and-slow-part1 source summary uses source_type: book-chapter (normalized from book)"
    - "AGENTS.md section 9 references the incremental update policy from Pass 3"
    - "Any LLM agent reading AGENTS.md can execute the full ingest pipeline without ambiguity about granularity or update strategy"
  artifacts:
    - path: "AGENTS.md"
      provides: "Encoded claim granularity rules (D-01 through D-05) and incremental update policy (D-06 through D-10)"
      contains: "append-then-synthesize"
    - path: "wiki/sources/src-2026-04-09-thinking-fast-and-slow-part1.md"
      provides: "Normalized source_type field (book → book-chapter)"
      contains: "source_type: book-chapter"
  key_links:
    - from: "AGENTS.md section 10 Pass 2"
      to: "AGENTS.md section 10 Pass 0"
      via: "source type classification drives extraction depth"
      pattern: "source.type.*extraction|extraction.*source.type"
    - from: "AGENTS.md section 10 Pass 3"
      to: "AGENTS.md section 9"
      via: "merge pass references structured operations UPDATE"
      pattern: "append-then-synthesize"
---

<objective>
Encode user decisions D-01 through D-10 into AGENTS.md sections 9, 10, and 11.1, making the compiler pipeline fully operational for any LLM agent. Also normalize the existing `book` source_type usage to the canonical `book-chapter`.

Purpose: The pipeline specification in AGENTS.md currently describes WHAT each pass does but lacks the granularity rules (how fine to extract) and update policy (how to merge into existing pages). These two pieces are the missing operational detail that prevents an agent from running the pipeline unambiguously. Additionally, an inconsistency exists between the current source_type enum and the example source page that uses `book` — this plan locks the single canonical value `book-chapter`.

Output: Updated AGENTS.md with claim granularity rules in Pass 2, incremental update policy in Pass 3, `book-chapter` added to Pass 0 type enum as the single canonical value, cross-reference from section 9, and normalized source_type in the existing example source summary page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ingestion-provenance-pipeline/03-CONTEXT.md
@.planning/phases/03-ingestion-provenance-pipeline/03-RESEARCH.md
@.planning/phases/03-ingestion-provenance-pipeline/03-REVIEWS.md

<interfaces>
<!-- Current AGENTS.md structure relevant to this plan -->
<!-- IMPORTANT: Patches MUST target sections by header, not by line number. -->
<!-- AGENTS.md may drift; locate edits via section heading strings. -->

Section 9 — Titled something like "## 9. Structured Operations" (UPDATE, MERGE, SUPERSEDE, ARCHIVE).
Currently has no reference to incremental update policy. D-10 requires adding a cross-reference to Pass 3 policy at the end of the UPDATE operation definition.

Section 10 — Titled something like "## 10. Pipeline passes" or "## 10. Compiler Pipeline".
Inside that section, Pass 0 (Classify) defines the source type enum. Current enum does NOT include any book type.

Section 10 Pass 2 (Extract) — currently says to apply type-appropriate extraction but gives no granularity rules.
D-01 through D-05 add the adaptive granularity specification here.

Section 10 Pass 3 (Merge) — currently says "UPDATE existing pages" but gives no policy for HOW to update.
D-06 through D-10 add the append-then-synthesize policy here.

Section 11.1 — Titled something like "## 11.1 Ingest Workflow". Step 5 is the Extract step, step 6 is the Merge step.

Existing source summary file that must be normalized:
wiki/sources/src-2026-04-09-thinking-fast-and-slow-part1.md — currently contains `source_type: book` in its frontmatter.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Encode claim granularity rules, normalize book-chapter source type, and add granularity cross-reference in AGENTS.md</name>
  <files>AGENTS.md, wiki/sources/src-2026-04-09-thinking-fast-and-slow-part1.md</files>
  <read_first>
    - AGENTS.md (full file — executor must see current state; locate sections by heading strings, NOT line numbers)
    - wiki/sources/src-2026-04-09-thinking-fast-and-slow-part1.md (to confirm current source_type value and normalize it)
    - .planning/phases/03-ingestion-provenance-pipeline/03-CONTEXT.md (decisions D-01 through D-05)
    - .planning/phases/03-ingestion-provenance-pipeline/03-REVIEWS.md (the Codex concerns about brittleness and book vs book-chapter half-settlement)
  </read_first>
  <action>
Patch AGENTS.md by **locating sections via their heading strings, never by line number or by adjacent literal prose** (the file may have drifted since planning). Use grep/search to find each heading, then edit within that section.

**1. Section 10, Pass 0 (Classify) — Normalize to `book-chapter` as the single canonical book source type:**

Locate the Pass 0 subsection inside section 10 (search for a heading like "Pass 0" or "Classify" inside the "## 10." section). Find the line enumerating source types (it currently lists things like "article, paper, transcript, journal entry, data file, image-heavy").

Update that enumeration to include `book-chapter` as a canonical type:
- Add `book-chapter` to the enumeration (alphabetical insertion is fine, e.g., `article, paper, book-chapter, transcript, journal entry, data file, image-heavy`).
- Do NOT add `book` as a separate value. The canonical type is `book-chapter`.

Immediately after the types enumeration line, add the following note (exact text, in place, preserving surrounding Markdown context):

```markdown
- **Note:** `book-chapter` is the canonical source type for book content. Full books MUST be ingested as a sequence of `book-chapter` sources (one per chapter or coherent section). An older source summary (src-2026-04-09-thinking-fast-and-slow-part1) previously used `source_type: book`; that value is being normalized to `book-chapter` in this plan. Agents MUST use `book-chapter` going forward; `book` is no longer accepted.
```

**2. Section 10, Pass 2 (Extract) — Add claim granularity rules (per D-01 through D-05):**

Locate the Pass 2 subsection inside section 10 (search for "Pass 2" and "Extract"). After the existing content describing what the Extract pass does, append a new subsection (use `####` heading depth; adjust to match the file's existing heading conventions if they differ):

```markdown
#### Claim Granularity Rules

Source classification (Pass 0) drives extraction depth. The guiding heuristic: **"the smallest unit that preserves meaningful provenance without making the page unreadable."**

| Source Type | Default Granularity | Guidance |
|-------------|-------------------|----------|
| article, paper, report, technical doc | Atomic claims | One provenance marker per distinct assertion. Split when a paragraph contains multiple independently important assertions. |
| book-chapter, essay | Atomic for factual/conceptual claims; paragraph-level for broader interpretive passages | Important factual claims get individual provenance. Interpretive or argumentative passages that form a single coherent point stay grouped. |
| transcript, meeting notes, journal entry | Paragraph-level or utterance-level clusters | Group by natural conversation turns or reflection units. Individual sentences rarely stand alone as claims. |
| image-heavy, mixed media | Tied to specific image, caption, or observation | Each image or visual element that contributes a distinct claim gets its own provenance marker referencing the image locator. |

**Bias toward atomic:** Across all source types, prefer atomic granularity for durable factual and conceptual claims. The split/group decision:
- **Split** when a paragraph contains multiple independently important assertions that future readers might cite separately.
- **Keep grouped** when a passage is only useful as one bundled observation and splitting would lose context.

**Duplication check before editing:** Before inserting this subsection, search Pass 2 for any existing language about granularity or extraction depth. If overlapping content already exists, replace it with the canonical block above rather than creating a duplicate. Preserve any non-overlapping text.
```

**3. Section 11.1 step 5 — Add granularity cross-reference (per D-05):**

Locate step 5 in the Ingest Workflow under section 11.1 (search for `5.` and the word "Extract" near it). Update the step to cross-reference the new granularity rules. The updated step MUST mention "claim granularity rules from Section 10 Pass 2" and reference the source type classified in step 3. Example edit — replace the existing step 5 text with something functionally equivalent to:

```markdown
5. **Extract** (Pipeline Pass 2): Extract claims with provenance locators, applying the claim granularity rules from Section 10 Pass 2 based on the source type classified in step 3. Create the source summary page under `wiki/sources/`.
```

Preserve any additional detail the current step 5 text contains (e.g., specific path conventions) and merge it with the cross-reference.

**4. Normalize the existing example source summary — `wiki/sources/src-2026-04-09-thinking-fast-and-slow-part1.md`:**

Open the file and locate the frontmatter line `source_type: book` (or whatever current value). Change it to:
```yaml
source_type: book-chapter
```
If the file already uses `book-chapter`, leave it as-is. If it uses `book`, change it to `book-chapter`. Do not modify any other frontmatter or body content.
  </action>
  <verify>
    <automated>grep -n '^## 10' AGENTS.md && grep -c 'book-chapter' AGENTS.md && ! grep -E '^\s*-?\s*\*?\*?Types:\*?\*?.*\bbook\b[^-]' AGENTS.md && grep -c 'Claim Granularity Rules' AGENTS.md && grep -c 'smallest unit that preserves meaningful provenance' AGENTS.md && grep 'claim granularity rules from Section 10' AGENTS.md && grep 'source_type: book-chapter' wiki/sources/src-2026-04-09-thinking-fast-and-slow-part1.md && ! grep 'source_type: book$' wiki/sources/src-2026-04-09-thinking-fast-and-slow-part1.md</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md contains the string `book-chapter` in the Pass 0 source type enumeration
    - AGENTS.md does NOT list `book` as a standalone source type (only `book-chapter` is canonical)
    - AGENTS.md contains `#### Claim Granularity Rules` (or equivalently-depthed heading) subsection in Pass 2
    - AGENTS.md contains the verbatim heuristic: `the smallest unit that preserves meaningful provenance without making the page unreadable`
    - AGENTS.md contains a 4-row granularity table with columns Source Type, Default Granularity, Guidance
    - AGENTS.md contains `Bias toward atomic:` text
    - AGENTS.md section 11.1 step 5 contains the string `claim granularity rules from Section 10 Pass 2`
    - `wiki/sources/src-2026-04-09-thinking-fast-and-slow-part1.md` contains `source_type: book-chapter` and does NOT contain `source_type: book` (bare)
    - All existing content in sections 10 and 11.1 that is NOT about granularity/source-type is preserved (no deletions)
    - No edit relied on line numbers; all edits were located via section/heading search
  </acceptance_criteria>
  <done>Pass 0 includes book-chapter type (book normalized away), Pass 2 has granularity rules table and heuristic, workflow step 5 cross-references granularity rules, existing example source file normalized to book-chapter.</done>
</task>

<task type="auto">
  <name>Task 2: Encode incremental update policy in AGENTS.md sections 9, 10, and 11.1 (section-header-based patching, softened stale wording)</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md (re-read after Task 1 modifications — locate sections by heading strings, not line numbers)
    - .planning/phases/03-ingestion-provenance-pipeline/03-CONTEXT.md (decisions D-06 through D-10)
    - .planning/phases/03-ingestion-provenance-pipeline/03-REVIEWS.md (Codex concern: stale-marking must not front-run Phase 5)
  </read_first>
  <action>
Patch AGENTS.md by **locating sections via heading strings, never by line number**. Use grep/search to find each section heading, then edit within.

**1. Section 10, Pass 3 (Merge) — Add incremental update policy (per D-06 through D-09):**

Locate the Pass 3 subsection inside section 10 (search for "Pass 3" and "Merge"). After the existing content describing what the Merge pass does, append a new subsection. IMPORTANT: the supersede language MUST be softened so it does not front-run Phase 5's formal contradiction/staleness workflows. Use exactly this block (adjust heading depth only to match the file's conventions):

```markdown
#### Incremental Update Policy: Append-Then-Synthesize

The default policy for living wiki pages (entities, concepts, overviews, comparisons):

1. **Append in the detail layer:** Add new claims into the appropriate detail sections, preserving all existing material. Never silently delete existing claims. Insert new claims at the end of the relevant section with their provenance markers.
2. **Mark superseded or stale claims per current schema conventions:** When new information contradicts or replaces an existing claim, mark the old claim as superseded or stale using the epistemic and provenance syntax currently documented in the schema (see Section 6), and add a short note pointing to the superseding claim. Never remove the old claim — the provenance trail must remain visible. Phase 5 will formalize the exact contradiction and staleness semantics; until then, follow current schema conventions and keep the old claim visible.
3. **Re-synthesize the summary layer:** After appending new detail, rewrite the TL;DR and Key Facts sections so they reflect the complete current state of the page — all claims, old and new. This is the only place where rewriting is expected on every update.
4. **Record framing shifts:** If new material fundamentally changes a page's framing or interpretation, record the shift in a decision/reflection entry (see Section 11.4) rather than hiding it inside prose edits.

**Exceptions:**
- **Logs and source summary pages:** Strict append-only. These are records, not living synthesis. Never rewrite existing log entries or source summary content.
- **Full section rewrite:** Reserved for exceptional cases only — severe page drift, extensive duplication, or fundamentally broken earlier structure. When performed, log the rationale as a decision record.

**Mantra:** "Append in the detail layer, synthesize in the summary layer, supersede explicitly when needed."
```

**Duplication check before inserting:** Search Pass 3 for existing language about "append", "synthesize", "update policy", or "preserve". If overlapping content already exists, replace it with this canonical block rather than duplicating. Preserve any non-overlapping Pass 3 content.

**2. Section 9 — Add cross-reference to Pass 3 update policy (per D-10):**

Locate the UPDATE operation definition inside section 9 (search for the UPDATE heading or the word `UPDATE` as an operation name). At the end of the UPDATE operation definition (after its final bullet/step), append:

```markdown
For the full incremental update policy governing how new claims integrate with existing content during ingestion, see Section 10 Pass 3 (Append-Then-Synthesize).
```

**3. Section 11.1 step 6 — Add update policy cross-reference (per D-10):**

Locate step 6 in the Ingest Workflow under section 11.1 (search for `6.` and the word "Merge" near it). Update the step to cross-reference the new update policy. The updated step MUST mention `append-then-synthesize policy (Section 10 Pass 3)`. Example — replace step 6 with something functionally equivalent to:

```markdown
6. **Merge** (Pipeline Pass 3): Update or create entity/concept/overview pages using UPDATE operations (Section 9) and the append-then-synthesize policy (Section 10 Pass 3). Generate wikilinks on first mention. MERGE pages if the source reveals duplicates.
```

Preserve any additional detail currently in step 6.
  </action>
  <verify>
    <automated>grep -c 'Append-Then-Synthesize' AGENTS.md && grep -c 'append-then-synthesize' AGENTS.md && grep 'Section 10 Pass 3.*Append-Then-Synthesize' AGENTS.md && grep 'append-then-synthesize policy' AGENTS.md && grep -c 'per current schema conventions' AGENTS.md && grep 'Phase 5 will formalize' AGENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md contains `#### Incremental Update Policy: Append-Then-Synthesize` (or equivalently-depthed heading) subsection in Pass 3
    - AGENTS.md contains the 4-step update policy (Append, Mark superseded/stale, Re-synthesize, Record framing shifts)
    - AGENTS.md supersede text contains `per current schema conventions` and `Phase 5 will formalize` (softened wording, does not front-run Phase 5)
    - AGENTS.md contains the exceptions block mentioning `Logs and source summary pages` and `Full section rewrite`
    - AGENTS.md contains the verbatim mantra: `Append in the detail layer, synthesize in the summary layer, supersede explicitly when needed.`
    - AGENTS.md section 9 UPDATE definition contains `Section 10 Pass 3 (Append-Then-Synthesize)`
    - AGENTS.md section 11.1 step 6 contains `append-then-synthesize policy (Section 10 Pass 3)`
    - All existing content in sections 9, 10, and 11.1 that is NOT about update policy is preserved (no deletions)
    - No edit relied on line numbers; all edits were located via section/heading search
  </acceptance_criteria>
  <done>Pass 3 has incremental update policy with 4 steps and softened stale wording + exceptions, section 9 UPDATE cross-references it, workflow step 6 cross-references it.</done>
</task>

</tasks>

<verification>
- `grep -c 'book-chapter' AGENTS.md` returns >= 2
- `grep -c 'Claim Granularity Rules' AGENTS.md` returns 1
- `grep -c 'Append-Then-Synthesize' AGENTS.md` returns >= 2 (header + cross-ref)
- `grep -c 'append-then-synthesize' AGENTS.md` returns >= 3 (header + section 9 ref + section 11.1 ref)
- `grep 'per current schema conventions' AGENTS.md` matches (softened wording confirmed)
- `grep 'source_type: book-chapter' wiki/sources/src-2026-04-09-thinking-fast-and-slow-part1.md` matches
- `! grep 'source_type: book$' wiki/sources/src-2026-04-09-thinking-fast-and-slow-part1.md` (no bare book)
- All existing section headers (`## 1` through `## 16` or equivalent) still present
</verification>

<success_criteria>
AGENTS.md sections 9, 10, and 11.1 contain all operational detail from decisions D-01 through D-10. An LLM agent reading only AGENTS.md can determine: what source types exist (with `book-chapter` as the single canonical book type), how fine to extract claims for each type, and how to update existing wiki pages without destroying content. The existing example source summary is normalized to the canonical `book-chapter`. Supersede/stale wording is softened so it does not pre-commit Phase 5 semantics. All edits are located by section/heading, never by line number.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ingestion-provenance-pipeline/03-01-SUMMARY.md`
</output>
