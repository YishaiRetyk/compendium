---
phase: 04-query-structured-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AGENTS.md
  - schema/templates/source-summary.md
  - wiki/sources/src-2026-04-09-thinking-fast-and-slow-part1.md
  - wiki/sources/src-2026-04-10-kahneman-prospect-theory.md
  - wiki/sources/src-2026-04-10-personal-decision-journal.md
autonomous: true
requirements:
  - QURY-03

must_haves:
  truths:
    - "Source summary pages carry compilation_status, compiled_against_hash, and compiled_targets fields in frontmatter"
    - "AGENTS.md section 5 documents the three new compilation tracking fields with explicit state transition rules"
    - "AGENTS.md section 11.1 ingest workflow sets compilation status fields after merge pass"
    - "Existing source pages have compilation_status: compiled with correct compiled_targets derived from log.md"
  artifacts:
    - path: "AGENTS.md"
      provides: "Compilation status field schema (section 5) with transition rules and retroactive ingest workflow update (section 11.1)"
      contains: "compilation_status"
    - path: "schema/templates/source-summary.md"
      provides: "Updated source summary template with compilation tracking fields"
      contains: "compilation_status"
    - path: "wiki/sources/src-2026-04-09-thinking-fast-and-slow-part1.md"
      provides: "Backfilled compilation tracking fields on existing source"
      contains: "compilation_status: compiled"
    - path: "wiki/sources/src-2026-04-10-kahneman-prospect-theory.md"
      provides: "Backfilled compilation tracking fields on existing source"
      contains: "compilation_status: compiled"
    - path: "wiki/sources/src-2026-04-10-personal-decision-journal.md"
      provides: "Backfilled compilation tracking fields on existing source"
      contains: "compilation_status: compiled"
  key_links:
    - from: "AGENTS.md section 5"
      to: "schema/templates/source-summary.md"
      via: "Template mirrors documented schema"
      pattern: "compilation_status.*pending.*partial.*compiled.*stale"
    - from: "AGENTS.md section 11.1"
      to: "AGENTS.md section 5"
      via: "Ingest workflow references compilation status fields"
      pattern: "compilation_status"
---

<objective>
Add compilation status tracking infrastructure to AGENTS.md and all source summary pages, with explicit state transition rules.

Purpose: Delta compilation (QURY-03) requires source summary pages to carry fields that indicate whether their claims have been fully compiled into topic pages. This plan adds the schema definition with deterministic transition semantics, updates the ingest workflow to set these fields, updates the template, and backfills existing source pages. Addresses review concern: compilation_status transition rules must be explicit to prevent downstream drift.
Output: AGENTS.md sections 5 and 11.1 updated; source summary template updated; 3 existing source pages backfilled with compilation tracking fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-query-structured-operations/04-CONTEXT.md
@.planning/phases/04-query-structured-operations/04-RESEARCH.md

<interfaces>
<!-- Current source summary template frontmatter fields (from schema/templates/source-summary.md): -->
```yaml
path: sources/YYYY/YYYY-MM/YYYY-MM-DD-slug/source.md
url: "https://..."
content_hash: "sha256:abc123..."
ingested_at: YYYY-MM-DD
source_type: article|paper|transcript|journal|data|image
```

<!-- Current AGENTS.md section 5 source summary additional fields (lines 521-531): -->
```yaml
### Source Summary Additional Fields

Source summary pages (`type: source`) include these additional frontmatter fields:

path: sources/YYYY/YYYY-MM/YYYY-MM-DD-slug/source.md
url: "https://..."
content_hash: "sha256:abc123..."
ingested_at: YYYY-MM-DD
source_type: article|paper|transcript|journal|data|image
```

<!-- Current AGENTS.md section 11.1 ingest workflow step 6 (line 938): -->
```
6. **Merge** (Pipeline Pass 3): Update or create entity/concept/overview pages using UPDATE operations (Section 9) and the append-then-synthesize policy (Section 10 Pass 3). Generate wikilinks on first mention. MERGE pages if the source reveals duplicates.
```

<!-- Existing source page frontmatter example (src-2026-04-10-kahneman-prospect-theory.md): -->
```yaml
content_hash: "sha256:3dc81e24f5873bfc3d010d52ba63d754f3f0eae090e0d0671784b4704bcd2b3c"
ingested_at: 2026-04-10
source_type: article
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add compilation tracking fields with state transition rules to AGENTS.md sections 5 and 11.1</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md (lines 473-548 for section 5, lines 918-948 for section 11.1)
    - .planning/phases/04-query-structured-operations/04-CONTEXT.md (D-06 through D-11)
    - wiki/log.md (to derive compiled_targets for existing sources)
  </read_first>
  <action>
**Section 5 update** (after the existing source summary additional fields block, around line 531):

Add a new subsection "### Compilation Tracking Fields (Source Summary Pages)" after the existing source summary fields. Include these three new fields in the YAML code block that already shows source-specific fields, and add documentation with explicit transition rules.

Add to the existing source summary YAML block:
```yaml
# Compilation tracking
compilation_status: pending     # pending | partial | compiled | stale
compiled_against_hash: ""       # SHA-256 of source content at last compilation
compiled_targets: []            # Wiki page IDs that received compiled claims
```

Add a field documentation table after the block:

| Field | Type | Description |
|-------|------|-------------|
| `compilation_status` | enum | Compilation lifecycle: `pending` (new, uncompiled), `partial` (some claims merged), `compiled` (all claims merged into topic pages), `stale` (source content changed since last compilation) |
| `compiled_against_hash` | string | SHA-256 hash of source content at time of last compilation. Copied from `content_hash` when compilation completes. When `content_hash` changes on re-ingest and no longer matches `compiled_against_hash`, status resets to `stale`. |
| `compiled_targets` | list | YAML list of wiki page IDs (the `id` field value, not file paths) that received claims from this source during compilation. Example: `[prospect-theory, loss-aversion, daniel-kahneman]` |

**Addresses review concern: explicit state transition rules.** Add immediately after the field table:

```markdown
#### Compilation Status Transition Rules

The following transitions are the ONLY valid state changes. Any other transition is a bug.

| From | To | Trigger | Who Sets It |
|------|----|---------|-------------|
| (new source) | `pending` | Source ingested, before merge pass | Ingest workflow step 5 (extract) |
| `pending` | `compiled` | All extracted claims merged into topic pages | Ingest workflow step 6a |
| `pending` | `partial` | Some claims merged, others deferred | Ingest workflow step 6a |
| `partial` | `compiled` | Remaining claims compiled (via query delta or manual) | Query workflow step 6 or follow-up ingest |
| `compiled` | `stale` | `content_hash` changed on re-ingest (no longer matches `compiled_against_hash`) | Ingest workflow on re-ingest detection |
| `stale` | `compiled` | Re-compilation completed against new content | Query workflow step 6 or follow-up ingest |
| `stale` | `partial` | Partial re-compilation completed | Query workflow step 6 |

**Invariants:**
- `compiled_against_hash` is ALWAYS equal to `content_hash` when `compilation_status` is `compiled`.
- `compiled_against_hash` differs from `content_hash` when `compilation_status` is `stale`.
- `compiled_targets` is empty ONLY when `compilation_status` is `pending`.
- Pages missing `compilation_status` (pre-Phase-4 legacy) are treated as `compiled` by tooling.
```

Add to the Frontmatter Validation Checklist (the numbered list ending around line 547), a new item 12:
```
12. For `type: source` pages: `compilation_status` is one of: `pending`, `partial`, `compiled`, `stale`
```

**Section 11.1 update** (ingest workflow step 6, line 938):

Replace step 6 with an expanded version that includes compilation status setting as a sub-step:

```
6. **Merge** (Pipeline Pass 3): Update or create entity/concept/overview pages using UPDATE operations (Section 9) and the append-then-synthesize policy (Section 10 Pass 3). Generate wikilinks on first mention. MERGE pages if the source reveals duplicates.
   - 6a. After merge is complete, update the source summary page's compilation tracking fields:
     - Set `compilation_status` to `compiled` if all extracted claims were merged into topic pages, or `partial` if some claims were deferred.
     - Set `compiled_against_hash` to the current `content_hash` value.
     - Set `compiled_targets` to the list of wiki page IDs that received claims from this source (page IDs only, not paths).
```

Per D-11 (locked decision): this is a retroactive addition to the ingest spec. Per research pitfall 2: this is added as a sub-step (6a) within the existing merge step, NOT as a new top-level step, to avoid renumbering.
  </action>
  <verify>
    <automated>grep -c "compilation_status" AGENTS.md | grep -q "[3-9]" && grep -q "Transition Rules" AGENTS.md && echo "PASS: compilation_status with transition rules in AGENTS.md" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md contains the string `compilation_status: pending`
    - AGENTS.md contains `compiled_against_hash`
    - AGENTS.md contains `compiled_targets`
    - AGENTS.md section 5 contains `pending | partial | compiled | stale`
    - AGENTS.md contains `#### Compilation Status Transition Rules` heading
    - AGENTS.md contains the transition table with columns `From | To | Trigger | Who Sets It`
    - AGENTS.md contains `compiled_against_hash` is ALWAYS equal to `content_hash` when `compilation_status` is `compiled` in the invariants
    - AGENTS.md contains "Pages missing `compilation_status` (pre-Phase-4 legacy) are treated as `compiled`"
    - AGENTS.md section 11.1 step 6 contains `6a.` sub-step with `compilation_status`
    - AGENTS.md frontmatter validation checklist contains item 12 referencing `compilation_status`
  </acceptance_criteria>
  <done>AGENTS.md section 5 documents three compilation tracking fields with enum values, explicit state transition table, and invariants; section 11.1 step 6 includes sub-step 6a that sets these fields after merge; validation checklist has a new item 12</done>
</task>

<task type="auto">
  <name>Task 2: Backfill compilation tracking fields on source template and existing source pages</name>
  <files>schema/templates/source-summary.md, wiki/sources/src-2026-04-09-thinking-fast-and-slow-part1.md, wiki/sources/src-2026-04-10-kahneman-prospect-theory.md, wiki/sources/src-2026-04-10-personal-decision-journal.md</files>
  <read_first>
    - schema/templates/source-summary.md
    - wiki/sources/src-2026-04-09-thinking-fast-and-slow-part1.md
    - wiki/sources/src-2026-04-10-kahneman-prospect-theory.md
    - wiki/sources/src-2026-04-10-personal-decision-journal.md
    - wiki/log.md (to derive compiled_targets for each source)
    - AGENTS.md (the section 5 update from Task 1 -- verify fields match)
  </read_first>
  <action>
**Template update** (schema/templates/source-summary.md):

Add three new fields to the frontmatter block, after `source_type:`:
```yaml
compilation_status:
compiled_against_hash: ""
compiled_targets: []
```
These are empty/default values following the Phase 2 decision (D-02 from phase 02: templates use empty/default values rather than placeholder text).

**Existing source pages backfill:**

For each of the 3 existing source pages, add the three fields after `source_type:` in frontmatter. All three were fully ingested with merge passes in Phase 3, so per research open question 1, set `compilation_status: compiled` and `compiled_against_hash` to their current `content_hash` value.

**src-2026-04-09-thinking-fast-and-slow-part1.md** -- add after `source_type: book-chapter`:
```yaml
compilation_status: compiled
compiled_against_hash: "<copy the exact content_hash value from this file>"
compiled_targets:
  - daniel-kahneman
  - cognitive-biases
  - system-1-vs-system-2
  - decision-making
```
(Targets derived from log.md 2026-04-09 schema entry and the example pages created in Phase 2 that reference this source.)

**src-2026-04-10-kahneman-prospect-theory.md** -- add after `source_type: article`:
```yaml
compilation_status: compiled
compiled_against_hash: "sha256:3dc81e24f5873bfc3d010d52ba63d754f3f0eae090e0d0671784b4704bcd2b3c"
compiled_targets:
  - daniel-kahneman
  - cognitive-biases
  - system-1-vs-system-2
  - decision-making
  - prospect-theory
  - loss-aversion
```
(Targets derived from log.md 2026-04-10 ingest entry which lists all UPDATED and CREATED pages.)

**src-2026-04-10-personal-decision-journal.md** -- add after `source_type: journal entry`:
```yaml
compilation_status: compiled
compiled_against_hash: "<copy the exact content_hash value from this file>"
compiled_targets:
  - personal-decision-patterns
  - decision-making
```
(Targets derived from log.md 2026-04-10 journal ingest entry: CREATED personal-decision-patterns.md, UPDATED decision-making.md link-only.)

IMPORTANT: For `compiled_against_hash`, copy the EXACT value from each file's existing `content_hash` field. Do not recompute.
  </action>
  <verify>
    <automated>for f in schema/templates/source-summary.md wiki/sources/src-2026-04-09-thinking-fast-and-slow-part1.md wiki/sources/src-2026-04-10-kahneman-prospect-theory.md wiki/sources/src-2026-04-10-personal-decision-journal.md; do grep -q "compilation_status" "$f" && echo "PASS: $f" || echo "FAIL: $f"; done</automated>
  </verify>
  <acceptance_criteria>
    - schema/templates/source-summary.md contains `compilation_status:` in frontmatter
    - schema/templates/source-summary.md contains `compiled_against_hash: ""`
    - schema/templates/source-summary.md contains `compiled_targets: []`
    - wiki/sources/src-2026-04-09-thinking-fast-and-slow-part1.md contains `compilation_status: compiled`
    - wiki/sources/src-2026-04-10-kahneman-prospect-theory.md contains `compilation_status: compiled`
    - wiki/sources/src-2026-04-10-kahneman-prospect-theory.md contains `compiled_against_hash: "sha256:3dc81e24`
    - wiki/sources/src-2026-04-10-personal-decision-journal.md contains `compilation_status: compiled`
    - All 3 existing source pages have `compiled_targets:` as YAML lists with page IDs
  </acceptance_criteria>
  <done>Source summary template has three empty compilation tracking fields; all 3 existing source pages have compilation_status: compiled with correct compiled_against_hash and compiled_targets derived from log.md</done>
</task>

</tasks>

<verification>
- grep "compilation_status" AGENTS.md returns matches in section 5, section 11.1, and validation checklist
- grep "Transition Rules" AGENTS.md confirms transition table is present
- All 4 source files (template + 3 existing) contain compilation_status in frontmatter
- python3 -c "import yaml; [yaml.safe_load(open(f).read().split('---')[1]) for f in ['wiki/sources/src-2026-04-09-thinking-fast-and-slow-part1.md','wiki/sources/src-2026-04-10-kahneman-prospect-theory.md','wiki/sources/src-2026-04-10-personal-decision-journal.md']]" parses without error
</verification>

<success_criteria>
- AGENTS.md section 5 documents compilation_status, compiled_against_hash, compiled_targets with enum values, explicit state transition table, and invariants
- AGENTS.md section 11.1 step 6 includes sub-step 6a setting compilation fields after merge
- Source summary template includes three new empty fields
- All 3 existing source pages have compilation_status: compiled with accurate targets
</success_criteria>

<output>
After completion, create `.planning/phases/04-query-structured-operations/04-01-SUMMARY.md`
</output>
