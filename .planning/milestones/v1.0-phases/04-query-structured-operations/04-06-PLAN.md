---
phase: 04-query-structured-operations
plan: 06
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
  - 04-03
  - 04-04
  - 04-05
files_modified:
  - wiki/index.md
  - wiki/log.md
autonomous: false
requirements:
  - QURY-01

must_haves:
  truths:
    - "A question asked against the wiki produces a cited answer referencing specific wiki pages and sources"
    - "The query workflow executes end-to-end: search -> read -> synthesize -> write-back decision -> log"
    - "bin/search.sh successfully finds relevant pages for the test question"
    - "bin/validate-op.sh validates any operations proposed during write-back"
    - "The query log entry in wiki/log.md uses the structured format from section 11.2"
    - "A privacy-sensitive query scenario is tested to verify inheritance rules work"
  artifacts:
    - path: "wiki/log.md"
      provides: "Query log entries demonstrating the full workflow including write-back and no-write-back paths"
      contains: "query |"
  key_links:
    - from: "AGENTS.md section 11.2"
      to: "wiki/log.md"
      via: "Query workflow produces structured log entry"
      pattern: "query.*|"
    - from: "bin/search.sh"
      to: "wiki/index.md"
      via: "Search helper finds relevant pages from index"
      pattern: "wiki/"
---

<objective>
End-to-end validation of the complete query workflow against the existing wiki, with a validation matrix covering write-back, no-write-back, and privacy-sensitive scenarios.

Purpose: Proves that all Phase 4 deliverables work together: the spec (AGENTS.md), the search helper (bin/search.sh), the validator (bin/validate-op.sh), and the compilation tracking fields. Multiple test scenarios verify different paths through the workflow. Addresses review concern: single query is insufficient -- a small validation matrix covering key paths provides confidence. Human reviews the result.
Output: Query log entries in wiki/log.md; possible wiki page updates; human sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-query-structured-operations/04-CONTEXT.md
@.planning/phases/04-query-structured-operations/04-01-SUMMARY.md
@.planning/phases/04-query-structured-operations/04-02-SUMMARY.md
@.planning/phases/04-query-structured-operations/04-03-SUMMARY.md
@.planning/phases/04-query-structured-operations/04-04-SUMMARY.md
@.planning/phases/04-query-structured-operations/04-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute validation matrix -- three query scenarios covering different workflow paths</name>
  <files>wiki/log.md, wiki/index.md</files>
  <read_first>
    - AGENTS.md (section 11.2 -- the rewritten query workflow from Plan 04)
    - wiki/index.md (search starting point)
    - bin/search.sh (verify it works)
    - bin/validate-op.sh (verify it is available)
    - wiki/log.md (current state, append new entries)
    - wiki/overviews/personal-decision-patterns.md (local_only page for privacy scenario)
  </read_first>
  <action>
Execute the query workflow defined in AGENTS.md section 11.2 for three scenarios that together cover the critical paths:

**Scenario A: Standard query with likely NO-WRITE-BACK (pure lookup path)**

Question: "How does prospect theory explain why people make irrational financial decisions?"

Follow all 10 steps. This question is well-covered by existing wiki pages (prospect-theory.md, loss-aversion.md, cognitive-biases.md). The expected write-back decision is NO-WRITE-BACK because the answer primarily restates existing pages.

Expected log entry:
```
## [YYYY-MM-DD] query | How does prospect theory explain irrational financial decisions?

answer: <one-line summary>
write_back: NO-WRITE-BACK: reformulated restatement of existing prospect-theory and loss-aversion pages
delta_compiled: none
pages_affected: none
```

**Scenario B: Query that triggers WRITE-BACK (novel synthesis path)**

Question: "Compare the different types of cognitive biases in the wiki -- which are heuristic-origin and which are loss-aversion-origin?"

Follow all 10 steps. This question asks for a synthesis that may produce a connection not explicitly captured. If the answer produces a novel categorization, write it back (either UPDATE cognitive-biases.md or CREATE a new comparison page). Run `bin/validate-op.sh` before applying.

Expected log entry (if write-back triggers):
```
## [YYYY-MM-DD] query | Compare heuristic-origin vs loss-aversion-origin cognitive biases

answer: <one-line summary>
write_back: WRITE-BACK: new connection between existing pages -> UPDATE cognitive-biases
delta_compiled: none
pages_affected: cognitive-biases
```

If the answer does NOT produce genuinely novel synthesis (e.g., the categorization is already explicit in cognitive-biases.md), log NO-WRITE-BACK honestly. The test validates that the decision logic works correctly regardless of which path is taken.

**Scenario C: Privacy-sensitive query (privacy inheritance path)**

Question: "What personal decision patterns have I recorded?"

Follow all 10 steps. This question involves `local_only` content (personal-decision-patterns.md sources from the journal entry which is `local_only`). Verify:
1. `bin/search.sh` finds the relevant pages
2. If write-back is triggered, the target must be `local_only`
3. `bin/validate-op.sh` would reject writing local_only-derived content to a `cloud_safe` page
4. Log the privacy classification in the write-back decision

Expected: The answer references `local_only` pages. If no novel synthesis, NO-WRITE-BACK. If write-back triggers, target must be `local_only`.

**For all scenarios:**
- Use `bin/search.sh` in step 1 and record its output
- Check `compilation_status` on all referenced source pages (should all be `compiled` from Plan 01 backfill)
- Use today's UTC date in log entries
- Append entries at the bottom of wiki/log.md (newest at bottom)

IMPORTANT: Write-back decisions must be HONEST. Do not force write-back just to demonstrate the feature. The validation proves the decision logic works, including the "skip" path.
  </action>
  <verify>
    <automated>grep -c "query |" wiki/log.md | grep -q "[3-9]\|[1-9][0-9]" && tail -30 wiki/log.md | grep -q "write_back:" && echo "PASS: multiple query log entries with write-back decisions exist" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - wiki/log.md contains at least 3 new entries starting with `## [2026-` and containing `query |`
    - Each log entry contains `answer:`, `write_back:`, `delta_compiled:`, and `pages_affected:` fields
    - At least one entry has `NO-WRITE-BACK:` with a reason
    - If any entry has `WRITE-BACK:`, it specifies the trigger and target page
    - Scenario C log entry demonstrates privacy awareness (mentions local_only content or privacy consideration)
    - bin/search.sh was used in step 1 for all scenarios and returned results
    - If write-back was applied, `bin/validate-op.sh` was run on the target page before application
    - delta_compiled field is present in all entries (expected: "none" since all sources are compiled)
  </acceptance_criteria>
  <done>Three query scenarios executed end-to-end covering no-write-back (pure lookup), write-back (novel synthesis), and privacy-sensitive paths; all log entries follow the structured format from AGENTS.md section 11.2</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review of complete Phase 4 deliverables</name>
  <what-built>Complete Phase 4: query workflow spec, CLI search helper, operations validator, compilation tracking, executor model, operation logging, and end-to-end validation with three test scenarios.</what-built>
  <action>Present Phase 4 deliverables for human review. Run automated smoke tests on bin/search.sh and bin/validate-op.sh. Display tail of wiki/log.md showing query entries. Summarize AGENTS.md section changes for human inspection.</action>
  <how-to-verify>
Review the following deliverables:

1. **bin/search.sh** -- Run these commands:
   - `bash bin/search.sh "prospect theory"` -- should show results with TL;DR
   - `bash bin/search.sh --paths-only "bias"` -- should show bare file paths
   - `bash bin/search.sh --query "What is loss aversion?"` -- should show formatted LLM prompt

2. **bin/validate-op.sh** -- Run these commands:
   - `bash bin/validate-op.sh UPDATE wiki/entities/daniel-kahneman.md` -- should PASS all 5 checks
   - `bash bin/validate-op.sh UPDATE wiki/entities/nonexistent.md` -- should FAIL check 1
   - `bash bin/validate-op.sh MERGE wiki/concepts/prospect-theory.md wiki/concepts/prospect-theory.md` -- should FAIL (same page)

3. **AGENTS.md updates** -- Check these sections contain Phase 4 additions:
   - Section 5: compilation_status fields with transition rules table
   - Section 9: bin/validate-op.sh reference + per-operation preconditions/postconditions
   - Section 11.1: step 6a compilation status setting
   - Section 11.2: Write-Back Rules, Privacy Inheritance, Delta Compilation, Query Log Entry Format, Worked Example
   - Section 12: Structured Operation Log Entries with examples

4. **Query log entries** -- Read tail of wiki/log.md:
   - Three query entries with structured format
   - Write-back decisions are honest and well-reasoned
   - Privacy scenario correctly handled

5. **Compilation tracking** -- Check source pages:
   - `grep "compilation_status" wiki/sources/*.md` -- all should show `compiled`

6. **Obsidian vault** -- Open in Obsidian and verify links still resolve
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <verify>
    <automated>bash bin/search.sh "prospect theory" | grep -q "wiki/" && bash bin/validate-op.sh UPDATE wiki/entities/daniel-kahneman.md | grep -q "RESULT: PASS" && grep -q "compilation_status: compiled" wiki/sources/src-2026-04-10-kahneman-prospect-theory.md && echo "PASS: tools functional and compilation tracking present" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms bin/search.sh returns results in all three modes
    - Human confirms bin/validate-op.sh prints PASS for valid operations and FAIL for invalid
    - Human confirms wiki/log.md has structured query log entries from all three test scenarios
    - Human confirms AGENTS.md section 11.2 has Write-Back Rules, Privacy Inheritance, Delta Compilation, Worked Example
    - Human confirms AGENTS.md section 9 has per-operation preconditions/postconditions
    - Human confirms compilation_status fields exist on all source pages
    - Human confirms Obsidian vault links resolve
  </acceptance_criteria>
  <done>Human approves all Phase 4 deliverables: spec updates (sections 5, 9, 11.1, 11.2, 12), CLI tools (search, validator), compilation tracking, per-operation semantics, and end-to-end validation across three scenarios</done>
</task>

</tasks>

<verification>
- Three query scenarios executed end-to-end per AGENTS.md section 11.2
- Validation matrix covers: no-write-back path, write-back path, privacy-sensitive path
- bin/search.sh used for page discovery in all scenarios
- bin/validate-op.sh used for any write-back operations
- Structured query log entries appended to wiki/log.md for all scenarios
- Human reviews all Phase 4 deliverables
</verification>

<success_criteria>
- Three real questions were asked against the wiki and answered with citations
- Write-back decisions were made correctly per D-01/D-03/D-04 for each scenario
- Privacy inheritance was verified for local_only content
- Delta compilation check confirmed all sources are compiled
- All Phase 4 tools (search, validator) are working
- Human approves the complete phase deliverables
</success_criteria>

<output>
After completion, create `.planning/phases/04-query-structured-operations/04-06-SUMMARY.md`
</output>
