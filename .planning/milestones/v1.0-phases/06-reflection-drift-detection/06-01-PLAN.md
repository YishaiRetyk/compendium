---
phase: 06-reflection-drift-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - schema/templates/decision.md
  - wiki/decisions/dr-2026-04-14-phase6-decision-type.md
  - wiki/index.md
  - AGENTS.md
autonomous: true
requirements: [DCSN-01, DCSN-02]

must_haves:
  truths:
    - "A decision record template exists with all required sections (TL;DR, Decision, Why, Alternatives Considered, Consequences, Affected Pages, Sources)"
    - "Decision pages have dedicated frontmatter fields (trigger_type, affected_pages) and use type: decision"
    - "AGENTS.md documents the decision page type with frontmatter schema, validation checklist updates, worked example, directory rule, and index category"
    - "An example decision record exists in wiki/decisions/ and appears in wiki/index.md under a Decisions category"
    - "The canonical naming convention dr-YYYY-MM-DD-slug.md is documented in AGENTS.md"
  artifacts:
    - path: "schema/templates/decision.md"
      provides: "Decision record page type template"
      contains: "type: decision"
    - path: "wiki/decisions/dr-2026-04-14-phase6-decision-type.md"
      provides: "Example decision record page"
      contains: "trigger_type: schema-update"
    - path: "AGENTS.md"
      provides: "Schema documentation for decision page type"
      contains: "4.6 Decision"
    - path: "wiki/index.md"
      provides: "Decisions index category"
      contains: "## Decisions"
  key_links:
    - from: "schema/templates/decision.md"
      to: "AGENTS.md section 4.6"
      via: "page type definition"
      pattern: "4\\.6 Decision"
    - from: "wiki/decisions/dr-2026-04-14-phase6-decision-type.md"
      to: "wiki/index.md"
      via: "index entry under Decisions category"
      pattern: "dr-2026-04-14-phase6-decision-type"
    - from: "AGENTS.md section 5"
      to: "AGENTS.md section 4.6"
      via: "type enum includes decision"
      pattern: "entity.*concept.*source.*comparison.*overview.*decision"
---

<objective>
Create the decision record page type as a first-class wiki citizen: template, directory, AGENTS.md documentation (section 4.6 page type, section 5 frontmatter with type enum + validation checklist, section 12 index category), and a bootstrapping example decision record.

Purpose: Decision records capture why structural changes were made to the wiki. This plan establishes the complete page type contract — template, schema, naming convention, directory rule, frontmatter validation rules — that Plan 02 (lint validation) and Plan 03 (reflect workflow) both depend on.
Output: decision.md template, example decision record, updated AGENTS.md sections 4/5/12, updated index.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-reflection-drift-detection/06-CONTEXT.md
@.planning/phases/06-reflection-drift-detection/06-RESEARCH.md

<interfaces>
<!-- From AGENTS.md section 5 -- base frontmatter fields pattern that decision template must follow -->
<!-- All existing types use these 16 base fields plus type-specific extensions -->
Base fields: id, title, type, status, summary, created_at, updated_at, sources, epistemic_status, tags, domains, supersedes, superseded_by, privacy, aliases, has_contradictions, knowledge_domain

<!-- From schema/templates/ -- established template pattern: YAML frontmatter + FORBIDDEN PATTERNS comment + sections with HTML comments -->
<!-- Decision template follows this exact skeleton pattern -->

<!-- From AGENTS.md section 4 -- page type definition pattern -->
<!-- Each page type has: description paragraph, "When to use" bullet list, frontmatter table, section ordering, file naming, worked example -->

<!-- From wiki/index.md -- index category pattern -->
<!-- Format: ## CategoryName\n- [[Page Title]] -- summary (epistemic_status, date) -->
Categories: Entities, Concepts, Sources, Comparisons, Overviews
New: Decisions

<!-- From AGENTS.md section 12 -- index categories list -->
<!-- "Organized by page type: Entities, Concepts, Sources, Comparisons, Overviews." -->

<!-- From AGENTS.md section 5 -- Frontmatter Validation Checklist -->
<!-- Currently has 14 items. Decision-specific items will be added as 15-17. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create decision record template and example page</name>
  <files>schema/templates/decision.md, wiki/decisions/dr-2026-04-14-phase6-decision-type.md</files>
  <read_first>
    - schema/templates/overview.md (established template pattern to follow)
    - schema/templates/entity.md (second template for pattern confirmation)
    - wiki/index.md (current index structure)
    - AGENTS.md lines 473-590 (section 5 frontmatter schema)
    - .planning/phases/06-reflection-drift-detection/06-RESEARCH.md lines 261-323 (decision template code example)
    - .planning/phases/06-reflection-drift-detection/06-CONTEXT.md (D-01 through D-05 locked decisions)
  </read_first>
  <action>
    **Create schema/templates/decision.md** per D-01 through D-05:

    Frontmatter (all 16 base fields + decision-specific):
    ```yaml
    ---
    id:
    title:
    type: decision
    status: active
    summary: ""
    created_at:
    updated_at:
    sources: []
    epistemic_status: sourced
    tags: []
    domains: []
    supersedes:
    superseded_by:
    privacy: cloud_safe
    aliases: []
    has_contradictions: false
    knowledge_domain: ""
    trigger_type:
    affected_pages: []
    ---
    ```

    Decision-specific fields:
    - `trigger_type`: one of `merge`, `split`, `schema-update`, `domain-reorg`, `reframing`, `contradiction-resolution` (per D-02)
    - `affected_pages`: YAML list of page IDs (not wikilinks, consistent with sources field convention)

    FORBIDDEN PATTERNS block between frontmatter and first section (same as all other templates).

    Body sections per D-03, in order:
    1. `## TL;DR` -- HTML comment: "One-sentence summary of the decision and its impact."
    2. `## Decision` -- HTML comment: "What was decided. State clearly and concisely."
    3. `## Why` -- HTML comment: "The reasoning behind this decision. What problem was it solving? What framing was adopted and what did it replace?"
    4. `## Alternatives Considered` -- HTML comment: "What other options were evaluated. Why were they rejected?"
    5. `## Consequences` -- HTML comment: "What changes as a result. What becomes easier/harder? What follow-on work is needed?"
    6. `## Affected Pages` -- HTML comment: "Wikilinks to pages affected by this decision. Format: - [[Page Title]] -- how it was affected"
    7. `## Sources` -- HTML comment: "Human-readable source list if applicable. Format: - [[src-YYYY-MM-DD-slug]]: \"Title\" (date)"

    **Create wiki/decisions/ directory** (mkdir -p).

    **Create wiki/decisions/dr-2026-04-14-phase6-decision-type.md** -- a real bootstrapping decision record documenting the introduction of the decision page type itself:
    - `id: dr-2026-04-14-phase6-decision-type`
    - `title: "Introduce Decision Record Page Type"`
    - `type: decision`
    - `status: active`
    - `summary: "Decision records are a dedicated page type (type: decision) with their own template, directory (wiki/decisions/), and index category, rather than overloading the overview type."`
    - `created_at: 2026-04-14`
    - `updated_at: 2026-04-14`
    - `trigger_type: schema-update`
    - `affected_pages: []` (no pre-existing pages affected -- this is the inaugural record)
    - `privacy: cloud_safe`
    - `epistemic_status: sourced`
    - `knowledge_domain: software`
    - `tags: [meta, schema]`
    - `domains: [wiki-infrastructure]`

    Body content (all 7 sections must have real prose, not just comments, per DCSN-02):
    - **TL;DR:** Decision records get a dedicated `type: decision` page type with their own template, directory, and index category, replacing the prior convention of storing them as overview pages.
    - **Decision:** Created `wiki/decisions/` directory, `schema/templates/decision.md` template, and added `decision` to the type enum in AGENTS.md section 5. Decision records use structured sections (TL;DR, Decision, Why, Alternatives, Consequences, Affected Pages, Sources) and type-specific frontmatter fields (`trigger_type`, `affected_pages`).
    - **Why:** The previous reflect workflow (section 11.4) stored decision records as overview pages in `wiki/overviews/`. This conflated structural reasoning with topic synthesis. A dedicated type enables targeted queries via Dataview (`type = "decision"`), clearer navigation in the index, and machine-parseable audit trails via `trigger_type` and `affected_pages` fields. The framing adopted is "decision records as first-class page type." The framing it replaced is "decision records overloaded onto overview type."
    - **Alternatives Considered:** (a) Keep using overview type with a tag -- rejected because tags are ad hoc and don't enforce structural sections or type-specific frontmatter. (b) Use a flat markdown file outside wiki/ -- rejected because it breaks Obsidian graph navigation and Dataview queries. (c) Embed decisions inline in log.md -- rejected because decisions need structured sections and bidirectional links.
    - **Consequences:** All six trigger types (merge, split, schema-update, domain-reorg, reframing, contradiction-resolution) produce decision records in `wiki/decisions/`. Existing overview pages are NOT retroactively converted. The `decision_history` field on affected pages enables bidirectional navigation. Lint validation gains decision-specific checks (trigger_type enum, affected_pages type).
    - **Affected Pages:** None (inaugural record).
    - **Sources:** None (internal schema decision).
  </action>
  <verify>
    <automated>grep -c "type: decision" schema/templates/decision.md && grep -c "trigger_type:" schema/templates/decision.md && grep -c "affected_pages:" schema/templates/decision.md && test -f wiki/decisions/dr-2026-04-14-phase6-decision-type.md && grep -c "## Alternatives Considered" wiki/decisions/dr-2026-04-14-phase6-decision-type.md && grep -c "## Why" wiki/decisions/dr-2026-04-14-phase6-decision-type.md</automated>
  </verify>
  <acceptance_criteria>
    - schema/templates/decision.md contains `type: decision` in frontmatter
    - schema/templates/decision.md contains `trigger_type:` in frontmatter
    - schema/templates/decision.md contains `affected_pages: []` in frontmatter
    - schema/templates/decision.md contains all 7 body sections: `## TL;DR`, `## Decision`, `## Why`, `## Alternatives Considered`, `## Consequences`, `## Affected Pages`, `## Sources`
    - schema/templates/decision.md contains `FORBIDDEN PATTERNS` comment block
    - wiki/decisions/dr-2026-04-14-phase6-decision-type.md contains `trigger_type: schema-update`
    - wiki/decisions/dr-2026-04-14-phase6-decision-type.md contains `id: dr-2026-04-14-phase6-decision-type`
    - wiki/decisions/dr-2026-04-14-phase6-decision-type.md `## Why` section contains the string "framing adopted" and "framing it replaced" (verifying DCSN-02 content: what framing was adopted and what it replaced)
    - wiki/decisions/dr-2026-04-14-phase6-decision-type.md `## Alternatives Considered` section has at least 2 alternatives listed
    - wiki/decisions/dr-2026-04-14-phase6-decision-type.md all 7 body sections have real content (not just HTML comments)
  </acceptance_criteria>
  <done>Decision template exists in schema/templates/ with all base + decision-specific fields and 7 structured sections. Example decision record exists in wiki/decisions/ with real prose in all 7 sections documenting the introduction of the page type, including adopted framing, replaced framing, and alternatives considered.</done>
</task>

<task type="auto">
  <name>Task 2: Update AGENTS.md sections 4, 5, and 12 for decision page type, and add Decisions category to index.md</name>
  <files>AGENTS.md, wiki/index.md</files>
  <read_first>
    - AGENTS.md lines 123-472 (section 4 page types -- all 5 existing type definitions for pattern)
    - AGENTS.md lines 473-590 (section 5 frontmatter schema)
    - AGENTS.md lines 1269-1296 (section 12 index and log)
    - AGENTS.md lines 572-590 (frontmatter validation checklist)
    - wiki/index.md (current index with 5 categories)
    - schema/templates/decision.md (just created in Task 1)
    - wiki/decisions/dr-2026-04-14-phase6-decision-type.md (just created in Task 1)
  </read_first>
  <action>
    **AGENTS.md Section 4 -- Add section 4.6 Decision:**

    Add after section 4.5 Overview, before section 5. Follow the exact pattern of existing page type definitions (description paragraph, "When to use" bullet list, frontmatter table, section ordering, file naming, worked example).

    ```
    ### 4.6 Decision (`type: decision`)

    Decision records capture why structural changes were made to the wiki. They answer the question: "Why is the wiki shaped this way?" Create a decision record when future-you would reasonably ask that question.

    **When to use:**

    - Page merges or splits
    - Schema updates (new fields, changed conventions)
    - Domain reorganization (moving pages between categories)
    - Significant reframing of a concept or topic
    - Major supersession (SUPERSEDE of a key page or concept)
    - Contradiction-resolution decisions (structural resolution, not the contradiction itself)

    **Directory:** `wiki/decisions/`

    **File naming:** `dr-YYYY-MM-DD-slug.md`. The `dr-` prefix prevents ID collisions with other page types. The date provides natural chronological sorting. The slug provides human readability.

    **ID convention:** Same as filename without `.md` extension: `dr-YYYY-MM-DD-slug`. Use lowercase, hyphen-separated slugs. The ID is used in `decision_history` on affected pages.

    **Frontmatter (in addition to base fields):**

    | Field | Type | Required | Description |
    |-------|------|----------|-------------|
    | `trigger_type` | enum | Yes | One of: `merge`, `split`, `schema-update`, `domain-reorg`, `reframing`, `contradiction-resolution` |
    | `affected_pages` | list | Yes | YAML list of page IDs (the `id` field value) of pages affected by this decision. Empty list `[]` is valid for inaugural or infrastructure-only records. |

    **Section ordering (all required):**

    1. ## TL;DR
    2. ## Decision
    3. ## Why -- Must state what framing was adopted and what it replaced.
    4. ## Alternatives Considered -- Must list alternatives and why they were rejected.
    5. ## Consequences
    6. ## Affected Pages -- Wikilinks to affected pages with how each was affected.
    7. ## Sources

    **Epistemic pattern:** Decision records use `epistemic_status: sourced` (the decision itself is the source of truth). They do NOT participate in staleness tracking or contradiction detection -- a decision is a historical fact, not a claim that can become stale.

    **`decision_history` back-link:** Pages affected by a decision gain a `decision_history` field in their frontmatter -- a YAML list of decision record IDs. This field is optional (not part of BASE_FIELDS); it is added when the first decision references a page. A visible "Decision History" section in the page body is optional -- include only when the history is meaningful for readers (per D-05).

    **Worked example:**
    ```
    (Include an abbreviated version of dr-2026-04-14-phase6-decision-type.md showing the frontmatter and first 2-3 sections, following the same format as the entity/concept/source worked examples in sections 4.1-4.5.)
    ```

    **AGENTS.md Section 5 -- Frontmatter updates:**

    1. In the `type` enum on the Base Fields block (line ~481), add `decision`:
       Change: `type: entity|concept|source|comparison|overview`
       To: `type: entity|concept|source|comparison|overview|decision`

    2. In the Field Descriptions table, update the `type` field description:
       Change: "Page type: `entity`, `concept`, `source`, `comparison`, `overview`."
       To: "Page type: `entity`, `concept`, `source`, `comparison`, `overview`, `decision`."

    3. Add a new subsection after "Compilation Tracking Fields" and before "Frontmatter Validation Checklist":

       ```
       ### Decision Record Additional Fields

       Decision record pages (`type: decision`) include these additional frontmatter fields:

       | Field | Type | Required | Description |
       |-------|------|----------|-------------|
       | `trigger_type` | enum | Yes | What prompted this decision: `merge`, `split`, `schema-update`, `domain-reorg`, `reframing`, `contradiction-resolution` |
       | `affected_pages` | list | Yes | YAML list of page IDs affected by this decision. Used for bidirectional navigation via `decision_history` on those pages. |

       ### Optional Back-Link Field

       Any page type may include this field when referenced by a decision record:

       | Field | Type | Required | Description |
       |-------|------|----------|-------------|
       | `decision_history` | list | No | YAML list of decision record IDs (e.g., `[dr-2026-04-14-slug]`). Added when a decision record lists this page in `affected_pages`. NOT a base field -- absence is valid. When present, must be a YAML list of strings. |
       ```

    4. In the Frontmatter Validation Checklist, update item 2:
       Change: "`type` is one of: `entity`, `concept`, `source`, `comparison`, `overview`"
       To: "`type` is one of: `entity`, `concept`, `source`, `comparison`, `overview`, `decision`"

    5. Add new validation items at the end of the checklist:
       - "15. For `type: decision` pages: `trigger_type` is one of: `merge`, `split`, `schema-update`, `domain-reorg`, `reframing`, `contradiction-resolution`"
       - "16. For `type: decision` pages: `affected_pages` is present and is a YAML list of string IDs"
       - "17. If `decision_history` is present on any page: it is a YAML list of string IDs"

    **AGENTS.md Section 12 -- Index category update:**

    In the index.md description (line ~1274), change:
    "Organized by page type: Entities, Concepts, Sources, Comparisons, Overviews."
    To:
    "Organized by page type: Entities, Concepts, Sources, Comparisons, Overviews, Decisions."

    **wiki/index.md -- Add Decisions category:**

    Add a new `## Decisions` section after `## Overviews` with the example decision record entry:
    ```
    ## Decisions

    - [[dr-2026-04-14-phase6-decision-type]] -- Decision records are a dedicated page type with their own template, directory, and index category (sourced, 2026-04-14)
    ```
  </action>
  <verify>
    <automated>grep -c "4.6 Decision" AGENTS.md && grep "entity|concept|source|comparison|overview|decision" AGENTS.md | head -1 && grep -c "## Decisions" wiki/index.md && grep -c "trigger_type" AGENTS.md && grep -c "decision_history" AGENTS.md && grep -c "wiki/decisions/" AGENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md contains `### 4.6 Decision` section header
    - AGENTS.md section 4.6 contains `**Directory:** \`wiki/decisions/\`` (directory rule explicit per review concern)
    - AGENTS.md section 4.6 contains `**File naming:** \`dr-YYYY-MM-DD-slug.md\`` (naming convention explicit per review concern)
    - AGENTS.md section 4.6 contains `**ID convention:**` paragraph (ID format explicit per review concern)
    - AGENTS.md section 5 type enum reads `entity|concept|source|comparison|overview|decision`
    - AGENTS.md contains `### Decision Record Additional Fields` subsection
    - AGENTS.md contains `### Optional Back-Link Field` subsection with `decision_history`
    - AGENTS.md validation checklist item 2 includes `decision` in the type list
    - AGENTS.md validation checklist contains items 15, 16, 17 for decision-specific validation
    - AGENTS.md section 12 mentions "Decisions" in the category list
    - wiki/index.md contains `## Decisions` section
    - wiki/index.md contains `[[dr-2026-04-14-phase6-decision-type]]`
  </acceptance_criteria>
  <done>AGENTS.md documents the decision page type in section 4.6 (with explicit directory rule, naming convention, and ID convention), frontmatter schema in section 5 (type enum, decision-specific fields, decision_history back-link, validation checklist items 15-17), and index category in section 12. wiki/index.md has a Decisions category with the example record.</done>
</task>

</tasks>

<verification>
- `grep "type: decision" schema/templates/decision.md` returns a match
- `grep "4.6 Decision" AGENTS.md` returns a match
- `grep "## Decisions" wiki/index.md` returns a match
- `ls wiki/decisions/dr-2026-04-14-phase6-decision-type.md` exists
- `grep "wiki/decisions/" AGENTS.md` returns a match (directory rule documented)
- `grep "dr-YYYY-MM-DD-slug" AGENTS.md` returns a match (naming convention documented)
</verification>

<success_criteria>
Decision record page type is fully defined: template exists with required sections enforcing DCSN-02 content (framing adopted, replaced, alternatives), example page exists with real prose, AGENTS.md schema documents it in sections 4 (with directory rule, naming convention, ID convention), 5 (type enum, validation checklist), and 12 (index category), and the index has a Decisions category. Any agent reading AGENTS.md can create decision records following the documented conventions.
</success_criteria>

<output>
After completion, create `.planning/phases/06-reflection-drift-detection/06-01-SUMMARY.md`
</output>
