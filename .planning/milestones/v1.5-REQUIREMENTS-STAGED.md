# Requirements: LLM Wiki Compiler — Milestone v1.5 Python Migration

**Defined:** 2026-06-18 (on the laptop; imported + renumbered 2026-07-03 — see v1.5-MILESTONE-BRIEF.md)
**Core Value:** The wiki is a persistent, compounding artifact — cross-references are already there, contradictions already flagged, synthesis already reflects everything ingested.

## v1.5 Requirements

Requirements for milestone v1.5 Python Migration. Each maps to roadmap phases (numbering continues from Phase 23 (v1.4 Source Lifecycle) → starts at Phase 24).

Design lineage: the 7-agent codebase assessment (2026-06-18) — ~10.2k lines of `bin/` (72% already Python inside `python3` heredocs), ~1.2k lines already-Python `bin/lib/`, and a 259-file / ~15k-line bash test suite (73% black-box CLI characterization tests). Strategy LOCKED via `/gsd-new-milestone`: **full migration** behind `.sh` exec-shims (shim-and-swap), a shared `common/` core, and a **two-layer test strategy** (parity oracle + net-new pytest unit layer) with the wholesale CLI→pytest conversion as a deferrable terminal phase. This is a pure internal refactor — it relocates logic and adds no user-facing capability; behavior parity is the acceptance bar throughout.

### Packaging & Shared Core (PKG)

- [ ] **PKG-01**: An installable Python package exists (`pyproject.toml` with pinned runtime deps `PyYAML` + `ruamel.yaml` and `console_scripts` entry points); `pip install -e .` succeeds and exposes each migrated tool as an importable module
- [ ] **PKG-02**: A shared `common/` package is extracted from the existing `bin/lib/*.py` — privacy resolver, YAML round-trip (the `make_yaml` canonicalization chokepoint), vault walker, and wiki-page primitives (`parse_frontmatter`, provenance/wikilink regexes) — as a single source of truth, eliminating the `lint`↔`audit-claims` byte-copy and the duplicated "is-under-`wiki-local/`" predicates
- [ ] **PKG-03**: Every migrated `bin/<name>.sh` becomes a thin shim that `exec`s its Python entry point, preserving the exact argv / exit-code / stdout-vs-stderr contract; all existing callers (CI, pre-commit, schema docs, `.claude/settings.local.json`) invoke it unchanged
- [ ] **PKG-04**: CI installs the package (additive edit to the three workflows); all existing CI gates (`lint`, `privacy-leak`, `strict`, `skills-check`, `neutrality`, `setup-parity`) stay green and their required-check names are unchanged

### Parity Harness & Test Infrastructure (TEST)

- [ ] **TEST-01**: The shared test harness routes every script-under-test invocation through one `invoke_tool` seam selectable by `WIKI_IMPL=bash|py`, so the existing black-box suite can run against either implementation and diff byte-for-byte
- [ ] **TEST-02**: All phase test suites (09–20) are wired into CI, so the parity net is enforced rather than merely runnable locally (today only phases 07–08 gate in CI)
- [ ] **TEST-03**: Before any script is ported, its behavior is frozen as a characterization golden (full stdout + exit code + resulting file tree); missing coverage is backfilled first — `validate-op.sh` (zero tests today) and the untested `search.sh` modes get characterization tests before they are touched
- [ ] **TEST-04**: Implementation-asserting tests (e.g. the `hashlib`-not-`sha256sum` grep, the `pdf-extract` `api/generate` body-grep) are quarantined or rewritten to assert behavior, so they neither block nor invert under the Python implementation
- [ ] **TEST-05**: A pytest harness is stood up — `conftest.py` with a `git_repo`/`tmp_path` fixture replicating `make_bare_repo`/`make_fixture_repo`, a golden-tree comparison helper, and `skipif` markers for the Ollama/network tests
- [ ] **TEST-06**: Net-new pytest unit tests cover the extracted `common/` core and each cluster's modules (e.g. `make_yaml`, `resolve_effective_claim_privacy`, `classify_page`, the lint check functions) — direct module-level coverage that does not exist today

### Script Migration (MIG) — parity-gated, cluster by cluster

- [ ] **MIG-01**: The brownfield cluster (`brownfield.sh` + the 5 `bin/lib/*.py` modules, promoted into `common/` / `brownfield/`) is ported and parity-green across its phase-10/11 suites, including byte-exact YAML/REPORT fixture output (the `make_yaml` chokepoint) — the lowest-risk large script and the architectural template for the rest
- [ ] **MIG-02**: `lint.sh` and `audit-claims.sh` are ported together onto the shared `common/` primitives (retiring the byte-copy), parity-green — including the `lint --ci --format json` schema (locked by a golden consumed by `json-to-annotations.py`), the dual-mode exit-code semantics, and the `audit-claims --verifier` STDIN-only / `shell=False` egress security contract preserved verbatim
- [ ] **MIG-03**: The privacy/neutrality checkers (`check-neutrality.sh`, `check-privacy.sh`, `check-sources-cloud-safe.sh`) are ported, parity-green, preserving their distinct exit-code semantics (exit 2 on violation) and the CI `privacy-leak` / `neutrality` hard gates
- [ ] **MIG-04**: The setup/release family (`init-wizard.sh`, `gen-skills.sh`, `release.sh`, `sync-claude.sh`) is ported — preserving `init-wizard`'s interactive prompt contract + the wizard-vs-manual byte-equality gate (`setup-parity`), `sync-claude`'s exit-2-on-drift, `gen-skills`'s exit-1-on-drift, and the latency-sensitive pre-commit-hot-path behavior
- [ ] **MIG-05**: The wiki-operations family (`ingest.sh`, `validate-op.sh`, `search.sh`, `requirements-sync.sh`, `pdf-extract.sh`) is ported, parity-green; external boundaries (poppler, the Ollama HTTP endpoint, git) remain subprocess/HTTP calls in Python
- [ ] **MIG-06**: `migrate-privacy-dirs.sh` is retired (spent Phase-15 one-off, already executed) and `install-hooks.sh` is left as bash (6-line `git config`); both decisions are recorded

### Cutover & Documentation (CUT)

- [ ] **CUT-01**: A decision record documents the migration (shim-and-swap, `common/` consolidation, the two-layer test strategy, the retirements); all `bin/*.sh` references across `schema/`, `docs/`, `CLAUDE.md`/`AGENTS.md`, and `.claude/settings.local.json` are verified accurate post-migration (and `AGENTS.md ≡ CLAUDE.md` byte-equality held)
- [ ] **CUT-02** *(terminal, deferrable)*: The existing black-box CLI/characterization tests are converted onto the established pytest harness, the bash test suite is retired, and a single `pytest` entrypoint runs the full suite in parallel — depends on all MIG phases being parity-green; cleanly cut if timeline slips (the system ships complete without it, with tests still running via the instrumented-bash oracle)

## Future Requirements

Deferred. Tracked but not in the current roadmap.

### External-tool re-platforming

- **LIBSWAP**: Replacing external-tool orchestration (poppler / Ollama / git / jq) with native Python libraries beyond thin subprocess/HTTP wrapping — only if a concrete need arises; the external boundaries stay processes regardless of language

### Shim removal

- **SHIMOUT**: Retiring the `bin/<name>.sh` exec-shims and pointing CI/hooks/docs directly at the Python entry points — a clean, independent post-migration step once nothing depends on the `.sh` paths

## Out of Scope

Explicitly excluded. Documented to prevent scope creep.

| Feature | Reason |
|---------|--------|
| Any behavioral change or new feature during the port | Pure parity refactor; behavior parity is the acceptance bar — any change is a separate, post-migration decision |
| Removing the `bin/<name>.sh` shims / flipping CI to call modules directly | Deferred (FUTURE: SHIMOUT); the shims are the stable command interface that keeps ~800 load-bearing references green |
| Migrating the 4 `schema/brownfield/migrations/*.sh` scripts | They ride the `suggest`/`verify` op-hash byte-copy contract; defer to a later pass to avoid changing that contract mid-migration |
| Porting `migrate-privacy-dirs.sh` | Spent one-off (Phase 15, already executed) — retire, not port (MIG-06) |
| Rewriting `install-hooks.sh` in Python | 6-line `git config`; Python adds only interpreter startup to a hot path — leave as bash (MIG-06) |
| Backlog 999.3 / 999.5 / 999.6 and the `repository` source type | Unrelated feature backlog; not part of the re-platform |

## Traceability

Which phases cover which requirements. Filled during roadmap creation. Phase numbering continues from Phase 21 (v1.3) → v1.5 starts at Phase 24. Restructured 2026-06-18 (human-approved) from 6 phases (22–27) to 3 (22–24): a frozen-foundation → parallel-migration+cutover → terminal-conversion fan-out/fan-in.

| Requirement | Phase | Status |
|-------------|-------|--------|
| PKG-01 | Phase 24 | Pending |
| PKG-02 | Phase 24 | Pending |
| PKG-03 | Phase 24 | Pending |
| PKG-04 | Phase 24 | Pending |
| TEST-01 | Phase 24 | Pending |
| TEST-02 | Phase 24 | Pending |
| TEST-03 | Phase 24 | Pending |
| TEST-04 | Phase 24 | Pending |
| TEST-05 | Phase 24 | Pending |
| MIG-01 | Phase 25 (Wave 1 — brownfield) | Pending |
| MIG-02 | Phase 25 (Wave 1 — lint + audit-claims) | Pending |
| MIG-03 | Phase 25 (Wave 1 — checkers) | Pending |
| MIG-04 | Phase 25 (Wave 1 — setup/release) | Pending |
| MIG-05 | Phase 25 (Wave 1 — wiki-ops) | Pending |
| MIG-06 | Phase 25 (Wave 1 — folded into wiki-ops) | Pending |
| TEST-06 | Phase 25 (spans all Wave-1 cluster plans; grown alongside each port) | Pending |
| CUT-01 | Phase 25 (Wave 2 — cutover fan-in) | Pending |
| CUT-02 | Phase 26 (terminal, deferrable) | Pending |

**Coverage:**
- v1.5 requirements: 18 total (PKG ×4, TEST ×6, MIG ×6, CUT ×2)
- Mapped to phases: 18/18 (100%) — Phase 24 = 9 (PKG-01..04, TEST-01..05); Phase 25 = 8 (MIG-01..06, CUT-01, TEST-06); Phase 26 = 1 (CUT-02). Each requirement maps to exactly one phase. TEST-06 spans the Wave-1 cluster plans inside Phase 25 (net-new unit coverage grown alongside each port), not deferred to a separate phase.
- Unmapped: 0

**Phase summary:**
- Phase 24 — Foundation (Package Skeleton + Frozen Shared Core + Parity Oracle): PKG-01, PKG-02, PKG-03, PKG-04, TEST-01, TEST-02, TEST-03, TEST-04, TEST-05
- Phase 25 — Parallel Migration + Cutover: MIG-01, MIG-02, MIG-03, MIG-04, MIG-05, MIG-06, CUT-01, TEST-06
  - Wave 1 (parallel cluster ports, ≈5–6 plans, isolated worktrees, frozen `common/` read-only): brownfield (MIG-01) ‖ lint+audit-claims (MIG-02) ‖ checkers (MIG-03) ‖ setup/release (MIG-04) ‖ wiki-ops+retirements (MIG-05, MIG-06); TEST-06 grown per cluster
  - Wave 2 (cutover fan-in, 1 plan, depends on all of Wave 1): CUT-01
- Phase 26 — Wholesale CLI→Pytest Conversion (terminal, deferrable): CUT-02

---
*Requirements defined: 2026-06-18; traceability filled by roadmapper 2026-06-18; restructured 6→3 phases 2026-06-18 (human-approved)*
