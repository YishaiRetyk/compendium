# Phase 12.2: Local Wiki Write Gate - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-04
**Phase:** 12.2-local-wiki-write-gate
**Areas discussed:** Gate implementation surface, Gate scope (rolled into Surface), Hook composition with sync-claude, Exemptions + brownfield handling

---

## Gate implementation surface

| Option | Description | Selected |
|--------|-------------|----------|
| Extend bin/lint.sh with --staged mode | Single policy surface; reuses strict_new_pages/strict_added_epistemic_claims by swapping diff source. SC3-aligned. | ✓ |
| New bin/wgate.sh wrapper | Pattern-twin of check-privacy.sh; self-contained. Violates SC3 — invents a parallel policy surface. | |
| Inline check in .githooks/pre-commit | Smallest surface but duplicates frontmatter parsing and has no test harness. | |

**User's choice:** Extend bin/lint.sh with --staged mode (Recommended).
**Notes:** Single source of truth wins consistently across the WGATE phase. Mirrors Phase 9-02 / 9-06 patterns.

---

### Follow-up: How `--staged` reads file content

| Option | Description | Selected |
|--------|-------------|----------|
| Working tree | Read working-tree files for `git diff --cached --diff-filter=A` paths. Simple. Documents "commit what you staged" assumption. | ✓ |
| Staged blob via `git show :path` | Strictly correct under dirty working tree. Extra subprocess per file; different read path than existing lint code. | |
| Both, depending on flag | Premature. | |

**User's choice:** Working tree (Recommended).

---

### Follow-up: Gate scope (D-10 vs D-10+D-08)

| Option | Description | Selected |
|--------|-------------|----------|
| D-10 only (provenance) | WGATE-02 mandates only D-10. Keep gate fast and focused. | ✓ |
| Both D-10 and D-08 | Full strict-equivalent locally. Doubles complexity for marginal value. | |
| D-10 now, D-08 in follow-up phase | Same outcome as option 1, with explicit deferred seed. | |

**User's choice:** D-10 only (Recommended).
**Notes:** D-08 staged enforcement explicitly deferred — captured in CONTEXT deferred section.

---

### Follow-up: Flag shape

| Option | Description | Selected |
|--------|-------------|----------|
| `--strict --staged --category provenance` | `--staged` is orthogonal to `--strict`. Composable with --skip-category, --format json, etc. | ✓ |
| `--pre-commit` shortcut | Bundles assumptions; breaks Phase-9 orthogonal-flags style. | |
| Both: --staged + --pre-commit alias | Two ways to do the same thing; more code paths. | |

**User's choice:** `--strict --staged --category provenance` (Recommended).

---

### Follow-up: Exit codes on violation

| Option | Description | Selected |
|--------|-------------|----------|
| Match --strict exit policy (exit 1) | Reuses existing CI_SEVERITY_REMAP (provenance => error). No new dialect. | ✓ |
| Distinct exit code (e.g., 3) | Diagnostic but introduces a new convention; needs §11.3 amendment. | |
| Exit 2 (matches public-path leak gates) | Conflates structural-not-leak gating with privacy/neutrality leaks. | |

**User's choice:** Match --strict exit policy (Recommended).

---

## Hook composition with sync-claude

| Option | Description | Selected |
|--------|-------------|----------|
| Single pre-commit script: sync-claude first, then write-gate | sync-claude must run first because it can `git add CLAUDE.md` (mutates staging). Single file, easy to read. | ✓ |
| Two separate hook fragments + a runner | New convention (.d/ directory + runner) not currently documented. Over-engineered for two hooks. | |
| Inline write-gate, document chaining for users | Conflicts with Surface = lint.sh extension; would re-implement staged check inline. | |

**User's choice:** Single pre-commit script (Recommended).

---

### Follow-up: Failure message UX

| Option | Description | Selected |
|--------|-------------|----------|
| lint.sh's existing add_finding text + hook footer | Reuses Phase 9-02 output format; SC5 actionable. One trailing line points at fixes. | ✓ |
| Hook-customized output (more verbose) | Drifts from --ci JSON contract; duplicates lint output formatting. | |
| Short hook output, point to a doc | Forces an extra round-trip; doc drift silently degrades UX. | |

**User's choice:** lint.sh's existing add_finding text + hook footer (Recommended).

---

### Follow-up: Hook installation

| Option | Description | Selected |
|--------|-------------|----------|
| Keep bin/install-hooks.sh as-is | Hooks remain explicit-opt-in. New gate plugs into the same .githooks/pre-commit file install-hooks already activates. | ✓ |
| Auto-install on first ingest/commit | Surprises users who didn't opt in. | |
| CI assertion that hooks are installed | Invasive; hard to detect from git metadata; scope-creep. | |

**User's choice:** Keep bin/install-hooks.sh as-is (Recommended).
**Notes:** core.hooksPath is currently unset on this machine — explicit `bash bin/install-hooks.sh` activates the gate.

---

### Follow-up: Bypass mechanism

| Option | Description | Selected |
|--------|-------------|----------|
| Only --no-verify | Standard git escape; AGENTS.md §3 already declares it the canonical bypass. | ✓ |
| Add WGATE_SKIP=1 env var | Second escape hatch on top of --no-verify rarely useful. | |
| Per-page wgate_exempt: true frontmatter | Permanent bypass surface defeats the gate's purpose. | |

**User's choice:** Only --no-verify (Recommended).

---

## Exemptions + brownfield handling

| Option | Description | Selected |
|--------|-------------|----------|
| Exempt `bootstrap_stage: bootstrapped` pages | Mirrors lint.sh line-885 special-case precedent. SC4-aligned. | ✓ |
| Exempt the entire .brownfield/ stage path | .brownfield/ is gitignored (TMPL-04); never reaches staged index. No-op. | |
| No brownfield exemption — force migration first | Violates SC4: "brownfield transitional cases are not falsely blocked." | |

**User's choice:** Exempt `bootstrap_stage: bootstrapped` (Recommended).
**Notes:** `bootstrap_stage: verified` pages are NOT exempt — they've been promoted through the 5-gate verifier and are first-class wiki content from the gate's perspective.

---

### Follow-up: Frontmatter read mechanism

| Option | Description | Selected |
|--------|-------------|----------|
| Reuse lint.sh's parse_fm_from_text() | Single parser surface; consistent failure modes. | ✓ |
| Lightweight grep for the four fields | Brittle on edge cases (multi-line strings, BOM, blank lines). | |

**User's choice:** Reuse parse_fm_from_text() (Recommended).

---

### Follow-up: examples/ path handling

| Option | Description | Selected |
|--------|-------------|----------|
| Skip path-level before frontmatter parse | Matches lint.sh EXCLUDE_DIRS = {maintenance, examples}. Cheaper. | ✓ |
| Honor `example: true` frontmatter in addition to path-prefix | More thorough but only matters if examples migrate out of examples/, which contradicts NEUT-04. | |

**User's choice:** Skip path-level before frontmatter parse (Recommended).
**Notes:** `example: true` is honored secondarily per AGENTS.md §5; in v1.1 the path filter handles all cases.

---

### Follow-up: Documentation anchor

| Option | Description | Selected |
|--------|-------------|----------|
| AGENTS.md §11.3 (CI mode block) | Phase 9-06 already established §11.3 as canonical for --strict / --ci policy. WGATE plugs in. | ✓ |
| docs/reference/write-gate.md as a new dedicated doc | Drift risk between AGENTS.md and the new doc. | |
| Both: §11.3 normative + ci.md operational | Discussed and effectively adopted: §11.3 normative; ci.md and CONTRIBUTING.md operationally link back. | (effectively chosen) |

**User's choice:** AGENTS.md §11.3 (Recommended).
**Notes:** ci.md and CONTRIBUTING.md still get operational sections — they LINK to §11.3 rather than restate the rules (Phase 9-06 contract).

---

## Claude's Discretion

- Exact wording of the AGENTS.md §11.3 `--staged` subsection (semantic content fixed by D-15; cadence and prose open).
- Exact wording of the `.githooks/pre-commit` footer line (must mention the three actionable paths; phrasing open).
- Whether `--staged` lives in a new helper function or is woven into existing `strict_check()` via a base-ref/diff-source parameter (planner's call).
- Whether to source `tests/phase-09/lib.sh` directly or extract shared helpers to `tests/lib/` (planner's call; both are precedented).
- Whether `bin/install-hooks.sh` prints a friendly note about the new gate (cosmetic; not contract).

## Deferred Ideas

- D-08 staged enforcement (added epistemic claims need a matching DR at commit time) — defer to a future phase if local quality regressions surface.
- `WGATE_SKIP=1` env var — rejected; revisit if granular skip is needed.
- Per-page `wgate_exempt: true` frontmatter — rejected; permanent bypass surface.
- `--pre-commit` shortcut flag — rejected in favor of orthogonal `--staged`.
- Auto-install hooks on first ingest/commit — rejected; revisit if contributor onboarding friction shows up.
- CI assertion that hooks are installed — rejected as scope-creep.
- `.githooks/pre-commit` `.d/` runner pattern — rejected as premature.
- Hook-customized verbose output — rejected; revisit if user feedback shows lint output is too terse.
- Distinct exit code for write-gate blocks — rejected; revisit if diagnostic tooling ever needs the distinction.
