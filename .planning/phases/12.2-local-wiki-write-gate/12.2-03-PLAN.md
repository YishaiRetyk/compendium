---
phase: 12.2-local-wiki-write-gate
plan: 03
type: execute
wave: 2
depends_on: [12.2-01]
files_modified:
  - .githooks/pre-commit
autonomous: true
requirements: [WGATE-01, WGATE-04]
must_haves:
  truths:
    - ".githooks/pre-commit invokes the write-gate AFTER the existing sync-claude check (D-07 hook ordering)"
    - "Existing sync-claude block is preserved unchanged (WGATE-04 / SC6)"
    - "The hook calls `bash bin/lint.sh --strict --staged --category provenance` (D-05 invocation)"
    - "On non-zero exit from the gate, the hook appends a single trailing footer line per D-08 (mentions the three actionable paths: add [prov:] markers, set type:source/decision, --no-verify)"
    - "Exit propagates: gate non-zero -> hook non-zero -> commit aborted"
    - "The set -euo pipefail block at the top is preserved (no regression on strictness)"
    - "The hook script remains pure bash and continues to require nothing beyond what was already required"
    - "bin/install-hooks.sh remains UNCHANGED (D-10 — opt-in via existing script)"
  artifacts:
    - path: ".githooks/pre-commit"
      provides: "Composed sync-claude + write-gate pre-commit hook"
      contains: "bin/lint.sh --strict --staged"
      min_lines: 14
  key_links:
    - from: ".githooks/pre-commit"
      to: "bin/lint.sh"
      via: "bash bin/lint.sh --strict --staged --category provenance"
      pattern: "bin/lint\\.sh.*--strict.*--staged.*--category provenance"
    - from: ".githooks/pre-commit"
      to: "bin/sync-claude.sh"
      via: "preserved-unchanged sync-claude --check block at the top of the hook (D-07 ordering)"
      pattern: "bin/sync-claude\\.sh --check"
    - from: "git pre-commit event"
      to: "exit-code propagation"
      via: "hook exits 1 if either sync-claude resync OR write-gate fires; exit 0 if both pass"
      pattern: "exit 1"
---

<objective>
Compose the write-gate into `.githooks/pre-commit`. The existing 11-line sync-claude block is preserved verbatim; the write-gate is appended AFTER it (D-07 ordering — sync-claude can mutate the staging area, must run first).

The hook invokes `bash bin/lint.sh --strict --staged --category provenance` (the production gate built in Plan 02) and, on non-zero exit, prints a single footer line listing the three actionable paths per D-08 (add [prov:] markers, set type:source/decision in frontmatter, or `git commit --no-verify` to bypass) before exiting 1.

`bin/install-hooks.sh` is NOT modified (D-10 — hooks remain explicit-opt-in).

Output: ~6-9 line addition to `.githooks/pre-commit`. No other files change. After this plan lands, Plan 01's `test_hook_ordering_sync_then_gate.sh` turns GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.2-local-wiki-write-gate/12.2-CONTEXT.md
@.planning/phases/12.2-local-wiki-write-gate/12.2-PATTERNS.md
@.planning/phases/12.2-local-wiki-write-gate/12.2-01-SUMMARY.md
@CLAUDE.md

# The file being modified
@.githooks/pre-commit

# The gate the hook will invoke (delivered by Plan 02 — already in place by Wave 2)
@bin/lint.sh

# The sync-claude script preserved unchanged
@bin/sync-claude.sh

# Tests this plan turns GREEN (specifically test_hook_ordering_sync_then_gate.sh)
@tests/phase-12.2/test_hook_ordering_sync_then_gate.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the write-gate block to .githooks/pre-commit</name>
  <files>
    .githooks/pre-commit
  </files>
  <read_first>
    - .githooks/pre-commit (current 11-line file in full)
    - bin/lint.sh (verify the `--strict --staged --category provenance` invocation works against the gate built in Plan 02)
    - bin/sync-claude.sh (read the `--check` mode contract; preserved unchanged)
    - .planning/phases/12.2-local-wiki-write-gate/12.2-CONTEXT.md D-07 (sync-claude first, then write-gate), D-08 (footer wording), D-09 (--no-verify only)
    - .planning/phases/12.2-local-wiki-write-gate/12.2-PATTERNS.md §2 (hook composition pattern + verbatim current file)
    - tests/phase-12.2/test_hook_ordering_sync_then_gate.sh (the test that must pass after this plan lands — read its assertions to confirm the footer text matches)
  </read_first>
  <action>
    Append the write-gate block to `.githooks/pre-commit`. The existing 11-line file is preserved verbatim; the new lines follow.

    **Final file shape** (lines 1-11 are the existing file; lines 13+ are new):

    ```bash
    #!/usr/bin/env bash
    # Pre-commit hook: enforce AGENTS.md <-> CLAUDE.md byte-equality (D-03)
    # AND run the local wiki write-gate over staged changes (Phase 12.2).
    set -euo pipefail

    if ! bash bin/sync-claude.sh --check 2>/dev/null; then
        echo "CLAUDE.md drift detected. Auto-syncing..." >&2
        bash bin/sync-claude.sh
        git add CLAUDE.md
        echo "CLAUDE.md resynced and re-staged. Re-run commit." >&2
        exit 1
    fi

    # Phase 12.2 / WGATE-01..04: local wiki write-gate over staged changes.
    # Reuses bin/lint.sh --strict policy via the additive --staged flag.
    # See AGENTS.md §11.3 for the normative contract (exemption ordering,
    # bypass, exit codes). Bypass via `git commit --no-verify` when needed.
    if ! bash bin/lint.sh --strict --staged --category provenance >&2; then
        echo "" >&2
        echo "Write-gate failed. Add [prov:source_id#locator] markers to the page, set type: source / type: decision in frontmatter if it's not a synthesized page, or run \`git commit --no-verify\` to bypass." >&2
        exit 1
    fi
    ```

    **Edits to make:**

    1. Update the header comment on line 2 from
       `# Pre-commit hook: enforce AGENTS.md <-> CLAUDE.md byte-equality (D-03).`
       to a two-line comment that names both responsibilities (line 2: existing comment without trailing period; line 3 new: `# AND run the local wiki write-gate over staged changes (Phase 12.2).`).
    2. After line 11 (the closing `fi` of the sync-claude block), insert a blank line followed by the new write-gate block (5 substantive lines + 1 leading comment block + 1 blank line).

    **Footer line wording (D-08, exact)** — the user-facing message that surfaces below lint's own findings:

    ```
    Write-gate failed. Add [prov:source_id#locator] markers to the page, set type: source / type: decision in frontmatter if it's not a synthesized page, or run `git commit --no-verify` to bypass.
    ```

    Three actionable paths per D-08, in this order:
    (a) add `[prov:source_id#locator]` markers (the primary fix),
    (b) set `type: source` / `type: decision` in frontmatter (the type-exemption escape),
    (c) `git commit --no-verify` (the bypass per D-09).

    **Critical: lint output is sent to stderr (`>&2`)** so the operator sees the per-page `error/provenance/<path>:` findings before the footer line. The footer line itself goes to stderr. The hook exits 1 if the gate is non-zero.

    **No bash if-else with command-substitution.** Use the simple `if ! cmd; then ... fi` pattern (mirrors the sync-claude block).

    **Per D-09, NO env-var bypass (no `WGATE_SKIP=1`)** and NO per-page frontmatter opt-out. The only bypass is git's `--no-verify` flag.

    **Per D-10, do NOT modify `bin/install-hooks.sh`.** Activation remains via `bash bin/install-hooks.sh` (existing script). Plan 04 documents activation in CONTRIBUTING.md and ci.md.

    **chmod +x preservation:** `.githooks/pre-commit` was already executable; do not change permissions.
  </action>
  <verify>
    <automated>
      cd "$(git rev-parse --show-toplevel)" && \
      test -x .githooks/pre-commit && \
      grep -c 'bin/sync-claude.sh --check' .githooks/pre-commit | grep -q '^1$' && \
      grep -c 'bin/lint.sh --strict --staged --category provenance' .githooks/pre-commit | grep -q '^1$' && \
      grep -q 'no-verify' .githooks/pre-commit && \
      grep -q 'prov:source_id#locator' .githooks/pre-commit && \
      grep -q 'type: source / type: decision' .githooks/pre-commit && \
      bash -n .githooks/pre-commit && \
      bash tests/phase-12.2/run.sh; rc=$? ; echo "Phase 12.2 aggregator exit: $rc (expect 11/11 GREEN after Plans 02 + 03)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `.githooks/pre-commit` is executable (`test -x .githooks/pre-commit` succeeds).
    - `grep -c 'bin/sync-claude.sh --check' .githooks/pre-commit` returns `1` (sync-claude block preserved exactly once).
    - `grep -c 'bin/lint.sh --strict --staged --category provenance' .githooks/pre-commit` returns `1` (gate invocation present exactly once).
    - The sync-claude block appears BEFORE the write-gate block in the file (verify with `awk '/sync-claude.sh --check/{s=NR} /lint\\.sh --strict --staged/{l=NR} END{exit !(s<l)}' .githooks/pre-commit`).
    - The footer line contains all three actionable paths: `[prov:source_id#locator]`, `type: source / type: decision`, `--no-verify`.
    - `bash -n .githooks/pre-commit` succeeds (no bash syntax errors).
    - `set -euo pipefail` is on line 4 (or wherever the existing file had it — preserved unchanged).
    - **Tests pass:** `bash tests/phase-12.2/run.sh` reports 11/11 (all 10 D-20 tests + the `test_staged_requires_strict.sh` D-05 test). Specifically `test_hook_ordering_sync_then_gate.sh` and `test_no_verify_bypass_smoke.sh` MUST be in the GREEN set.
    - The script does NOT introduce any new env-var dependencies (`grep -E '\\$\\{?WGATE' .githooks/pre-commit` returns no matches per D-09).
    - The hook continues to work for the existing CLAUDE.md drift case (regression check: `bash .githooks/pre-commit` against the current clean repo state should run both checks and exit 0).
  </acceptance_criteria>
  <done>
    `.githooks/pre-commit` composes the sync-claude check (preserved unchanged) with the new write-gate. Hook ordering is sync-claude first, write-gate second (D-07). The hook footer mentions all three actionable paths (D-08). Bypass is git's `--no-verify` only (D-09). All 11 Phase-12.2 tests pass. `bin/install-hooks.sh` is unchanged (D-10).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-commit hook process -> $REPO_ROOT/bin scripts | hook invokes `bin/sync-claude.sh` and `bin/lint.sh` via `bash`; both are repo-controlled scripts |
| --no-verify bypass | T4: intentional per D-09 — git's flag, documented in footer |
| set -euo pipefail propagation | shell options propagate from the hook's `set -euo pipefail` to the invoked bash subprocesses' own `set -euo pipefail` lines (defense in depth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.2-03-01 | Tampering | hook script integrity | accept | `.githooks/pre-commit` lives in the repo; modifications must go through git review like any other code. `core.hooksPath` is set by `bin/install-hooks.sh` — explicit opt-in. |
| T-12.2-03-02 | DoS | very-long lint runtime | accept | `bin/lint.sh --strict --staged --category provenance` is bounded: only iterates `git diff --cached --diff-filter=A` paths under 4 wiki dirs, parses frontmatter via PyYAML safe_load. Empirical bound: O(staged-page-count) — typically 1-5 pages per commit, ms-scale. |
| T-12.2-03-03 | Bypass | `git commit --no-verify` | accept | Intentional per D-09. Documented in footer line, ci.md, CONTRIBUTING.md, and AGENTS.md §11.3 (Plan 04). |
</threat_model>

<verification>
After Plan 03 lands:
1. `.githooks/pre-commit` has both blocks, sync-claude before write-gate.
2. `bash tests/phase-12.2/run.sh` reports 11/11.
3. `bash tests/phase-09/run.sh` still reports the same passing count (no regression on PR-mode tests).
4. The current repo's clean state still passes the pre-commit hook (sanity-check on regression to the existing flow).
5. `bash bin/install-hooks.sh && git -c core.hooksPath=.githooks status` continues to work as before.
</verification>

<success_criteria>
- Sync-claude block preserved unchanged.
- Write-gate appended after sync-claude (D-07 order).
- Hook invokes `bash bin/lint.sh --strict --staged --category provenance`.
- Hook footer line names the three actionable paths from D-08.
- Hook exits non-zero when the gate is non-zero.
- `bin/install-hooks.sh` is unchanged.
- All 11 Phase 12.2 tests pass.
- No new env-var bypass surface.
</success_criteria>

<output>
After completion, create `.planning/phases/12.2-local-wiki-write-gate/12.2-03-SUMMARY.md` documenting:
- Diff stats (lines added to .githooks/pre-commit).
- `bash tests/phase-12.2/run.sh` exit + 11/11 confirmation.
- Hook ordering verification (sync-claude before write-gate).
- Confirmation that bin/install-hooks.sh was NOT modified.
- Sanity check: hook against current repo state still exits 0 (no regression).
</output>
</content>
</invoke>