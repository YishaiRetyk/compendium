---
phase: 12.2-local-wiki-write-gate
plan: 04
type: execute
wave: 3
depends_on: [12.2-02, 12.2-03]
files_modified:
  - AGENTS.md
  - schema/AGENTS.template.md
  - docs/reference/ci.md
  - CONTRIBUTING.md
autonomous: true
requirements: [WGATE-01, WGATE-04]
must_haves:
  truths:
    - "AGENTS.md §11.3 is the normative source of truth for --staged semantics (D-16)"
    - "AGENTS.md §11.3 has a new --staged flag row in the orthogonal-flags table"
    - "AGENTS.md §11.3 has a new 'Staged-mode rules' subsection covering exemption ordering D-15, --no-verify bypass D-09, and the --staged-requires-strict D-05 rule"
    - "schema/AGENTS.template.md is byte-equal to AGENTS.md for §11.3 (D-18)"
    - "CLAUDE.md is byte-equal to AGENTS.md (auto-synced by .githooks/pre-commit; will refresh on commit)"
    - "docs/reference/ci.md gets a 'Local pre-commit write gate' section opening with a source-of-truth blockquote pointing at AGENTS.md §11.3 (D-17)"
    - "CONTRIBUTING.md gets a one-line install reminder + bypass note pointing at §11.3 / ci.md (D-17)"
    - "All examples in template-public files use abstract placeholders (CLAUDE.md §3) — NEVER real vault terms"
    - "The §11.3 source-of-truth blockquote is amended to extend the 'source of truth for' sentence with the --staged contract (e.g., '(e) the --staged mode contract')"
  artifacts:
    - path: "AGENTS.md"
      provides: "§11.3 amended with --staged flag row + Staged-mode rules subsection"
      contains: "--staged"
    - path: "schema/AGENTS.template.md"
      provides: "Byte-equal mirror of AGENTS.md §11.3"
      contains: "--staged"
    - path: "docs/reference/ci.md"
      provides: "Local pre-commit write gate section linking to AGENTS.md §11.3"
      contains: "Local pre-commit write gate"
    - path: "CONTRIBUTING.md"
      provides: "Install reminder + bypass pointer"
      contains: "install-hooks.sh"
  key_links:
    - from: "docs/reference/ci.md"
      to: "AGENTS.md §11.3"
      via: "source-of-truth blockquote at top of new section"
      pattern: "Source of truth.*AGENTS\\.md"
    - from: "CONTRIBUTING.md"
      to: "docs/reference/ci.md + AGENTS.md §11.3"
      via: "single-sentence install reminder pointing at both"
      pattern: "docs/reference/ci\\.md.*AGENTS\\.md"
    - from: "schema/AGENTS.template.md §11.3"
      to: "AGENTS.md §11.3"
      via: "byte-equal mirror (Phase 11-05 canonical-AGENTS.md fixture path enforces equality on canonical answers)"
      pattern: "--staged"
---

<objective>
Document the Phase 12.2 write-gate across the four template-public surfaces, with AGENTS.md §11.3 as the normative source of truth and ci.md / CONTRIBUTING.md linking back rather than restating (D-16, D-17). Mirror to schema/AGENTS.template.md for byte-equality (D-18). CLAUDE.md auto-syncs via the pre-commit hook (no direct edit needed).

Per CLAUDE.md §3, every example in these files uses abstract placeholders (`<concept-slug>`, `<source-id>`, `<page-title>`, etc.) — NEVER real vault terms. The `bin/check-neutrality.sh` denylist gate is a backstop, not the primary defense.

Output:
- AGENTS.md §11.3: 1 new flag row in the orthogonal-flags table + 1 new "Staged-mode rules" subsection (D-15 exemption ordering, D-09 bypass, D-05 --staged-requires-strict).
- schema/AGENTS.template.md: identical edit to §11.3 (byte-equal mirror).
- docs/reference/ci.md: 1 new "Local pre-commit write gate" section (with a source-of-truth blockquote, an install line, a bypass line, and a fix-the-blocked-commit list — links to §11.3, does NOT restate exemption rules).
- CONTRIBUTING.md: 1 new "Local pre-commit gate" subsection (single sentence with install command + bypass + link to §11.3 / ci.md).

The pre-commit hook auto-syncs CLAUDE.md from AGENTS.md on commit (via bin/sync-claude.sh).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.2-local-wiki-write-gate/12.2-CONTEXT.md
@.planning/phases/12.2-local-wiki-write-gate/12.2-PATTERNS.md
@.planning/phases/12.2-local-wiki-write-gate/12.2-02-SUMMARY.md
@.planning/phases/12.2-local-wiki-write-gate/12.2-03-SUMMARY.md
@CLAUDE.md

# Files being modified
@AGENTS.md
@schema/AGENTS.template.md
@docs/reference/ci.md
@CONTRIBUTING.md

# bin/sync-claude.sh — handles AGENTS.md ↔ CLAUDE.md byte-equality (referenced, not modified)
@bin/sync-claude.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend AGENTS.md §11.3 + mirror to schema/AGENTS.template.md (single semantic edit, applied to both files)</name>
  <files>
    AGENTS.md
    schema/AGENTS.template.md
  </files>
  <read_first>
    - AGENTS.md §11.3 (lines ~1039-1097, full read of the CI-mode block)
    - schema/AGENTS.template.md — find the §11.3 block (matches the AGENTS.md offset) via `grep -n '^#### CI mode' schema/AGENTS.template.md`
    - .planning/phases/12.2-local-wiki-write-gate/12.2-CONTEXT.md D-15 (verbatim exemption ordering), D-05, D-09, D-16, D-18
    - .planning/phases/12.2-local-wiki-write-gate/12.2-PATTERNS.md §3 (existing AGENTS.md §11.3 source-of-truth blockquote + flag-table format)
    - CLAUDE.md §3 (template-public placeholder rule — NEVER use real vault terms in §11.3 amendments)
  </read_first>
  <action>
    Apply the SAME diff to both AGENTS.md and schema/AGENTS.template.md. The §11.3 block is non-placeholder content, so both files contain identical text in that section. The Phase 11-05 canonical-AGENTS.md byte-equality fixture will catch drift between them on the next setup-parity CI run.

    **Edit 1: extend the source-of-truth blockquote.** Find the existing line:

    > **Source of truth for Phase 9 CI contracts.** This section is the authoritative specification for: (a) the severity-remap dispatch table, (b) the `--format json` output schema, (c) the escape-hatch marker contract, and (d) the `--require-version` semantics. Other docs (`docs/reference/ci.md`, `CONTRIBUTING.md`, `.github/workflows/lint.yml` comments) MUST link here rather than restating the policy. Drift between this section and the shipped code is a Phase 9 regression.

    Replace with (extending the "(d)" clause to add "(e)"):

    > **Source of truth for Phase 9 / Phase 12.2 CI + local-gate contracts.** This section is the authoritative specification for: (a) the severity-remap dispatch table, (b) the `--format json` output schema, (c) the escape-hatch marker contract, (d) the `--require-version` semantics, and (e) the `--staged` local-write-gate contract (Phase 12.2). Other docs (`docs/reference/ci.md`, `CONTRIBUTING.md`, `.github/workflows/lint.yml` comments) MUST link here rather than restating the policy. Drift between this section and the shipped code is a regression.

    **Edit 2: add a `--staged` row to the orthogonal-flags table.** Find the existing table starting:

    ```
    | Flag | Effect |
    |------|--------|
    | `--format json` | ... |
    | `--ci` | ... |
    | `--skip-category <cat>` | ... |
    | `--strict` | ... |
    | `--require-version X.Y.Z` | ... |
    | `--version` | ... |
    | `--count-skips` | ... |
    ```

    Add ONE new row AFTER the `--strict` row (so `--staged` sits next to its policy parent):

    ```
    | `--staged` | Phase 12.2 local-write-gate scope swap. With `--strict`, replaces the diff source from `git diff origin/main...HEAD` to `git diff --cached --name-only --diff-filter=A` and applies D-10 (new-page provenance) ONLY — D-08 (DR-match) stays CI-only. Files are read from the working tree, not from staged blobs. No-op without `--strict`. Used by `.githooks/pre-commit`. See "Staged-mode rules" below. |
    ```

    **Edit 3: add a new "Staged-mode rules" subsection.** Insert AFTER the existing "**CI workflow reference:**" line and BEFORE the "**Steps:**" line (i.e., between the Phase 9 escape-hatch / CI workflow content and the workflow-procedure list). The new subsection:

    ```markdown
    **Staged-mode rules (`--staged`, Phase 12.2):**

    `bin/lint.sh --staged` is the local-write-gate scope swap. The `.githooks/pre-commit` hook invokes `bash bin/lint.sh --strict --staged --category provenance` after the AGENTS.md ↔ CLAUDE.md sync check. Rules:

    1. **Requires `--strict`.** `--staged` is a no-op without `--strict` (no provenance enforcement; standard categories run as usual). The pre-commit hook always passes both flags together.
    2. **Diff source:** `git diff --cached --name-only --diff-filter=A` (status-A entries in the staged index). Files are read from the WORKING TREE, not from staged blobs — pre-commit hooks fire after `git add`, so working-tree content matches the index for the typical add-then-commit flow. If you `git add foo.md && echo extra >> foo.md && git commit`, the gate sees the dirty version (which already contains the staged content); known caveat, not a bug.
    3. **Scope:** D-10 (new-page provenance) ONLY. Pages staged as status-A under `wiki/{entities,concepts,overviews,comparisons}/` must contain at least one `[prov:` marker. D-08 (DR-match for added inferred/tentative claims) stays CI-only — not enforced at commit time.
    4. **Exemption ordering** (first match wins):
       1. Path NOT under `wiki/{entities,concepts,overviews,comparisons}/` — not gated.
       2. Path under `examples/` anywhere in the tree — not gated (path-prefix exemption, mirrors `EXCLUDE_DIRS` for full-lint).
       3. Frontmatter `type: source` — not gated (source pages are themselves the provenance anchors).
       4. Frontmatter `type: decision` — not gated (decision records are the gating mechanism, can't gate on themselves).
       5. Frontmatter `example: true` — not gated (reference content, anywhere in the tree).
       6. Frontmatter `bootstrap_stage: bootstrapped` — not gated (brownfield in-flight; provenance-bootstrap migration `02-provenance-bootstrap.sh` adds markers later).
    5. **`bootstrap_stage: verified` is NOT exempt.** Pages promoted through the brownfield 5-gate `verify --promote` flow are first-class wiki content from the gate's perspective — they must carry `[prov:]` markers like any other entity / concept / overview / comparison page.
    6. **Exit policy:** reuses `--strict`'s contract — exit 1 iff any post-remap error-severity finding exists. `provenance` already maps to `error` in the severity remap.
    7. **Bypass:** `git commit --no-verify` only. No `WGATE_SKIP=1` env var. No per-page `wgate_exempt: true` frontmatter (would create a permanent bypass surface defeating the gate's purpose). Per AGENTS.md §3, `--no-verify` is the operator's escape hatch — use rarely, document the reason in the commit message when used.
    8. **Hook activation:** `bash bin/install-hooks.sh` once per clone. The hook composes the existing AGENTS.md ↔ CLAUDE.md sync check (runs first; can re-stage CLAUDE.md) with the new write-gate (runs second; read-only over the staged index).

    **Failure UX:** when the gate blocks, `bin/lint.sh` prints per-page `error/provenance/<path>: new <type> page has zero [prov:...] markers (D-10; ...)` lines, and the hook appends a single trailing footer line listing the three actionable paths (add `[prov:source_id#locator]` markers, set `type: source` / `type: decision` in frontmatter if it's not a synthesized page, or `git commit --no-verify` to bypass).
    ```

    Use abstract placeholders only. The illustrative `<type>` and `<path>` in the failure-UX paragraph use angle-bracket placeholders, NOT real terms.

    **Edit 4: apply the SAME three edits to schema/AGENTS.template.md.** The §11.3 block is non-placeholder content; both files contain identical text in §11.3.

    The Phase 11-05 canonical-AGENTS.md byte-equality fixture is regenerated as part of the rendering pipeline; if the canonical fixture goes stale (`tests/phase-09.1/test_byte_equality.sh` or the setup-parity CI fails), regenerate it via the Phase 8-01 wizard render routine. Surface this in the SUMMARY if the regen step is required.

    **CLAUDE.md auto-syncs.** Do NOT manually edit CLAUDE.md. The `.githooks/pre-commit` `bin/sync-claude.sh --check` block detects AGENTS.md drift on the next `git commit` and re-stages CLAUDE.md automatically.

    **Pre-stage CLAUDE.md sync** (mirrors the Phase 09.1-02 precedent that landed the commit cleanly first try): immediately after editing AGENTS.md, run `bash bin/sync-claude.sh && git add CLAUDE.md` so the commit doesn't trigger an in-hook resync loop.
  </action>
  <verify>
    <automated>
      cd "$(git rev-parse --show-toplevel)" && \
      grep -c '^| `--staged` |' AGENTS.md | grep -q '^1$' && \
      grep -c '^| `--staged` |' schema/AGENTS.template.md | grep -q '^1$' && \
      grep -q 'Staged-mode rules' AGENTS.md && \
      grep -q 'Staged-mode rules' schema/AGENTS.template.md && \
      grep -q 'Phase 12.2 / Phase 12.2\|Phase 9 / Phase 12.2' AGENTS.md && \
      grep -q 'bootstrap_stage: bootstrapped' AGENTS.md && \
      grep -q 'bootstrap_stage: verified.*NOT exempt\|verified.*NOT exempt' AGENTS.md && \
      grep -q 'WGATE_SKIP\|wgate_exempt' AGENTS.md && \
      bash bin/sync-claude.sh --check 2>&1; rc=$? ; echo "sync-claude --check exit: $rc (expect non-zero pre-commit; sync-claude.sh resync on actual commit)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^| `--staged` |' AGENTS.md` returns `1` (single new flag row).
    - `grep -c '^| `--staged` |' schema/AGENTS.template.md` returns `1` (mirror).
    - The §11.3 source-of-truth blockquote contains the literal `(e) the` (extension to a 5th item).
    - Both files contain the literal substring `Staged-mode rules`.
    - Both files document the D-15 exemption ordering with all 6 numbered items.
    - Both files document `bootstrap_stage: verified` is NOT exempt (D-12).
    - Both files document `WGATE_SKIP` is rejected and `wgate_exempt` is rejected (anti-bypass paragraph).
    - Both files document `--staged` requires `--strict` (D-05).
    - `diff <(awk '/^#### CI mode/{p=1} p{print} /^### 11\\.4/{exit}' AGENTS.md) <(awk '/^#### CI mode/{p=1} p{print} /^### 11\\.4/{exit}' schema/AGENTS.template.md)` returns no output (byte-equal §11.3).
    - No real vault terms appear in the new content (verify with `grep -iE 'kahneman|loss-aversion|prospect-theory' AGENTS.md` returning no matches inside the new lines — use `git diff` scoped to the §11.3 region).
    - `bash -n` over modified files succeeds (sanity; AGENTS.md is markdown not bash, but this is a no-op safe check).
    - The pre-commit hook will recognize the drift between AGENTS.md and CLAUDE.md and resync; running `bash bin/sync-claude.sh && git add CLAUDE.md` after Edit 4 makes the commit clean first-try.
  </acceptance_criteria>
  <done>
    AGENTS.md §11.3 has the new --staged row + Staged-mode rules subsection. schema/AGENTS.template.md mirrors byte-equally. CLAUDE.md auto-resyncs via the pre-commit hook (or via `bash bin/sync-claude.sh && git add CLAUDE.md` pre-stage). All abstract placeholders; no real vault terms.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append "Local pre-commit write gate" to docs/reference/ci.md + "Local pre-commit gate" to CONTRIBUTING.md (link, do not restate)</name>
  <files>
    docs/reference/ci.md
    CONTRIBUTING.md
  </files>
  <read_first>
    - docs/reference/ci.md (full file — find the existing `## --strict mode (quality ratchet)` section and the section ordering)
    - CONTRIBUTING.md (full file — find the existing `### CI gate` subsection that this plan adds adjacent to)
    - .planning/phases/12.2-local-wiki-write-gate/12.2-CONTEXT.md D-17 (operational sections link to §11.3, do NOT restate)
    - .planning/phases/12.2-local-wiki-write-gate/12.2-PATTERNS.md §5 (ci.md operational shape) and §6 (CONTRIBUTING.md install reminder)
    - CLAUDE.md §3 (template-public file rule)
  </read_first>
  <action>
    **Edit 1: docs/reference/ci.md** — append a new top-level section AFTER the existing `## --strict mode (quality ratchet)` section (or at a natural section boundary near it). The new section:

    ```markdown
    ## Local pre-commit write gate (Phase 12.2)

    > **Source of truth:** The authoritative specification lives in [AGENTS.md §11.3 "CI mode"](../../AGENTS.md). This page reproduces the install + bypass commands for ergonomic reference — the rules themselves (exemption ordering, exit codes, scope) must stay in sync with §11.3. If you find a discrepancy, §11.3 wins and this page is the bug.

    `bin/lint.sh --staged` is a local-only scope swap that gates new synthesized wiki pages over the **staged index** (not `origin/main...HEAD`), so structurally invalid writes are caught BEFORE they land in local history. The CI `--strict` job remains the merge-time ratchet; this is the pre-commit-time complement.

    **What it fails on:**

    - A page staged as git-diff status `A` under `wiki/{entities,concepts,overviews,comparisons}/` with zero `[prov:...]` markers in the body. This is D-10 (new-page provenance) ONLY — D-08 (DR-match for added inferred/tentative claims) is enforced by the CI `strict` job, not the local gate.

    **What it does not fail on:** see the AGENTS.md §11.3 "Staged-mode rules" subsection for the full exemption ordering. Summary: pages outside the four required-types directories, anything under `examples/`, `type: source` or `type: decision` pages, `example: true`, and `bootstrap_stage: bootstrapped` are exempt; `bootstrap_stage: verified` is NOT exempt.

    **How to install:**

    ```sh
    bash bin/install-hooks.sh
    ```

    Activates `core.hooksPath=.githooks`, which composes the existing AGENTS.md ↔ CLAUDE.md sync check with the new write-gate.

    **How to bypass (rarely):**

    ```sh
    git commit --no-verify
    ```

    Per AGENTS.md §3, `--no-verify` is the only operator escape; document the reason in the commit message when used. There is no `WGATE_SKIP=1` env var and no per-page `wgate_exempt: true` frontmatter — those would create permanent bypass surfaces and are explicitly rejected.

    **How to fix a blocked commit:**

    1. Add `[prov:source_id#locator]` markers to the page body. This is the primary fix.
    2. If the page is a source summary or decision record (not a synthesized page), set `type: source` or `type: decision` in frontmatter — it should not have been gated; review the page type.
    3. As a last resort, `git commit --no-verify` to bypass and follow up with provenance markers in the next commit.
    ```

    **Edit 2: CONTRIBUTING.md** — append a new subsection AFTER the existing `### CI gate` subsection. The new subsection (a single sentence + cross-link, per D-17):

    ```markdown
    ### Local pre-commit gate

    Run `bash bin/install-hooks.sh` once per clone to activate the AGENTS.md ↔ CLAUDE.md sync check and the local wiki write-gate (Phase 12.2). The gate blocks new staged pages under `wiki/{entities,concepts,overviews,comparisons}/` that contain zero `[prov:]` markers; bypass with `git commit --no-verify` (rare, document the reason in the commit message). See [docs/reference/ci.md](docs/reference/ci.md) and [AGENTS.md §11.3](AGENTS.md) for the full contract (exemption ordering, exit codes, --staged-requires-strict).
    ```

    Both files use abstract placeholders (`source_id`, `locator`) — NEVER real vault terms.

    The ci.md and CONTRIBUTING.md sections do NOT restate the D-15 exemption ordering; they LINK to §11.3 (D-17 / Pattern D / Phase 9-06 contract).

    Per CLAUDE.md §3, ensure no `kahneman`, `loss-aversion`, `prospect-theory`, or other Kahneman-cluster terms appear. (Verify with `grep -iE 'kahneman|loss-aversion|prospect-theory' docs/reference/ci.md CONTRIBUTING.md`.)
  </action>
  <verify>
    <automated>
      cd "$(git rev-parse --show-toplevel)" && \
      grep -c '^## Local pre-commit write gate' docs/reference/ci.md | grep -q '^1$' && \
      grep -c '^### Local pre-commit gate' CONTRIBUTING.md | grep -q '^1$' && \
      grep -q 'Source of truth' docs/reference/ci.md && \
      grep -q 'AGENTS.md §11.3' docs/reference/ci.md && \
      grep -q 'install-hooks.sh' docs/reference/ci.md && \
      grep -q 'install-hooks.sh' CONTRIBUTING.md && \
      grep -q 'no-verify' docs/reference/ci.md && \
      grep -q 'no-verify' CONTRIBUTING.md && \
      ! grep -iE 'kahneman|loss-aversion|prospect-theory' docs/reference/ci.md CONTRIBUTING.md && \
      bash bin/check-neutrality.sh; rc=$?; echo "check-neutrality exit: $rc (expect 0 — abstract placeholders only)" && \
      bash bin/check-privacy.sh; rc=$?; echo "check-privacy exit: $rc (expect 0)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `docs/reference/ci.md` has a single `## Local pre-commit write gate (Phase 12.2)` section.
    - The new ci.md section opens with a `> **Source of truth:**` blockquote pointing at AGENTS.md §11.3.
    - The ci.md section does NOT restate the 6-item D-15 exemption list verbatim — instead summarizes 1-2 sentences and links to §11.3.
    - The ci.md section includes the literal command `bash bin/install-hooks.sh`.
    - The ci.md section includes the literal command `git commit --no-verify`.
    - `CONTRIBUTING.md` has a single `### Local pre-commit gate` subsection.
    - The CONTRIBUTING.md addition is approximately ONE PARAGRAPH (≤6 lines) and links to both `docs/reference/ci.md` and `AGENTS.md §11.3`.
    - Neither file contains `kahneman`, `loss-aversion`, `prospect-theory`, or other denylisted vault terms.
    - `bash bin/check-neutrality.sh` exits 0 on the live tree (no new neutrality violations introduced).
    - `bash bin/check-privacy.sh` exits 0 on the live tree (no `privacy: local_only` strings introduced into public paths).
    - `grep -c "Phase 12.2" docs/reference/ci.md` returns ≥ 1.
    - `grep -c "Phase 12.2" CONTRIBUTING.md` returns 0 or 1 (the phase reference is optional in CONTRIBUTING.md per Phase 9-06 spec-duplication-fix precedent — see STATE.md for prior art).
  </acceptance_criteria>
  <done>
    docs/reference/ci.md and CONTRIBUTING.md operationalize the write-gate (install command + bypass command + actionable fix list) by linking to AGENTS.md §11.3 as the normative source, NOT by restating the rules. Neutrality and privacy gates exit 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AGENTS.md ↔ CLAUDE.md sync | T5: docs drift across the byte-equality boundary — mitigated by `.githooks/pre-commit` running `bin/sync-claude.sh --check` before any commit |
| schema/AGENTS.template.md ↔ AGENTS.md byte-equality | T6: drift caught by the Phase 11-05 canonical-AGENTS.md fixture / setup-parity CI |
| docs/reference/ci.md and CONTRIBUTING.md ↔ AGENTS.md §11.3 spec-duplication | T7: drift caught by the Phase 9-06 spec-duplication test guard (links not restatements) |
| template-public file ↔ vault-private terms | T8: real vault terms accidentally inserted into AGENTS.md / CLAUDE.md / docs / CONTRIBUTING.md — mitigated by the `bin/check-neutrality.sh` denylist gate (NEUT-06/08) AND the CLAUDE.md §3 "use placeholders" rule |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.2-04-01 | Information disclosure | new docs content | mitigate | All examples use abstract placeholders (`<concept-slug>`, `source_id`, `locator`); verified by `grep` for known denylisted terms during verify step + `bash bin/check-neutrality.sh` exit 0 |
| T-12.2-04-02 | Tampering / drift | AGENTS.md vs schema/AGENTS.template.md | mitigate | Same diff applied to both files; setup-parity CI byte-equality fixture catches drift on next CI run |
| T-12.2-04-03 | Spec drift | docs/reference/ci.md restating §11.3 rules verbatim | mitigate | Section structure: source-of-truth blockquote at top + summary-and-link pattern (no full re-table); follows Phase 9-06 spec-duplication-fix precedent |
| T-12.2-04-04 | No executable surface added | this plan only modifies markdown content | accept | No code surface, no new bash/python; threat surface limited to docs accuracy |
</threat_model>

<verification>
After Plan 04 lands:
1. AGENTS.md §11.3 has the new `--staged` row + "Staged-mode rules" subsection.
2. schema/AGENTS.template.md mirrors §11.3 byte-equally.
3. CLAUDE.md auto-syncs on commit (or via `bash bin/sync-claude.sh && git add CLAUDE.md` pre-stage).
4. docs/reference/ci.md has a "Local pre-commit write gate" section linking to §11.3.
5. CONTRIBUTING.md has a "Local pre-commit gate" subsection linking to ci.md and §11.3.
6. `bash bin/check-neutrality.sh` exits 0.
7. `bash bin/check-privacy.sh` exits 0.
8. `bash tests/phase-12.2/run.sh` still reports 11/11 (no test regression).
9. `bash tests/phase-09/run.sh` still reports its current passing count (no Phase 9 regression).
10. `bash tests/phase-09.1/run.sh` still reports its current passing count (byte-equality fixture; may need regen if it fails — surface in SUMMARY).
</verification>

<success_criteria>
- AGENTS.md §11.3 amended with --staged row + Staged-mode rules subsection.
- schema/AGENTS.template.md byte-equally mirrored.
- docs/reference/ci.md has new "Local pre-commit write gate" section with source-of-truth blockquote.
- CONTRIBUTING.md has new "Local pre-commit gate" subsection.
- All examples use abstract placeholders (no Kahneman / personal terms).
- bin/check-neutrality.sh exits 0.
- bin/check-privacy.sh exits 0.
- All Phase 12.2 tests still pass (no regression).
- Operational docs LINK to §11.3 rather than restating rules.
</success_criteria>

<output>
After completion, create `.planning/phases/12.2-local-wiki-write-gate/12.2-04-SUMMARY.md` documenting:
- Diff stats per file (lines added/removed in AGENTS.md, schema/AGENTS.template.md, ci.md, CONTRIBUTING.md, CLAUDE.md auto-resync).
- `bash bin/check-neutrality.sh` exit code (must be 0).
- `bash bin/check-privacy.sh` exit code (must be 0).
- Whether the canonical-AGENTS.md fixture needed regeneration; if so, the regen command run.
- Confirmation that all Phase 12.2 + Phase 9 + Phase 9.1 tests still pass.
</output>
</content>
</invoke>