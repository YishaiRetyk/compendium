---
phase: 12.2-local-wiki-write-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase-12.2/run.sh
  - tests/phase-12.2/lib.sh
  - tests/phase-12.2/test_staged_blocks_concept_no_prov.sh
  - tests/phase-12.2/test_staged_passes_concept_with_prov.sh
  - tests/phase-12.2/test_staged_passes_modified_existing.sh
  - tests/phase-12.2/test_staged_exempts_type_source.sh
  - tests/phase-12.2/test_staged_exempts_type_decision.sh
  - tests/phase-12.2/test_staged_exempts_examples_path.sh
  - tests/phase-12.2/test_staged_exempts_bootstrap_bootstrapped.sh
  - tests/phase-12.2/test_staged_blocks_bootstrap_verified.sh
  - tests/phase-12.2/test_hook_ordering_sync_then_gate.sh
  - tests/phase-12.2/test_no_verify_bypass_smoke.sh
  - tests/phase-12.2/test_staged_requires_strict.sh
autonomous: true
requirements: [WGATE-01, WGATE-02, WGATE-03, WGATE-04]
must_haves:
  truths:
    - "Wave-0 RED test harness exists, mirroring tests/phase-09's shape"
    - "Aggregator emits the machine-parseable banner `PHASE 12.2 TESTS: N/M`"
    - "All 10 D-20 scenarios are encoded as separate test_*.sh files"
    - "Each test asserts the exact exit code D-20 specifies (per-scenario AC verifies that contract via grep)"
    - "Tests are origin-independent (do not call seed_origin_main_ref); staged-mode is by definition not PR-mode"
    - "Tests fail today (mostly RED — Plan 02 turns them GREEN) and the smoke test (--no-verify, hook absent) passes"
  artifacts:
    - path: "tests/phase-12.2/run.sh"
      provides: "Per-phase aggregator emitting `PHASE 12.2 TESTS: N/M`"
      contains: "PHASE 12.2 TESTS:"
    - path: "tests/phase-12.2/lib.sh"
      provides: "Helpers: make_bare_repo, setup_git_author, assert_exit_code, cleanup_fixture_repo, write_page (no seed_origin_main_ref — staged-mode is origin-independent per D-19)"
      contains: "make_bare_repo"
    - path: "tests/phase-12.2/test_staged_blocks_concept_no_prov.sh"
      provides: "D-20 scenario 1: greenfield wiki/concepts/foo.md, status A, zero [prov:] -> exit 1"
    - path: "tests/phase-12.2/test_staged_passes_concept_with_prov.sh"
      provides: "D-20 scenario 2: greenfield wiki/concepts/foo.md with [prov:src#sec:x] -> exit 0"
    - path: "tests/phase-12.2/test_staged_passes_modified_existing.sh"
      provides: "D-20 scenario 3: status M (modified) without [prov:] -> exit 0 (only status-A pages gate)"
    - path: "tests/phase-12.2/test_staged_exempts_type_source.sh"
      provides: "D-20 scenario 4: type: source + SOURCE_EXTRA_FIELDS, zero [prov:] -> exit 0"
    - path: "tests/phase-12.2/test_staged_exempts_type_decision.sh"
      provides: "D-20 scenario 5: wiki/decisions/dr-X.md with type: decision -> exit 0"
    - path: "tests/phase-12.2/test_staged_exempts_examples_path.sh"
      provides: "D-20 scenario 6: examples/.../concepts/foo.md, zero [prov:] -> exit 0"
    - path: "tests/phase-12.2/test_staged_exempts_bootstrap_bootstrapped.sh"
      provides: "D-20 scenario 7: bootstrap_stage: bootstrapped + zero [prov:] -> exit 0"
    - path: "tests/phase-12.2/test_staged_blocks_bootstrap_verified.sh"
      provides: "D-20 scenario 8: bootstrap_stage: verified + zero [prov:] -> exit 1 (verified is NOT exempt per D-12)"
    - path: "tests/phase-12.2/test_hook_ordering_sync_then_gate.sh"
      provides: "D-20 scenario 9: pre-commit ordering — sync-claude resyncs first, write-gate fires after"
    - path: "tests/phase-12.2/test_no_verify_bypass_smoke.sh"
      provides: "D-20 scenario 10: blocked commit + git commit --no-verify -> succeeds (sanity check on git's flag)"
    - path: "tests/phase-12.2/test_staged_requires_strict.sh"
      provides: "D-05 contract: --staged without --strict no-ops or short-errors with usage hint"
  key_links:
    - from: "tests/phase-12.2/lib.sh"
      to: "REPO_ROOT/bin/lint.sh"
      via: "tests invoke `bash \"$REPO_ROOT/bin/lint.sh\" --strict --staged --category provenance` against fixture working trees"
      pattern: "bin/lint.sh.*--strict.*--staged"
    - from: "tests/phase-12.2/test_hook_ordering_sync_then_gate.sh"
      to: ".githooks/pre-commit"
      via: "test invokes the hook via `git -c core.hooksPath=.githooks commit ...` after `git clone \"$REPO_ROOT\" \"$tmp\"` — distinct strategy from tests 1-8 (mktemp + git init)"
      pattern: "core.hooksPath=.githooks|\\.githooks/pre-commit"
    - from: "tests/phase-12.2/run.sh"
      to: "tests/phase-12.2/test_*.sh"
      via: "shopt -s nullglob loop with PASS/FAIL tally; banner `PHASE 12.2 TESTS: ${PASS}/${TOTAL}`"
      pattern: "PHASE 12\\.2 TESTS"
---

<objective>
Author the Wave-0 RED test harness for Phase 12.2 BEFORE the implementation lands. The harness mirrors tests/phase-09 exactly: a `run.sh` aggregator emitting `PHASE 12.2 TESTS: N/M`, a `lib.sh` with the make_bare_repo / setup_git_author / assert_exit_code / cleanup_fixture_repo / write_page subset of phase-09's helpers (no seed_origin_main_ref — staged-mode is origin-independent per D-19), and 11 test_*.sh files (the 10 D-20 scenarios + a `--staged requires --strict` D-05 contract test).

Per checker feedback (revision 1): split into TWO tasks — (Task 1) test scaffolding only (aggregator + lib + directory) so the harness boots without scenarios present; (Task 2) the 11 per-scenario test files with PER-SCENARIO acceptance criteria that grep each test for the correct exit-code contract. The split exposes Test #9's distinct fixture strategy (`git clone "$REPO_ROOT" "$tmp"` vs mktemp+init) as a separable verify point.

Purpose: lock the contract before any code change. Plans 02 and 03 turn this RED suite GREEN by extending bin/lint.sh and the pre-commit hook. The aggregator banner is the machine-parseable evidence Plan 05's VERIFICATION.md cites.

Output: 13 new files under tests/phase-12.2/ — 1 aggregator + 1 lib + 11 tests. No production code is modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.2-local-wiki-write-gate/12.2-CONTEXT.md
@.planning/phases/12.2-local-wiki-write-gate/12.2-PATTERNS.md
@CLAUDE.md

# Test analogs (the EXACT shape to copy)
@tests/phase-09/run.sh
@tests/phase-09/lib.sh
@tests/phase-09/test_lint_strict_new_page.sh
@tests/phase-12.1/run.sh

# The integration target (read-only here, modified in Plan 02)
@bin/lint.sh
@.githooks/pre-commit

<interfaces>
<!-- Helpers that tests/phase-12.2/lib.sh must export (verbatim signatures from tests/phase-09/lib.sh, minus seed_origin_main_ref). -->

```bash
# tests/phase-09/lib.sh exports (Phase 12.2 reuses these four):
REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"

# Phase 12.2 SUBSTITUTES make_fixture_repo with a simpler make_bare_repo
# (no fixtures/ directory needed — each test stages its own page-shape inline).
make_bare_repo() {
    local tmp
    tmp="$(mktemp -d -t phase12-2-XXXXXX)"
    (cd "$tmp" && git init -q -b main && \
        git config user.email "fixture@example.com" && \
        git config user.name "Fixture" && \
        git -c commit.gpgsign=false commit -q --allow-empty -m "seed")
    echo "$tmp"
}

setup_git_author() {
    local repo="$1" name="$2" email="$3"
    # ... sets git config user.name/user.email + commits a unique-name seed file
}

assert_exit_code() {
    local expected="$1" actual="$2" desc="$3"
    # ... echoes FAIL on mismatch and returns 1
}

cleanup_fixture_repo() {
    local path="$1"
    # ... safe rm -rf of mktemp dir (asserts /tmp/* prefix)
}

write_page() {
    local repo="$1" relpath="$2"
    # ... reads body from stdin, mkdir -p the parent, writes to "$repo/$relpath"
}
```

For Phase 12.2, copy the four phase-09 helpers verbatim into tests/phase-12.2/lib.sh with REPO_ROOT/path adjustments per PATTERNS.md §9 (recommended-default — keeps each phase's test surface self-contained). Phase 12.2 does NOT need fixture directories — tests build fixture content inline via `make_bare_repo` + `write_page`.

Test #9 (`test_hook_ordering_sync_then_gate.sh`) is materially different: it needs the LIVE `.githooks/pre-commit`, `bin/sync-claude.sh`, and `bin/lint.sh` available in the fixture. The recommended approach is `git clone "$REPO_ROOT" "$tmp"` (vs mktemp + git init for tests 1-8). This fixture-creation difference is reflected in Task 2's per-scenario AC (grep for `git clone` and `core.hooksPath=.githooks`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the test directory + aggregator + lib (scaffolding only)</name>
  <files>
    tests/phase-12.2/run.sh
    tests/phase-12.2/lib.sh
  </files>
  <read_first>
    - tests/phase-09/run.sh (EXACT aggregator shape — copy verbatim with `09` -> `12.2` substitutions)
    - tests/phase-09/lib.sh (EXACT helper shape — copy the four helpers minus seed_origin_main_ref)
    - tests/phase-12.1/run.sh (most recent aggregator copy showing `PHASE 12.1 TESTS: N/M` pattern)
    - .planning/phases/12.2-local-wiki-write-gate/12.2-PATTERNS.md §8, §9 (verbatim test-scaffolding patterns)
  </read_first>
  <action>
    Create the test directory, aggregator, and helper library — no scenario files yet (those land in Task 2).

    **`tests/phase-12.2/run.sh`** — copy `tests/phase-09/run.sh` (49 lines) verbatim, substituting `09` -> `12.2` in:
    - the `# tests/phase-09/run.sh` header comment
    - the `Usage: tests/phase-09/run.sh` heredoc
    - the `PHASE 09 TESTS:` banner (-> `PHASE 12.2 TESTS:`)

    **`tests/phase-12.2/lib.sh`** — copy approach per PATTERNS.md §9:
    - Header: `#!/usr/bin/env bash` + comment `# tests/phase-12.2/lib.sh -- Phase 12.2 test helpers (subset of tests/phase-09/lib.sh — no seed_origin_main_ref since staged-mode is origin-independent per D-19).`
    - `set -euo pipefail`
    - `REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"` (unchanged — still 2 levels up)
    - Define a `make_bare_repo()` helper (inline-mktemp variant — no fixtures dir needed):
      ```bash
      make_bare_repo() {
          local tmp
          tmp="$(mktemp -d -t phase12-2-XXXXXX)"
          (cd "$tmp" && git init -q -b main && \
              git config user.email "fixture@example.com" && \
              git config user.name "Fixture" && \
              git -c commit.gpgsign=false commit -q --allow-empty -m "seed")
          echo "$tmp"
      }
      ```
    - Copy `setup_git_author()` verbatim from tests/phase-09/lib.sh lines ~31-45 (per-call unique filename via email-slug + nanoseconds + RANDOM)
    - Copy `assert_exit_code()` verbatim from tests/phase-09/lib.sh
    - Copy `cleanup_fixture_repo()` verbatim from tests/phase-09/lib.sh (the `[[ "$path" == /tmp/* ]]` safety check is load-bearing — keep it)
    - Define a small helper `write_page()` for test ergonomics:
      ```bash
      write_page() {
          local repo="$1" relpath="$2"
          local dir="$repo/$(dirname "$relpath")"
          mkdir -p "$dir"
          # Body is read from stdin (heredoc).
          cat > "$repo/$relpath"
      }
      ```

    **`chmod +x`** on both files: `chmod +x tests/phase-12.2/run.sh tests/phase-12.2/lib.sh`.

    The aggregator MUST run cleanly when no test files exist yet — phase-09's `shopt -s nullglob` glob-loop pattern handles the empty case (loop body skipped, banner emits `PHASE 12.2 TESTS: 0/0`).
  </action>
  <verify>
    <automated>
      cd "$(git rev-parse --show-toplevel)" && \
      test -f tests/phase-12.2/run.sh && \
      test -f tests/phase-12.2/lib.sh && \
      test -x tests/phase-12.2/run.sh && \
      test -x tests/phase-12.2/lib.sh && \
      grep -q 'PHASE 12.2 TESTS:' tests/phase-12.2/run.sh && \
      grep -q 'make_bare_repo' tests/phase-12.2/lib.sh && \
      grep -q 'setup_git_author' tests/phase-12.2/lib.sh && \
      grep -q 'assert_exit_code' tests/phase-12.2/lib.sh && \
      grep -q 'cleanup_fixture_repo' tests/phase-12.2/lib.sh && \
      grep -q 'write_page' tests/phase-12.2/lib.sh && \
      ! grep -q 'seed_origin_main_ref' tests/phase-12.2/lib.sh && \
      bash -n tests/phase-12.2/run.sh && \
      bash -n tests/phase-12.2/lib.sh && \
      bash tests/phase-12.2/run.sh; rc=$? ; echo "aggregator exit: $rc (expect 0 with banner '0/0' since no test files exist yet)"
    </automated>
  </verify>
  <acceptance_criteria>
    - File `tests/phase-12.2/run.sh` exists and is executable.
    - File `tests/phase-12.2/lib.sh` exists and is executable.
    - `bash -n tests/phase-12.2/run.sh` returns 0 (no syntax errors).
    - `bash -n tests/phase-12.2/lib.sh` returns 0 (no syntax errors).
    - `grep -q 'PHASE 12.2 TESTS:' tests/phase-12.2/run.sh` succeeds.
    - `grep -q 'make_bare_repo' tests/phase-12.2/lib.sh` succeeds.
    - `grep -q 'setup_git_author' tests/phase-12.2/lib.sh` succeeds.
    - `grep -q 'assert_exit_code' tests/phase-12.2/lib.sh` succeeds.
    - `grep -q 'cleanup_fixture_repo' tests/phase-12.2/lib.sh` succeeds.
    - `grep -q 'write_page' tests/phase-12.2/lib.sh` succeeds.
    - `! grep -q 'seed_origin_main_ref' tests/phase-12.2/lib.sh` succeeds (omission per D-19).
    - `bash tests/phase-12.2/run.sh` exits 0 (no scenario files yet) and emits `PHASE 12.2 TESTS: 0/0` (the empty-glob banner).
  </acceptance_criteria>
  <done>
    Test directory scaffolding lands. Aggregator runs cleanly without scenario files. Helpers exported. Task 2 builds the 11 scenario tests on top.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create the 11 per-scenario test files (10 D-20 scenarios + D-05 contract)</name>
  <files>
    tests/phase-12.2/test_staged_blocks_concept_no_prov.sh
    tests/phase-12.2/test_staged_passes_concept_with_prov.sh
    tests/phase-12.2/test_staged_passes_modified_existing.sh
    tests/phase-12.2/test_staged_exempts_type_source.sh
    tests/phase-12.2/test_staged_exempts_type_decision.sh
    tests/phase-12.2/test_staged_exempts_examples_path.sh
    tests/phase-12.2/test_staged_exempts_bootstrap_bootstrapped.sh
    tests/phase-12.2/test_staged_blocks_bootstrap_verified.sh
    tests/phase-12.2/test_hook_ordering_sync_then_gate.sh
    tests/phase-12.2/test_no_verify_bypass_smoke.sh
    tests/phase-12.2/test_staged_requires_strict.sh
  </files>
  <read_first>
    - tests/phase-09/test_lint_strict_new_page.sh (closest-shape strict-mode test analog)
    - .planning/phases/12.2-local-wiki-write-gate/12.2-CONTEXT.md (D-20 scenario list — 10 scenarios; D-05 — `--staged` requires `--strict`)
    - .planning/phases/12.2-local-wiki-write-gate/12.2-PATTERNS.md §10 (verbatim test-scenario patterns + frontmatter mutation pattern)
    - bin/lint.sh lines 511-533 (strict_new_pages — to understand what status-A means in the staged variant)
    - bin/lint.sh lines 354 (VALID_TYPES) and 360-366 (BASE_FIELDS, SOURCE_EXTRA_FIELDS) — fixture pages MUST satisfy the YAML schema or unrelated `yaml`-category failures will mask provenance findings
  </read_first>
  <action>
    Create exactly 11 `test_*.sh` files. Each test:
    - starts with `#!/usr/bin/env bash` + `set -euo pipefail`
    - sources `lib.sh` from the same directory
    - creates a fixture via `make_bare_repo` (or `git clone "$REPO_ROOT" "$tmp"` for the hook-ordering test)
    - traps `cleanup_fixture_repo "$REPO" EXIT`
    - stages the page-under-test via `git add`
    - invokes the gate (or hook) and captures rc with `set +e ; rc=$? ; set -e`
    - asserts exit code via `assert_exit_code <expected> <rc> "<desc>"`
    - mirrors the `/tmp/wgate-out.$$ /tmp/wgate-err.$$` stdout/stderr capture pattern from PATTERNS.md §10

    **`test_staged_blocks_concept_no_prov.sh`** (D-20 #1 — greenfield concept, status A, zero `[prov:]` -> exit 1):
    ```bash
    #!/usr/bin/env bash
    # WGATE-02 / D-20 scenario 1: greenfield wiki/concepts/foo.md, status A,
    # zero [prov:] -> bin/lint.sh --strict --staged --category provenance exits 1.
    set -euo pipefail
    SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
    source "$SCRIPT_DIR/lib.sh"

    REPO="$(make_bare_repo)"
    trap 'cleanup_fixture_repo "$REPO"' EXIT

    write_page "$REPO" "wiki/concepts/foo.md" <<'EOF'
    ---
    id: foo
    title: "Foo"
    type: concept
    status: active
    summary: "Test concept with no provenance."
    created_at: 2026-05-04
    updated_at: 2026-05-04
    sources: []
    epistemic_status: tentative
    tags: []
    domains: []
    supersedes:
    superseded_by:
    privacy: cloud_safe
    aliases: []
    has_contradictions: false
    knowledge_domain: ""
    ---

    Foo concept body without any provenance markers.
    EOF
    (cd "$REPO" && git add wiki/concepts/foo.md)

    set +e
    (cd "$REPO" && bash "$REPO_ROOT/bin/lint.sh" --strict --staged --category provenance "$REPO/wiki/" >/tmp/wgate-out.$$ 2>/tmp/wgate-err.$$)
    rc=$?
    set -e

    if ! assert_exit_code 1 "$rc" "blocked: greenfield concept without [prov:]"; then
        cat /tmp/wgate-out.$$ /tmp/wgate-err.$$ >&2
        rm -f /tmp/wgate-out.$$ /tmp/wgate-err.$$
        exit 1
    fi
    if ! grep -qi "prov" /tmp/wgate-out.$$ /tmp/wgate-err.$$; then
        echo "FAIL: error output should mention provenance" >&2
        cat /tmp/wgate-out.$$ /tmp/wgate-err.$$ >&2
        rm -f /tmp/wgate-out.$$ /tmp/wgate-err.$$
        exit 1
    fi
    rm -f /tmp/wgate-out.$$ /tmp/wgate-err.$$
    echo "PASS: blocked: greenfield concept without [prov:]"
    ```

    **`test_staged_passes_concept_with_prov.sh`** (D-20 #2 — same shape but inject `[prov:src-2026-04-15-x#sec:intro|direct|2026-05-04]` in the body; assert exit 0).

    **`test_staged_passes_modified_existing.sh`** (D-20 #3 — write `wiki/concepts/foo.md` to working tree, `git add`, commit so it's part of seed history. Then modify the body (delete a line, do NOT add a `[prov:]`), `git add`, run gate. Assert exit 0 because status is M not A.).

    **`test_staged_exempts_type_source.sh`** (D-20 #4 — page at `wiki/sources/foo.md` with `type: source` plus the SOURCE_EXTRA_FIELDS frontmatter (path, content_hash, ingested_at, source_type, compilation_status), zero `[prov:]`. Stage it. Assert exit 0.). Use the SOURCE_EXTRA_FIELDS from PATTERNS.md §10 frontmatter-mutation pattern. The frontmatter MUST literally contain `type: source`.

    **`test_staged_exempts_type_decision.sh`** (D-20 #5 — page at `wiki/decisions/dr-2026-05-04-test.md` with `type: decision`, `trigger_type: schema-update`, `affected_pages: []`, zero `[prov:]`. Stage it. Assert exit 0.). The path itself is under `wiki/decisions/`, NOT under one of the four PROVENANCE_REQUIRED_TYPES dirs, so this scenario also confirms the path-prefix gate before the type-frontmatter gate. The frontmatter MUST literally contain `type: decision`.

    **`test_staged_exempts_examples_path.sh`** (D-20 #6 — page at `examples/example-domain/concepts/foo.md` with `type: concept`, zero `[prov:]`. Stage it. Assert exit 0.). The page MUST go under `examples/` anywhere in the tree (D-14 path-prefix exemption). Use a placeholder slug `examples/example-domain/concepts/foo.md` — DO NOT use a real domain term per CLAUDE.md §3 (template-public file rule applies to test fixtures shipped to public paths). The test path string MUST contain the literal substring `examples/`.

    **`test_staged_exempts_bootstrap_bootstrapped.sh`** (D-20 #7 — page at `wiki/concepts/foo.md` with `bootstrap_stage: bootstrapped` in frontmatter, zero `[prov:]`. Assert exit 0.). The frontmatter MUST literally contain `bootstrap_stage: bootstrapped`.

    **`test_staged_blocks_bootstrap_verified.sh`** (D-20 #8 — same shape but `bootstrap_stage: verified`, zero `[prov:]`. Assert exit 1. Per D-12, verified is NOT exempt.). Also assert error output mentions provenance. The frontmatter MUST literally contain `bootstrap_stage: verified`.

    **`test_hook_ordering_sync_then_gate.sh`** (D-20 #9 — composes Plans 02 and 03; runs the actual `.githooks/pre-commit` script):
    Approach: use the LIVE `.githooks/pre-commit` from the repo (`$REPO_ROOT/.githooks/pre-commit`). Recommended approach: clone REPO_ROOT into a throwaway temp dir using `git clone "$REPO_ROOT" "$tmp"` so `bin/`, `.githooks/`, `AGENTS.md`, `CLAUDE.md` are all present. This is materially different from tests 1-8 which use `make_bare_repo` (mktemp + git init).
    Test stages: (a) introduce CLAUDE.md drift (e.g., delete a single line in CLAUDE.md), (b) stage a `wiki/concepts/foo.md` greenfield page with no `[prov:]`, (c) commit via `git -c core.hooksPath=.githooks commit -m "..."` and assert exit 1 with sync-claude message, (d) re-run commit (now sync-claude is clean) and assert exit 1 with the write-gate footer line, (e) add `[prov:src-X#sec:y]` to the page, re-stage, re-commit, assert exit 0.
    The test MUST contain the literal strings `git clone` (or `git clone "$REPO_ROOT"`), `core.hooksPath=.githooks`, and `sync-claude` (referenced in the assertion that sync-claude fires first).

    **`test_no_verify_bypass_smoke.sh`** (D-20 #10 — same setup as #9 stage (b), commit with `git -c core.hooksPath=.githooks commit --no-verify -m "..."`, assert exit 0). This is a sanity-check on git's own `--no-verify` flag (D-09), not new code. The test MUST contain the literal substring `--no-verify`.

    **`test_staged_requires_strict.sh`** (D-05 — `--staged` without `--strict` either short-errors or no-ops):
    Run `bash "$REPO_ROOT/bin/lint.sh" --staged --category provenance` (no `--strict`). Per D-05, this either prints a usage hint to stderr and exits non-zero, OR no-ops (does nothing) and exits 0. Recommended assertion: stage a wiki/concepts/foo.md with zero `[prov:]`, run `--staged` WITHOUT `--strict`, then assert that the gate did NOT fire (exit 0 — i.e., no `provenance` finding emitted at error severity). This is the "no-op without strict" contract.

    **All tests use the `set +e ; rc=$? ; set -e` idiom** so the test harness can assert a non-zero exit without aborting the script. Mirror the `/tmp/wgate-out.$$ /tmp/wgate-err.$$` pattern from PATTERNS.md §10 for stdout/stderr capture.

    **chmod +x on every file** — `chmod +x tests/phase-12.2/test_*.sh`.

    **Per CLAUDE.md §3 template-public rules:** use `example-domain`, `foo`, `bar`, `dr-2026-05-04-test` — abstract placeholders. NEVER use real vault terms.
  </action>
  <verify>
    <automated>
      cd "$(git rev-parse --show-toplevel)" && \
      [ "$(ls tests/phase-12.2/test_*.sh 2>/dev/null | wc -l)" = "11" ] && \
      for t in tests/phase-12.2/test_*.sh; do test -x "$t" || { echo "NOT EXECUTABLE: $t"; exit 1; }; bash -n "$t" || { echo "SYNTAX ERROR: $t"; exit 1; }; done && \
      bash tests/phase-12.2/run.sh; rc=$? ; echo "aggregator exit: $rc (RED expected today; Plan 02 turns most tests GREEN)"
    </automated>
  </verify>
  <acceptance_criteria>
    Per-scenario AC (each row asserts the test ENCODES the correct D-20 exit-code expectation):

    - `[ "$(ls tests/phase-12.2/test_*.sh | wc -l)" = "11" ]` — exactly 11 scenario files exist.
    - **D-20 #1 (block):** `grep -q 'assert_exit_code 1' tests/phase-12.2/test_staged_blocks_concept_no_prov.sh` succeeds.
    - **D-20 #2 (pass with prov):** `grep -q 'assert_exit_code 0' tests/phase-12.2/test_staged_passes_concept_with_prov.sh` AND `grep -q '\[prov:' tests/phase-12.2/test_staged_passes_concept_with_prov.sh` both succeed.
    - **D-20 #3 (modified, not added):** `grep -q 'assert_exit_code 0' tests/phase-12.2/test_staged_passes_modified_existing.sh` succeeds.
    - **D-20 #4 (type:source exempt):** `grep -q 'type: source' tests/phase-12.2/test_staged_exempts_type_source.sh` AND `grep -q 'assert_exit_code 0' tests/phase-12.2/test_staged_exempts_type_source.sh` both succeed.
    - **D-20 #5 (type:decision exempt):** `grep -q 'type: decision' tests/phase-12.2/test_staged_exempts_type_decision.sh` AND `grep -q 'assert_exit_code 0' tests/phase-12.2/test_staged_exempts_type_decision.sh` both succeed.
    - **D-20 #6 (examples/ path exempt):** `grep -q 'examples/' tests/phase-12.2/test_staged_exempts_examples_path.sh` AND `grep -q 'assert_exit_code 0' tests/phase-12.2/test_staged_exempts_examples_path.sh` both succeed.
    - **D-20 #7 (bootstrap_stage:bootstrapped exempt):** `grep -q 'bootstrap_stage: bootstrapped' tests/phase-12.2/test_staged_exempts_bootstrap_bootstrapped.sh` AND `grep -q 'assert_exit_code 0' tests/phase-12.2/test_staged_exempts_bootstrap_bootstrapped.sh` both succeed.
    - **D-20 #8 (bootstrap_stage:verified NOT exempt):** `grep -q 'bootstrap_stage: verified' tests/phase-12.2/test_staged_blocks_bootstrap_verified.sh` AND `grep -q 'assert_exit_code 1' tests/phase-12.2/test_staged_blocks_bootstrap_verified.sh` both succeed.
    - **D-20 #9 (hook ordering):** `grep -q 'sync-claude' tests/phase-12.2/test_hook_ordering_sync_then_gate.sh` AND `grep -q 'assert_exit_code 1' tests/phase-12.2/test_hook_ordering_sync_then_gate.sh` AND `grep -q 'core.hooksPath=.githooks' tests/phase-12.2/test_hook_ordering_sync_then_gate.sh` AND `grep -q 'git clone' tests/phase-12.2/test_hook_ordering_sync_then_gate.sh` all succeed.
    - **D-20 #10 (no-verify bypass smoke):** `grep -q -- '--no-verify' tests/phase-12.2/test_no_verify_bypass_smoke.sh` AND `grep -q 'assert_exit_code 0' tests/phase-12.2/test_no_verify_bypass_smoke.sh` both succeed.
    - **D-05 contract:** `grep -q 'staged' tests/phase-12.2/test_staged_requires_strict.sh` AND (`grep -q 'assert_exit_code 0' tests/phase-12.2/test_staged_requires_strict.sh` OR `grep -q 'assert_exit_code 1' tests/phase-12.2/test_staged_requires_strict.sh`) succeeds (the test accepts either no-op exit 0 OR usage-error exit 1 per D-05).
    - Each `test_*.sh` file starts with `#!/usr/bin/env bash` and `set -euo pipefail`. Verify: `for t in tests/phase-12.2/test_*.sh; do head -1 "$t" | grep -q '^#!/usr/bin/env bash' && grep -q 'set -euo pipefail' "$t" || exit 1; done`.
    - Each `test_*.sh` file is executable: `for t in tests/phase-12.2/test_*.sh; do test -x "$t" || exit 1; done`.
    - Each `test_*.sh` file passes `bash -n` syntax check: `for t in tests/phase-12.2/test_*.sh; do bash -n "$t" || exit 1; done`.
    - Each scenario test (1-10) plus the contract test references `bin/lint.sh --strict --staged` OR (for `test_no_verify_bypass_smoke.sh` and `test_hook_ordering_sync_then_gate.sh`) references `core.hooksPath=.githooks`.
    - No test fixture file uses real personal-vault terms or real example-cluster slugs (CLAUDE.md §3). Use abstract placeholders only. Verify: `! grep -rE '(kahneman|tversky|hinton|vaswani)' tests/phase-12.2/`.
    - Each test creates a `mktemp` fixture (or `git clone` for #9) and traps cleanup on EXIT: `for t in tests/phase-12.2/test_*.sh; do grep -q 'trap.*cleanup_fixture_repo.*EXIT\|trap.*rm -rf.*EXIT' "$t" || exit 1; done`.
    - `bash tests/phase-12.2/run.sh` produces a banner of the form `PHASE 12.2 TESTS: N/11`. The N value MAY be 0 or small (Plan 02 + Plan 03 turn the suite GREEN). The aggregator script itself MUST run without bash syntax errors.
  </acceptance_criteria>
  <done>
    Wave-0 RED test harness lands. All 11 scenario files encode the correct D-20 / D-05 exit-code contract, verifiable by grep. Most tests fail today (RED) — Plan 02's `--staged` extension turns them GREEN. Plan 03's hook composition turns the hook-ordering tests GREEN. The `--no-verify` smoke test passes today (it's testing git's flag, not our code).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture repo -> live $REPO_ROOT | tests invoke `bash "$REPO_ROOT/bin/lint.sh" ...` from inside throwaway fixture cwd; lint reads working tree files in fixture cwd, NOT in $REPO_ROOT (no cross-tree leakage) |
| /tmp fixture lifecycle | tests rely on `mktemp -d` + EXIT trap; cleanup_fixture_repo asserts `/tmp/*` prefix before `rm -rf` (load-bearing safety check) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.2-01-01 | Tampering | tests/phase-12.2/lib.sh `cleanup_fixture_repo` | mitigate | Inherit phase-09 `[[ "$path" == /tmp/* ]]` guard before rm -rf; never accept a path from outside mktemp |
| T-12.2-01-02 | Information disclosure | test fixture content | accept | Fixtures contain abstract placeholders only (CLAUDE.md §3); no `local_only` data flows through tests |
| T-12.2-01-03 | DoS | test_hook_ordering | accept | Test invokes `git clone "$REPO_ROOT" "$tmp"` once per test; clone is local fs, ms-scale, fixture cleaned via trap |
| T-12.2-01-04 | Path traversal | test fixture page paths | mitigate | Fixture paths are hardcoded (`wiki/concepts/foo.md`, etc.); no user input flows into paths |
</threat_model>

<verification>
After Plan 01 lands:
1. `bash tests/phase-12.2/run.sh` runs without script-level errors and emits `PHASE 12.2 TESTS: N/11`.
2. The smoke test (`test_no_verify_bypass_smoke.sh`) passes today (git's `--no-verify` flag works without our code).
3. The 8 staged-mode tests fail today (RED) — they assert behavior `bin/lint.sh` does not yet implement.
4. The hook-ordering test fails today (RED) — `.githooks/pre-commit` does not yet invoke the write-gate.
5. The `test_staged_requires_strict.sh` test may pass today (lint.sh ignores unknown `--staged` and no-ops, satisfying the no-op contract) — Plan 02 must preserve this contract.
</verification>

<success_criteria>
- All 13 files exist under tests/phase-12.2/.
- All test files are executable.
- Aggregator emits `PHASE 12.2 TESTS: N/11`.
- All 10 D-20 scenarios are encoded as separate `test_*.sh` files with per-scenario AC verifying exit-code contract.
- The D-05 contract (`--staged` requires `--strict`) is encoded as `test_staged_requires_strict.sh`.
- No test uses real personal-vault terms or real example-cluster slugs.
- All tests use mktemp + EXIT-trap cleanup (or `git clone` for the hook-ordering test).
- The aggregator itself runs without bash syntax errors.
</success_criteria>

<output>
After completion, create `.planning/phases/12.2-local-wiki-write-gate/12.2-01-SUMMARY.md` with:
- Aggregator banner output (`PHASE 12.2 TESTS: N/11`) before any production code is modified.
- N value (the number of tests that pass against unmodified bin/lint.sh + .githooks/pre-commit).
- Confirmation that the smoke test (no-verify) passes today.
- Confirmation that the hook-ordering test fails today (RED — it depends on Plan 03).
- Confirmation that the 8 staged-mode tests fail today (RED — they depend on Plan 02).
- Confirmation that all per-scenario AC grep checks pass (each test encodes its correct exit-code expectation).
</output>
</content>
