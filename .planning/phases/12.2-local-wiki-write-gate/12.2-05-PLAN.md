---
phase: 12.2-local-wiki-write-gate
plan: 05
type: execute
wave: 4
depends_on: [12.2-04]
files_modified:
  - .planning/phases/12.2-local-wiki-write-gate/12.2-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [WGATE-01, WGATE-02, WGATE-03, WGATE-04]
must_haves:
  truths:
    - "12.2-VERIFICATION.md exists in the Phase 7 / 12.1 mirror format"
    - "Each WGATE-0X requirement has a [x] **REQ-ID**: Complete row with file-path evidence"
    - "Each row cites: specific test(s) under tests/phase-12.2/, AGENTS.md §11.3 line range, .githooks/pre-commit evidence, and bin/lint.sh `staged_new_pages` evidence"
    - "REQUIREMENTS.md WGATE-01..04 flipped from Pending to Complete (bullet checkbox + status line + traceability table row)"
    - "bin/requirements-sync.sh --strict --phase 12.2 exits 0 (D-21)"
    - "bin/requirements-sync.sh --require-complete --phase 12.2 exits 0"
    - "bin/lint.sh --category yaml,provenance --strict exits 0 on the live tree (sanity check — no regression on real wiki content)"
    - "Phase-base SHA captured at the SPEC-commit anchor (Phase 12.1 precedent)"
  artifacts:
    - path: ".planning/phases/12.2-local-wiki-write-gate/12.2-VERIFICATION.md"
      provides: "Phase verification artifact in Phase 7 / 12.1 mirror format with WGATE-01..04 evidence"
      contains: "REQ-ID Verification"
      min_lines: 100
    - path: ".planning/REQUIREMENTS.md"
      provides: "WGATE-01..04 status flipped from Pending to Complete"
      contains: "WGATE-01 | Phase 12.2 | Complete"
  key_links:
    - from: ".planning/phases/12.2-local-wiki-write-gate/12.2-VERIFICATION.md"
      to: "tests/phase-12.2/run.sh + bin/lint.sh + .githooks/pre-commit + AGENTS.md §11.3"
      via: "Behavioral Spot-Checks table + REQ-ID Verification bullets cite exact evidence per requirement"
      pattern: "tests/phase-12\\.2|bin/lint\\.sh|\\.githooks/pre-commit"
    - from: "bin/requirements-sync.sh"
      to: ".planning/REQUIREMENTS.md WGATE rows"
      via: "exit-0 contract on --strict --phase 12.2 and --require-complete --phase 12.2"
      pattern: "WGATE-0[1-4].*Complete"
---

<objective>
Author `12.2-VERIFICATION.md` in the Phase 7 / 12.1 mirror format. Each of WGATE-01..04 is verified via the `bin/requirements-sync.sh`-parseable `- [x] **REQ-ID**: Complete` line followed by indented Truth/Evidence/Acceptance bullets. After the verification artifact lands, flip REQUIREMENTS.md WGATE-01..04 from `Pending` to `Complete` (bullet checkbox + status line per row + traceability table row), then run `bin/requirements-sync.sh --strict --phase 12.2` and `--require-complete --phase 12.2` — both MUST exit 0.

Per D-21, the verification artifact precedes the REQUIREMENTS.md flip (write VERIFICATION.md FIRST, then flip, then sync — exit-0 sync is the green-light gate that flipping was correct).

Per D-22, follow the Phase 12.1 mirror format exactly (the most recent and highest-fidelity precedent).

Output:
- `.planning/phases/12.2-local-wiki-write-gate/12.2-VERIFICATION.md` (~150-220 lines).
- 5 line-edits in `.planning/REQUIREMENTS.md` (4 bullets `[ ]` → `[x]` with status note + 4 traceability rows `Pending` → `Complete`).
- Verbatim CLI evidence for `bin/requirements-sync.sh --strict --phase 12.2` and `--require-complete --phase 12.2` exit-0 captured in the VERIFICATION.md Behavioral Spot-Checks block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.2-local-wiki-write-gate/12.2-CONTEXT.md
@.planning/phases/12.2-local-wiki-write-gate/12.2-PATTERNS.md
@.planning/phases/12.2-local-wiki-write-gate/12.2-01-SUMMARY.md
@.planning/phases/12.2-local-wiki-write-gate/12.2-02-SUMMARY.md
@.planning/phases/12.2-local-wiki-write-gate/12.2-03-SUMMARY.md
@.planning/phases/12.2-local-wiki-write-gate/12.2-04-SUMMARY.md
@CLAUDE.md

# Verification format precedent (most recent, highest-fidelity)
@.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md

# REQUIREMENTS.md — the file flipped at the end
@.planning/REQUIREMENTS.md

# bin/requirements-sync.sh — read for the exit-code contract being satisfied
@bin/requirements-sync.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 12.2-VERIFICATION.md, flip REQUIREMENTS.md WGATE-01..04, run sync gates</name>
  <files>
    .planning/phases/12.2-local-wiki-write-gate/12.2-VERIFICATION.md
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md (full file — exact mirror format including frontmatter, Goal Achievement table, Required Artifacts table, Behavioral Spot-Checks table, REQ-ID Verification bullets, and verbatim CLI evidence blocks)
    - .planning/REQUIREMENTS.md (find WGATE-01..04 bullets at lines ~130-133 and the traceability table rows for WGATE-01..04 at lines ~282-285)
    - .planning/phases/12.2-local-wiki-write-gate/12.2-CONTEXT.md D-21 (verification artifact PRECEDES the flip), D-22 (Phase 7 / 12.1 mirror format)
    - .planning/phases/12.2-local-wiki-write-gate/12.2-PATTERNS.md §7 (REQUIREMENTS.md status flip diff pattern from Phase 12.1) and §11 (12.2-VERIFICATION.md mirror)
    - bin/requirements-sync.sh — confirm `--strict --phase` and `--require-complete --phase` flag semantics
  </read_first>
  <action>
    Six edits in sequence. The order is mandatory: write VERIFICATION.md FIRST, then flip REQUIREMENTS.md, then capture sync evidence into VERIFICATION.md.

    **Edit 1: Capture phase-base SHA** before any edit. Run:
    ```bash
    PHASE_BASE_SHA=$(git log --format=%H -n 1 -- .planning/phases/12.2-local-wiki-write-gate/12.2-CONTEXT.md)
    PHASE_BASE_SHA_SHORT=$(git log --format=%h -n 1 -- .planning/phases/12.2-local-wiki-write-gate/12.2-CONTEXT.md)
    echo "phase_base_sha: $PHASE_BASE_SHA"
    echo "phase_base_sha_short: $PHASE_BASE_SHA_SHORT"
    ```
    Substitute these literal values into the VERIFICATION.md frontmatter (Edit 2). Phase 12.1 anchored to the SPEC commit; Phase 12.2 anchors to the CONTEXT commit (this phase has no separate SPEC.md — CONTEXT.md is the planning anchor).

    **Edit 2: Author `.planning/phases/12.2-local-wiki-write-gate/12.2-VERIFICATION.md`** in the Phase 12.1 mirror format. Use the following template, substituting actual SHAs and verbatim CLI output:

    ```markdown
    ---
    phase: 12.2-local-wiki-write-gate
    verified: <ISO-8601 timestamp YYYY-MM-DDTHH:MM:SSZ>
    status: passed
    score: 7/7 success criteria verified (4/4 WGATE requirements satisfied)
    overrides_applied: 0
    re_verification:
      previous_status: pending
      previous_score: 0/4 requirements verified
      gaps_closed: [WGATE-01, WGATE-02, WGATE-03, WGATE-04]
      gaps_remaining: []
      regressions: []
    phase_base_sha: <SHA from Edit 1>
    phase_base_sha_short: <SHA-short from Edit 1>
    phase_base_anchor: "git log --format=%H -n 1 -- .planning/phases/12.2-local-wiki-write-gate/12.2-CONTEXT.md"
    ---

    # Phase 12.2 Verification — Local Wiki Write Gate

    **Phase:** 12.2-local-wiki-write-gate
    **Status:** Complete (passed)
    **Verified:** <YYYY-MM-DD>
    **Phase-base SHA (anchor for diff acceptance check):** `<short>` (full: `<full>`)
    **Phase-base anchor command:** `git log --format=%H -n 1 -- .planning/phases/12.2-local-wiki-write-gate/12.2-CONTEXT.md`

    The phase-base SHA is derived from the CONTEXT commit (Phase 12.2 has no separate SPEC.md — CONTEXT.md is the planning anchor). Anchoring to the CONTEXT-commit SHA ensures `git diff <SHA>..HEAD` captures every Phase 12.2 content commit (Plans 12.2-01 through 12.2-05).

    ---

    ## Goal Achievement

    ### Observable Truths (7 ROADMAP.md success criteria → evidence)

    | # | Truth | Status | Evidence |
    |---|-------|--------|----------|
    | 1 | The local commit path runs a deterministic gate against staged wiki changes before commit completes | VERIFIED | `.githooks/pre-commit` lines 13+ invoke `bash bin/lint.sh --strict --staged --category provenance`; `bash tests/phase-12.2/run.sh` → `PHASE 12.2 TESTS: 11/11` (Behavioral Spot-Checks row 1) |
    | 2 | New staged pages under `wiki/{entities,concepts,overviews,comparisons}/` fail the gate when zero `[prov:...]` markers | VERIFIED | `tests/phase-12.2/test_staged_blocks_concept_no_prov.sh` PASS (greenfield concept blocked); `tests/phase-12.2/test_staged_passes_concept_with_prov.sh` PASS (concept with prov accepted) |
    | 3 | Gate reuses `bin/lint.sh --strict` semantics (single policy surface) | VERIFIED | `bin/lint.sh` adds `staged_new_pages()` (~533:553) reusing `parse_fm_from_text`, `PROVENANCE_PRESENCE_RE`, `PROVENANCE_REQUIRED_TYPES`, `add_finding`, and `CI_SEVERITY_REMAP` from the existing strict-mode plumbing — no parallel constants introduced |
    | 4 | Exemptions aligned with schema/tooling: `type:source`, `type:decision`, `examples/`, brownfield not falsely blocked | VERIFIED | tests `test_staged_exempts_type_source.sh`, `test_staged_exempts_type_decision.sh`, `test_staged_exempts_examples_path.sh`, `test_staged_exempts_bootstrap_bootstrapped.sh` all PASS; `test_staged_blocks_bootstrap_verified.sh` PASS confirming D-12 (verified is NOT exempt) |
    | 5 | Pre-commit UX is actionable: failures point to file/path and exact reason | VERIFIED | `bin/lint.sh` failure message includes "[prov:source_id#locator] markers... type:source/decision in frontmatter... `git commit --no-verify`"; `.githooks/pre-commit` footer line repeats the three actionable paths (D-08) |
    | 6 | Existing AGENTS.md ↔ CLAUDE.md sync hook continues to work unchanged | VERIFIED | `.githooks/pre-commit` lines 1-12 (sync-claude block) preserved verbatim; ordering D-07 (sync-claude first, write-gate second); `tests/phase-12.2/test_hook_ordering_sync_then_gate.sh` PASS exercises the full ordering |
    | 7 | Gate is staged-index aware (NOT origin/main...HEAD) | VERIFIED | `staged_new_pages()` uses `git diff --cached --name-only --diff-filter=A`; PR-mode `strict_new_pages()` (origin/main...HEAD) is preserved unchanged for CI use; dispatch in `strict_check()` selects via `STAGED_MODE` (D-02) |

    **Score:** 7/7 success criteria verified.

    ## Required Artifacts

    | # | Artifact | Expected | Exists | Substantive | Wired | Status |
    |---|----------|----------|--------|-------------|-------|--------|
    | 1 | `tests/phase-12.2/run.sh` | Per-phase aggregator (mirror of phase-09/12.1) | YES | YES (`PHASE 12.2 TESTS:` banner) | YES (`bash tests/phase-12.2/run.sh` → 11/11) | VERIFIED |
    | 2 | `tests/phase-12.2/lib.sh` | Helper library (subset of phase-09; no `seed_origin_main_ref`) | YES | YES (`make_bare_repo`, `setup_git_author`, `assert_exit_code`, `cleanup_fixture_repo`, `write_page`) | YES (sourced by all 11 test_*.sh) | VERIFIED |
    | 3 | `tests/phase-12.2/test_*.sh` (11 files) | 10 D-20 scenarios + D-05 contract test | YES (11) | YES (per-scenario assertions) | YES (aggregator runs them) | VERIFIED |
    | 4 | `bin/lint.sh` `--staged` flag wiring | argv parser + env-var bridge + staged_new_pages() + dispatch + exemption set + LINT_VERSION bump | YES | YES (~60-100 lines added) | YES (Plan 02 acceptance) | VERIFIED |
    | 5 | `.githooks/pre-commit` write-gate composition | sync-claude preserved + write-gate appended (D-07) | YES | YES (footer line per D-08) | YES (Plan 03 acceptance + test_hook_ordering PASS) | VERIFIED |
    | 6 | `AGENTS.md §11.3` --staged row + Staged-mode rules | --staged row + 8-item subsection (D-15 + D-09 + D-05) | YES | YES (placeholders only per CLAUDE.md §3) | YES (CLAUDE.md byte-equal via sync; schema/AGENTS.template.md mirror) | VERIFIED |
    | 7 | `schema/AGENTS.template.md §11.3` byte-equal mirror | identical to AGENTS.md §11.3 | YES | YES | YES (Phase 11-05 canonical-AGENTS.md fixture path) | VERIFIED |
    | 8 | `CLAUDE.md` byte-equal mirror | auto-synced by .githooks/pre-commit | YES | YES | YES (sync-claude --check exit 0 post-edit) | VERIFIED |
    | 9 | `docs/reference/ci.md` "Local pre-commit write gate" section | source-of-truth blockquote + install + bypass + fix-list | YES | YES | YES (links to AGENTS.md §11.3, no rule restatement per D-17) | VERIFIED |
    | 10 | `CONTRIBUTING.md` "Local pre-commit gate" subsection | one-paragraph install + bypass + cross-link | YES | YES | YES (links to ci.md and §11.3 per D-17) | VERIFIED |
    | 11 | `.planning/phases/12.2-.../12.2-VERIFICATION.md` | This file (Phase 12.1 mirror) | YES | YES (D-21 + D-22) | YES (`bin/requirements-sync.sh` consumer) | VERIFIED |

    ## Behavioral Spot-Checks

    | Behavior | Command | Result | Status |
    |----------|---------|--------|--------|
    | Phase 12.2 contract tests green | `bash tests/phase-12.2/run.sh` | `PHASE 12.2 TESTS: 11/11` (verbatim block below) | PASS |
    | Phase 9 regression check | `bash tests/phase-09/run.sh` | (unchanged passing count from baseline; no PR-mode regression) | PASS |
    | bin/lint.sh --version | `bash bin/lint.sh --version` | `1.2.0` (MINOR bump for additive --staged) | PASS |
    | Live-tree provenance lint | `bash bin/lint.sh --category yaml,provenance --strict` (against the real repo) | `exit: 0` (no provenance regressions on shipped wiki content) | PASS |
    | Neutrality gate | `bash bin/check-neutrality.sh` | `exit: 0` | PASS |
    | Privacy-leak gate | `bash bin/check-privacy.sh` | `exit: 0` | PASS |
    | requirements-sync strict mode (Phase 12.2) | `bash bin/requirements-sync.sh --strict --phase 12.2` | (4 rows, 0 drift, exit 0 — verbatim block below) | PASS |
    | requirements-sync require-complete mode (Phase 12.2) | `bash bin/requirements-sync.sh --require-complete --phase 12.2` | (4 rows, 0 drift, 0 incomplete, exit 0 — verbatim block below) | PASS |
    | AGENTS.md ↔ CLAUDE.md sync clean | `bash bin/sync-claude.sh --check` | `exit: 0` post-Edit-2 | PASS |

    ### Verbatim CLI evidence — Phase 12.2 tests

    ```
    <paste full output of `bash tests/phase-12.2/run.sh` here including all per-test PASS lines and the trailing `PHASE 12.2 TESTS: 11/11` banner>
    ```

    ### Verbatim CLI evidence — requirements-sync (sourced live during verification)

    ```
    === bash bin/requirements-sync.sh --strict --phase 12.2 ===
    <paste verbatim output>

    === bash bin/requirements-sync.sh --require-complete --phase 12.2 ===
    <paste verbatim output>
    ```

    ## REQUIREMENTS.md Diff (WGATE-01..04 status flip — per D-21)

    ```diff
    --- a/.planning/REQUIREMENTS.md
    +++ b/.planning/REQUIREMENTS.md
    @@ <line for WGATE-01>
    -- [ ] **WGATE-01**: ...
    +- [x] **WGATE-01**: ... See `.planning/phases/12.2-local-wiki-write-gate/12.2-VERIFICATION.md`.
    @@ <line for WGATE-02>
    -- [ ] **WGATE-02**: ...
    +- [x] **WGATE-02**: ... See `.planning/phases/12.2-local-wiki-write-gate/12.2-VERIFICATION.md`.
    @@ <line for WGATE-03>
    -- [ ] **WGATE-03**: ...
    +- [x] **WGATE-03**: ... See `.planning/phases/12.2-local-wiki-write-gate/12.2-VERIFICATION.md`.
    @@ <line for WGATE-04>
    -- [ ] **WGATE-04**: ...
    +- [x] **WGATE-04**: ... See `.planning/phases/12.2-local-wiki-write-gate/12.2-VERIFICATION.md`.
    @@ <traceability table rows>
    -| WGATE-01 | Phase 12.2 | Pending |
    +| WGATE-01 | Phase 12.2 | Complete |
    -| WGATE-02 | Phase 12.2 | Pending |
    +| WGATE-02 | Phase 12.2 | Complete |
    -| WGATE-03 | Phase 12.2 | Pending |
    +| WGATE-03 | Phase 12.2 | Complete |
    -| WGATE-04 | Phase 12.2 | Pending |
    +| WGATE-04 | Phase 12.2 | Complete |
    ```

    (Capture the LIVE diff as `git diff <PHASE_BASE_SHA>..HEAD -- .planning/REQUIREMENTS.md` after Edit 3 below and substitute into this block.)

    ## Requirements Coverage

    | Requirement | Source Plan | Description | Status | Evidence |
    |-------------|-------------|-------------|--------|----------|
    | WGATE-01 | 12.2-03 (hook composition) | Local commit path runs deterministic gate before commit | VERIFIED | `.githooks/pre-commit` lines 13+; `tests/phase-12.2/test_hook_ordering_sync_then_gate.sh` PASS |
    | WGATE-02 | 12.2-02 (lint --staged) | Zero-prov new staged synthesized pages fail the gate | VERIFIED | `tests/phase-12.2/test_staged_blocks_concept_no_prov.sh` + `test_staged_passes_concept_with_prov.sh` PASS |
    | WGATE-03 | 12.2-02 (lint --staged) | Gate is staged-index aware, composes with --strict, does NOT rely on origin/main...HEAD | VERIFIED | `staged_new_pages()` uses `git diff --cached --name-only --diff-filter=A`; PR-mode strict_new_pages preserved; `STAGED_MODE` dispatch in `strict_check()` |
    | WGATE-04 | 12.2-02 + 12.2-03 + 12.2-04 | Exemptions match schema; AGENTS.md ↔ CLAUDE.md sync intact | VERIFIED | All 4 exemption tests PASS (type:source, type:decision, examples/, bootstrap_stage:bootstrapped); sync-claude block preserved verbatim in pre-commit; `bin/check-neutrality.sh` exit 0 |

    ## REQ-ID Verification

    This section emits the per-REQ-ID `- [x] **WGATE-0X**: Complete` lines consumed by `bin/requirements-sync.sh` (parser at `bin/requirements-sync.sh:138-143` `LEADING_JUNK` + `REQ_STATUS` regex). Pattern mirrors `12.1-VERIFICATION.md:180-188` (NEUT-08).

    - [x] **WGATE-01**: Complete
      - **Truth:** Local commit path runs a deterministic gate against staged wiki changes before commit completes.
      - **Evidence:** `.githooks/pre-commit` lines 13+ invoke `bash bin/lint.sh --strict --staged --category provenance`; existing sync-claude block (lines 1-12) preserved unchanged.
      - **Evidence:** `tests/phase-12.2/test_hook_ordering_sync_then_gate.sh` PASS (live-hook ordering test using `git -c core.hooksPath=.githooks commit`).
      - **Evidence:** `tests/phase-12.2/test_no_verify_bypass_smoke.sh` PASS (--no-verify bypass works as expected per D-09).
      - **Acceptance:** ROADMAP.md Phase 12.2 SC #1 (deterministic gate fires before commit) + SC #6 (sync hook intact).

    - [x] **WGATE-02**: Complete
      - **Truth:** New staged pages under `wiki/{entities,concepts,overviews,comparisons}/` fail the gate when they contain zero `[prov:...]` markers.
      - **Evidence:** `tests/phase-12.2/test_staged_blocks_concept_no_prov.sh` PASS (greenfield concept blocked, error message mentions provenance).
      - **Evidence:** `tests/phase-12.2/test_staged_passes_concept_with_prov.sh` PASS (concept with at least one [prov:] passes).
      - **Evidence:** `tests/phase-12.2/test_staged_passes_modified_existing.sh` PASS (status M not gated; only status A).
      - **Evidence:** `bin/lint.sh staged_new_pages()` enumerates the four required-types directories via path prefix.
      - **Acceptance:** ROADMAP.md Phase 12.2 SC #2.

    - [x] **WGATE-03**: Complete
      - **Truth:** Gate is staged-index aware and composes with `bin/lint.sh --strict` policy without relying on `origin/main...HEAD`.
      - **Evidence:** `bin/lint.sh staged_new_pages()` uses `git diff --cached --name-only --diff-filter=A` (NOT origin/main...HEAD).
      - **Evidence:** PR-mode `strict_new_pages()` preserved unchanged for CI consumption.
      - **Evidence:** `strict_check()` dispatches via `STAGED_MODE` env-var; the orthogonal-flag arg parser in `bin/lint.sh` mirrors the `--strict` / `--ci` / `--count-skips` precedent (single policy surface per D-13).
      - **Evidence:** `tests/phase-09/run.sh` baseline (no regression in PR-mode strict tests).
      - **Acceptance:** ROADMAP.md Phase 12.2 SC #3 + SC #7.

    - [x] **WGATE-04**: Complete
      - **Truth:** Exemptions match the schema/tooling contract: `type:source`, `type:decision`, `examples/`, and brownfield transitional cases are not falsely blocked, and the AGENTS.md ↔ CLAUDE.md sync hook remains intact.
      - **Evidence:** `tests/phase-12.2/test_staged_exempts_type_source.sh` PASS (type:source + SOURCE_EXTRA_FIELDS exempted).
      - **Evidence:** `tests/phase-12.2/test_staged_exempts_type_decision.sh` PASS (type:decision exempted by both path-prefix and frontmatter).
      - **Evidence:** `tests/phase-12.2/test_staged_exempts_examples_path.sh` PASS (path prefix `examples/` exempted).
      - **Evidence:** `tests/phase-12.2/test_staged_exempts_bootstrap_bootstrapped.sh` PASS (brownfield in-flight pages exempted).
      - **Evidence:** `tests/phase-12.2/test_staged_blocks_bootstrap_verified.sh` PASS confirming D-12 (verified is NOT exempt).
      - **Evidence:** `.githooks/pre-commit` lines 1-12 byte-identical to pre-Plan-03 (sync-claude block preserved per D-07 / SC6).
      - **Evidence:** `AGENTS.md §11.3` "Staged-mode rules" subsection documents the 6-item D-15 exemption ordering verbatim.
      - **Acceptance:** ROADMAP.md Phase 12.2 SC #4 + SC #5 + SC #6.

    ---

    *Phase 12.2 verification — local wiki write gate complete. Pre-commit-time complement to the CI `--strict` quality ratchet. Clears the path for Phase 13 (Claim Faithfulness Audit).*
    ```

    **Edit 3: Flip REQUIREMENTS.md WGATE-01..04.** For each of the 4 bullet entries (lines ~130-133):
    - Change `- [ ] **WGATE-XX**: <existing description>` to `- [x] **WGATE-XX**: <existing description>. **Status:** Complete (Phase 12.2). See \`.planning/phases/12.2-local-wiki-write-gate/12.2-VERIFICATION.md\`.`

    For each of the 4 traceability table rows (lines ~282-285):
    - Change `| WGATE-XX | Phase 12.2 | Pending |` to `| WGATE-XX | Phase 12.2 | Complete |`.

    **Edit 4: Run sync gates and capture evidence.** Run:
    ```bash
    bash bin/requirements-sync.sh --strict --phase 12.2 2>&1; echo "exit: $?"
    bash bin/requirements-sync.sh --require-complete --phase 12.2 2>&1; echo "exit: $?"
    bash tests/phase-12.2/run.sh 2>&1; echo "exit: $?"
    bash bin/lint.sh --category yaml,provenance --strict 2>&1; echo "exit: $?"
    bash bin/check-neutrality.sh 2>&1; echo "exit: $?"
    bash bin/check-privacy.sh 2>&1; echo "exit: $?"
    ```

    All MUST exit 0. If `--strict --phase 12.2` reports drift, REQUIREMENTS.md and VERIFICATION.md disagree; investigate and fix BEFORE proceeding (typical fixes: missing checkbox flip, mistyped REQ-ID, missing `- [x] **WGATE-XX**: Complete` in VERIFICATION.md REQ-ID Verification section).

    **Edit 5: Substitute the verbatim CLI output into VERIFICATION.md** Behavioral Spot-Checks block (replacing the `<paste...>` placeholders).

    **Edit 6: Substitute the actual `git diff <PHASE_BASE_SHA>..HEAD -- .planning/REQUIREMENTS.md` output** into the "REQUIREMENTS.md Diff" code-fenced block.
  </action>
  <verify>
    <automated>
      cd "$(git rev-parse --show-toplevel)" && \
      test -f .planning/phases/12.2-local-wiki-write-gate/12.2-VERIFICATION.md && \
      grep -c '^- \[x\] \*\*WGATE-0[1-4]\*\*: Complete$' .planning/phases/12.2-local-wiki-write-gate/12.2-VERIFICATION.md | grep -q '^4$' && \
      grep -c '^- \[x\] \*\*WGATE-0[1-4]\*\*' .planning/REQUIREMENTS.md | grep -q '^4$' && \
      grep -cE '^\| WGATE-0[1-4] \| Phase 12\.2 \| Complete \|$' .planning/REQUIREMENTS.md | grep -q '^4$' && \
      ! grep -E '^\| WGATE-0[1-4] \| Phase 12\.2 \| Pending \|$' .planning/REQUIREMENTS.md && \
      bash bin/requirements-sync.sh --strict --phase 12.2; rc1=$? && \
      bash bin/requirements-sync.sh --require-complete --phase 12.2; rc2=$? && \
      bash tests/phase-12.2/run.sh; rc3=$? && \
      bash bin/lint.sh --category yaml,provenance --strict; rc4=$? && \
      bash bin/check-neutrality.sh; rc5=$? && \
      bash bin/check-privacy.sh; rc6=$? && \
      echo "exits: sync-strict=$rc1 sync-require-complete=$rc2 phase-12.2-tests=$rc3 lint-strict=$rc4 neutrality=$rc5 privacy=$rc6" && \
      [ "$rc1" = "0" ] && [ "$rc2" = "0" ] && [ "$rc3" = "0" ] && [ "$rc4" = "0" ] && [ "$rc5" = "0" ] && [ "$rc6" = "0" ]
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/12.2-local-wiki-write-gate/12.2-VERIFICATION.md` exists.
    - The file's frontmatter contains all 11 fields from the Phase 12.1 mirror (phase, verified, status, score, overrides_applied, re_verification, phase_base_sha, phase_base_sha_short, phase_base_anchor).
    - The file contains exactly 4 lines matching `^- \[x\] \*\*WGATE-0[1-4]\*\*: Complete$`.
    - Each WGATE-0X bullet is followed by indented `- **Truth:**`, `- **Evidence:**` (multiple), `- **Acceptance:**` lines.
    - The Behavioral Spot-Checks section contains a verbatim CLI evidence block for `bash tests/phase-12.2/run.sh` showing `PHASE 12.2 TESTS: 11/11`.
    - The Behavioral Spot-Checks section contains verbatim CLI evidence for both `--strict --phase 12.2` and `--require-complete --phase 12.2` showing exit 0.
    - REQUIREMENTS.md has exactly 4 lines matching `^- \[x\] \*\*WGATE-0[1-4]\*\*` (all four bullets flipped).
    - REQUIREMENTS.md has exactly 4 traceability table rows matching `^\| WGATE-0[1-4] \| Phase 12\.2 \| Complete \|$`.
    - REQUIREMENTS.md has ZERO traceability table rows matching `^\| WGATE-0[1-4] \| Phase 12\.2 \| Pending \|$`.
    - `bash bin/requirements-sync.sh --strict --phase 12.2` exits 0.
    - `bash bin/requirements-sync.sh --require-complete --phase 12.2` exits 0.
    - `bash tests/phase-12.2/run.sh` exits 0 with `PHASE 12.2 TESTS: 11/11`.
    - `bash bin/lint.sh --category yaml,provenance --strict` exits 0 (sanity check on the live wiki tree).
    - `bash bin/check-neutrality.sh` exits 0.
    - `bash bin/check-privacy.sh` exits 0.
    - VERIFICATION.md uses abstract placeholders (`<concept-slug>`, `source_id`, `locator`) — no real vault terms.
  </acceptance_criteria>
  <done>
    Phase 12.2 closes. WGATE-01..04 flipped to Complete in REQUIREMENTS.md. 12.2-VERIFICATION.md authored in the Phase 12.1 mirror format with verbatim CLI evidence per requirement. Both `--strict --phase 12.2` and `--require-complete --phase 12.2` exit 0. All test suites green. All gates green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VERIFICATION.md ↔ REQUIREMENTS.md (parser-consumed) | T9: parser drift — `bin/requirements-sync.sh --strict` exits non-zero if the two artifacts disagree on REQ-ID status |
| `phase_base_sha` anchor | T10: misanchored SHA causes the diff-acceptance check in CLOSE-01 (Phase 13.2) to slip; mitigate by anchoring to CONTEXT.md commit (Phase 12.2 has no SPEC.md) |
| flipping REQUIREMENTS.md before VERIFICATION.md exists | T11: REQUIREMENTS.md says Complete but no evidence artifact exists → milestone-closure-time embarrassment; mitigate by D-21 ordering (write VERIFICATION.md FIRST, then flip) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.2-05-01 | Tampering | parser drift between VERIFICATION.md and REQUIREMENTS.md | mitigate | `bin/requirements-sync.sh --strict --phase 12.2` exits non-zero on drift; verification step requires exit 0 |
| T-12.2-05-02 | Information disclosure | new docs content in VERIFICATION.md | mitigate | Use only abstract placeholders; verify via grep for known denylisted terms in the new file |
| T-12.2-05-03 | No executable surface added | this plan only edits markdown | accept | No code; threat surface is artifact accuracy + sync gate exit 0 |
</threat_model>

<verification>
After Plan 05 lands:
1. `.planning/phases/12.2-local-wiki-write-gate/12.2-VERIFICATION.md` exists with all 4 WGATE bullets verified.
2. REQUIREMENTS.md WGATE-01..04 all show `[x]` and `Complete`.
3. `bash bin/requirements-sync.sh --strict --phase 12.2` exits 0 (drift gate).
4. `bash bin/requirements-sync.sh --require-complete --phase 12.2` exits 0 (completion gate).
5. `bash tests/phase-12.2/run.sh` exits 0 with `PHASE 12.2 TESTS: 11/11`.
6. `bash bin/lint.sh --category yaml,provenance --strict` exits 0 against the live wiki tree.
7. `bash bin/check-neutrality.sh` exits 0.
8. `bash bin/check-privacy.sh` exits 0.
9. Phase 12.2 is now closable; Phase 13 (Claim Faithfulness Audit) is unblocked.
</verification>

<success_criteria>
- 12.2-VERIFICATION.md exists in the Phase 12.1 mirror format with all required sections.
- 4 [x] **WGATE-0X**: Complete bullets present.
- 4 REQUIREMENTS.md WGATE bullets flipped to [x].
- 4 REQUIREMENTS.md traceability table rows flipped to Complete.
- bin/requirements-sync.sh --strict --phase 12.2 exits 0.
- bin/requirements-sync.sh --require-complete --phase 12.2 exits 0.
- tests/phase-12.2/run.sh exits 0 with 11/11.
- bin/lint.sh --category yaml,provenance --strict exits 0 against the live tree.
- All neutrality + privacy gates green.
- VERIFICATION.md frontmatter has phase_base_sha anchored to CONTEXT.md.
</success_criteria>

<output>
After completion, create `.planning/phases/12.2-local-wiki-write-gate/12.2-05-SUMMARY.md` documenting:
- VERIFICATION.md line count.
- All 6 verbatim CLI exit codes (sync-strict, sync-require-complete, phase-12.2-tests, lint-strict, neutrality, privacy) — each MUST be 0.
- Confirmation that phase_base_sha is anchored to CONTEXT.md (since Phase 12.2 has no separate SPEC.md).
- The 4 traceability table row diffs (Pending → Complete).
- Phase 12.2 closure note.
</output>
</content>
</invoke>