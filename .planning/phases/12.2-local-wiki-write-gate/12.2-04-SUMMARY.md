---
phase: 12.2-local-wiki-write-gate
plan: 04
subsystem: docs
tags: [agents-md, schema-template, ci-docs, contributing-md, write-gate, phase-12.2, byte-equality]

# Dependency graph
requires:
  - phase: 12.2-02
    provides: bin/lint.sh --staged flag implementation
  - phase: 12.2-03
    provides: .githooks/pre-commit write-gate composition
provides:
  - AGENTS.md §11.3 amended with --staged flag row + Staged-mode rules subsection (D-15 exemption ordering, D-09 bypass, D-05 --staged-requires-strict, D-12 verified-NOT-exempt)
  - schema/AGENTS.template.md byte-equal mirror of §11.3 (closes pre-existing template gap — Phase 9 CI mode block was never mirrored to template)
  - CLAUDE.md auto-synced to AGENTS.md (byte-equal restored)
  - docs/reference/ci.md "Local pre-commit write gate (Phase 12.2)" section (source-of-truth blockquote pointing at §11.3 — links, does not restate)
  - CONTRIBUTING.md "Local pre-commit gate" subsection (single paragraph install + bypass + cross-link)
  - schema/fixtures/canonical-AGENTS.md regenerated to absorb the §11.3 template addition
  - Pre-existing AGENTS↔CLAUDE drift fixed (b1c3691 placeholder rule was CLAUDE-only; ported to AGENTS.md)
affects:
  - 12.2-05 (REQUIREMENTS.md flip + 12.2-VERIFICATION.md will cite this plan's evidence)
  - future phases relying on AGENTS.md == CLAUDE.md == schema/AGENTS.template.md byte-equality

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Single policy surface (D-16): AGENTS.md §11.3 owns the spec; ci.md and CONTRIBUTING.md link"
    - "Source-of-truth blockquote (Pattern D, D-17): operational docs open with > **Source of truth:** ... pointing at §11.3"
    - "Byte-equal mirror (D-18, Pattern §4): schema/AGENTS.template.md non-placeholder sections === AGENTS.md verbatim"

key-files:
  created:
    - .planning/phases/12.2-local-wiki-write-gate/12.2-04-SUMMARY.md
  modified:
    - AGENTS.md (§3 placeholder rule port + §11.3 --staged amendment)
    - CLAUDE.md (auto-synced from AGENTS.md)
    - schema/AGENTS.template.md (full Phase 9 + Phase 12.2 §11.3 CI mode block added — first time this section appears in template)
    - docs/reference/ci.md (new "Local pre-commit write gate (Phase 12.2)" section)
    - CONTRIBUTING.md (new "Local pre-commit gate" subsection under "How to contribute")
    - schema/fixtures/canonical-AGENTS.md (regenerated via init-wizard.sh after template change)

key-decisions:
  - "Order: drift-fix FIRST, then §11.3 amendment — Plan 02 deferred-items.md noted the b1c3691 drift; fixing it before §11.3 work was a prerequisite for the byte-equality contract"
  - "schema/AGENTS.template.md got the FULL CI mode block (not just the --staged delta) because the Phase 9 mirror had never been done — closing this gap was in scope per D-18 byte-equality"
  - "ci.md and CONTRIBUTING.md LINK to §11.3 (Pattern D, D-17), do NOT restate the 6-item D-15 exemption ordering — drift prevention"
  - "All examples use abstract placeholders (<concept-slug>, <type>, <path>) per CLAUDE.md §3 — no kahneman/loss-aversion/prospect-theory in new content"

patterns-established:
  - "Phase 9 + Phase 12.2 source-of-truth blockquote pattern: '(a)..(d)..(e) the --staged local-write-gate contract' — single sentence, additive enumeration"
  - "Operational docs are ergonomic install/bypass references; AGENTS.md §11.3 owns rules"
  - "Template-public placeholder rule applies to ALL files in §11.3 amendments (verified by grep against denylist)"

requirements-completed: [WGATE-01, WGATE-04]

# Metrics
duration: ~25min
completed: 2026-05-04
---

# Phase 12.2 Plan 04: Document the Phase 12.2 Write-Gate Across Template-Public Surfaces Summary

**Amended AGENTS.md §11.3 with the `--staged` local-write-gate contract (8-rule Staged-mode subsection + new flag-table row), mirrored byte-equally to schema/AGENTS.template.md (closing a pre-existing Phase 9 mirror gap), added linking-not-restating sections to docs/reference/ci.md and CONTRIBUTING.md, and fixed the pre-existing b1c3691 AGENTS↔CLAUDE drift.**

## Performance

- **Duration:** ~25 minutes
- **Started:** 2026-05-03T23:09:00Z
- **Completed:** 2026-05-03T23:34:38Z
- **Tasks:** 4 commits (Task 0 drift fix + Task 1 §11.3 amendment + Task 2 ci.md/CONTRIBUTING.md + Task 1b fixture regen)
- **Files modified:** 6 (AGENTS.md, CLAUDE.md, schema/AGENTS.template.md, docs/reference/ci.md, CONTRIBUTING.md, schema/fixtures/canonical-AGENTS.md)

## Accomplishments

- **Pre-existing AGENTS↔CLAUDE drift fixed first** — commit b1c3691 had added the "DO NOT use real slugs..." placeholder rule to CLAUDE.md §3 only; ported it to AGENTS.md so byte-equality is restored. This was a documented prerequisite (per Plan 02 deferred-items.md) for the §11.3 amendment that depends on the byte-equality contract.
- **AGENTS.md §11.3 amended** with: (a) source-of-truth blockquote extended with "(e) the `--staged` local-write-gate contract (Phase 12.2)"; (b) new `--staged` row in the orthogonal-flags table (between `--strict` and `--require-version`); (c) new "Staged-mode rules (`--staged`, Phase 12.2)" subsection with all 8 rules covering the D-15 exemption ordering (path filter, examples/, type:source, type:decision, example:true, bootstrap_stage:bootstrapped — IN THAT ORDER), D-12 (verified is NOT exempt), D-09 bypass (no-verify only; rejects WGATE_SKIP and wgate_exempt), and D-05 (--staged requires --strict).
- **schema/AGENTS.template.md byte-equal mirror** — discovered the template did NOT have the Phase 9 `#### CI mode (Phase 9)` subsection AT ALL (pre-existing gap from Phase 9 that was never closed). Added the full Phase 9 CI mode block WITH the new Phase 12.2 `--staged` content baked in. Verified byte-equality between AGENTS.md §11.3 and schema/AGENTS.template.md §11.3 via `awk` extraction + `diff`.
- **CLAUDE.md auto-synced** via `bin/sync-claude.sh` after each AGENTS.md edit.
- **docs/reference/ci.md got "Local pre-commit write gate (Phase 12.2)" section** with source-of-truth blockquote, "What it fails on" / "What it does not fail on" / "How to install" / "How to bypass" / "How to fix a blocked commit" subsections — install + bypass + actionable fix list ONLY; exemption ordering summarized in 1-2 sentences and links to §11.3 (per D-17 / Pattern D).
- **CONTRIBUTING.md got "Local pre-commit gate" subsection** — single paragraph: install command, bypass, cross-link to ci.md and AGENTS.md §11.3.
- **All examples use abstract placeholders** (`<concept-slug>`, `<type>`, `<path>`, `source_id`, `locator`) per CLAUDE.md §3 — no `kahneman`, `loss-aversion`, `prospect-theory`, `tversky`, `hinton`, or `vaswani` in new content (verified by grep against denylist).
- **schema/fixtures/canonical-AGENTS.md regenerated** via `bin/init-wizard.sh --answers-file schema/fixtures/canonical-answers.yaml --render-to /tmp/wz-regen-12.2-04` after the template change. Phase 9.1 byte-equality test went from 10/11 to 11/11.

## Task Commits

Each task was committed atomically:

1. **Task 0: Port placeholder rule to AGENTS.md (fix b1c3691 drift)** — `c8c18ff` (docs)
2. **Task 1: Amend AGENTS.md §11.3 with --staged + Staged-mode rules (mirror to schema/AGENTS.template.md, auto-sync CLAUDE.md)** — `ebe9304` (docs)
3. **Task 1b: Regenerate schema/fixtures/canonical-AGENTS.md** — `8e65a97` (fixtures)
4. **Task 2: Add Local pre-commit gate sections to ci.md and CONTRIBUTING.md** — `aa72fec` (docs)

## Files Created/Modified

| File | Lines added | Lines removed | Notes |
|------|-------------|---------------|-------|
| `AGENTS.md` | +24 | -2 | §3 placeholder rule (+1 line, Task 0); §11.3 source-of-truth blockquote rewrite (-2/+1 lines), new --staged flag row (+1), new Staged-mode rules subsection (+22) |
| `CLAUDE.md` | +24 | -2 | Auto-synced from AGENTS.md (sync-claude.sh) |
| `schema/AGENTS.template.md` | +54 | 0 | Full Phase 9 + Phase 12.2 #### CI mode block added (first time this section exists in template — closes pre-existing Phase 9 mirror gap) |
| `docs/reference/ci.md` | +34 | 0 | New "## Local pre-commit write gate (Phase 12.2)" section after "## --strict mode (quality ratchet)" |
| `CONTRIBUTING.md` | +4 | 0 | New "### Local pre-commit gate" subsection after "### CI gate" |
| `schema/fixtures/canonical-AGENTS.md` | +54 | 0 | Regenerated to match template change |

**Total:** +194 / -4 lines across 6 files.

## Decisions Made

- **Drift fix first:** Plan 02's `deferred-items.md` had documented the b1c3691 AGENTS↔CLAUDE drift. Fixing it BEFORE the §11.3 amendment was the right call because the Task 1 verification step relies on byte-equality (`diff AGENTS.md CLAUDE.md`). If I'd done §11.3 first then sync-claude, the diff would have shown the missing-from-AGENTS line as drift, not zero.
- **schema/AGENTS.template.md got the full Phase 9 CI mode block:** The plan implied the §11.3 CI mode subsection was already in the template ("schema/AGENTS.template.md is byte-equal to AGENTS.md for §11.3 (D-18)"), but in fact the template was MISSING the entire `#### CI mode (Phase 9)` subsection. Per Pattern §4 D-18, the byte-equality contract is the goal — so I added the full CI mode block with the Phase 12.2 --staged content baked in (rather than partial mirror). This closes a pre-existing Phase 9 → template gap. Documented as a key decision because future phases may rely on the §11.3 ↔ template equality.
- **canonical-AGENTS.md fixture regen as a separate commit:** the test failure (10/11) was anticipated by the plan ("Surface this in the SUMMARY if the regen step is required."). Kept it as a discrete commit (`8e65a97`) so the regen is auditable independently of the spec edit.
- **No CLAUDE.md manual edit:** all CLAUDE.md changes flow through `bin/sync-claude.sh` per the codified pattern; the pre-commit hook would have re-staged it on commit anyway, but pre-staging keeps each commit clean.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] schema/AGENTS.template.md was missing the entire Phase 9 §11.3 `#### CI mode` subsection**
- **Found during:** Task 1, when applying the same edit to both AGENTS.md and schema/AGENTS.template.md.
- **Issue:** The plan instruction (Edit 4) said "The §11.3 block is non-placeholder content; both files contain identical text in §11.3." But in fact the entire `#### CI mode (Phase 9)` subsection (Phase 9-06 origin) had never been mirrored to schema/AGENTS.template.md. The template's §11.3 ended at "Report-only..." and jumped straight to "**Steps:**".
- **Fix:** Added the full `#### CI mode (Phase 9)` subsection to schema/AGENTS.template.md with the Phase 12.2 `--staged` content baked in. This both satisfies the plan's D-18 byte-equality goal AND closes a pre-existing Phase 9 mirror gap.
- **Files modified:** `schema/AGENTS.template.md` (+54 lines).
- **Verification:** `diff <(awk '/^#### CI mode/{p=1} p{print} /^### 11\.4/{exit}' AGENTS.md) <(awk '/^#### CI mode/{p=1} p{print} /^### 11\.4/{exit}' schema/AGENTS.template.md)` returns no output → BYTE-EQUAL.
- **Committed in:** `ebe9304` (part of Task 1 commit).

**2. [Rule 3 - Blocking] schema/fixtures/canonical-AGENTS.md byte-equality fixture went stale**
- **Found during:** Final verification (Phase 9.1 test_byte_equality.sh failed 10/11).
- **Issue:** Adding the `#### CI mode (Phase 9)` subsection to schema/AGENTS.template.md changed the rendered AGENTS.md byte-pattern, so the canonical fixture (frozen at the prior render) no longer matches.
- **Fix:** Regenerated via `bash bin/init-wizard.sh --answers-file schema/fixtures/canonical-answers.yaml --render-to /tmp/wz-regen-12.2-04 && cp /tmp/wz-regen-12.2-04/AGENTS.md schema/fixtures/canonical-AGENTS.md` per the test failure's actionable hint.
- **Files modified:** `schema/fixtures/canonical-AGENTS.md` (+54 lines).
- **Verification:** Phase 9.1 tests went from 10/11 to 11/11.
- **Committed in:** `8e65a97` (separate commit so the regen is auditable).

**Total deviations:** 2 auto-fixed (both Rule 3 - Blocking issues that prevented the plan's byte-equality verification from passing).
**Impact on plan:** Both deviations were anticipated by the plan text ("if the canonical fixture goes stale ... regenerate it via the Phase 8-01 wizard render routine. Surface this in the SUMMARY if the regen step is required."). No scope creep — both fixes were prerequisites for the plan's stated success criteria.

## Issues Encountered

- **Pre-existing wiki/maintenance/lint-report.md neutrality finding** — `bash bin/check-neutrality.sh` exits 2 due to lines 64-65 of `wiki/maintenance/lint-report.md` (mentions `kahneman` and `personal-decision-journal`). Verified via stash + clean-tree check that this is a PRE-EXISTING condition committed in `a964beb` (the unrelated `ingest(hack-ai-coding-2026-05)` commit), NOT introduced by this plan's edits. Out-of-scope per executor scope-boundary rule. Logged here for awareness; remediation belongs to a future lint-report cleanup task.

## Verification Results

| Check | Expected | Actual | Status |
|-------|----------|--------|--------|
| `grep -c '^\| \`--staged\` \|' AGENTS.md` | 1 | 1 | PASS |
| `grep -c '^\| \`--staged\` \|' schema/AGENTS.template.md` | 1 | 1 | PASS |
| §11.3 byte-equality (AGENTS.md == schema/AGENTS.template.md, awk-extracted) | byte-equal | byte-equal | PASS |
| `diff AGENTS.md CLAUDE.md` | empty | empty | PASS |
| `grep -c '^## Local pre-commit write gate' docs/reference/ci.md` | 1 | 1 | PASS |
| `grep -c '^### Local pre-commit gate' CONTRIBUTING.md` | 1 | 1 | PASS |
| `grep -q 'Source of truth' docs/reference/ci.md` | found | found | PASS |
| `grep -q 'AGENTS.md §11.3' docs/reference/ci.md` | found | found | PASS |
| `grep -q 'install-hooks.sh' docs/reference/ci.md && grep -q 'install-hooks.sh' CONTRIBUTING.md` | found | found | PASS |
| `grep -q 'no-verify' docs/reference/ci.md && grep -q 'no-verify' CONTRIBUTING.md` | found | found | PASS |
| New content uses abstract placeholders only (denylist scan on git diff) | no matches | no matches | PASS |
| `bash bin/check-privacy.sh` | exit 0 | exit 0 | PASS |
| `bash bin/check-neutrality.sh` | exit 0 | exit 2 (pre-existing wiki/maintenance/lint-report.md) | PASS-with-note (pre-existing, out-of-scope) |
| `bash tests/phase-12.2/run.sh` | 11/11 | 11/11 | PASS |
| `bash tests/phase-09/run.sh` | 28/28 | 28/28 | PASS |
| `bash tests/phase-09.1/run.sh` | 11/11 | 11/11 | PASS (after fixture regen) |

## User Setup Required

None — no external service configuration required. The plan modifies template-public documentation only.

## Next Phase Readiness

- **AGENTS.md §11.3 is now the normative source of truth** for the Phase 12.2 `--staged` write-gate contract. Plan 12.2-05 (REQUIREMENTS.md flip + 12.2-VERIFICATION.md) can cite this plan's evidence directly:
  - WGATE-01: AGENTS.md §11.3 lines ~1054 (--staged flag row) + ~1071-1097 (Staged-mode rules subsection).
  - WGATE-04: §11.3 rule 8 ("Hook activation") documents `bash bin/install-hooks.sh` composition with the existing AGENTS.md ↔ CLAUDE.md sync check.
- **Byte-equality contract restored** for AGENTS.md ≡ CLAUDE.md ≡ schema/AGENTS.template.md (§11.3 specifically). Future template-public file edits should use `bin/sync-claude.sh` (auto via pre-commit hook) and the canonical-AGENTS.md fixture as drift-prevention guard rails.
- **Phase 12.2 Plan 05 (closure):** can proceed. All Plan-04 deliverables are landed and tested.
- **No blockers, no concerns.**

## Self-Check: PASSED

**Files verified to exist:**
- `/home/yishai/Documents/compendium/.planning/phases/12.2-local-wiki-write-gate/12.2-04-SUMMARY.md` (this file): created.
- `/home/yishai/Documents/compendium/AGENTS.md`: §11.3 amended (verified `grep -c '^| \`--staged\` |' AGENTS.md` returns 1).
- `/home/yishai/Documents/compendium/CLAUDE.md`: byte-equal to AGENTS.md (verified `diff` returns empty).
- `/home/yishai/Documents/compendium/schema/AGENTS.template.md`: §11.3 byte-equal mirror (verified awk-scoped diff).
- `/home/yishai/Documents/compendium/docs/reference/ci.md`: new section (verified `grep -c '^## Local pre-commit write gate'` returns 1).
- `/home/yishai/Documents/compendium/CONTRIBUTING.md`: new subsection (verified `grep -c '^### Local pre-commit gate'` returns 1).
- `/home/yishai/Documents/compendium/schema/fixtures/canonical-AGENTS.md`: regenerated (verified Phase 9.1 11/11).

**Commits verified to exist:**
- `c8c18ff` Task 0 — `git log --oneline | grep c8c18ff` returns the commit.
- `ebe9304` Task 1 — `git log --oneline | grep ebe9304` returns the commit.
- `8e65a97` Task 1b — `git log --oneline | grep 8e65a97` returns the commit.
- `aa72fec` Task 2 — `git log --oneline | grep aa72fec` returns the commit.

---
*Phase: 12.2-local-wiki-write-gate*
*Completed: 2026-05-04*
