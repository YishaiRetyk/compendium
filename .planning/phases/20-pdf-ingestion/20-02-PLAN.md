---
phase: 20-pdf-ingestion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - schema/reference/pdf-ingestion.md
  - schema/reference/source-types.md
  - schema/reference/frontmatter.md
  - schema/workflows/ingest.md
  - AGENTS.md
  - CLAUDE.md
  - schema/AGENTS.template.md
  - schema/fixtures/canonical-AGENTS.md
autonomous: true
requirements: [PDF-01, PDF-02, PDF-03]
user_setup: []

must_haves:
  truths:
    - "D-01: An agent reading schema/reference/ finds an authoritative pdf-ingestion.md holding BOTH the sub-case convention and the acquisition runbook"
    - "D-02: source-types.md's section-4 registry row for pdf is finalized with a format-orthogonal verdict and a Convention Doc pointer to pdf-ingestion.md"
    - "D-05: the registry verdict is format-orthogonal — PDF is an acquisition-path sub-case; content classifies to its parent type at Pass 0, never locked to a single parent type"
    - "D-04: pdf-ingestion.md defines a generic marker-emitting extractor contract with olmOCR 2 via Ollama (richardyoung/olmocr2:7b-q8) as the named worked instance"
    - "D-06: frontmatter.md documents the four flat PDF extraction fields (extraction_tool, extraction_model, extraction_date, original_asset)"
    - "D-08: pdf-ingestion.md states the tiered epistemic policy: born-digital to sourced (no verify mandate); degraded/scanned to tentative + N-page spot-verify, upgradeable"
    - "D-09: claims from PDF sources keep support_type: direct — hallucination risk is carried by epistemic_status, never by support type"
    - "D-03: AGENTS.md and CLAUDE.md both carry a routing-table row for pdf-ingestion.md and are byte-identical"
  artifacts:
    - path: "schema/reference/pdf-ingestion.md"
      provides: "Authoritative PDF sub-case convention + acquisition runbook (lazy-loaded)"
      contains: "page: N"
      min_lines: 60
    - path: "schema/reference/source-types.md"
      provides: "Finalized section-4 pdf registry row (format-orthogonal verdict + Convention Doc pointer)"
      contains: "pdf-ingestion.md"
    - path: "schema/reference/frontmatter.md"
      provides: "Four flat PDF extraction fields documented in the Source Summary block"
      contains: "extraction_tool"
    - path: "schema/workflows/ingest.md"
      provides: "Pass-0 PDF sub-bullet: classify to parent type, then layer the PDF convention"
      contains: "pdf-ingestion.md"
    - path: "AGENTS.md"
      provides: "Routing-table row pointing at pdf-ingestion.md"
      contains: "pdf-ingestion.md"
    - path: "schema/AGENTS.template.md"
      provides: "Wizard-source routing rows for pdf-ingestion.md + source-types.md (Phase 19 drift repair)"
      contains: "pdf-ingestion.md"
    - path: "schema/fixtures/canonical-AGENTS.md"
      provides: "Regenerated wizard-render fixture in sync with the template edit (setup-parity byte-equality gate — review round 3)"
      contains: "pdf-ingestion.md"
  key_links:
    - from: "schema/reference/source-types.md"
      to: "schema/reference/pdf-ingestion.md"
      via: "Convention Doc column pointer in the section-4 pdf row"
      pattern: "pdf-ingestion.md"
    - from: "AGENTS.md"
      to: "schema/reference/pdf-ingestion.md"
      via: "routing-table row"
      pattern: "pdf-ingestion.md"
    - from: "CLAUDE.md"
      to: "AGENTS.md"
      via: "byte-equality (bin/sync-claude.sh)"
      pattern: "pdf-ingestion.md"
---

<objective>
Author the authoritative PDF convention doc `schema/reference/pdf-ingestion.md` (sub-case convention + acquisition runbook) and wire it into the schema machinery exactly as Phase 19 wired source-types.md: finalize the `source-types.md` section-4 `pdf` registry row with a format-orthogonal verdict, add the four flat extraction fields to `frontmatter.md`, add a Pass-0 PDF pointer to `ingest.md`, and add a routing-table row to AGENTS.md + byte-synced CLAUDE.md.

Purpose: PDF is a format-orthogonal acquisition-path sub-case (D-05), NOT a new `source_type` enum value. The convention is lazy-loaded (D-01): not needed at Pass-0 classification time, so it lives in its own file with only a one-row pointer in source-types.md (D-02). This covers PDF-01 (doc half), PDF-02 (convention + fields), and PDF-03 (tiered epistemic policy).
Output: one new authoritative file + four in-place edits + the byte-synced CLAUDE.md mirror.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-pdf-ingestion/20-CONTEXT.md
@.planning/phases/20-pdf-ingestion/20-RESEARCH.md
@.planning/phases/20-pdf-ingestion/20-PATTERNS.md

<interfaces>
Structural model to mirror: schema/reference/source-types.md sections 5-6 (research-report worked instance).
The research-report section walks: Classification, Provenance, Citation registry, Epistemic default, Ingest checklist.

source-types.md section-4 registry — CURRENT provisional pdf row (L49):
  | `pdf` | sub-case of `article` or `paper` (provisional) | Acquisition (provisional) | (Phase 20) | Page locators (`#p`) already exist; ... |

source-types.md section-4 registry — FINALIZE to (format-orthogonal, D-05; Convention Doc pointer, D-02):
  | `pdf` | sub-case (format-orthogonal; any parent type) | Acquisition (always); Epistemic Default (degraded input only) | `schema/reference/pdf-ingestion.md` | verdict note |

frontmatter.md Source Summary block (L62-73) — the `type: source` YAML to extend with the 4 flat fields.
The page-marker + `#p` locator grammar is already fully specified in schema/reference/provenance.md L36-58 — do NOT redefine it; reference it.

AGENTS.md routing table (the "Resolvable references" table, ~L45-63) — add ONE row, topical placement near the source-types row.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author schema/reference/pdf-ingestion.md (convention + runbook)</name>
  <read_first>
    - schema/reference/source-types.md (section 5 "research-report — Worked Secondary Instance" L55-96 and section 6 Ingest Checklist — the structural model to mirror; header style L1-6)
    - schema/reference/provenance.md (L24-58 — #p locator + page-marker grammar to REFERENCE, not redefine)
    - wiki-cloud/overviews/pdf-text-extraction-for-llm-ingestion.md (the SOTA overview grounding the olmOCR-2 recommendation per D-04 — READ for content; do NOT embed its real slug in the template-public output, see neutrality note)
    - wiki-cloud/concepts/vlm-ocr-hallucination.md (the failure mode the tiered epistemic policy defends against — READ for content; do NOT embed its real slug in the template-public output)
    - .planning/phases/20-pdf-ingestion/20-CONTEXT.md (D-01..D-12 — the locked decisions this doc encodes)
    - CLAUDE.md (section 3 "What Agents Must NOT Do" — the neutrality placeholder rule for template-public files)
  </read_first>
  <action>
    Create `schema/reference/pdf-ingestion.md` as a self-contained authoritative reference. Mirror the source-types.md header + worked-instance structure. Required sections, in order:

    1. Header (copy the authoritative-reference style of source-types.md L1-6): an h1 "# PDF Ingestion", then a blockquote line "> Agent-authoritative reference for the PDF-as-sub-case convention and the PDF-to-Markdown acquisition runbook." and "> The AGENTS.md routing table points here. If you find a discrepancy between this file and AGENTS.md, this file wins.". Then a one-line "Use this file when ..." sentence.

    2. Classification — PDF is a format-orthogonal acquisition-path sub-case (D-05): at Pass 0 the content is classified normally to its parent type (article/paper/data/...); the PDF convention layers on top. Walk the 5 dimensions explicitly: only Acquisition changes unconditionally; Epistemic Default changes conditionally (degraded input only); Locator/Granularity/Drift are unchanged from the parent type. State plainly: PDF does NOT add a source_type enum value (anti-pattern, D-05).

    3. Frontmatter fields — the four flat snake_case fields (D-06), present ONLY on PDF-acquired sources: extraction_tool (the extractor that emitted the page markers; olmOCR is the worked instance), extraction_model (the model tag, e.g. richardyoung/olmocr2:7b-q8), extraction_date (ISO 8601 acquisition date — kept DISTINCT from ingested_at because acquisition date and wiki-ingest date can legitimately differ and flat fields stay independently Dataview-queryable; record this rationale here since the field ships in this wave and the decision is locked from here on — review round 2; the DR in Plan 04 restates it), original_asset (a bare co-located filename in the bundle, e.g. original.pdf — never a path with directory components). State that lint REQUIRES all four when original_asset points at a *.pdf (D-07; the check lands in Plan 03).

    4. #p locator usage — REFERENCE provenance.md (do not redefine): claims from PDF sources use the existing #p<N> page locators resolving against the page markers the acquisition emits; support_type stays direct (D-09 — PDF is primary; OCR is extraction, not derivation; hallucination risk is carried by epistemic_status, NEVER by support_type).

    5. Tiered epistemic policy (D-08, PDF-03) — the human classifies clean-vs-degraded at acquisition time (born-digital vs scan is trivially observable; no fragile heuristics): Born-digital/clean goes to normal "sourced" default, no verification mandate. Degraded/scanned goes to page-level epistemic_status: tentative default + MANDATORY spot-verification of N pages (use N = 3; selection = first page, a middle page, and the last page, which catches header/body/footer drift), upgradeable to sourced only after the spot-verified pages match the co-located PDF. Spot-verification is read-only: read the N pages against the co-located original_asset PDF, or resolve #p<N> via bin/audit-claims.sh (no new audit machinery). Describe the VLM-OCR confident-hallucination failure mode in one or two sentences of generic prose (plausible-but-wrong text emitted with high fluency on degraded input). NEUTRALITY (cross-AI review fix): do NOT embed the real vault slug of the local concept page — if pointing at vault context is useful, use the abstract placeholder form `wiki-cloud/concepts/<concept-slug>.md`. When mentioning the support-type anti-pattern in prose, write "the derived support type" rather than the literal frontmatter string, so the literal `support_type: derived` never appears in this file (keeps the negative acceptance grep meaningful).

    6. Acquisition runbook — document the generic contract (D-04): any PDF-to-Markdown extractor that emits the page markers satisfies the pipeline. Name the worked instance: olmOCR 2 via Ollama, model tag richardyoung/olmocr2:7b-q8, run via bin/pdf-extract.sh. Explain the pipeline reality: Ollama takes images, not PDFs — the loop is pdftoppm page-render to per-page model call to marker assembly. Prerequisites note: poppler (pdftoppm/pdfinfo), a reachable Ollama server, jq, base64. Two operational notes in the runbook (round 4): (1) the FIRST page's model call includes the cold model load and can exceed the default `PDF_EXTRACT_TIMEOUT=300` on a slow host — raise the env override rather than reading it as a hang; (2) the pipeline uses GNU `base64 -w0`, so the runbook targets GNU coreutils/Linux (macOS would need a portability shim — out of scope). Attribute the recommendation generically (cross-AI review fix — D-04 says cite the wiki's own SOTA overview, but this file is template-public): write "recommended per the local wiki's PDF-extraction tooling overview (`wiki-cloud/overviews/<overview-slug>.md`)" using the abstract placeholder — do NOT embed the real overview slug.

    7. Ingest checklist (mirror source-types.md section 6): 1) run "bin/pdf-extract.sh <input.pdf>" to get marked Markdown; 2) "bin/ingest.sh --asset <input.pdf> <extracted.md>" to co-locate the PDF in the bundle; 3) author the source summary with source_type = the content's PARENT type + the four extraction_* fields; 4) claims use #p<N> + support_type: direct; 5) if degraded then tentative + spot-verify N pages, else sourced.

    8. See Also — bare path-stub pointers to source-types.md, frontmatter.md, provenance.md, ingest.md (Phase 16 bare-pointer pattern).

    NEUTRALITY (MUST — template-public file): use abstract placeholders <source-id>, <YYYY-MM-DD-slug>, <page-title>, <input.pdf> for any example. Public tool names olmOCR / Ollama / poppler and the public model tag richardyoung/olmocr2:7b-q8 are PERMITTED (D-04). Do NOT use any real vault slug.
  </action>
  <verify>
    <automated>test -f schema/reference/pdf-ingestion.md && grep -qF 'page: N' schema/reference/pdf-ingestion.md && grep -q 'extraction_tool' schema/reference/pdf-ingestion.md && grep -qi 'tentative' schema/reference/pdf-ingestion.md && bash bin/check-neutrality.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -f schema/reference/pdf-ingestion.md` succeeds and `[ "$(wc -l < schema/reference/pdf-ingestion.md)" -ge 60 ]`
    - `grep -qF 'page: N' schema/reference/pdf-ingestion.md` succeeds (references the marker grammar)
    - All four field names present: `grep -q extraction_tool` and `grep -q extraction_model` and `grep -q extraction_date` and `grep -q original_asset` against the file
    - `grep -q 'format-orthogonal' schema/reference/pdf-ingestion.md` succeeds (D-05 verdict)
    - `grep -qi tentative` and `grep -qi 'spot-verif'` and `grep -qi 'born-digital'` against the file all succeed (tiered policy, PDF-03)
    - `grep -q 'support_type: direct' schema/reference/pdf-ingestion.md` succeeds AND `! grep -q 'support_type: derived' schema/reference/pdf-ingestion.md`
    - `grep -q 'pdftoppm' schema/reference/pdf-ingestion.md` succeeds (runbook present)
    - No real vault slugs leaked (review fix): `! grep -qE 'pdf-text-extraction-for-llm-ingestion|vlm-ocr-hallucination' schema/reference/pdf-ingestion.md`
    - `bash bin/check-neutrality.sh` exits 0
  </acceptance_criteria>
  <done>pdf-ingestion.md exists as an authoritative file covering classification (format-orthogonal), the four flat fields, #p locator usage with support_type direct, the tiered epistemic policy (born-digital/degraded), the acquisition runbook (generic contract + olmOCR worked instance), and an ingest checklist. Passes neutrality.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize source-types.md registry row + frontmatter.md fields + ingest.md pointer</name>
  <read_first>
    - schema/reference/source-types.md (section 3 Retro-fit Table L29-41 for dimension vocabulary; section 4 Evaluated Candidates registry L43-53, the provisional pdf row at L49)
    - schema/reference/frontmatter.md (Source Summary Additional Fields block L58-75 — the insertion point)
    - schema/workflows/ingest.md (Pass-0 step 3 L17-18, the research-report sub-bullet L18 — the precedent to parallel)
    - .planning/phases/20-pdf-ingestion/20-PATTERNS.md (source-types.md, frontmatter.md, ingest.md sections — exact edit shapes)
  </read_first>
  <action>
    Three in-place edits:

    (a) schema/reference/source-types.md section 4 — replace the provisional pdf row (L49). The new row's five columns are: Candidate = "`pdf`"; Verdict = "sub-case (format-orthogonal; any parent type)"; Dimensions That Change = "Acquisition (always); Epistemic Default (degraded input only)"; Convention Doc = "`schema/reference/pdf-ingestion.md`"; Notes = "Acquisition-path sub-case applicable to any document type; content classifies normally at Pass 0 (article/paper/data/...) and the PDF convention layers on. `#p` page locators + page markers already exist; claims stay `support_type: direct`; degraded scans get `tentative` + spot-verification (D-08)." Do NOT add `pdf` to the section-3 Retro-fit Table (that table is the 7 enum values; PDF is a sub-case, not an enum value). Do NOT touch the `video` or `repository` rows.

    (b) schema/reference/frontmatter.md — in the Source Summary Additional Fields YAML block (after the `source_type: ...` line at L67, before the `# Compilation tracking` comment at L69), add the four flat fields with the present-only-for-PDF note. Insert these lines: a blank line, then a comment "# PDF sub-case fields — present ONLY when the source was acquired from a PDF", then a comment "# (original_asset points at the co-located PDF). See schema/reference/pdf-ingestion.md.", then the four field lines:
        extraction_tool: olmocr
        extraction_model: "richardyoung/olmocr2:7b-q8"
        extraction_date: YYYY-MM-DD
        original_asset: original.pdf
        Keep the existing flat snake_case style. Do NOT add a nested YAML block (D-06: flat for Dataview). The conditional note MUST be prominent (cross-AI review round 2 — optional fields inside the shared YAML example otherwise read as required for ALL sources): the comment must say explicitly that the four fields are OMITTED entirely on non-PDF sources, not left empty. If the file documents source-summary fields in a Field Descriptions table, add a one-row description for each of the four fields matching that table's style (marked conditional/PDF-only); otherwise the inline YAML comments suffice.

    (c) schema/workflows/ingest.md — after the research-report sub-bullet (L18) under step 3, add a parallel PDF sub-bullet (one line, indented to match): "   - **PDF sources:** PDFs are a format-orthogonal acquisition sub-case, NOT a source_type. Classify the content to its parent type (article / paper / data / ...) as normal; then layer the PDF convention (extraction frontmatter fields, `#p` locators, tiered epistemic policy). See `schema/reference/pdf-ingestion.md`." Keep it a one-line pointer (lazy-load discipline, D-01). Do NOT introduce a `pdf` source_type.
  </action>
  <verify>
    <automated>grep -q 'format-orthogonal' schema/reference/source-types.md && grep -q 'pdf-ingestion.md' schema/reference/source-types.md && grep -q 'extraction_tool' schema/reference/frontmatter.md && grep -q 'pdf-ingestion.md' schema/workflows/ingest.md && bash bin/lint.sh --ci --dry-run --category yaml >/dev/null 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'format-orthogonal' schema/reference/source-types.md` AND `grep -q 'pdf-ingestion.md' schema/reference/source-types.md` both succeed
    - The pdf REGISTRY ROW no longer says provisional — anchor the negative grep to the table row, not the whole file (review round 2: a future prose line mentioning "pdf … provisional" must not break this): `! grep -E '^\| .pdf.' schema/reference/source-types.md | grep -q provisional` (no line beginning with the pdf table cell still contains "provisional"; the video line may still be provisional)
    - The section-3 Retro-fit Table still has exactly 7 type rows: `[ "$(grep -cE '^\| \`(article|paper|transcript|journal|data|image|research-report)\`' schema/reference/source-types.md)" -eq 7 ]`
    - All four fields present in frontmatter.md: `grep -q extraction_tool` and `grep -q extraction_model` and `grep -q extraction_date` and `grep -q original_asset` against schema/reference/frontmatter.md
    - `grep -q 'pdf-ingestion.md' schema/workflows/ingest.md` succeeds
    - frontmatter.md states the four fields are OMITTED on non-PDF sources (prominent conditional note — review round 2)
    - `bash bin/lint.sh --ci --dry-run --category yaml` exits 0 (review round 2: the UNSCOPED `--ci` gate is red today with 18 pre-existing crossref errors out of phase scope — scope every lint gate to the categories this phase touches; `--dry-run` keeps a verification-only run from rewriting lint-report.md and appending log.md entries. No source page yet carries original_asset, so the future conditional check is moot here)
  </acceptance_criteria>
  <done>The source-types.md pdf row is finalized (format-orthogonal verdict + Convention Doc pointer), the four flat extraction fields are documented in frontmatter.md, and ingest.md Pass-0 has a one-line PDF pointer. Lint stays green; the 7-value source_type enum is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Add the pdf-ingestion.md routing-table row to AGENTS.md and byte-sync CLAUDE.md</name>
  <read_first>
    - AGENTS.md (the "Resolvable references" routing table ~L45-63 — the row format to copy)
    - schema/AGENTS.template.md (the wizard-source routing table ~L44-63 — the second surface to mirror the row into; review round 2)
    - bin/sync-claude.sh (the byte-equality sync mechanism — how CLAUDE.md is regenerated; do NOT hand-edit CLAUDE.md)
    - schema/fixtures/README.md (the Regeneration Rule — whenever schema/AGENTS.template.md changes, canonical-AGENTS.md MUST be regenerated in the same commit; review round 3)
    - .planning/phases/20-pdf-ingestion/20-RESEARCH.md (Pitfall 6 — CLAUDE.md drift on the routing commit; the sync-then-add recipe)
  </read_first>
  <action>
    1. Edit AGENTS.md only. In the "Resolvable references" routing table, add ONE new row immediately AFTER the existing "| Adding/evaluating a new source type | `schema/reference/source-types.md` |" row (topical grouping with source-types): "> | Ingesting a PDF source (acquisition runbook + sub-case convention) | `schema/reference/pdf-ingestion.md` |". Match the exact "> | ... | ... |" blockquote-table cell format of the surrounding rows (note the leading "> " on each routing-table row).

    2. Update the AGENTS.md inclusion-audit baseline (cross-AI review fix — the resident-core header at AGENTS.md L7-8 requires every added line to be justified and the baseline re-recorded): the new row passes the inclusion test as `dispatch` (a routing-table pointer). Update the `<!-- inclusion-audit: N lines @ YYYY-MM-DD -->` comment at AGENTS.md L7 so N equals the new actual line count (`wc -l < AGENTS.md` AFTER the row is added) and the date is today's ISO date.

    3. Do NOT hand-edit CLAUDE.md. Regenerate it from AGENTS.md: run `bash bin/sync-claude.sh` then stage it. The Phase 09.1-02 precedent: `bash bin/sync-claude.sh && git add CLAUDE.md` BEFORE committing collapses the 2-attempt path to 1.

    4. Mirror the routing row into `schema/AGENTS.template.md` (cross-AI review round 2 — the template is the wizard source consumed by `bin/init-wizard.sh`; routers generated for new vaults would otherwise miss pdf-ingestion.md). The template's "Resolvable references" table (~L44-63) currently lacks even the source-types.md row — pre-existing Phase 19 drift, VERIFIED. Add BOTH rows to the template table, matching the SAME `> | ... | ... |` blockquote row format as the surrounding template rows (round 4, VERIFIED: the template's routing rows carry the same leading `> ` blockquote prefix as AGENTS.md — do NOT drop it; nothing downstream gates the prefix, so a dropped `> ` would silently corrupt the table and regenerate into the fixture): the Phase 19 row "| Adding/evaluating a new source type | `schema/reference/source-types.md` |" (drift repair, one line) and the new "| Ingesting a PDF source (acquisition runbook + sub-case convention) | `schema/reference/pdf-ingestion.md` |" immediately after it. Do NOT touch the template's `{{...}}` placeholders or any other section.

    5. Regenerate `schema/fixtures/canonical-AGENTS.md` (cross-AI review round 3, VERIFIED gap: the template edit in step 4 changes the wizard render, and `tests/phase-08/test_canonical_byte_equality.sh` + the `.github/workflows/setup-parity.yml` CI job assert byte-equality of the render against this fixture — a template edit WITHOUT the regenerated fixture in the same commit fails CI). Follow the schema/fixtures/README.md Regeneration Rule exactly: `bash bin/init-wizard.sh --answers-file schema/fixtures/canonical-answers.yaml --render-to /tmp/wz-regen && cp /tmp/wz-regen/AGENTS.md schema/fixtures/canonical-AGENTS.md`. Then confirm with `bash tests/phase-08/test_canonical_byte_equality.sh` (exit 0).

    6. The pdf-ingestion.md file MUST already exist (Task 1) so the routing lint category validates the new row points at a real file in the same commit.

    7. Pre-commit gates that must pass on this commit: `bin/sync-claude.sh --check` (byte-equality), `bin/check-neutrality.sh`, `bin/gen-skills.sh --check`, `bash tests/phase-08/test_canonical_byte_equality.sh` (fixture parity — round 3). Run them before committing.

    8. COMMIT SHAPE (review round 3): Tasks 1-3 land as ONE `schema:` commit — one logical operation (CLAUDE.md §3). This also makes two atomicity requirements trivial: the routing row and the file it points at land together (step 6), and the template edit and the regenerated fixture land together (the setup-parity CI gate fails if they are split across commits).
  </action>
  <verify>
    <automated>grep -q 'pdf-ingestion.md' AGENTS.md && grep -q 'pdf-ingestion.md' CLAUDE.md && grep -q 'pdf-ingestion.md' schema/AGENTS.template.md && grep -q 'pdf-ingestion.md' schema/fixtures/canonical-AGENTS.md && bash bin/sync-claude.sh --check && bash tests/phase-08/test_canonical_byte_equality.sh >/dev/null 2>&1 && bash bin/lint.sh --ci --dry-run --category routing >/dev/null 2>&1 && bash bin/check-neutrality.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'pdf-ingestion.md' AGENTS.md` succeeds (routing row added)
    - `grep -q 'pdf-ingestion.md' CLAUDE.md` succeeds (mirror synced)
    - `grep -q 'pdf-ingestion.md' schema/AGENTS.template.md` AND `grep -q 'source-types.md' schema/AGENTS.template.md` both succeed (wizard-source routing propagated + Phase 19 drift repaired — review round 2)
    - `bash bin/sync-claude.sh --check` exits 0 (AGENTS.md and CLAUDE.md byte-identical)
    - `cmp -s AGENTS.md CLAUDE.md` exits 0 (direct byte-equality confirmation)
    - `bash bin/lint.sh --ci --dry-run --category routing` exits 0 (the new row points at an existing file; --ci makes the exit code meaningful, --dry-run keeps the verification run from rewriting lint-report.md/log.md — review round 2)
    - Inclusion-audit baseline updated: `[ "$(grep -oP 'inclusion-audit: \K[0-9]+' AGENTS.md)" -eq "$(wc -l < AGENTS.md)" ]`
    - `grep -q 'pdf-ingestion.md' schema/fixtures/canonical-AGENTS.md` succeeds (fixture regenerated from the edited template — review round 3)
    - `bash tests/phase-08/test_canonical_byte_equality.sh` exits 0 (wizard render byte-equal to the regenerated fixture — the setup-parity CI gate this commit must survive)
    - `bash bin/check-neutrality.sh` exits 0
    - `bash bin/gen-skills.sh --check` exits 0 (skills overlay not drifted)
  </acceptance_criteria>
  <done>AGENTS.md carries a routing-table row for pdf-ingestion.md; CLAUDE.md is byte-synced via bin/sync-claude.sh; schema/AGENTS.template.md mirrors the row (plus the source-types.md drift repair); schema/fixtures/canonical-AGENTS.md is regenerated in the same commit and the byte-equality test passes; routing lint validates the pointer; neutrality and skills gates pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan edits template-public schema/reference + workflow docs and the router (AGENTS.md/CLAUDE.md). The only trust boundary is the template-public surface: a real vault slug or personal term leaking into published documentation.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Information Disclosure | pdf-ingestion.md / frontmatter.md / ingest.md / AGENTS.md (template-public) | mitigate | Use abstract placeholders (<source-id>, <YYYY-MM-DD-slug>, <page-title>) for all examples; public tool names olmOCR/Ollama/poppler permitted (D-04). bin/check-neutrality.sh runs as a pre-commit backstop on every edited file. |
| T-20-06 | Tampering | CLAUDE.md byte-drift from AGENTS.md | mitigate | Never hand-edit CLAUDE.md; regenerate via bin/sync-claude.sh and verify with --check (cmp -s). The pre-commit hook blocks a drifted commit. |
| T-20-07 | Tampering | routing row points at a non-existent file | mitigate | pdf-ingestion.md is authored in Task 1 within the same plan/commit; bin/lint.sh --category routing validates forward-path existence + inverse reachability before commit. |
</threat_model>

<verification>
- schema/reference/pdf-ingestion.md exists, >= 60 lines, passes neutrality, covers all 8 required sections.
- source-types.md pdf row finalized (format-orthogonal + Convention Doc pointer); the 7-value source_type enum and Retro-fit Table untouched.
- frontmatter.md documents the four flat extraction fields.
- ingest.md Pass-0 has the one-line PDF pointer.
- AGENTS.md routing row added with inclusion-audit baseline updated; cmp -s AGENTS.md CLAUDE.md exits 0; bash bin/lint.sh --ci --category routing exits 0.
- schema/AGENTS.template.md carries the pdf-ingestion.md routing row (plus the source-types.md drift-repair row) — review round 2.
- schema/fixtures/canonical-AGENTS.md regenerated in the same commit as the template edit; `bash tests/phase-08/test_canonical_byte_equality.sh` exits 0 (setup-parity gate — review round 3). Tasks 1-3 land as ONE `schema:` commit.
- `bash bin/lint.sh --ci --dry-run --category yaml` exits 0 (review round 2: unscoped `--ci` is red today with 18 pre-existing crossref errors out of phase scope); bash bin/check-neutrality.sh exits 0; bash bin/gen-skills.sh --check exits 0.
- No real vault slugs in schema/reference/pdf-ingestion.md (abstract placeholders only; public tool names permitted).
</verification>

<success_criteria>
- PDF-01 (doc half): the acquisition runbook is documented in an authoritative schema/ file an agent can find via the routing table.
- PDF-02 (convention): the four flat extraction fields are documented; claims use existing #p locators; support_type stays direct.
- PDF-03: the tiered VLM-hallucination epistemic policy (born-digital to sourced; degraded to tentative + spot-verify) is written into the convention.
</success_criteria>

<output>
After completion, create `.planning/phases/20-pdf-ingestion/20-02-SUMMARY.md`.
</output>
