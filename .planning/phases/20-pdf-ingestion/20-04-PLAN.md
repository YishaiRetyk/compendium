---
phase: 20-pdf-ingestion
plan: 04
type: execute
wave: 3
depends_on: ["20-01", "20-02", "20-03"]
files_modified:
  - sources/
  - wiki-cloud/sources/
  - wiki-cloud/entities/
  - wiki-cloud/concepts/
  - wiki-cloud/overviews/
  - wiki-cloud/comparisons/
  - wiki-cloud/index.md
  - wiki-cloud/log.md
  - wiki-cloud/decisions/
  - wiki-local/maintenance/
autonomous: false
requirements: [PDF-04]
user_setup: []

must_haves:
  truths:
    - "D-12: One real PDF (user-supplied, clean born-digital, cloud-safe, ~5-30 pages) has been acquired via bin/pdf-extract.sh, ingested, and its wiki pages live in wiki-cloud/sources/ with #p-anchored provenance"
    - "The original PDF co-exists as a bundle asset alongside source.md in the dated sources/ bundle dir"
    - "The source summary records extraction_tool, extraction_model, extraction_date, original_asset and source_type = the content's parent type"
    - "bin/audit-claims.sh resolves the #p locators on the ingested source against the <!-- page: N --> markers"
    - "A schema-update decision record captures PDF-as-format-orthogonal-sub-case + first local-model-invoking script"
  artifacts:
    - path: "wiki-cloud/sources/"
      provides: "Source summary page for the ingested PDF with extraction fields + #p-anchored claims"
      contains: "extraction_tool"
    - path: "sources/"
      provides: "Dated bundle dir holding source.md (the page-marked Markdown) + the original PDF asset"
    - path: "wiki-cloud/decisions/"
      provides: "schema-update DR for the PDF sub-case decision (Phase 18/19 precedent)"
      contains: "trigger_type: schema-update"
  key_links:
    - from: "wiki-cloud/sources/<source-id>.md"
      to: "sources/.../source.md page markers"
      via: "#p<N> locators in claim provenance"
      pattern: "#p[0-9]"
    - from: "wiki-cloud/sources/<source-id>.md frontmatter"
      to: "the co-located PDF"
      via: "original_asset relative path"
      pattern: "original_asset"
---

<objective>
Exercise the entire PDF pipeline end-to-end on one real, user-supplied PDF (PDF-04): acquire via `bin/pdf-extract.sh`, ingest via `bin/ingest.sh --asset` (co-locating the original PDF as a bundle asset), author the source summary + topic pages with `#p`-anchored provenance and the four extraction fields, verify with `bin/audit-claims.sh`, and author the schema-update decision record. The PDF is supplied by the user at a `checkpoint:human-action` (D-12: one clean born-digital cloud-safe PDF, ~5-30 pages).

Purpose: This is the only task in the phase that runs a real model inference and proves the convention works in anger, not just at the convention level. It validates SC-4 (real PDF acquired, ingested, page-anchored, bundle-co-located).
Output: a new dated bundle in sources/, a source summary + topic pages in wiki-cloud/, updated index.md + log.md, and a decision record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-pdf-ingestion/20-CONTEXT.md
@.planning/phases/20-pdf-ingestion/20-RESEARCH.md
@.planning/phases/20-pdf-ingestion/20-PATTERNS.md

<interfaces>
The authoritative convention to FOLLOW (authored in Plan 02): schema/reference/pdf-ingestion.md — its Ingest checklist is the script for this plan.
The tools to use (Plans 01 + 03): bin/pdf-extract.sh (acquisition), bin/ingest.sh --asset (bundle), bin/audit-claims.sh (read-only #p resolution).
The ingest workflow: schema/workflows/ingest.md — the full classify/diff/extract/merge/lint/log/commit procedure. PDF classifies to its content PARENT type (article/paper/data/...), NOT a pdf source_type.
DR analog: wiki-cloud/decisions/dr-2026-06-10-source-type-contract.md (Phase 19 schema-update DR — same trigger_type, same domain).

privacy: sources/ is cloud-safe-only (D-12). The validation artifact MUST be a cloud-safe PDF — verify before ingest. The ingested wiki pages use REAL slugs (sources/ + wiki-cloud/ are NOT template-public — real terms are fine here).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Obtain the validation PDF from the user</name>
  <files>(none — collects a user-supplied path)</files>
  <action>
    Pause and request the validation PDF from the user. Ask for ONE PDF meeting all D-12 criteria: clean born-digital (NOT a scan), cloud-safe (no private/local-only content — it will be committed to the cloud-safe sources/ tree), ~5-30 pages. When the user supplies an absolute path, run `pdfinfo <path>` to confirm it is a valid PDF with a page count in range, and ask the user to confirm the content is cloud-safe. Do NOT proceed to Task 2 until a valid path is confirmed (or the user says "skip" to defer PDF-04).
  </action>
  <what-built>The full PDF pipeline (pdf-extract.sh, the convention, --asset, lint enforcement) is ready. This checkpoint collects the one real artifact needed to exercise it.</what-built>
  <how-to-verify>
    Ask the user to supply ONE PDF meeting all of these (D-12):
    1. Clean born-digital (NOT a scan — born-digital gets the `sourced` tier; a scan would need the degraded tier, which is out of scope for this validation per the Deferred Ideas).
    2. Cloud-safe (no private/local-only content — sources/ is cloud-safe-only; the original will be committed).
    3. ~5-30 pages (local 7B-q8 inference practicality).
    Have the user place it at a path and tell you the absolute path. Confirm: run `pdfinfo <path>` to verify page count is in range and the file is a valid PDF; run `du -h <path>` and sanity-check the size (a few MB at most — `--asset` has no size guard, and a binary blob in git history is effectively permanent: removing it later requires a history rewrite, so the cloud-safety call must be firm now, not revisable). Confirm with the user that the content is cloud-safe before proceeding.
  </how-to-verify>
  <resume-signal>Provide the absolute path to a clean born-digital cloud-safe PDF (~5-30 pages), or say "skip" to defer PDF-04 validation.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Acquire + ingest the PDF, author pages with #p provenance, verify, log</name>
  <files>sources/, wiki-cloud/sources/, wiki-cloud/index.md, wiki-cloud/log.md</files>
  <read_first>
    - schema/reference/pdf-ingestion.md (the Ingest checklist authored in Plan 02 — this is the exact procedure to follow)
    - schema/workflows/ingest.md (the full classify/diff/extract/merge/lint/log/commit procedure)
    - schema/reference/provenance.md (L24-58 — #p locator + page-marker grammar for authoring claim provenance)
    - schema/reference/frontmatter.md (Source Summary Additional Fields — the four extraction fields + base source fields)
    - schema/reference/page-types.md (section order + authoring conventions for the source summary and the topic pages created in the merge pass — review round 3)
    - schema/reference/log-format.md (the authoritative index entry + log entry shapes — review round 3)
    - schema/reference/privacy.md (tier routing rules — confirms the cloud placement of every page this ingest writes — review round 3)
    - wiki-cloud/index.md and wiki-cloud/log.md (the navigation artifacts to update; the log entry format)
    - bin/pdf-extract.sh, bin/ingest.sh, bin/audit-claims.sh (the tools to invoke)
  </read_first>
  <action>
    Follow the pdf-ingestion.md Ingest checklist with the user-supplied PDF at <PDF_PATH>:

    1. ACQUIRE: run `bin/pdf-extract.sh --out /tmp/extracted.md <PDF_PATH>`. This renders each page (pdftoppm), OCRs via Ollama olmocr2:7b-q8, and assembles `<!-- page: N -->` markers. Confirm the marker count equals `pdfinfo <PDF_PATH>` page count (Pitfall 3). If the model output is poor at default 150 DPI, re-run tuning DPI (e.g. 200) and/or the model context via the env hook (`OLLAMA_OPTIONS_JSON='{"num_ctx":8192}'` — dense pages, review round 2) — this is the "tune on real artifact" step (RESEARCH A2). Spot-check 2-3 pages of /tmp/extracted.md against the PDF visually for gross OCR sanity.

    2. CLASSIFY (Pass 0): read the extracted content; classify to its PARENT type (article / paper / data / ...). Do NOT use a `pdf` source_type (D-05). Choose a real kebab-case slug from the document's actual title (real slugs are fine here — sources/ is NOT template-public).

    3. INGEST with asset: run `bin/ingest.sh --slug <slug> --asset <PDF_PATH> /tmp/extracted.md`. This creates `sources/YYYY/YYYY-MM/YYYY-MM-DD-<slug>/` with `source.md` (the page-marked Markdown) AND the original PDF co-located. Confirm both files exist in the bundle dir.

    4. EXTRACT + author the source summary at `wiki-cloud/sources/src-YYYY-MM-DD-<slug>.md`:
       - Full base frontmatter (id, title, type: source, status: active, summary, created_at, updated_at, sources, epistemic_status, tags, domains, supersedes/superseded_by null, aliases, has_contradictions: false, knowledge_domain).
       - Source extra fields: path (to the bundle source.md), content_hash, ingested_at, source_type (the PARENT type), compilation_status.
       - The four PDF fields (D-06): extraction_tool (olmocr), extraction_model ("richardyoung/olmocr2:7b-q8"), extraction_date (today), original_asset (the relative basename of the co-located PDF, e.g. original.pdf).
       - epistemic_status: sourced (born-digital clean tier — D-08; no spot-verification mandate for clean input).
       - Claims with `[prov:src-YYYY-MM-DD-<slug>#p<N>|direct]` provenance — support_type: direct (D-09), `#p<N>` page locators pointing at the correct page markers.

    5. MERGE (Pass 3): create/update entity/concept/overview topic pages from the extracted claims, each claim carrying its `#p<N>|direct` provenance. Wikilinks on first mention (`[[id|Title]]`). Update the source summary compilation tracking fields (compilation_status, compiled_against_hash, compiled_targets) per ingest.md step 6a.

    6. RECORD the pre-ingest base ref (consumed by step 10's POST-COMMIT audit): `PRE_REF=$(git rev-parse HEAD)`, taken before the step-9 ingest commit. If re-deriving in a later session, PRE_REF is the parent of the `ingest(<slug>)` commit: `PRE_REF=$(git rev-parse '<ingest-commit-sha>^')` (cross-AI review round 3 — the audit itself moves to step 10, AFTER the commit; see there for why).

    7. LINT: run `bin/lint.sh --ci --dry-run --category yaml` — it MUST exit 0 (review round 2: the UNSCOPED `--ci` gate is red today with 18 pre-existing crossref errors unrelated to this phase; fixing them is its own logical operation, out of scope — optionally file them as a separate lint-fix todo). The yaml category contains the new conditional check, which now fires on this source (it has original_asset=*.pdf) and confirms all four extraction fields are present. Additionally assert the ingest introduced no NEW crossref errors (delta check, not absolute): capture the crossref error count before and after the ingest (`bash bin/lint.sh --ci --dry-run --category crossref 2>&1 | grep -c ERROR || true` style) and require after <= before. Fix any finding the new pages caused before committing. NOTE (review round 3): new pages sharing domains/tags with existing pages can create NEW missing-cross-reference errors that are only resolvable by adding backlinks to EXISTING pages — that is a normal part of the merge pass (Pass 3), not scope creep; do not read such a delta failure as a regression, finish the merge. Reserve ONE non-dry-run `bin/lint.sh` invocation for the run whose lint-report.md/log.md updates are intentionally part of the ingest commit; all other verification runs use `--dry-run`.

    7b. CLOUD-SAFETY GUARD (review round 2): run `bash bin/check-sources-cloud-safe.sh` post-ingest, pre-commit — the existing fail-closed PRIV-03 guard and, since this is the first-ever bundle with a binary asset under sources/, a compatibility check that the guard tolerates the new bundle shape. SCOPE (review round 3 — do not overstate): the guard checks exactly two things — no raw source carries `privacy: local_only` frontmatter, and no `sources/local-only/` dir exists. It parses source Markdown frontmatter only; it CANNOT inspect the PDF binary's content. Human confirmation (Task 1) stays the primary content-level check; this is the mechanical backstop.

    8. NAV: update `wiki-cloud/index.md` with the new + modified pages; append an `## [YYYY-MM-DD] ingest | <source title>` entry to `wiki-cloud/log.md` listing affected pages + rationale.

    9. COMMIT: one commit, `ingest(<slug>): <one-line summary>` (one logical operation even if it touches many files — CLAUDE.md §3).

    10. AUDIT (POST-COMMIT — cross-AI review round 3; supersedes the round-2 worklist assertions, BOTH of which were defective, VERIFIED at source: (a) `--emit-worklist` entries carry NO `verdict` key (audit-claims.sh usage: "no verdict"), and claims whose locator fails to resolve hit `add_finding('insufficient-locator', ...)` and are EXCLUDED from the worklist before append — so a worklist-based `verdict == "insufficient-locator"` filter is structurally vacuous and can never fail, for any input; (b) the recency selector resolves changed pages via `git diff $PRE_REF...HEAD` over committed changes only (git_diff_changed_pages, audit-claims.sh:577-587) — run before the ingest commit the diff is empty, the new source is never selected, and the selection assertion fails spuriously). Run the audit AFTER the step-9 commit and assert over FINDINGS, each of which carries `source_id` AND `verdict` (add_finding shape, audit-claims.sh:745-757):
       - `FINDINGS=$(bash bin/audit-claims.sh --since "$PRE_REF" --sample 100 --format json 2>/dev/null)` — the generous `--sample` plus `--since` guarantees the new source's claims are actually selected (the default sample-20 priority ranking gives no such guarantee).
       - Selection assertion (non-vacuous — recency now fires because the commit exists): `echo "$FINDINGS" | jq -e --arg sid "<source-id>" '[.[] | select(.source_id == $sid)] | length > 0'`.
       - Resolution assertion: `echo "$FINDINGS" | jq -e --arg sid "<source-id>" '[.[] | select(.source_id == $sid and .verdict == "insufficient-locator")] | length == 0'`. In no-verifier mode every RESOLVABLE claim yields an `insufficient`/"verifier not run" placeholder finding and every UNRESOLVABLE locator yields an `insufficient-locator` finding — so the two assertions together read exactly "selected, and all locators resolved", non-vacuously.
       - Optional completeness check (selector-independent): count `[prov:<source-id>#p` markers across the new wiki pages and compare against the source-scoped findings count — equal means no claim escaped the sample; if the findings count is LOWER, raise `--sample` and re-run rather than weakening the assertion.
       - Do NOT use a global `! grep -q insufficient-locator` over unscoped output — it can NEVER pass while the Phase 19 `#sec:` residue exists (~71% insufficient-locator on derived-report claims, tracked in 19-REVIEW.md).
       - SIDE EFFECT (review round 2, VERIFIED): every audit mode rewrites `wiki-local/maintenance/audit-report.md` and advances `audit-state.md`. This is by design (D-15 checkpoint), not read-only. Both files are wiki-local control-plane (never pushed) — leave them as uncommitted local state (running post-commit makes this the natural outcome); do NOT treat their mutation as an anomaly and do NOT fold them into the cloud-facing ingest commit.
       This is spot-verification with existing machinery only; no new audit flags are added in this phase. If a push is planned, the ordering constraint is commit-then-audit, not push-then-audit — run the audit before any push.

    PRIVACY: verify the PDF + extracted content are cloud-safe before committing to sources/ (D-12). If the content turns out local-only, STOP — do NOT ingest it anywhere (cross-AI review fix: a wiki-local/ ingest would be privacy-safe but FAILS PDF-04, which requires the artifact in cloud-safe sources/). Return to the Task 1 checkpoint and request a replacement cloud-safe PDF instead.
  </action>
  <verify>
    <automated>bash bin/lint.sh --ci --dry-run --category yaml && bash bin/check-sources-cloud-safe.sh && FINDINGS=$(bash bin/audit-claims.sh --since "$PRE_REF" --sample 100 --format json 2>/dev/null) && echo "$FINDINGS" | jq -e --arg sid "<source-id>" '[.[] | select(.source_id == $sid)] | length > 0' && echo "$FINDINGS" | jq -e --arg sid "<source-id>" '[.[] | select(.source_id == $sid and .verdict == "insufficient-locator")] | length == 0'</automated>
    <!-- Round 3: the audit assertions MUST run AFTER the step-9 ingest commit (the recency selector diffs committed changes only). PRE_REF = the parent of the ingest(<slug>) commit; substitute <source-id> with the real source id. -->
  </verify>
  <acceptance_criteria>
    - A new dated bundle exists under sources/ containing BOTH source.md and a *.pdf (a bundle dir with both `source.md` and `*.pdf` present)
    - The bundle source.md carries page markers (review fix: `sources/**/source.md` needs globstar, which is off by default — use find): `find sources -name source.md -exec grep -l '^<!-- page:' {} +` returns at least one path
    - A source summary page exists in wiki-cloud/sources/ with all four extraction fields (grep the new summary for extraction_tool, extraction_model, extraction_date, original_asset)
    - The summary's source_type is a PARENT type, NOT pdf: `! grep -q 'source_type: pdf'` against the new file
    - The summary's original_asset is a bare co-located filename (no directory components — matches the Plan 03 lint guard)
    - At least one claim uses a #p locator with direct support: `grep -qE '#p[0-9]'` and a `|direct` marker against the new wiki pages
    - `bash bin/lint.sh --ci --dry-run --category yaml` exits 0 (the conditional PDF-field check now fires and passes on the real source; review round 2: unscoped `--ci` is red today with 18 pre-existing crossref errors out of phase scope)
    - The crossref error count did not increase vs the pre-ingest baseline (delta check — the new pages introduced no NEW crossref errors)
    - `bash bin/check-sources-cloud-safe.sh` exits 0 (mechanical D-12 backstop; also proves the guard tolerates the first binary-asset bundle — review round 2)
    - The source-scoped FINDINGS assertions (run POST-COMMIT — review round 3) pass BOTH ways: the new source's claims WERE selected (`--format json` findings filtered by source_id, length > 0 — no vacuous pass) AND none carry `verdict: insufficient-locator` (jq filter on source_id, NOT a global grep). Worklist-based assertions are forbidden here — worklist entries carry no verdict and exclude unresolvable locators entirely
    - `wiki-cloud/index.md` and `wiki-cloud/log.md` reference the new source
    - `wiki-local/maintenance/audit-report.md` + `audit-state.md` mutations from the audit run are expected control-plane writes, not anomalies (review round 2)
  </acceptance_criteria>
  <done>One real PDF is acquired, ingested with --asset (PDF co-located), source summary + topic pages authored with #p|direct provenance and the four extraction fields, committed as one ingest, and the POST-COMMIT source-scoped audit findings show the source selected with every locator resolving; scoped lint (yaml) green with no new crossref errors, cloud-safe guard green, index + log updated.</done>
</task>

<task type="auto">
  <name>Task 3: Author the schema-update decision record</name>
  <files>wiki-cloud/decisions/dr-2026-06-11-pdf-ingestion.md</files>
  <read_first>
    - wiki-cloud/decisions/dr-2026-06-10-source-type-contract.md (Phase 19 schema-update DR — frontmatter + body structure to mirror)
    - schema/reference/page-types.md (decision page section order + required fields, if needed)
    - .planning/phases/20-pdf-ingestion/20-CONTEXT.md (the decisions D-01..D-12 to summarize in the DR)
  </read_first>
  <action>
    Create `wiki-cloud/decisions/dr-2026-06-11-pdf-ingestion.md` mirroring the Phase 19 source-type-contract DR. Frontmatter: id dr-2026-06-11-pdf-ingestion; title "PDF as Format-Orthogonal Source Sub-Case + First Local-Model Acquisition Script"; type decision; status active; summary one sentence; created_at/updated_at 2026-06-11 (or today); sources []; epistemic_status sourced; tags [meta, schema]; domains [wiki-infrastructure]; supersedes/superseded_by null; aliases [dr-2026-06-11-pdf-ingestion]; has_contradictions false; knowledge_domain software; trigger_type schema-update; affected_pages [] (or list the schema files touched).

    Body (mirror dr-2026-06-10: `## TL;DR` then `## Decision` with numbered coupled deliverables): capture the load-bearing decisions — (1) PDF is a format-orthogonal acquisition-path sub-case, NOT a new source_type (D-05); (2) lazy-loaded authoritative pdf-ingestion.md + lean registry-row pointer (D-01/D-02); (3) four flat extraction frontmatter fields + conditional lint enforcement (D-06/D-07); (4) tiered epistemic policy for VLM hallucination — born-digital sourced / degraded tentative+spot-verify, support_type stays direct (D-08/D-09); (5) bin/pdf-extract.sh is the FIRST repo script to invoke a local model — the ingest.sh "no LLM calls" charter is per-script, not repo-wide (D-10). Include ONE sentence recording why `extraction_date` is kept distinct from `ingested_at` (acquisition date and wiki-ingest date can legitimately differ; flat fields stay independently Dataview-queryable) — this closes the RESEARCH open question that the plans otherwise resolve silently (review fix). Note this DR aids Phase 21 (video), which inherits the format-orthogonal pattern.

    NAV (review fix): add the DR to `wiki-cloud/index.md` (decisions listing) and append a log entry to `wiki-cloud/log.md` per the log-format reference — a new decision page is navigable content, same as any other page.

    NEUTRALITY: wiki-cloud/decisions/ is a cloud tier but the DR is about template-public schema — avoid quoting the validation artifact's real slug; speak in terms of the convention. Public tool names permitted.

    Lint: trigger_type must be `schema-update` (valid); affected_pages must be a YAML list. Run `bin/lint.sh --ci --dry-run --category yaml` after (the decision-page checks live in the yaml category; unscoped `--ci` is red today from out-of-scope crossref backlog — review round 2).

    Commit: `reflect(pdf-ingestion): record PDF sub-case schema decision` (or roll under the ingest commit if authored in the same operation — but a standalone DR is cleaner as its own reflect-prefixed commit).
  </action>
  <verify>
    <automated>test -f wiki-cloud/decisions/dr-2026-06-11-pdf-ingestion.md && grep -q 'trigger_type: schema-update' wiki-cloud/decisions/dr-2026-06-11-pdf-ingestion.md && bash bin/lint.sh --ci --dry-run --category yaml</automated>
  </verify>
  <acceptance_criteria>
    - `test -f wiki-cloud/decisions/dr-2026-06-11-pdf-ingestion.md` succeeds
    - `grep -q 'trigger_type: schema-update' wiki-cloud/decisions/dr-2026-06-11-pdf-ingestion.md` succeeds
    - `grep -q 'type: decision' wiki-cloud/decisions/dr-2026-06-11-pdf-ingestion.md` succeeds
    - `grep -qi 'format-orthogonal' wiki-cloud/decisions/dr-2026-06-11-pdf-ingestion.md` succeeds
    - `grep -qi 'extraction_date' wiki-cloud/decisions/dr-2026-06-11-pdf-ingestion.md` succeeds (kept-distinct rationale recorded)
    - `grep -q 'dr-2026-06-11-pdf-ingestion' wiki-cloud/index.md` succeeds (DR indexed)
    - `bash bin/lint.sh --ci --dry-run --category yaml` exits 0 (decision-page validation lives in the yaml category: valid trigger_type, affected_pages is a list; review round 2 — unscoped `--ci` red today from out-of-scope crossref backlog)
  </acceptance_criteria>
  <done>A schema-update decision record documents the PDF sub-case decisions; it lints clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification of the end-to-end ingest</name>
  <files>(none — read-only human review of the ingested pages)</files>
  <action>
    Pause for the user to verify the end-to-end ingest. Present the new bundle dir under sources/ (source.md with page markers + the original PDF), the new source summary in wiki-cloud/sources/ (four extraction fields, parent source_type, #p|direct claims), and the confirmation that `bin/lint.sh` is green and `bin/audit-claims.sh` resolves the locators. Ask the user to spot-check 2-3 cited #p pages against the original PDF for faithfulness. Wait for explicit approval before marking the phase verification complete.
  </action>
  <what-built>One real PDF was acquired via the pipeline, ingested with the original co-located as a bundle asset, authored into wiki pages with #p-anchored provenance, audited, and a decision record written.</what-built>
  <how-to-verify>
    1. Open the new bundle dir under sources/ — confirm BOTH source.md (with `<!-- page: N -->` markers) and the original PDF are present.
    2. Open the new source summary in wiki-cloud/sources/ — confirm the four extraction fields, source_type = a parent type (not pdf), and that claims carry `#p<N>|direct` provenance pointing at sensible pages.
    3. Spot-check 2-3 claims: open the cited #p page in the original PDF and confirm the claim text faithfully reflects that page (born-digital sanity check — catches any gross OCR/hallucination drift).
    4. Confirm the scoped lint gate (`bin/lint.sh --ci --dry-run --category yaml`) is green and the POST-COMMIT source-scoped audit findings (`--format json`, filtered by source_id) show the new source selected with no `insufficient-locator` verdict.
  </how-to-verify>
  <resume-signal>Type "approved" if the ingest is faithful and complete, or describe issues (e.g. wrong page anchoring, OCR drift, missing fields) to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied PDF -> sources/ (committed, cloud-safe) | A private/local-only PDF must not be committed to the cloud-safe sources/ tree |
| PDF content -> Ollama local model | base64 page images POSTed to localhost (inherited from pdf-extract.sh) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-11 | Information Disclosure | committing a non-cloud-safe PDF to sources/ | mitigate | The human-action checkpoint (Task 1) explicitly requires the user to confirm the PDF is cloud-safe AND treats the call as irreversible (binary in git history); Task 2 re-verifies before commit and on a local-only finding STOPS and requests a replacement PDF — never a wiki-local fallback, which would fail PDF-04 (D-12). sources/ is cloud-safe-only by convention. |
| T-20-12 | Repudiation/Faithfulness | VLM hallucination ingested as high-confidence | mitigate | Born-digital clean tier (D-08) + the Task 4 human spot-check of 2-3 cited #p pages against the original PDF + bin/audit-claims.sh resolution catch gross OCR/hallucination drift before approval. |
| T-20-13 | Information Disclosure | real vault slug leaking into the DR (template-adjacent) | accept | The DR lives in a cloud tier and is about the convention, not the artifact; it avoids quoting the validation slug. Low risk — the DR is not a template-public surface (bin/check-neutrality.sh does not gate wiki-cloud/decisions/). |
</threat_model>

<verification>
- A new sources/ bundle holds source.md (page-marked) + the original PDF asset.
- A wiki-cloud/sources/ summary records the four extraction fields, a parent source_type, and #p|direct claims.
- The POST-COMMIT source-scoped audit findings (`--since <pre-ingest-ref> --sample 100 --format json` + jq filter on source_id — review round 3: findings, never the verdict-less worklist; run AFTER the ingest commit, the recency selector diffs committed changes only) show the new source's claims selected with no insufficient-locator verdict; `bash bin/lint.sh --ci --dry-run --category yaml` exits 0 (conditional check now exercised on a real PDF source; unscoped `--ci` red today from out-of-scope crossref backlog); no NEW crossref errors vs the pre-ingest baseline; `bash bin/check-sources-cloud-safe.sh` exits 0.
- A schema-update DR exists, lints clean, records the extraction_date rationale, and is referenced from index.md + log.md.
- index.md + log.md updated; human checkpoint approved.
</verification>

<success_criteria>
- PDF-04: one real PDF acquired via the pipeline, ingested, wiki pages in sources/ + wiki-cloud/ with page-anchored provenance, original PDF co-located as a bundle asset.
- The phase's conditional lint check is proven non-vacuously (it now fires and passes on a real original_asset=*.pdf source).
</success_criteria>

<output>
After completion, create `.planning/phases/20-pdf-ingestion/20-04-SUMMARY.md`.
</output>
