# Plan 24-02 Summary — common/ over-extraction (wave 2)

**Executed:** 2026-07-03. **Requirements:** PKG-02 (+ TEST-06 seed). **Status:** Complete.

## What shipped

- `src/compendium/common/` — 6 modules: verbatim byte-identical lifts of the 5 `bin/lib/*.py`
  files (`privacy.py`, `yaml_rt.py`, `walk.py`, `classify.py`, `provenance.py`; zero cross-imports
  confirmed — all leaf modules) + the new `page.py` extracted from the AUTHORITATIVE `bin/lint.sh`
  heredoc at LINT_VERSION 1.12.0 (per RB-3: the extracted set is `_FENCE_OPEN_RE` + `_mask_fences`
  + `mask_markdown` + `_HTMLCOM_RE`/`_INLINE_RE`/`_FM_RE` + `PROV_RE` + `EPISTEMIC_INLINE_RE` +
  wikilink regexes + `EXCLUDE_*` + `parse_frontmatter`, plus audit-claims' `parse_frontmatter_str`
  in-memory variant — the plan's `_FENCE_RE` no longer exists).
- `common/__init__.py` re-exports the stable names (verified against each module's actual
  `__all__`/defs, not the plan's indicative list).
- `tests/test_common_extraction.py` — 9 tests: **Test 0 precondition** (audit-claims ≡ lint.sh for
  the 4 shared symbols, modulo the "(COPIED from lint.sh)" docstring — an automated gate, passing
  at HEAD), make_yaml settings + byte-exact round-trip vs a committed fixture pair
  (`tests/fixtures/common/frontmatter_{in.md,out.yaml}`, expected side GENERATED by running the
  lifted `make_yaml()`), `_is_local` case-fold behaviors, per-module byte-identity with bin/lib,
  `parse_frontmatter`/`parse_frontmatter_str` 3-tuple contracts, regex matches, and page.py↔lint.sh
  source-identity across 14 anchors. All green (venv, pinned deps).
- `docs/reference/common-api-inventory.md` — completeness evidence: 6 module APIs + 9 outside
  primitives classified (a) Phase-25 switch targets: audit-claims byte-copy + its page-walk copy +
  brownfield's in-memory parse; (b) intentionally distinct: validate-op's check-oriented parser,
  check-neutrality's raw-block probe, check-privacy's path scan, audit's `_fence_mask_lines`
  (different API shape, RB-3), lint's network-drift helpers.

## Verification

`pytest tests/test_common_extraction.py tests/test_packaging.py` → 10 passed ✓;
`diff bin/lib/* src/compendium/common/*` byte-identical per module ✓; bin/ and bin/lib untouched
(`git diff` empty) ✓; neutrality gate exit 0 ✓; `common/` importable via the installed package ✓.

## Deviations from plan

- page.py content re-derived per RB-3 (fence scanner replaced `_FENCE_RE`; fresh line anchors).
- Inventory gained the two RB-3 additions (audit `_fence_mask_lines`, audit page-walk copy) and
  brownfield's heredoc-internal `parse_frontmatter` (found during the sweep, classified (a)).
- Fixture expected file is `frontmatter_out.yaml` (dumped YAML block), not a full `.md` — the
  round-trip chokepoint is the YAML canonicalization, and the test compares those bytes exactly.
