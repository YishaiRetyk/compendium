---
phase: 16-reference-extraction
plan: "02"
type: execute
wave: 1
depends_on:
  - "16-01"
files_modified:
  - AGENTS.md
  - CLAUDE.md
  - schema/reference/provenance.md
  - schema/workflows/lint.md
autonomous: true
requirements:
  - REF-03

must_haves:
  truths:
    - "schema/reference/provenance.md exists and contains all §6 syntax/epistemics content including Contradiction Inline Syntax"
    - "schema/workflows/lint.md exists and contains ONLY the decay table + staleness auto-fix rules seeded from §6 — no lint workflow procedure"
    - "AGENTS.md §6 contains only the one-line provenance requirement + two stub pointers (provenance.md and lint.md)"
    - "The consumer-split seam is respected: Contradiction Inline Syntax (lines 580–602) is in provenance.md, not lint.md"
    - "schema/workflows/lint.md has a prominent partial-content header stating Phase 17 owns the full lint workflow"
    - "AGENTS.md and CLAUDE.md are byte-identical after extraction"
  artifacts:
    - path: "schema/reference/provenance.md"
      provides: "§6 syntax/epistemics: inline provenance syntax, locator types, support types, epistemic markers, contradiction markers"
      min_lines: 130
    - path: "schema/workflows/lint.md"
      provides: "§6 decay math: decay rate table, epistemic modifiers, staleness auto-fix rules (Phase 16 seed only)"
      min_lines: 30
    - path: "AGENTS.md"
      provides: "§6 one-line provenance requirement + pointers to provenance.md and lint.md"
      contains: "schema/reference/provenance.md"
  key_links:
    - from: "AGENTS.md §6 stub"
      to: "schema/reference/provenance.md"
      via: "bare pointer"
      pattern: "schema/reference/provenance\\.md"
    - from: "AGENTS.md §6 stub"
      to: "schema/workflows/lint.md"
      via: "bare pointer"
      pattern: "schema/workflows/lint\\.md"
---

<objective>
Extract §6 Provenance with the consumer-split: syntax/epistemics → schema/reference/provenance.md; decay table + staleness auto-fix → schema/workflows/lint.md (seeded only).

Purpose: REF-03 (consumer-split). §6 is 220 lines covering two distinct consumers: provenance syntax/epistemics (needed at authoring time, JIT) and decay math (only consumed by bin/lint.sh, belongs in the lint workflow reference). The split is LOCKED. Phase 16 creates schema/workflows/lint.md with only the decay content; Phase 17 adds the full lint procedure.

Output: Two new files; AGENTS.md §6 replaced by 1 requirement line + 2 stub pointers; CLAUDE.md synced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/16-reference-extraction/16-RESEARCH.md
@.planning/phases/16-reference-extraction/16-CONTEXT.md
@.planning/phases/16-reference-extraction/16-PATTERNS.md
@.planning/milestones/v1.2-MILESTONE-BRIEF.md
@.planning/phases/16-reference-extraction/16-01-SUMMARY.md

<interfaces>
<!-- §6 CONSUMER-SPLIT LINE RANGES (from RESEARCH.md "§6 Split Seam Detail", verified): -->
<!-- provenance.md gets: lines 399–553 (provenance syntax through Mixed Inline Grammar) -->
<!--                   + lines 580–602 (Contradiction Inline Syntax subsection) -->
<!-- lint.md gets: lines 555–578 (Decay Rate Table + epistemic modifiers + hash override + date fallback) -->
<!--              + lines 604–618 (Staleness Auto-Fix Rules) -->
<!-- CRITICAL: lines 580–602 Contradiction Inline Syntax → provenance.md NOT lint.md -->
<!-- (it describes a marker format, not decay math) -->

<!-- SEAM LANDMARKS (from RESEARCH.md §6 Split Seam): -->
<!-- Line 542: ### Mixed Inline Grammar (last provenance-syntax subsection before split) -->
<!-- Line 553: end of Mixed Inline Grammar -->
<!-- Line 555: ### Domain-Based Decay Rate Table — first lint-owned line -->
<!-- Line 580: ### Contradiction Inline Syntax (back to provenance scope) -->
<!-- Line 604: ### Staleness Auto-Fix Rules (lint-owned) -->
<!-- Line 618: end of §6 -->

<!-- §6 RESIDENT REMNANT (D-03 — from RESEARCH.md Resident Remnant Specification): -->
<!-- One line provenance requirement + 2 stub pointers: -->
<!-- ## 6. Provenance, Epistemics, and Staleness -->
<!-- Every factual claim MUST have an inline provenance marker `[prov:source_id#locator]`. -->
<!-- → Syntax, locator types, epistemic markers, and contradiction markers: `schema/reference/provenance.md`. -->
<!-- → Decay table and staleness auto-fix: `schema/workflows/lint.md`. -->

<!-- TEMPLATE PLACEHOLDER: AGENTS.template.md line 569 has {{DECAY_PROFILE}} in the decay-table section -->
<!-- This placeholder MOVES to schema/workflows/lint.md in the template copy -->
<!-- In live AGENTS.md/CLAUDE.md: rendered value (no {{...}}) moves to lint.md leaf -->
<!-- This placeholder is NOT in provenance.md -->

<!-- lint.md MUST have this partial-content header (from PATTERNS.md and RESEARCH.md): -->
<!-- # Lint Reference: Staleness and Decay -->
<!-- > **Note:** This file contains only the decay table and staleness auto-fix rules (seeded by Phase 16). -->
<!-- > The full lint workflow procedure (bin/lint.sh steps, severity tiers, CI flags) is added in Phase 17. -->
<!-- > AGENTS.md §6 decay/staleness content points here. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create schema/reference/provenance.md (§6 syntax/epistemics + Contradiction Inline Syntax)</name>
  <files>schema/reference/provenance.md</files>
  <read_first>
    1. CLAUDE.md lines 399–553 — §6 content from "### Inline Provenance Syntax" through end of "### Mixed Inline Grammar" — ALL of this goes to provenance.md
    2. CLAUDE.md lines 580–602 — "### Contradiction Inline Syntax" subsection — also goes to provenance.md
    3. CLAUDE.md lines 555–578 — "### Domain-Based Decay Rate Table" + modifiers — does NOT go to provenance.md (goes to lint.md)
    4. CLAUDE.md lines 604–618 — "### Staleness Auto-Fix Rules" — does NOT go to provenance.md (goes to lint.md)
    5. PATTERNS.md "schema/reference/provenance.md" section — structural header pattern
  </read_first>
  <action>
    Create `schema/reference/provenance.md`.

    File structure:
    1. Header with agent-authoritative lede:
       ```
       # Provenance, Epistemics, and Contradiction

       > Agent-authoritative reference for inline provenance markers `[prov:...]`, epistemic status markers `[epistemic::]`, and contradiction markers `[contradiction:...]`.
       > AGENTS.md §6 (syntax/epistemics portion) points here.

       Every factual claim MUST have an inline provenance marker — see the syntax below.
       ```

    2. VERBATIM CONTENT from CLAUDE.md lines 399–553:
       Copy all of §6 from "### Inline Provenance Syntax" through the end of "### Mixed Inline Grammar" section (line 553). This includes: Inline Provenance Syntax, Locator Types table, Page-marker convention, Support Types table, Checked At, Examples in Context, Bad vs. Good Provenance Examples, Source Registry, Provenance Validation Rules, Inline Epistemic Markers, Page-Level vs Claim-Level Epistemic Status, Mixed Inline Grammar.

       Do NOT include: the section header "## 6. Provenance, Epistemics, and Staleness" (line ~399 in §6 — use the file's own header). Do NOT include lines 555–578 (decay table) or lines 604–618 (staleness auto-fix).

    3. CONTRADICTION INLINE SYNTAX (lines 580–602):
       After the Mixed Inline Grammar section, append verbatim the "### Contradiction Inline Syntax" subsection (lines 580–602), including its rules and example.

    4. See Also footer:
       ```
       ## See Also

       - [AGENTS.md](../../AGENTS.md) — §6 stub (pointer to this file and to lint.md for decay math).
       - `schema/workflows/lint.md` — decay rate table, epistemic modifiers, staleness auto-fix rules.
       - `schema/reference/frontmatter.md` — frontmatter fields including `epistemic_status`.
       ```

    SEAM CHECK: After writing, verify:
    - File contains "### Contradiction Inline Syntax" (from lines 580–602)
    - File does NOT contain "### Domain-Based Decay Rate Table" (lines 555–578 — those go to lint.md)
    - File does NOT contain "### Staleness Auto-Fix Rules" (lines 604–618 — those go to lint.md)

    Run: `bash bin/check-neutrality.sh` — must exit 0.
  </action>
  <verify>
    <automated>test -f /home/yishai/Documents/compendium/schema/reference/provenance.md && grep -q "Contradiction Inline Syntax" /home/yishai/Documents/compendium/schema/reference/provenance.md && ! grep -q "Domain-Based Decay Rate Table" /home/yishai/Documents/compendium/schema/reference/provenance.md && ! grep -q "Staleness Auto-Fix Rules" /home/yishai/Documents/compendium/schema/reference/provenance.md</automated>
  </verify>
  <done>schema/reference/provenance.md exists with ≥130 lines. Contains "Contradiction Inline Syntax". Does NOT contain "Domain-Based Decay Rate Table" or "Staleness Auto-Fix Rules" (those are lint.md scope). Neutrality gate passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create schema/workflows/lint.md (decay seed); stub §6 in AGENTS.md; sync to CLAUDE.md; commit</name>
  <files>schema/workflows/lint.md, AGENTS.md, CLAUDE.md</files>
  <read_first>
    1. CLAUDE.md lines 555–578 — "### Domain-Based Decay Rate Table" through date fallback chain — verbatim content for lint.md
    2. CLAUDE.md lines 604–618 — "### Staleness Auto-Fix Rules" — verbatim content for lint.md
    3. PATTERNS.md "schema/workflows/lint.md" section — partial-content header pattern (CRITICAL: must not add §11.3 lint procedure)
    4. RESEARCH.md "§6 Consumer-Split Seam" and "Pitfall 3: Putting Content in §6 Staleness Seed Beyond Its Scope"
    5. AGENTS.md current §6 header location (around line 399 after prior edits shifted lines)
    6. RESEARCH.md "Resident Remnant Specification" for §6 — exact remnant text
  </read_first>
  <action>
    STEP A — Create schema/workflows/lint.md (decay seed ONLY):

    CRITICAL: This file is seeded by Phase 16 and OWNED by Phase 17. Do NOT add any lint workflow procedure content from §11.3. The seeded file contains ONLY the two decay-math subsections from §6.

    File structure:
    ```
    # Lint Reference: Staleness and Decay

    > **Note:** This file contains only the decay table and staleness auto-fix rules (seeded by Phase 16).
    > The full lint workflow procedure (`bin/lint.sh` steps, severity tiers, CI flags) is added in Phase 17.
    > AGENTS.md §6 decay/staleness content points here.

    [verbatim content of CLAUDE.md lines 555–578: Domain-Based Decay Rate Table section]

    [verbatim content of CLAUDE.md lines 604–618: Staleness Auto-Fix Rules section]

    ## See Also

    - [AGENTS.md](../../AGENTS.md) — §6 stub (decay/staleness pointer to this file).
    - `schema/reference/provenance.md` — provenance syntax, epistemic markers, contradiction markers.
    ```

    Verify: file contains "Domain-Based Decay Rate Table" AND "Staleness Auto-Fix Rules" AND "Phase 16" AND "Phase 17". Does NOT contain "Pass 0", "Pass 1", "## Steps:" or any §11.3 lint workflow procedure text.

    STEP B — Edit AGENTS.md §6:
    Find the current §6 header "## 6. Provenance, Epistemics, and Staleness" in AGENTS.md (line number has shifted from original 399 due to Plan 01 extractions). Replace the entire §6 block with the D-03 remnant:
    ```
    ## 6. Provenance, Epistemics, and Staleness

    Every factual claim MUST have an inline provenance marker `[prov:source_id#locator]`.

    → Syntax, locator types, epistemic markers, and contradiction markers: `schema/reference/provenance.md`.
    → Decay table and staleness auto-fix: `schema/workflows/lint.md`.
    ```

    This is the D-03 remnant: ONE LINE provenance requirement (not the example — that is in provenance.md), plus TWO pointer stubs.

    STEP C — Sync and verify:
    Run: `bash bin/sync-claude.sh` (propagate to CLAUDE.md)
    Run: `bash bin/sync-claude.sh --check` (verify byte equality) — must exit 0
    Run: `bash bin/check-neutrality.sh` — must exit 0

    STEP D — Commit:
    Stage: AGENTS.md, CLAUDE.md, schema/reference/provenance.md, schema/workflows/lint.md
    Commit: `schema(ref-03): extract §6 with consumer-split → provenance.md + lint.md seed`
  </action>
  <verify>
    <automated>bash /home/yishai/Documents/compendium/bin/sync-claude.sh --check && test -f /home/yishai/Documents/compendium/schema/workflows/lint.md && grep -q "Decay Period" /home/yishai/Documents/compendium/schema/workflows/lint.md && grep -q "schema/reference/provenance.md" /home/yishai/Documents/compendium/AGENTS.md && grep -q "schema/workflows/lint.md" /home/yishai/Documents/compendium/AGENTS.md</automated>
  </verify>
  <done>
    - schema/workflows/lint.md exists with decay table ("Domain-Based Decay Rate Table"), epistemic modifiers, hash override, date fallback chain, and Staleness Auto-Fix Rules
    - schema/workflows/lint.md has the partial-content header noting Phase 17 adds the procedure
    - schema/workflows/lint.md does NOT contain any §11.3 lint procedure steps
    - AGENTS.md §6 contains: one provenance requirement line + two stubs (provenance.md + lint.md)
    - `bash bin/sync-claude.sh --check` exits 0
    - `bash bin/check-neutrality.sh` exits 0
    - All four files committed in one commit
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| §6 content → schema/reference/provenance.md (public) | Extracted provenance examples use abstract placeholders (they already do in §6) |
| §6 decay content → schema/workflows/lint.md (public) | Decay table is structural math; no vault-specific content |
| AGENTS.md ≡ CLAUDE.md | Byte-equality must hold after every commit |
| lint.md scope boundary | Phase 16 must not add §11.3 procedure content to lint.md (Phase 17 owns it) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02-01 | Information Disclosure | schema/reference/provenance.md | mitigate | Run `bash bin/check-neutrality.sh`; §6 examples already use abstract placeholder form |
| T-16-02-02 | Tampering | Consumer-split seam (Contradiction Inline Syntax mis-routed) | mitigate | Verify with grep: provenance.md has "Contradiction Inline Syntax", lint.md does NOT |
| T-16-02-03 | Tampering | AGENTS.md ≡ CLAUDE.md byte equality | mitigate | `bash bin/sync-claude.sh --check` before commit |
| T-16-02-04 | Tampering | lint.md scope creep (§11.3 added) | mitigate | Verify lint.md does NOT contain "Pass 0" / "## Steps:" / §11.3 procedure text |
</threat_model>

<verification>
After plan 02 commit:
- `test -f schema/reference/provenance.md && wc -l schema/reference/provenance.md` — expect ≥130 lines
- `test -f schema/workflows/lint.md && grep -c "Decay Period" schema/workflows/lint.md` — expect ≥1
- `grep -q "Contradiction Inline Syntax" schema/reference/provenance.md` — exits 0 (correct routing)
- `! grep -q "Domain-Based Decay Rate Table" schema/reference/provenance.md` — exits 0 (correct exclusion)
- `grep -q "Staleness Auto-Fix Rules" schema/workflows/lint.md` — exits 0 (correct routing)
- `! grep -q "^## 11\.3" schema/workflows/lint.md` — exits 0 (Phase 17 scope not pre-empted)
- `bash bin/sync-claude.sh --check` — exits 0
- `bash bin/check-neutrality.sh` — exits 0
</verification>

<success_criteria>
schema/reference/provenance.md contains full §6 syntax/epistemics content (including Contradiction Inline Syntax). schema/workflows/lint.md is seeded with only the decay table + staleness auto-fix content and carries the Phase 17 ownership note. AGENTS.md §6 has only the 1-line provenance requirement + 2 stub pointers. AGENTS.md ≡ CLAUDE.md byte-identical. Neutrality gate passes.
</success_criteria>

<output>
After completion, create `.planning/phases/16-reference-extraction/16-02-SUMMARY.md`
</output>
