---
phase: 16-reference-extraction
plan: "04"
type: execute
wave: 2
depends_on:
  - "16-03"
files_modified:
  - AGENTS.md
  - CLAUDE.md
  - schema/AGENTS.template.md
  - tests/phase-09.1/test_template_parity.sh
  - tests/phase-09.1/test_agents_section_4_residue.sh
  - tests/phase-09.1/test_agents_section_16.sh
  - tests/phase-10/test_agents_template_parity_section_5.sh
  - tests/phase-10/test_agents_section_5_bootstrap_stage.sh
  - wiki-cloud/decisions/dr-2026-06-04-reference-extraction.md
  - wiki-cloud/index.md
  - wiki-cloud/log.md
autonomous: true
requirements:
  - REF-08
  - REF-09
  - REF-10

must_haves:
  truths:
    - "The IMPORTANT:-flagged routing table is present at the top of AGENTS.md (after §1, before §2)"
    - "Every routing table row that is NOT annotated *(Phase 17)* points to a file that exists on disk; the ingest/query/reflect rows are visually marked 'inline in §11 until Phase 17 — do not dereference yet' so a dangling pointer never overrides still-inline §11 content"
    - "AGENTS.md line 3 is updated to the D-09 'router' framing"
    - "AGENTS.md §3 MUST-NOT line 130 is updated to reflect that linked files are also authoritative"
    - "A repo-wide authority-language grep sweep ran (sole / authoritative / No other file / does NOT contain rules / sole source of truth) and every now-false statement was reconciled to the router framing (Codex HIGH) — including §2's 'schema/ does NOT contain rules or conventions'"
    - "schema/AGENTS.template.md mirrors this plan's OWN 3 structural changes (router line 3, routing table, §3 MUST-NOT line) — the §4–§16 stub mirrors were already done per-commit in Plans 16-01/02/03"
    - "AGENTS.md and CLAUDE.md are byte-identical"
    - "The 5 prior-phase parity tests that codify the monolith shape (phase-09.1 §4/§16 + §4-residue + §16-Appendix-C; phase-10 §5-parity + §5-bootstrap_stage) are rewritten to the post-extraction reality AND run green alongside phase-07/08"
    - "Decision record dr-2026-06-04-reference-extraction.md exists with affected_pages: [] (DRFT-04 + §4.6 infra-record precedent)"
    - "bin/sync-claude.sh --check exits 0"
    - "bin/check-neutrality.sh exits 0"
    - "Post-render stub-integrity: every schema/reference/… and schema/workflows/… (non-Phase-17) and docs/reference/… path referenced in the RENDERED AGENTS.md resolves on disk (init-wizard --dry-run alone is insufficient — it only checks the 5 rendered files for leftover {{...}})"
  artifacts:
    - path: "AGENTS.md"
      provides: "Routing table at top of core + D-09 line 3 update + §3 MUST-NOT line 130 update"
      contains: "IMPORTANT"
    - path: "schema/AGENTS.template.md"
      provides: "Template mirrors all stubs + routing table"
      contains: "schema/reference/page-types.md"
    - path: "wiki-cloud/decisions/dr-2026-06-04-reference-extraction.md"
      provides: "REF-10 decision record (trigger_type: schema-update, affected_pages: [])"
      contains: "trigger_type: schema-update"
    - path: "tests/phase-09.1/test_template_parity.sh"
      provides: "parity test rewritten to post-extraction shape (§4/§16 stubs, not full bodies)"
      contains: "## 4."
  key_links:
    - from: "AGENTS.md routing table"
      to: "schema/reference/page-types.md"
      via: "routing table row"
      pattern: "schema/reference/page-types\\.md"
    - from: "AGENTS.md routing table"
      to: "schema/reference/privacy.md"
      via: "routing table row"
      pattern: "schema/reference/privacy\\.md"
    - from: "schema/AGENTS.template.md"
      to: "routing stubs"
      via: "parallel mirror"
      pattern: "schema/reference/page-types\\.md"
---

<objective>
Add the IMPORTANT:-flagged routing table to AGENTS.md core; update D-09 framing (line 3 + §3 MUST-NOT line 130); run the authority-language grep sweep; mirror THIS plan's 3 structural changes into schema/AGENTS.template.md; rewrite + run the 5 prior-phase parity tests; write the REF-10 decision record.

Purpose: REF-08 (routing table — the load-bearing dispatch artifact; Vercel 56%-miss mitigation), REF-09 (template mirror — but only this plan's own 3 changes; the §4–§16 stub mirrors were done per-commit in 16-01/02/03), REF-10 (schema-update DR; D-09 framing). This is the final plan of Phase 16 — it wires everything together and proves correctness via the FULL gate + test suite (phase-07/08 AND the rewritten phase-09.1/phase-10 parity tests).

Output: Routing table in core (Phase-17 rows clearly non-dereferenceable); template mirrored for this plan's changes; D-09 framing applied repo-wide; 5 parity tests reconciled and green; DR written with affected_pages: []; all CI gates green.

REVIEW NOTE (cross-AI, addressed in this re-plan):
1. HIGH (Claude, proven against repo): the §4–§16 stub mirror was unowned/mis-sequenced and silently breaks pre-existing parity tests. RESOLVED in two parts — (a) per-commit stub mirroring moved into Plans 16-01/02/03; (b) NEW Task 3 here rewrites the 5 prior-phase tests that codify the monolith shape and RUNS them. VERIFIED affected tests: tests/phase-09.1/{test_template_parity.sh (§4/§16 byte-parity), test_agents_section_4_residue.sh (6 subsection pointers), test_agents_section_16.sh (Appendix C 10 rules)} and tests/phase-10/{test_agents_template_parity_section_5.sh (§5 byte-parity), test_agents_section_5_bootstrap_stage.sh (§5 bootstrap_stage rows)}. NOTE: CI (.github/workflows) currently runs ONLY phase-07 + phase-08; phase-09.1/phase-10 are per-phase aggregators run manually — but the established precedent (tests/phase-07/test_reference_stubs.sh: "Phase 08-04 / 09-06 / 13.1 precedent for relaxing prior-phase tests when a successor plan populates the stub") REQUIRES a successor phase to update them. Task 3 follows that precedent.
2. MEDIUM (both): routing-table dangling Phase-17 rows. RESOLVED — ingest/query/reflect rows are rendered visually distinct ("inline in §11 until Phase 17 — do not dereference yet") so a dangling pointer never overrides the still-inline §11 content. lint.md is fine (16-02 creates it).
3. HIGH (Codex): now-false authority language beyond line 3. RESOLVED — Task 1 runs a repo-wide grep sweep for `sole`, `authoritative`, `No other file`, `does NOT contain rules`, `sole source of truth` and reconciles every now-false statement (incl. §2 "schema/ does NOT contain rules or conventions").
4. MEDIUM (Claude; Codex concurs on the cleaner choice): DR affected_pages → `[]` (not [index, log]). index.md/log.md DO carry id:index/id:log frontmatter (Codex correct that [index,log] is schema-valid), BUT DRFT-04 hard-excludes those tokens from page-ID resolution and §4.6's infra-record precedent (dr-2026-04-14-phase6-decision-type) is `[]`. Use `[]`; narrate the extraction in the body. Do NOT list AGENTS.md or the extracted reference files (they are not id-bearing wiki pages).
5. LOW (both): post-render stub-integrity. RESOLVED — Task 2 adds an explicit assertion that every schema/reference/…, schema/workflows/… (non-Phase-17), and docs/reference/… path in the RENDERED AGENTS.md resolves on disk (init-wizard --dry-run alone only checks the 5 rendered files for leftover {{...}}).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/16-reference-extraction/16-RESEARCH.md
@.planning/phases/16-reference-extraction/16-CONTEXT.md
@.planning/phases/16-reference-extraction/16-PATTERNS.md
@.planning/milestones/v1.2-MILESTONE-BRIEF.md
@.planning/phases/16-reference-extraction/16-03-SUMMARY.md

<interfaces>
<!-- ROUTING TABLE DESIGN (D-05, from PATTERNS.md "AGENTS.md / CLAUDE.md" section): -->
<!-- Placed AFTER §1 block and BEFORE §2 Directory Structure -->
<!-- Two-axis: static reference topics + workflow operations -->
<!-- ~30 lines, IMPORTANT:-flagged markdown blockquote with table -->

<!-- EXACT ROUTING TABLE FORM (Phase-17 workflow rows are visually separated and marked -->
<!-- NON-DEREFERENCEABLE so a dangling pointer never overrides still-inline §11 content): -->
<!--
> **IMPORTANT — Reference Routing Table**
>
> This file is the router. Each linked file is authoritative for its own sections (D-09).
> Read the target file before acting — do not rely on the stub alone.
>
> **Resolvable references** (the target file exists — read it before acting):
>
> | When you need this | Go to |
> |-------------------|-------|
> | Authoring a wiki page (type rules, section order) | `schema/reference/page-types.md` |
> | Checking required frontmatter fields | `schema/reference/frontmatter.md` |
> | Adding `[prov:]` or `[epistemic::]` markers | `schema/reference/provenance.md` |
> | Decay table / staleness auto-fix math | `schema/workflows/lint.md` |
> | Creating cross-references (wikilinks) | `schema/reference/wikilinks.md` |
> | Determining `wiki-cloud/` vs `wiki-local/` placement | `schema/reference/privacy.md` |
> | Wiki capacity / scaling signals | `docs/reference/scaling.md` |
> | Obsidian, Git, and optional tools | `docs/reference/tooling.md` |
>
> **Workflows — STILL INLINE in §11 until Phase 17. Do NOT dereference these paths yet;**
> **the authoritative content is §11 below until the file is created in Phase 17.**
>
> | Workflow | Where it lives NOW |
> |----------|--------------------|
> | Ingest | §11.1 (inline). Future home: `schema/workflows/ingest.md` *(Phase 17)* |
> | Query | §11.2 (inline). Future home: `schema/workflows/query.md` *(Phase 17)* |
> | Lint | §11.3 (inline). Decay math already at `schema/workflows/lint.md`; full procedure *(Phase 17)* |
> | Reflect | §11.4 (inline). Future home: `schema/workflows/reflect.md` *(Phase 17)* |
-->
<!-- RATIONALE (both reviewers, MEDIUM): a router row that says "go to schema/workflows/ingest.md" -->
<!-- when that file does not exist AND the content is right there in §11 is worse than no row — the -->
<!-- agent follows the dangling pointer, gets file-not-found, and misses the inline content. The split -->
<!-- table makes the resolvable refs visually distinct from the not-yet-dereferenceable workflow rows. -->

<!-- AUTHORITY-LANGUAGE GREP SWEEP (Codex HIGH — Task 1 STEP A.5): -->
<!-- After updating line 3 and §3 MUST-NOT line 130, grep the WHOLE AGENTS.md for the now-false -->
<!-- "sole authority" framing and reconcile each hit to the router model. Required search terms: -->
<!--   grep -nE 'sole authoritative|sole source of truth|No other file|does NOT contain rules|does not contain rules|does NOT contain conventions' AGENTS.md -->
<!-- Known hits to reconcile (verify against the live file at Task 1 time): -->
<!--   • Preamble: "This is the sole authoritative specification" → "...the authoritative router for the spec; -->
<!--     each linked reference file is authoritative for its own sections." -->
<!--   • Preamble: "No other file contains conventions, rules, or workflow definitions." → "This file routes to -->
<!--     reference files under schema/reference/ and schema/workflows/; each is authoritative for its own sections." -->
<!--   • Line 3 (already updated above): the D-09 router framing. -->
<!--   • §2 Schema directory rules: "It does NOT contain rules or conventions -- those live only in this file." -->
<!--     → "schema/reference/ and schema/workflows/ hold authoritative reference content the router links to; -->
<!--     schema/templates/ holds blank templates." (This is now FALSE as written — Codex's specific example.) -->
<!--   • §3 MUST-NOT: "DO NOT put conventions or rules in any file other than AGENTS.md. This is the sole -->
<!--     source of truth." (line ~130 — updated to router framing as the existing plan already specifies) -->
<!-- Reconcile ALL hits, not just the known ones. The sweep is the gate; do not leave a contradictory sentence. -->
<!-- NOTE: examples/ and docs/ may contain narrative "sole source of truth" references — those are -->
<!-- end-user/illustrative and out of scope; the sweep targets AGENTS.md (and its CLAUDE.md mirror via sync) -->
<!-- plus schema/AGENTS.template.md. If a docs/ file makes a now-false structural claim, fix it too, but -->
<!-- AGENTS.md is the required surface. -->

<!-- D-09 LINE 3 UPDATE (from RESEARCH.md Resident Remnant Specification): -->
<!-- Current line 3: "No other file contains conventions, rules, or workflow definitions." -->
<!-- After update: "This file is the router; each linked file listed in the routing table is authoritative for its own sections." -->

<!-- §3 MUST-NOT LINE 130 UPDATE (from RESEARCH.md, Pitfall 5): -->
<!-- Current: "DO NOT put conventions or rules in any file other than AGENTS.md. This is the sole source of truth." -->
<!-- After: "DO NOT put conventions or rules in any file other than AGENTS.md or the files listed in the routing table. AGENTS.md is the router; each linked file is authoritative for its own sections." -->

<!-- AGENTS.template.md MIRROR RULE (REF-09, from RESEARCH.md): -->
<!-- The template is 34 lines shorter than AGENTS.md due to 4 {{placeholder}} occurrences -->
<!-- For every stub and the routing table added to AGENTS.md, apply the IDENTICAL text to AGENTS.template.md at the same logical position -->
<!-- Placeholders that MOVED with sections (to extracted leaf files): -->
<!--   {{PRIMARY_DOMAIN}} (×2, was in §5 block) → now in schema/reference/frontmatter.md (template copy) -->
<!--   {{DECAY_PROFILE}} (was in §6 decay section) → now in schema/workflows/lint.md (template copy) -->
<!-- These were already moved when Plans 01-03 created the leaf files -->
<!-- Remaining in template: {{AGENT_FILENAME}} (§1) and {{PRIMARY_DOMAIN}} (§2) — UNTOUCHED -->
<!-- bin/sync-claude.sh --check does NOT validate template — manual correctness check only -->

<!-- DECISION RECORD (REF-10, from PATTERNS.md "wiki-cloud/decisions/dr-2026-06-04-reference-extraction.md"): -->
<!-- Frontmatter: -->
<!--   id: dr-2026-06-04-reference-extraction -->
<!--   title: "Reference Extraction: AGENTS.md Monolith to schema/reference/ + schema/workflows/" -->
<!--   type: decision -->
<!--   trigger_type: schema-update -->
<!--   affected_pages: []   ← MEDIUM review fix: NOT [index, log]. -->
<!--   Rationale: index.md/log.md DO carry id:index/id:log frontmatter (so [index,log] would be -->
<!--   schema-valid), BUT DRFT-04 hard-excludes the index/log tokens from page-ID resolution, and the -->
<!--   §4.6 precedent for an infrastructure/inaugural record (dr-2026-04-14-phase6-decision-type) is []. -->
<!--   The extraction targets (schema/reference/*.md, schema/workflows/*.md, AGENTS.md) are NOT id-bearing -->
<!--   wiki pages and must NOT appear in affected_pages either. Narrate the extraction in the body; the -->
<!--   "## Affected Pages" body section describes the change narratively (it need not name id-bearing pages -->
<!--   when affected_pages is []). -->
<!-- All 7 required sections: TL;DR → Decision → Why → Alternatives Considered → Consequences → Affected Pages → Sources -->
<!-- Why must cover: D-09 old vs new framing; inclusion test; D-04 §4 names-only deviation -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add routing table + D-09 updates to AGENTS.md; mirror into AGENTS.template.md; verify byte-equality</name>
  <files>AGENTS.md, CLAUDE.md, schema/AGENTS.template.md</files>
  <read_first>
    1. AGENTS.md — read full current state post-Plans 01–03 to understand actual line positions before inserting routing table
    2. AGENTS.md lines 1–35 — locate §1 end and §2 start (routing table inserts between them)
    3. AGENTS.md lines 127–140 — §3 MUST-NOT list; find line 130 (the "sole source of truth" line to update)
    4. AGENTS.md lines 1–5 — the document header; find line 3 to update to D-09 framing
    5. schema/AGENTS.template.md — read §1 area + §2 area to confirm mirror positions; check placeholder presence
    6. PATTERNS.md "AGENTS.md / CLAUDE.md" section — routing table form and per-section stub forms
    7. RESEARCH.md "Routing Table Design (REF-08, D-05)" — two-axis structure; operation rows pre-declared with *(Phase 17)* annotation
  </read_first>
  <action>
    STEP A — Update AGENTS.md document header (D-09):
    Find line 3: `> No other file contains conventions, rules, or workflow definitions.`
    Replace with: `> This file is the router; each linked file listed in the routing table is authoritative for its own sections.`

    STEP A.5 — Authority-language grep sweep (Codex HIGH — run BEFORE inserting the routing table):
    Run: `grep -nE 'sole authoritative|sole source of truth|No other file|does NOT contain rules|does not contain rules|does NOT contain conventions' AGENTS.md`
    For EVERY hit, reconcile the now-false "sole authority" claim to the router framing. See the interfaces block "AUTHORITY-LANGUAGE GREP SWEEP" for the known hits and their replacement text — in particular:
    - Preamble "This is the sole authoritative specification" → router-authority phrasing
    - Preamble "No other file contains conventions, rules, or workflow definitions." → routes-to-reference-files phrasing
    - §2 Schema directory rules "It does NOT contain rules or conventions -- those live only in this file." → "schema/reference/ and schema/workflows/ hold authoritative reference content the router links to; schema/templates/ holds blank templates." (Codex's specific now-false example)
    Re-run the grep after edits; the only surviving matches must be intentional (e.g., the word "authoritative" inside the NEW router phrasing). Do NOT leave a contradictory sentence. The CLAUDE.md mirror is fixed automatically by the sync in Step D; apply the SAME reconciliations to schema/AGENTS.template.md in Step E.

    STEP B — Insert the SPLIT routing table between §1 and §2:
    Find the "## 2. Directory Structure" header. Insert the following BEFORE it (after the blank line following §1 content). NOTE the two sub-tables: resolvable refs vs. not-yet-dereferenceable workflow rows (both reviewers, MEDIUM — a dangling Phase-17 pointer must never override still-inline §11 content):

    ```
    > **IMPORTANT — Reference Routing Table**
    >
    > This file is the router. Each linked file is authoritative for its own sections (D-09).
    > Read the target file before acting — do not rely on the stub alone.
    >
    > **Resolvable references** (the target file exists — read it before acting):
    >
    > | When you need this | Go to |
    > |-------------------|-------|
    > | Authoring a wiki page (type rules, section order) | `schema/reference/page-types.md` |
    > | Checking required frontmatter fields | `schema/reference/frontmatter.md` |
    > | Adding `[prov:]` or `[epistemic::]` markers | `schema/reference/provenance.md` |
    > | Decay table / staleness auto-fix math | `schema/workflows/lint.md` |
    > | Creating cross-references (wikilinks) | `schema/reference/wikilinks.md` |
    > | Determining `wiki-cloud/` vs `wiki-local/` placement | `schema/reference/privacy.md` |
    > | Wiki capacity / scaling signals | `docs/reference/scaling.md` |
    > | Obsidian, Git, and optional tools | `docs/reference/tooling.md` |
    >
    > **Workflows — STILL INLINE in §11 until Phase 17. Do NOT dereference these paths yet;**
    > **the authoritative content is §11 below until the file is created in Phase 17.**
    >
    > | Workflow | Where it lives NOW |
    > |----------|--------------------|
    > | Ingest | §11.1 (inline). Future home: `schema/workflows/ingest.md` *(Phase 17)* |
    > | Query | §11.2 (inline). Future home: `schema/workflows/query.md` *(Phase 17)* |
    > | Lint | §11.3 (inline). Decay math already at `schema/workflows/lint.md`; full procedure *(Phase 17)* |
    > | Reflect | §11.4 (inline). Future home: `schema/workflows/reflect.md` *(Phase 17)* |
    ```

    STEP C — Update §3 MUST-NOT line 130:
    Find: `- DO NOT put conventions or rules in any file other than AGENTS.md. This is the sole source of truth.`
    Replace with: `- DO NOT put conventions or rules in any file other than AGENTS.md or the files listed in the routing table. AGENTS.md is the router; each linked file is authoritative for its own sections.`

    STEP D — Propagate to CLAUDE.md:
    Run: `bash bin/sync-claude.sh`
    Run: `bash bin/sync-claude.sh --check` — must exit 0.

    STEP E — Mirror THIS plan's 3 changes into schema/AGENTS.template.md (the §4–§16 stub mirrors were already done per-commit in Plans 16-01/02/03):
    Apply the SAME three changes to schema/AGENTS.template.md at the same logical positions: (1) the D-09 line-3 router framing, (2) the split routing table inserted between §1 and §2, (3) the §3 MUST-NOT line update, (4) the authority-language grep-sweep reconciliations from Step A.5. The template content mirrors AGENTS.md except for the 4 {{placeholder}} lines ({{AGENT_FILENAME}} in §1 and {{PRIMARY_DOMAIN}} in §2) which must remain unchanged. Do NOT re-mirror the §4–§16 stubs here — those were committed per-commit upstream; this step adds only the router/table/MUST-NOT/authority-language changes.

    Verify the template now matches AGENTS.md outside the placeholder lines:
    `diff <(grep -v '{{' AGENTS.md) <(grep -v '{{' schema/AGENTS.template.md)` — should be empty (every non-placeholder line is byte-identical). If non-empty, an upstream per-commit mirror (16-01/02/03) or this step is incomplete — reconcile before committing. The template does NOT get the `sync-claude.sh --check` treatment (that only validates AGENTS.md ≡ CLAUDE.md); this diff is the manual template-parity check.

    STEP F — Routing table target file existence check (resolvable rows only):
    For every file path in the RESOLVABLE references sub-table, verify it exists on disk:
    - `test -f schema/reference/page-types.md` — should pass (Plan 01)
    - `test -f schema/reference/frontmatter.md` — should pass (Plan 01)
    - `test -f schema/reference/provenance.md` — should pass (Plan 02)
    - `test -f schema/workflows/lint.md` — should pass (Plan 02)
    - `test -f schema/reference/wikilinks.md` — should pass (Plan 03)
    - `test -f schema/reference/privacy.md` — should pass (Plan 03)
    - `test -f docs/reference/scaling.md` — should pass (Plan 03)
    - `test -f docs/reference/tooling.md` — should pass (Plan 03)
    - `schema/workflows/ingest.md`, `query.md`, `reflect.md` — in the WORKFLOWS sub-table marked "do NOT dereference yet" / *(Phase 17)*; do NOT check.

    STEP F.5 — Post-render stub-integrity assertion (LOW review fix — init-wizard --dry-run is insufficient):
    init-wizard --dry-run only previews the 5 rendered files and checks for leftover {{...}}; it does NOT verify the inline §4/§5/§6/§8/§13 stub pointers or routing-table refs in the RENDERED AGENTS.md resolve. Add an explicit check:
    1. Render the template to a scratch dir: `bash bin/init-wizard.sh --answers-file <fixture> --render-to /tmp/p16-render` (use the canonical answers fixture if one exists, e.g. schema/fixtures/canonical-answers.yaml; otherwise --dry-run output captured to a file). If no fixture exists, fall back to asserting against the live AGENTS.md (the rendered AGENTS.md differs only in the 4 placeholder values, none of which are file paths).
    2. Extract every `schema/reference/…`, `schema/workflows/…`, and `docs/reference/…` path referenced in the rendered (or live) AGENTS.md, EXCLUDING the three Phase-17 workflow paths (ingest.md, query.md, reflect.md):
       `grep -oE '(schema/reference|schema/workflows|docs/reference)/[a-z0-9-]+\.md' AGENTS.md | sort -u | grep -vE 'workflows/(ingest|query|reflect)\.md'`
    3. For each extracted path, assert `test -f`. Any miss is a broken stub pointer — STOP and fix.

    STEP G — Run CI gate suite:
    `bash bin/check-neutrality.sh` — must exit 0
    `bash bin/check-privacy.sh` — must exit 0
    `bash bin/init-wizard.sh --dry-run` — must exit 0 (setup-parity check; still run it, but F.5 is the real stub-integrity guard)

    STEP H — Commit (AGENTS.md + CLAUDE.md + template only; parity tests are Task 3; DR and wiki files are Task 2):
    Commit: `schema(ref-08/09): add split routing table + D-09 router framing; authority-language sweep; mirror template`
  </action>
  <verify>
    <automated>bash /home/yishai/Documents/compendium/bin/sync-claude.sh --check && grep -q "IMPORTANT" /home/yishai/Documents/compendium/AGENTS.md && grep -q "do NOT dereference" /home/yishai/Documents/compendium/AGENTS.md && ! grep -qE 'sole authoritative|sole source of truth|No other file|does NOT contain rules|does NOT contain conventions' /home/yishai/Documents/compendium/AGENTS.md && diff <(grep -v '{{' /home/yishai/Documents/compendium/AGENTS.md) <(grep -v '{{' /home/yishai/Documents/compendium/schema/AGENTS.template.md) && for p in $(grep -oE '(schema/reference|schema/workflows|docs/reference)/[a-z0-9-]+\.md' /home/yishai/Documents/compendium/AGENTS.md | sort -u | grep -vE 'workflows/(ingest|query|reflect)\.md'); do test -f "/home/yishai/Documents/compendium/$p" || { echo "MISSING: $p"; exit 1; }; done</automated>
  </verify>
  <done>
    - AGENTS.md contains "IMPORTANT" (routing table present) and "do NOT dereference" (Phase-17 workflow rows marked non-dereferenceable)
    - AGENTS.md line 3 is updated to D-09 "router" framing
    - AGENTS.md §3 MUST-NOT contains "routing table" (line 130 updated)
    - Authority-language sweep: `grep -E 'sole authoritative|sole source of truth|No other file|does NOT contain rules|does NOT contain conventions' AGENTS.md` returns NO now-false statements (only intentional router phrasing survives)
    - schema/AGENTS.template.md matches AGENTS.md on every non-{{placeholder}} line (`diff <(grep -v '{{' AGENTS.md) <(grep -v '{{' template)` is empty) — confirms per-commit upstream mirrors + this plan's changes all landed
    - Post-render stub-integrity (STEP F.5): every schema/reference/…, schema/workflows/… (non-Phase-17), docs/reference/… path in the rendered/live AGENTS.md resolves on disk
    - All 8 resolvable routing-table target files resolve on disk; ingest/query/reflect rows intentionally NOT existence-checked (marked do-not-dereference)
    - `bash bin/sync-claude.sh --check` exits 0; `bash bin/check-neutrality.sh` exits 0; `bash bin/check-privacy.sh` exits 0; `bash bin/init-wizard.sh --dry-run` exits 0
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write REF-10 decision record; update wiki-cloud/index.md and log.md; run full test suite; commit</name>
  <files>wiki-cloud/decisions/dr-2026-06-04-reference-extraction.md, wiki-cloud/index.md, wiki-cloud/log.md</files>
  <read_first>
    1. wiki-cloud/decisions/dr-2026-06-04-privacy-asymmetric-two-dir.md — structural analog (same-day schema-update DR; frontmatter template from PATTERNS.md)
    2. wiki-cloud/index.md — find Decisions section to add new DR entry
    3. wiki-cloud/log.md — append schema: operation log entry at end
    4. PATTERNS.md "wiki-cloud/decisions/dr-2026-06-04-reference-extraction.md" — frontmatter + 7-section structure
    5. CLAUDE.md §4.6 — Decision record section ordering (all 7 required sections)
    6. 16-CONTEXT.md decisions D-01 through D-09 — source material for Why section
  </read_first>
  <action>
    STEP A — Create wiki-cloud/decisions/dr-2026-06-04-reference-extraction.md:

    FRONTMATTER:
    ```yaml
    ---
    id: dr-2026-06-04-reference-extraction
    title: "Reference Extraction: AGENTS.md Monolith to schema/reference/ + schema/workflows/"
    type: decision
    status: active
    summary: "Extracts §4/§5/§6/§7/§8/§13/§14/§15/§16 from the AGENTS.md monolith into standalone leaf files under schema/reference/ and schema/workflows/; AGENTS.md becomes a router with bare stubs; evolves 'sole source of truth' framing to 'router + per-section authority'."
    created_at: 2026-06-04
    updated_at: 2026-06-04
    sources: []
    epistemic_status: sourced
    tags:
      - meta
      - schema
    domains:
      - wiki-infrastructure
    supersedes: null
    superseded_by: null
    aliases:
      - dr-2026-06-04-reference-extraction
    has_contradictions: false
    knowledge_domain: software
    trigger_type: schema-update
    affected_pages: []
    ---
    ```
    NOTE (review fix): affected_pages is `[]`, NOT [index, log]. index.md/log.md carry id:index/id:log
    frontmatter (so [index,log] would be schema-valid), but DRFT-04 hard-excludes those tokens from
    page-ID resolution and §4.6's infra-record precedent is []. The extracted schema/reference/*.md and
    schema/workflows/*.md files are not id-bearing wiki pages, so they do NOT belong in affected_pages
    either. The extraction is narrated in the body (## Decision, ## Consequences, ## Affected Pages).

    BODY SECTIONS (all 7 required per §4.6):

    ## TL;DR
    Phase 16 extracts every static reference section of the 1,655-line AGENTS.md/CLAUDE.md monolith into standalone markdown files under `schema/reference/` and `schema/workflows/`. AGENTS.md becomes a router with bare stubs; each leaf file is authoritative for its own domain. The "sole authoritative specification" framing evolves from "no other file contains rules" to "this file is the router; each linked file is authoritative for its own sections" (D-09).

    ## Decision
    Extract §4 Page Types → `schema/reference/page-types.md`; §5 Frontmatter → `schema/reference/frontmatter.md`; §6 (consumer-split) syntax/epistemics → `schema/reference/provenance.md`, decay/staleness → `schema/workflows/lint.md`; §7 dissolves (no file); §8 Wikilinks → `schema/reference/wikilinks.md`; §13 Privacy (Phase-15 asymmetric form) → `schema/reference/privacy.md`; §14 → `docs/reference/scaling.md`; §15 → `docs/reference/tooling.md`; §16 deleted. A two-axis `IMPORTANT:`-flagged routing table is added at the top of core.

    ## Why
    Applied the inclusion test (ambient / unscriptable-unacceptable-miss-cost / dispatch) section-by-section. Sections that earn residence are those every agent needs on every turn; sections with clean JIT load points and gate-caught miss costs extract.

    The old framing ("no other file contains conventions") became a logical contradiction once reference files existed. The new D-09 framing ("this file is the router; each linked file is authoritative for its own sections") is cleaner: AGENTS.md is the dispatch artifact; leaf files own their domain.

    D-04 deviation from the Extraction Map's resident-remnant column: core was designed to keep §4 "type roster + section orderings (~10 lines)" resident, but the inclusion test finds the section-ordering tables fail all three clauses (they have a clean JIT load point at authoring time and a gate-caught miss from lint). Only the 6 type names (dispatch vocabulary) remain in core; the tables extract to `page-types.md`. The §4↔§7 dedupe (one roster, §7 dissolved) is preserved — only what the merged remnant contains is trimmed by the test.

    ## Alternatives Considered
    **Option A — Keep all reference content inline (status quo):** The spec continues to grow as an always-loaded monolith. Rejected: the spec already violates its own §7 progressive-disclosure principle; continued growth worsens context-window consumption on every turn.

    **Option B — Extract but keep inline summaries (partial stubs):** Each extracted section leaves a paragraph-level summary in core. Rejected (D-01): partial summaries create a second copy of content that drifts from the leaf file, and agents may act on the partial inline text instead of reading the authoritative reference.

    **Option C — Extract with collective-authority framing:** Core states "this file + schema/reference/ + schema/workflows/ collectively are the authority." Rejected (D-09): "collectively" is less crisp about who owns what. The router-plus-per-section-authority model is cleaner — core dispatches, leaf files own.

    ## Consequences
    - AGENTS.md is substantially smaller (from 1,655 lines to approximately the ~145-line resident core output expected from the inclusion test; Phase 17 completes the reduction).
    - Agents starting a task read AGENTS.md, find the routing table, and follow one hop to the relevant reference file — no more reading the full monolith for authoring-time reference lookup.
    - `bin/sync-claude.sh --check` (AGENTS.md ≡ CLAUDE.md) remains the byte-equality gate. `bin/sync-claude.sh --check-tree` (routing-stub target resolution check) is added at the end of Phase 17, when the full `schema/` tree exists.
    - Phase 17 owns `schema/workflows/lint.md` full procedure; Phase 16 seeds only the decay table and staleness auto-fix rules from §6.

    ## Affected Pages
    No id-bearing wiki pages are affected (affected_pages: []). This is an infrastructure/schema-update
    record, mirroring the §4.6 precedent dr-2026-04-14-phase6-decision-type. The change reshapes the
    AGENTS.md/CLAUDE.md spec and creates reference files under schema/reference/ and schema/workflows/ —
    none of which are id-bearing wiki pages (DRFT-04 also excludes the index/log tokens from page-ID
    resolution). For navigability, wiki-cloud/index.md gains a Decisions entry and wiki-cloud/log.md gains
    a schema: operation entry, but those are navigation artifacts, not pages tracked via decision_history.

    ## Sources
    Design decisions D-01 through D-09 in `.planning/phases/16-reference-extraction/16-CONTEXT.md`. Milestone brief Extraction Map and inclusion test in `.planning/milestones/v1.2-MILESTONE-BRIEF.md`.

    STEP B — Update wiki-cloud/index.md:
    Find the "Decisions" section of index.md. Add an entry:
    `- [[dr-2026-06-04-reference-extraction|Reference Extraction: AGENTS.md Monolith to schema/reference/ + schema/workflows/]] — Extracts static reference sections into standalone leaf files; evolves the spec to a router-plus-per-section-authority model (D-09). (sourced, 2026-06-04)`

    STEP C — Append to wiki-cloud/log.md:
    ```
    ## [2026-06-04] schema | reference extraction (Phase 16 complete)

    UPDATE index: added dr-2026-06-04-reference-extraction entry
    UPDATE log: this entry
    result: §4/§5/§6/§7/§8/§13/§14/§15/§16 extracted to schema/reference/*.md + schema/workflows/lint.md (seed) + docs/reference/scaling.md + docs/reference/tooling.md; AGENTS.md routing table added; D-09 framing applied; AGENTS.template.md mirrored
    pages_affected: dr-2026-06-04-reference-extraction, index, log
    ```

    STEP D — Run the wiki + CI gate suite (parity tests are Task 3):
    `bash bin/lint.sh --ci` — must exit 0 (lint scans wiki-cloud/ only, not schema/; no behavioral regression)
    `bash bin/check-neutrality.sh` — must exit 0
    `bash bin/check-privacy.sh` — must exit 0
    `bash tests/phase-07/run.sh` — must pass (tests reference AGENTS.md structure)
    `bash tests/phase-08/run.sh` — must pass (wizard pipeline including template byte-equality)

    NOTE: tests/phase-09.1 and tests/phase-10 will FAIL at this point because they still codify the
    pre-extraction monolith shape — that is EXPECTED. Task 3 rewrites them to the post-extraction reality
    and runs them green. Do NOT attempt to make them pass by reverting extraction; reconcile the tests in Task 3.

    If phase-07 or phase-08 fails, diagnose and fix before committing (most likely the template mirror).

    STEP E — Commit DR + wiki nav (parity-test reconciliation is the final Task 3 commit):
    Stage: wiki-cloud/decisions/dr-2026-06-04-reference-extraction.md, wiki-cloud/index.md, wiki-cloud/log.md
    Commit: `schema(ref-10): write reference-extraction DR (affected_pages: []); update index and log`

    STEP F — Verify final state:
    `bash bin/sync-claude.sh --check` — exits 0 (byte equality final check)
    `ls wiki-cloud/decisions/dr-*-reference-extraction*.md` — exists
    `grep -q "IMPORTANT" AGENTS.md` — routing table present
    `grep -q "filename/path ONLY" schema/reference/wikilinks.md` — REF-05 verbatim guard
  </action>
  <verify>
    <automated>test -f /home/yishai/Documents/compendium/wiki-cloud/decisions/dr-2026-06-04-reference-extraction.md && grep -q "trigger_type: schema-update" /home/yishai/Documents/compendium/wiki-cloud/decisions/dr-2026-06-04-reference-extraction.md && bash /home/yishai/Documents/compendium/bin/sync-claude.sh --check && bash /home/yishai/Documents/compendium/bin/check-neutrality.sh</automated>
  </verify>
  <done>
    - wiki-cloud/decisions/dr-2026-06-04-reference-extraction.md exists with trigger_type: schema-update, affected_pages: [], and all 7 required sections
    - wiki-cloud/index.md has the new DR entry in Decisions section
    - wiki-cloud/log.md has the schema: operation appended
    - `bash bin/sync-claude.sh --check` exits 0
    - `bash bin/check-neutrality.sh` exits 0
    - `bash bin/check-privacy.sh` exits 0
    - `bash bin/lint.sh --ci` exits 0 (no behavioral regression in wiki)
    - `bash tests/phase-07/run.sh && bash tests/phase-08/run.sh` passes (phase-09.1/phase-10 are reconciled and run in Task 3)
    - Every resolvable routing table target file exists on disk
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Reconcile the 5 prior-phase parity/residue tests to the post-extraction shape; run phase-09.1 + phase-10 green; final commit</name>
  <files>tests/phase-09.1/test_template_parity.sh, tests/phase-09.1/test_agents_section_4_residue.sh, tests/phase-09.1/test_agents_section_16.sh, tests/phase-10/test_agents_template_parity_section_5.sh, tests/phase-10/test_agents_section_5_bootstrap_stage.sh</files>
  <read_first>
    1. tests/phase-09.1/test_template_parity.sh — current §4/§16 byte-parity assertions (awk `^## 4.`→`^## 5.` and `^## 16.`→EOF); these now codify the monolith and must move to the stub shape
    2. tests/phase-09.1/test_agents_section_4_residue.sh — asserts 6 `See: schema/examples/<type>.md` pointers + 6 subsections in §4; the §4 stub no longer has these
    3. tests/phase-09.1/test_agents_section_16.sh — asserts §16 Appendix C 10 rules verbatim; §16 is now DELETED
    4. tests/phase-10/test_agents_template_parity_section_5.sh — asserts §5 field-descriptions table byte-parity AGENTS↔template; §5 is now a 2-line stub
    5. tests/phase-10/test_agents_section_5_bootstrap_stage.sh — asserts bootstrap_stage/bootstrap_date rows in §5; those rows now live in schema/reference/frontmatter.md
    6. tests/phase-07/test_reference_stubs.sh lines 15–40 — the RELAXATION PRECEDENT ("Phase 08-04 / 09-06 / 13.1 precedent for relaxing prior-phase tests when a successor plan populates the stub") — follow this pattern
    7. .github/workflows/setup-parity.yml + neutrality.yml — confirm CI runs ONLY phase-07 + phase-08 (phase-09.1/phase-10 are manual aggregators); the rewrite keeps them runnable and green for anyone who runs them
  </read_first>
  <action>
    These 5 tests codify the PRE-extraction monolith shape. Phase 16 changes that shape, so the tests
    MUST be reconciled to the post-extraction reality — exactly as prior phases relaxed their predecessors'
    tests when a successor populated a stub (tests/phase-07/test_reference_stubs.sh precedent). Do NOT
    delete the test files; rewrite their assertions so they verify the NEW invariant. Each rewritten test
    must still assert something real (the stub exists and points to the right leaf file; the moved content
    landed in the leaf file), not just trivially pass.

    REWRITE 1 — tests/phase-09.1/test_template_parity.sh:
    Replace the §4/§16 byte-parity assertions with post-extraction assertions:
    - §4: AGENTS.md §4 is now a stub containing "schema/reference/page-types.md"; assert the stub is byte-identical between AGENTS.md and schema/AGENTS.template.md (extract `^## 4\.`→`^## 5\.` from both; cmp). Also assert schema/reference/page-types.md exists and contains the six subsection markers (the §4 body moved there).
    - §16: AGENTS.md has NO `^## 16\.` header (deleted). Assert `! grep -q '^## 16\.' AGENTS.md` AND `! grep -q '^## 16\.' schema/AGENTS.template.md`. Remove the §16 byte-parity block entirely.
    - Update the header comment block (lines 2–6) to describe the post-extraction shape, not the pre-extraction line ranges.

    REWRITE 2 — tests/phase-09.1/test_agents_section_4_residue.sh:
    The §4 stub no longer has 6 `See: schema/examples/<type>.md` pointers or 6 subsections. Rewrite to assert the NEW residue:
    - AGENTS.md §4 contains the six type NAMES dispatch list (entity, concept, source, comparison, overview, decision) and the pointer "schema/reference/page-types.md".
    - The six worked-example `See: schema/examples/<type>.md` pointers now live in schema/reference/page-types.md, not AGENTS.md: assert `grep -c '^See: schema/examples/.* for a concrete filled-in instance\.$' schema/reference/page-types.md` is 6 (the §4 body moved there verbatim).
    - Keep the anti-pattern checks (no **See:** bold, no markdown-link See) but point them at page-types.md.

    REWRITE 3 — tests/phase-09.1/test_agents_section_16.sh:
    §16 is deleted, so the I-7/I-8/I-9 assertions about Appendix A/B pointers and Appendix C 10 rules no longer apply to AGENTS.md. Rewrite to assert:
    - `! grep -q '^## 16\. Appendices' AGENTS.md` (section gone).
    - The Appendix C rule HOMES survive per the Plan 16-03 disposition table: grep AGENTS.md §3 for the canonical phrases (ISO 8601, snake_case, "What Agents Must NOT Do", "one commit per logical operation") and grep schema/reference/privacy.md for "Privacy default". This proves the QRC content was absorbed, not lost.
    - Appendix A/B discoverability: docs/reference/dataview-queries.md and docs/reference/commit-examples.md still exist on disk (they are reachable via the docs/ tree; the §16 pointers were dropped intentionally).

    REWRITE 4 — tests/phase-10/test_agents_template_parity_section_5.sh:
    §5 is now a 2-line stub. Rewrite to assert:
    - AGENTS.md §5 stub is byte-identical to schema/AGENTS.template.md §5 stub (extract `^## 5\.`→`^## 6\.` from both; cmp) — the stub contains no placeholder, so parity holds.
    - The §5 field-descriptions table (incl. the bootstrap_stage / bootstrap_date rows the old test guarded) now lives in schema/reference/frontmatter.md: assert frontmatter.md contains the `bootstrap_stage` and `bootstrap_date` rows.

    REWRITE 5 — tests/phase-10/test_agents_section_5_bootstrap_stage.sh:
    The bootstrap_stage/bootstrap_date §5 rows moved to frontmatter.md. Rewrite to assert those rows are present in schema/reference/frontmatter.md (not AGENTS.md §5). If this test also checked behavior independent of §5 location, preserve that part.

    RUN the reconciled suites — they must be GREEN:
    `bash tests/phase-09.1/run.sh` — must pass
    `bash tests/phase-10/run.sh` — must pass
    Also re-run `bash tests/phase-07/run.sh && bash tests/phase-08/run.sh` to confirm no regression in the CI-wired suites.

    NEUTRALITY: the rewritten tests must not introduce real vault terms (they reference only abstract type names and file paths). Run `bash bin/check-neutrality.sh` (tests/ is not in PUBLIC_PATHS, but keep the discipline).

    FINAL COMMIT:
    Stage: tests/phase-09.1/test_template_parity.sh, tests/phase-09.1/test_agents_section_4_residue.sh, tests/phase-09.1/test_agents_section_16.sh, tests/phase-10/test_agents_template_parity_section_5.sh, tests/phase-10/test_agents_section_5_bootstrap_stage.sh
    Commit: `test(16): reconcile phase-09.1/phase-10 parity tests to post-extraction shape (08-04/09-06 precedent)`
  </action>
  <verify>
    <automated>bash /home/yishai/Documents/compendium/tests/phase-09.1/run.sh && bash /home/yishai/Documents/compendium/tests/phase-10/run.sh && bash /home/yishai/Documents/compendium/tests/phase-07/run.sh && bash /home/yishai/Documents/compendium/tests/phase-08/run.sh</automated>
  </verify>
  <done>
    - All 5 tests rewritten to assert the post-extraction shape (stubs + leaf-file content homes), not the monolith
    - `bash tests/phase-09.1/run.sh` passes (was red against the extraction before the rewrite)
    - `bash tests/phase-10/run.sh` passes
    - `bash tests/phase-07/run.sh && bash tests/phase-08/run.sh` still pass (no regression in CI-wired suites)
    - Each rewritten test asserts a real post-extraction invariant (stub points to correct leaf; moved content present in leaf), not a trivial pass
    - Final Phase 16 commit landed; REF-01..REF-10 complete
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AGENTS.md routing table → public | Must not contain private vault terms; routing table rows use only abstract file paths |
| schema/AGENTS.template.md → public | Template mirrors must not introduce private terms |
| wiki-cloud/decisions/ → cloud-safe | DR uses abstract placeholders; no private wiki content |
| AGENTS.md ≡ CLAUDE.md | Final byte-equality check after all edits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04-01 | Information Disclosure | AGENTS.md routing table (public file) | mitigate | Routing table rows contain only file paths and one-line descriptions — no vault content; `bash bin/check-neutrality.sh` |
| T-16-04-02 | Information Disclosure | wiki-cloud/decisions/dr-*-reference-extraction.md | mitigate | DR references only planning artifacts (not wiki-local/ or private content); abstract placeholders in any examples; neutrality check |
| T-16-04-03 | Tampering | AGENTS.template.md mirror accuracy | mitigate | Step E `diff <(grep -v '{{' AGENTS.md) <(grep -v '{{' template)` empty; `bash tests/phase-08/run.sh` + reconciled phase-09.1/phase-10 validate parity |
| T-16-04-04 | Tampering | Routing table targets — dangling Phase-17 pointers | mitigate | Phase-17 workflow rows marked "do NOT dereference yet"; Step F `test -f` resolvable rows only; Step F.5 post-render stub-integrity scan; `--check-tree` added at Phase 17 end |
| T-16-04-05 | Tampering | CI / prior-phase test regression | mitigate | Task 3 reconciles the 5 monolith-shape tests; `bash tests/phase-07/08/09.1/10 run.sh` all green before final commit |
| T-16-04-06 | Tampering | Now-false authority language survives (logical contradiction in spec) | mitigate | Step A.5 grep sweep (sole/authoritative/No other file/does NOT contain rules) reconciled to router framing; verify grep returns no now-false hits |
</threat_model>

<verification>
Full phase gate (run after Plan 04 final commit):
- `bash bin/sync-claude.sh --check` — exits 0 (AGENTS.md ≡ CLAUDE.md)
- `bash bin/check-neutrality.sh` — exits 0
- `bash bin/check-privacy.sh` — exits 0 (schema/ now covered — Plan 16-00 Task 2)
- `bash bin/lint.sh --ci` — exits 0
- `bash tests/phase-07/run.sh && bash tests/phase-08/run.sh` — pass (CI-wired suites)
- `bash tests/phase-09.1/run.sh && bash tests/phase-10/run.sh` — pass (reconciled in Task 3; were red against the extraction before the rewrite)
- `grep -q "IMPORTANT" AGENTS.md && grep -q "do NOT dereference" AGENTS.md` — split routing table present (Phase-17 rows non-dereferenceable)
- `! grep -qE 'sole authoritative|sole source of truth|No other file|does NOT contain rules|does NOT contain conventions' AGENTS.md` — authority-language sweep clean (no surviving now-false claim)
- `diff <(grep -v '{{' AGENTS.md) <(grep -v '{{' schema/AGENTS.template.md)` — empty (template parity outside placeholders)
- post-render stub-integrity: every schema/reference/…, schema/workflows/… (non-Phase-17), docs/reference/… path in AGENTS.md resolves on disk
- `grep -q "filename/path ONLY" schema/reference/wikilinks.md` — REF-05 verbatim guard
- `! grep -q "^## 16\. Appendices" AGENTS.md` — §16 deleted
- `ls wiki-cloud/decisions/dr-*-reference-extraction*.md` — DR exists
- `grep -q "trigger_type: schema-update" wiki-cloud/decisions/dr-2026-06-04-reference-extraction.md && grep -q "affected_pages: \[\]" wiki-cloud/decisions/dr-2026-06-04-reference-extraction.md`
- For each extracted file: `test -f schema/reference/{page-types,frontmatter,provenance,wikilinks,privacy}.md`
- `test -f schema/workflows/lint.md && test -f docs/reference/scaling.md && test -f docs/reference/tooling.md`
</verification>

<success_criteria>
Split routing table present in AGENTS.md with IMPORTANT: flag — resolvable rows point to existing files; ingest/query/reflect rows marked "do NOT dereference yet" / *(Phase 17)*. D-09 line 3 updated; §3 MUST-NOT line 130 updated; repo-wide authority-language sweep reconciled. schema/AGENTS.template.md matches AGENTS.md on every non-placeholder line (per-commit upstream mirrors + this plan's changes). Decision record exists with trigger_type: schema-update, affected_pages: [], all 7 sections. The 5 prior-phase parity/residue tests reconciled to the post-extraction shape and green. All gates green: sync-claude --check, neutrality, privacy, lint --ci, phase-07/08/09.1/10 tests. Phase 16 REF-01..REF-10 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/16-reference-extraction/16-04-SUMMARY.md`
</output>
