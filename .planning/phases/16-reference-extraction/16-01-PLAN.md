---
phase: 16-reference-extraction
plan: "01"
type: execute
wave: 1
depends_on:
  - "16-00"
files_modified:
  - AGENTS.md
  - CLAUDE.md
  - schema/AGENTS.template.md
  - schema/reference/page-types.md
  - schema/reference/frontmatter.md
autonomous: true
requirements:
  - REF-01
  - REF-02
  - REF-04
  - REF-09

must_haves:
  truths:
    - "schema/reference/page-types.md exists and contains the full §4 content plus the §7 per-type section ordering table merged in"
    - "schema/reference/frontmatter.md exists and contains the full §5 content (~140 lines)"
    - "AGENTS.md §4 is replaced by a 6-type-names dispatch list + bare stub pointer"
    - "AGENTS.md §5 is replaced by an Option-B ~4-line pointer only"
    - "§7 Progressive Disclosure section is deleted from AGENTS.md (dissolves, no stub)"
    - "schema/AGENTS.template.md mirrors the SAME §4 stub, §5 stub, and §7 deletion in the SAME commit (REF-09 per-commit mirror — the template has no sync gate, so this is a manual same-commit obligation)"
    - "Every dissolved §7 subsection has a named destination per the §7 Disposition Table (no silent content loss)"
    - "AGENTS.md and CLAUDE.md are byte-identical after extraction"
    - "AGENTS.md ≡ schema/AGENTS.template.md modulo the {{placeholder}} lines after this commit (no Wave-1→Wave-2 red-parity window)"
  artifacts:
    - path: "schema/reference/page-types.md"
      provides: "§4 page type rules + §7 section orderings merged"
      min_lines: 100
    - path: "schema/reference/frontmatter.md"
      provides: "§5 frontmatter schema + validation checklist"
      min_lines: 130
    - path: "AGENTS.md"
      provides: "§4 stub (6 names + pointer) + §5 Option-B pointer + §7 dissolved"
      contains: "schema/reference/page-types.md"
    - path: "schema/AGENTS.template.md"
      provides: "§4/§5 stubs + §7 deletion mirrored from AGENTS.md (same commit)"
      contains: "schema/reference/page-types.md"
  key_links:
    - from: "AGENTS.md §4 stub"
      to: "schema/reference/page-types.md"
      via: "bare pointer"
      pattern: "schema/reference/page-types\\.md"
    - from: "AGENTS.md §5 stub"
      to: "schema/reference/frontmatter.md"
      via: "Option-B pointer"
      pattern: "schema/reference/frontmatter\\.md"
---

<objective>
Extract §4 Page Types and §5 Frontmatter from AGENTS.md into standalone reference files; dissolve §7 Progressive Disclosure.

Purpose: REF-01 (page-types.md), REF-02 (frontmatter.md), and REF-04 (§7 dissolved). These three sections are pure reference content — they have clean JIT load points (authoring time) and their miss-cost is gate-caught by lint. By D-04 and D-01, core keeps only the 6 type names as a dispatch list for §4, an Option-B pointer for §5, and nothing for §7.

Output: Two new schema/reference/ files; AGENTS.md §4, §5, §7 replaced with stubs/dissolved; CLAUDE.md byte-synced; schema/AGENTS.template.md mirrored in the SAME commit (REF-09).

REVIEW NOTE (cross-AI, addressed in this re-plan): Both reviewers rated the template-mirror as a HIGH concern — deferring all template edits to Plan 16-04 leaves a multi-commit window where AGENTS.md points to extracted refs but the wizard template still renders the full monolith, breaks bisectability, and (Claude proved) silently red-fails pre-existing parity tests. FIX: each editing plan (16-01/02/03) now mirrors its OWN stub edits into schema/AGENTS.template.md in the SAME commit. This plan owns the §4 stub, §5 stub, and §7 deletion mirrors. The template has no automated sync gate (sync-claude.sh only validates AGENTS.md ≡ CLAUDE.md), so the mirror is a MANUAL same-commit obligation — apply identical text at the same logical position, leaving the {{placeholder}} lines untouched. The parity-test reconciliation for the sections this plan changes is owned by the new final task in Plan 16-04 (Task 3), which rewrites the 5 prior-phase tests that codify the monolith shape and RUNS them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/16-reference-extraction/16-RESEARCH.md
@.planning/phases/16-reference-extraction/16-CONTEXT.md
@.planning/phases/16-reference-extraction/16-PATTERNS.md
@.planning/milestones/v1.2-MILESTONE-BRIEF.md

<interfaces>
<!-- KEY LINE RANGES (verified against live CLAUDE.md, 1655 lines — RESEARCH.md §"Exact Section Line Ranges") -->
<!-- §4 Page Types: lines 141–258 (118 lines) → schema/reference/page-types.md -->
<!-- §5 Frontmatter: lines 259–398 (140 lines) → schema/reference/frontmatter.md -->
<!-- §7 Progressive Disclosure: lines 619–654 (36 lines) → DISSOLVE (no stub, no file) -->
<!-- §7 Per-Type Section Ordering table: lines 635–643 → merges into page-types.md -->
<!-- §3 Navigation Rule: lines 113–121 — stays resident (already in core, §7 nav rules are DUPLICATE) -->

<!-- RESIDENT REMNANT for §4 (D-04 names-only — from RESEARCH.md Resident Remnant Specification): -->
<!-- Six type names as compact dispatch list: entity, concept, source, comparison, overview, decision -->
<!-- Plus stub: "→ See `schema/reference/page-types.md` for section ordering, authoring conventions, and type details." -->

<!-- RESIDENT REMNANT for §5 (Option-B pointer ~4 lines — LOCKED): -->
<!-- "→ Full frontmatter schema and validation checklist in `schema/reference/frontmatter.md`." -->

<!-- AGENTS.template.md placeholder lines that move with §5 (PATTERNS.md line ~95–99): -->
<!-- Template line 285: knowledge_domain: "{{PRIMARY_DOMAIN}}" — moves to schema/reference/frontmatter.md (template copy only) -->
<!-- Template line 286: privacy_default: {{DEFAULT_PRIVACY}} — moves to schema/reference/frontmatter.md (template copy only) -->
<!-- In AGENTS.md/CLAUDE.md: these lines appear rendered (no {{...}}), they move to frontmatter.md leaf -->

<!-- STUB FORMAT (D-01 — bare pointer, PATTERNS.md): -->
<!--   §4 section replacement: -->
<!--   ## 4. Page Types and Templates -->
<!--   Six page types: **entity**, **concept**, **source**, **comparison**, **overview**, **decision**. -->
<!--   → See `schema/reference/page-types.md` for section ordering, authoring conventions, and full type details. -->
<!--   §5 section replacement: -->
<!--   ## 5. Frontmatter Schema -->
<!--   → Full frontmatter schema and validation checklist in `schema/reference/frontmatter.md`. -->
<!--   §7: DELETE lines 619–654 entirely. No stub, no remnant. §3 nav rule already covers it. -->

<!-- §7 DISPOSITION TABLE (every dissolved subsection → named destination; no silent loss) -->
<!-- Required by both reviewers (MEDIUM): "audit for absorption" is too soft for a deletion. -->
<!-- | §7 subsection                         | Destination                                              | -->
<!-- |---------------------------------------|----------------------------------------------------------| -->
<!-- | "## 7. Progressive Disclosure" intro   | DROPPED — restates the shallow-to-deep principle already  | -->
<!-- |   ("Principle: ... shallow-to-deep")   |   stated in §3 LLM Navigation Rule + AGENTS.md preamble.  | -->
<!-- | "### Rules for LLM Agents" (7 bullets) | DROPPED — bullets 1–3 (read index first; TL;DR/Key Facts | -->
<!-- |                                        |   before Detail) duplicate §3 "LLM Navigation Rule"      | -->
<!-- |                                        |   VERBATIM (3 numbered steps). Bullets 4–7 (TL;DR length, | -->
<!-- |                                        |   Key Facts compact, Detail narrative, Sources at bottom)| -->
<!-- |                                        |   → MERGED into page-types.md "### Authoring Conventions".| -->
<!-- | "### Per-Type Section Ordering" table   | → page-types.md "## Per-Type Section Ordering" table.     | -->
<!-- | "### Why This Matters" (worked example) | DROPPED — illustrative narrative, not a rule; the value   | -->
<!-- |                                        |   (progressive disclosure saves context) is captured by  | -->
<!-- |                                        |   §3 nav rule + the ordering itself.                     | -->
<!-- VERIFICATION OBLIGATION: before deleting §7, the executor MUST grep AGENTS.md §3 to confirm the -->
<!-- "LLM Navigation Rule" 3 numbered steps (read index first; TL;DR/Key Facts before Detail) are -->
<!-- present. If absent, STOP — the §7 nav rules cannot be dropped as "duplicate" until §3 proves it. -->

<!-- TEMPLATE MIRROR (REF-09, per-commit — same commit as the AGENTS.md edit): -->
<!-- schema/AGENTS.template.md must receive the IDENTICAL §4 stub, §5 stub, and §7 deletion. -->
<!-- The template mirrors AGENTS.md except for {{placeholder}} lines. §4/§5/§7 contain NO placeholders -->
<!-- in their stub forms, so the stub text is byte-identical between AGENTS.md and template. -->
<!-- NOTE: the §5 leaf file frontmatter.md carries the template-copy placeholder lines -->
<!-- ({{PRIMARY_DOMAIN}} ×2 from the §5 base-fields block) — those move to frontmatter.md, NOT to -->
<!-- the §5 stub. The §5 STUB in both AGENTS.md and template is the same 2-line pointer (no placeholder). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create schema/reference/page-types.md (§4 content + §7 ordering table merged)</name>
  <files>schema/reference/page-types.md</files>
  <read_first>
    1. CLAUDE.md lines 141–258 — full §4 content to extract verbatim (all subsections 4.1–4.6)
    2. CLAUDE.md lines 619–654 — §7 Progressive Disclosure content to dissolve; lines 635–643 are the Per-Type Section Ordering table that merges here
    3. PATTERNS.md "schema/reference/page-types.md" section — exact file header pattern and "source of truth" lede
    4. PATTERNS.md "Shared Patterns" "Source of truth lede for schema/reference/*.md files" — lede pattern
  </read_first>
  <action>
    Create `schema/reference/page-types.md` (directory `schema/reference/` is new — `mkdir -p` as needed).

    File structure (in order):

    1. HEADER with agent-authoritative lede (from PATTERNS.md pattern):
       ```
       # Page Types and Templates

       > Agent-authoritative reference for wiki page types, section ordering, and authoring conventions.
       > AGENTS.md §4 points here. If you find a discrepancy between this file and AGENTS.md, this file wins.
       ```

    2. DISPATCH SECTION (short, since the type names also stay in core):
       ```
       ## Page Types (dispatch vocabulary)

       Six types exist: **entity**, **concept**, **source**, **comparison**, **overview**, **decision**.
       ```

    3. VERBATIM §4 CONTENT: Copy CLAUDE.md lines 141–258 verbatim EXCEPT the section header "## 4. Page Types and Templates" (already above). Include all six subsections 4.1–4.6 with their full text, field tables, "When to use:", "Example:", and "See:" lines.

    4. MERGED SECTION ORDERING TABLE: After the six type subsections, add a merged section:
       ```
       ## Per-Type Section Ordering

       | Page Type | Section Order |
       |-----------|--------------|
       | Entity | TL;DR -> Key Facts -> Detail -> Related Pages -> Sources |
       | Concept | TL;DR -> Key Facts -> Detail -> Related Pages -> Sources |
       | Source Summary | TL;DR -> Key Takeaways -> Extracted Claims -> Notes -> Source Metadata |
       | Comparison | TL;DR -> Bottom Line -> Comparison Table -> Detailed Comparison -> Sources |
       | Overview | TL;DR -> Key Facts -> Detail -> Related Pages -> Sources |
       | Decision | TL;DR -> Decision -> Why -> Alternatives Considered -> Consequences -> Affected Pages -> Sources |

       ### Authoring Conventions

       - **TL;DR** MUST be 1 short paragraph or 2–4 bullets.
       - **Key Facts** MUST be compact bullets with inline provenance markers.
       - **Detail** contains full narrative, synthesis, caveats, and nuance.
       - **Sources** at the bottom lists human-readable source references with wikilinks to source summary pages.
       ```
       (This absorbs the §7 lines 623–631 authoring rules and lines 635–643 ordering table — both dissolve from core with §7.)

    5. SEE ALSO footer (from PATTERNS.md):
       ```
       ## See Also

       - [AGENTS.md](../../AGENTS.md) — §4 stub (dispatch pointer to this file).
       - `schema/reference/frontmatter.md` — frontmatter validation checklist.
       - `schema/templates/` — blank page templates per type.
       ```

    NEUTRALITY RULE: All examples and illustrative content in the extracted §4 text use abstract placeholders. CLAUDE.md §4 already uses abstract placeholder forms per §3 MUST-NOT (e.g., `<concept-slug>`, `<entity-id>`). Do not introduce real vault terms.
    
    After creating the file, run: `bash bin/check-neutrality.sh`
    Verify it exits 0.
  </action>
  <verify>
    <automated>test -f /home/yishai/Documents/compendium/schema/reference/page-types.md && wc -l /home/yishai/Documents/compendium/schema/reference/page-types.md && grep -c "section order" /home/yishai/Documents/compendium/schema/reference/page-types.md</automated>
  </verify>
  <done>schema/reference/page-types.md exists with ≥100 lines. `grep -c "section order"` returns ≥1. `bash bin/check-neutrality.sh` exits 0. The file contains all six type subsections (4.1–4.6) verbatim and the merged Per-Type Section Ordering table from §7. Note: this leaf file is created here but NOT committed in this task — it is staged and committed in Task 2 as part of the combined commit (page-types.md + frontmatter.md + AGENTS.md/CLAUDE.md stub edits).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create schema/reference/frontmatter.md (§5 content); stub §4/§5/§7 in AGENTS.md AND schema/AGENTS.template.md (per-commit mirror); sync to CLAUDE.md; commit</name>
  <files>schema/reference/frontmatter.md, AGENTS.md, CLAUDE.md, schema/AGENTS.template.md</files>
  <read_first>
    1. CLAUDE.md lines 259–398 — full §5 Frontmatter content to extract verbatim
    2. CLAUDE.md lines 141–143 — current §4 header (to replace with stub)
    3. CLAUDE.md lines 619–654 — §7 block to DELETE entirely (no stub)
    4. PATTERNS.md "schema/reference/frontmatter.md" section — structural pattern + placeholder migration note
    5. PATTERNS.md "AGENTS.md / CLAUDE.md" section — per-section stub forms table; §4/§5/§7 rows
    6. RESEARCH.md "Resident Remnant Specification" — exact remnant text for §4 and §5
    7. RESEARCH.md "AGENTS.template.md Placeholder Preservation" — which {{placeholder}} lines move
    8. AGENTS.md §3 "LLM Navigation Rule" (the 3 numbered steps) — MUST confirm present before dropping §7 nav rules as duplicate (§7 Disposition Table verification obligation)
    9. schema/AGENTS.template.md — read its §4/§5/§7 regions to confirm the same logical positions for the per-commit mirror; note the {{placeholder}} lines so they stay untouched
  </read_first>
  <action>
    STEP A — Create schema/reference/frontmatter.md:

    File structure:
    1. Header with agent-authoritative lede:
       ```
       # Frontmatter Schema

       > Agent-authoritative reference for all wiki page frontmatter fields, types, validation rules, and compilation tracking.
       > AGENTS.md §5 points here.
       ```
    2. Verbatim copy of CLAUDE.md lines 259–398 content (all of §5): Base Fields YAML block, Field Descriptions table, Source Summary Additional Fields, Compilation Tracking Fields, Compilation Status Transition Rules table, Decision Record Additional Fields, Optional Back-Link Field, and the Frontmatter Validation Checklist. Do NOT include the "## 5. Frontmatter Schema" header (already in file header above).
    3. See Also footer:
       ```
       ## See Also

       - [AGENTS.md](../../AGENTS.md) — §5 stub (pointer to this file).
       - `schema/reference/page-types.md` — per-type section ordering and authoring conventions.
       ```

    STEP A.5 — §7 disposition guard (MUST run before deleting §7):
    Grep AGENTS.md §3 to confirm the "LLM Navigation Rule" 3 numbered steps are present:
    `grep -A8 "LLM Navigation Rule" AGENTS.md` — must show "Read `wiki-cloud/index.md` FIRST", "Scan `## TL;DR` and `## Key Facts`", and "Only read `## Detail`". If any are absent, STOP — do not delete §7's "Rules for LLM Agents" as duplicate until §3 proves coverage. (This is the §7 Disposition Table verification obligation — see interfaces block. §7 bullets 4–7 about TL;DR/Key Facts/Detail/Sources formatting are MERGED into page-types.md "### Authoring Conventions" by Task 1; confirm Task 1 created that subsection.)

    STEP B — Edit AGENTS.md (three replacements in sequence, working bottom-up to preserve line numbers):

    REPLACEMENT 1 — §7 DELETE (lines 619–654):
    Delete the entire §7 Progressive Disclosure block (the "## 7. Progressive Disclosure" header through the end of that section, including "### Rules for LLM Agents", the 7 bullets, "### Per-Type Section Ordering" table, and "### Why This Matters"). NO stub left. The nav rules are already in §3 (verified in Step A.5); the formatting bullets and ordering table are in page-types.md.

    REPLACEMENT 2 — §5 STUB (lines 259–398, replace with ~4 lines):
    Replace the entire §5 content with:
    ```
    ## 5. Frontmatter Schema

    → Full frontmatter schema and validation checklist in `schema/reference/frontmatter.md`.
    ```
    
    REPLACEMENT 3 — §4 STUB (lines 141–258, replace with ~6 lines):
    Replace the entire §4 content with:
    ```
    ## 4. Page Types and Templates

    Six page types: **entity**, **concept**, **source**, **comparison**, **overview**, **decision**.

    → See `schema/reference/page-types.md` for section ordering, authoring conventions, and full type details.
    ```

    IMPORTANT: Work bottom-up when editing AGENTS.md to avoid line-number drift between replacements.

    STEP B.5 — Mirror the SAME edits into schema/AGENTS.template.md (REF-09, per-commit mirror):
    Apply the IDENTICAL three changes (§7 deletion, §5 stub, §4 stub) to schema/AGENTS.template.md at the same logical positions, working bottom-up there too. The template's §4/§5/§7 regions differ from AGENTS.md ONLY in {{placeholder}} lines; the §4 and §5 STUBS and the §7 DELETION are byte-identical to what you wrote in AGENTS.md (the stubs contain no placeholders). After mirroring, the template's §4/§5/§7 must match AGENTS.md's §4/§5/§7 exactly. This closes the HIGH template-mirror concern and prevents a Wave-1→Wave-2 red-parity window. The template has NO sync gate — this mirror is manual and MUST be in this commit.

    Verify the mirror locally:
    - `awk '/^## 4\./{f=1} /^## 5\./{f=0} f' AGENTS.md > /tmp/a4; awk '/^## 4\./{f=1} /^## 5\./{f=0} f' schema/AGENTS.template.md > /tmp/t4; diff /tmp/a4 /tmp/t4` — should be empty (the §4 stub matches)
    - `! grep -q "^## 7\. Progressive Disclosure" schema/AGENTS.template.md` — §7 gone from template too

    STEP C — Propagate to CLAUDE.md:
    Run: `bash bin/sync-claude.sh`
    This copies AGENTS.md to CLAUDE.md atomically. Then run: `bash bin/sync-claude.sh --check`
    Must exit 0 (byte-identical).

    STEP D — Verify neutrality:
    Run: `bash bin/check-neutrality.sh`
    Must exit 0.

    STEP E — Commit:
    Stage: AGENTS.md, CLAUDE.md, schema/AGENTS.template.md, schema/reference/page-types.md, schema/reference/frontmatter.md
    Commit: `schema(ref-01/02/04/09): extract §4 page-types + §5 frontmatter; dissolve §7; mirror template`

    One commit per logical operation (AGENTS.md convention). The template mirror MUST be in THIS commit, not deferred.
    NOTE: the pre-commit hook runs `sync-claude.sh --check` (AGENTS↔CLAUDE) — it does NOT validate the template. The template mirror correctness is enforced by the Plan 16-04 Task 3 parity-test rewrite, which RUNS the updated tests/phase-09.1 + tests/phase-10 suites.
  </action>
  <verify>
    <automated>bash /home/yishai/Documents/compendium/bin/sync-claude.sh --check && test -f /home/yishai/Documents/compendium/schema/reference/frontmatter.md && grep -q "schema/reference/page-types.md" /home/yishai/Documents/compendium/AGENTS.md && ! grep -q "^## 7\. Progressive Disclosure" /home/yishai/Documents/compendium/AGENTS.md && grep -q "schema/reference/page-types.md" /home/yishai/Documents/compendium/schema/AGENTS.template.md && ! grep -q "^## 7\. Progressive Disclosure" /home/yishai/Documents/compendium/schema/AGENTS.template.md</automated>
  </verify>
  <done>
    - schema/reference/frontmatter.md exists with ≥130 lines (verbatim §5 content)
    - AGENTS.md §4 contains only "Six page types: entity/concept/source/comparison/overview/decision" + stub pointer
    - AGENTS.md §5 contains only the Option-B ~2-line pointer to schema/reference/frontmatter.md
    - AGENTS.md has NO "## 7. Progressive Disclosure" header
    - schema/AGENTS.template.md §4/§5 stubs match AGENTS.md and template has NO "## 7. Progressive Disclosure" header (per-commit mirror — REF-09)
    - §3 "LLM Navigation Rule" 3 numbered steps confirmed present (Step A.5 guard) before §7 was dropped
    - `bash bin/sync-claude.sh --check` exits 0 (AGENTS.md ≡ CLAUDE.md byte-identical)
    - `bash bin/check-neutrality.sh` exits 0
    - All FIVE files (incl. schema/AGENTS.template.md) staged and committed in one commit
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| §4/§5 content → schema/reference/*.md (public) | Extracted text must contain no private vault terms |
| AGENTS.md ≡ CLAUDE.md | Byte-equality must hold after every commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-01 | Information Disclosure | schema/reference/page-types.md | mitigate | Run `bash bin/check-neutrality.sh` after file creation; §4 already uses abstract placeholders per §3 MUST-NOT |
| T-16-01-02 | Information Disclosure | schema/reference/frontmatter.md | mitigate | Same neutrality check; §5 is structural schema with no vault-specific content |
| T-16-01-03 | Tampering | AGENTS.md ≡ CLAUDE.md byte equality | mitigate | Run `bash bin/sync-claude.sh --check` before commit; pre-commit hook enforces it |
</threat_model>

<verification>
After plan 01 commit:
- `test -f schema/reference/page-types.md && wc -l schema/reference/page-types.md` — expect ≥100 lines
- `test -f schema/reference/frontmatter.md && wc -l schema/reference/frontmatter.md` — expect ≥130 lines
- `grep -q "schema/reference/page-types.md" AGENTS.md` — exits 0 (stub present)
- `grep -q "schema/reference/frontmatter.md" AGENTS.md` — exits 0 (stub present)
- `! grep -q "^## 7\." AGENTS.md` — exits 0 (§7 dissolved)
- `grep -q "schema/reference/page-types.md" schema/AGENTS.template.md` — exits 0 (template §4 stub mirrored)
- `grep -q "schema/reference/frontmatter.md" schema/AGENTS.template.md` — exits 0 (template §5 stub mirrored)
- `! grep -q "^## 7\. Progressive Disclosure" schema/AGENTS.template.md` — exits 0 (template §7 dissolved)
- `bash bin/sync-claude.sh --check` — exits 0
- `bash bin/check-neutrality.sh` — exits 0
- Content preservation: `grep -q "type: entity|concept|source|comparison|overview|decision" schema/reference/page-types.md` and `grep -q "Frontmatter Validation Checklist" schema/reference/frontmatter.md`
- §7 disposition: page-types.md contains "### Authoring Conventions" (§7 bullets 4–7 merged) and "## Per-Type Section Ordering" (§7 ordering table merged); §3 "LLM Navigation Rule" present in AGENTS.md (§7 bullets 1–3 covered)
</verification>

<success_criteria>
schema/reference/page-types.md and schema/reference/frontmatter.md exist with the full verbatim content of §4 and §5 respectively (plus §7 ordering table merged into page-types.md). AGENTS.md §4 has only the 6 type names + stub; §5 has only the Option-B pointer; §7 is gone. AGENTS.md ≡ CLAUDE.md byte-identical. Neutrality gate passes.
</success_criteria>

<output>
After completion, create `.planning/phases/16-reference-extraction/16-01-SUMMARY.md`
</output>
