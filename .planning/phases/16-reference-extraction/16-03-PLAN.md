---
phase: 16-reference-extraction
plan: "03"
type: execute
wave: 1
depends_on:
  - "16-02"
files_modified:
  - AGENTS.md
  - CLAUDE.md
  - schema/reference/wikilinks.md
  - schema/reference/privacy.md
  - docs/reference/scaling.md
  - docs/reference/tooling.md
autonomous: true
requirements:
  - REF-05
  - REF-06
  - REF-07

must_haves:
  truths:
    - "schema/reference/wikilinks.md exists and carries the v1.1.1 uniform-piped-link truth verbatim (filename/path ONLY resolution)"
    - "schema/reference/privacy.md exists with the asymmetric two-directory model rules"
    - "docs/reference/scaling.md exists with the §14 content"
    - "docs/reference/tooling.md exists with the §15 content"
    - "§8, §13, §14, §15, §16 are replaced with stubs or deleted from AGENTS.md"
    - "Appendix C rule 6 ('Privacy default: wiki-local/ tier') is absorbed before §16 deletion"
    - "AGENTS.md and CLAUDE.md are byte-identical after all changes"
  artifacts:
    - path: "schema/reference/wikilinks.md"
      provides: "§8 wikilink conventions + §3 Red Links; v1.1.1 uniform-piped-link truth verbatim"
      min_lines: 45
    - path: "schema/reference/privacy.md"
      provides: "§13 asymmetric tier model (agent-authoritative terse)"
      min_lines: 15
    - path: "docs/reference/scaling.md"
      provides: "§14 Scaling Boundaries (end-user reference)"
      min_lines: 40
    - path: "docs/reference/tooling.md"
      provides: "§15 Tooling and Integrations (end-user reference)"
      min_lines: 20
    - path: "AGENTS.md"
      provides: "§8 one-liner stub + §13 structural pointer + §14/§15 stubs + §16 deleted"
      contains: "schema/reference/wikilinks.md"
  key_links:
    - from: "AGENTS.md §8 stub"
      to: "schema/reference/wikilinks.md"
      via: "bare pointer"
      pattern: "schema/reference/wikilinks\\.md"
    - from: "AGENTS.md §13 stub"
      to: "schema/reference/privacy.md"
      via: "structural pointer"
      pattern: "schema/reference/privacy\\.md"
    - from: "schema/reference/wikilinks.md"
      to: "v1.1.1 truth"
      via: "verbatim text"
      pattern: "filename/path ONLY"
---

<objective>
Extract §8 wikilinks → schema/reference/wikilinks.md; extract §13 privacy → schema/reference/privacy.md; extract §14 → docs/reference/scaling.md; extract §15 → docs/reference/tooling.md; delete §16 (after Appendix C audit).

Purpose: REF-05 (wikilinks.md — carries v1.1.1 verbatim link truth), REF-06 (privacy.md — Phase-15 asymmetric form), REF-07 (§14/§15 → docs/, §16 deleted). This plan completes all leaf-file extractions. After this plan, only the routing table, template mirror, and decision record remain (Plan 04).

Output: Four new leaf files; AGENTS.md §8/§13/§14/§15/§16 replaced or deleted; CLAUDE.md synced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/16-reference-extraction/16-RESEARCH.md
@.planning/phases/16-reference-extraction/16-CONTEXT.md
@.planning/phases/16-reference-extraction/16-PATTERNS.md
@.planning/milestones/v1.2-MILESTONE-BRIEF.md
@.planning/phases/16-reference-extraction/16-02-SUMMARY.md

<interfaces>
<!-- LINE RANGES in live CLAUDE.md at Plan 03 start (lines have shifted from Plan 01/02 removals): -->
<!-- NOTE: After Plan 01 (§4/§5/§7 replaced with stubs) and Plan 02 (§6 replaced with 4-line stub), -->
<!-- the actual line numbers in AGENTS.md at Plan 03 start are DIFFERENT from original positions. -->
<!-- The executor MUST locate sections by their ## header text, not by line number. -->

<!-- §8 Wikilinks: ## 8. Wikilink and Graph Conventions -->
<!--   Original: lines 655–705 (51 lines) → schema/reference/wikilinks.md -->
<!--   Also include: §3 Red Links block (3 lines, ~line 123–125 in original, around "### Red Links") -->
<!--   §8 RESIDENT REMNANT: one-liner "Use [[id|Title]] for ALL intra-wiki links — see wikilinks.md." -->

<!-- §13 Privacy: ## 13. Privacy Routing -->
<!--   Original: lines 1556–1559 (4 lines, already a 2-line pointer post-Phase-15) -->
<!--   This is the ALREADY-REWRITTEN asymmetric form from Phase 15 -->
<!--   EXTRACT the 2-line pointer content into privacy.md; replace §13 with bare stub -->
<!--   REF-06: extract the Phase-15 asymmetric form, not the old per-page form -->

<!-- §14 Scaling: ## 14. Scaling Boundaries -->
<!--   Original: lines 1560–1606 (47 lines) → docs/reference/scaling.md -->
<!--   §14 gets NO stub in core — a routing-table row covers it -->

<!-- §15 Tooling: ## 15. Tooling and Integrations -->
<!--   Original: lines 1607–1631 (25 lines) → docs/reference/tooling.md -->
<!--   §15 gets NO stub in core — a routing-table row covers it -->

<!-- §16 Appendices: ## 16. Appendices and Examples -->
<!--   Original: lines 1632–1655 (24 lines) — DELETE -->
<!--   BEFORE DELETING: audit Appendix C rules for absorption needs -->
<!--   Appendix C rule 6 "Privacy default: wiki-local/ tier" MUST be verified present in -->
<!--   AGENTS.md §3 MUST-NOT list before deletion (per RESEARCH.md Pitfall 6) -->
<!--   ALSO check: rule 3 wikilink form (already in §3 MUST-NOT), rule 4 provenance (in §6 remnant) -->
<!--   §16 Appendix A and B are pure pointers to docs/ — no absorption needed -->

<!-- §8 STUB FORMS (D-01, from PATTERNS.md per-section stub forms table): -->
<!-- ## 8. Wikilink and Graph Conventions -->
<!-- Use `[[id|Title]]` for ALL intra-wiki links — see `schema/reference/wikilinks.md`. -->
<!-- (§3 Red Links note moves to wikilinks.md; stub in §3 Red Links location: "→ See wikilinks.md") -->

<!-- §13 STUB (D-01, structural pointer form from Phase 15 CONTEXT D-16): -->
<!-- ## 13. Privacy Routing -->
<!-- → See `schema/reference/privacy.md` for the agent-facing tier rules. -->
<!--   For the full human-facing model: `docs/reference/privacy-model.md`. -->

<!-- REF-05 VERBATIM GUARD: wikilinks.md MUST contain this exact text from CLAUDE.md lines 659–670: -->
<!-- "1. Use `[[id|Exact Title]]` for ALL intra-wiki cross-references in page body text. -->
<!--    The target before `|` is the page `id` (= filename stem — always resolves in Obsidian -->
<!--    since Obsidian resolves `[[X]]` by **filename/path ONLY**, never by `title` and never -->
<!--    by `aliases`). The display text after `|` is the exact canonical `title`." -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create schema/reference/wikilinks.md and schema/reference/privacy.md</name>
  <files>schema/reference/wikilinks.md, schema/reference/privacy.md</files>
  <read_first>
    1. CLAUDE.md section "## 8. Wikilink and Graph Conventions" — full §8 content to extract
    2. CLAUDE.md "### Red Links" (§3 Red Links paragraph ~lines 123–125) — also goes to wikilinks.md
    3. CLAUDE.md section "## 13. Privacy Routing" — current 1–2 line asymmetric pointer to extract
    4. PATTERNS.md "schema/reference/wikilinks.md" section — verbatim preservation requirement for REF-05
    5. PATTERNS.md "schema/reference/privacy.md" section — structure pattern showing relationship to docs/reference/privacy-model.md
    6. docs/reference/privacy-model.md lines 1–12 — audience distinction (do NOT duplicate)
  </read_first>
  <action>
    CREATE FILE A — schema/reference/wikilinks.md:

    File structure:
    1. Header:
       ```
       # Wikilink and Graph Conventions

       > Agent-authoritative reference for intra-wiki linking: the uniform piped-link form, red links, and Obsidian graph behavior.
       > AGENTS.md §8 and §3 Red Links point here. This file carries the v1.1.1 uniform-piped-link truth.
       ```

    2. VERBATIM §3 Red Links content (the "### Red Links" paragraph from §3, ~lines 123–125):
       Include this as a section at the top, before the main §8 content:
       ```
       ## Red Links

       Red links (wikilinks to non-existent pages) are allowed and intentional. They signal knowledge gaps that the lint workflow tracks. Do not remove red links unless creating the target page or confirming the gap is irrelevant.
       ```

    3. VERBATIM §8 CONTENT: Copy the entire §8 section verbatim — Rules (items 1–9), Bad vs. Good Wikilink Examples, Graph View Implications. The "## 8. Wikilink and Graph Conventions" header is NOT copied (file has its own header).

    REF-05 GUARD: Verify that Rule 1 text carries verbatim the exact phrase "filename/path ONLY" and the phrase "Obsidian resolves `[[X]]` by **filename/path ONLY**". This is non-negotiable.

    4. See Also footer:
       ```
       ## See Also

       - [AGENTS.md](../../AGENTS.md) — §8 stub (pointer to this file).
       - `schema/reference/page-types.md` — per-type section ordering (relevant for link context).
       ```

    CREATE FILE B — schema/reference/privacy.md:

    File structure:
    ```
    # Privacy Routing

    > Agent-authoritative reference for wiki privacy tier routing: `wiki-cloud/` (cloud-safe) vs `wiki-local/` (local-only).
    > AGENTS.md §13 points here.

    ## Structural Rule

    Vault tier is **structural**: the directory a page lives in determines its privacy tier.

    - `wiki-cloud/` — cloud-safe tier. Cloud sessions may read freely.
    - `wiki-local/` — local-only tier. Cloud sessions MUST NOT read this tier.

    The enforcement mechanism is the **directory boundary** (harness permissions), not a per-turn agent rule.

    **One-way permeability:** local sessions read both tiers; cloud sessions read only `wiki-cloud/`.

    **Write-back rule:** If ANY contributing source-summary lives under `wiki-local/`, the write-back target page MUST go into `wiki-local/`.

    **Privacy default:** When in doubt, place content in `wiki-local/` — do not expose to cloud sessions.

    ## See Also

    - `docs/reference/privacy-model.md` — full asymmetric model: enforcement options (deny-profile vs. separate-repo), fail-direction table, `sources-local/` forward reference. End-user audience.
    - [AGENTS.md](../../AGENTS.md) — §13 stub (pointer to this file).
    ```

    NOTE: The "Privacy default: wiki-local/ tier" rule is included in privacy.md. This covers Appendix C rule 6 which will be deleted with §16 — verify this text is present before Plan 03 Task 2 deletes §16.

    Run: `bash bin/check-neutrality.sh` — must exit 0.
  </action>
  <verify>
    <automated>test -f /home/yishai/Documents/compendium/schema/reference/wikilinks.md && grep -q "filename/path ONLY" /home/yishai/Documents/compendium/schema/reference/wikilinks.md && test -f /home/yishai/Documents/compendium/schema/reference/privacy.md && grep -q "wiki-local" /home/yishai/Documents/compendium/schema/reference/privacy.md</automated>
  </verify>
  <done>
    schema/reference/wikilinks.md exists and contains "filename/path ONLY" (REF-05 verbatim guard). schema/reference/privacy.md exists and contains the asymmetric tier model. Both files pass `bash bin/check-neutrality.sh`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create docs/reference/scaling.md + tooling.md; stub/delete §8/§13/§14/§15/§16 in AGENTS.md; sync CLAUDE.md; commit</name>
  <files>docs/reference/scaling.md, docs/reference/tooling.md, AGENTS.md, CLAUDE.md</files>
  <read_first>
    1. CLAUDE.md section "## 14. Scaling Boundaries" — full §14 content to extract verbatim
    2. CLAUDE.md section "## 15. Tooling and Integrations" — full §15 content to extract verbatim
    3. CLAUDE.md section "## 16. Appendices and Examples" — audit Appendix C rule 6 before deletion
    4. PATTERNS.md "docs/reference/scaling.md" and "docs/reference/tooling.md" sections — file header patterns
    5. PATTERNS.md "AGENTS.md / CLAUDE.md" — per-section stub forms table for §8/§13/§14/§15/§16
    6. RESEARCH.md "Pitfall 6: §16 Deletion Dropping the Appendix C Quick Reference Card" — absorption audit
    7. AGENTS.md current §3 MUST-NOT list — confirm rule 6 is present there (DO NOT read wiki-local/ from cloud session)
  </read_first>
  <action>
    STEP A — Create docs/reference/scaling.md:
    ```
    # Scaling Boundaries

    > Informational reference for wiki scaling heuristics: when to upgrade from single index to split indexes, incremental lint, and DB-backed metadata. Not normative — these are provisional signals to watch.
    ```
    Then verbatim copy §14 content (all of "## 14. Scaling Boundaries" minus the header itself): the "Important:" disclaimer, the "These tiers are additive" sentence, and all four tier subsections (Tier 1–4) with their full content.

    Add See Also footer:
    ```
    ## See Also

    - [AGENTS.md](../../AGENTS.md) — §14 stub (pointer to this file).
    - [docs/reference/index.md](index.md)
    ```

    STEP B — Create docs/reference/tooling.md:
    ```
    # Tooling and Integrations

    > Informational reference for tools the wiki is designed to work with. Not normative — the wiki functions as plain markdown files in a git repo regardless of tooling.
    ```
    Then verbatim copy §15 content (all of "## 15. Tooling and Integrations" minus the header): the blockquote disclaimer and all three subsections (Obsidian, Git, Optional Future Tools).

    Add See Also footer:
    ```
    ## See Also

    - [AGENTS.md](../../AGENTS.md) — §15 stub (pointer to this file).
    - [docs/reference/index.md](index.md)
    ```

    STEP C — Appendix C audit (BEFORE deleting §16):
    Read §16 Appendix C rules 1–10. For each rule, verify it is covered:
    - Rule 1 (read index.md first): covered by §3 Navigation Rule (stays resident)
    - Rule 2 (TL;DR before Detail): covered by §3 Navigation Rule
    - Rule 3 (wikilink form): covered by §3 MUST-NOT + §8 one-liner stub
    - Rule 4 (provenance for every claim): covered by §6 remnant requirement line
    - Rule 5 (one commit per operation): covered by §3 Commit granularity residue
    - Rule 6 (Privacy default wiki-local/): covered by schema/reference/privacy.md ("Privacy default:" section just created in Task 1) AND present in §3 MUST-NOT ("DO NOT read wiki-local/ from a cloud session")
    - Rules 7–10: ISO 8601 dates, snake_case, ops vocabulary, see §3 MUST-NOT — all covered
    All rules confirmed covered. §16 may be safely deleted.

    STEP D — Edit AGENTS.md (work bottom-up to avoid line-number drift):

    DELETION 1 — §16 DELETE:
    Find "## 16. Appendices and Examples" and delete this entire section through end of file.
    
    STUB 1 — §15 REPLACE:
    Find "## 15. Tooling and Integrations" and replace entire §15 content with:
    ```
    ## 15. Tooling and Integrations

    → See `docs/reference/tooling.md` for Obsidian, Git, and optional tooling notes.
    ```

    STUB 2 — §14 REPLACE:
    Find "## 14. Scaling Boundaries" and replace entire §14 content with:
    ```
    ## 14. Scaling Boundaries

    → See `docs/reference/scaling.md` for scaling tier heuristics.
    ```

    STUB 3 — §13 REPLACE:
    Find "## 13. Privacy Routing" and replace the existing 2-line pointer with the new stub:
    ```
    ## 13. Privacy Routing

    → See `schema/reference/privacy.md` for the agent-facing tier rules.
      For the full human-facing model: `docs/reference/privacy-model.md`.
    ```

    STUB 4 — §8 REPLACE:
    Find "## 8. Wikilink and Graph Conventions" and replace entire §8 with:
    ```
    ## 8. Wikilink and Graph Conventions

    Use `[[id|Title]]` for ALL intra-wiki links — see `schema/reference/wikilinks.md`.
    ```
    
    §3 Red Links stub: Find "### Red Links" in §3 and replace the paragraph with:
    ```
    ### Red Links

    → See `schema/reference/wikilinks.md` (red links section).
    ```

    STEP E — Sync and verify:
    Run: `bash bin/sync-claude.sh`
    Run: `bash bin/sync-claude.sh --check` — must exit 0
    Run: `bash bin/check-neutrality.sh` — must exit 0

    STEP F — Verify §16 is gone:
    `! grep -q "^## 16\. Appendices" AGENTS.md`

    STEP G — Commit all six files:
    Stage: AGENTS.md, CLAUDE.md, docs/reference/scaling.md, docs/reference/tooling.md, schema/reference/wikilinks.md, schema/reference/privacy.md
    (schema/reference/wikilinks.md and schema/reference/privacy.md were unstaged from Task 1 — stage them now)
    Commit: `schema(ref-05/06/07): extract §8 wikilinks + §13 privacy; §14/§15 to docs/; delete §16`
  </action>
  <verify>
    <automated>bash /home/yishai/Documents/compendium/bin/sync-claude.sh --check && test -f /home/yishai/Documents/compendium/docs/reference/scaling.md && test -f /home/yishai/Documents/compendium/docs/reference/tooling.md && ! grep -q "^## 16\. Appendices" /home/yishai/Documents/compendium/AGENTS.md && grep -q "schema/reference/wikilinks.md" /home/yishai/Documents/compendium/AGENTS.md && grep -q "schema/reference/privacy.md" /home/yishai/Documents/compendium/AGENTS.md</automated>
  </verify>
  <done>
    - docs/reference/scaling.md exists with §14 content (≥40 lines)
    - docs/reference/tooling.md exists with §15 content (≥20 lines)
    - AGENTS.md has NO "## 16. Appendices" section
    - AGENTS.md §8 stub contains "schema/reference/wikilinks.md"
    - AGENTS.md §13 stub contains "schema/reference/privacy.md"
    - AGENTS.md §14 stub contains "docs/reference/scaling.md"
    - AGENTS.md §15 stub contains "docs/reference/tooling.md"
    - `bash bin/sync-claude.sh --check` exits 0
    - `bash bin/check-neutrality.sh` exits 0
    - All six files committed in one commit
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema/reference/wikilinks.md → public | Must carry v1.1.1 truth verbatim; no private vault terms |
| schema/reference/privacy.md → public | Asymmetric model rules; abstract placeholders only |
| docs/reference/scaling.md, tooling.md → public | End-user informational; §14/§15 already neutral content |
| AGENTS.md ≡ CLAUDE.md | Byte-equality after every commit |
| §16 deletion | Appendix C rules absorbed before deletion (rule 6 verified in privacy.md) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03-01 | Information Disclosure | schema/reference/wikilinks.md | mitigate | `bash bin/check-neutrality.sh`; §8 uses only abstract examples |
| T-16-03-02 | Information Disclosure | schema/reference/privacy.md | mitigate | Privacy.md itself has no vault-specific content; neutrality check |
| T-16-03-03 | Tampering | REF-05 verbatim guard | mitigate | grep for "filename/path ONLY" in wikilinks.md before commit |
| T-16-03-04 | Tampering | §16 Appendix C rule 6 silent drop | mitigate | Audit each Appendix C rule against coverage map before deleting §16 (RESEARCH Pitfall 6) |
| T-16-03-05 | Tampering | AGENTS.md ≡ CLAUDE.md | mitigate | `bash bin/sync-claude.sh --check` before commit |
</threat_model>

<verification>
After plan 03 commit:
- `grep -q "filename/path ONLY" schema/reference/wikilinks.md` — exits 0 (REF-05 verbatim guard)
- `test -f docs/reference/scaling.md && test -f docs/reference/tooling.md` — both exist
- `! grep -q "^## 16\." AGENTS.md` — exits 0 (§16 deleted)
- `grep -q "schema/reference/wikilinks.md" AGENTS.md` — exits 0 (§8 stub present)
- `grep -q "schema/reference/privacy.md" AGENTS.md` — exits 0 (§13 stub present)
- `bash bin/sync-claude.sh --check` — exits 0
- `bash bin/check-neutrality.sh` — exits 0
</verification>

<success_criteria>
All four leaf files created: wikilinks.md (with verbatim v1.1.1 truth), privacy.md (asymmetric model), scaling.md (§14), tooling.md (§15). AGENTS.md §8/§13/§14/§15 replaced with stubs. §16 deleted after Appendix C rule audit. AGENTS.md ≡ CLAUDE.md byte-identical. Neutrality gate passes.
</success_criteria>

<output>
After completion, create `.planning/phases/16-reference-extraction/16-03-SUMMARY.md`
</output>
