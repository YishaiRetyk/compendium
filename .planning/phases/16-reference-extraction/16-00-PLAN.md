---
phase: 16-reference-extraction
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - bin/check-neutrality.sh
  - bin/check-privacy.sh
autonomous: true
requirements:
  - REF-01
  - REF-02
  - REF-03
  - REF-04
  - REF-05
  - REF-06
  - REF-07
  - REF-08
  - REF-09
  - REF-10

must_haves:
  truths:
    - "bin/check-neutrality.sh covers schema/ in PUBLIC_PATHS so extracted reference files cannot escape neutrality scanning"
    - "bin/check-privacy.sh covers schema in PUBLIC_PATHS so the structural wiki-local/ path guard is symmetric with the release ALLOWLIST (both reviewers: cheap hygiene fix, adopt regardless of severity framing)"
    - "schema/reference/ and schema/workflows/ directories are declared ready-to-receive (created on first file write in Wave 1)"
  artifacts:
    - path: "bin/check-neutrality.sh"
      provides: "PUBLIC_PATHS covers schema/"
      contains: "schema"
    - path: "bin/check-privacy.sh"
      provides: "PUBLIC_PATHS covers schema (structural path-guard symmetry)"
      contains: "schema"
  key_links:
    - from: "schema/reference/*.md"
      to: "bin/check-neutrality.sh"
      via: "PUBLIC_PATHS array scan"
      pattern: "schema.*check-neutrality"
---

<objective>
Wave 0 gate: extend bin/check-neutrality.sh PUBLIC_PATHS to cover the new schema/ directory tree.

Purpose: The RESEARCH.md surfaced a critical gap — schema/ is in the release ALLOWLIST (bin/release.sh) but NOT in check-neutrality.sh PUBLIC_PATHS. Without this fix, extracted schema/reference/*.md files escape the neutrality scanner, creating a real leak risk for private vault terms entering template-public files. This must be fixed BEFORE any extraction commit.

Output: One-line edit to bin/check-neutrality.sh adding 'schema' to PUBLIC_PATHS array at line 94, plus a parallel one-line edit to bin/check-privacy.sh PUBLIC_PATHS (line 76) adding 'schema' for structural-path-guard symmetry. Both verified by grep.

REVIEW NOTE (cross-AI, addressed in this re-plan): Codex rated the check-privacy.sh schema/ gap HIGH; Claude rated it LOW-MEDIUM hygiene. Both reviewers agreed the one-line fix is worth doing regardless of severity framing because it removes the asymmetry between bin/release.sh (which allowlists `schema`) and the structural path guard (which omitted it). This plan now closes BOTH the neutrality gap (Task 1) and the privacy-path-guard gap (Task 2). Verified: no test in tests/ hardcodes the exact PUBLIC_PATHS array content, and tests/phase-15/test_check_privacy_rekey.sh only exercises behavior (wiki-local path present/absent), so adding `schema` is regression-safe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/16-reference-extraction/16-RESEARCH.md
@.planning/phases/16-reference-extraction/16-CONTEXT.md
@.planning/milestones/v1.2-MILESTONE-BRIEF.md

<interfaces>
<!-- From bin/check-neutrality.sh (direct inspection — RESEARCH.md confirmed) -->
<!-- Line 94 (the only line to edit): -->
```bash
PUBLIC_PATHS=(AGENTS.md CLAUDE.md README.md PRIVACY.md docs .github wiki-cloud bin)
```
<!-- After edit: -->
```bash
PUBLIC_PATHS=(AGENTS.md CLAUDE.md README.md PRIVACY.md docs .github wiki-cloud bin schema)
```
<!-- bin/release.sh ALLOWLIST (for reference — schema is already here): -->
<!-- schema is in release allowlist, which is why the neutrality gap matters -->
<!-- bin/check-privacy.sh PUBLIC_PATHS (line 76 — the second line to edit): -->
```bash
PUBLIC_PATHS=(examples docs AGENTS.md CLAUDE.md README.md PRIVACY.md .github)
```
<!-- After edit (add `schema`): -->
```bash
PUBLIC_PATHS=(examples docs AGENTS.md CLAUDE.md README.md PRIVACY.md .github schema)
```
<!-- Also update the help-text "Scope: PUBLIC_PATHS scanned:" line (~line 39) to mention schema/ -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `schema` to PUBLIC_PATHS in bin/check-neutrality.sh</name>
  <files>bin/check-neutrality.sh</files>
  <read_first>
    1. bin/check-neutrality.sh lines 89–110 — confirm current PUBLIC_PATHS at line 94 before editing
    2. RESEARCH.md "Critical Gap — neutrality gate does not currently cover schema/" section — exact fix specification
    3. PATTERNS.md "bin/check-neutrality.sh (one-line PUBLIC_PATHS extension)" section — exact before/after
  </read_first>
  <action>
    Read bin/check-neutrality.sh to confirm line 94 reads:
    `PUBLIC_PATHS=(AGENTS.md CLAUDE.md README.md PRIVACY.md docs .github wiki-cloud bin)`

    Edit that line to add `schema` as the last element:
    `PUBLIC_PATHS=(AGENTS.md CLAUDE.md README.md PRIVACY.md docs .github wiki-cloud bin schema)`

    This is a one-character-word addition. Do NOT change anything else on this line or elsewhere in the file. The comment on line 93 ("Public control-plane paths scanned (D-06). examples/ is explicitly excluded.") stays unchanged.

    After the edit, commit: `schema: add schema/ to check-neutrality.sh PUBLIC_PATHS (Wave 0 neutrality gate)`

    This is the Wave 0 prerequisite for all extraction commits in Plans 01–04. Without it, extracted schema/reference/*.md files escape the CI neutrality scanner even though schema/ ships publicly via bin/release.sh ALLOWLIST.
  </action>
  <verify>
    <automated>grep -n 'schema' /home/yishai/Documents/compendium/bin/check-neutrality.sh | grep 'PUBLIC_PATHS'</automated>
  </verify>
  <done>Line 94 of bin/check-neutrality.sh contains `schema` in the PUBLIC_PATHS array. `bash bin/check-neutrality.sh` exits 0 on the current repo. The pre-commit hook does not fire on this single-file commit (no AGENTS.md/CLAUDE.md change).</done>
</task>

<task type="auto">
  <name>Task 2: Add `schema` to PUBLIC_PATHS in bin/check-privacy.sh (structural path-guard symmetry)</name>
  <files>bin/check-privacy.sh</files>
  <read_first>
    1. bin/check-privacy.sh lines 74–81 — confirm current PUBLIC_PATHS at line 76 before editing
    2. bin/check-privacy.sh lines 36–45 — the help-text "Scope: PUBLIC_PATHS scanned:" line to keep in sync
    3. RESEARCH.md "Critical Gap" / cross-AI review consensus — both reviewers agreed to add `schema` regardless of severity
  </read_first>
  <action>
    Read bin/check-privacy.sh to confirm line 76 reads:
    `PUBLIC_PATHS=(examples docs AGENTS.md CLAUDE.md README.md PRIVACY.md .github)`

    Edit that line to add `schema` as the last element:
    `PUBLIC_PATHS=(examples docs AGENTS.md CLAUDE.md README.md PRIVACY.md .github schema)`

    Also update the help-text line (~line 39) so the documented scope stays in sync. Change:
    `  PUBLIC_PATHS scanned: examples/, docs/, AGENTS.md, CLAUDE.md, README.md, PRIVACY.md, .github/`
    to:
    `  PUBLIC_PATHS scanned: examples/, docs/, AGENTS.md, CLAUDE.md, README.md, PRIVACY.md, .github/, schema/`

    Do NOT change anything else. This makes the structural wiki-local/ path guard symmetric with bin/release.sh's `schema` ALLOWLIST entry — a file like `schema/wiki-local/...` (or any copied local-only material under schema/) is now caught by the path guard, not just by the content neutrality scanner.

    Commit (combine with Task 1 — single Wave 0 gate-arming commit):
    `schema: arm check-neutrality.sh + check-privacy.sh PUBLIC_PATHS over schema/ (Wave 0 gates)`

    Both edits are tool-only (no AGENTS.md/CLAUDE.md change) so the sync pre-commit hook does not fire.
  </action>
  <verify>
    <automated>grep -nE 'PUBLIC_PATHS=\(.*schema' /home/yishai/Documents/compendium/bin/check-privacy.sh && bash /home/yishai/Documents/compendium/bin/check-privacy.sh && bash /home/yishai/Documents/compendium/tests/phase-15/test_check_privacy_rekey.sh</automated>
  </verify>
  <done>Line 76 of bin/check-privacy.sh contains `schema` in PUBLIC_PATHS, and the help-text scope line is in sync. `bash bin/check-privacy.sh` exits 0 on the current repo. `bash tests/phase-15/test_check_privacy_rekey.sh` still passes (no PUBLIC_PATHS content assertion). Tasks 1 and 2 land in one Wave 0 commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema/reference/*.md → public release | Extracted files ship via release.sh ALLOWLIST; must not contain private vault terms (content) AND must not be a copied wiki-local/ path (structure) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-00-01 | Information Disclosure | bin/check-neutrality.sh PUBLIC_PATHS | mitigate | Task 1: add `schema` to PUBLIC_PATHS so the content neutrality scanner covers all extracted reference files from Wave 1 onward |
| T-16-00-02 | Information Disclosure | bin/check-privacy.sh PUBLIC_PATHS | mitigate | Task 2: add `schema` to PUBLIC_PATHS so the structural wiki-local/ path guard is symmetric with the release ALLOWLIST (closes the Codex-HIGH / Claude-hygiene asymmetry) |
</threat_model>

<verification>
After Wave 0 commit:
- `grep 'schema' bin/check-neutrality.sh` returns the PUBLIC_PATHS line with schema present
- `grep -E 'PUBLIC_PATHS=\(.*schema' bin/check-privacy.sh` returns the PUBLIC_PATHS line with schema present
- `bash bin/check-neutrality.sh` exits 0 (no denylist violations in current tree)
- `bash bin/check-privacy.sh` exits 0 (no wiki-local/ path under public surfaces)
- `bash tests/phase-15/test_check_privacy_rekey.sh` still passes
- `git log --oneline -1` shows the single Wave 0 gate-arming commit
</verification>

<success_criteria>
bin/check-neutrality.sh line 94 AND bin/check-privacy.sh line 76 each contain `schema` in PUBLIC_PATHS. Both the content neutrality gate and the structural privacy path guard are now armed to cover schema/reference/*.md and schema/workflows/lint.md files as they are created in Waves 1–2. The release-ALLOWLIST ↔ path-guard asymmetry is closed.
</success_criteria>

<output>
After completion, create `.planning/phases/16-reference-extraction/16-00-SUMMARY.md`
</output>
