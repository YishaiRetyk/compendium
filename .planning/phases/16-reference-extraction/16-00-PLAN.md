---
phase: 16-reference-extraction
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - bin/check-neutrality.sh
  - bin/check-privacy.sh
  - bin/lint.sh
  - wiki-cloud/decisions/dr-2026-06-04-privacy-asymmetric-two-dir.md
  - wiki-cloud/maintenance/lint-report.md
  - wiki-cloud/log.md
autonomous: true
requirements:
  - REF-01
  - REF-02
  - REF-03
  - REF-04
  - REF-05
  - REF-06
  - REF-07
  - REF-08
  - REF-09
  - REF-10

must_haves:
  truths:
    - "bin/check-neutrality.sh exits 0 on the current committed tree BEFORE any extraction commit gates on it — the three pre-existing wiki-cloud/ content hits (privacy DR, lint-report.md, log.md) are neutralized via the sanctioned neutrality_exempt frontmatter precedent (CYCLE-3 NEW HIGH #1)"
    - "bin/check-neutrality.sh covers schema/ in PUBLIC_PATHS so extracted reference files cannot escape neutrality scanning"
    - "bin/check-neutrality.sh scans file BASENAMES (not just content) so a denylisted term in a filename cannot leak through the release ALLOWLIST unscanned; the one legitimately-named historical DR (dr-2026-04-15-kahneman-to-examples.md) is exempted via an explicit narrow PATH_NAME_EXEMPT entry (CYCLE-3 NEW HIGH #2)"
    - "bin/lint.sh emits neutrality_exempt: true in the hardcoded lint-report.md frontmatter template so the exemption is DURABLE across lint regeneration (16-04 Task 2 runs bin/lint.sh --ci, which would otherwise re-wipe a hand-added exemption — root cause of the recurring 12.1 regression)"
    - "bin/check-privacy.sh covers schema in PUBLIC_PATHS so the structural wiki-local/ path guard is symmetric with the release ALLOWLIST (both reviewers: cheap hygiene fix, adopt regardless of severity framing)"
    - "schema/reference/ and schema/workflows/ directories are declared ready-to-receive (created on first file write in Wave 1)"
  artifacts:
    - path: "bin/check-neutrality.sh"
      provides: "PUBLIC_PATHS covers schema/ + basename path-name scanning with explicit PATH_NAME_EXEMPT"
      contains: "schema"
    - path: "bin/check-privacy.sh"
      provides: "PUBLIC_PATHS covers schema (structural path-guard symmetry)"
      contains: "schema"
    - path: "wiki-cloud/decisions/dr-2026-06-04-privacy-asymmetric-two-dir.md"
      provides: "neutrality_exempt frontmatter (legitimately names denylisted DR ids in affected_pages)"
      contains: "neutrality_exempt: true"
    - path: "bin/lint.sh"
      provides: "lint-report.md regeneration template carries neutrality_exempt: true (durable across --ci runs)"
      contains: "neutrality_exempt: true"
  key_links:
    - from: "schema/reference/*.md"
      to: "bin/check-neutrality.sh"
      via: "PUBLIC_PATHS array scan"
      pattern: "schema.*check-neutrality"
---

<objective>
Wave 0 gate-arming + neutrality pre-clear: make `bash bin/check-neutrality.sh` exit 0 on the current committed tree, extend its scanning to cover the new schema/ directory tree AND file basenames, and keep the lint-report exemption durable across regeneration. This is the prerequisite for every neutrality-gated commit in Plans 01–04.

Purpose (two intertwined goals):

(1) SCANNING-COVERAGE GAP (original Phase 16 finding): schema/ is in the release ALLOWLIST (bin/release.sh) but was NOT in check-neutrality.sh PUBLIC_PATHS. Without Task 1, extracted schema/reference/*.md files escape the content neutrality scanner. Task 2 adds the parallel check-privacy.sh coverage for structural-path-guard symmetry.

(2) GATE-IS-ALREADY-RED (CYCLE-3 cross-AI review, both reviewers, repo-verified): `bash bin/check-neutrality.sh` currently exits 2 because three COMMITTED wiki-cloud/ files carry denylisted terms (`kahneman` / `personal-decision-journal`) outside the sanctioned `examples/kahneman/` path:
  - `wiki-cloud/decisions/dr-2026-06-04-privacy-asymmetric-two-dir.md:30,92` — legitimately names denylisted DR ids in affected_pages + body
  - `wiki-cloud/maintenance/lint-report.md:86,87` — drift-detection findings list raw source paths
  - `wiki-cloud/log.md:344,345` — an append-only lint narration entry
Since 16-00 Task 1 only ADDS `schema` to PUBLIC_PATHS, it does NOT clear these pre-existing wiki-cloud/ hits. Every downstream `check-neutrality.sh exits 0` assertion (16-01..16-04) would go red on the FIRST gated wave for reasons unrelated to the extraction. Tasks 3–5 of THIS plan pre-clear them so the gate is genuinely green before any extraction commit — the reviewers' preferred remedy ("make the gate green first") over weakening the assertions to diff-based.

(3) FILENAME-LEVEL LEAK (CYCLE-3 NEW HIGH #2, Codex-led, Claude-confirmed): `check-neutrality.sh` scans file CONTENT, not path NAMES. `wiki-cloud/decisions/dr-2026-04-15-kahneman-to-examples.md` ships via the `wiki-cloud/decisions` release ALLOWLIST with a denylisted term in its filename → leaks publicly unscanned. Renaming that DR id is rejected as disproportionate: the slug is the canonical `id` of a shipped, deeply-referenced decision record (wikilink target in wiki-cloud/index.md, listed in 3 other DRs' bodies/affected_pages, referenced by the Phase-14 graph-link resolver) — a rename cascades far beyond Phase 16 scope. Instead Task 6 adds BASENAME scanning to check-neutrality.sh with an explicit narrow PATH_NAME_EXEMPT entry for exactly that one historical filename. This is the reviewers' "add path-name scanning with an explicit policy" option, and it STRENGTHENS protection for the NEW schema/reference/*.md files (their basenames are now scanned too) while making any FUTURE denylisted filename hard-fail.

Output:
  - Task 1: add `schema` to bin/check-neutrality.sh PUBLIC_PATHS (line 94).
  - Task 2: add `schema` to bin/check-privacy.sh PUBLIC_PATHS (line 76) + help-text sync.
  - Task 3: add `neutrality_exempt: true` frontmatter to wiki-cloud/decisions/dr-2026-06-04-privacy-asymmetric-two-dir.md (sanctioned precedent — wiki-cloud/index.md:17, dr-2026-04-15-kahneman-to-examples.md, dr-2026-04-16-progressive-disclosure-extraction.md:27 already use it).
  - Task 4: make the lint-report exemption DURABLE — add `neutrality_exempt: true` to the hardcoded report-frontmatter template in bin/lint.sh (~line 2523, after `knowledge_domain: ""`) AND to the current wiki-cloud/maintenance/lint-report.md file. (Root cause: lint.sh regenerates the report from a hardcoded template that preserves only `created_at`; a hand-added exemption is wiped on the next run — which is exactly what regressed the Phase-12.1 fix, and exactly what 16-04 Task 2's `bin/lint.sh --ci` would re-trigger.)
  - Task 5: add `neutrality_exempt: true` frontmatter to wiki-cloud/log.md (frontmatter add only — NOT a log-entry rewrite, which AGENTS.md §12 forbids; lint.sh only APPENDS to log.md and never rewrites its frontmatter, so the exemption is durable).
  - Task 6: add basename path-name scanning to bin/check-neutrality.sh with a narrow PATH_NAME_EXEMPT = {"wiki-cloud/decisions/dr-2026-04-15-kahneman-to-examples.md"}.

REVIEW NOTE (cross-AI): Codex rated the check-privacy.sh schema/ gap HIGH; Claude rated it LOW-MEDIUM hygiene. Both agreed the one-line fix is worth doing regardless of severity framing because it removes the asymmetry between bin/release.sh (allowlists `schema`) and the structural path guard. Verified: no test in tests/ hardcodes the exact PUBLIC_PATHS array content, and tests/phase-15/test_check_privacy_rekey.sh only exercises behavior (wiki-local path present/absent), so adding `schema` is regression-safe. The neutrality_exempt precedent is well established (07-05, 09.1, 12.1). Adding basename scanning is verified non-breaking: the only currently-shipping denylisted filename is the single exempted DR; all schema/reference/*.md leaf basenames are abstract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/16-reference-extraction/16-RESEARCH.md
@.planning/phases/16-reference-extraction/16-CONTEXT.md
@.planning/milestones/v1.2-MILESTONE-BRIEF.md

<interfaces>
<!-- From bin/check-neutrality.sh (direct inspection — RESEARCH.md confirmed) -->
<!-- Line 94 (the only line to edit): -->
```bash
PUBLIC_PATHS=(AGENTS.md CLAUDE.md README.md PRIVACY.md docs .github wiki-cloud bin)
```
<!-- After edit: -->
```bash
PUBLIC_PATHS=(AGENTS.md CLAUDE.md README.md PRIVACY.md docs .github wiki-cloud bin schema)
```
<!-- bin/release.sh ALLOWLIST (for reference — schema is already here): -->
<!-- schema is in release allowlist, which is why the neutrality gap matters -->
<!-- bin/check-privacy.sh PUBLIC_PATHS (line 76 — the second line to edit): -->
```bash
PUBLIC_PATHS=(examples docs AGENTS.md CLAUDE.md README.md PRIVACY.md .github)
```
<!-- After edit (add `schema`): -->
```bash
PUBLIC_PATHS=(examples docs AGENTS.md CLAUDE.md README.md PRIVACY.md .github schema)
```
<!-- Also update the help-text "Scope: PUBLIC_PATHS scanned:" line (~line 39) to mention schema/ -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `schema` to PUBLIC_PATHS in bin/check-neutrality.sh</name>
  <files>bin/check-neutrality.sh</files>
  <read_first>
    1. bin/check-neutrality.sh lines 89–110 — confirm current PUBLIC_PATHS at line 94 before editing
    2. RESEARCH.md "Critical Gap — neutrality gate does not currently cover schema/" section — exact fix specification
    3. PATTERNS.md "bin/check-neutrality.sh (one-line PUBLIC_PATHS extension)" section — exact before/after
  </read_first>
  <action>
    Read bin/check-neutrality.sh to confirm line 94 reads:
    `PUBLIC_PATHS=(AGENTS.md CLAUDE.md README.md PRIVACY.md docs .github wiki-cloud bin)`

    Edit that line to add `schema` as the last element:
    `PUBLIC_PATHS=(AGENTS.md CLAUDE.md README.md PRIVACY.md docs .github wiki-cloud bin schema)`

    This is a one-character-word addition. Do NOT change anything else on this line or elsewhere in the file. The comment on line 93 ("Public control-plane paths scanned (D-06). examples/ is explicitly excluded.") stays unchanged.

    After the edit, commit: `schema: add schema/ to check-neutrality.sh PUBLIC_PATHS (Wave 0 neutrality gate)`

    This is the Wave 0 prerequisite for all extraction commits in Plans 01–04. Without it, extracted schema/reference/*.md files escape the CI neutrality scanner even though schema/ ships publicly via bin/release.sh ALLOWLIST.
  </action>
  <verify>
    <automated>grep -n 'schema' /home/yishai/Documents/compendium/bin/check-neutrality.sh | grep 'PUBLIC_PATHS'</automated>
  </verify>
  <done>Line 94 of bin/check-neutrality.sh contains `schema` in the PUBLIC_PATHS array. NOTE: `bash bin/check-neutrality.sh` does NOT yet exit 0 after this task alone — adding `schema` to PUBLIC_PATHS does not clear the three pre-existing wiki-cloud/ content hits; those are cleared by Tasks 3–5 of this same Wave-0 plan. The exit-0 precondition is asserted in this plan's final `<verification>` AFTER all six tasks land. The pre-commit hook does not fire on the bin/ edits (no AGENTS.md/CLAUDE.md change).</done>
</task>

<task type="auto">
  <name>Task 2: Add `schema` to PUBLIC_PATHS in bin/check-privacy.sh (structural path-guard symmetry)</name>
  <files>bin/check-privacy.sh</files>
  <read_first>
    1. bin/check-privacy.sh lines 74–81 — confirm current PUBLIC_PATHS at line 76 before editing
    2. bin/check-privacy.sh lines 36–45 — the help-text "Scope: PUBLIC_PATHS scanned:" line to keep in sync
    3. RESEARCH.md "Critical Gap" / cross-AI review consensus — both reviewers agreed to add `schema` regardless of severity
  </read_first>
  <action>
    Read bin/check-privacy.sh to confirm line 76 reads:
    `PUBLIC_PATHS=(examples docs AGENTS.md CLAUDE.md README.md PRIVACY.md .github)`

    Edit that line to add `schema` as the last element:
    `PUBLIC_PATHS=(examples docs AGENTS.md CLAUDE.md README.md PRIVACY.md .github schema)`

    Also update the help-text line (~line 39) so the documented scope stays in sync. Change:
    `  PUBLIC_PATHS scanned: examples/, docs/, AGENTS.md, CLAUDE.md, README.md, PRIVACY.md, .github/`
    to:
    `  PUBLIC_PATHS scanned: examples/, docs/, AGENTS.md, CLAUDE.md, README.md, PRIVACY.md, .github/, schema/`

    Do NOT change anything else. This makes the structural wiki-local/ path guard symmetric with bin/release.sh's `schema` ALLOWLIST entry — a file like `schema/wiki-local/...` (or any copied local-only material under schema/) is now caught by the path guard, not just by the content neutrality scanner.

    Do NOT commit yet — Tasks 1–6 land as ONE Wave-0 commit (see Task 6). check-privacy.sh exits 0 already on the current tree, so this edit can be verified independently now.
  </action>
  <verify>
    <automated>grep -nE 'PUBLIC_PATHS=\(.*schema' /home/yishai/Documents/compendium/bin/check-privacy.sh && bash /home/yishai/Documents/compendium/bin/check-privacy.sh && bash /home/yishai/Documents/compendium/tests/phase-15/test_check_privacy_rekey.sh</automated>
  </verify>
  <done>Line 76 of bin/check-privacy.sh contains `schema` in PUBLIC_PATHS, and the help-text scope line is in sync. `bash bin/check-privacy.sh` exits 0 on the current repo. `bash tests/phase-15/test_check_privacy_rekey.sh` still passes (no PUBLIC_PATHS content assertion).</done>
</task>

<task type="auto">
  <name>Task 3: Add `neutrality_exempt: true` to the privacy DR (CYCLE-3 NEW HIGH #1, hit 1 of 3)</name>
  <files>wiki-cloud/decisions/dr-2026-06-04-privacy-asymmetric-two-dir.md</files>
  <read_first>
    1. wiki-cloud/decisions/dr-2026-06-04-privacy-asymmetric-two-dir.md frontmatter (lines 1–28) — confirm there is NO existing `neutrality_exempt` field and find the closing `---`
    2. wiki-cloud/decisions/dr-2026-04-16-progressive-disclosure-extraction.md line 27 — the closest precedent for the exemption + its rationale comment style
    3. wiki-cloud/index.md line 17 — second precedent (the DR-catalog exemption)
  </read_first>
  <action>
    The file `wiki-cloud/decisions/dr-2026-06-04-privacy-asymmetric-two-dir.md` legitimately names the denylisted DR id `dr-2026-04-15-kahneman-to-examples` at line 30 (inside its `affected_pages` YAML list) and line 92 (inside the `## Affected Pages` body, listing all existing decision-record pages by id). These are structurally fixed canonical slugs — they CANNOT be reworded without breaking the DR-id references. This is the identical legitimate-naming situation that already carries `neutrality_exempt: true` on three other in-repo files.

    Add `neutrality_exempt: true` to this file's YAML frontmatter, immediately after the `knowledge_domain` line (or, if absent, as the last frontmatter field before the closing `---`). Use the established rationale-comment style:

    `neutrality_exempt: true  # affected_pages + ## Affected Pages legitimately name the canonical DR id dr-2026-04-15-kahneman-to-examples (and other dr- ids); slugs are structurally fixed and cannot be reworded. Mirrors wiki-cloud/index.md:17 / dr-2026-04-16-progressive-disclosure-extraction.md:27 precedent.`

    Do NOT alter any body text or the affected_pages list — only add the one frontmatter line. This is a `wiki-cloud/decisions/` content file, NOT a template-public file, so adding a frontmatter field is appropriate (it is not a log entry, so §12 append-only does not apply).
  </action>
  <verify>
    <automated>grep -q '^neutrality_exempt: true' /home/yishai/Documents/compendium/wiki-cloud/decisions/dr-2026-06-04-privacy-asymmetric-two-dir.md && echo OK</automated>
  </verify>
  <done>wiki-cloud/decisions/dr-2026-06-04-privacy-asymmetric-two-dir.md frontmatter contains `neutrality_exempt: true`. The scanner now skips this file (its two `kahneman` hits no longer count). Body and affected_pages unchanged.</done>
</task>

<task type="auto">
  <name>Task 4: Make the lint-report exemption DURABLE in bin/lint.sh + current file (CYCLE-3 NEW HIGH #1, hit 2 of 3)</name>
  <files>bin/lint.sh, wiki-cloud/maintenance/lint-report.md</files>
  <read_first>
    1. bin/lint.sh lines 2469–2543 — the lint-report.md regeneration block. Confirm the hardcoded frontmatter template (the `report_content = f"""---` heredoc starting ~line 2504) preserves ONLY `created_at` from the existing file (lines 2471–2482) and has NO `neutrality_exempt` field. THIS IS THE ROOT CAUSE of the recurring regression: a hand-added exemption on lint-report.md is wiped on the next `bin/lint.sh` run.
    2. wiki-cloud/maintenance/lint-report.md frontmatter (lines 1–21) — confirm no `neutrality_exempt` field currently (it was added in Phase 12.1 but wiped by a later regeneration)
    3. wiki-cloud/maintenance/lint-report.md lines 86–87 — the two drift findings naming `kahneman` and `personal-decision-journal` raw source paths
  </read_first>
  <action>
    TWO edits, both required (one durable, one immediate):

    (a) DURABLE — edit the hardcoded report-frontmatter template in bin/lint.sh. In the `report_content` f-string heredoc (~line 2504–2524), add a `neutrality_exempt: true` line immediately after the `knowledge_domain: ""` line and before the closing `---`. The relevant template fragment becomes:
    ```
    has_contradictions: false
    knowledge_domain: ""
    neutrality_exempt: true
    ---
    ```
    This guarantees EVERY regeneration of lint-report.md (including 16-04 Task 2's `bash bin/lint.sh --ci`) carries the exemption. Do not change any other line of the heredoc or the surrounding write logic.

    (b) IMMEDIATE — edit the current wiki-cloud/maintenance/lint-report.md so the gate is green NOW (before lint.sh is next run). Add `neutrality_exempt: true` to its frontmatter immediately after `knowledge_domain: ""` (line 20) and before the closing `---` (line 21). This mirrors the template edit so the file is exempt the moment Task 6's commit lands, without requiring a lint run first.

    Rationale: lint-report.md legitimately names `kahneman` / `personal-decision-journal` because they are raw source directory paths surfaced by the drift detector (DRFT-01). The content is correct; the file just needs the sanctioned per-file exemption — made permanent so it cannot silently regress again.
  </action>
  <verify>
    <automated>grep -q 'neutrality_exempt: true' /home/yishai/Documents/compendium/bin/lint.sh && grep -q '^neutrality_exempt: true' /home/yishai/Documents/compendium/wiki-cloud/maintenance/lint-report.md && echo OK</automated>
  </verify>
  <done>bin/lint.sh's hardcoded lint-report frontmatter template emits `neutrality_exempt: true`, AND the current wiki-cloud/maintenance/lint-report.md frontmatter carries it. Re-running `bash bin/lint.sh` regenerates the report WITH the exemption intact (no regression). The scanner now skips lint-report.md.</done>
</task>

<task type="auto">
  <name>Task 5: Add `neutrality_exempt: true` to wiki-cloud/log.md (CYCLE-3 NEW HIGH #1, hit 3 of 3)</name>
  <files>wiki-cloud/log.md</files>
  <read_first>
    1. wiki-cloud/log.md lines 1–6 — confirm the frontmatter block (opening `---`, `id: log`, closing `---`) and that there is no existing `neutrality_exempt` field
    2. wiki-cloud/log.md lines 342–346 — the append-only lint narration entry that names `kahneman`
    3. AGENTS.md §12 (log.md rules) — confirm log entries are append-only and MUST NOT be rewritten (this is why we exempt via frontmatter rather than reword the entry)
  </read_first>
  <action>
    wiki-cloud/log.md line 344–345 names `kahneman` inside a historical, append-only lint narration entry ("ran `--fix` over wiki/ ... + examples/ (8 lint-visible kahneman pages updated)"). Per AGENTS.md §12, log entries are append-only and MUST NOT be rewritten — so rewording the narration is forbidden. The correct, schema-compliant fix is a frontmatter exemption (adding a frontmatter field is NOT a log-entry rewrite).

    Add `neutrality_exempt: true` to wiki-cloud/log.md's frontmatter block (after the last existing frontmatter field, before the closing `---`). Use the rationale comment:
    `neutrality_exempt: true  # Append-only activity log; a historical lint narration entry legitimately names a denylisted term and AGENTS.md §12 forbids rewriting log entries. Per-file exemption per the established neutrality_exempt precedent.`

    Durability note: lint.sh only APPENDS to log.md (see bin/lint.sh ~line 2547, which concatenates a new `## [date] lint` block) and NEVER rewrites the frontmatter, so this exemption persists across future lint runs. Do not touch any existing log entry text.
  </action>
  <verify>
    <automated>grep -q '^neutrality_exempt: true' /home/yishai/Documents/compendium/wiki-cloud/log.md && echo OK</automated>
  </verify>
  <done>wiki-cloud/log.md frontmatter contains `neutrality_exempt: true`. The scanner now skips log.md. No existing log entry was modified (§12 append-only honored).</done>
</task>

<task type="auto">
  <name>Task 6: Add basename path-name scanning to bin/check-neutrality.sh + land the Wave-0 commit (CYCLE-3 NEW HIGH #2)</name>
  <files>bin/check-neutrality.sh</files>
  <read_first>
    1. bin/check-neutrality.sh lines 119–230 — the Python scanning block: how it iterates PUBLIC_PATHS, walks files, reads `text`, applies SELF_REFERENTIAL_EXEMPT (line 214), the frontmatter `has_neutrality_exempt` skip (line 222), and the `SANCTIONED_PATH_RE` line-level strip (line 225). Find the exact point where each file's content is matched against `terms`.
    2. bin/check-neutrality.sh lines 161–169 — the existing SELF_REFERENTIAL_EXEMPT set and SANCTIONED_PATH_RE definition (model the new PATH_NAME_EXEMPT set in the same place/style).
    3. bin/release.sh lines 24–38 — confirm `wiki-cloud/decisions` is allowlisted (line 33) so the filename ships publicly.
  </read_first>
  <action>
    Add file-BASENAME scanning to bin/check-neutrality.sh so a denylisted term in a path/filename is caught, not just in content. Concretely:

    1. Near the existing exemption definitions (~lines 161–169), add a narrow explicit path-name exemption set:
    ```python
    # Path-name exemption: the ONLY legitimately denylisted-named file that ships.
    # Its slug is the canonical id of a deeply-referenced historical decision record
    # (wikilink target in wiki-cloud/index.md; named in 3 other DRs' affected_pages;
    # referenced by the Phase-14 graph-link resolver). Renaming it cascades far beyond
    # scope, so it is exempted by exact relative path. Any OTHER denylisted filename
    # (including future schema/reference/*.md leaves) MUST fail.
    PATH_NAME_EXEMPT = {
        "wiki-cloud/decisions/dr-2026-04-15-kahneman-to-examples.md",
    }
    ```

    2. In the per-file loop, BEFORE (or alongside) the content scan, match the file's relative path string against `terms`. Use the same denylist `terms` and the same `SANCTIONED_PATH_RE` strip that content scanning uses (so a path containing a sanctioned `examples/kahneman/...` segment is still allowed). Skip the path-name check when `rel_path in PATH_NAME_EXEMPT`. On a hit, emit a finding in the SAME format the content scanner uses but flagging the path itself — e.g. `print(f"{rel_path}:0: {term} (in filename)")` — and set the same non-zero exit (2). Place the path-name check so it runs for every scanned file under PUBLIC_PATHS, regardless of extension (filenames have no frontmatter, so the `neutrality_exempt` frontmatter skip does NOT cover them — PATH_NAME_EXEMPT is the only path-name escape hatch, by design).

    3. Keep behavior additive: do not change content-scanning logic, the SELF_REFERENTIAL_EXEMPT set, the frontmatter exemption, or the SANCTIONED_PATH_RE strip. The path-name scan is a NEW, independent check layered on top.

    Then verify the WHOLE Wave-0 result is green and land the single commit:

    ```
    bash bin/check-neutrality.sh ; echo "exit=$?"   # MUST print exit=0
    ```

    Commit all six tasks together as ONE Wave-0 commit (bin/ edits + the three wiki-cloud exemptions):
    `schema: arm + pre-clear neutrality gate for Phase 16 (PUBLIC_PATHS schema/, basename scanning, exempt privacy DR/lint-report/log) (Wave 0)`

    Pre-commit hook note: this commit touches no AGENTS.md/CLAUDE.md, so the sync hook does not fire. The pre-commit write-gate (`bin/lint.sh --strict --staged --category provenance`) only gates NEW status-A pages under wiki-cloud/{entities,concepts,overviews,comparisons}/ — none of the touched files are in those dirs (the DR is under decisions/, lint-report under maintenance/, log.md is excluded), so the gate does not block. If for any reason a future hook does fire, do NOT bypass; resolve the finding.
  </action>
  <verify>
    <automated>grep -q 'PATH_NAME_EXEMPT' /home/yishai/Documents/compendium/bin/check-neutrality.sh && bash /home/yishai/Documents/compendium/bin/check-neutrality.sh && echo "NEUTRALITY_GREEN"</automated>
  </verify>
  <done>bin/check-neutrality.sh scans file basenames against the denylist with a narrow PATH_NAME_EXEMPT for the one historical DR. `bash bin/check-neutrality.sh` exits 0 on the full current tree (all six Wave-0 edits applied). The filename leak is closed: any FUTURE denylisted filename under a public path fails the gate. All six tasks landed in one Wave-0 commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema/reference/*.md → public release | Extracted files ship via release.sh ALLOWLIST; must not contain private vault terms (content) AND must not be a copied wiki-local/ path (structure) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-00-01 | Information Disclosure | bin/check-neutrality.sh PUBLIC_PATHS | mitigate | Task 1: add `schema` to PUBLIC_PATHS so the content neutrality scanner covers all extracted reference files from Wave 1 onward |
| T-16-00-02 | Information Disclosure | bin/check-privacy.sh PUBLIC_PATHS | mitigate | Task 2: add `schema` to PUBLIC_PATHS so the structural wiki-local/ path guard is symmetric with the release ALLOWLIST (closes the Codex-HIGH / Claude-hygiene asymmetry) |
| T-16-00-03 | Information Disclosure | denylisted term in a SHIPPED FILENAME under wiki-cloud/decisions release ALLOWLIST | mitigate | Task 6: add basename path-name scanning to check-neutrality.sh with a narrow explicit PATH_NAME_EXEMPT for the one historical DR; any future denylisted filename hard-fails (CYCLE-3 NEW HIGH #2) |
| T-16-00-04 | Tampering / Regression | lint.sh regenerates lint-report.md, wiping a hand-added neutrality_exempt | mitigate | Task 4(a): bake `neutrality_exempt: true` into the hardcoded report template in lint.sh so the exemption survives every regeneration (root-cause fix for the recurring 12.1-style regression) |
</threat_model>

<verification>
After the single Wave 0 commit (all six tasks):
- `grep 'schema' bin/check-neutrality.sh` returns the PUBLIC_PATHS line with schema present
- `grep -E 'PUBLIC_PATHS=\(.*schema' bin/check-privacy.sh` returns the PUBLIC_PATHS line with schema present
- `grep -q 'PATH_NAME_EXEMPT' bin/check-neutrality.sh` — basename scanning + explicit exemption present (NEW HIGH #2 closed)
- `grep -q '^neutrality_exempt: true' wiki-cloud/decisions/dr-2026-06-04-privacy-asymmetric-two-dir.md` — privacy DR exempt (hit 1)
- `grep -q 'neutrality_exempt: true' bin/lint.sh` AND `grep -q '^neutrality_exempt: true' wiki-cloud/maintenance/lint-report.md` — lint-report exempt + DURABLE across regeneration (hit 2)
- `grep -q '^neutrality_exempt: true' wiki-cloud/log.md` — log.md exempt, no entry rewritten (hit 3)
- **`bash bin/check-neutrality.sh; echo "exit=$?"` prints `exit=0`** — THE load-bearing precondition for every downstream Phase-16 wave. This is the single assertion the cycle-3 review said was unachievable as previously planned; it is now genuinely true on the committed tree.
- `bash bin/check-privacy.sh` exits 0 (no wiki-local/ path under public surfaces)
- `bash tests/phase-15/test_check_privacy_rekey.sh` still passes
- REGENERATION-SAFETY CHECK: `bash bin/lint.sh >/dev/null 2>&1; bash bin/check-neutrality.sh; echo "post-lint exit=$?"` prints `exit=0` (proves the lint.sh template fix holds — 16-04 Task 2's `bin/lint.sh --ci` will not re-break the gate)
- `git log --oneline -1` shows the single Wave 0 gate-arming + pre-clear commit
</verification>

<success_criteria>
`bash bin/check-neutrality.sh` exits 0 on the committed tree (gate is GREEN before any extraction commit gates on it). bin/check-neutrality.sh line 94 AND bin/check-privacy.sh line 76 each contain `schema` in PUBLIC_PATHS; check-neutrality.sh additionally scans file basenames with a narrow PATH_NAME_EXEMPT. The three pre-existing wiki-cloud/ content hits are neutralized via the sanctioned `neutrality_exempt` precedent, and the lint-report exemption is durable across regeneration. The content neutrality gate, the basename/filename leak guard, and the structural privacy path guard are all armed to cover schema/reference/*.md and schema/workflows/lint.md as they are created in Waves 1–2. The release-ALLOWLIST ↔ path-guard asymmetry and the filename-leak are both closed.
</success_criteria>

<output>
After completion, create `.planning/phases/16-reference-extraction/16-00-SUMMARY.md`
</output>
