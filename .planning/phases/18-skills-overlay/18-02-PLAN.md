---
phase: 18-skills-overlay
plan: "02"
type: execute
wave: 2
depends_on:
  - "18-01"
files_modified:
  - wiki-cloud/decisions/dr-2026-06-08-skills-overlay.md
  - wiki-cloud/index.md
  - docs/reference/skills.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - SKILL-01
  - SKILL-02

must_haves:
  truths:
    - "Decision record dr-2026-06-08-skills-overlay.md exists in wiki-cloud/decisions/ with correct frontmatter"
    - "wiki-cloud/index.md Decisions section has the DR entry in wikilink format"
    - "docs/reference/skills.md explains the overlay, generated nature, and --check gate for adopters"
    - "ROADMAP.md Phase 18 plan list is updated from TBD to the three plans"
  artifacts:
    - path: "wiki-cloud/decisions/dr-2026-06-08-skills-overlay.md"
      provides: "Decision record: two-layer SOT model, model-invocation choice, D-07 rationale"
      contains: "dr-2026-06-08-skills-overlay"
    - path: "wiki-cloud/index.md"
      provides: "DR entry in Decisions section"
      contains: "dr-2026-06-08-skills-overlay"
    - path: "docs/reference/skills.md"
      provides: "Adopter-facing skills overlay documentation"
      contains: "gen-skills.sh"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 18 plan list updated (18-00, 18-01, 18-02)"
      contains: "18-00-PLAN.md"
  key_links:
    - from: "wiki-cloud/index.md"
      to: "wiki-cloud/decisions/dr-2026-06-08-skills-overlay.md"
      via: "[[dr-2026-06-08-skills-overlay|...]] wikilink in Decisions section"
      pattern: "dr-2026-06-08-skills-overlay"
    - from: "docs/reference/skills.md"
      to: "bin/gen-skills.sh"
      via: "prose reference to the generator script"
      pattern: "gen-skills\\.sh"
---

<objective>
Author the decision record, register it in the index, document the overlay for adopters, and update the ROADMAP.

Purpose: D-09 requires a decision record capturing the two-layer SOT model and model-invocation choice. D-11 requires an adopter-facing docs page. These are governance and documentation deliverables that complete Phase 18's coverage without modifying any tooling (Plan 01 owns all tooling).

Output: wiki-cloud/decisions/dr-2026-06-08-skills-overlay.md (D-09), wiki-cloud/index.md entry, docs/reference/skills.md (D-11), ROADMAP.md plan list updated.
</objective>

<execution_context>
@/home/yishai/.claude/get-shit-done/workflows/execute-plan.md
@/home/yishai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-skills-overlay/18-SPEC.md
@.planning/phases/18-skills-overlay/18-CONTEXT.md
@.planning/phases/18-skills-overlay/18-PATTERNS.md
@.planning/phases/18-skills-overlay/18-01-SUMMARY.md

<interfaces>
<!-- Key contracts for documents created in this plan. Read before implementing. -->

From wiki-cloud/decisions/dr-2026-06-05-workflow-extraction.md (frontmatter template — copy and substitute):
```yaml
---
id: dr-2026-06-05-workflow-extraction
title: "Workflow Extraction: AGENTS.md §9–§12 to schema/workflows/ + routing guard + inclusion tripwire"
type: decision
status: active
summary: "Extracts §9 (operations vocabulary)..."
created_at: 2026-06-07
updated_at: 2026-06-07
sources: []
epistemic_status: sourced
tags:
  - meta
  - schema
domains:
  - wiki-infrastructure
supersedes: null
superseded_by: null
aliases:
  - dr-2026-06-05-workflow-extraction
has_contradictions: false
knowledge_domain: software
trigger_type: schema-update
affected_pages: []
---
```

For Phase 18 DR, substitute (leave all other fields identical):
- id: dr-2026-06-08-skills-overlay
- title: "Skills Overlay: Four Generated SKILL.md Routers + bin/gen-skills.sh Drift Gate"
- summary: one sentence describing the two-layer SOT model and --check gate
- created_at / updated_at: 2026-06-08
- aliases: [dr-2026-06-08-skills-overlay]
- affected_pages: [index]  (index.md gets the new DR entry)

From wiki-cloud/index.md Decisions section (existing entry format to copy):
```markdown
- [[dr-2026-06-05-workflow-extraction|Workflow Extraction: AGENTS.md §9–§12 to schema/workflows/ + routing guard + inclusion tripwire]] — Extracts workflow procedures into standalone files; abolishes cross-file §N refs via the routing lint category; adds inclusion-audit baseline header. (sourced, 2026-06-07)
```

New entry format for Phase 18:
```markdown
- [[dr-2026-06-08-skills-overlay|Skills Overlay: Four Generated SKILL.md Routers + bin/gen-skills.sh Drift Gate]] — Introduces four thin SKILL.md pointer routers generated by bin/gen-skills.sh with a --check drift gate; records the two-layer SOT model and model-invocation choice. (sourced, 2026-06-08)
```

From docs/reference/release.md (structure pattern for docs/reference/skills.md):
- Plain markdown, no YAML frontmatter
- H1 title
- Short one-paragraph intro
- Sections with ## H2 headers
- Imperative style for "how to" sections
- Cross-references to relevant bin/ scripts and schema/ files

CRITICAL NEUTRALITY RULE (AGENTS.md §3, §3 last bullet): docs/reference/skills.md is a template-public file. Use abstract placeholders (<op>, <operation-name>, etc.) for any illustrative examples. Do NOT use real private vault terms. The only real terms allowed are the four ops (ingest/query/lint/reflect) since these are structural parts of the schema, not private content.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author decision record and register in wiki-cloud/index.md</name>
  <files>
    wiki-cloud/decisions/dr-2026-06-08-skills-overlay.md,
    wiki-cloud/index.md
  </files>
  <read_first>
    - wiki-cloud/decisions/dr-2026-06-05-workflow-extraction.md (frontmatter template to clone; section structure to mirror)
    - wiki-cloud/index.md lines 82-95 (Decisions section — append new entry at end)
    - .planning/phases/18-skills-overlay/18-SPEC.md (Source-of-Truth Model section — content for the DR body)
    - .planning/phases/18-skills-overlay/18-CONTEXT.md (D-01 through D-11, D-07 rationale for structural assertions)
    - schema/workflows/ingest.md, schema/workflows/query.md, schema/workflows/lint.md, schema/workflows/reflect.md (behavioral SOT files referenced in DR)
  </read_first>
  <action>
**A) Create wiki-cloud/decisions/dr-2026-06-08-skills-overlay.md**

Full file content:

```markdown
---
id: dr-2026-06-08-skills-overlay
title: "Skills Overlay: Four Generated SKILL.md Routers + bin/gen-skills.sh Drift Gate"
type: decision
status: active
summary: "Introduces four thin .claude/skills/{op}/SKILL.md pointer routers generated deterministically by bin/gen-skills.sh from a single body template; guards them with a --check regenerate-diff + structural-assertion gate; records the two-layer SOT model (behavioral SOT = schema/workflows/*.md; artifact SOT = the generator template) and the model-invocation-enabled choice."
created_at: 2026-06-08
updated_at: 2026-06-08
sources: []
epistemic_status: sourced
tags:
  - meta
  - schema
domains:
  - wiki-infrastructure
supersedes: null
superseded_by: null
aliases:
  - dr-2026-06-08-skills-overlay
has_contradictions: false
knowledge_domain: software
trigger_type: schema-update
affected_pages:
  - index
---

# Skills Overlay: Four Generated SKILL.md Routers + bin/gen-skills.sh Drift Gate

## TL;DR

Phase 18 adds the optional Claude Code accelerator layer that the v1.2 design deferred to last: four thin `.claude/skills/{op}/SKILL.md` pointer routers for the core operations (ingest, query, lint, reflect). Each skill body is a ≤3-line invocation pointer to its corresponding `schema/workflows/{op}.md` file and encodes zero authoritative content. The files are generated deterministically by `bin/gen-skills.sh` and guarded by a `--check` regenerate-diff + structural-assertion gate wired into both the pre-commit hook and CI. Markdown remains the sole source of truth for behavior.

## Decision

Add a `.claude/skills/` overlay of exactly four skill routers — ingest, query, lint, reflect — generated from a single parameterized body template by `bin/gen-skills.sh`, with:

1. **Four SKILL.md files** at `.claude/skills/{ingest,query,lint,reflect}/SKILL.md`, each containing YAML frontmatter (`name`, `description`) and a ≤3-line pointer body: `"You have been invoked to {op}. Read \`schema/workflows/{op}.md\` and follow it verbatim."`
2. **Generator as artifact SOT** (`bin/gen-skills.sh`): deterministic, idempotent, zero external deps; a structural twin of `bin/sync-claude.sh`.
3. **`--check` gate**: regenerates to a temp dir, diffs with `cmp -s`, plus independent structural assertions (≤3 body lines, directory purity, no first-person pronoun, no `disable-model-invocation: true`). Exits 0 on match, 1 on any drift.
4. **Gate wiring**: pre-commit auto-fix + restage between sync-claude and lint; CI hard-fail (no auto-fix) as a required status check.
5. **Model invocation enabled**: `description` carries the what+when discovery signal; no `disable-model-invocation: true` key set.

## Why

**Design lineage:** Two independent advisors in the v1.2 design phase rejected a skills-first extraction as "vendor coupling disguised as progressive disclosure." The adopted framing: markdown is the primary substrate; skills come last as thin wrappers / runtime accelerators, never the source of truth. Phase 18 is the last step in the v1.2 schema-architecture trajectory (after Phases 15–17 established the markdown-authoritative schema tree).

**Two-layer Source-of-Truth Model:**

| Layer | Source of truth | Status of the SKILL.md file |
|-------|-----------------|-------------------------------|
| **Behavior** (what the operation does) | `schema/workflows/{op}.md` | body is a pointer; encodes zero behavior |
| **Artifact text** (the bytes of the skill file) | `bin/gen-skills.sh` template + description data | generated copy; `--check` guarded |

This mirrors the existing `AGENTS.md → CLAUDE.md` derived-artifact relationship: hand-edits to CLAUDE.md are wrong; hand-edits to SKILL.md files are equally wrong and are caught by `--check`.

**Why model invocation is enabled:** The `description` field carries the what+when signal at L1 (~100 tokens/skill, always loaded). Requiring explicit invocation (`disable-model-invocation: true`) reduces ergonomics without improving safety — the body is a pure pointer and the structural assertions enforce inertness mechanically. Description economy is preserved by the ≤1024-char constraint and the structural assertion that no first-person pronoun appears.

**Why four skills and not more:** Minimalism precedent (Phase 8 WZRD-07/D-02). The four core operations (ingest, query, lint, reflect) map 1:1 to the four schema/workflows/{op}.md files. Audit, brownfield, release, and structured-operations are operator/maintenance workflows, not user-facing operations.

## Why NOT the Alternatives

**Alternative A: Skip the overlay entirely.** The skill routers add ~400 tokens of permanent ambient cost. Rejected: the ergonomic benefit (model auto-invocation + `/ingest`-style dispatch) was committed as Phase C of the v1.2 design and is small in scope.

**Alternative B: Hand-author each SKILL.md file.** Rejected: hand-authored files diverge over time; a fattened body would violate SKILL-02 without a mechanical gate. The generator pattern is the established repo idiom (see `sync-claude.sh`).

**Alternative C: Use a generic AI skill-creator skill to author the skill files.** Rejected explicitly in the SPEC: a skill-creator would produce non-uniform, non-thin bodies with authoritative content, directly violating SKILL-01/SKILL-02.

**Alternative D: Extra skills for audit/brownfield/release.** Rejected: minimalism precedent (WZRD-07/D-02); those are operator workflows not warranting always-loaded ambient cost.

## Consequences

- Four new committed files under `.claude/skills/` (tracked via `.gitignore` negation `!.claude/skills/` + `!.claude/skills/**`).
- One new `bin/gen-skills.sh` script (~100 lines, pure bash, zero new deps).
- `.githooks/pre-commit` gains a skills block between sync-claude and lint.
- CI `lint.yml` gains a `skills-check` required status check job.
- `bin/check-neutrality.sh` `PUBLIC_PATHS` extended with `.claude/skills` (D-10).
- No change to `AGENTS.md`/`CLAUDE.md` resident core (D-08 — skills are pointers, not authoritative content).
- No change to any `schema/workflows/` file — behavioral SOT is read-only from skills' perspective.

## Affected Pages

- [[index|Index]] — Decisions section: new DR entry added.

## Sources

- `18-SPEC.md` — locked requirements (SKILL-01, SKILL-02) and two-layer SOT model
- `18-CONTEXT.md` — implementation decisions D-01 through D-11
- Design lineage: `.planning/phases/999.4-v1-2-schema-architecture-progressive-disclosure-refactor/CONTEXT-NOTES.md` §"Phase C — Claude Skills Overlay"
```

**B) Update wiki-cloud/index.md**

Read wiki-cloud/index.md. Two changes:
1. In the YAML frontmatter, update `updated_at` from `2026-05-06T22:30:00` to `2026-06-08`.
2. Append this line at the END of the `## Decisions` section (after the existing last DR entry):
```markdown
- [[dr-2026-06-08-skills-overlay|Skills Overlay: Four Generated SKILL.md Routers + bin/gen-skills.sh Drift Gate]] — Introduces four thin SKILL.md pointer routers generated by bin/gen-skills.sh with a --check drift gate; records the two-layer SOT model and model-invocation choice. (sourced, 2026-06-08)
```

The `## Decisions` section is currently the last section in index.md; the new entry must follow the existing last DR entry (`dr-2026-06-05-workflow-extraction`).
  </action>
  <verify>
    <automated>grep -c 'dr-2026-06-08-skills-overlay' /home/yishai/Documents/compendium/wiki-cloud/index.md</automated>
  </verify>
  <acceptance_criteria>
    - `wiki-cloud/decisions/dr-2026-06-08-skills-overlay.md` exists
    - `grep '^id: dr-2026-06-08-skills-overlay' wiki-cloud/decisions/dr-2026-06-08-skills-overlay.md` matches
    - `grep '^type: decision' wiki-cloud/decisions/dr-2026-06-08-skills-overlay.md` matches
    - `grep '^trigger_type: schema-update' wiki-cloud/decisions/dr-2026-06-08-skills-overlay.md` matches
    - `grep '^created_at: 2026-06-08' wiki-cloud/decisions/dr-2026-06-08-skills-overlay.md` matches
    - `grep 'two-layer' wiki-cloud/decisions/dr-2026-06-08-skills-overlay.md` matches (SOT model documented)
    - `grep 'dr-2026-06-08-skills-overlay' wiki-cloud/index.md` returns exactly 1 match (the new Decisions entry)
    - `grep 'updated_at: 2026-06-08' wiki-cloud/index.md` matches (frontmatter updated)
    - The DR body is NOT empty (must contain TL;DR, Decision, Why, Alternatives, Consequences sections)
    - No private vault terms in the DR (run `bash bin/check-neutrality.sh` exits 0)
    - Wikilink format correct: `[[dr-2026-06-08-skills-overlay|...]]` with pipe separator
  </acceptance_criteria>
  <done>DR exists at correct path with valid frontmatter and body; index.md Decisions section contains the wikilink entry; check-neutrality exits 0</done>
</task>

<task type="auto">
  <name>Task 2: Create docs/reference/skills.md and update ROADMAP.md</name>
  <files>
    docs/reference/skills.md,
    .planning/ROADMAP.md
  </files>
  <read_first>
    - docs/reference/release.md (structure template: plain markdown, no frontmatter, H1 + sections, imperative style)
    - .planning/ROADMAP.md Phase 18 section (update plan list from TBD to the three plans)
    - .planning/phases/18-skills-overlay/18-CONTEXT.md (D-11 content spec for skills.md)
    - .planning/phases/18-skills-overlay/18-PATTERNS.md (docs/reference/skills.md section — content outline)
    - bin/gen-skills.sh (read to verify exact commands before documenting them)
  </read_first>
  <action>
**A) Create docs/reference/skills.md (D-11)**

Plain markdown, no YAML frontmatter. Follow the structure template from docs/reference/release.md (H1 + sections, no frontmatter). Use abstract placeholders in examples where appropriate — this is a template-public file (AGENTS.md §3 neutrality rule).

Full content:

```markdown
# Claude Code Skills Overlay

This document describes the four Claude Code skill routers shipped with the wiki compiler
schema. Skill files are **generated**, not hand-authored — do not edit them directly.

The four skills provide ergonomic access to the core wiki operations (ingest, query, lint,
reflect) in Claude Code. When invoked, each skill dispatches to its corresponding workflow
file (`schema/workflows/{op}.md`), which remains the sole authoritative source for behavior.

## How skills are generated

The generator `bin/gen-skills.sh` produces all four skill files from a single parameterized
body template and four inline description strings. Re-running the generator overwrites the
files identically (idempotent).

```bash
bash bin/gen-skills.sh           # generate / regenerate all four SKILL.md files
bash bin/gen-skills.sh --check   # drift check: exits 0 on match, 1 on any drift
```

### Two-layer Source-of-Truth model

| Layer | Source of truth | The SKILL.md file |
|-------|-----------------|-------------------|
| **Behavior** | `schema/workflows/{op}.md` | pointer only — zero behavior encoded |
| **Artifact text** (bytes) | `bin/gen-skills.sh` template + description data | generated copy, --check guarded |

This mirrors the existing `AGENTS.md → CLAUDE.md` generated-artifact relationship.
Hand-editing a `SKILL.md` file is prohibited; the `--check` gate will detect and reject it.

## The four skills

| Operation | Skill path | Behavioral source of truth |
|-----------|-----------|---------------------------|
| ingest | `.claude/skills/ingest/SKILL.md` | `schema/workflows/ingest.md` |
| query | `.claude/skills/query/SKILL.md` | `schema/workflows/query.md` |
| lint | `.claude/skills/lint/SKILL.md` | `schema/workflows/lint.md` |
| reflect | `.claude/skills/reflect/SKILL.md` | `schema/workflows/reflect.md` |

Each skill directory contains **only** `SKILL.md`. Claude Code loads all `.md` files in a
skill directory at Level-2 scope — extra files would inflate context unexpectedly.

## Modifying a skill

To change a skill's description or body:

1. Edit `bin/gen-skills.sh` — update the `get_desc()` case for the relevant op (description)
   or the `body_for()` heredoc template (body).
2. Run `bash bin/gen-skills.sh` to regenerate.
3. Run `bash bin/gen-skills.sh --check` to verify the committed files match.
4. Stage and commit: `git add .claude/skills/ && git commit`.

Do **not** edit `SKILL.md` files directly. The `--check` gate rejects hand-edits.

## Drift gate

The `--check` gate runs in two places:

| Location | Behavior on drift |
|----------|-------------------|
| `.githooks/pre-commit` | Auto-regenerates, re-stages, asks you to re-run commit |
| CI `lint.yml` `skills-check` job | Hard-fails the PR — no auto-fix |

The gate checks two things:
- **Regenerate-diff**: regenerates all four files to a temp dir and compares byte-for-byte
  with the committed files using `cmp -s`.
- **Structural assertions**: independently verifies each SKILL.md body is ≤3 post-frontmatter
  lines, each skill dir contains only `SKILL.md`, no description contains a first-person
  pronoun, and `disable-model-invocation: true` is absent.

The structural assertions are not redundant with the regenerate-diff — a fattened template
(with procedural content in the body template) would still pass diff but fail the line-count
assertion.

## Adopter notes

The four `SKILL.md` files are **committed** to the repository (not gitignored). Claude Code
discovers skills from the filesystem at session start and cannot lazily materialize them, so
a fresh clone must have the files present. The `.gitignore` includes explicit negation lines
(`!.claude/skills/` and `!.claude/skills/**`) to un-ignore these files from the broader
`.claude/` gitignore entry.

When adopting this template for a new vault, the generated skill files are included
automatically. If you regenerate (e.g., after updating `bin/gen-skills.sh` descriptions),
re-stage and commit the updated SKILL.md files.
```

**B) Update .planning/ROADMAP.md Phase 18 section**

Read ROADMAP.md. Locate the Phase 18 section:
```
**Plans**: TBD

Plans:

- [ ] 18-01: TBD
```

Replace with:
```
**Plans**: 3 plans

Plans:

**Wave 0**

- [ ] 18-00-PLAN.md — .gitignore un-ignore + RED test harness (11 tests)

**Wave 1** *(blocked on Wave 0)*

- [ ] 18-01-PLAN.md — bin/gen-skills.sh + four SKILL.md files + pre-commit + neutrality + CI gate

**Wave 2** *(blocked on Wave 1)*

- [ ] 18-02-PLAN.md — Decision record (D-09) + docs/reference/skills.md (D-11) + index.md + ROADMAP
```
  </action>
  <verify>
    <automated>bash /home/yishai/Documents/compendium/bin/check-neutrality.sh && echo "neutrality: OK"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/reference/skills.md` exists
    - `grep 'gen-skills.sh' docs/reference/skills.md` returns 2+ matches (file references the generator)
    - `grep 'schema/workflows' docs/reference/skills.md` returns 4+ matches (all four ops referenced)
    - `grep 'two-layer\|Two-layer' docs/reference/skills.md` matches (SOT model documented for adopters)
    - No private vault terms in docs/reference/skills.md (check-neutrality exits 0)
    - `grep '18-00-PLAN.md' .planning/ROADMAP.md` matches
    - `grep '18-01-PLAN.md' .planning/ROADMAP.md` matches
    - `grep '18-02-PLAN.md' .planning/ROADMAP.md` matches
    - `grep 'TBD' .planning/ROADMAP.md | grep -i '18-01:'` returns no matches (TBD placeholder replaced)
    - `bash bin/check-neutrality.sh` exits 0 (no regressions from new docs file)
    - `bash bin/sync-claude.sh --check` exits 0 (AGENTS.md ≡ CLAUDE.md unchanged)
    - `bash bin/lint.sh --ci 2>/dev/null; echo $?` exits 0 (no new lint errors from new files)
  </acceptance_criteria>
  <done>docs/reference/skills.md explains the overlay for adopters; ROADMAP.md Phase 18 plan list shows 18-00/01/02; check-neutrality exits 0; sync-claude exits 0; lint exits 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DR body → wiki-cloud/ public surface | Decision record is a template-public wiki page; private vault terms must not appear |
| docs/reference/skills.md → public release surface | This file ships in the orphan-branch release; must use abstract placeholders per AGENTS.md §3 neutrality rule |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02-01 | Information Disclosure | DR or docs page inadvertently includes a private vault slug (from the user's personal wiki content) | mitigate | `bin/check-neutrality.sh` (with D-10 extension scanning .claude/skills/) runs in CI and pre-commit; acceptance criteria require `check-neutrality` exits 0 after this plan lands. Template-public neutrality rule applied at write-time. |
| T-18-02-02 | Tampering | index.md Decisions section entry uses a bare [[Title]] wikilink (without pipe separator), causing Obsidian link resolution failure | mitigate | AGENTS.md §3 forbids bare [[Title]] links; the entry format explicitly uses `[[id|Title]]` with a pipe. Acceptance criteria verify the wikilink format via grep. |
| T-18-02-03 | Information Disclosure | docs/reference/skills.md examples contain real op names (ingest/query/lint/reflect) that are structural schema terms, not private vault terms | accept | These four op names are part of the public schema, not private vault content. They are always present in AGENTS.md, CLAUDE.md, and schema/workflows/. No denylist hit expected; neutrality gate confirms. |
</threat_model>

<verification>
After Plan 02 completes (full Phase 18 verification):
- `bash bin/gen-skills.sh --check` exits 0
- `bash bin/check-neutrality.sh` exits 0
- `bash bin/sync-claude.sh --check` exits 0
- `bash bin/lint.sh --ci 2>/dev/null` exits 0 (no new lint errors)
- `bash tests/phase-18/run.sh` emits PHASE 18 TESTS: 11/11
- `git ls-files .claude/skills/` lists all four SKILL.md files
- `wiki-cloud/decisions/dr-2026-06-08-skills-overlay.md` passes lint frontmatter check
- `grep 'dr-2026-06-08-skills-overlay' wiki-cloud/index.md` returns 1 match
- `grep '18-02-PLAN.md' .planning/ROADMAP.md` returns 1 match
</verification>

<success_criteria>
- Decision record dr-2026-06-08-skills-overlay.md exists with all required frontmatter fields, body with TL;DR/Decision/Why/Alternatives/Consequences sections, and is registered in wiki-cloud/index.md Decisions section
- docs/reference/skills.md explains the overlay, generated nature, two-layer SOT model, drift gate behavior, and adopter notes — no private vault terms
- ROADMAP.md Phase 18 plan list updated: 3 plans, 3 waves, with all plan file names
- All existing CI gates remain green: check-neutrality, sync-claude --check, lint --ci, tests/phase-18 11/11
</success_criteria>

<output>
After completion, create `.planning/phases/18-skills-overlay/18-02-SUMMARY.md` following the template at `@/home/yishai/.claude/get-shit-done/templates/summary.md`.
</output>
