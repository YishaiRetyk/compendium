---
phase: 10-brownfield-scan-bootstrap
plan: 10-06-fixture-created-at-override
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/brownfield_yaml.py
  - tests/phase-10/test_brownfield_bootstrap_apply_clean.sh
  - tests/phase-10/test_brownfield_bootstrap_apply_no_fm.sh
  - tests/phase-10/test_brownfield_bootstrap_apply_crlf.sh
  - tests/phase-10/test_brownfield_bootstrap_apply_dataview.sh
  - tests/phase-10/test_brownfield_bootstrap_apply_comments.sh
  - docs/reference/brownfield.md
autonomous: true
requirements:
  - BRWN-21
gap_closure: true
closes_gaps:
  - "Byte-exact fixture tests prove `bootstrap` produces identical output on sample vaults across runs (BRWN-21 dual golden contract: transformed-output)"
must_haves:
  truths:
    - "All 5 apply_* byte-equality tests pass on any date >= 2026-04-18"
    - "Phase 10 aggregator prints `PHASE 10 TESTS: 32/32`"
    - "No regression in prior phase test suites (07=22/22, 08=21/21, 09=28/28, 09.1=11/11)"
    - "BROWNFIELD_FIXTURE_CREATED_AT is documented in docs/reference/brownfield.md"
    - "Fallback to file_mtime_iso() is preserved when the env var is absent"
    - "Invalid BROWNFIELD_FIXTURE_CREATED_AT values (non-ISO-date) raise ValueError (fail-loud)"
  artifacts:
    - path: "bin/lib/brownfield_yaml.py"
      provides: "BROWNFIELD_FIXTURE_CREATED_AT env override inside build_d14_sentinel_set()"
      contains: "BROWNFIELD_FIXTURE_CREATED_AT"
    - path: "tests/phase-10/test_brownfield_bootstrap_apply_clean.sh"
      provides: "BROWNFIELD_FIXTURE_CREATED_AT=2026-04-17 export alongside BROWNFIELD_FIXTURE_TODAY"
      contains: 'BROWNFIELD_FIXTURE_CREATED_AT="2026-04-17"'
    - path: "tests/phase-10/test_brownfield_bootstrap_apply_no_fm.sh"
      provides: "BROWNFIELD_FIXTURE_CREATED_AT=2026-04-17 export"
      contains: 'BROWNFIELD_FIXTURE_CREATED_AT="2026-04-17"'
    - path: "tests/phase-10/test_brownfield_bootstrap_apply_crlf.sh"
      provides: "BROWNFIELD_FIXTURE_CREATED_AT=2026-04-17 export"
      contains: 'BROWNFIELD_FIXTURE_CREATED_AT="2026-04-17"'
    - path: "tests/phase-10/test_brownfield_bootstrap_apply_dataview.sh"
      provides: "BROWNFIELD_FIXTURE_CREATED_AT=2026-04-17 export"
      contains: 'BROWNFIELD_FIXTURE_CREATED_AT="2026-04-17"'
    - path: "tests/phase-10/test_brownfield_bootstrap_apply_comments.sh"
      provides: "BROWNFIELD_FIXTURE_CREATED_AT=2026-04-17 export"
      contains: 'BROWNFIELD_FIXTURE_CREATED_AT="2026-04-17"'
    - path: "docs/reference/brownfield.md"
      provides: "Documented scope of BROWNFIELD_FIXTURE_CREATED_AT (fixture-testing only)"
      contains: "BROWNFIELD_FIXTURE_CREATED_AT"
  key_links:
    - from: "tests/phase-10/test_brownfield_bootstrap_apply_*.sh"
      to: "bin/lib/brownfield_yaml.py::build_d14_sentinel_set"
      via: "BROWNFIELD_FIXTURE_CREATED_AT environment variable"
      pattern: "BROWNFIELD_FIXTURE_CREATED_AT"
    - from: "bin/lib/brownfield_yaml.py::build_d14_sentinel_set"
      to: "datetime.date.fromisoformat()"
      via: "os.environ.get('BROWNFIELD_FIXTURE_CREATED_AT') guarded parse"
      pattern: "datetime.date.fromisoformat"
---

<objective>
Close the single gap reported in 10-VERIFICATION.md (BRWN-21 transformed-output byte-equality) by adding a BROWNFIELD_FIXTURE_CREATED_AT environment-variable override that pins the `created_at` field inside `build_d14_sentinel_set()`, mirroring the existing BROWNFIELD_FIXTURE_TODAY pattern that pins `updated_at` and `bootstrap_date`. Once the override is wired, the five failing apply_* fixture tests export the new variable and regain deterministic byte-equality against their committed expected/page.md fixtures on any calendar date.

Purpose: Restore the BRWN-21 "identical output on sample vaults across runs" contract. Today (2026-04-18) the aggregator reports 27/32; after this plan it must report 32/32 every day.

Output:
- Env-override code path in bin/lib/brownfield_yaml.py (with fail-loud ValueError on malformed inputs).
- Five test files updated to export BROWNFIELD_FIXTURE_CREATED_AT="2026-04-17".
- docs/reference/brownfield.md mentions the new variable with explicit fixture-only scope.
- Phase 10 aggregator returns to 32/32; prior phase regressions remain green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-brownfield-scan-bootstrap/10-CONTEXT.md
@.planning/phases/10-brownfield-scan-bootstrap/10-VERIFICATION.md
@.planning/phases/10-brownfield-scan-bootstrap/10-03-PLAN.md
@.planning/phases/10-brownfield-scan-bootstrap/10-01-PLAN.md
@bin/lib/brownfield_yaml.py
@bin/brownfield.sh
@tests/phase-10/lib.sh
@tests/phase-10/test_brownfield_bootstrap_apply_clean.sh
@docs/reference/brownfield.md

<interfaces>
<!-- Key contract surfaces the executor will touch. Extracted from bin/lib/brownfield_yaml.py. -->

From bin/lib/brownfield_yaml.py:
```python
def file_mtime_iso(path: str) -> datetime.date:
    """File mtime as a ``datetime.date`` for unquoted YAML date rendering.
    UTC per AGENTS.md §3 + Phase 3 D-11 precedent.
    """
    stat = os.stat(path)
    return datetime.datetime.fromtimestamp(stat.st_mtime, tz=datetime.timezone.utc).date()

def build_d14_sentinel_set(
    path: str,
    body: str,
    today: datetime.date,
) -> dict[str, Any]:
    """Assemble the D-14 sentinel dict for a single page.
    ... (returns dict containing id, title, created_at, updated_at, ..., bootstrap_date)
    """
    # Current created_at derivation (lines ~278-282):
    try:
        created_at = file_mtime_iso(path)
    except OSError:
        created_at = datetime.date(today.year, today.month, today.day)
```

From bin/brownfield.sh (BROWNFIELD_FIXTURE_TODAY precedent, lines 110-117 — mirror exactly):
```bash
# Honor BROWNFIELD_FIXTURE_TODAY for pinned-date fixture tests; otherwise
# compute today's UTC date.  The env var is scoped to test runs per the
# fixture-date-freeze convention (tests/phase-10/fixtures/README.md).
if [ -n "${BROWNFIELD_FIXTURE_TODAY:-}" ]; then
    BROWNFIELD_TODAY="$BROWNFIELD_FIXTURE_TODAY"
else
    BROWNFIELD_TODAY="$(date -u '+%Y-%m-%d')"
fi
```

Failing test pattern (all 5 apply_* tests share this shape):
```bash
export BROWNFIELD_FIXTURE_TODAY="2026-04-17"
FIXTURE="<name>"
tmp=$(make_fixture_repo "$FIXTURE")
bash "$REPO_ROOT/bin/brownfield.sh" bootstrap --apply --root "$tmp/input" >/dev/null 2>&1
assert_byte_equal \
    "$REPO_ROOT/tests/phase-10/fixtures/$FIXTURE/expected/page.md" \
    "$tmp/input/page.md" \
    "bootstrap --apply on $FIXTURE did not produce expected/page.md"
```
Root cause: `make_fixture_repo` uses plain `cp` (no `-p`), so `file_mtime_iso` returns today's date (2026-04-18), not the fixture-pinned 2026-04-17.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BROWNFIELD_FIXTURE_CREATED_AT env override in bin/lib/brownfield_yaml.py</name>
  <files>bin/lib/brownfield_yaml.py</files>
  <read_first>
    - bin/lib/brownfield_yaml.py (whole file — focus on the module docstring block at lines 1-22 and build_d14_sentinel_set at lines 259-314)
    - bin/brownfield.sh lines 108-117 (BROWNFIELD_FIXTURE_TODAY precedent to mirror)
    - tests/phase-10/test_brownfield_bootstrap_apply_clean.sh (understand how the env var flows into the Python process)
  </read_first>
  <action>
    Make two edits inside bin/lib/brownfield_yaml.py.

    EDIT 1 — Extend the module docstring (lines 1-22). Locate the final bullet in the docstring block that ends with:
        "  - make_yaml()          — factory for a pre-configured ruamel.yaml.YAML instance."
    Immediately AFTER the closing triple-quote-terminating line of the docstring (line 22), NO — actually add a new bullet BEFORE the closing `"""` so it reads, in order:
        "  - file_mtime_iso()     — file mtime as YYYY-MM-DD (UTC).  Overridable via BROWNFIELD_FIXTURE_CREATED_AT (fixture-testing only; scope mirrors BROWNFIELD_FIXTURE_TODAY in bin/brownfield.sh)."
    Replace the existing bare file_mtime_iso bullet (currently: "  - file_mtime_iso()     — file mtime as YYYY-MM-DD (UTC).") with the expanded version above. Do NOT add a separate module-level constant for the env var name — the os.environ.get() call in Edit 2 is the single source of truth.

    EDIT 2 — Inside build_d14_sentinel_set (currently at lines 259-314), locate the block:

        try:
            created_at = file_mtime_iso(path)
        except OSError:
            # New / unreadable mtime; fall back to today (never fails the bootstrap).
            created_at = datetime.date(today.year, today.month, today.day)

    REPLACE that block with:

        # BRWN-21 fixture-date-freeze: BROWNFIELD_FIXTURE_CREATED_AT pins created_at
        # for byte-equality fixture tests.  Scope mirrors BROWNFIELD_FIXTURE_TODAY
        # in bin/brownfield.sh (fixture-testing only; production bootstrap uses
        # file mtime per D-11).  Fail-loud on malformed values — the mechanical-
        # only brownfield contract rejects silent fallbacks on bad fixture pins.
        created_at_override = os.environ.get('BROWNFIELD_FIXTURE_CREATED_AT')
        if created_at_override:
            try:
                created_at = datetime.date.fromisoformat(created_at_override)
            except ValueError as exc:
                raise ValueError(
                    f"BROWNFIELD_FIXTURE_CREATED_AT must be YYYY-MM-DD, got: "
                    f"{created_at_override!r}"
                ) from exc
        else:
            try:
                created_at = file_mtime_iso(path)
            except OSError:
                # New / unreadable mtime; fall back to today (never fails the bootstrap).
                created_at = datetime.date(today.year, today.month, today.day)

    Preserve surrounding code untouched: the `updated_at` / `bootstrap_date` distinct-instance construction immediately below MUST remain exactly as-is (the ruamel anchor/alias issue documented in its comment still applies).

    Do NOT change any function signature, do NOT add the env var to __all__, do NOT touch VALID_ENUMS / FIELD_CLASS_A / FIELD_CLASS_B. The `os` module is already imported at line 26, so no new imports are required.
  </action>
  <verify>
    <automated>
      grep -q "BROWNFIELD_FIXTURE_CREATED_AT" bin/lib/brownfield_yaml.py &amp;&amp; \
      python3 -c "import os, sys, datetime; os.environ['BROWNFIELD_FIXTURE_CREATED_AT']='2026-04-17'; sys.path.insert(0, 'bin/lib'); from brownfield_yaml import build_d14_sentinel_set; d=build_d14_sentinel_set('/tmp/nonexistent-brownfield-test', '', datetime.date(2026,4,18)); assert d['created_at'] == datetime.date(2026,4,17), d['created_at']; assert d['updated_at'] == datetime.date(2026,4,18), d['updated_at']; print('OK-override')" &amp;&amp; \
      python3 -c "import os, sys, datetime; os.environ.pop('BROWNFIELD_FIXTURE_CREATED_AT', None); sys.path.insert(0, 'bin/lib'); from brownfield_yaml import build_d14_sentinel_set; d=build_d14_sentinel_set('/tmp/nonexistent-brownfield-test', '', datetime.date(2026,4,18)); assert d['created_at'] == datetime.date(2026,4,18), d['created_at']; print('OK-fallback')" &amp;&amp; \
      python3 -c "import os, sys, datetime; os.environ['BROWNFIELD_FIXTURE_CREATED_AT']='not-a-date'; sys.path.insert(0, 'bin/lib'); from brownfield_yaml import build_d14_sentinel_set;
try:
    build_d14_sentinel_set('/tmp/nonexistent-brownfield-test', '', datetime.date(2026,4,18))
    print('FAIL-should-have-raised'); sys.exit(1)
except ValueError as e:
    assert 'BROWNFIELD_FIXTURE_CREATED_AT' in str(e), str(e); print('OK-fail-loud')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "BROWNFIELD_FIXTURE_CREATED_AT" bin/lib/brownfield_yaml.py` exits 0.
    - With `BROWNFIELD_FIXTURE_CREATED_AT=2026-04-17` set, calling `build_d14_sentinel_set('/tmp/nonexistent-brownfield-test', '', datetime.date(2026,4,18))` returns a dict where `created_at == datetime.date(2026,4,17)` AND `updated_at == datetime.date(2026,4,18)` (today param is untouched by the override).
    - With the env var absent, calling the same function on a nonexistent path returns `created_at == today` (OSError fallback still works) AND with an existing file it returns the file's mtime (file_mtime_iso path preserved).
    - With `BROWNFIELD_FIXTURE_CREATED_AT='not-a-date'` set, calling the function raises a `ValueError` whose message contains the substring `BROWNFIELD_FIXTURE_CREATED_AT`.
    - The `updated_at` and `bootstrap_date` distinct-datetime construction comment immediately below the replaced block is preserved verbatim.
    - No new imports added to bin/lib/brownfield_yaml.py; `__all__` list is unchanged.
  </acceptance_criteria>
  <done>
    The env-override code path exists, works in both pinned and unpinned modes, and fails loudly on malformed input. All three automated verify commands print their respective `OK-*` sentinels.
  </done>
</task>

<task type="auto">
  <name>Task 2: Export BROWNFIELD_FIXTURE_CREATED_AT="2026-04-17" in the five failing apply_* tests</name>
  <files>
    tests/phase-10/test_brownfield_bootstrap_apply_clean.sh,
    tests/phase-10/test_brownfield_bootstrap_apply_no_fm.sh,
    tests/phase-10/test_brownfield_bootstrap_apply_crlf.sh,
    tests/phase-10/test_brownfield_bootstrap_apply_dataview.sh,
    tests/phase-10/test_brownfield_bootstrap_apply_comments.sh
  </files>
  <read_first>
    - tests/phase-10/test_brownfield_bootstrap_apply_clean.sh (lines 1-12 — confirms line 7 has BROWNFIELD_FIXTURE_TODAY export)
    - tests/phase-10/test_brownfield_bootstrap_apply_no_fm.sh (lines 1-10 — line 6)
    - tests/phase-10/test_brownfield_bootstrap_apply_crlf.sh (lines 1-10 — line 6)
    - tests/phase-10/test_brownfield_bootstrap_apply_dataview.sh (lines 1-10 — line 6)
    - tests/phase-10/test_brownfield_bootstrap_apply_comments.sh (lines 1-10 — line 6)
  </read_first>
  <action>
    In each of the five files below, insert a new line IMMEDIATELY AFTER the existing `export BROWNFIELD_FIXTURE_TODAY="2026-04-17"` line. The new line MUST read exactly:
        export BROWNFIELD_FIXTURE_CREATED_AT="2026-04-17"
    using the SAME double-quote style (not single quotes, no surrounding whitespace beyond the single trailing newline). The insertion preserves the existing blank line that follows (in files where one exists).

    Exact insertion points:
    - tests/phase-10/test_brownfield_bootstrap_apply_clean.sh — insert after line 7 (between `export BROWNFIELD_FIXTURE_TODAY="2026-04-17"` and the blank line at current line 8).
    - tests/phase-10/test_brownfield_bootstrap_apply_no_fm.sh — insert after line 6.
    - tests/phase-10/test_brownfield_bootstrap_apply_crlf.sh — insert after line 6.
    - tests/phase-10/test_brownfield_bootstrap_apply_dataview.sh — insert after line 6.
    - tests/phase-10/test_brownfield_bootstrap_apply_comments.sh — insert after line 6.

    Do NOT alter any other line in any of these files. Do NOT add comments. Do NOT reorder assertions.
  </action>
  <verify>
    <automated>
      [ "$(grep -l 'BROWNFIELD_FIXTURE_CREATED_AT="2026-04-17"' tests/phase-10/test_brownfield_bootstrap_apply_*.sh | wc -l)" = "5" ] &amp;&amp; \
      for f in tests/phase-10/test_brownfield_bootstrap_apply_clean.sh tests/phase-10/test_brownfield_bootstrap_apply_no_fm.sh tests/phase-10/test_brownfield_bootstrap_apply_crlf.sh tests/phase-10/test_brownfield_bootstrap_apply_dataview.sh tests/phase-10/test_brownfield_bootstrap_apply_comments.sh; do
        awk '/BROWNFIELD_FIXTURE_TODAY="2026-04-17"/{seen_today=NR; next} seen_today &amp;&amp; NR==seen_today+1 &amp;&amp; /BROWNFIELD_FIXTURE_CREATED_AT="2026-04-17"/{ok=1} END{if(!ok){print "FAIL adjacency: " FILENAME > "/dev/stderr"; exit 1}}' "$f" || exit 1
      done &amp;&amp; echo "ADJACENCY-OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -l 'BROWNFIELD_FIXTURE_CREATED_AT="2026-04-17"' tests/phase-10/test_brownfield_bootstrap_apply_*.sh | wc -l` prints exactly `5`.
    - In each of the five files, the `BROWNFIELD_FIXTURE_CREATED_AT` export line immediately follows the `BROWNFIELD_FIXTURE_TODAY` export line (no blank line between them).
    - Each edited file's shebang is still `#!/usr/bin/env bash` on line 1.
    - Each edited file's `set -euo pipefail` line is still present.
    - Running `bash tests/phase-10/test_brownfield_bootstrap_apply_clean.sh` now prints `PASS: bootstrap apply clean-frontmatter` and exits 0 at system date 2026-04-18 or later.
    - Running each of the other four apply_* tests individually also prints `PASS: bootstrap apply <fixture>` and exits 0.
  </acceptance_criteria>
  <done>
    All five apply_* tests export the new env var and pass individually on today's date (2026-04-18).
  </done>
</task>

<task type="auto">
  <name>Task 3: Audit the other seven BROWNFIELD_FIXTURE_TODAY-using tests — no changes required, documented decisions</name>
  <files>
    (no file modifications — audit-only task)
  </files>
  <read_first>
    - tests/phase-10/test_brownfield_bootstrap_applied_manifest.sh
    - tests/phase-10/test_brownfield_bootstrap_typed_merge.sh
    - tests/phase-10/test_brownfield_bootstrap_skip_tabs.sh
    - tests/phase-10/test_brownfield_bootstrap_skip_dupkeys.sh
    - tests/phase-10/test_brownfield_bootstrap_dryrun.sh
    - tests/phase-10/test_brownfield_bootstrap_help.sh
    - tests/phase-10/test_brownfield_bootstrap_idempotent.sh
  </read_first>
  <action>
    This task is the deliberate NO-CHANGE audit trail for the seven remaining BROWNFIELD_FIXTURE_TODAY-using tests. Planner decision rule (authoritative): if a test's assertion touches `created_at` or performs byte-equality against a committed fixture that carries a pinned `created_at`, add the BROWNFIELD_FIXTURE_CREATED_AT export; otherwise leave alone. The per-test decisions are fixed by the planner (no executor discretion):

    1. test_brownfield_bootstrap_applied_manifest.sh
       Decision: NOT NEEDED.
       Reason: Asserts APPLIED.md contains `## Run ` headers and the string `page.md`. Does NOT assert created_at. Does NOT do byte-equality on transformed output.

    2. test_brownfield_bootstrap_typed_merge.sh
       Decision: NOT NEEDED.
       Reason: Uses a custom inline vault (not a fixture cp). Asserts `type: concept`, `privacy: cloud_safe`, `bootstrap_stage: bootstrapped`, `bootstrap_date: 2026-04-17` via grep. Does NOT assert created_at (intentionally — the inline vault's mtime is self-consistent within the test).

    3. test_brownfield_bootstrap_skip_tabs.sh
       Decision: NOT NEEDED.
       Reason: Input file is unparseable YAML, so bootstrap routes it to SKIPPED.md and never injects any frontmatter. `created_at` derivation is never reached for skipped files. Test asserts input sha256 is unchanged + SKIPPED.md header present.

    4. test_brownfield_bootstrap_skip_dupkeys.sh
       Decision: NOT NEEDED.
       Reason: Same as skip_tabs — duplicate-key pre-scan routes to SKIPPED.md; no sentinel injection path runs.

    5. test_brownfield_bootstrap_dryrun.sh
       Decision: NOT NEEDED.
       Reason: Asserts REPORT.md exists, 5 stderr labels present, APPLIED.md NOT created, input/expected sha256 unchanged. No byte-equality against transformed output (dry-run never writes the page). created_at is computed in memory for the report summary but is never compared to a committed fixture.

    6. test_brownfield_bootstrap_help.sh
       Decision: NOT NEEDED.
       Reason: Runs `bash bin/brownfield.sh bootstrap --help` and greps for four flag strings. Never invokes bootstrap on any file. The BROWNFIELD_FIXTURE_TODAY export at line 7 is vestigial — harmless but unused.

    7. test_brownfield_bootstrap_idempotent.sh
       Decision: NOT NEEDED (critical — document this reasoning explicitly).
       Reason: Copies two fixtures into a fresh mktemp vault, runs `bootstrap --apply` twice, compares sha256 snapshots across the TWO RUNS (not against a committed fixture). Within the test process, the copied files' mtime is whatever the cp assigned (today). Run 1 injects `created_at = <today>`. Run 2 hits the per-file `bootstrap_stage: bootstrapped` idempotency skip at bin/brownfield.sh:324 and writes nothing. The assertion `before == after` holds because the second run is a no-op. Pinning created_at would not change behavior but would obscure the fact that idempotency depends on the sentinel skip, not on date determinism.

    Executor action: leave all seven files untouched. This task is complete by reading (not writing).
  </action>
  <verify>
    <automated>
      [ "$(git diff --name-only HEAD -- tests/phase-10/test_brownfield_bootstrap_applied_manifest.sh tests/phase-10/test_brownfield_bootstrap_typed_merge.sh tests/phase-10/test_brownfield_bootstrap_skip_tabs.sh tests/phase-10/test_brownfield_bootstrap_skip_dupkeys.sh tests/phase-10/test_brownfield_bootstrap_dryrun.sh tests/phase-10/test_brownfield_bootstrap_help.sh tests/phase-10/test_brownfield_bootstrap_idempotent.sh | wc -l)" = "0" ] &amp;&amp; \
      [ "$(grep -l 'BROWNFIELD_FIXTURE_CREATED_AT' tests/phase-10/test_brownfield_bootstrap_applied_manifest.sh tests/phase-10/test_brownfield_bootstrap_typed_merge.sh tests/phase-10/test_brownfield_bootstrap_skip_tabs.sh tests/phase-10/test_brownfield_bootstrap_skip_dupkeys.sh tests/phase-10/test_brownfield_bootstrap_dryrun.sh tests/phase-10/test_brownfield_bootstrap_help.sh tests/phase-10/test_brownfield_bootstrap_idempotent.sh 2>/dev/null | wc -l)" = "0" ] &amp;&amp; \
      echo "AUDIT-CLEAN"
    </automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only HEAD --` on the seven listed files returns zero entries.
    - None of the seven listed files contain the string `BROWNFIELD_FIXTURE_CREATED_AT`.
    - All seven tests still individually pass: `for t in applied_manifest typed_merge skip_tabs skip_dupkeys dryrun help idempotent; do PYTHONPATH=$HOME/.local/lib/python3/dist-packages bash tests/phase-10/test_brownfield_bootstrap_${t}.sh; done` exits 0 seven times.
  </acceptance_criteria>
  <done>
    The seven tests remain untouched; this plan's audit recorded the per-test rationale in the action block above for future-plan archaeology; all seven still pass on 2026-04-18.
  </done>
</task>

<task type="auto">
  <name>Task 4: Document BROWNFIELD_FIXTURE_CREATED_AT in docs/reference/brownfield.md</name>
  <files>docs/reference/brownfield.md</files>
  <read_first>
    - docs/reference/brownfield.md (whole 194-line file — pay particular attention to the "Known limitations" section around lines 182-186 and the "See also" section at lines 188-194, since the new content slots before "See also")
  </read_first>
  <action>
    Insert a new top-level section titled `## Fixture testing environment variables` between the existing `## Known limitations` section (ends around line 186) and the existing `## See also` section (begins at line 188). The new section MUST contain exactly the following content (two paragraphs, a two-row table, and one warning blockquote; LF line endings; no trailing whitespace):

        ## Fixture testing environment variables

        Two environment variables pin date fields to deterministic values so the Phase-10 byte-equality fixture tests (`tests/phase-10/test_brownfield_bootstrap_apply_*.sh`) produce identical output on every calendar day. **These variables are fixture-testing only; do not use them in production bootstrap runs.** Production bootstrap uses file mtime for `created_at` and UTC today for `updated_at` / `bootstrap_date` per D-11.

        | Variable | Pins | Consumed by |
        |----------|------|-------------|
        | `BROWNFIELD_FIXTURE_TODAY` | `updated_at`, `bootstrap_date` | `bin/brownfield.sh` (bootstrap + scan today derivation) |
        | `BROWNFIELD_FIXTURE_CREATED_AT` | `created_at` (bypasses `file_mtime_iso`) | `bin/lib/brownfield_yaml.py::build_d14_sentinel_set` |

        Both variables accept `YYYY-MM-DD` strings only. `BROWNFIELD_FIXTURE_CREATED_AT` fails loud with `ValueError` on any malformed value — the mechanical-only brownfield contract rejects silent fallbacks on bad fixture pins. When unset, the production code paths (`file_mtime_iso` + `date -u '+%Y-%m-%d'`) run unchanged.

        > **Scope warning:** Exporting `BROWNFIELD_FIXTURE_CREATED_AT` on a real vault overrides the mtime-derived `created_at` for every page in that run. This is exactly what you want during fixture regression tests and exactly what you do NOT want in production. Keep these exports inside `tests/phase-10/test_*.sh` files.

    Do NOT alter the existing "See also" section. Do NOT remove or renumber any existing bullet. The final file MUST still be at least 194 lines (currently 194 lines; adding ~18 lines of new content brings it to approximately 212 lines).
  </action>
  <verify>
    <automated>
      grep -q "BROWNFIELD_FIXTURE_CREATED_AT" docs/reference/brownfield.md &amp;&amp; \
      grep -q "## Fixture testing environment variables" docs/reference/brownfield.md &amp;&amp; \
      grep -q "fixture-testing only" docs/reference/brownfield.md &amp;&amp; \
      [ "$(wc -l < docs/reference/brownfield.md)" -ge 194 ] &amp;&amp; \
      [ "$(grep -c "^## " docs/reference/brownfield.md)" -ge 11 ] &amp;&amp; \
      awk '/^## Fixture testing environment variables$/{fixture=NR} /^## See also$/{seealso=NR} END{exit !(fixture &amp;&amp; seealso &amp;&amp; fixture < seealso)}' docs/reference/brownfield.md &amp;&amp; \
      echo "DOC-OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "BROWNFIELD_FIXTURE_CREATED_AT" docs/reference/brownfield.md` exits 0.
    - `grep -q "## Fixture testing environment variables" docs/reference/brownfield.md` exits 0.
    - `grep -q "fixture-testing only" docs/reference/brownfield.md` exits 0.
    - `wc -l docs/reference/brownfield.md` >= 194.
    - The new `## Fixture testing environment variables` section appears BEFORE the existing `## See also` section in the file (awk check above).
    - Total number of `^## ` headers in the file is >= 11 (the existing 10 top-level sections plus the new one).
  </acceptance_criteria>
  <done>
    docs/reference/brownfield.md documents the new env var with explicit fixture-only scope and fail-loud semantics; file passes all grep/wc checks.
  </done>
</task>

<task type="auto">
  <name>Task 5: Run Phase 10 aggregator + prior-phase regression suite and confirm 32/32 + no regressions</name>
  <files>
    (no file modifications — verification run)
  </files>
  <read_first>
    - tests/phase-10/run.sh (lines 1-49 — confirms aggregator prints `PHASE 10 TESTS: <n>/<total>` on exit)
  </read_first>
  <action>
    Execute the full test aggregator for Phase 10 and the four prior phases. All five commands MUST complete and print their respective passing totals. If any one of them fails, STOP, report which phase regressed, and do not commit — revisit Task 1/2/4 as appropriate.

    Commands (run in sequence; each uses the PYTHONPATH export so ruamel.yaml is resolvable):

        export PYTHONPATH="$HOME/.local/lib/python3/dist-packages"
        bash tests/phase-10/run.sh        # expect: PHASE 10 TESTS: 32/32
        bash tests/phase-07/run.sh        # expect: PHASE 07 TESTS: 22/22
        bash tests/phase-08/run.sh        # expect: PHASE 08 TESTS: 21/21
        bash tests/phase-09/run.sh        # expect: PHASE 09 TESTS: 28/28
        bash tests/phase-09.1/run.sh      # expect: PHASE 09.1 TESTS: 11/11

    If Phase 10 is at 31/32, the single failure is almost certainly one of the five apply_* tests — re-check that Task 2's adjacency check passed and that BROWNFIELD_FIXTURE_CREATED_AT matches exactly the double-quoted "2026-04-17" string. If Phase 10 is at 32/32 but a prior phase regressed (vanishingly unlikely since this plan touches only two files outside of tests/phase-10/), revert Task 1/2/4 and investigate the specific failing test.

    No code changes in this task. Do not modify any file; this is the phase verification run.
  </action>
  <verify>
    <automated>
      export PYTHONPATH="$HOME/.local/lib/python3/dist-packages" &amp;&amp; \
      bash tests/phase-10/run.sh 2>&amp;1 | tee /tmp/phase-10-gap-closure.log &amp;&amp; \
      grep -qE '^PHASE 10 TESTS: 32/32' /tmp/phase-10-gap-closure.log &amp;&amp; \
      bash tests/phase-07/run.sh 2>&amp;1 | grep -qE '^PHASE 07 TESTS: 22/22' &amp;&amp; \
      bash tests/phase-08/run.sh 2>&amp;1 | grep -qE '^PHASE 08 TESTS: 21/21' &amp;&amp; \
      bash tests/phase-09/run.sh 2>&amp;1 | grep -qE '^PHASE 09 TESTS: 28/28' &amp;&amp; \
      bash tests/phase-09.1/run.sh 2>&amp;1 | grep -qE '^PHASE 09.1 TESTS: 11/11' &amp;&amp; \
      echo "ALL-GREEN"
    </automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/phase-10/run.sh` exits 0 and stdout contains the exact line `PHASE 10 TESTS: 32/32`.
    - `bash tests/phase-07/run.sh` exits 0 and stdout contains the exact line `PHASE 07 TESTS: 22/22`.
    - `bash tests/phase-08/run.sh` exits 0 and stdout contains the exact line `PHASE 08 TESTS: 21/21`.
    - `bash tests/phase-09/run.sh` exits 0 and stdout contains the exact line `PHASE 09 TESTS: 28/28`.
    - `bash tests/phase-09.1/run.sh` exits 0 and stdout contains the exact line `PHASE 09.1 TESTS: 11/11`.
    - The combined verify command above echoes `ALL-GREEN`.
  </acceptance_criteria>
  <done>
    Phase 10 BRWN-21 gap is closed. All five phase aggregators green on 2026-04-18. Plan ready for commit as `fix(phase-10): pin created_at in fixture tests via BROWNFIELD_FIXTURE_CREATED_AT override` (per repo convention — see recent `docs(10): ...` / `test(10-05): ...` commits).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → python process env | Environment variables set by a workflow step flow into `os.environ.get()` inside `bin/lib/brownfield_yaml.py::build_d14_sentinel_set`. |
| Developer shell → test run | Developer exports `BROWNFIELD_FIXTURE_CREATED_AT` for local fixture regeneration; same ingress as CI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06-01 | Tampering | `build_d14_sentinel_set` env-var read | mitigate | ValueError guard rejects non-ISO-date values. Docstring + `docs/reference/brownfield.md` explicitly scope the variable to fixture-testing-only; production bootstrap runs without the export use `file_mtime_iso`. No secrets, no code execution — the override only sets a date field. |
| T-10-06-02 | Information Disclosure | env var logging | accept | The variable's value is a public ISO date, not sensitive. It appears in test logs and in the single `ValueError` message on malformed input. No PII, no credentials, no exfiltration surface. |
| T-10-06-03 | Repudiation | silent-pass on malformed input | mitigate | The replaced block explicitly re-raises with a clear `ValueError('BROWNFIELD_FIXTURE_CREATED_AT must be YYYY-MM-DD, got: ...')` instead of falling back to mtime. Fail-loud matches the mechanical-only brownfield contract (D-11): bad fixture pins stop the run rather than silently corrupting `created_at`. |
| T-10-06-04 | Spoofing | operator mistakenly exports var in production | accept-with-warning | An operator who sets `BROWNFIELD_FIXTURE_CREATED_AT` in a production shell would pin `created_at` for every vault page they bootstrap that session. Mitigation is documentary: the new docs/reference/brownfield.md section carries a `> **Scope warning:**` blockquote saying "Keep these exports inside `tests/phase-10/test_*.sh` files." Severity is low — `created_at` is not a security-critical field; worst case is a misleading creation date, easily rolled back via `git reset --hard` (§Rollback). |

None of the threats reach `high` or `critical` severity. Opt-out is not used; the register is declared honestly per the phase's security_enforcement policy.
</threat_model>

<verification>
## Plan-Level Verification

1. **Gap closure primary check:**
   `PYTHONPATH=$HOME/.local/lib/python3/dist-packages bash tests/phase-10/run.sh` prints `PHASE 10 TESTS: 32/32` and exits 0.

2. **No prior-phase regression:**
   ```
   for p in 07 08 09 09.1; do
     PYTHONPATH=$HOME/.local/lib/python3/dist-packages bash tests/phase-$p/run.sh
   done
   ```
   All four print `PHASE $p TESTS: N/N` and exit 0.

3. **Code path exists and is documented:**
   `grep -q "BROWNFIELD_FIXTURE_CREATED_AT" bin/lib/brownfield_yaml.py docs/reference/brownfield.md` exits 0 (matches in both files).

4. **Fail-loud semantic check:**
   ```
   python3 -c "
   import os, sys, datetime
   os.environ['BROWNFIELD_FIXTURE_CREATED_AT']='garbage'
   sys.path.insert(0, 'bin/lib')
   from brownfield_yaml import build_d14_sentinel_set
   try:
       build_d14_sentinel_set('/tmp/x', '', datetime.date(2026,4,18))
       sys.exit(1)
   except ValueError as e:
       assert 'BROWNFIELD_FIXTURE_CREATED_AT' in str(e)
       print('OK')
   "
   ```
   prints `OK` and exits 0.

5. **Production fallback unchanged:**
   With the env var unset and a real file path, `build_d14_sentinel_set` returns `created_at` equal to `file_mtime_iso(path)` (verified by Task 1's automated verify block).

6. **Git cleanliness on audit tests:**
   `git diff --name-only HEAD -- tests/phase-10/test_brownfield_bootstrap_{applied_manifest,typed_merge,skip_tabs,skip_dupkeys,dryrun,help,idempotent}.sh` returns zero lines.

## Single Commit

Per AGENTS.md §3 one-commit-per-logical-operation: this plan ships as ONE commit.

**Suggested commit message:**
```
fix(phase-10): pin created_at in fixture tests via BROWNFIELD_FIXTURE_CREATED_AT override

Closes BRWN-21 transformed-output byte-equality gap (10-VERIFICATION.md).
Adds env-var override inside bin/lib/brownfield_yaml.py::build_d14_sentinel_set
mirroring BROWNFIELD_FIXTURE_TODAY precedent; five apply_* tests now export
BROWNFIELD_FIXTURE_CREATED_AT="2026-04-17" alongside BROWNFIELD_FIXTURE_TODAY.
Fail-loud on malformed ISO-date input. Documented in docs/reference/brownfield.md.

Phase 10: 27/32 -> 32/32. No prior-phase regressions.
```
</verification>

<success_criteria>
- [ ] `bin/lib/brownfield_yaml.py` contains `BROWNFIELD_FIXTURE_CREATED_AT` env-override code path with ValueError guard.
- [ ] Five `tests/phase-10/test_brownfield_bootstrap_apply_*.sh` files export `BROWNFIELD_FIXTURE_CREATED_AT="2026-04-17"` immediately below their existing `BROWNFIELD_FIXTURE_TODAY` export.
- [ ] The seven other BROWNFIELD_FIXTURE_TODAY-using tests are UNTOUCHED (documented decision per Task 3).
- [ ] `docs/reference/brownfield.md` documents the new variable under a `## Fixture testing environment variables` section with explicit fixture-only scope warning; file still >= 194 lines.
- [ ] `bash tests/phase-10/run.sh` prints `PHASE 10 TESTS: 32/32` on a system date >= 2026-04-18.
- [ ] Prior-phase aggregators (07, 08, 09, 09.1) remain green at their committed totals.
- [ ] ValueError fires on malformed `BROWNFIELD_FIXTURE_CREATED_AT` values (fail-loud contract).
- [ ] Fallback to `file_mtime_iso` is preserved when the env var is absent.
- [ ] No new runtime dependencies (no imports added to `bin/lib/brownfield_yaml.py`).
</success_criteria>

<output>
After plan execution, create `.planning/phases/10-brownfield-scan-bootstrap/10-06-SUMMARY.md` documenting:
- Files modified (2 code + 5 tests + 1 doc = 8 files; commit should be a single `fix(phase-10):` commit).
- Gap closure evidence: BRWN-21 test suite flips from 27/32 to 32/32 (capture the aggregator output line).
- Fail-loud verification output (the `OK-fail-loud` sentinel from Task 1's verify block).
- Confirmation that seven audited tests remain untouched.
- Note for Phase 11 / downstream: the `BROWNFIELD_FIXTURE_CREATED_AT` variable is scoped to fixture-testing per docs/reference/brownfield.md; production bootstrap continues to derive `created_at` from file mtime per D-11.
</output>
