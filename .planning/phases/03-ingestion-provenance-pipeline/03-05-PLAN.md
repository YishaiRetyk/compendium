---
phase: 03-ingestion-provenance-pipeline
plan: 05
type: execute
wave: 4
depends_on:
  - 03-01
  - 03-02
  - 03-03
  - 03-04
files_modified:
  - .planning/phases/03-ingestion-provenance-pipeline/03-PHASE-VERIFICATION.md
autonomous: false
requirements:
  - INGST-01
  - INGST-02
  - INGST-03
  - INGST-04
  - INGST-05
  - INGST-06
  - CMPL-01
  - CMPL-02
  - CMPL-03
  - CMPL-04
  - CMPL-05
  - PROV-01
  - PROV-02
  - PROV-03
  - PROV-04
  - PROV-05

must_haves:
  truths:
    - "A phase-level verification report exists at .planning/phases/03-ingestion-provenance-pipeline/03-PHASE-VERIFICATION.md"
    - "The report confirms both source types (article + journal entry) were ingested successfully"
    - "The report confirms atomic vs paragraph-level extraction visibly differ across the two ingests"
    - "The report confirms at least one merge decision in Plan 03 was made based on actual diff output, not preselection"
    - "The report confirms privacy handling is consistent: cloud_safe overview was not contaminated by local_only content"
    - "The report confirms no existing provenance markers were removed across the entire phase"
    - "A human has reviewed and signed off on the report"
  artifacts:
    - path: ".planning/phases/03-ingestion-provenance-pipeline/03-PHASE-VERIFICATION.md"
      provides: "Phase-level sign-off checklist distinct from per-plan verification"
      contains: "PHASE VERIFICATION"
  key_links:
    - from: ".planning/phases/03-ingestion-provenance-pipeline/03-PHASE-VERIFICATION.md"
      to: "wiki/sources/src-2026-04-10-kahneman-prospect-theory.md, wiki/sources/src-2026-04-10-personal-decision-journal.md"
      via: "cited in the verification report as evidence"
      pattern: "src-2026-04-10-"
---

<objective>
Produce a phase-level verification checklist confirming that Phase 3 as a whole achieved its goals, not just that each plan passed its own per-plan verification. Per review feedback, per-plan grep checks can be satisfied individually while still leaving cross-plan concerns unaddressed (diff-driven merge, privacy consistency, provenance preservation across the whole phase).

Purpose: Close the cross-plan gap identified in REVIEWS.md Consensus Summary item 5. Per-plan verification is necessary but not sufficient. This plan adds a single phase-level sign-off.

Output: `.planning/phases/03-ingestion-provenance-pipeline/03-PHASE-VERIFICATION.md` — a report populated by running cross-plan checks against the executed phase and reviewed by a human.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ingestion-provenance-pipeline/03-CONTEXT.md
@.planning/phases/03-ingestion-provenance-pipeline/03-REVIEWS.md
@.planning/phases/03-ingestion-provenance-pipeline/03-01-SUMMARY.md
@.planning/phases/03-ingestion-provenance-pipeline/03-02-SUMMARY.md
@.planning/phases/03-ingestion-provenance-pipeline/03-03-SUMMARY.md
@.planning/phases/03-ingestion-provenance-pipeline/03-04-SUMMARY.md

<interfaces>
<!-- Expected post-phase state -->
wiki/sources/
  src-2026-04-09-thinking-fast-and-slow-part1.md    # existing, normalized to source_type: book-chapter
  src-2026-04-10-kahneman-prospect-theory.md        # new (Plan 03), privacy: cloud_safe, source_type: article
  src-2026-04-10-personal-decision-journal.md      # new (Plan 04), privacy: local_only, source_type: journal entry

wiki/overviews/
  decision-making.md                                # cloud_safe (unchanged privacy)
  personal-decision-patterns.md                     # new (Plan 04), privacy: local_only

AGENTS.md
  section 10 Pass 2: Claim Granularity Rules (from Plan 01)
  section 10 Pass 3: Append-Then-Synthesize Incremental Update Policy (from Plan 01)

bin/ingest.sh                                        # created in Plan 02, executable
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate phase-level verification report by running cross-plan checks</name>
  <files>.planning/phases/03-ingestion-provenance-pipeline/03-PHASE-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/03-ingestion-provenance-pipeline/03-01-SUMMARY.md
    - .planning/phases/03-ingestion-provenance-pipeline/03-02-SUMMARY.md
    - .planning/phases/03-ingestion-provenance-pipeline/03-03-SUMMARY.md
    - .planning/phases/03-ingestion-provenance-pipeline/03-04-SUMMARY.md
    - wiki/log.md (to extract the Plan 03 log entry with its UPDATED/CREATED page list, which is the primary evidence of diff-driven merge)
    - wiki/index.md
    - AGENTS.md (verify the Plan 01 structural edits survived)
  </read_first>
  <action>
Run each of the cross-plan checks below, capture the actual command output, and write a report at `.planning/phases/03-ingestion-provenance-pipeline/03-PHASE-VERIFICATION.md` with the following structure. For each check, paste the actual output or a 1-line summary plus PASS/FAIL.

**Report template:**

```markdown
# Phase 3 — Phase-Level Verification

**Phase:** 03-ingestion-provenance-pipeline
**Generated:** <ISO date>
**Plans covered:** 03-01, 03-02, 03-03, 03-04

## Cross-plan checks

### 1. Both source types ingested

- Expected: two new source summary files in `wiki/sources/` for 2026-04-10: one article, one journal entry.
- Command: `ls wiki/sources/src-2026-04-10-*.md && grep -H 'source_type:' wiki/sources/src-2026-04-10-*.md`
- Actual: <paste output>
- Result: PASS | FAIL

### 2. Atomic vs paragraph-level granularity visibly differ

- Expected: article source has 8-15 provenance markers; journal source has 4-7.
- Commands:
  - `grep -c '\[prov:src-2026-04-10-kahneman-prospect-theory#' wiki/sources/src-2026-04-10-kahneman-prospect-theory.md`
  - `grep -c '\[prov:src-2026-04-10-personal-decision-journal#para:' wiki/sources/src-2026-04-10-personal-decision-journal.md`
- Actual: article=<N>, journal=<M>
- Check: N >= 8 AND N <= 15 AND M >= 4 AND M <= 7 AND N > M
- Result: PASS | FAIL

### 3. Diff-driven merge (not preselection) in Plan 03

- Expected: the Plan 03 log entry names specific pages UPDATED and, optionally, pages CREATED, with per-page rationale — evidence that the diff pass actually ran and made decisions.
- Command: `awk '/## \[2026-04-10\] ingest \| Prospect Theory/,/## \[/{print}' wiki/log.md`
- Actual: <paste the matching log entry>
- Manual check: does the log entry list specific pages updated and explain why (or list a new concept page created and explain why)? If the log says all four candidate pages were updated with no rationale, that is a weak pass — note it.
- Result: PASS | WEAK PASS | FAIL

### 4. Privacy handling consistent

- Expected: journal source summary and `personal-decision-patterns.md` are `local_only`; `decision-making.md` remains `cloud_safe` and contains no local_only provenance markers.
- Commands:
  - `grep 'privacy:' wiki/sources/src-2026-04-10-personal-decision-journal.md`
  - `grep 'privacy:' wiki/overviews/personal-decision-patterns.md`
  - `grep 'privacy:' wiki/overviews/decision-making.md`
  - `grep -l '\[prov:src-2026-04-10-personal-decision-journal' wiki/`
- Actual: <paste outputs>
- Check: the last command must list ONLY `wiki/sources/src-2026-04-10-personal-decision-journal.md` and `wiki/overviews/personal-decision-patterns.md`. If any other file appears (especially any cloud_safe page), it is a privacy leak.
- Result: PASS | FAIL

### 5. No existing provenance markers removed across the phase

- Expected: no deletions of pre-existing `[prov:src-...]` markers anywhere in `wiki/`.
- Command: `git log --since='2026-04-10' --diff-filter=D -S '[prov:' -- wiki/` AND `git diff <phase-start-commit>..HEAD -- wiki/ | grep -E '^-.*\[prov:src-2026-04-09'`
- Actual: <paste output>
- Check: zero deletions of existing `[prov:src-2026-04-09-*]` markers.
- Result: PASS | FAIL

### 6. AGENTS.md structural edits survived

- Expected: Plan 01's edits (granularity rules, append-then-synthesize policy, book-chapter enum, softened stale wording) are still present after Plans 03/04 executed.
- Commands:
  - `grep -c 'book-chapter' AGENTS.md`
  - `grep -c 'Claim Granularity Rules' AGENTS.md`
  - `grep -c 'Append-Then-Synthesize' AGENTS.md`
  - `grep -c 'per current schema conventions' AGENTS.md`
- Actual: <paste>
- Result: PASS | FAIL

### 7. CLI helper still works

- Expected: `bin/ingest.sh --help` runs cleanly.
- Command: `bash bin/ingest.sh --help >/dev/null 2>&1 && echo OK || echo FAIL`
- Actual: <paste>
- Result: PASS | FAIL

### 8. Source type normalization held

- Expected: the existing example source `src-2026-04-09-thinking-fast-and-slow-part1.md` is still `source_type: book-chapter` and no file has reverted to `source_type: book`.
- Commands:
  - `grep 'source_type: book-chapter' wiki/sources/src-2026-04-09-thinking-fast-and-slow-part1.md`
  - `! grep -R '^source_type: book$' wiki/sources/`
- Actual: <paste>
- Result: PASS | FAIL

## Summary

- Total checks: 8
- Passed: <N>
- Failed: <M>
- Overall: PASS | FAIL | PASS WITH NOTES

## Human sign-off

Reviewer: _______________
Date: _______________
Notes: _______________
```

Fill in every `<paste>` and `<N>` placeholder with real output. If any check fails, stop and escalate — do NOT hand-wave a failure into a pass.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-ingestion-provenance-pipeline/03-PHASE-VERIFICATION.md && grep -q 'Phase 3 — Phase-Level Verification' .planning/phases/03-ingestion-provenance-pipeline/03-PHASE-VERIFICATION.md && grep -c 'PASS\|FAIL' .planning/phases/03-ingestion-provenance-pipeline/03-PHASE-VERIFICATION.md && grep -q 'Total checks: 8' .planning/phases/03-ingestion-provenance-pipeline/03-PHASE-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/03-ingestion-provenance-pipeline/03-PHASE-VERIFICATION.md` exists
    - Contains header `# Phase 3 — Phase-Level Verification`
    - Contains 8 numbered check sections
    - Each check section has a `Result:` line with PASS, FAIL, or (for check 3) WEAK PASS
    - Every `<paste>` placeholder has been replaced with real command output
    - Contains a Summary section with Total/Passed/Failed counts and an Overall line
    - Contains a Human sign-off section (initially blank for reviewer)
    - If any check is FAIL, the Overall line is FAIL (do not mask failures)
  </acceptance_criteria>
  <done>Phase verification report written with real output from all 8 cross-plan checks, summary populated, ready for human sign-off.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human sign-off on phase verification report</name>
  <files>.planning/phases/03-ingestion-provenance-pipeline/03-PHASE-VERIFICATION.md</files>
  <what-built>
`.planning/phases/03-ingestion-provenance-pipeline/03-PHASE-VERIFICATION.md` — 8 cross-plan checks with actual output captured.
  </what-built>
  <action>
This is a human verification checkpoint. The only modification allowed is filling in the Human sign-off section (reviewer name, date, optional notes). The executor MUST pause here and present the report to the user. The user re-runs each check's command independently, compares to the pasted output, judges the Overall PASS/FAIL, and either fills in the sign-off or requests re-execution.
  </action>
  <how-to-verify>
Open `.planning/phases/03-ingestion-provenance-pipeline/03-PHASE-VERIFICATION.md` and read it end to end. For each of the 8 checks:

1. Confirm the command output pasted into the report matches what you get when you re-run the command yourself.
2. Confirm the PASS/FAIL judgment is reasonable given the output.
3. For check 3 (diff-driven merge), read the Plan 03 log entry carefully: does it genuinely show the diff pass made decisions, or does it look like all four candidate pages were mechanically updated? If mechanical, mark WEAK PASS with a note and consider whether Plan 03 needs re-execution.
4. For check 4 (privacy), verify that `grep -l '\[prov:src-2026-04-10-personal-decision-journal' wiki/` lists ONLY the two expected files. Any third file is a privacy leak and the check must be FAIL.
5. For check 5 (no provenance removed), verify the git diff output shows zero `-` lines for existing prov markers.

If the Overall line is PASS, fill in the Human sign-off section with your name and the current date and approve.

If the Overall line is FAIL or any individual check looks wrong, do NOT approve — describe what needs to be fixed.
  </how-to-verify>
  <verify>
    <automated>MANUAL — this is a human verification checkpoint. The executor MUST pause and wait for the user to independently re-run each check and sign off. No automated command replaces the human review.</automated>
  </verify>
  <done>Human reviewer has re-run the cross-plan checks, confirmed the pasted output matches, judged the Overall result, and either filled in the Human sign-off section with name+date or sent specific checks back for re-execution.</done>
  <resume-signal>Type `approved` with your name (to fill in the sign-off section), or describe which check failed and what must be re-executed.</resume-signal>
</task>

</tasks>

<verification>
- `.planning/phases/03-ingestion-provenance-pipeline/03-PHASE-VERIFICATION.md` exists
- All 8 checks have a Result line
- Overall summary line is present
- Human sign-off has been obtained (Task 2 approved)
</verification>

<success_criteria>
Phase 3 has a single cross-plan sign-off document confirming that the phase as a whole achieved its goals, not just that each plan individually passed its grep checks. The document captures real command output for each check, uses explicit PASS/FAIL judgments, and has a human sign-off section. This closes the cross-plan gap flagged in REVIEWS.md Consensus Summary.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ingestion-provenance-pipeline/03-05-SUMMARY.md`
</output>
