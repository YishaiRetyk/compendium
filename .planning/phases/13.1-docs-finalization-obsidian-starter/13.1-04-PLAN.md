---
phase: 13.1-docs-finalization-obsidian-starter
plan: 04
type: execute
wave: 2
depends_on: [02, 03]
files_modified:
  - docs/reference/schema-tour.md
  - docs/reference/privacy-model.md
  - docs/reference/examples.md
  - docs/reference/agent-parity.md
autonomous: true
requirements: [DEBT-02, DEBT-01]
user_setup: []

must_haves:
  truths:
    - "The three never-populated stubs (schema-tour, privacy-model, examples) are fully authored against the shipped v1.1 surface"
    - "examples.md documents the examples/dataview-fixtures/ inventory + the expected row count per canonical Dataview query, and CORRECTS the stale .obsidianignore 'hides from indexing' claim"
    - "agent-parity.md exists as the DEBT-02 deliverable: structural-equivalence rubric + how to re-run the Codex parity diff"
    - "No stub status lines remain in the three authored docs"
    - "All authored docs use placeholders only (D-12); examples/kahneman/ is the only concrete cluster"
  artifacts:
    - path: "docs/reference/schema-tour.md"
      provides: "Full schema walkthrough (§5 frontmatter + §6 provenance)"
      contains: "## See also"
    - path: "docs/reference/privacy-model.md"
      provides: "Privacy tiers + §13 precedence + CI enforcement"
      contains: "local_only"
    - path: "docs/reference/examples.md"
      provides: "examples/ usage + dataview-fixtures count matrix + corrected .obsidianignore description"
      contains: "examples/dataview-fixtures"
    - path: "docs/reference/agent-parity.md"
      provides: "DEBT-02 structural-equivalence rubric + re-run instructions"
      contains: "structural equivalence"
  key_links:
    - from: "docs/reference/examples.md"
      to: "examples/dataview-fixtures/ (Plan 02)"
      via: "expected-count matrix per canonical query"
      pattern: "examples/dataview-fixtures"
    - from: "docs/reference/agent-parity.md"
      to: "13.1-VERIFICATION.md DEBT-02 evidence (Plan 03)"
      via: "structural-diff method + golden inventory"
      pattern: "examples/kahneman"
---

<objective>
Fully author the three never-populated `docs/reference/` stubs (schema-tour, privacy-model, examples) and create the new `agent-parity.md` (the DEBT-02 deliverable). `examples.md` folds in the `examples/dataview-fixtures/` expected-count matrix (from Plan 02) and corrects the stale `.obsidianignore` claim; `agent-parity.md` captures the structural-equivalence rubric + how to re-run the Codex parity diff (from Plan 03).

Purpose: SC4 — every reference file is filled out with accurate v1.1 descriptions. (Accuracy-pass of the substantial docs + index reconciliation is Plan 05.)
Output: 3 authored stubs + 1 new agent-parity.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-CONTEXT.md
@.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-PATTERNS.md
@.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-RESEARCH.md
@.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-02-SUMMARY.md
@.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-03-SUMMARY.md

<interfaces>
<!-- House style (Shared Pattern B): H1 + optional > blockquote + ## TL;DR + optional       -->
<!-- source-of-truth pointer + ## sections + ## See also footer (links to ../../AGENTS.md,    -->
<!-- ../README.md, sibling docs). Analogs: ci.md, three-layer-model.md, dataview-queries.md.   -->

schema-tour.md   → analog three-layer-model.md; source of truth = CLAUDE.md §5 (frontmatter) + §6 (provenance/claim syntax)
privacy-model.md → analog three-layer-model.md; source of truth = CLAUDE.md §13 (reuse the 7-row decision table verbatim — it is neutral); reference bin/check-privacy.sh + cross-link ci.md
examples.md      → analogs dataview-queries.md + examples/kahneman/README.md
agent-parity.md  → analog ci.md (rubric table + how-to-run)

Stub status lines to REMOVE on authoring (RESEARCH "Deprecated"):
  "> Status: stub — populated in v1.1 Phase 12."

.obsidianignore CORRECTION (Shared Pattern C / Pitfall 1): it is NOT Obsidian-native. Live
.obsidian/app.json is {} so examples/ IS indexed by Dataview today. Describe .obsidianignore as a
release-manifest + graph-hygiene CONVENTION (consumed by bin/release.sh + docs), NOT an
Obsidian-enforced indexing exclusion. Real exclusion mechanism = Settings → Files & Links →
Excluded files (app.json userIgnoreFilters).

DEBT-02 golden inventory (VERIFIED live, page-type subdirs only): 1 entity, 3 concepts, 1 comparison, 1 overview, 2 sources.
(README.md and log.md carry type: overview in their own frontmatter but are NOT wiki pages — never count them.)
Substantive source = sources/2026/2026-04/2026-04-10-kahneman-prospect-theory/source.md (NEVER the placeholder).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author schema-tour.md and privacy-model.md</name>
  <read_first>
    - docs/reference/schema-tour.md and docs/reference/privacy-model.md (current stubs to replace)
    - docs/reference/three-layer-model.md (house-style analog: prose + tables + See also)
    - CLAUDE.md §5 (frontmatter base fields + validation checklist), §6 (provenance/epistemics/staleness), §13 (privacy three-level precedence + 7-row decision table)
    - .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-PATTERNS.md (schema-tour + privacy-model analog sections, Shared Pattern B + D)
  </read_first>
  <files>docs/reference/schema-tour.md, docs/reference/privacy-model.md</files>
  <action>
    Replace both stubs with full content (remove the `> Status: stub` lines):
    schema-tour.md: a walkthrough of the frontmatter schema (§5 base fields, the per-type additions, the validation checklist) and how claims cite sources (§6 `[prov:source_id#locator|support_type|checked_at]` grammar, locator types, epistemic markers, the page-marker `<!-- page: N -->` convention). Open with a source-of-truth pointer to `[AGENTS.md §5](../../AGENTS.md)` / §6 ("...§5 wins and this page is the bug"). Use ONLY placeholders (`<concept-slug>`, `<source-id>`, `<YYYY-MM-DD-slug>`) or sanctioned `examples/kahneman/` references for any concrete example. End with `## See also`.
    privacy-model.md: explain `local_only`/`cloud_safe` tiers, the §13 three-level precedence, fail-closed default, and CI enforcement via `bin/check-privacy.sh` (cross-link `ci.md`). Reuse the §13 7-row decision table VERBATIM (it is already neutral). Open with a source-of-truth pointer to `[AGENTS.md §13](../../AGENTS.md)`. End with `## See also`.
    Neutrality (D-12): placeholders only; gate is the backstop.
  </action>
  <acceptance_criteria>
    - `! grep -rl 'Status: stub' docs/reference/schema-tour.md docs/reference/privacy-model.md` (no stub lines remain)
    - `grep -q '## See also' docs/reference/schema-tour.md` AND `grep -q '## See also' docs/reference/privacy-model.md`
    - `grep -q 'prov:' docs/reference/schema-tour.md` (provenance grammar covered)
    - `grep -q 'local_only' docs/reference/privacy-model.md` AND `grep -q 'cloud_safe' docs/reference/privacy-model.md`
    - `grep -qi 'check-privacy' docs/reference/privacy-model.md` (CI enforcement referenced)
    - each file is materially longer than the stub (>= 25 lines): `[ "$(wc -l < docs/reference/schema-tour.md)" -ge 25 ]` and same for privacy-model.md
    - `bash bin/check-neutrality.sh` exits 0
  </acceptance_criteria>
  <verify>
    <automated>! grep -l 'Status: stub' docs/reference/schema-tour.md docs/reference/privacy-model.md && grep -q '## See also' docs/reference/schema-tour.md && grep -q 'local_only' docs/reference/privacy-model.md && [ "$(wc -l < docs/reference/schema-tour.md)" -ge 25 ] && bash bin/check-neutrality.sh</automated>
  </verify>
  <done>Both stubs fully authored against §5/§6/§13, stub lines removed, See-also footers present, neutrality exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Author examples.md (with dataview-fixtures count matrix + .obsidianignore correction)</name>
  <read_first>
    - docs/reference/examples.md (current stub — note its stale .obsidianignore claim to CORRECT)
    - docs/reference/dataview-queries.md (query-block house style) and examples/kahneman/README.md (cluster description)
    - .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-02-SUMMARY.md (the per-query expected-count matrix from Plan 02 fixtures)
    - .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-PATTERNS.md (examples.md analog + Shared Pattern C) and 13.1-RESEARCH.md (Pitfall 1)
    - CLAUDE.md §5 (example: true lint-skip)
  </read_first>
  <files>docs/reference/examples.md</files>
  <action>
    Replace the examples.md stub (remove `> Status: stub`) with three parts (D-10/D-11):
    1. How `examples/` works + the `example: true` lint-skip convention (CLAUDE.md §5): reference-only clusters, excluded from the published wiki, skipped by lint.
    2. The CORRECTED `.obsidianignore` description (Shared Pattern C / Pitfall 1): state plainly that `.obsidianignore` is NOT an Obsidian-native feature; the live `.obsidian/app.json` is empty so Obsidian/Dataview index `examples/` today; `.obsidianignore` is a release-manifest + graph-hygiene CONVENTION (consumed by `bin/release.sh` + docs). The real exclusion mechanism is Settings → Files & Links → Excluded files (`app.json` `userIgnoreFilters`). Replace any prior "hides examples/ from indexing" wording.
    3. The `examples/dataview-fixtures/` inventory + the EXPECTED ROW COUNT per canonical Dataview query: write the five fixture-scoped query variants (`FROM "examples/dataview-fixtures"`) using the dataview-queries.md fenced-block style, each annotated with its expected count drawn verbatim from 13.1-02-SUMMARY's matrix. Note that >=2 fixtures carry `bootstrap_stage` so one open exercises fresh-starter + post-bootstrap rendering.
    End with `## See also`. Neutrality (D-12): placeholders + sanctioned kahneman references only.
  </action>
  <acceptance_criteria>
    - `! grep -l 'Status: stub' docs/reference/examples.md`
    - `grep -q 'examples/dataview-fixtures' docs/reference/examples.md` (fixture set documented)
    - `grep -q 'example: true' docs/reference/examples.md` (lint-skip convention covered)
    - The .obsidianignore correction is present: `grep -qi 'userIgnoreFilters\|Excluded files' docs/reference/examples.md` AND the doc does NOT assert .obsidianignore hides examples from indexing — `! grep -qi 'obsidianignore.*hides.*index' docs/reference/examples.md`
    - >=5 dataview fenced blocks scoped to the fixtures: `grep -c 'FROM "examples/dataview-fixtures"' docs/reference/examples.md` >= 5
    - Each fixture-scoped query has an expected count annotation: the doc contains the word "expected" near the query blocks
    - `grep -q '## See also' docs/reference/examples.md`
    - `bash bin/check-neutrality.sh` exits 0
  </acceptance_criteria>
  <verify>
    <automated>! grep -l 'Status: stub' docs/reference/examples.md && grep -q 'examples/dataview-fixtures' docs/reference/examples.md && [ "$(grep -c 'FROM "examples/dataview-fixtures"' docs/reference/examples.md)" -ge 5 ] && grep -qi 'Excluded files' docs/reference/examples.md && grep -q '## See also' docs/reference/examples.md && bash bin/check-neutrality.sh</automated>
  </verify>
  <done>examples.md fully authored: examples/ usage + corrected .obsidianignore + the five fixture-scoped queries with documented expected counts; stub line removed; neutrality exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Author agent-parity.md (DEBT-02 deliverable)</name>
  <read_first>
    - docs/reference/ci.md (rubric-table + how-to-run house style analog)
    - .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-03-SUMMARY.md (the DEBT-02 structural-diff matrix + prose-variance findings from Plan 03)
    - .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-PATTERNS.md (agent-parity.md analog + Shared Pattern E) and 13.1-RESEARCH.md (Pattern 2 codex exec, Pattern 3 structural diff)
  </read_first>
  <files>docs/reference/agent-parity.md</files>
  <action>
    Create `docs/reference/agent-parity.md` following the ci.md rubric/how-to-run house style (Shared Pattern B). Required content (D-07/D-08):
    1. `# Agent Parity` + a `> Reference documentation for ...` blockquote + `## TL;DR`.
    2. The STRUCTURAL-EQUIVALENCE TOLERANCE RUBRIC: PASS = same page set, same page types, same provenance source IDs + locator targets, valid frontmatter. Claim prose/wording may differ freely (noted, not a failure). Present as a clear policy table or bullet list.
    3. HOW TO RE-RUN the Codex parity diff: the `codex exec` invocation (RESEARCH Pattern 2, absolute codex path, `-C $SCRATCH -s workspace-write --skip-git-repo-check -o ...`) + the structural-diff method (RESEARCH Pattern 3: `pagetypes()` / `provtargets()` helpers). Golden = `examples/kahneman/` (1 entity, 3 concepts, 1 comparison, 1 overview, 2 sources — VERIFIED live multiset, page-type subdirs only; README.md/log.md carry type: overview but are not wiki pages). The `pagetypes()` helper must branch on whether `$1/wiki` exists, since the golden is FLAT (no wiki/ subdir) while scratch wikis nest under wiki/. Substantive source = the prospect-theory raw source (NOT the placeholder).
    4. A diff-presentation table template (rows = {page-set, page-types, prov-source-IDs, prov-locator-targets, frontmatter-valid}; cols = {Claude vs golden, Codex vs golden, Claude vs Codex}; cells = MATCH / DIFFERS(noted)). Optionally cite the actual Plan-03 result.
    5. `## See also` footer (link to ../../AGENTS.md, ../README.md, index.md, ../../.planning/.../13.1-VERIFICATION.md or just the verification concept).
    Neutrality (D-12): placeholders + sanctioned kahneman references only.
  </action>
  <acceptance_criteria>
    - `docs/reference/agent-parity.md` exists
    - `grep -qi 'structural equivalence' docs/reference/agent-parity.md` (rubric present)
    - `grep -q 'codex exec' docs/reference/agent-parity.md` (re-run invocation present)
    - `grep -q 'examples/kahneman' docs/reference/agent-parity.md` (golden referenced)
    - `grep -q 'prospect-theory' docs/reference/agent-parity.md` (substantive source named, not the placeholder) AND `! grep -q 'thinking-fast-and-slow-part1' docs/reference/agent-parity.md`
    - a diff-presentation table is present: `grep -qi 'prov.*locator\|page.set\|page.types' docs/reference/agent-parity.md`
    - `grep -q '## See also' docs/reference/agent-parity.md`
    - `bash bin/check-neutrality.sh` exits 0
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/reference/agent-parity.md && grep -qi 'structural equivalence' docs/reference/agent-parity.md && grep -q 'codex exec' docs/reference/agent-parity.md && grep -q 'examples/kahneman' docs/reference/agent-parity.md && ! grep -q 'thinking-fast-and-slow-part1' docs/reference/agent-parity.md && grep -q '## See also' docs/reference/agent-parity.md && bash bin/check-neutrality.sh</automated>
  </verify>
  <done>agent-parity.md authored with the structural-equivalence rubric, the codex exec re-run instructions, the golden inventory, and the diff-presentation table; uses the substantive source not the placeholder; neutrality exits 0. (index.md registration happens in Plan 05.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → `docs/` (template-public) | Authored docs are published in the neutral template; private wiki terms must not leak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.1-09 | Information Disclosure | `docs/reference/{schema-tour,privacy-model,examples,agent-parity}.md` | mitigate | D-12: placeholders + sanctioned `examples/kahneman/` references only. `bash bin/check-neutrality.sh` exits 0 (write-time discipline primary; gate backstop). [HIGH — block on neutrality failure.] |
| T-13.1-10 | Tampering | drift between docs and CLAUDE.md source of truth | mitigate | Each authored doc opens with a source-of-truth pointer to the governing CLAUDE.md section; privacy-model reuses the §13 decision table verbatim rather than paraphrasing. |
</threat_model>

<verification>
- `bash bin/lint.sh --category yaml,provenance,orphan,crossref` clean
- `bash bin/check-neutrality.sh` exits 0
- No `Status: stub` lines remain in the three authored docs
- examples.md fixture-scoped query count >= 5 with documented expected counts
</verification>

<success_criteria>
- schema-tour, privacy-model, examples fully authored (no stub lines) (SC4)
- examples.md folds in the dataview-fixtures count matrix + corrects the .obsidianignore claim (D-11 / Pitfall 1)
- agent-parity.md ships the structural-equivalence rubric + re-run instructions (DEBT-02 / D-11)
- Neutrality clean (D-12)
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-04-SUMMARY.md`
</output>
