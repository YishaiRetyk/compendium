---
phase: 13.1-docs-finalization-obsidian-starter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - schema/obsidian/entity.md
  - schema/obsidian/concept.md
  - schema/obsidian/source-summary.md
  - schema/obsidian/comparison.md
  - schema/obsidian/overview.md
  - schema/obsidian/decision.md
  - docs/reference/obsidian-starter.md
autonomous: true
requirements: [OBSID-01, OBSID-02, OBSID-03]
user_setup: []

must_haves:
  truths:
    - "Six token-ized page templates exist under schema/obsidian/, one per supported page type"
    - "Each Obsidian template has the same frontmatter key order and section headers as its schema/templates/ sibling (no field/section drift)"
    - "Only title/created_at/updated_at carry auto-fill tokens; id/summary/tags stay user-typed"
    - "decision.md preserves its asymmetric extra fields; the other five templates do NOT carry them"
    - "A reference doc explains day-1 page-creation: what auto-fills vs what the user hand-types, with a Setup-once section"
    - "The starter ships zero dashboards, dataviewjs, hotkeys, or CSS (OBSID-03 negative assertion)"
  artifacts:
    - path: "schema/obsidian/entity.md"
      provides: "Token-ized entity template"
      contains: "{{title}}"
    - path: "schema/obsidian/decision.md"
      provides: "Token-ized decision template with asymmetric fields preserved"
      contains: "trigger_type:"
    - path: "docs/reference/obsidian-starter.md"
      provides: "OBSID-02 reference doc"
      contains: "Setup"
  key_links:
    - from: "schema/obsidian/*.md"
      to: "schema/templates/*.md"
      via: "field-list + section-header drift-guard diff"
      pattern: "diff.*schema/templates.*schema/obsidian"
    - from: "docs/reference/obsidian-starter.md"
      to: "schema/obsidian/"
      via: "Setup-once section referencing the template folder"
      pattern: "schema/obsidian"
---

<objective>
Ship the minimal Obsidian starter: six token-ized page-type templates under `schema/obsidian/` (derived without drift from `schema/templates/`) plus an OBSID-02 reference doc explaining day-1 page-creation ergonomics. Page-creation ergonomics ONLY — no dashboards, views, hotkeys, or CSS (OBSID-03).

Purpose: Close the day-1 page-creation gap so a user can insert a schema-correct page skeleton via Obsidian's built-in Templates core plugin, with `title`/`created_at`/`updated_at` auto-filling.
Output: `schema/obsidian/{entity,concept,source-summary,comparison,overview,decision}.md` + `docs/reference/obsidian-starter.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-CONTEXT.md
@.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-PATTERNS.md
@.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-RESEARCH.md

<interfaces>
<!-- Canonical templates to derive from. Token-ize ONLY title/created_at/updated_at per D-01. -->
<!-- Preserve key order, section headers, and the FORBIDDEN PATTERNS comment block verbatim. -->

schema/templates/ base frontmatter key order (entity/concept/overview/comparison — IDENTICAL):
  id, title, type, status, summary, created_at, updated_at, sources, epistemic_status,
  tags, domains, supersedes, superseded_by, privacy, aliases

source-summary.md adds, after aliases:
  path, url, content_hash, ingested_at, source_type, compilation_status,
  compiled_against_hash, compiled_targets

decision.md ASYMMETRY (the ONLY template with these — do NOT add to the other five):
  epistemic_status: sourced   (non-empty default)
  privacy: cloud_safe         (non-empty default)
  has_contradictions: false
  knowledge_domain: ""
  trigger_type:
  affected_pages: []

Section orders (preserve exactly):
  entity/concept/overview: TL;DR -> Key Facts -> Detail -> Related Pages -> Sources
  comparison:              TL;DR -> Bottom Line -> Comparison Table -> Detailed Comparison -> Sources
  source-summary:          TL;DR -> Key Takeaways -> Extracted Claims -> Notes -> Source Metadata
  decision:                TL;DR -> Decision -> Why -> Alternatives Considered -> Consequences -> Affected Pages -> Sources
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the six token-ized schema/obsidian/ templates</name>
  <read_first>
    - schema/templates/entity.md, concept.md, overview.md, comparison.md, source-summary.md, decision.md (the six canonical siblings — exact source to copy from)
    - .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-PATTERNS.md (Pattern 1 + the decision.md asymmetry guard, lines ~34-193)
    - CLAUDE.md §3 (neutrality — placeholders only) and §5 (frontmatter base fields)
  </read_first>
  <files>schema/obsidian/entity.md, schema/obsidian/concept.md, schema/obsidian/overview.md, schema/obsidian/comparison.md, schema/obsidian/source-summary.md, schema/obsidian/decision.md</files>
  <action>
    Create the `schema/obsidian/` directory and author six templates, each derived from its `schema/templates/` sibling by copying the file VERBATIM and applying EXACTLY these three token substitutions (D-01/D-02):
      - `title:` → `title: {{title}}`
      - `created_at:` → `created_at: {{date:YYYY-MM-DD}}`
      - `updated_at:` → `updated_at: {{date:YYYY-MM-DD}}`
    Make NO other changes. Specifically:
      - Keep `id:`, `summary: ""`, `tags: []`, `epistemic_status:`, `privacy:` UNFILLED (user hand-types these per D-01 — do not add tokens or defaults).
      - Preserve the exact frontmatter key order of each sibling.
      - Preserve the FORBIDDEN PATTERNS HTML comment block verbatim.
      - Preserve every `## ` section header AND each section's guidance HTML comment verbatim.
    Per-file specifics:
      - entity.md / concept.md / overview.md: keep `type:` = entity/concept/overview respectively. Sections: TL;DR -> Key Facts -> Detail -> Related Pages -> Sources.
      - comparison.md: sections TL;DR -> Bottom Line -> Comparison Table -> Detailed Comparison -> Sources.
      - source-summary.md: include the source-specific tail (path, url, content_hash, ingested_at, source_type, compilation_status, compiled_against_hash, compiled_targets) UNCHANGED — do NOT token-ize any of them (ingest populates them). Sections: TL;DR -> Key Takeaways -> Extracted Claims -> Notes -> Source Metadata.
      - decision.md (ASYMMETRY GUARD): keep `epistemic_status: sourced` and `privacy: cloud_safe` as non-empty defaults; keep `has_contradictions: false`, `knowledge_domain: ""`, `trigger_type:`, `affected_pages: []`. These four fields appear ONLY here. Keep the decision FORBIDDEN-PATTERNS comment variant (mentions `affected_pages[]`). Sections: TL;DR -> Decision -> Why -> Alternatives Considered -> Consequences -> Affected Pages -> Sources.
    Neutrality (D-12): templates contain NO concrete content — only empty fields, tokens, and the existing guidance comments. No real wiki slugs/terms.
  </action>
  <acceptance_criteria>
    - `ls schema/obsidian/entity.md schema/obsidian/concept.md schema/obsidian/source-summary.md schema/obsidian/comparison.md schema/obsidian/overview.md schema/obsidian/decision.md` all exist
    - `grep -c '{{title}}' schema/obsidian/entity.md` == 1 AND `grep -c '{{date:YYYY-MM-DD}}' schema/obsidian/entity.md` == 2
    - DRIFT-GUARD (field list + section headers) passes for all six with EMPTY diff output:
      `for t in entity concept source-summary comparison overview decision; do diff <(grep -E '^[a-z_]+:' schema/templates/$t.md) <(grep -E '^[a-z_]+:' schema/obsidian/$t.md | sed 's/ {{.*}}//'); diff <(grep '^## ' schema/templates/$t.md) <(grep '^## ' schema/obsidian/$t.md); done` produces no output and exits 0
    - `grep -c 'trigger_type:' schema/obsidian/decision.md` == 1 AND `grep -L 'trigger_type:' schema/obsidian/entity.md schema/obsidian/concept.md schema/obsidian/overview.md schema/obsidian/comparison.md schema/obsidian/source-summary.md` lists all five (none carry it)
    - `grep -c 'privacy: cloud_safe' schema/obsidian/decision.md` == 1 AND `grep 'epistemic_status: sourced' schema/obsidian/decision.md` matches
    - OBSID-03 negative: `! ls schema/obsidian/*.css schema/obsidian/*dashboard* 2>/dev/null` AND `! grep -rl 'dataviewjs\|hotkey' schema/obsidian/`
    - `bash bin/check-neutrality.sh` exits 0
  </acceptance_criteria>
  <verify>
    <automated>for t in entity concept source-summary comparison overview decision; do diff <(grep -E '^[a-z_]+:' schema/templates/$t.md) <(grep -E '^[a-z_]+:' schema/obsidian/$t.md | sed 's/ {{.*}}//') || exit 1; diff <(grep '^## ' schema/templates/$t.md) <(grep '^## ' schema/obsidian/$t.md) || exit 1; done; bash bin/check-neutrality.sh</automated>
  </verify>
  <done>Six token-ized templates exist; drift-guard produces zero diff for all six; decision asymmetry preserved; OBSID-03 negative assertions hold; neutrality exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Author docs/reference/obsidian-starter.md (OBSID-02)</name>
  <read_first>
    - docs/reference/ci.md (house style: TL;DR + source-of-truth pointer + Setup section + See also)
    - docs/reference/three-layer-model.md and docs/reference/dataview-queries.md (See-also footer shape)
    - CLAUDE.md §15 (Obsidian tooling) and §5 (frontmatter) — the sections this doc points to as source of truth
    - .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-PATTERNS.md (the obsidian-starter.md analog section, Shared Pattern B)
  </read_first>
  <files>docs/reference/obsidian-starter.md</files>
  <action>
    Author `docs/reference/obsidian-starter.md` following the reference-doc house style (Shared Pattern B). Required content:
      1. `# Obsidian Starter` H1 + a `> Reference documentation for ...` one-line blockquote.
      2. `## TL;DR` — 3-5 bullets: what ships (six page-type templates under `schema/obsidian/`), what auto-fills, that it is page-creation ergonomics only.
      3. A source-of-truth pointer blockquote pointing at `[AGENTS.md §15](../../AGENTS.md)` (Obsidian tooling) and §5 (frontmatter) using the ci.md pattern ("...§15 wins and this page is the bug").
      4. `## Setup (once)` — walk the user through enabling the built-in Templates CORE plugin and pointing its template folder at `schema/obsidian/` via Settings → Templates. State explicitly that the starter does NOT mutate `.obsidian/` config (D-03) — the user wires it once in their own vault. Works identically for fresh-starter and brownfield-with-existing-vault users.
      5. `## What auto-fills vs what you type` — auto-fills via tokens: `title` (`{{title}}`), `created_at`/`updated_at` (`{{date:YYYY-MM-DD}}`). User hand-types: `id`, `summary`, `tags`, `privacy`, `epistemic_status` (and source/decision-specific fields).
      6. `## Scope` — OBSID-03 statement: page-creation ergonomics ONLY; NO dashboards, workflow-specific Dataview surfaces, hotkey bundles, custom CSS/themes, or prescribed review workflows (deferred to backlog Phase 999.6).
      7. `## See also` footer with relative links to `../../AGENTS.md`, `../README.md`, `index.md`.
    Neutrality (D-12): placeholders only (`<page-title>`, `<concept-slug>`, etc.); no real wiki slugs/terms. Do NOT pre-wire any `.obsidian/` setting.
  </action>
  <acceptance_criteria>
    - `docs/reference/obsidian-starter.md` exists
    - `grep -q 'Setup' docs/reference/obsidian-starter.md` (Setup-once section present)
    - `grep -q 'schema/obsidian' docs/reference/obsidian-starter.md`
    - `grep -q '{{title}}' docs/reference/obsidian-starter.md` AND `grep -q '{{date:YYYY-MM-DD}}' docs/reference/obsidian-starter.md`
    - `grep -qi 'dashboard\|hotkey\|CSS' docs/reference/obsidian-starter.md` (OBSID-03 scope statement present, naming what is NOT shipped)
    - `grep -q '## See also' docs/reference/obsidian-starter.md`
    - `bash bin/check-neutrality.sh` exits 0
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/reference/obsidian-starter.md && grep -q 'Setup' docs/reference/obsidian-starter.md && grep -q 'schema/obsidian' docs/reference/obsidian-starter.md && grep -q '{{date:YYYY-MM-DD}}' docs/reference/obsidian-starter.md && grep -q '## See also' docs/reference/obsidian-starter.md && bash bin/check-neutrality.sh</automated>
  </verify>
  <done>obsidian-starter.md exists with Setup-once, auto-fill-vs-user-typed, OBSID-03 scope, and See-also sections; neutrality exits 0. (index.md registration of this doc happens in Plan 04.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → template-public paths (`schema/`, `docs/`) | Authored content crosses into files published in the neutral template; private wiki terms must not leak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.1-01 | Information Disclosure | `schema/obsidian/*.md`, `docs/reference/obsidian-starter.md` | mitigate | Per D-12/CLAUDE.md §3: placeholders only; templates contain only empty fields + tokens + existing guidance comments. Final gate `bash bin/check-neutrality.sh` exits 0 (write-time discipline is the primary defense; gate is the backstop). |
| T-13.1-02 | Tampering | `schema/obsidian/` vs `schema/templates/` drift | mitigate | OBSID-01 drift-guard diff (field list order-sensitive + `## ` headers) must produce zero output for all six templates; derive each file from its own canonical sibling, never a shared block. |
</threat_model>

<verification>
- `bash bin/lint.sh --category yaml,provenance,orphan,crossref` clean (templates are not wiki pages but lint must not regress)
- `bash bin/check-neutrality.sh` exits 0
- Drift-guard (Task 1 acceptance) produces zero diff for all six templates
- `bash bin/sync-claude.sh --check` exits 0 (this plan does not edit CLAUDE.md/AGENTS.md, but confirm no incidental drift)
</verification>

<success_criteria>
- Six `schema/obsidian/` templates exist with zero field/section drift vs `schema/templates/` siblings (OBSID-01)
- `docs/reference/obsidian-starter.md` explains setup + generated-vs-user-owned (OBSID-02)
- No dashboards/views/hotkeys/CSS shipped (OBSID-03)
- Neutrality gate exits 0 (D-12)
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-01-SUMMARY.md`
</output>
