---
phase: 13.1-docs-finalization-obsidian-starter
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-VERIFICATION.md
  - wiki/
autonomous: false
requirements: [DEBT-02, DEBT-04, DEBT-01]
user_setup:
  - service: codex-cli
    why: "DEBT-02 Codex-side parity ingest runs as the user's authenticated Codex session"
    env_vars:
      - name: CODEX_HOME
        source: "Existing Codex login on this machine (codex auth); no new secret to create"

must_haves:
  truths:
    - "PRE-CODEX SEED GUARD (mechanical, fail-closed): a deterministic guard resolves effective privacy for EVERY file under $SCRATCH/sources and exits non-zero on any non-cloud_safe file BEFORE codex exec runs — the local_only personal-decision-journal sharing sources/2026/2026-04/ can never reach the cloud"
    - "DEBT-02: both Claude and Codex re-ingest the substantive prospect-theory raw source into scratch wikis; CLAUDE-vs-CODEX agreement is the PRIMARY parity verdict; the golden comparison targets the PROSPECT-THEORY SUBSET of examples/kahneman/, not the full two-source cluster (a single source cannot reproduce a two-source synthesis)"
    - "DEBT-02: structural-equivalence verdict recorded (same page set, page types, provenance source IDs + locator targets, valid frontmatter); prose variance noted as expected, not a failure"
    - "DEBT-04: a genuine cross-source-synthesis write-back is executed against the LIVE wiki/ as an UPDATE to a named owning page (validate-op has no CREATE op — UPDATE is the validatable path), adds >=2 [prov:] markers from >=2 DISTINCT source_ids, passes validate-op, and is logged in wiki/log.md as a NEW today-dated WRITE-BACK line"
    - "DEBT-01: a precise Obsidian render checklist + a capture slot marked 'awaiting user render' is shipped in 13.1-VERIFICATION.md (phase NOT blocked on the GUI)"
    - "codex exec is recorded as CLOUD-BY-DEFAULT egress (per §11.7 verifier-locality): the seed guard is the SINGLE mandatory egress defense; -C/-s bound only LOCAL writes, not network egress"
    - "Scratch dirs are created under mktemp -d with a trap teardown (EXIT INT TERM); torn down by absolute path after diff capture; no scratch output committed"
  artifacts:
    - path: ".planning/phases/13.1-docs-finalization-obsidian-starter/13.1-VERIFICATION.md"
      provides: "Verification capture (Phase 13 mirror format) for DEBT-01/02/04"
      contains: "awaiting user render"
  key_links:
    - from: "DEBT-04 write-back"
      to: "wiki/log.md"
      via: "query workflow §11.2 log entry (NEW today-dated line, diffed vs HEAD~1)"
      pattern: "WRITE-BACK"
    - from: "DEBT-02 scratch outputs"
      to: "examples/kahneman/ prospect-theory subset"
      via: "structural-equivalence diff (page types + prov targets); Claude-vs-Codex primary"
      pattern: "prov:"
---

<objective>
Execute the two agent-runnable deferred v1.0 verifications and capture all results (plus the prepared DEBT-01 render checklist) into `13.1-VERIFICATION.md` using the Phase 13 mirror format:
- DEBT-02 (Codex agent-parity): Claude AND Codex each re-ingest the substantive prospect-theory raw source into an isolated scratch wiki. The PRIMARY parity verdict is CLAUDE-vs-CODEX structural equivalence (both run the same single source — this IS reproducible). The golden comparison targets the PROSPECT-THEORY SUBSET of `examples/kahneman/` (the pages derivable from that one source), NOT the full two-source cluster — a single-source ingest cannot reproduce a two-source synthesis (REVIEWS HIGH "DEBT-02 structurally impossible from a single seeded source"). Tolerance = structural equivalence, D-07.
- DEBT-04 (genuine write-back): run a cross-source-synthesis query against the LIVE `wiki/` that triggers a real WRITE-BACK as an UPDATE to a named owning page (validate-op has NO CREATE op — UPDATE is the only validatable branch), producing >=2 new [prov:] markers from >=2 distinct source_ids, validate-op PASS, logged per §11.2 as a NEW today-dated line.
- DEBT-01 (Obsidian render): ship a precise render checklist + a capture slot marked "awaiting user render" — prepared-and-await per D-09 (Phase 13.2 re-runs/audits).

Purpose: Close v1.0's three deferred verifications end-to-end (except the single GUI step the user owns).
Output: `13.1-VERIFICATION.md`; a durable DEBT-04 write-back commit to `wiki/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-CONTEXT.md
@.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-RESEARCH.md
@.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-PATTERNS.md
@.planning/phases/13-claim-faithfulness-audit/13-VERIFICATION.md

<interfaces>
<!-- THE SEED DIRECTORY HAS THREE SOURCES (VERIFIED 2026-06-01) — ONE IS LOCAL_ONLY:           -->
<!--   sources/2026/2026-04/2026-04-09-thinking-fast-and-slow-part1/  (cloud_safe placeholder)  -->
<!--   sources/2026/2026-04/2026-04-10-kahneman-prospect-theory/      (cloud_safe — THE source) -->
<!--   sources/2026/2026-04/2026-04-10-personal-decision-journal/     (NO frontmatter -> §13     -->
<!--      fail-closed LOCAL_ONLY; body is a real salary negotiation + personal confessions).     -->
<!-- NEVER `cp -r .../2026-04/*` or any month-glob — that egresses the journal to the OpenAI     -->
<!-- cloud. Enumerate EXACTLY the one cloud_safe file by full path. The mechanical seed guard     -->
<!-- (Task 1 step 1b) is the hard precondition; the prose ban alone is not enough.               -->

<!-- Golden reference inventory (VERIFIED against live examples/kahneman/ on 2026-06-01):        -->
<!--   FULL cluster = 1 entity, 3 concepts, 1 comparison, 1 overview, 2 sources (from 2 raw      -->
<!--   sources: prospect-theory + thinking-fast-and-slow-part1).                                 -->
<!--   The single prospect-theory source maps to a SUBSET of those pages. DEBT-02 golden          -->
<!--   comparison targets THAT SUBSET only (`grep -rl prospect-theory examples/kahneman/` to       -->
<!--   enumerate the prospect-theory-derived golden pages), because one source cannot              -->
<!--   reproduce the two-source multiset. README.md/log.md carry type: overview in their own       -->
<!--   frontmatter but are NOT wiki pages — count ONLY page-type subdirs.                          -->

Substantive raw source (cloud_safe — the ONLY file that may enter the Codex subprocess; VERIFIED privacy: cloud_safe):
  sources/2026/2026-04/2026-04-10-kahneman-prospect-theory/source.md
  (NEVER use thinking-fast-and-slow-part1 — copyright placeholder; NEVER the personal-decision-journal — local_only.)

EFFECTIVE-PRIVACY SEED GUARD (mechanical, fail-closed; the SINGLE mandatory egress defense):
  # Resolve §13 effective privacy for every file the scratch will expose to Codex.
  # A source dir/file is admissible ONLY if it explicitly declares `privacy: cloud_safe`
  # in frontmatter. No frontmatter, or any non-cloud_safe value, or any directory signal
  # => treated as local_only => FAIL CLOSED (non-zero exit, abort before codex exec).
  seed_guard() {  # $1 = $SCRATCH
    local bad=0
    while IFS= read -r f; do
      head -20 "$f" | grep -qE '^privacy:[[:space:]]*cloud_safe[[:space:]]*$' || { echo "SEED GUARD: non-cloud_safe (fail-closed): $f" >&2; bad=1; }
    done < <(find "$1/sources" -type f -name '*.md')
    return $bad
  }
  # Run seed_guard "$SCRATCH" && exit non-zero on failure BEFORE any codex exec.

codex exec invocation (RESEARCH Pattern 2; flags VERIFIED via codex exec --help).
  CODEX = CLOUD-BY-DEFAULT EGRESS (per §11.7 verifier-locality contract): `codex exec` runs on
  OpenAI infra; ANYTHING it reads becomes cloud context. `-s/--sandbox` governs only LOCAL
  shell-command execution; `-C` is a cwd, NOT a network jail. Neither bounds egress. The seed
  guard above is therefore the SINGLE mandatory egress defense, not defense-in-depth.
  /home/yishai/.nvm/versions/node/v24.14.0/bin/codex exec \
    -C "$SCRATCH" -s workspace-write --skip-git-repo-check \
    -o "$SCRATCH/.codex-last-message.txt" "<ingest prompt>"
  If approvals still block: PREFER the skip-codex checkpoint path (record Codex-side blocked-on-auth,
  Phase 13.2 re-runs it) OR a `-c approval_policy` config tweak. Do NOT use
  --dangerously-bypass-approvals-and-sandbox on this machine (REVIEWS MEDIUM: it grants the cloud
  agent account-wide local read, and -C is not a jail — it could read the local_only journal in the
  parent tree). The scratch is mktemp -d but its PARENT is the real FS; bypass is banned here.

Structural-diff helpers (RESEARCH Pattern 3):
  # pagetypes() must handle BOTH layouts: scratch wikis nest pages under wiki/, but the
  # golden examples/kahneman/ stores them FLAT (no wiki/ subdir). Branch on whether $1/wiki
  # exists, and count ONLY the page-type subdirs so README.md/log.md (which carry
  # type: overview in their own frontmatter) never inflate the multiset.
  pagetypes()  {
    local base="$1"; [ -d "$base/wiki" ] && base="$base/wiki"
    grep -rhE '^type:' "$base"/{entities,concepts,sources,comparisons,overviews,decisions} 2>/dev/null | sort | uniq -c;
  }
  provtargets(){ grep -rohE '\[prov:[^]|]+' "$1" | sed 's/\[prov://' | sort -u; }

DEBT-04 scenario (CONCRETE + REPRODUCIBLE; REVIEWS MEDIUM "underspecified"):
  The live wiki is financial-AI / DDD / agent-skills / Ralph (NOT Kahneman). validate-op.sh
  accepts ONLY UPDATE|MERGE|SUPERSEDE|ARCHIVE (VERIFIED: `grep -n CREATE bin/validate-op.sh`
  => no matches) — so the write-back MUST be an UPDATE to an existing owning page, NOT a CREATE
  (REVIEWS HIGH "validate-op has no CREATE op").
  PINNED CANDIDATE scenario (executor may substitute an equally-genuine one, but it MUST hit >=2
  distinct source_ids and an existing owning page):
    Question: "How does progressive disclosure operate as a shared design principle across
      Anthropic Agent Skills and the Ralph autonomous-loop playbook?"
    Owning page: wiki/concepts/progressive-disclosure.md (exists; UPDATE target).
    Contributing sources (>=2 distinct, both cloud_safe): src-2026-05-06-anthropic-agent-skills-overview
      (or -best-practices) AND src-2026-05-06-ralph-playbook.
    Trigger: "new connection between existing pages" (§11.2) — genuinely not on any single page today.
  Correct outcome = UPDATE the owning page (append-then-synthesize, §10 Pass 3) with
  [prov:source_id#locator] on every synthesized claim drawn from >=2 distinct source_ids,
  updated_at bumped, validate-op UPDATE PASS, and a §11.2 log line:
    write_back: WRITE-BACK: <trigger> -> UPDATE <page_id>   (NEW, today-dated).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Precondition check for the DEBT-02 Codex-side run. Claude has confirmed the Codex CLI is installed at the absolute path and the substantive prospect-theory raw source is present and cloud_safe.</what-built>
  <how-to-verify>
    Confirm you are logged in to Codex on this machine so a non-interactive `codex exec` run will not fail on auth:
    1. Run: `/home/yishai/.nvm/versions/node/v24.14.0/bin/codex login status` (or your usual `codex` login check).
    2. If not authenticated, run your normal `codex login` flow.
    Codex auth is your existing session — no new secret is created. The run uses an isolated `mktemp -d` scratch dir seeded with ONLY the explicitly-enumerated `cloud_safe` prospect-theory source (the mechanical fail-closed seed guard in Task 1 enforces this — the `local_only` personal-decision-journal in the same source month can never reach the cloud subprocess). Note codex = cloud-by-default egress; the seed guard is the single egress defense.
  </how-to-verify>
  <resume-signal>Type "authenticated" once Codex login is confirmed, or "skip-codex" to record DEBT-02 Codex-side as blocked-on-auth (Claude-side still runs; Phase 13.2 re-runs the Codex side).</resume-signal>
  <wave-barrier-note>
    WAVE 1→2 BARRIER SEMANTICS (REVIEWS MEDIUM): this checkpoint is autonomous:false and blocking, but the Wave 1→2 barrier is satisfied once Plan 02 completes AND this checkpoint REACHES RESOLUTION (either "authenticated" OR "skip-codex") — the actual Codex run need NOT finish for Wave 2 to start. The optional Codex column is not on Wave 2's critical path; Wave 2 (Plan 04) consumes the Claude-vs-golden-subset diff and the method docs, both available at checkpoint resolution.
  </wave-barrier-note>
</task>

<task type="auto">
  <name>Task 1: DEBT-02 — Claude + Codex parity ingest into scratch + structural diff</name>
  <read_first>
    - .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-RESEARCH.md (Pattern 2 codex exec, Pattern 3 structural diff, Pitfall 2 source selection, Security Domain)
    - .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-PATTERNS.md (Shared Pattern E)
    - CLAUDE.md §11.1 (ingest workflow) and §10 (pipeline passes)
    - sources/2026/2026-04/2026-04-10-kahneman-prospect-theory/source.md (the ONE source both agents ingest — enumerate by FULL path; never glob the month dir)
    - examples/kahneman/ (the golden — FULL multiset 1 entity / 3 concepts / 1 comparison / 1 overview / 2 sources from TWO raw sources; DEBT-02 compares against the PROSPECT-THEORY SUBSET only, enumerated via `grep -rl prospect-theory examples/kahneman/`; count page-type subdirs only, NOT README.md/log.md; golden is FLAT — no wiki/ subdir)
    - sources/2026/2026-04/2026-04-10-personal-decision-journal/source.md (DO NOT SEED — local_only by §13 fail-closed; named here so the executor knows exactly which sibling to exclude)
  </read_first>
  <files>(scratch dirs under mktemp -d — ephemeral, NOT committed; results captured into 13.1-VERIFICATION.md in Task 3)</files>
  <action>
    Run BOTH sides of the DEBT-02 parity ingest (D-06):
    1. Create two isolated scratch wikis via `mktemp -d` (one for Claude, one for Codex; per RESEARCH Open Q3 they do NOT share). IMMEDIATELY install a teardown trap BEFORE any risky work: `S1=$(mktemp -d); S2=$(mktemp -d); trap 'rm -rf "$S1" "$S2"' EXIT INT TERM`. (REVIEWS MEDIUM: mktemp gives `/tmp/tmp.XXXX` with no "scratch" substring outside the repo, so a `git status | grep scratch` check is blind — the trap + absolute-path assertions below are the real teardown guarantee.) Each scratch contains: a copy of `CLAUDE.md` (the schema authority), an empty `wiki/` skeleton (entities/concepts/sources/comparisons/overviews/decisions subdirs + empty index.md/log.md), and — seeded by EXACT FULL PATH, never a glob — ONLY `sources/2026/2026-04/2026-04-10-kahneman-prospect-theory/source.md` (place it at the same relative path inside the scratch). NEVER `cp -r sources/2026/2026-04/*` (that would egress the local_only personal-decision-journal).
    1b. MANDATORY FAIL-CLOSED SEED GUARD (the single egress defense — run BEFORE any codex exec): run the `seed_guard "$S2"` function from the Interfaces block over the Codex scratch (and `$S1` for symmetry). It walks every `$SCRATCH/sources/**/*.md` and requires an explicit `privacy: cloud_safe` frontmatter line; anything else (no frontmatter / other value / dir signal) FAILS CLOSED with a non-zero exit. If the guard returns non-zero, ABORT the Codex run (do not proceed to codex exec) and record the abort. This is the mechanical trigger T-13.1-05 was missing.
    2. Claude side: ingest the source into the Claude scratch wiki following the §11.1 ingest workflow (source summary page + entity/concept/comparison/overview pages with [prov:] markers).
    3. Codex side (ONLY if seed guard passed AND checkpoint resolved "authenticated"): run the `codex exec` invocation (Interfaces block) against the Codex scratch dir. Capture the `-o` last-message file. If `workspace-write` still prompts, PREFER recording skip-codex (Phase 13.2 re-runs) or a `-c approval_policy` tweak — do NOT use `--dangerously-bypass-approvals-and-sandbox` on this machine (banned per Interfaces: -C is not a jail, bypass grants account-wide local read to a cloud agent in a tree that contains the local_only journal). If the checkpoint resolved "skip-codex", record DEBT-02 Codex-side as blocked-on-auth and still complete the Claude-side ingest + the Claude-vs-golden-subset diff.
    4. Structural-equivalence diff (D-07) using the Pattern 3 helpers. Compute the page-type multiset + provenance-target set for {Claude scratch, Codex scratch (if ran)}. GOLDEN COMPARISON TARGETS THE PROSPECT-THEORY SUBSET ONLY: enumerate the golden pages derivable from the one source via `grep -rl prospect-theory examples/kahneman/` and diff the scratch outputs against THAT subset multiset (a single source cannot produce the full two-source cluster — comparing against the full 6-page golden is structurally guaranteed to "differ" and is not a real verdict). Run `bash bin/lint.sh --category yaml,provenance "$S1/wiki"` and (if ran) `"$S2/wiki"` — PASS the EXPLICIT scratch wiki path so lint does NOT default to the live `wiki/` (REVIEWS MEDIUM); scope provenance expectations to the one seeded source. Build the diff matrix (rows = {page-set, page-types, prov-source-IDs, prov-locator-targets, frontmatter-valid}; cols = {Claude vs golden-subset, Codex vs golden-subset, **Claude vs Codex (PRIMARY verdict)**}; cells = MATCH / DIFFERS(noted)). The Claude-vs-Codex column is the headline parity result (both ran the identical single source — reproducible); the golden-subset columns are secondary context. Prose differences listed separately as "expected variance, not a failure."
    5. Teardown is handled by the trap; after capturing diff data, optionally `rm -rf "$S1" "$S2"` explicitly and record the absolute paths + that they no longer exist. Do NOT commit any scratch output.
    Record the full invocation, the diff matrix data, the seed-guard result, and prose-variance notes for Task 3 (they go verbatim into 13.1-VERIFICATION.md and feed Plan 04's agent-parity.md).
  </action>
  <acceptance_criteria>
    - The chosen source is the substantive one: the ingest prompt references `2026-04-10-kahneman-prospect-theory/source.md` and NOT `thinking-fast-and-slow-part1` and NOT `personal-decision-journal`
    - SEED GUARD ran and passed before any codex exec: the recorded evidence shows `seed_guard` exited 0 over the Codex scratch (every seeded source explicitly `privacy: cloud_safe`); if it failed, the Codex run was aborted (recorded)
    - NO local_only source was seeded: `personal-decision-journal` appears in NEITHER scratch tree's sources (asserted before teardown)
    - Claude-side scratch produced a source summary page + >=1 entity/concept page with at least one `[prov:` marker (grep over the scratch tree before teardown)
    - `bash bin/lint.sh --category yaml,provenance "$S1/wiki"` exits clean against the Claude scratch output (EXPLICIT path; and the Codex scratch `"$S2/wiki"` if the Codex side ran) — NOT the live wiki/
    - The structural-diff matrix targets the PROSPECT-THEORY SUBSET of the golden (not the full 6-page cluster) and the CLAUDE-vs-CODEX column is recorded as the primary verdict (or Codex column marked blocked-on-auth)
    - Scratch dirs torn down by ABSOLUTE PATH: `test ! -d "$S1" && test ! -d "$S2"` (trap-backed); `git status --porcelain` shows no files under any scratch path and no `.codex-last-message`
    - `--dangerously-bypass-approvals-and-sandbox` was NOT used (recorded)
  </acceptance_criteria>
  <verify>
    <!-- Scratch lint runs inline during the action (against ephemeral mktemp -d trees that are
         torn down by the trap at task end, so it cannot be re-run here). The mechanical
         postcondition that CAN be asserted afterward is teardown cleanliness: no scratch output
         leaked into the repo, and no .codex-last-message artifact remains. -->
    <automated>test -z "$(git status --porcelain | grep -iE 'scratch|\.codex-last-message|tmp\.')"</automated>
  </verify>
  <done>Seed guard passed (fail-closed) before any cloud egress; both ingests ran (or Codex side recorded blocked-on-auth); structural-diff matrix computed vs the prospect-theory golden SUBSET with Claude-vs-Codex as the primary verdict; lint clean on EXPLICIT scratch wiki paths; bypass flag not used; scratch torn down by trap; evidence captured for Task 3.</done>
</task>

<task type="auto">
  <name>Task 2: DEBT-04 — execute genuine write-back query against live wiki/</name>
  <read_first>
    - CLAUDE.md §11.2 (Query workflow: Write-Back Rules, Privacy Inheritance, Query Log Entry Format) and §9 (executor model + validate-op)
    - .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-RESEARCH.md (Code Examples DEBT-04 scenario + Pitfall 5)
    - wiki/index.md (to find candidate pages for a cross-source synthesis question)
    - bin/validate-op.sh (the deterministic gate to run before applying the op)
  </read_first>
  <files>wiki/ (the EXISTING owning page — UPDATE only; plus wiki/index.md and wiki/log.md per §11.2)</files>
  <action>
    Execute a genuine DEBT-04 write-back against the LIVE `wiki/` following the §11.2 query workflow. The op MUST be an UPDATE to an EXISTING owning page — `bin/validate-op.sh` has NO CREATE branch (VERIFIED: accepts only UPDATE|MERGE|SUPERSEDE|ARCHIVE), so a CREATE could not pass the mandated `validate-op → PASS` gate (REVIEWS HIGH). The cross-source synthesis is therefore framed as new claims appended to a page that ALREADY owns the topic.
    1. Use the PINNED CANDIDATE scenario from the Interfaces block (or an equally-genuine substitute that still UPDATEs an existing page with >=2 distinct source_ids): question = "How does progressive disclosure operate as a shared design principle across Anthropic Agent Skills and the Ralph autonomous-loop playbook?"; owning page = `wiki/concepts/progressive-disclosure.md` (UPDATE target); contributing sources = `src-2026-05-06-anthropic-agent-skills-overview` (or -best-practices) AND `src-2026-05-06-ralph-playbook` (both cloud_safe). Confirm via §11.1/§11.2 navigation (read index.md first, then TL;DR/Key Facts) that this connection is genuinely NOT already on the page — if it is, pick a different >=2-source connection. Do NOT pick a lookup that would honestly produce NO-WRITE-BACK.
    2. Synthesize the answer; every synthesized claim carries `[prov:source_id#locator]`, and the appended block as a whole MUST cite >=2 DISTINCT source_ids (that is what makes it a genuine cross-source synthesis, not a thin one-source connection — REVIEWS MEDIUM). Apply Privacy Inheritance (§11.2): target inherits the strictest privacy among contributing sources (both candidates are cloud_safe).
    3. Run `bash bin/validate-op.sh UPDATE wiki/concepts/progressive-disclosure.md` BEFORE applying — it MUST print PASS.
    4. Apply the UPDATE (append-then-synthesize per §10 Pass 3): append the new provenanced claims into the appropriate Detail/Key-Facts section, re-synthesize TL;DR/Key Facts, add both new source IDs to the `sources` frontmatter list, bump `updated_at` to today, update `wiki/index.md` if the page summary materially changed, and append the §11.2 query log entry to `wiki/log.md` with the exact format on a NEW today-dated line:
       `write_back: WRITE-BACK: new connection between existing pages -> UPDATE progressive-disclosure`
    5. Commit as a single `query(progressive-disclosure):` operation. Record the exact query, write-back trigger, affected page, validate-op result, and the new log line for Task 3.
    REDACTION NOTE: the pinned scenario uses only cloud_safe sources (fully transparent record). If a substitute touches a `local_only` source, keep the 13.1-VERIFICATION.md evidence REDACTION-SAFE (counts/paths/labels only — no local_only body).
  </action>
  <acceptance_criteria>
    - The op is an UPDATE (not CREATE): `bash bin/validate-op.sh UPDATE wiki/concepts/progressive-disclosure.md` → PASS (recorded; substitute path allowed but it MUST be an UPDATE that validate-op accepts)
    - The appended write-back cites >=2 DISTINCT source_ids: the diff `git diff HEAD~1 -- <owning-page>` adds `[prov:` markers referencing at least two different source_id values (e.g. one agent-skills source AND ralph-playbook)
    - NEW today-dated WRITE-BACK log line (NOT the 7 pre-existing entries — REVIEWS HIGH false-positive gate): `git diff HEAD~1 -- wiki/log.md | grep -E '^\+.*WRITE-BACK'` shows EXACTLY the one new line, dated today, naming the UPDATE target page_id
    - `git diff HEAD~1 -- <owning-page>` shows the page gained at least one NEW `[prov:` marker AND an updated `updated_at`
    - `bash bin/lint.sh --category yaml,provenance,orphan,crossref` exits clean after the write-back (default wiki/ scope is correct here — this DOES touch the live wiki)
    - The write-back is a single `query(<topic>):` commit (not split)
    - Privacy inheritance honored: target page privacy == strictest among contributing sources
  </acceptance_criteria>
  <verify>
    <automated>bash bin/lint.sh --category yaml,provenance,orphan,crossref && git diff HEAD~1 -- wiki/log.md | grep -qE '^\+.*WRITE-BACK'</automated>
  </verify>
  <done>A genuine cross-source UPDATE write-back (>=2 distinct source_ids) appended provenanced claims to an existing owning page, validate-op UPDATE PASS, a NEW today-dated §11.2 log line (diffed vs HEAD~1, not the 7 pre-existing entries), lint clean, one query() commit. Evidence captured for Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Author 13.1-VERIFICATION.md (Phase 13 mirror) incl. DEBT-01 render checklist + capture slot</name>
  <read_first>
    - .planning/phases/13-claim-faithfulness-audit/13-VERIFICATION.md (exact mirror skeleton: frontmatter, Goal Achievement table, Required Artifacts, Behavioral Spot-Checks, REQ-ID Verification)
    - .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-PATTERNS.md (the 13.1-VERIFICATION.md analog section + phase-specific evidence layout + Shared Pattern C)
    - .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-RESEARCH.md (Pitfall 1 — the DEBT-01 render checklist must keep examples/ indexed; graph-cleanliness sub-check)
    - The DEBT-02 + DEBT-04 evidence captured in Tasks 1-2
  </read_first>
  <files>.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-VERIFICATION.md</files>
  <action>
    Author `13.1-VERIFICATION.md` mirroring `13-VERIFICATION.md`:
    1. Frontmatter: phase `13.1-docs-finalization-obsidian-starter`, verified timestamp, status, score (success criteria verified), and `phase_base_sha` derived from the CONTEXT commit (`git log --format=%H -n 1 -- .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-CONTEXT.md`) plus the short SHA + anchor command. Include the redaction note.
    2. Goal Achievement table: the 6 ROADMAP success criteria → evidence/status.
    3. Required Artifacts + Behavioral Spot-Checks + Requirements Coverage tables.
    4. DEBT-02 section: the structural-diff matrix from Task 1 (golden column targets the PROSPECT-THEORY SUBSET; CLAUDE-vs-CODEX is the primary verdict), the `codex exec` invocation used, the seed-guard result, the scratch teardown note, and prose-variance notes (expected, not failures). If Codex side was skipped, record blocked-on-auth + that Phase 13.2 re-runs it. STATE PLAINLY (per §11.7 verifier-locality): "codex = cloud egress by default; `-s/--sandbox` confines only local shell writes, not network egress; `-C` is a cwd, not a jail. The fail-closed seed guard was the single mandatory egress defense; `--dangerously-bypass-approvals-and-sandbox` was NOT used."
    5. DEBT-04 section: the exact query, write-back trigger, affected page, that the op was an UPDATE (validate-op has no CREATE branch), the >=2 distinct contributing source_ids, `bin/validate-op.sh UPDATE` result (PASS), and the NEW today-dated §11.2 log line.
    6. DEBT-01 section: a precise Obsidian RENDER CHECKLIST + a capture slot marked **"awaiting user render"** (D-09). The checklist MUST instruct the user:
       - Enable the Dataview community plugin (precondition).
       - Do NOT add `examples/` to Settings → Files & Links → Excluded files for this render pass (fixtures must stay indexed — Pitfall 1).
       - Run the five fixture-scoped Dataview queries (`FROM "examples/dataview-fixtures"`) and confirm each returns the documented expected count (the matrix lives in examples.md, Plan 04 — cross-reference it). Note the two Dataview-vs-grep divergences (WHERE !privacy matches omitted-AND-null; contains(domains,...) needs a list) so a count mismatch is interpreted correctly rather than treated as a fixture bug.
       - Confirm wikilinks resolve.
       - SC1 GRAPH SUB-CHECK — REFRAMED (REVIEWS MEDIUM: under Obsidian's SINGLE Excluded-files mechanism you cannot keep examples/ indexed for Dataview AND hide it from the graph; D-03 forbids mutating committed `.obsidian/`. So SC1's literal "graph not contaminated by examples/" is NOT deliverable by any 13.1 plan — it is RENEGOTIATED to "examples/ forms a VISUALLY ISOLABLE sub-graph"). The checklist asks the user to visually confirm the examples/ pages (kahneman cluster + dataview-fixtures) form a disconnected/isolable sub-graph rather than entangling the personal wiki graph, OR to note they performed a one-time Settings → Files & Links → Excluded files add of `examples/` for a graph-only pass. RECORD in the verification note that SC1 is reframed to "isolable sub-graph" and flag the formal SC1 renegotiation (a short decision record, trigger_type: reframing) as a Phase 13.2 follow-up (13.2 SC2 audits/closes DEBT-01).
    7. REQ-ID Verification: `- [x]`/`- [ ]` lines for DEBT-01 (awaiting user render — leave appropriately marked), DEBT-02, DEBT-04 (consumed by bin/requirements-sync.sh).
    Neutrality (D-12): placeholders only outside the sanctioned `examples/kahneman/...` PATH references. NOTE: `bin/check-neutrality.sh` does NOT scan `.planning/` (it is outside PUBLIC_PATHS), so the repo gate is meaningless for this file — neutrality here is enforced by a TARGETED denylist grep over the file itself + write-time discipline. The personal-decision-journal must never be named except as a redaction-safe label.
  </action>
  <acceptance_criteria>
    - `.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-VERIFICATION.md` exists
    - `grep -q 'awaiting user render' .../13.1-VERIFICATION.md` (DEBT-01 capture slot present)
    - The DEBT-01 checklist contains the Pitfall-1 instruction: `grep -qi 'Excluded files' .../13.1-VERIFICATION.md` AND it tells the user NOT to exclude examples/dataview-fixtures (fixtures stay indexed)
    - DEBT-02 structural-diff matrix present (golden-SUBSET columns + Claude-vs-Codex primary) and the codex exec invocation recorded; the codex=cloud-egress statement is present: `grep -qi 'cloud egress\|egress by default' .../13.1-VERIFICATION.md`
    - DEBT-04 evidence present: the query, WRITE-BACK trigger, affected page, UPDATE op, >=2 source_ids, validate-op PASS, and the §11.2 log line
    - REQ-ID lines for DEBT-01/02/04 present in the format requirements-sync.sh consumes
    - Frontmatter `phase_base_sha` populated from the CONTEXT-commit SHA
    - TARGETED neutrality (repo gate does NOT scan .planning/): no UNSANCTIONED private term leaks — `grep -niE 'salary|personal-decision' .../13.1-VERIFICATION.md` returns only redaction-safe label lines (no journal body); a `kahneman`/`prospect-theory` mention is acceptable ONLY as part of an `examples/kahneman/...` path or the sanctioned golden-source reference, never as bare leaked vault content
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-VERIFICATION.md; test -f "$F" && grep -q 'awaiting user render' "$F" && grep -qi 'Excluded files' "$F" && grep -q 'WRITE-BACK' "$F" && grep -qE 'DEBT-0[124]' "$F" && grep -qi 'cloud egress\|egress by default' "$F" && ! grep -qi 'salary' "$F"</automated>
  </verify>
  <done>13.1-VERIFICATION.md authored in Phase 13 mirror format with the DEBT-02 diff matrix (golden-subset + Claude-vs-Codex primary + codex=cloud-egress statement), DEBT-04 UPDATE write-back evidence (>=2 source_ids, new dated log line), and the DEBT-01 render checklist + "awaiting user render" capture slot; REQ-ID lines present; targeted neutrality clean (the repo gate does not scan .planning/).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → `codex exec` cloud subprocess | Source content crosses to a cloud agent; only cloud_safe material may cross. |
| `codex exec` → filesystem | The cloud subprocess can write files; writes must be confined to a throwaway scratch dir. |
| DEBT-04 write-back → committed `wiki/` | Synthesized claims become durable; privacy inheritance and provenance must hold. |
| author → committed `13.1-VERIFICATION.md` | Evidence committed to main; local_only content must be redacted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.1-05 | Information Disclosure | `codex exec` subprocess (cloud) | **mitigate [HIGH — mechanical block]** | THE MECHANICAL TRIGGER (REVIEWS HIGH/MEDIUM: prose-only before): a fail-closed `seed_guard "$SCRATCH"` (Interfaces block) walks every `$SCRATCH/sources/**/*.md` and exits non-zero unless each declares `privacy: cloud_safe` — run as a HARD PRECONDITION before any `codex exec`; on failure the Codex run is ABORTED. The seed dir has a THIRD source (`personal-decision-journal`, local_only by §13 fail-closed); only the explicitly-enumerated prospect-theory file is seeded (never a month-glob). codex = cloud egress by default — this guard is the SINGLE mandatory egress defense, not defense-in-depth. |
| T-13.1-06 | Tampering / Info Disclosure | `codex exec` egress + filesystem | mitigate | CLARIFIED (REVIEWS HIGH "sandbox ≠ egress"): `-s workspace-write` confines only LOCAL shell writes; `-C "$SCRATCH"` is a cwd, NOT a network jail — NEITHER bounds egress. `--dangerously-bypass-approvals-and-sandbox` is BANNED on this machine (it grants the cloud agent account-wide local read in a tree that contains the local_only journal). Egress protection rests entirely on the seed guard (T-13.1-05). `$SCRATCH` is throwaway `mktemp -d` with a `trap ... EXIT INT TERM` teardown; never writes live `wiki/`. |
| T-13.1-07 | Information Disclosure | `13.1-VERIFICATION.md` (committed to main) | mitigate | Redaction-safe evidence only for any local_only-touching content (counts/paths/labels). `bin/check-neutrality.sh` does NOT scan `.planning/`, so neutrality here is enforced by a TARGETED denylist grep over the file + write-time discipline (the pinned DEBT-04 scenario uses only cloud_safe sources). |
| T-13.1-08 | Repudiation | DEBT-04 write-back provenance | mitigate | The op is an UPDATE to an existing page (validate-op has no CREATE branch); every synthesized claim carries `[prov:source_id#locator]` from >=2 distinct source_ids; `bin/validate-op.sh UPDATE` PASS before apply; a NEW today-dated §11.2 log line (diffed vs HEAD~1) records the operation. |
</threat_model>

<verification>
- PRECONDITION: fail-closed `seed_guard` exited 0 before any codex exec (or the Codex run was aborted/skip-codex)
- `bash bin/lint.sh --category yaml,provenance,orphan,crossref` clean after DEBT-04 write-back; scratch lint used EXPLICIT `$S1/wiki` / `$S2/wiki` paths (never the live wiki/)
- `bash bin/validate-op.sh UPDATE <owning-page>` PASS recorded for the write-back op (UPDATE, not CREATE)
- NEW today-dated WRITE-BACK line confirmed via `git diff HEAD~1 -- wiki/log.md` (NOT the 7 pre-existing entries)
- DEBT-02 diff targets the prospect-theory golden SUBSET; Claude-vs-Codex is the primary verdict; codex=cloud-egress stated in the record
- Targeted neutrality grep over 13.1-VERIFICATION.md clean (repo gate does not scan .planning/)
- Scratch dirs torn down by trap (absolute-path assertion); `git status --porcelain` shows no scratch/tmp/.codex-last-message artifacts
- 13.1-VERIFICATION.md contains the DEBT-01 render checklist + "awaiting user render" slot
</verification>

<success_criteria>
- DEBT-02 structural-equivalence verdict recorded: PRIMARY = Claude vs Codex (reproducible, same single source); SECONDARY = Claude/Codex vs the prospect-theory golden SUBSET; prose variance noted as non-failure; codex=cloud-egress stated
- DEBT-04 genuine cross-source UPDATE write-back: >=2 distinct source_ids, durable provenanced page, validate-op UPDATE PASS, NEW today-dated §11.2 log line, one query() commit
- Fail-closed seed guard enforced before any cloud egress; bypass flag not used
- DEBT-01 render checklist + capture slot shipped (prepared-and-await; phase not blocked)
- 13.1-VERIFICATION.md mirrors Phase 13 format; targeted neutrality clean
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-03-SUMMARY.md` (record the agent-parity diff matrix + prose-variance findings for Plan 04's agent-parity.md).
</output>
