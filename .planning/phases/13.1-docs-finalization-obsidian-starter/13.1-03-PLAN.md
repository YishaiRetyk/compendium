---
phase: 13.1-docs-finalization-obsidian-starter
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-VERIFICATION.md
  - wiki/
autonomous: false
requirements: [DEBT-02, DEBT-04, DEBT-01]
user_setup:
  - service: codex-cli
    why: "DEBT-02 Codex-side parity ingest runs as the user's authenticated Codex session"
    env_vars:
      - name: CODEX_HOME
        source: "Existing Codex login on this machine (codex auth); no new secret to create"

must_haves:
  truths:
    - "DEBT-02: both Claude and Codex re-ingest the substantive prospect-theory raw source into scratch wikis; outputs are structurally diffed against the examples/kahneman/ golden and against each other"
    - "DEBT-02: structural-equivalence verdict recorded (same page set, page types, provenance source IDs + locator targets, valid frontmatter); prose variance noted as expected, not a failure"
    - "DEBT-04: a genuine cross-source-synthesis write-back query is executed against the LIVE wiki/, produces a durable provenanced page, passes validate-op, and is logged in wiki/log.md"
    - "DEBT-01: a precise Obsidian render checklist + a capture slot marked 'awaiting user render' is shipped in 13.1-VERIFICATION.md (phase NOT blocked on the GUI)"
    - "Scratch dirs are torn down after diff capture; no scratch output committed"
  artifacts:
    - path: ".planning/phases/13.1-docs-finalization-obsidian-starter/13.1-VERIFICATION.md"
      provides: "Verification capture (Phase 13 mirror format) for DEBT-01/02/04"
      contains: "awaiting user render"
  key_links:
    - from: "DEBT-04 write-back"
      to: "wiki/log.md"
      via: "query workflow §11.2 log entry"
      pattern: "WRITE-BACK"
    - from: "DEBT-02 scratch outputs"
      to: "examples/kahneman/ golden"
      via: "structural-equivalence diff (page types + prov targets)"
      pattern: "prov:"
---

<objective>
Execute the two agent-runnable deferred v1.0 verifications and capture all results (plus the prepared DEBT-01 render checklist) into `13.1-VERIFICATION.md` using the Phase 13 mirror format:
- DEBT-02 (Codex agent-parity): Claude AND Codex each re-ingest the substantive prospect-theory raw source into an isolated scratch wiki; structurally diff both outputs against the committed `examples/kahneman/` golden and against each other (tolerance = structural equivalence, D-07).
- DEBT-04 (genuine write-back): run a cross-source-synthesis query against the LIVE `wiki/` that triggers a real WRITE-BACK, producing a durable provenanced page, validate-op PASS, logged per §11.2.
- DEBT-01 (Obsidian render): ship a precise render checklist + a capture slot marked "awaiting user render" — prepared-and-await per D-09 (Phase 13.2 re-runs/audits).

Purpose: Close v1.0's three deferred verifications end-to-end (except the single GUI step the user owns).
Output: `13.1-VERIFICATION.md`; a durable DEBT-04 write-back commit to `wiki/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-CONTEXT.md
@.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-RESEARCH.md
@.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-PATTERNS.md
@.planning/phases/13-claim-faithfulness-audit/13-VERIFICATION.md

<interfaces>
<!-- Golden reference inventory (VERIFIED against live examples/kahneman/ on 2026-06-01): -->
<!--   1 entity, 3 concepts, 1 comparison, 2 overviews, 2 sources                          -->
<!-- NOTE: actual golden has 2 overviews (RESEARCH/D-08 said 1 — the live tree is          -->
<!-- authoritative; use the actual multiset for the structural diff).                       -->

Substantive raw source (cloud_safe — safe for the Codex subprocess, VERIFIED privacy: cloud_safe):
  sources/2026/2026-04/2026-04-10-kahneman-prospect-theory/source.md
  (NEVER use thinking-fast-and-slow-part1 — it is a copyright placeholder, not real content.)

codex exec invocation (RESEARCH Pattern 2; flags VERIFIED via codex exec --help):
  /home/yishai/.nvm/versions/node/v24.14.0/bin/codex exec \
    -C "$SCRATCH" -s workspace-write --skip-git-repo-check \
    -o "$SCRATCH/.codex-last-message.txt" "<ingest prompt>"
  Fallback if approvals still block: --dangerously-bypass-approvals-and-sandbox
  (acceptable because $SCRATCH is a throwaway mktemp -d with only the seeded source; document the justification).

Structural-diff helpers (RESEARCH Pattern 3):
  pagetypes()  { grep -rhE '^type:' "$1"/wiki 2>/dev/null | sort | uniq -c; }
  provtargets(){ grep -rohE '\[prov:[^]|]+' "$1" | sed 's/\[prov://' | sort -u; }

DEBT-04 scenario shape (04-VERIFICATION.md): the answer must be genuinely novel cross-source
synthesis (a "new connection between existing pages" trigger per §11.2), NOT a lookup.
Correct outcome = UPDATE the owning page (or CREATE a typed page if none owns it) with
[prov:source_id#locator] on every synthesized claim, updated_at bumped, validate-op PASS,
and a §11.2 log line: write_back: WRITE-BACK: <trigger> -> UPDATE/CREATE <page_id>.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Precondition check for the DEBT-02 Codex-side run. Claude has confirmed the Codex CLI is installed at the absolute path and the substantive prospect-theory raw source is present and cloud_safe.</what-built>
  <how-to-verify>
    Confirm you are logged in to Codex on this machine so a non-interactive `codex exec` run will not fail on auth:
    1. Run: `/home/yishai/.nvm/versions/node/v24.14.0/bin/codex login status` (or your usual `codex` login check).
    2. If not authenticated, run your normal `codex login` flow.
    Codex auth is your existing session — no new secret is created. The run uses an isolated `mktemp -d` scratch dir and only the `cloud_safe` prospect-theory source, so no `local_only` content reaches the cloud subprocess.
  </how-to-verify>
  <resume-signal>Type "authenticated" once Codex login is confirmed, or "skip-codex" to record DEBT-02 Codex-side as blocked-on-auth (Claude-side still runs; Phase 13.2 re-runs the Codex side).</resume-signal>
</task>

<task type="auto">
  <name>Task 1: DEBT-02 — Claude + Codex parity ingest into scratch + structural diff</name>
  <read_first>
    - .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-RESEARCH.md (Pattern 2 codex exec, Pattern 3 structural diff, Pitfall 2 source selection, Security Domain)
    - .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-PATTERNS.md (Shared Pattern E)
    - CLAUDE.md §11.1 (ingest workflow) and §10 (pipeline passes)
    - sources/2026/2026-04/2026-04-10-kahneman-prospect-theory/source.md (the source both agents ingest)
    - examples/kahneman/ (the golden — note the ACTUAL multiset: 1 entity / 3 concepts / 1 comparison / 2 overviews / 2 sources)
  </read_first>
  <files>(scratch dirs under mktemp -d — ephemeral, NOT committed; results captured into 13.1-VERIFICATION.md in Task 3)</files>
  <action>
    Run BOTH sides of the DEBT-02 parity ingest (D-06):
    1. Seed two isolated scratch wikis via `mktemp -d` (one for Claude, one for Codex; per RESEARCH Open Q3 they do NOT share). Each scratch contains: a copy of `CLAUDE.md` (the schema authority), an empty `wiki/` skeleton (entities/concepts/sources/comparisons/overviews/decisions subdirs + empty index.md/log.md), and ONLY the chosen raw source at `sources/2026/2026-04/2026-04-10-kahneman-prospect-theory/source.md`. Do NOT seed any `local_only` material.
    2. Claude side: ingest the source into the Claude scratch wiki following the §11.1 ingest workflow (source summary page + entity/concept/comparison/overview pages with [prov:] markers).
    3. Codex side: run the `codex exec` invocation (Interfaces block) against the Codex scratch dir. Capture the `-o` last-message file. If `workspace-write` still prompts, fall back to `--dangerously-bypass-approvals-and-sandbox` and document the justification (throwaway scratch, single cloud_safe source). If the checkpoint resolved "skip-codex", record DEBT-02 Codex-side as blocked-on-auth and still complete the Claude-side ingest + Claude-vs-golden diff.
    4. Structural-equivalence diff (D-07) using the Pattern 3 helpers: compute page-type multiset and provenance-target set for {Claude scratch, Codex scratch, examples/kahneman}. Run `bash bin/lint.sh --category yaml,provenance` against each scratch output (zero errors expected). Build the diff matrix (rows = {page-set, page-types, prov-source-IDs, prov-locator-targets, frontmatter-valid}; cols = {Claude vs golden, Codex vs golden, Claude vs Codex}; cells = MATCH / DIFFERS(noted)). Prose differences are listed separately as "expected variance, not a failure."
    5. Tear down both scratch dirs after capturing the diff data (record the teardown). Do NOT commit any scratch output.
    Record the full invocation, the diff matrix data, and prose-variance notes for Task 3 (they go verbatim into 13.1-VERIFICATION.md and feed Plan 04's agent-parity.md).
  </action>
  <acceptance_criteria>
    - The chosen source is the substantive one: the ingest prompt references `2026-04-10-kahneman-prospect-theory/source.md` and NOT `thinking-fast-and-slow-part1`
    - Claude-side scratch produced a source summary page + >=1 entity/concept page with at least one `[prov:` marker (grep over the scratch tree before teardown)
    - `bash bin/lint.sh --category yaml,provenance` exits clean against the Claude scratch output (and the Codex scratch output if the Codex side ran)
    - A structural-diff matrix is produced with the three comparison columns and per-row MATCH/DIFFERS verdicts (captured for Task 3)
    - Scratch dirs are torn down: the recorded scratch paths no longer exist; `git status --porcelain` shows no files under any scratch path
    - No `local_only` source was seeded into either scratch (only the cloud_safe prospect-theory source)
  </acceptance_criteria>
  <verify>
    <automated>echo "Manual-execution task: verification asserted via Task 3 evidence (diff matrix + invocation + teardown note) captured in 13.1-VERIFICATION.md; lint --category yaml,provenance was run against scratch outputs before teardown."</automated>
  </verify>
  <done>Both ingests ran (or Codex side recorded blocked-on-auth); structural-diff matrix computed vs golden and agent-to-agent; lint clean on scratch outputs; scratch torn down; evidence captured for Task 3.</done>
</task>

<task type="auto">
  <name>Task 2: DEBT-04 — execute genuine write-back query against live wiki/</name>
  <read_first>
    - CLAUDE.md §11.2 (Query workflow: Write-Back Rules, Privacy Inheritance, Query Log Entry Format) and §9 (executor model + validate-op)
    - .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-RESEARCH.md (Code Examples DEBT-04 scenario + Pitfall 5)
    - wiki/index.md (to find candidate pages for a cross-source synthesis question)
    - bin/validate-op.sh (the deterministic gate to run before applying the op)
  </read_first>
  <files>wiki/ (the page that owns the synthesis — UPDATE or CREATE; plus wiki/index.md and wiki/log.md per §11.2)</files>
  <action>
    Execute a genuine DEBT-04 write-back against the LIVE `wiki/` following the §11.2 query workflow:
    1. Pick a question whose answer is a cross-source synthesis NOT already captured on any single page (a "new connection between existing pages" trigger — see Pitfall 5). Use the §11.1/§11.2 navigation rule (read index.md first, then TL;DR/Key Facts). Do NOT pick a lookup that would honestly produce NO-WRITE-BACK.
    2. Synthesize the answer with `[prov:source_id#locator]` on every synthesized claim. Apply Privacy Inheritance (§11.2): the write-back target inherits the strictest privacy among contributing sources.
    3. Decide write-back target by page ownership: UPDATE the owning page, or CREATE a typed page if none owns it. Run `bash bin/validate-op.sh <OP> <target_path>` BEFORE applying — it must PASS.
    4. Apply the op (append-then-synthesize per §10 Pass 3), bump `updated_at`, update `wiki/index.md` if a page was created/significantly modified, and append the §11.2 log entry to `wiki/log.md` with the exact format:
       `write_back: WRITE-BACK: <trigger> -> UPDATE/CREATE <page_id>`
    5. Commit as a normal `query(<topic>):` operation (one logical commit). Record the exact query, write-back decision + trigger, affected page, validate-op result, and the log line for Task 3.
    REDACTION NOTE: if any contributing source is `local_only`, keep the 13.1-VERIFICATION.md evidence REDACTION-SAFE (counts/paths/labels only — no local_only body). Prefer a cloud_safe-sourced synthesis to keep the record fully transparent.
  </action>
  <acceptance_criteria>
    - `bash bin/validate-op.sh <OP> wiki/<owning-page>.md` → PASS (recorded)
    - The affected page contains at least one NEW `[prov:` marker added by this write-back
    - `grep 'WRITE-BACK' wiki/log.md` matches the new §11.2 log line with the trigger and target page_id
    - `bash bin/lint.sh --category yaml,provenance,orphan,crossref` exits clean after the write-back
    - The write-back is a single `query(<topic>):` commit (not split)
    - Privacy inheritance honored: target page privacy == strictest among contributing sources
  </acceptance_criteria>
  <verify>
    <automated>bash bin/lint.sh --category yaml,provenance,orphan,crossref && grep -q 'WRITE-BACK' wiki/log.md</automated>
  </verify>
  <done>A genuine cross-source write-back produced a durable provenanced page, validate-op PASS, §11.2 log line present, lint clean, committed as one query() operation. Evidence captured for Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Author 13.1-VERIFICATION.md (Phase 13 mirror) incl. DEBT-01 render checklist + capture slot</name>
  <read_first>
    - .planning/phases/13-claim-faithfulness-audit/13-VERIFICATION.md (exact mirror skeleton: frontmatter, Goal Achievement table, Required Artifacts, Behavioral Spot-Checks, REQ-ID Verification)
    - .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-PATTERNS.md (the 13.1-VERIFICATION.md analog section + phase-specific evidence layout + Shared Pattern C)
    - .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-RESEARCH.md (Pitfall 1 — the DEBT-01 render checklist must keep examples/ indexed; graph-cleanliness sub-check)
    - The DEBT-02 + DEBT-04 evidence captured in Tasks 1-2
  </read_first>
  <files>.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-VERIFICATION.md</files>
  <action>
    Author `13.1-VERIFICATION.md` mirroring `13-VERIFICATION.md`:
    1. Frontmatter: phase `13.1-docs-finalization-obsidian-starter`, verified timestamp, status, score (success criteria verified), and `phase_base_sha` derived from the CONTEXT commit (`git log --format=%H -n 1 -- .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-CONTEXT.md`) plus the short SHA + anchor command. Include the redaction note.
    2. Goal Achievement table: the 6 ROADMAP success criteria → evidence/status.
    3. Required Artifacts + Behavioral Spot-Checks + Requirements Coverage tables.
    4. DEBT-02 section: the structural-diff matrix from Task 1, the `codex exec` invocation used, the scratch teardown note, and prose-variance notes (expected, not failures). If Codex side was skipped, record blocked-on-auth + that Phase 13.2 re-runs it.
    5. DEBT-04 section: the exact query, write-back decision + trigger, affected page, `bin/validate-op.sh` result (PASS), and the §11.2 log line.
    6. DEBT-01 section: a precise Obsidian RENDER CHECKLIST + a capture slot marked **"awaiting user render"** (D-09). The checklist MUST instruct the user:
       - Enable the Dataview community plugin (precondition).
       - Do NOT add `examples/` to Settings → Files & Links → Excluded files for this render pass (fixtures must stay indexed — Pitfall 1).
       - Run the five fixture-scoped Dataview queries (`FROM "examples/dataview-fixtures"`) and confirm each returns the documented expected count (the matrix lives in examples.md, Plan 04 — cross-reference it).
       - Confirm wikilinks resolve.
       - Graph-cleanliness sub-check: temporarily add `examples/kahneman/` (NOT the fixtures) to Excluded files OR visually confirm the Kahneman cluster is an isolated sub-graph.
    7. REQ-ID Verification: `- [x]`/`- [ ]` lines for DEBT-01 (awaiting user render — leave appropriately marked), DEBT-02, DEBT-04 (consumed by bin/requirements-sync.sh).
    Neutrality (D-12): placeholders only outside the sanctioned kahneman references.
  </action>
  <acceptance_criteria>
    - `.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-VERIFICATION.md` exists
    - `grep -q 'awaiting user render' .../13.1-VERIFICATION.md` (DEBT-01 capture slot present)
    - The DEBT-01 checklist contains the Pitfall-1 instruction: `grep -qi 'Excluded files' .../13.1-VERIFICATION.md` AND it tells the user NOT to exclude examples/dataview-fixtures (fixtures stay indexed)
    - DEBT-02 structural-diff matrix present (the three comparison columns) and the codex exec invocation recorded
    - DEBT-04 evidence present: the query, WRITE-BACK trigger, affected page, validate-op PASS, and the §11.2 log line
    - REQ-ID lines for DEBT-01/02/04 present in the format requirements-sync.sh consumes
    - Frontmatter `phase_base_sha` populated from the CONTEXT-commit SHA
    - `bash bin/check-neutrality.sh` exits 0
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-VERIFICATION.md; test -f "$F" && grep -q 'awaiting user render' "$F" && grep -qi 'Excluded files' "$F" && grep -q 'WRITE-BACK' "$F" && grep -qE 'DEBT-0[124]' "$F" && bash bin/check-neutrality.sh</automated>
  </verify>
  <done>13.1-VERIFICATION.md authored in Phase 13 mirror format with DEBT-02 diff matrix, DEBT-04 write-back evidence, and the DEBT-01 render checklist + "awaiting user render" capture slot; REQ-ID lines present; neutrality exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → `codex exec` cloud subprocess | Source content crosses to a cloud agent; only cloud_safe material may cross. |
| `codex exec` → filesystem | The cloud subprocess can write files; writes must be confined to a throwaway scratch dir. |
| DEBT-04 write-back → committed `wiki/` | Synthesized claims become durable; privacy inheritance and provenance must hold. |
| author → committed `13.1-VERIFICATION.md` | Evidence committed to main; local_only content must be redacted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.1-05 | Information Disclosure | `codex exec` subprocess (cloud) | mitigate | Seed ONLY the `cloud_safe` prospect-theory source (privacy: cloud_safe VERIFIED); never feed local_only material to Codex. [HIGH — block: do not run Codex side if any seeded source is local_only.] |
| T-13.1-06 | Tampering | `codex exec` filesystem writes | mitigate | `-C "$SCRATCH"` working root + `-s workspace-write`; `$SCRATCH` is throwaway `mktemp -d`; teardown after diff; never writes live `wiki/`. |
| T-13.1-07 | Information Disclosure | `13.1-VERIFICATION.md` (committed to main) | mitigate | Redaction-safe evidence only for any local_only-touching DEBT-04 content (counts/paths/labels); prefer cloud_safe-sourced synthesis. `bash bin/check-neutrality.sh` exits 0. |
| T-13.1-08 | Repudiation | DEBT-04 write-back provenance | mitigate | Every synthesized claim carries `[prov:source_id#locator]`; `bin/validate-op.sh` PASS before apply; §11.2 log line records the operation. |
</threat_model>

<verification>
- `bash bin/lint.sh --category yaml,provenance,orphan,crossref` clean after DEBT-04 write-back
- `bash bin/validate-op.sh` PASS recorded for the write-back op
- `bash bin/check-neutrality.sh` exits 0 on 13.1-VERIFICATION.md
- Scratch dirs torn down; `git status --porcelain` shows no scratch artifacts
- 13.1-VERIFICATION.md contains the DEBT-01 render checklist + "awaiting user render" slot
</verification>

<success_criteria>
- DEBT-02 structural-equivalence verdict recorded (Claude vs golden, Codex vs golden, Claude vs Codex); prose variance noted as non-failure
- DEBT-04 genuine write-back: durable provenanced page, validate-op PASS, §11.2 log line, one query() commit
- DEBT-01 render checklist + capture slot shipped (prepared-and-await; phase not blocked)
- 13.1-VERIFICATION.md mirrors Phase 13 format; neutrality clean
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-03-SUMMARY.md` (record the agent-parity diff matrix + prose-variance findings for Plan 04's agent-parity.md).
</output>
