---
phase: 13.1
reviewers: [claude]
reviewers_requested: [claude]
reviewers_failed: [gemini, codex]
review_cycle: 2
reviewed_at: 2026-06-01T00:00:00Z
plans_reviewed: [13.1-01-PLAN.md, 13.1-02-PLAN.md, 13.1-03-PLAN.md, 13.1-04-PLAN.md, 13.1-05-PLAN.md]
revision_commit: a01ff2f
prior_cycle_high_count: 14
current_high_count: 0
---

# Cross-AI Plan Review — Phase 13.1 (Convergence Cycle 2)

> **Reviewer coverage note (cycle 2):** Per the convergence runner, only the **Claude**
> reviewer ran this cycle. **Gemini** (headless OAuth / untrusted-directory) and **Codex**
> (network unreachable to the OpenAI backend, `os error 101`) remain non-functional in this
> environment and were not attempted. This is a single-model re-review of the revised plans
> (commit `a01ff2f`), grounded against the actual binaries and tracked files in the repo. Treat
> findings as high-confidence but un-cross-checked by an independent model — the same coverage
> caveat as cycle 1.

This cycle verifies that the revision (`a01ff2f`) resolves the **14 HIGH concerns** raised in
cycle 1. Each prior HIGH was re-checked against the revised plan text AND re-grounded against the
live repo (binaries, source files, frontmatter). The grounding commands and their outputs are
recorded inline so the verdicts are auditable, not asserted.

## Gemini Review

_Not attempted — non-functional in this environment (headless OAuth / untrusted workspace)._

---

## Codex Review

_Not attempted — non-functional in this environment (network unreachable to the OpenAI backend,
`os error 101` reconnect loop)._

---

## Claude Review

### 1. Summary

The cycle-1 review found Phase 13.1 architecturally sound but pervaded by **vacuous/blind
acceptance gates, self-contradictory criteria, two structurally-impossible success criteria, and
a real privacy exposure**. The revision (`a01ff2f`) addresses **all 14 HIGH concerns**, and every
fix was re-grounded against the repo this cycle and found to be **correct and load-bearing** — not
a paper acknowledgement. The deterministic substrate the cycle-1 review praised (wave/file-overlap
isolation, drift-guard core, `pagetypes()`/`provtargets()` helpers, codex CLI flags, skip-codex
degradation, prepared-and-await DEBT-01) is preserved. **No new HIGH concerns are introduced by
the revision**, and the two MEDIUM items most entangled with HIGHs (SC1 graph-cleanliness, the
Plan-02→04 count handoff) are also addressed. Residual risk drops from HIGH to **MEDIUM** — the
remaining risk is execution discipline against gates that are now honestly characterized as
blind-for-some-paths, plus the un-cross-checked single-model coverage.

### 2. Verdict on the 14 cycle-1 HIGH concerns

Every prior HIGH is **FULLY RESOLVED**. Grounding evidence (re-run this cycle):

| # | Cycle-1 HIGH | Resolution in `a01ff2f` | Verdict |
|---|--------------|--------------------------|---------|
| 1 | `check-neutrality.sh` exits 2 at baseline → every neutrality gate unsatisfiable | Plan 01 **Task 0 (pre-flight)** adds `neutrality_exempt: true` to `wiki/maintenance/lint-report.md`. Grounded: `bin/check-neutrality.sh` indeed exits 2 today on lines 82–83; `neutrality_exempt: true` is a *real* documented skip (script lines 66, 180–185, 222); drift findings are preserved (gate criterion requires it). | FULLY RESOLVED |
| 2 | Neutrality blind to `schema/` & `examples/` → Plans 01/02 gate on a no-op | Both plans now **state the blindness explicitly** and replace it with positive write-time proof: full-file reverse-substitution diff (Plan 01) + `^fixture-` id + synthetic-token assertions + a **targeted denylist grep over the created files** (Plans 01/02). Grounded: `PUBLIC_PATHS=(… wiki bin)` — `schema/` absent; `examples/` pruned (script lines 169, 202). | FULLY RESOLVED |
| 3 | Third `local_only` journal in seed dir → no mechanical guard | Plan 03 ships a **fail-closed `seed_guard`** run as a HARD precondition before any `codex exec`; aborts on any non-`cloud_safe` file. Grounded: the journal exists, has NO frontmatter (H1 only) → §13 fail-closed `local_only`; the guard's `head -20 \| grep '^privacy: cloud_safe$'` correctly fails it; prospect-theory source is `cloud_safe`. | FULLY RESOLVED |
| 4 | Sandbox flag ≠ egress protection | Plan 03 + threat register **record codex = cloud-by-default egress** (per §11.7), name the seed guard the single mandatory defense, and **ban `--dangerously-bypass-approvals-and-sandbox`** on this machine. Verification record mandated to state it plainly. | FULLY RESOLVED |
| 5 | Bare `kahneman` token in Plan 04's own instructions trips the gate | Plan 04 mandates **path-only references / the hyphenated `prospect-theory` slug / `<source-id>` placeholder**, never a bare `kahneman` token. Acceptance asserts `! grep thinking-fast`; prose guidance enumerates the trap. | FULLY RESOLVED |
| 6 | Sanctioned topical PROSE ("prospect theory", "loss aversion") trips the gate | Plans 04/05 mandate describing the golden **by path + type + count**, never bare topical phrases. Grounded: planting `prospect theory` prose under `docs/` **does** trip exit 2 (confirmed live), so the constraint is real and correctly stated. | FULLY RESOLVED |
| 7 | DEBT-02 structural equivalence impossible from a single seeded source | Plan 03 **retargets to the prospect-theory SUBSET** of the golden (enumerated via `grep -rl prospect-theory examples/kahneman/`) and makes **Claude-vs-Codex the PRIMARY reproducible verdict**; golden-subset columns are secondary. | FULLY RESOLVED |
| 8 | `validate-op.sh` has no CREATE op → DEBT-04 CREATE branch can't pass | Plan 03 **constrains DEBT-04 to an UPDATE** of an existing owning page. Grounded: `validate-op.sh` accepts only `UPDATE\|MERGE\|SUPERSEDE\|ARCHIVE` (no CREATE); the pinned owning page `wiki/concepts/progressive-disclosure.md` exists. | FULLY RESOLVED |
| 9 | `grep WRITE-BACK wiki/log.md` false-positive (7 pre-existing) | Plan 03 scopes the gate to a **new today-dated line diffed vs HEAD~1** (`git diff HEAD~1 -- wiki/log.md \| grep '^+.*WRITE-BACK'`) AND asserts ≥2 distinct `[prov:]` source_ids. Grounded: log has exactly 7 pre-existing WRITE-BACK lines, so the diff-scoped gate is necessary and correct. | FULLY RESOLVED |
| 10 | Dataview counts are author-tallied greps, never validated | Plan 02 builds counts from a **per-page attribute matrix** + emits a machine-consumable `\| query \| expected_count \|` table; Plan 04 **re-derives ≥1 count from live fixtures** and asserts equality; both carry forward the two Dataview-vs-grep divergence notes for the 13.2 render. | FULLY RESOLVED |
| 11 | Lint never walks `examples/` → "zero findings via example:true" vacuous/mis-attributed | Plan 02 **corrects the mechanism to the path-prefix** `EXCLUDE_DIRS={'maintenance','examples'}` (not `example:true`), drops the vacuous fixtures-lint gate, keeps `example:true` only as the §5/D-04 schema marker. Grounded: `EXCLUDE_DIRS = {'maintenance','examples'}` + `examples/` path-prefix exemption (lint.sh lines 395, 578). | FULLY RESOLVED |
| 12 | `quickstart.md:35` ships the same false `.obsidianignore` claim, no plan corrects it | Plan 05 **adds `docs/quickstart.md` to scope** and corrects the claim. Grounded: line 35 indeed asserts "hidden from Obsidian by default via `.obsidianignore`"; acceptance asserts the false phrasing is removed. | FULLY RESOLVED |
| 13 | Shipped `.obsidianignore` keeps a self-falsifying header, no plan edits it | Plan 05 **adds `.obsidianignore` to scope**, rewrites the header truthfully, and **keeps it in the `bin/release.sh` allowlist**. Grounded: header lines 2–3 assert the falsehood; the file is in the release allowlist (line 26). | FULLY RESOLVED |
| 14 | brownfield.md single-phrase-gated → ~14 stale Phase-11 refs + wrong script name (+ "four vs five") | Plan 05 broadens the pass: fix line 3 (suggest/verify shipped), **line 140 `04-privacy-classification.sh` → `04-privacy-review.sh`**, audit **all ~15 `Phase 11` refs**, assert **every `0[1-4]-*.sh` names a real migration file**, and mandate **FIVE subcommands (incl. review-typing), never "four"**. Grounded: `grep -c 'Phase 11'` = 15; line 140 wrong / line 308 correct; 4 real scripts present; §11.5 says "five subcommands". (This subsumes the cycle-1 "four vs five" HIGH.) | FULLY RESOLVED |

> Note on counting: cycle 1 itemized 16 HIGH-severity bullets; the convergence runner and the
> revision commit treat them as **14 HIGH concerns** (the "drift-guard blind to ~60%" and "four vs
> five subcommands" items pair with their parents). Under either counting, **all are FULLY
> RESOLVED** — the drift-guard 60%-blindness is closed by Plan 01's full-file reverse-substitution
> diff (now the PRIMARY gate, with the field/header grep demoted to a secondary cross-check).

### 3. New concerns this cycle

**No new HIGH concerns.** The revision is surgical — it tightens gates and pins scenarios without
re-architecting, and the grounding checks confirm the new gate logic matches the live repo. A
small number of LOW/MEDIUM observations remain (none blocking, none regressions):

- **[MEDIUM] SC1 graph-cleanliness is renegotiated, not delivered (carried, now mitigated).**
  Plan 03 reframes SC1's literal "graph not contaminated by examples/" to "examples/ forms a
  visually isolable sub-graph" and flags a formal SC1 decision record (`trigger_type: reframing`)
  as a Phase-13.2 follow-up. This is the honest resolution given Obsidian's single Excluded-files
  mechanism and D-03's ban on mutating committed `.obsidian/`. Acceptable as a documented
  renegotiation; the only residue is that the formal decision record is deferred to 13.2.
- **[LOW] Plan 03 `files_modified: wiki/` is a broad glob that nominally overlaps Plan 01's
  `wiki/maintenance/lint-report.md` within Wave 1.** In practice there is no real collision (Plan
  01 touches only `lint-report.md`; Plan 03's write-back touches `progressive-disclosure.md` /
  `index.md` / `log.md`), and cycle 1 explicitly praised the wave isolation. Could be narrowed to
  the specific paths for clarity, but not a correctness risk.
- **[LOW] Single-model coverage persists.** Gemini and Codex remain non-functional, so the revised
  plans — like the originals — are peer-reviewed by only one external model. Worth recording for
  the 13.2 closure gate; not a plan defect.

### 4. Suggestions (non-blocking)

- Create the SC1 reframing decision record (`trigger_type: reframing`) early in 13.2 rather than
  leaving SC1's renegotiation implicit in the verification note.
- Optionally narrow Plan 03's `files_modified` from `wiki/` to the three concrete paths it writes,
  to make the Wave-1 disjointness self-evident to tooling.

### 5. Risk Assessment

**Overall: MEDIUM** (down from HIGH in cycle 1). All 14 cycle-1 HIGH concerns are fully resolved
with fixes that were re-grounded against the live repo and found correct. The two
structurally-impossible criteria are honestly renegotiated (DEBT-02 → subset + Claude-vs-Codex
primary; SC1 → isolable sub-graph + deferred decision record), the privacy exposure now has a
mechanical fail-closed trigger, every self-contradictory gate is repaired, and the blind gates are
either replaced with positive write-time proofs or explicitly labelled backstops. Residual risk is
ordinary execution risk plus the un-cross-checked single-model coverage — not load-bearing on the
plans' own success criteria. Recommend proceeding to execution.

---

## Consensus Summary

> Single-reviewer cycle (Claude only); Gemini and Codex remain non-functional in this environment.
> "Consensus" below reflects the Claude run's grounded re-verification of the cycle-1 findings, not
> cross-model agreement.

### Agreed Strengths

- The revision **resolves all 14 cycle-1 HIGH concerns** with concrete, repo-grounded fixes
  (verified this cycle against the actual binaries and tracked files), not paper acknowledgements.
- The architecture cycle-1 praised is **preserved**: clean wave/file-overlap isolation, single-owner
  index registration, the drift-guard core (now extended to a full-file reverse-substitution
  primary gate), `pagetypes()`/`provtargets()` helpers, skip-codex degradation, and prepared-and-
  await DEBT-01.

### Agreed Concerns (highest priority)

- **None at HIGH severity.** Remaining items are MEDIUM/LOW: SC1 is renegotiated (decision record
  deferred to 13.2), Plan 03's `wiki/` glob is broad-but-harmless, and the review remains
  single-model (no independent cross-check).

### Divergent Views

- None available — single-reviewer cycle. The absence of an independent model is itself the one
  carried coverage gap, to be noted for the Phase 13.2 closure gate.
