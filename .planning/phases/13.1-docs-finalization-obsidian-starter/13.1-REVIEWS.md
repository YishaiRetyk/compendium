---
phase: 13.1
reviewers: [claude]
reviewers_requested: [gemini, codex, claude]
reviewers_failed: [gemini, codex]
reviewed_at: 2026-05-31T23:26:13Z
plans_reviewed: [13.1-01-PLAN.md, 13.1-02-PLAN.md, 13.1-03-PLAN.md, 13.1-04-PLAN.md, 13.1-05-PLAN.md]
---

# Cross-AI Plan Review — Phase 13.1

> **Reviewer coverage note:** Three reviewers were requested (`--gemini --codex --claude`).
> Only **Claude** ran successfully. **Gemini** failed (headless OAuth authentication
> cancelled / untrusted-directory in this environment) and **Codex** failed (network
> unreachable to the OpenAI backend websocket — `os error 101` reconnect loop, even with
> the sandbox disabled). The Claude reviewer is a multi-agent, repo-grounded run (11 agents:
> 5 per-plan, 5 cross-cutting, 1 synthesizer) that verified claims against the actual
> binaries and tracked files, so the single review is unusually deep. Treat its findings as
> high-confidence but un-cross-checked by an independent model.

## Gemini Review

_Reviewer failed to run — headless OAuth authentication was cancelled and the working
directory was untrusted (`GEMINI_CLI_TRUST_WORKSPACE` / `--skip-trust` did not clear the
auth prompt). No review produced._

---

## Codex Review

_Reviewer failed to run — `codex exec` could not reach the OpenAI backend
(`wss://chatgpt.com/backend-api/codex/responses` → Network unreachable, os error 101;
5/5 reconnect attempts failed). No review produced._

---

## Claude Review

The adversarial review ran 11 repo-grounded agents (5 per-plan, 5 cross-cutting, 1
synthesizer) and verified claims against the actual binaries and tracked files. Strongest
findings are concrete and load-bearing (a third `local_only` journal in the seed dir,
`check-neutrality.sh` exiting 2 at baseline, the `kahneman`-token trap in Plan 04's own
instructions, `validate-op.sh` having no CREATE op, 7 pre-existing `WRITE-BACK` log entries).

### 1. Summary

Phase 13.1 is a well-architected, mostly-correct docs-and-fixtures phase whose
**deterministic substrate is sound but whose acceptance gates are pervasively vacuous or
self-contradictory**. The wave structure, file-overlap isolation, dependency wiring (incl.
the skip-codex degradation path), and the prepared-and-await framing for the deferred
Obsidian render (DEBT-01) are all correct and verified by multiple sub-reviewers. The
drift-guard catches the structural mutations that matter (field reorder/add/remove, section
reorder). However, a recurring, load-bearing failure mode runs through every plan: **the
plans gate on `bin/check-neutrality.sh exits 0` and `bin/lint.sh clean` for files those tools
physically never scan** (`schema/`, `examples/`, `.planning/`, `docs/` for lint), producing
false assurance — and `check-neutrality.sh` **already exits 2 at baseline** on a tracked file
no plan touches, making the criterion unsatisfiable regardless of plan quality. Two structural
success criteria (SC1 "graph not contaminated by examples/"; DEBT-02 "structural equivalence
vs golden") are **literally unachievable as written** — the former because Obsidian's single
Excluded-files mechanism cannot keep fixtures indexed for Dataview while hiding them from the
graph, the latter because a single-source seed cannot reproduce a two-source golden cluster.
The highest-severity safety finding is a real `local_only` personal journal sharing the seed
directory with the cloud-bound source, with no mechanical seed-time guard. None of these are
unfixable, but the phase as planned would "pass" gates while under-delivering on its own
success criteria.

### 2. Strengths

- **Drift-guard catches the structural mutations that matter** — empirically verified to catch reordered/added/removed frontmatter fields (value- AND order-sensitive) and renamed/added/removed `## ` section headers across all 6 real templates; correct zero-diff/exit-0 on correct token substitution.
- **decision.md asymmetry is accurately characterized** — the six decision-only fields correctly identified; the other five templates correctly lack `trigger_type`. `{{date:YYYY-MM-DD}}` is valid Obsidian Templates-core syntax.
- **Clean wave/file-overlap isolation** — every plan's `files_modified` set is disjoint within its wave; index.md registration correctly deferred to the single Wave-3 owner (Plan 05).
- **`pagetypes()`/`provtargets()` helpers are real and correct** — against live `examples/kahneman/` returns exactly `1 comparison, 3 concept, 1 entity, 1 overview, 2 source`, with correct FLAT-golden vs nested-scratch branching and correct exclusion of root README.md/log.md.
- **codex CLI flags verify against the installed binary** — `-C`, `-s workspace-write`, `--skip-git-repo-check`, `-o`, the bypass flag, and `codex login status` all exist.
- **Prepared-and-await (DEBT-01) is honestly scoped** — fixtures + checklist + capture slot with an explicit "awaiting user render" sentinel; REQ-ID left unchecked; deferred to Phase 13.2 SC5 audit. Pitfall-1 correctly propagated.
- **Skip-codex degradation is correctly designed** — Plan 03 completes the Claude-vs-golden diff even when Codex is auth-blocked; the autonomous:false checkpoint never hard-blocks the phase.
- **Synthetic fixture slugs** (`fixture-entity-1`, domains `alpha`/`beta`) are the correct write-time neutrality posture per D-12.
- **The .obsidianignore correction targets a genuinely-verified falsehood** — `app.json == {}`, examples/ IS indexed today, yet the shipped header falsely claims it hides examples/.

### 3. Concerns

#### [HIGH] `check-neutrality.sh` exits 2 at baseline — every plan's "neutrality exits 0" gate is unsatisfiable
*(independently flagged by 2 sub-reviewers)* `bash bin/check-neutrality.sh` at repo root **already exits 2** on `wiki/maintenance/lint-report.md:82: kahneman` / `:83: personal-decision-journal` (git-tracked, referenced by no plan). Touches all five plans — the gate is a guaranteed false-negative regardless of output, and risks an executor rationalizing away the pre-existing failure (and masking a real Plan 03 live-wiki write-back leak). **Must be fixed before Wave 1.**

#### [HIGH] `check-neutrality.sh` does NOT scan `schema/` or `examples/` — Plans 01 & 02's primary/backstop gate is blind to the files they create
*(flagged by 3+ sub-reviewers)* `PUBLIC_PATHS=(AGENTS.md CLAUDE.md README.md PRIVACY.md docs .github wiki bin)` — `schema/` absent; `examples/` absent AND explicitly pruned. Proven by planting `examples/dataview-fixtures/leaktest.md` with private terms → exit 0. The gates are no-ops for Plan 01's `schema/obsidian/*.md` and Plan 02's fixtures; neutrality rests entirely on author discipline, which the plans must state explicitly and back with positive synthetic-slug assertions + a targeted denylist grep.

#### [HIGH] A third, `local_only` personal journal shares the seed directory — no mechanical seed-time guard prevents egress to the OpenAI cloud
`sources/2026/2026-04/` contains THREE sources: `2026-04-10-personal-decision-journal/source.md` has no frontmatter (just an H1) → per §13 fail-closed it is `local_only`, and its body is a real salary negotiation + personal confessions. Plan 03 says "seed ONLY the cloud_safe source" in prose but mandates no file-level enumeration; any `cp -r .../2026-04/*` or month-glob egresses the journal to Codex. T-13.1-05 is marked `[HIGH — block]` but the block has no mechanical trigger.

#### [HIGH] The sandbox flag provides ZERO egress protection — the DEBT-02 privacy story rests on seeding correctness alone
`codex exec` runs on OpenAI infra; whatever it reads becomes cloud context. `-s/--sandbox` governs only *local* shell-command execution, not network egress; `-C` is a cwd, not a jail. The plan leans on `-C`/`-s` as if they bound egress, making the (untriggered) seeding guard the single mandatory defense, not defense-in-depth. The verification record must state codex = cloud-by-default per §11.7's own verifier-locality contract.

#### [HIGH] Plan 04's own instructions trip the neutrality gate — the bare `kahneman` token survives scrubbing
`check-neutrality.sh` only scrubs lines matching `examples/kahneman[...]` before denylist-matching. Plan 04 Task 3 instructs naming `sources/2026/2026-04/2026-04-10-kahneman-prospect-theory/source.md` and a `codex exec ... 2026-04-10-kahneman-prospect-theory` invocation — neither is an `examples/kahneman` path, so the bare `kahneman` token survives → exit 2, while the same task requires exit 0. Literal execution is impossible. Fix: use only the hyphenated `prospect-theory` token or a `<source-id>` placeholder, never `kahneman` on the same line.

#### [HIGH] Plans 04/05 highest *real* leak risk: sanctioned `examples/kahneman/` PROSE references trip the gate
Describing the golden cluster invites prose like "the prospect theory concept" / "loss aversion page" — those phrases ARE on the denylist, and the line-scrub removes only the literal path token. Tested: `See: examples/kahneman/concepts/loss-aversion.md for prospect theory.` → exit 2. Author guidance must mandate path-only references and describe the cluster by path/type/count, never bare topical phrases.

#### [HIGH] DEBT-02 "structural equivalence vs golden" is structurally impossible from a single seeded source
The golden cluster is a two-source synthesis; the plan seeds only prospect-theory. `grep -rl thinking-fast-and-slow-part1` → 5 dependent golden pages. A one-source ingest can never produce the 6-page multiset; RESEARCH even contradicts itself ("3 of 6 golden pages" vs "same multiset"). Honest fix: make structural-equivalence target the prospect-theory **subset**, and make Claude-vs-Codex (which IS reproducible) the primary parity verdict.

#### [HIGH] `validate-op.sh` has no CREATE op — DEBT-04's "CREATE a typed page" branch cannot pass the mandated gate
`grep -n CREATE bin/validate-op.sh` → no matches (accepts only `UPDATE|MERGE|SUPERSEDE|ARCHIVE`, matching §9). For a cross-source synthesis "NOT on any single page," CREATE is the likely-correct action, for which the mandated `validate-op.sh → PASS` has no codepath. Fix: constrain DEBT-04 to an UPDATE scenario, OR document that CREATE is validated by lint + frontmatter checklist and make the criterion match.

#### [HIGH] `grep 'WRITE-BACK' wiki/log.md` is a false-positive gate — 7 entries already exist
`grep -c 'WRITE-BACK' wiki/log.md` → 7 pre-existing, so the gate passes unconditionally even if Task 2 never executes. Fix: scope to a new today-dated entry + chosen page_id, or diff log.md against HEAD~1 for exactly one new line; better, assert the affected page's diff adds a new `[prov:]` marker.

#### [HIGH] Expected Dataview row counts are author-tallied greps, never validated against real Dataview semantics
SC1 requires Dataview render with expected counts, but the only checks are anchored greps that don't model Dataview. Divergences: `WHERE !privacy` matches omitted AND present-but-null (grep `-L '^privacy:'` only catches omission); `contains(domains,"alpha")` needs a YAML **list**, not a scalar. Fix: pin a per-page attribute matrix, derive each count deterministically, mandate domains/tags always be lists, label counts as static grep-proxies and flag divergence-prone queries for the 13.2 render.

#### [HIGH] Lint never walks `examples/` — Plan 02's "zero findings via example:true" is vacuous and mis-attributed
`bin/lint.sh` walks `wiki/` only; `examples/dataview-fixtures/` is never reached. The "zero findings" proves nothing, and `example: true` is NOT the operative skip mechanism for this path — the **path-prefix** is (proven: `examples/kahneman/.../daniel-kahneman.md` carries no `example: true` yet is never linted). Fix: state the skip mechanism honestly, keep `example: true` as the §5/D-04 schema marker, and drop the un-failable acceptance.

#### [HIGH] `quickstart.md` line 35 ships the SAME false .obsidianignore claim — and NO plan corrects it
Plan 04 corrects only `examples.md`. The identical "hidden from Obsidian by default via .obsidianignore" claim lives at `docs/quickstart.md:35` (a more prominent onboarding doc) and is in zero plans' `files_modified`. A user following quickstart is told examples/ is hidden, opens Obsidian, sees it fully indexed. Fix: add quickstart.md to Plan 04/05's accuracy pass (D-10 makes it in-scope — it is demonstrably drifted).

#### [HIGH] The shipped `.obsidianignore` file keeps a self-falsifying header — a known-false artifact ships to every template user
`.obsidianignore` is copied into every release via `bin/release.sh` ALLOWLIST; its header asserts it "hides examples/ from the Obsidian graph view + content indexing" (false). No plan edits the file. Post-fix, `examples.md` will say ".obsidianignore does NOT hide anything" while the shipped file three dirs up still claims it does — a contradiction shipped in the same release. Fix: rewrite the header (add the file to a plan's scope) or drop it from the allowlist.

#### [HIGH] `brownfield.md` accuracy-pass is single-phrase-gated — ~14 stale "Phase 11" refs (incl. a wrong script name) sail through
*(flagged by 2 sub-reviewers)* The gate catches only line 3, but `grep -c 'Phase 11'` = 15, ~6+ still imply unshipped work. Worse, line 140 names `04-privacy-classification.sh` but the shipped script AND §11.5 call it `04-privacy-review.sh` — a factual error the gate can't detect. Fix: add `! grep -qi '04-privacy-classification'`, assert every `0[1-4]-*.sh` reference matches a real migration file, audit all 15 refs.

#### [HIGH] Plan 05 prescribes "four subcommands" but §11.5 defines FIVE — the prescribed fix introduces NEW drift
§11.5 states "five subcommands" (scan/bootstrap/suggest/**review-typing**/verify); brownfield.md already documents `## review-typing`. Following the plan literally writes "four," contradicting the source of truth (and tripping Plan 05's own T-13.1-12 "docs-drift-while-fixing" threat). Fix: correct to "five (…/review-typing/…)".

#### [HIGH] Plan 01 drift-guard is blind to ~60% of each file
*(flagged by 2 sub-reviewers)* The gate diffs only `^[a-z_]+:` fields and `^## ` headers — the FORBIDDEN PATTERNS HTML-comment block and per-section guidance comments are unchecked. Proven: deleting the FORBIDDEN PATTERNS line and inserting a sneaky guidance comment both yield exit 0, so "copy VERBATIM" is unverifiable. Fix: add a full-file reverse-substitution diff (`sed` the tokens back, then whole-file `diff` must be empty).

#### [MEDIUM] SC1 "graph not contaminated by examples/" is never delivered — the checklist normalizes contamination
*(2 sub-reviewers)* Obsidian's single Excluded-files mechanism governs BOTH indexing and graph — you cannot keep fixtures indexed for Dataview while hiding them from the graph. Plan 02 ADDS 8–12 orphan pages to the indexed graph; Plan 03 "resolves" SC1 by asking the user to confirm an "isolated sub-graph" (still contamination, just disconnected). D-03 forbids mutating `.obsidian/`, arguably making SC1-as-written unsatisfiable. Fix: renegotiate SC1 via a decision record to "examples/ forms a visually isolable sub-graph," OR document a one-time Settings→Excluded-files step; optionally give fixtures one intra-fixture wikilink so they cluster.

#### [MEDIUM] Plan 02→04 count-matrix handoff has no machine-consumable schema, and Plan 04 never validates the counts
*(2 sub-reviewers)* The handoff is freeform prose; Plan 04's gate checks only `grep -c 'FROM "examples/dataview-fixtures"' >= 5` and the word "expected" — never that numbers are correct. Fix: Plan 02 emits a fenced `| query | expected_count |` table; Plan 04 re-derives ≥1 count from live fixtures and asserts equality.

#### [MEDIUM] Scratch-dir lint criterion targets the LIVE wiki (missing path arg)
`bash bin/lint.sh --category yaml,provenance` defaults `WIKI_DIR=wiki/`; Plan 01/03 omit `$SCRATCH/wiki`, so it lints the live tree. Also a single-source scratch may have broken provenance, so "exits clean" may be unachievable. Fix: pass `"$SCRATCH/wiki"`, scope provenance to the one seeded source.

#### [MEDIUM] No trap/teardown — scratch dirs (with copied source content) leak on crash; git-status check can't see /tmp
*(2 sub-reviewers)* `mktemp -d` → `/tmp/tmp.XXXX` (no "scratch" substring, outside repo), so `git status --porcelain | grep scratch` is doubly blind, and there's no `trap`. Fix: `trap 'rm -rf "$S1" "$S2"' EXIT INT TERM` before risky work; assert `! test -d $SCRATCH` on absolute paths.

#### [MEDIUM] `--dangerously-bypass-approvals-and-sandbox` fallback grants account-wide read to a cloud agent
The fallback disables the OS sandbox; `-C` is a cwd, not a jail. Model-generated commands can read anywhere the user can — including the `local_only` journal in the same tree. Fix: prefer `-c approval_policy` config or the skip-codex path; if used, run from an isolated /tmp tree with no repo data in its parent, and document the exposure.

#### [MEDIUM] T-13.1-05 "HIGH — block" has no mechanical trigger
The only acceptance ("no local_only source seeded") is a post-hoc self-check by the agent that did the seeding. Fix: a deterministic pre-codex guard resolving effective privacy (fail-closed) for every file under `$SCRATCH/sources`, non-zero exit on any non-`cloud_safe`, run as a hard precondition.

#### [MEDIUM] DEBT-04 cross-source synthesis is underspecified — risks a contrived write-back
Task 2 names no concrete question, source pair, or owning page; the live wiki is financial-AI/DDD/Ralph (not Kahneman), so the easiest path to satisfy gates is a thin "connection," and two executors produce different write-backs (SC5 reproducibility). Fix: pin a concrete scenario (named question + two specific live source IDs + expected owning page) + assert new claims carry `[prov:]` from ≥2 distinct source_ids.

#### [MEDIUM] Plan 04 has no branch for Plan 03's skip-codex outcome it depends on
If Plan 03 resolves skip-codex, the Codex-vs-golden column is empty, but Plan 04's gates (`grep codex exec`, table shape) are satisfiable from method-docs alone — the doc could ship asserting parity never measured. Fix: mark the Codex row "pending — deferred to 13.2 (Codex auth unavailable)" rather than emptying/fabricating it.

#### [MEDIUM] Plan 04/05 `bin/lint.sh` verification line is a no-op for the docs they author
lint scans `wiki/` only; all Plan 04/05 deliverables live under `docs/reference/`. Fix: drop the misleading lint line or replace with link-resolution/required-section checks.

#### [MEDIUM] `check-neutrality.sh` doesn't scan `.planning/` — Plan 03's "exits 0 on 13.1-VERIFICATION.md" is mechanically impossible
The file lives outside PUBLIC_PATHS. Fix: scope the gate to paths the plan writes, or run a denylist grep directly over the file.

#### [MEDIUM] Plan 05 index-coverage gate has a substring false-pass: `examples.md` ⊂ `commit-examples.md`
`grep -q "$b"` with `$b=examples.md` matches a `commit-examples.md` line, so examples.md can be silently dropped. Fix: anchor to the link form `grep -qE '\]\('"$b"'\)'`.

#### [MEDIUM] Plan 05 stub-free gate fails on Plan-04-owned files Plan 05 cannot edit
The three remaining stubs are Plan 04's; if Plan 04 under-delivers, Plan 05's recursive `! grep -rl 'Status: stub' docs/reference/` fails on files outside its `files_modified`. Fix: scope to owned files, or make it an explicit abort-with-message precondition.

#### [MEDIUM] Plan 03 (autonomous:false, blocking checkpoint) sits in Wave 1 with autonomous Plans 01/02 — barrier hazard
Wave 2 depends on 02 and 03; Plan 03's blocking codex-auth checkpoint is on the docs critical path purely for the optional Codex column. Fix: document that the Wave 1→2 barrier is satisfied once Plan 02 completes AND Plan 03 reaches checkpoint resolution (authenticated OR skip-codex) — the Codex run need not finish.

#### [MEDIUM] Plan 05 CONTRIBUTING.md "verify recipes accurate" is a rubber-stamp; OBSID-03 negative gate misses plain dataview blocks
The CONTRIBUTING check only confirms the index-pointer string exists. The OBSID-03 gate `! grep -rl 'dataviewjs\|hotkey'` misses plain ` ```dataview ` dashboards (forbidden by OBSID-03 scope) and `.css/.canvas/.base` files. Fix: quote verified recipe lines in SUMMARY; extend the gate to also catch ` ```dataview ` fences + a file-extension guard (note: file-side gate is NEGATIVE vs doc-side POSITIVE — prevent executor inversion).

#### [LOW] Traceability + spec-hygiene cluster
Plan 01 line 171 wrongly attributes obsidian-starter.md index registration to Plan 04 (it's Plan 05). Greedy `sed 's/ {{.*}}//'`; the thinking-fast exclusion is misattributed to privacy (it's a copyright placeholder — both sources are cloud_safe); three same-titled "Privacy Model" surfaces with no cross-link mandate; plans cite CLAUDE.md as authority where AGENTS.md is the byte-source. Each minor; collectively they argue for one "normalize the gates and authoring constraints" pass before execution.

### 4. Suggestions

**Pre-flight (before Wave 1):**
- Make `check-neutrality.sh` green at baseline: neutralize/regenerate `wiki/maintenance/lint-report.md`, OR add `wiki/maintenance/` to a scan exemption (control-plane artifact). Without this, every neutrality gate is noise.
- Decide neutrality scope honestly: either add `schema` (and handle `examples/`) to `PUBLIC_PATHS`, or replace the gate with content-free positive assertions for the paths each plan creates.

**Gate tightenings:**
- **Plan 01:** Full-file reverse-substitution diff as the *primary* gate; anchor token strip; extend OBSID-03 negative to plain ` ```dataview ` + config/canvas files.
- **Plan 02:** Drop the vacuous neutrality/lint acceptance; add positive synthetic-slug assertions (`^fixture-`, domains in `{alpha,beta}`), a targeted denylist grep over the fixtures, source-field presence checks, and a fenced count matrix.
- **Plan 03:** Fail-closed pre-codex seed guard + per-file `privacy: cloud_safe` assertion; `trap` teardown; pass `"$SCRATCH/wiki"` to lint; scope the WRITE-BACK grep to a new dated line + assert a new `[prov:]`; resolve CREATE-vs-validate-op; redefine DEBT-02 to the prospect-theory subset with Claude-vs-Codex primary; pin a concrete DEBT-04 scenario; drop the bypass fallback on this machine.
- **Plan 04:** Mandate path-only kahneman references; add a skip-codex branch for the Codex row; re-derive ≥1 Dataview count from live fixtures; drop the no-op lint line.
- **Plan 05:** "four"→"five (…/review-typing/…)"; broaden the brownfield gate (`04-privacy-classification` + every `0[1-4]-*.sh` real-file assertion + all-15-refs audit); anchor the index grep; scope the stub check to owned files; add `docs/quickstart.md` and the `.obsidianignore` header to scope.

**Spec/ordering:**
- Add a decision record renegotiating SC1 to "examples/ forms a visually isolable sub-graph" (or ship a one-time Settings→Excluded-files step), since D-03 makes literal "not contaminated" unsatisfiable.
- Document the Wave 1→2 barrier semantics.
- State plainly in 13.1-VERIFICATION.md that codex = cloud egress by default; sandbox confines local writes only.
- Standardize source-of-truth pointers on AGENTS.md (the byte-source).

### 5. Risk Assessment

**Overall: HIGH.** The phase carries an unusually dense cluster of HIGH findings that are
*load-bearing on its own success criteria*, not cosmetic. Three categories compound:
(1) **gates that cannot fail** — neutrality/lint/WRITE-BACK greps that are vacuous or blind for
the exact files each plan creates, plus a `check-neutrality` baseline that already exits 2, so
the phase could be reported "passing" while delivering little verified value; (2) **success
criteria that cannot be met as written** — SC1's graph-cleanliness (mutually exclusive with
keep-indexed-for-Dataview) and DEBT-02's golden-equivalence (single source vs two-source
golden); and (3) **a real privacy exposure** — a `local_only` personal journal in the
cloud-bound seed directory with no mechanical guard, compounded by the belief that the sandbox
flag bounds egress. Several HIGH findings are self-contradictory acceptance criteria (the
kahneman-token trap, CREATE-vs-validate-op) that make literal execution impossible. The
mitigating factors are real: the architecture, wave/dependency wiring, drift-guard core,
prepared-and-await framing, and skip-codex degradation are correct, and **every HIGH finding is
fixable with targeted gate/spec edits without re-architecting the phase.** Recommend a single
pre-execution "normalize gates + privacy guard + reconcile SC1/DEBT-02 scope" pass before
dispatching Wave 1; with that pass, residual risk drops to MEDIUM.

**On the specific focus questions** (verified answers): the `.obsidianignore` resolution is
*correct that examples/ is indexed today* (`app.json == {}`) but **incomplete** — the false
claim survives in the shipped file itself and in `quickstart.md:35`, neither in any plan's
scope. DEBT-01 "prepared-and-await" is a **legitimate** completion for what it scopes, but
SC1's "graph not contaminated" is *not* closeable by any plan (it's renormalized, not
delivered). DEBT-04's "novel synthesis" is **not concrete enough** to be reproducible. The
Codex egress risk is **real and under-guarded** (third local_only journal + no mechanical seed
gate + sandbox≠egress). And the drift-guard **misses ~60% of each template file**.

---

## Consensus Summary

> Only one of three requested reviewers (Claude) ran; Gemini and Codex failed on
> environment/auth/network constraints. There is no second model to cross-check against, so
> "consensus" below reflects agreement *among the Claude run's 11 sub-agents* (several findings
> were independently flagged by 2–3 sub-reviewers) rather than cross-model consensus.

### Agreed Strengths
- The phase **architecture** is sound: clean wave/file-overlap isolation, correct dependency wiring, single-owner index registration, and an honestly-scoped prepared-and-await for DEBT-01.
- The **deterministic substrate is real and verified**: the drift-guard catches field/section structural mutations; the `pagetypes()`/`provtargets()` helpers compute the correct golden multiset; codex CLI flags exist on the installed binary; skip-codex degradation is correctly designed.

### Agreed Concerns (highest priority — multiply-flagged)
1. **Vacuous/blind acceptance gates** — `check-neutrality.sh` does not scan `schema/`, `examples/`, or `.planning/`, and `lint.sh` does not walk `examples/` or `docs/`, yet every plan gates on them. Plus `check-neutrality.sh` already exits 2 at baseline. The gates produce false assurance.
2. **Real privacy exposure** — a third `local_only` personal journal shares the cloud-bound seed directory; the seeding guard is prose-only with no mechanical trigger; the sandbox flag does not bound egress.
3. **Self-contradictory / impossible criteria** — the bare-`kahneman`-token trap in Plan 04's own instructions, `validate-op.sh` having no CREATE op, the 7-pre-existing-`WRITE-BACK` false-positive gate, and Plan 01's drift-guard missing ~60% of each file.
4. **Success criteria unachievable as written** — SC1 graph-cleanliness vs keep-indexed-for-Dataview is mutually exclusive under Obsidian's single Excluded-files mechanism; DEBT-02 golden-equivalence is impossible from a single seeded source against a two-source golden.
5. **Drifted docs out of scope** — the false `.obsidianignore` claim also lives in `docs/quickstart.md:35` and in the shipped `.obsidianignore` header, neither of which any plan corrects; `brownfield.md` has ~14 stale Phase-11 refs and a wrong script name; Plan 05's "four subcommands" contradicts §11.5's five.

### Divergent Views
- None available — single-reviewer run. No independent model produced an opposing or
  corroborating view. This is itself a coverage gap worth noting for the 13.2 closure gate:
  the Phase 13.1 plans were peer-reviewed by only one external model.
