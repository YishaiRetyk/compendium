---
phase: 13.1-docs-finalization-obsidian-starter
plan: 05
type: execute
wave: 3
depends_on: [01, 04]
files_modified:
  - docs/reference/brownfield.md
  - docs/reference/ci.md
  - docs/reference/three-layer-model.md
  - docs/reference/dataview-queries.md
  - docs/reference/release.md
  - docs/reference/index.md
  - CONTRIBUTING.md
autonomous: true
requirements: [DEBT-02, DEBT-01]
user_setup: []

must_haves:
  truths:
    - "The five substantial reference docs are accuracy-passed — edited ONLY where they drifted from the shipped v1.1 surface, not churned"
    - "brownfield.md no longer describes suggest/verify as unshipped Phase 11 stubs (they shipped)"
    - "docs/reference/index.md lists EVERY reference file, including the previously-omitted dataview-queries.md, commit-examples.md, setup-prerequisites.md, agent-parity.md, and the new obsidian-starter.md"
    - "CONTRIBUTING.md merge-conflict recipes for index.md and log.md are verified accurate; an index pointer to them exists (no new merge-conflicts.md)"
    - "No reference file is missing from the index; no stub status line remains anywhere under docs/reference/"
  artifacts:
    - path: "docs/reference/index.md"
      provides: "Reconciled catalog listing every reference file"
      contains: "agent-parity.md"
  key_links:
    - from: "docs/reference/index.md"
      to: "every docs/reference/*.md"
      via: "one catalog line per file"
      pattern: "obsidian-starter.md"
    - from: "docs/reference/index.md"
      to: "CONTRIBUTING.md merge-conflict recipes"
      via: "index pointer"
      pattern: "CONTRIBUTING.md"
---

<objective>
Accuracy-pass the five substantial reference docs (edit ONLY on drift), verify CONTRIBUTING.md's merge-conflict recipes, and reconcile `docs/reference/index.md` so it lists every reference file (including the new `obsidian-starter.md` from Plan 01 and `agent-parity.md` from Plan 04, plus the previously-omitted `dataview-queries.md`, `commit-examples.md`, `setup-prerequisites.md`).

Purpose: Complete SC4 — every `/docs/reference/` file is filled out and discoverable, with no drift from the shipped v1.1 surface and no orphaned (unlisted) reference files.
Output: edited brownfield/ci/three-layer-model/dataview-queries/release docs (on-drift only), a reconciled index.md, and a verified CONTRIBUTING.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-CONTEXT.md
@.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-PATTERNS.md
@.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-RESEARCH.md

<interfaces>
<!-- Accuracy-pass policy (D-10): edit ONLY where drifted from the shipped v1.1 surface.       -->
<!-- Do NOT churn already-good content. CLAUDE.md is the source of truth; the doc is the bug.   -->

Known drift to fix (verified):
  brownfield.md  — opening describes suggest/verify as "two stubs ... that ship in Phase 11".
                   Phase 11 SHIPPED both. Update to reflect all four subcommands shipped
                   (scan/bootstrap/suggest/verify) per CLAUDE.md §11.5.
Verify (edit only if drifted):
  ci.md          — severity-remap table + --staged (Phase 12.2) contract vs CLAUDE.md §11.3.
  release.md     — allowlist matches bin/release.sh.
  dataview-queries.md — the 5 canonical queries match CLAUDE.md §16 App. A.
  three-layer-model.md — decision-record link resolves; likely stable.

index.md current entries (7): schema-tour, brownfield, privacy-model, three-layer-model, ci,
examples, release. MISSING (must add): dataview-queries.md, commit-examples.md,
setup-prerequisites.md, agent-parity.md (Plan 04), obsidian-starter.md (Plan 01).
Catalog line shape: `- [file](file) — <one-line> (Phase N)`. Drop "(Phase 12) populated-later"
framing from the now-authored stub descriptions.

CONTRIBUTING.md merge-conflict section: `## Merge conflicts` (~line 58) covers wiki/log.md
(append-only / merge=union opt-in) + wiki/index.md. Verify the recipes are still accurate; add
an index pointer (D-11: NO new merge-conflicts.md).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Accuracy-pass the five substantial reference docs (edit on drift only)</name>
  <read_first>
    - docs/reference/brownfield.md, ci.md, three-layer-model.md, dataview-queries.md, release.md (the five accuracy-pass targets — each is its own analog)
    - CLAUDE.md §11.5 (brownfield subcommands), §11.3 (CI mode / --staged), §16 Appendix A (canonical Dataview queries)
    - bin/release.sh (release allowlist source of truth)
    - .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-PATTERNS.md (the accuracy-pass section)
  </read_first>
  <files>docs/reference/brownfield.md, docs/reference/ci.md, docs/reference/three-layer-model.md, docs/reference/dataview-queries.md, docs/reference/release.md</files>
  <action>
    Apply a correctness pass to the five docs. Policy (D-10): edit ONLY where drifted from the shipped v1.1 surface; do NOT rewrite already-good content.
    - brownfield.md (KNOWN DRIFT): the opening paragraph calls `suggest` and `verify` "two stubs ... that ship in Phase 11". Both shipped (Phase 11 complete). Update the description to reflect all four subcommands shipped (scan/bootstrap/suggest/verify) consistent with CLAUDE.md §11.5. Fix any other Phase-11-pending phrasing in the doc.
    - ci.md: verify the severity-remap table and the `--staged` (Phase 12.2) contract match CLAUDE.md §11.3. If they match, leave unchanged; if drifted, fix to match §11.3 (the source of truth).
    - release.md: verify the documented allowlist matches `bin/release.sh`. Fix only on drift.
    - dataview-queries.md: verify the 5 canonical queries match CLAUDE.md §16 Appendix A. Fix only on drift.
    - three-layer-model.md: confirm the decision-record link resolves; fix only if broken.
    For each doc, if NO drift is found, make no edit and record "no drift" in the SUMMARY (it is valid for a file to be untouched).
    Neutrality (D-12): any edits use placeholders / sanctioned kahneman references only.
  </action>
  <acceptance_criteria>
    - brownfield.md drift fixed: `! grep -qi 'stubs.*ship in Phase 11\|suggest.*verify.*stub' docs/reference/brownfield.md` (no longer describes suggest/verify as unshipped stubs)
    - ci.md still consistent with §11.3: `grep -q -- '--staged' docs/reference/ci.md` (the Phase 12.2 contract is present)
    - dataview-queries.md still has 5 dataview blocks: `grep -c '```dataview' docs/reference/dataview-queries.md` >= 5
    - `bash bin/lint.sh --category yaml,provenance,orphan,crossref` clean
    - `bash bin/check-neutrality.sh` exits 0
    - `bash bin/sync-claude.sh --check` exits 0 (no incidental AGENTS/CLAUDE drift introduced)
  </acceptance_criteria>
  <verify>
    <automated>! grep -qi 'stubs.*ship in Phase 11' docs/reference/brownfield.md && [ "$(grep -c '```dataview' docs/reference/dataview-queries.md)" -ge 5 ] && bash bin/lint.sh --category yaml,provenance,orphan,crossref && bash bin/check-neutrality.sh</automated>
  </verify>
  <done>The five docs are accuracy-passed; brownfield.md no longer calls suggest/verify unshipped; other docs verified consistent (edited only on drift); lint + neutrality + sync-claude clean.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile docs/reference/index.md + verify CONTRIBUTING.md merge-conflict recipes</name>
  <read_first>
    - docs/reference/index.md (current 7-entry catalog to reconcile)
    - The full file list: `ls docs/reference/*.md` (must end with every file listed)
    - docs/reference/obsidian-starter.md (Plan 01) and docs/reference/agent-parity.md (Plan 04) — must exist before listing
    - CONTRIBUTING.md `## Merge conflicts` section (verify accuracy; add index pointer)
    - .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-PATTERNS.md (index.md reconcile section + D-11)
  </read_first>
  <files>docs/reference/index.md, CONTRIBUTING.md</files>
  <action>
    1. Reconcile `docs/reference/index.md` so EVERY `docs/reference/*.md` file (except index.md itself) has exactly one catalog line in the `- [file](file) — <one-line> (Phase N)` shape. Add the currently-missing entries: `dataview-queries.md`, `commit-examples.md`, `setup-prerequisites.md`, `agent-parity.md` (Plan 04), and `obsidian-starter.md` (Plan 01). For the now-authored stubs (schema-tour, privacy-model, examples), drop the "(Phase 12) populated-later" framing and give an accurate one-line description. Keep existing accurate entries unchanged where possible.
    2. Add an index pointer to the CONTRIBUTING.md merge-conflict recipes (D-11: NO new merge-conflicts.md). The existing Contributing section of index.md already points at `../../CONTRIBUTING.md` for "merge-conflict recipes" — verify that pointer is present and accurate; strengthen the wording if needed so a reader knows index.md/log.md conflict recipes live there.
    3. Verify CONTRIBUTING.md's `## Merge conflicts` recipes for `wiki/log.md` (append-only / `merge=union` opt-in) and `wiki/index.md` are still accurate against current tooling. Edit ONLY if a recipe is wrong; otherwise leave unchanged and record "verified accurate" in the SUMMARY.
    Neutrality (D-12): placeholders / sanctioned kahneman only.
  </action>
  <acceptance_criteria>
    - Index coverage: every reference file is listed — `for f in docs/reference/*.md; do b=$(basename "$f"); [ "$b" = index.md ] && continue; grep -q "$b" docs/reference/index.md || { echo "MISSING: $b"; exit 1; }; done` exits 0 with no MISSING output
    - The five additions are present: `grep -q 'dataview-queries.md' docs/reference/index.md && grep -q 'commit-examples.md' docs/reference/index.md && grep -q 'setup-prerequisites.md' docs/reference/index.md && grep -q 'agent-parity.md' docs/reference/index.md && grep -q 'obsidian-starter.md' docs/reference/index.md`
    - No stub status lines remain anywhere under docs/reference/: `! grep -rl 'Status: stub' docs/reference/`
    - index.md points at CONTRIBUTING.md for merge-conflict recipes: `grep -q 'CONTRIBUTING.md' docs/reference/index.md`
    - `bash bin/lint.sh --category yaml,provenance,orphan,crossref` clean
    - `bash bin/check-neutrality.sh` exits 0
  </acceptance_criteria>
  <verify>
    <automated>for f in docs/reference/*.md; do b=$(basename "$f"); [ "$b" = index.md ] && continue; grep -q "$b" docs/reference/index.md || { echo "MISSING: $b"; exit 1; }; done; ! grep -rl 'Status: stub' docs/reference/ && grep -q 'agent-parity.md' docs/reference/index.md && grep -q 'obsidian-starter.md' docs/reference/index.md && grep -q 'CONTRIBUTING.md' docs/reference/index.md && bash bin/check-neutrality.sh</automated>
  </verify>
  <done>index.md lists every reference file (5 additions present), points at CONTRIBUTING.md for merge-conflict recipes, no stub lines remain; CONTRIBUTING merge-conflict recipes verified accurate; lint + neutrality clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → `docs/`, `CONTRIBUTING.md` (template-public) | Edits are published in the neutral template; private wiki terms must not leak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.1-11 | Information Disclosure | accuracy-pass edits + index.md | mitigate | D-12: placeholders + sanctioned `examples/kahneman/` references only. `bash bin/check-neutrality.sh` exits 0. [HIGH — block on neutrality failure.] |
| T-13.1-12 | Tampering | docs drifting from CLAUDE.md while "fixing" them | mitigate | Accuracy-pass policy: edit ONLY on verified drift vs the §11.3/§11.5/§16 source of truth; `bash bin/sync-claude.sh --check` exits 0 to confirm no incidental AGENTS/CLAUDE drift. |
</threat_model>

<verification>
- `bash bin/lint.sh` (full) clean
- `bash bin/check-neutrality.sh` exits 0
- `bash bin/sync-claude.sh --check` exits 0
- Index coverage: every docs/reference/*.md is listed; no Status: stub lines remain
</verification>

<success_criteria>
- Five substantial docs accuracy-passed (edit-on-drift only); brownfield.md no longer describes suggest/verify as unshipped (SC4)
- index.md lists every reference file incl. the 5 additions (SC4)
- CONTRIBUTING.md merge-conflict recipes verified; index pointer present (D-11)
- Neutrality + lint + sync-claude clean
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-05-SUMMARY.md`
</output>
