---
phase: 13.1-docs-finalization-obsidian-starter
plan: 05
type: execute
wave: 3
depends_on: [01, 04]
files_modified:
  - docs/reference/brownfield.md
  - docs/reference/ci.md
  - docs/reference/three-layer-model.md
  - docs/reference/dataview-queries.md
  - docs/reference/release.md
  - docs/reference/index.md
  - docs/quickstart.md
  - .obsidianignore
  - CONTRIBUTING.md
autonomous: true
requirements: [DEBT-02, DEBT-01]
user_setup: []

must_haves:
  truths:
    - "The five substantial reference docs are accuracy-passed — edited ONLY where they drifted from the shipped v1.1 surface, not churned"
    - "brownfield.md no longer describes suggest/verify as unshipped Phase 11 stubs; ALL ~15 stale 'Phase 11' refs are audited; the wrong script name 04-privacy-classification.sh is corrected to 04-privacy-review.sh; every 0[1-4]-*.sh reference names a real migration script; and the subcommand count is FIVE (scan/bootstrap/suggest/review-typing/verify) per §11.5 — NOT four"
    - "docs/quickstart.md line ~35 false '.obsidianignore hides examples/ from Obsidian' claim is corrected (it is the same falsehood as examples.md; D-10 in-scope)"
    - ".obsidianignore's self-falsifying header (claims it hides examples/ from the graph + indexing) is rewritten so the shipped file does not contradict the corrected examples.md/quickstart.md"
    - "docs/reference/index.md lists EVERY reference file, including the previously-omitted dataview-queries.md, commit-examples.md, setup-prerequisites.md, agent-parity.md, and the new obsidian-starter.md (anchored to the link form, no substring false-pass)"
    - "CONTRIBUTING.md merge-conflict recipes for index.md and log.md are verified accurate (verified recipe lines quoted in SUMMARY, not rubber-stamped); an index pointer to them exists (no new merge-conflicts.md)"
    - "No reference file is missing from the index; no stub status line remains in any Plan-05-OWNED file (the Plan-04-owned stubs are out of this plan's scope)"
  artifacts:
    - path: "docs/reference/index.md"
      provides: "Reconciled catalog listing every reference file"
      contains: "agent-parity.md"
  key_links:
    - from: "docs/reference/index.md"
      to: "every docs/reference/*.md"
      via: "one catalog line per file"
      pattern: "obsidian-starter.md"
    - from: "docs/reference/index.md"
      to: "CONTRIBUTING.md merge-conflict recipes"
      via: "index pointer"
      pattern: "CONTRIBUTING.md"
---

<objective>
Accuracy-pass the five substantial reference docs (edit ONLY on drift), verify CONTRIBUTING.md's merge-conflict recipes, and reconcile `docs/reference/index.md` so it lists every reference file (including the new `obsidian-starter.md` from Plan 01 and `agent-parity.md` from Plan 04, plus the previously-omitted `dataview-queries.md`, `commit-examples.md`, `setup-prerequisites.md`).

Purpose: Complete SC4 — every `/docs/reference/` file is filled out and discoverable, with no drift from the shipped v1.1 surface and no orphaned (unlisted) reference files.
Output: edited brownfield/ci/three-layer-model/dataview-queries/release docs (on-drift only), a reconciled index.md, and a verified CONTRIBUTING.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-CONTEXT.md
@.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-PATTERNS.md
@.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-RESEARCH.md

<interfaces>
<!-- Accuracy-pass policy (D-10): edit ONLY where drifted from the shipped v1.1 surface.       -->
<!-- Do NOT churn already-good content. CLAUDE.md is the source of truth; the doc is the bug.   -->

Known drift to fix in brownfield.md (VERIFIED 2026-06-01 — REVIEWS HIGH "single-phrase-gated, ~14 stale Phase 11 refs incl. a wrong script name"):
  - Line 3: describes suggest/verify as "two stubs ... that ship in Phase 11". BOTH SHIPPED. Rewrite to
    reflect all FIVE subcommands shipped — scan / bootstrap / suggest / review-typing / verify — per §11.5
    (which states "five subcommands"). Do NOT write "four" (REVIEWS HIGH: §11.5 + the doc's own
    `## review-typing` section define FIVE; writing "four" introduces NEW drift and trips T-13.1-12).
  - Line 140: names `04-privacy-classification.sh` — WRONG. The shipped script AND §11.5 call it
    `04-privacy-review.sh` (line 308 already uses the correct name → the doc is internally inconsistent).
    Fix line 140 to `04-privacy-review.sh`.
  - Audit ALL `Phase 11` references (`grep -n 'Phase 11' docs/reference/brownfield.md` → ~15). Lines that
    imply suggest/verify/migrations are UNSHIPPED future work must be reworded to past/shipped tense.
    Lines that legitimately attribute historical authorship ("Brownfield Workflow populated in Phase 11",
    "Phase 10 + Phase 11 requirements") may remain as accurate history — judgment per line, but every one
    must be reviewed, not just line 3.
  - Every `0[1-4]-*.sh` reference must name a REAL migration script: the four real files are
    01-page-typing.sh, 02-provenance-bootstrap.sh, 03-cross-link-inference.sh, 04-privacy-review.sh
    (`ls schema/brownfield/migrations/*.sh`). No reference may name a nonexistent script.
Verify (edit only if drifted):
  ci.md          — severity-remap table + --staged (Phase 12.2) contract vs CLAUDE.md §11.3.
  release.md     — allowlist matches bin/release.sh.
  dataview-queries.md — the 5 canonical queries match CLAUDE.md §16 App. A.
  three-layer-model.md — decision-record link resolves; likely stable.

ALSO IN SCOPE (drifted template-public files no other plan corrects — REVIEWS HIGH; D-10 makes them in-scope):
  docs/quickstart.md (line ~35): repeats the SAME false claim as examples.md — "The examples/kahneman/
    cluster (hidden from Obsidian by default via .obsidianignore)". FALSE (app.json == {}, examples/ IS
    indexed). Correct it to match examples.md's corrected wording: .obsidianignore is a release-manifest +
    graph-hygiene CONVENTION, NOT an Obsidian-enforced exclusion; the real mechanism is Settings → Files &
    Links → Excluded files. Keep path-only kahneman references (the line already uses a path — safe).
  .obsidianignore (header lines 2-3): self-falsifying — asserts it "hides examples/ from the Obsidian graph
    view + content indexing". It does NOT (Obsidian does not read .obsidianignore). Rewrite the header so the
    shipped file does not contradict the now-corrected examples.md/quickstart.md: describe it accurately as a
    release-manifest + graph-hygiene convention consumed by bin/release.sh + docs (NOT an Obsidian feature),
    and that the real Obsidian exclusion is Settings → Files & Links → Excluded files. Do NOT delete the file
    or remove it from the bin/release.sh allowlist — just make its header truthful. The `examples/` body line
    stays (it is the release-manifest convention payload).

index.md current entries (7): schema-tour, brownfield, privacy-model, three-layer-model, ci,
examples, release. MISSING (must add): dataview-queries.md, commit-examples.md,
setup-prerequisites.md, agent-parity.md (Plan 04), obsidian-starter.md (Plan 01).
Catalog line shape: `- [file](file) — <one-line> (Phase N)`. Drop "(Phase 12) populated-later"
framing from the now-authored stub descriptions. ANCHOR coverage checks to the LINK FORM
`](file)` — a bare `grep -q examples.md` false-passes on `commit-examples.md` (REVIEWS MEDIUM substring trap).

CONTRIBUTING.md merge-conflict section: `## Merge conflicts` (~line 58) covers wiki/log.md
(append-only / merge=union opt-in) + wiki/index.md. Verify the recipes are still accurate (QUOTE the
verified recipe lines in the SUMMARY — do not rubber-stamp); add an index pointer (D-11: NO new merge-conflicts.md).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Accuracy-pass the five substantial reference docs (edit on drift only)</name>
  <read_first>
    - docs/reference/brownfield.md, ci.md, three-layer-model.md, dataview-queries.md, release.md (the five accuracy-pass targets — each is its own analog)
    - CLAUDE.md §11.5 (brownfield subcommands), §11.3 (CI mode / --staged), §16 Appendix A (canonical Dataview queries)
    - bin/release.sh (release allowlist source of truth)
    - .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-PATTERNS.md (the accuracy-pass section)
  </read_first>
  <files>docs/reference/brownfield.md, docs/reference/ci.md, docs/reference/three-layer-model.md, docs/reference/dataview-queries.md, docs/reference/release.md, docs/quickstart.md, .obsidianignore</files>
  <action>
    Apply a correctness pass. Policy (D-10): edit ONLY where drifted from the shipped v1.1 surface; do NOT rewrite already-good content.
    - brownfield.md (KNOWN DRIFT — broaden beyond line 3, REVIEWS HIGH):
        (i) Line 3: rewrite "two stubs (suggest, verify) that ship in Phase 11" to reflect that ALL FIVE subcommands shipped — scan / bootstrap / suggest / review-typing / verify — per §11.5 ("five subcommands"). Use the word FIVE (or enumerate all five), NEVER "four".
        (ii) Line 140: change `04-privacy-classification.sh` → `04-privacy-review.sh` (the real shipped name; line 308 already uses it).
        (iii) Audit every `Phase 11` reference (~15). Reword any that imply suggest/verify/migrations are unshipped future work to shipped/past tense; leave accurate historical-authorship references intact.
        (iv) Confirm every `0[1-4]-*.sh` reference names a real migration file (01-page-typing, 02-provenance-bootstrap, 03-cross-link-inference, 04-privacy-review).
    - ci.md: verify the severity-remap table and the `--staged` (Phase 12.2) contract match CLAUDE.md §11.3. Fix only on drift.
    - release.md: verify the documented allowlist matches `bin/release.sh`. Fix only on drift.
    - dataview-queries.md: verify the 5 canonical queries match CLAUDE.md §16 Appendix A. Fix only on drift.
    - three-layer-model.md: confirm the decision-record link resolves; fix only if broken.
    - docs/quickstart.md (line ~35, DRIFTED — REVIEWS HIGH): correct the false ".obsidianignore hides examples/ from Obsidian" claim to match examples.md's corrected wording (release-manifest + graph-hygiene CONVENTION, not an Obsidian feature; real exclusion = Settings → Files & Links → Excluded files). Keep the existing path-form kahneman reference.
    - .obsidianignore (header lines 2-3, SELF-FALSIFYING — REVIEWS HIGH): rewrite the header so it no longer claims to "hide examples/ from the Obsidian graph view + content indexing". Make it truthful: a release-manifest + graph-hygiene convention consumed by bin/release.sh + docs; Obsidian does NOT read this file; the real Obsidian exclusion is Settings → Files & Links → Excluded files. Keep the file, keep the `examples/` body line, keep it in the bin/release.sh allowlist — only the header prose changes.
    For each doc with NO drift, make no edit and record "no drift" in the SUMMARY.
    Neutrality (D-12): any edits use placeholders / sanctioned `examples/kahneman/...` PATH references only — never a bare `kahneman` token, never spaced topical denylist phrases.
  </action>
  <acceptance_criteria>
    - brownfield.md drift fixed: `! grep -qi 'stubs.*ship in Phase 11\|suggest.*verify.*stub' docs/reference/brownfield.md`
    - brownfield.md names FIVE subcommands, not four: `! grep -qi 'four subcommands' docs/reference/brownfield.md` AND `grep -qi 'review-typing' docs/reference/brownfield.md`
    - brownfield.md wrong script name corrected: `! grep -q '04-privacy-classification' docs/reference/brownfield.md` AND `grep -q '04-privacy-review' docs/reference/brownfield.md`
    - every `0[1-4]-*.sh` in brownfield.md is a real script: `for s in $(grep -oE '0[1-4]-[a-z-]+\.sh' docs/reference/brownfield.md | sort -u); do test -f "schema/brownfield/migrations/$s" || { echo "PHANTOM SCRIPT: $s"; exit 1; }; done`
    - quickstart.md false claim corrected: `! grep -qi 'hidden from Obsidian by default via' docs/quickstart.md` (the false phrasing is gone)
    - .obsidianignore header truthful: `! grep -qi 'hides examples/ from the Obsidian graph' .obsidianignore` AND `grep -qi 'Excluded files\|release-manifest\|does not read' .obsidianignore`
    - .obsidianignore still in the release allowlist (not dropped): `grep -q 'obsidianignore' bin/release.sh`
    - ci.md still consistent with §11.3: `grep -q -- '--staged' docs/reference/ci.md`
    - dataview-queries.md still has 5 dataview blocks: `grep -c '```dataview' docs/reference/dataview-queries.md` >= 5
    - `bash bin/check-neutrality.sh` exits 0 (REAL gate: docs/ + .obsidianignore is at repo root but NOT in PUBLIC_PATHS — so for .obsidianignore run a targeted denylist grep: `! grep -iE 'kahneman' .obsidianignore` unless inside an examples/kahneman path)
    - `bash bin/sync-claude.sh --check` exits 0 (no incidental AGENTS/CLAUDE drift introduced)
  </acceptance_criteria>
  <verify>
    <automated>! grep -qi 'stubs.*ship in Phase 11' docs/reference/brownfield.md && ! grep -qi 'four subcommands' docs/reference/brownfield.md && grep -qi 'review-typing' docs/reference/brownfield.md && ! grep -q '04-privacy-classification' docs/reference/brownfield.md && for s in $(grep -oE '0[1-4]-[a-z-]+\.sh' docs/reference/brownfield.md | sort -u); do test -f "schema/brownfield/migrations/$s" || exit 1; done; ! grep -qi 'hidden from Obsidian by default via' docs/quickstart.md && ! grep -qi 'hides examples/ from the Obsidian graph' .obsidianignore && [ "$(grep -c '```dataview' docs/reference/dataview-queries.md)" -ge 5 ] && bash bin/check-neutrality.sh && bash bin/sync-claude.sh --check</automated>
  </verify>
  <done>The five reference docs accuracy-passed; brownfield.md names FIVE subcommands (incl. review-typing), no longer calls suggest/verify unshipped, wrong script name fixed, every 0[1-4]-*.sh ref is a real file, all ~15 Phase-11 refs audited; quickstart.md + .obsidianignore false .obsidianignore claims corrected (file kept in release allowlist); lint/neutrality/sync-claude clean.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile docs/reference/index.md + verify CONTRIBUTING.md merge-conflict recipes</name>
  <read_first>
    - docs/reference/index.md (current 7-entry catalog to reconcile)
    - The full file list: `ls docs/reference/*.md` (must end with every file listed)
    - docs/reference/obsidian-starter.md (Plan 01) and docs/reference/agent-parity.md (Plan 04) — must exist before listing
    - CONTRIBUTING.md `## Merge conflicts` section (verify accuracy; add index pointer)
    - .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-PATTERNS.md (index.md reconcile section + D-11)
  </read_first>
  <files>docs/reference/index.md, CONTRIBUTING.md</files>
  <action>
    1. Reconcile `docs/reference/index.md` so EVERY `docs/reference/*.md` file (except index.md itself) has exactly one catalog line in the `- [file](file) — <one-line> (Phase N)` shape. Add the currently-missing entries: `dataview-queries.md`, `commit-examples.md`, `setup-prerequisites.md`, `agent-parity.md` (Plan 04), and `obsidian-starter.md` (Plan 01). For the now-authored stubs (schema-tour, privacy-model, examples), drop the "(Phase 12) populated-later" framing and give an accurate one-line description. Keep existing accurate entries unchanged where possible.
    2. Add an index pointer to the CONTRIBUTING.md merge-conflict recipes (D-11: NO new merge-conflicts.md). The existing Contributing section of index.md already points at `../../CONTRIBUTING.md` for "merge-conflict recipes" — verify that pointer is present and accurate; strengthen the wording if needed so a reader knows index.md/log.md conflict recipes live there.
    3. Verify CONTRIBUTING.md's `## Merge conflicts` recipes for `wiki/log.md` (append-only / `merge=union` opt-in) and `wiki/index.md` against current tooling. Edit ONLY if a recipe is wrong; otherwise leave unchanged. DO NOT rubber-stamp (REVIEWS MEDIUM): the SUMMARY MUST QUOTE the specific recipe lines you verified (e.g. the `merge=union` .gitattributes line, the index.md conflict-resolution steps) so the verification is auditable, not a bare "verified accurate".
    Neutrality (D-12): placeholders / sanctioned `examples/kahneman/...` PATH references only — no bare `kahneman` token, no spaced topical phrases.
  </action>
  <acceptance_criteria>
    - Index coverage anchored to the LINK FORM (REVIEWS MEDIUM substring false-pass: bare `grep examples.md` matches `commit-examples.md`): every reference file is listed as a markdown link — `for f in docs/reference/*.md; do b=$(basename "$f"); [ "$b" = index.md ] && continue; grep -qE '\]\('"$b"'\)' docs/reference/index.md || { echo "MISSING: $b"; exit 1; }; done` exits 0 with no MISSING output
    - The five additions are present as links: `for b in dataview-queries.md commit-examples.md setup-prerequisites.md agent-parity.md obsidian-starter.md; do grep -qE '\]\('"$b"'\)' docs/reference/index.md || { echo "MISSING LINK: $b"; exit 1; }; done`
    - No stub status lines remain in PLAN-05-OWNED files (REVIEWS MEDIUM: the 3 remaining stubs are PLAN-04-owned — Plan 05 cannot edit them, so scope the check to this plan's files; if a Plan-04 stub still has a status line, that is Plan 04's failure, surfaced as an explicit precondition message, NOT a Plan 05 gate failure): `! grep -l 'Status: stub' docs/reference/brownfield.md docs/reference/ci.md docs/reference/three-layer-model.md docs/reference/dataview-queries.md docs/reference/release.md docs/reference/index.md`. ADDITIONALLY, as a non-owned PRECONDITION cross-check (abort-with-message, not a silent fail): if `grep -rl 'Status: stub' docs/reference/` finds any file, print which file and that it is Plan-04-owned (schema-tour/privacy-model/examples) and must be completed by Plan 04 first.
    - index.md points at CONTRIBUTING.md for merge-conflict recipes (anchored): `grep -qE 'CONTRIBUTING\.md' docs/reference/index.md`
    - SUMMARY quotes the verified CONTRIBUTING.md recipe lines (not a rubber-stamp)
    - `bash bin/check-neutrality.sh` exits 0 (docs/ IS scanned — real gate)
  </acceptance_criteria>
  <verify>
    <automated>for f in docs/reference/*.md; do b=$(basename "$f"); [ "$b" = index.md ] && continue; grep -qE '\]\('"$b"'\)' docs/reference/index.md || { echo "MISSING: $b"; exit 1; }; done; ! grep -l 'Status: stub' docs/reference/brownfield.md docs/reference/ci.md docs/reference/three-layer-model.md docs/reference/dataview-queries.md docs/reference/release.md docs/reference/index.md && grep -qE '\]\(agent-parity.md\)' docs/reference/index.md && grep -qE '\]\(obsidian-starter.md\)' docs/reference/index.md && grep -qE 'CONTRIBUTING\.md' docs/reference/index.md && bash bin/check-neutrality.sh</automated>
  </verify>
  <done>index.md lists every reference file (5 additions present, ANCHORED to the link form — no substring false-pass), points at CONTRIBUTING.md for merge-conflict recipes; no stub lines in Plan-05-owned files (Plan-04 stubs cross-checked as an abort-with-message precondition); CONTRIBUTING recipes verified with quoted lines in SUMMARY; neutrality clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → `docs/`, `CONTRIBUTING.md` (template-public) | Edits are published in the neutral template; private wiki terms must not leak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.1-11 | Information Disclosure | accuracy-pass edits + index.md + quickstart.md + .obsidianignore | mitigate | D-12: PATH-only `examples/kahneman/...` references — NEVER a bare `kahneman` token (path-scrub does not catch it) and NEVER spaced topical denylist phrases. `bash bin/check-neutrality.sh` exits 0 for docs/ (real gate); `.obsidianignore` is at repo root and NOT in PUBLIC_PATHS, so it gets a targeted denylist grep. [HIGH — block on neutrality failure.] |
| T-13.1-12 | Tampering | docs drifting from §11.5/§11.3/§16 while "fixing" them (incl. the "four vs five subcommands" trap) | mitigate | Accuracy-pass policy: edit ONLY on verified drift vs the §11.3/§11.5/§16 source of truth. SPECIFIC GUARD against this plan's own trap: brownfield.md MUST say FIVE subcommands (scan/bootstrap/suggest/review-typing/verify), never "four" — writing "four" would itself be new drift contradicting §11.5. `bash bin/sync-claude.sh --check` exits 0 to confirm no incidental AGENTS/CLAUDE drift. |
</threat_model>

<verification>
- `bash bin/lint.sh` (full) clean — NOTE: lint walks `wiki/` only; it is a NO-OP for the docs/reference/ + .obsidianignore + CONTRIBUTING.md deliverables. Run it only to confirm the LIVE WIKI did not regress (e.g. via the Plan-03 write-back landing in the same wave window) — NOT as proof-of-correctness for this plan's docs. Doc proof = link-resolution / anchored-coverage / brownfield-script-real-file / false-claim-removed greps.
- `bash bin/check-neutrality.sh` exits 0 (real gate for docs/; for `.obsidianignore` at repo root, use the targeted denylist grep in Task 1)
- `bash bin/sync-claude.sh --check` exits 0
- Index coverage ANCHORED to the link form (no `examples.md` ⊂ `commit-examples.md` substring false-pass); no Status: stub lines remain in Plan-05-OWNED files (Plan-04 stubs are a cross-checked precondition, not a Plan-05 gate)
- brownfield.md: FIVE subcommands (incl. review-typing), no unshipped-stub phrasing, 04-privacy-review (not -classification), every 0[1-4]-*.sh ref a real file, all Phase-11 refs audited
- quickstart.md + .obsidianignore false ".obsidianignore hides examples/" claims corrected; .obsidianignore kept in the release allowlist
</verification>

<success_criteria>
- Five substantial docs accuracy-passed (edit-on-drift only); brownfield.md names FIVE subcommands (incl. review-typing), no longer describes suggest/verify as unshipped, wrong script name fixed, all Phase-11 refs audited (SC4)
- quickstart.md + .obsidianignore self-contradicting ".obsidianignore hides examples/" claims corrected so the release ships a consistent story (no doc says it hides while the file claims to)
- index.md lists every reference file incl. the 5 additions, anchored to the link form (SC4)
- CONTRIBUTING.md merge-conflict recipes verified with quoted lines in SUMMARY; index pointer present (D-11)
- Neutrality + sync-claude clean (lint is a no-op proof for these docs — used only to confirm the live wiki did not regress)
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-05-SUMMARY.md`
</output>
