# Phase 13.1: Docs Finalization + Obsidian Starter - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-01
**Phase:** 13.1-Docs Finalization + Obsidian Starter
**Areas discussed:** Obsidian starter shape, Dataview verification fixtures, Manual-verification model, Docs depth & new files

---

## Obsidian starter shape

### Q1 — Template format (presented twice; reframed with concrete previews on request)

| Option | Description | Selected |
|--------|-------------|----------|
| Templates-core tokens | Plain markdown + Obsidian built-in Templates core plugin tokens ({{date}}, {{title}}); zero community-plugin dependency; auto-fills created_at/title | ✓ |
| Plain markdown only | Byte-near copies of schema/templates/ with literal placeholders; nothing auto-fills | |
| Templater plugin | Community Templater plugin (<% %>) for richer auto-fill; adds a third-party dependency | |

**User's choice:** Templates-core tokens (confirmed on reframe).
**Notes:** User asked me to reframe Q1 with concrete side-by-side previews of each format's actual frontmatter; stuck with Templates-core after seeing them.

### Q2 — Vault config pre-wiring (elaborated on request before answering)

| Option | Description | Selected |
|--------|-------------|----------|
| Files + doc only | Ship schema/obsidian/ templates + reference doc with one-time setup step; never touch .obsidian/ | ✓ |
| Pre-wire template folder | Also edit committed .obsidian/ to enable Templates + set folder | |
| You decide | Planner picks | |

**User's choice:** Files + doc only.
**Notes:** User asked me to elaborate the tradeoff first; chose Files + doc only after the explanation (works identically for fresh-starter and brownfield users; avoids shipping someone else's vault config as canonical).

---

## Dataview verification fixtures

### Q1 — What backs the deterministic render check

| Option | Description | Selected |
|--------|-------------|----------|
| Build dataview-fixtures/ | Purpose-built examples/dataview-fixtures/ with example: true + documented expected counts | ✓ |
| Reuse examples/kahneman/ | Compute counts against the existing cluster; counts drift | |
| Hybrid | Dedicated fixtures for counts + kahneman for feel | |

**User's choice:** Build dataview-fixtures/.

### Q2 — Covering SC1's two vault states

| Option | Description | Selected |
|--------|-------------|----------|
| One fixture, both covered | Include bootstrap_stage-bearing pages so one open exercises both states | ✓ |
| Two snapshots, reuse brownfield | Dedicated fixture + point post-bootstrap at tests/phase-10|11 | |
| Two purpose-built snapshots | Separate fresh-starter + post-bootstrap snapshots | |

**User's choice:** One fixture, both covered.
**Notes:** Claude flagged a known tension (not re-asked): `.obsidianignore` excludes examples/ from indexing, so Dataview FROM "examples/dataview-fixtures" returns zero — researcher to reconcile.

---

## Manual-verification model

### Q1 — Execution split

| Option | Description | Selected |
|--------|-------------|----------|
| I do non-GUI, you do GUI/Codex | Claude does write-back + Claude-side ingest; user does Obsidian + Codex | |
| Full runbook, you execute all | Claude writes runbook, user runs everything | |
| I produce golden refs only | Claude generates goldens, user diffs | |
| [user override] I do Obsidian GUI, you invoke Codex CLI | Claude runs both ingest sides incl. `codex exec`; user only opens Obsidian | ✓ |

**User's choice:** Override — "same as 1 but I do obsidian gui you do codex - you can invoke codex cli."
**Notes:** Claude confirmed Codex CLI 0.135.0 is installed (`codex exec`). Claude now owns DEBT-02 (both sides) + DEBT-04; user owns only DEBT-01.

### Q2 — Agent-parity tolerance

| Option | Description | Selected |
|--------|-------------|----------|
| Structural equivalence | Same pages/types/provenance source IDs + locator targets + valid frontmatter; prose free | ✓ |
| Structural + provenance resolves | Above + every [prov:] resolves to a real passage | |
| Strict near-identical | Same pages + semantically-matching claims | |

**User's choice:** Structural equivalence.

### Q3 — Golden reference

| Option | Description | Selected |
|--------|-------------|----------|
| Existing kahneman = golden | Both agents diff against committed examples/kahneman/ + each other | ✓ |
| Captured Claude run = golden | Freeze one Claude ingest as golden | |
| Agent-to-agent only | No fixed golden | |

**User's choice:** Existing kahneman = golden.

### Q4 — DEBT-01 gating

| Option | Description | Selected |
|--------|-------------|----------|
| Prepared, await your sign-off | Ship fixtures + checklist + capture slot; phase not blocked on GUI; 13.2 confirms | ✓ |
| Hard block on render | Phase stays open until user does the render | |
| You decide | Planner picks | |

**User's choice:** Prepared, await your sign-off.

---

## Docs depth & new files

### Q1 — Doc work depth/scope

| Option | Description | Selected |
|--------|-------------|----------|
| Stubs full + accuracy-pass rest | Author 3 stubs + agent-parity.md; correctness-pass substantial docs; reconcile index | ✓ |
| Stubs only | Fill stubs + agent-parity.md; trust existing docs | |
| Full rewrite all | Rewrite every reference doc | |

**User's choice:** Stubs full + accuracy-pass rest.

### Q2 — Standalone vs folded for new docs

| Option | Description | Selected |
|--------|-------------|----------|
| agent-parity standalone, rest folded | agent-parity.md new; merge-conflict stays in CONTRIBUTING.md; dataview-fixtures fold into examples.md | ✓ |
| Each its own doc | agent-parity.md + merge-conflicts.md + expand dataview-queries.md | |
| You decide | Planner picks | |

**User's choice:** agent-parity standalone, rest folded.

---

## Claude's Discretion

- Exact prose/structure/section ordering of each authored reference doc (follow existing house style).
- Which single Kahneman raw source to use for DEBT-02; scratch-wiki location/teardown.
- Precise composition of examples/dataview-fixtures/ (page count, type/domain/status spread).
- 13.1-VERIFICATION.md evidence layout (Phase 12.1/12.2/13 mirror).
- Whether DEBT-04 and DEBT-02 share one scratch wiki or use separate ones.

## Deferred Ideas

- Pre-wired .obsidian/ Templates config (rejected for 13.1; revisit for true zero-setup onboarding).
- Obsidian dashboards / GTD views / hotkeys / CSS / prescribed workflows (OBSID-03 → backlog 999.6).
- Standalone merge-conflicts.md reference doc (folded into CONTRIBUTING.md for now).
- Auto page-break / PDF→md marker helper (already deferred to v1.2 from Phase 13).
