---
phase: 13.1-docs-finalization-obsidian-starter
plan: 01
subsystem: docs
tags: [obsidian, templates, neutrality, schema, frontmatter]

# Dependency graph
requires:
  - phase: schema/templates (pre-existing)
    provides: six canonical page-type templates the Obsidian set derives from
provides:
  - "schema/obsidian/ — six Templates-core token-ized page templates (entity, concept, source-summary, comparison, overview, decision), zero drift vs schema/templates/"
  - "docs/reference/obsidian-starter.md — OBSID-02 day-1 page-creation reference (Setup-once, auto-fill-vs-typed, OBSID-03 scope)"
  - "neutrality gate green at baseline (lint-report.md exempted) — every downstream plan's neutrality criterion is now meaningful"
affects: [13.1-04 (index.md registration of obsidian-starter happens in Plan 05), 13.1-05, 13.2-closure]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Obsidian Templates-core tokens ({{title}}, {{date:YYYY-MM-DD}}) for auto-fill-on-insert"
    - "Full-file reverse-substitution drift-guard: token-ize back to bare keys and diff byte-for-byte against canonical sibling"

key-files:
  created:
    - schema/obsidian/entity.md
    - schema/obsidian/concept.md
    - schema/obsidian/overview.md
    - schema/obsidian/comparison.md
    - schema/obsidian/source-summary.md
    - schema/obsidian/decision.md
    - docs/reference/obsidian-starter.md
  modified:
    - wiki/maintenance/lint-report.md

key-decisions:
  - "Used neutrality_exempt: true (the scanner's documented mechanism) to clear the pre-existing exit-2 on lint-report.md rather than deleting its real drift findings"
  - "Derived each Obsidian template from its OWN canonical sibling (verbatim copy + 3 token subs), never a shared block — preserves the decision.md asymmetry by construction"

patterns-established:
  - "Drift-guard = full-file reverse-substitution diff (catches FORBIDDEN-PATTERNS block + guidance comments, not just fields/headers)"
  - "schema/ neutrality proven via verbatim-copy invariant + targeted denylist grep (the repo gate does NOT scan schema/)"

requirements-completed: [OBSID-01, OBSID-02, OBSID-03]

# Metrics
duration: ~15min
completed: 2026-06-01
---

# Phase 13.1 Plan 01: Obsidian Starter (templates + reference doc) Summary

**Six token-ized Obsidian page templates under `schema/obsidian/` (byte-identical to `schema/templates/` modulo `{{title}}`/`{{date:YYYY-MM-DD}}` tokens) plus the OBSID-02 day-1 page-creation reference doc, with the neutrality gate cleared to green at baseline.**

## Performance

- **Duration:** ~15 min
- **Completed:** 2026-06-01
- **Tasks:** 3
- **Files modified:** 8 (7 created, 1 modified)

## Accomplishments
- Cleared the pre-existing `bin/check-neutrality.sh` exit-2 by exempting the generated `wiki/maintenance/lint-report.md` (real drift findings preserved) — every downstream plan's "neutrality exits 0" gate is now meaningful instead of an unconditional false-negative.
- Shipped six Obsidian Templates-core page templates with zero field/section drift vs their `schema/templates/` siblings; only `title`/`created_at`/`updated_at` carry auto-fill tokens. The `decision.md` asymmetry (non-empty `epistemic_status: sourced` / `privacy: cloud_safe` + `has_contradictions`/`knowledge_domain`/`trigger_type`/`affected_pages`) is preserved and absent from the other five.
- Authored `docs/reference/obsidian-starter.md` (OBSID-02): Setup-once, auto-fill-vs-user-typed table, OBSID-03 scope statement (no dashboards/views/hotkeys/CSS), and the See-also footer — all placeholder-neutral.

## Task Commits

1. **Task 0: make check-neutrality.sh green at baseline** - `2bc2258` (fix)
2. **Task 1: six token-ized schema/obsidian/ templates** - `8dd8940` (feat)
3. **Task 2: docs/reference/obsidian-starter.md (OBSID-02)** - `bc03f95` (docs)

## Files Created/Modified
- `wiki/maintenance/lint-report.md` — added `neutrality_exempt: true` to frontmatter (Task 0); drift findings untouched.
- `schema/obsidian/{entity,concept,overview,comparison,source-summary,decision}.md` — token-ized page templates (Task 1).
- `docs/reference/obsidian-starter.md` — OBSID-02 reference doc (Task 2).

## Decisions Made
- **lint-report.md exemption over deletion:** `lint-report.md` is a generated `type: overview`/`tags: [meta]` control-plane artifact under `wiki/maintenance/` (not in index.md). Its drift section mechanically echoes real source paths — those are findings, not leaks. Exempting it via the scanner's documented `neutrality_exempt: true` is correct and bounded; it does not weaken the gate for authored content.
- **Per-sibling derivation:** Each Obsidian template was copied from its own canonical sibling and token-substituted, never assembled from a shared frontmatter block. This guarantees the decision.md asymmetry survives without special-casing.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- The plan's verification step runs `bin/lint.sh`, which regenerates `lint-report.md`. During my verification run the live wiki had 0 drift findings, so the regenerated report dropped both the drift section and the `neutrality_exempt` line. I restored the committed Task 0 state (`git checkout -- wiki/maintenance/lint-report.md wiki/log.md`) so the deliverable matches Task 0's intent (exemption present, drift findings preserved) rather than committing a transient lint side-effect. Neutrality is green either way; the committed form is the intended one.

## Verification Results
- `bash bin/check-neutrality.sh` → exit 0 (baseline + after each task).
- Full-file reverse-substitution drift-guard → byte-identical for all six templates.
- decision asymmetry: `trigger_type:` present only in decision.md; absent from the other five.
- OBSID-03 negatives: no `*.css`/`*.canvas`/`*.base`/`*dashboard*` files; no `dataviewjs`/`hotkey`/```dataview fences under schema/obsidian/.
- Targeted denylist grep over schema/obsidian/ → no matches.
- `bash bin/sync-claude.sh --check` → exit 0; `bash bin/lint.sh --category yaml,provenance,orphan,crossref` → exit 0.

## Next Phase Readiness
- `schema/obsidian/` exists for Plan 02 (fixtures) and downstream docs work.
- `obsidian-starter.md` is authored; its `index.md` registration is owned by Plan 05 (the single Wave-3 index owner), not this plan.
- Neutrality baseline is green, unblocking the neutrality criterion in every other 13.1 plan.

## Self-Check: PASSED

All 8 deliverable files exist on disk; all 3 task commits (`2bc2258`, `8dd8940`, `bc03f95`) are present in git history.

---
*Phase: 13.1-docs-finalization-obsidian-starter*
*Completed: 2026-06-01*
