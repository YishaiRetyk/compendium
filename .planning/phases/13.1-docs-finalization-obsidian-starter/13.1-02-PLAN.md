---
phase: 13.1-docs-finalization-obsidian-starter
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/dataview-fixtures/
autonomous: true
requirements: [DEBT-01]
user_setup: []

must_haves:
  truths:
    - "examples/dataview-fixtures/ contains a purpose-built page set spanning page types, domains, and statuses"
    - "Every fixture page carries example: true (the §5/D-04 schema marker) — NOTE the operative lint-skip for this PATH is the path-prefix EXCLUDE_DIRS={'maintenance','examples'} in bin/lint.sh, NOT example:true (lint never walks examples/ at all); example:true is the correct schema marker for these pages regardless"
    - "domains and tags are always YAML LISTS (never scalars) so contains(domains,...) Dataview predicates resolve"
    - "At least two fixture pages carry bootstrap_stage so one Obsidian open exercises both fresh-starter and post-bootstrap rendering"
    - "A machine-consumable per-query count matrix (fenced | query | expected_count | table) is emitted in the SUMMARY for Plan 04 to consume and re-derive"
    - "Each of the five canonical Dataview queries has a deterministic, non-trivial expected row count against the fixtures, derived from a per-page attribute matrix"
    - "The stale-pages and missing-privacy LIST queries each have at least one nonzero hit"
  artifacts:
    - path: "examples/dataview-fixtures/"
      provides: "Dataview render fixtures (example:true schema marker; lint-skipped via examples/ path-prefix)"
      contains: "example: true"
  key_links:
    - from: "examples/dataview-fixtures/*.md"
      to: "bin/lint.sh examples/ path-prefix skip (EXCLUDE_DIRS)"
      via: "path-prefix exclusion (NOT example:true — lint never reaches examples/)"
      pattern: "example:\\s*true"
---

<objective>
Build `examples/dataview-fixtures/` — a small, deterministic, self-contained set of wiki pages (each `example: true`) spanning page types, domains, and statuses, with a couple carrying `bootstrap_stage`, so that one Obsidian open exercises both fresh-starter and post-bootstrap rendering and every canonical Dataview query returns a documented, non-trivial row count.

Purpose: Make DEBT-01's Dataview render verification deterministic — counts can't drift because the fixtures are purpose-built and lint-skipped. The expected-count matrix is documented in `examples.md` (Plan 04, which consumes this fixture set).
Output: `examples/dataview-fixtures/*.md` (~8-12 pages).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-CONTEXT.md
@.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-PATTERNS.md
@.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-RESEARCH.md

<interfaces>
<!-- Fixture page frontmatter shape (derived from examples/kahneman/entities/daniel-kahneman.md + example:true). -->
<!-- All content is placeholder/neutral per D-12. -->

Base fixture frontmatter (per page) — domains AND tags MUST be YAML LISTS, never scalars
(REVIEWS HIGH: `contains(domains,"alpha")` needs a list; a scalar silently fails the Dataview predicate):
  id: <fixture-slug>                                  # MUST start with `fixture-` (positive neutrality marker)
  title: <Fixture Title>
  type: entity|concept|source|comparison|overview   # spread across types
  status: active|stale|archived                       # spread for LIST queries
  summary: "<one-line>"
  created_at: 2026-04-01
  updated_at: 2026-04-01
  sources: []
  epistemic_status: sourced|stale                     # spread some stale
  tags:                                               # ALWAYS a list (even single-element)
    - <tag>
  domains:                                            # ALWAYS a list; spread so per-domain queries return >=2
    - <domain>
  supersedes:
  superseded_by:
  privacy: cloud_safe    # OMIT THE WHOLE LINE on a couple pages to exercise missing-privacy LIST (Dataview WHERE !privacy)
  aliases: []
  has_contradictions: false
  knowledge_domain: <domain>
  example: true          # §5/D-04 schema marker. NOTE: the lint-skip for examples/ is the PATH-PREFIX
                         # EXCLUDE_DIRS={'maintenance','examples'} in bin/lint.sh — lint NEVER walks examples/,
                         # so example:true is the schema-correctness marker, not the operative skip for this path.

bootstrap_stage subset (>=2 pages, per tests/phase-10/fixtures/no-frontmatter/expected/page.md):
  bootstrap_stage: bootstrapped
  bootstrap_date: 2026-04-17

The five canonical Dataview queries (from docs/reference/dataview-queries.md) the fixture-scoped
variants must satisfy (queries point FROM "examples/dataview-fixtures"). NOTE: expected counts are
STATIC GREP-PROXIES derived from a per-page attribute matrix — Obsidian Dataview semantics differ from
grep in two known ways the SUMMARY must flag for the 13.2 live render: (a) `WHERE !privacy` matches BOTH
omitted AND present-but-null `privacy`, whereas `grep -L '^privacy:'` only catches OMISSION; (b)
`contains(domains, X)` requires `domains` to be a YAML LIST. Build counts deterministically from the matrix:
  1. active entities (TABLE)         — WHERE status = "active", type entity
  2. sources by domain (TABLE)       — type source, contains(domains, "<domain>")
  3. stale pages (LIST)              — WHERE epistemic_status = "stale"
  4. missing-privacy (LIST)          — WHERE !privacy  (omit the privacy line entirely on those pages)
  5. pages in a domain (TABLE)       — contains(domains, "<domain>") AND status = "active"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the examples/dataview-fixtures/ page set</name>
  <read_first>
    - examples/kahneman/entities/daniel-kahneman.md (real wiki page frontmatter shape to copy)
    - tests/phase-10/fixtures/no-frontmatter/expected/page.md (bootstrap_stage + bootstrap_date field shape)
    - .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-PATTERNS.md (the examples/dataview-fixtures section + composition guidance)
    - CLAUDE.md §5 (example: true lint-skip + base fields) and §16 Appendix A (canonical Dataview queries)
  </read_first>
  <files>examples/dataview-fixtures/ (8-12 new .md files)</files>
  <action>
    Create `examples/dataview-fixtures/` and author 8-12 fixture pages with the base frontmatter shape above. Design the spread so each canonical query has a non-trivial, deterministic count. A concrete composition that satisfies all five queries (adjust counts but preserve the constraints):
      - 3 pages `type: entity, status: active` (drives query 1: active entities count = 3).
      - 2 pages `type: source` with the SAME `domains: [<domain-a>]` (drives query 2: sources by domain = 2; these are source-summary-shaped but for fixture purposes only need valid frontmatter — include the source tail fields path/url/content_hash/ingested_at/source_type/compilation_status/compiled_against_hash/compiled_targets so they are schema-valid).
      - 2 pages with `epistemic_status: stale` (drives query 3: stale LIST = 2; spread across types).
      - 2 pages that OMIT the `privacy:` field entirely (drives query 4: missing-privacy LIST = 2).
      - >=2 pages sharing `domains: [<domain-a>]` AND `status: active` (drives query 5: pages-in-domain = the active subset).
      - 1 page `status: archived` and 1 `type: comparison` / 1 `type: overview` to round out the type/status spread.
    Make >=2 pages carry `bootstrap_stage: bootstrapped` + `bootstrap_date: 2026-04-17` (D-05) so the post-bootstrap render is exercised.
    EVERY page MUST carry `example: true` (schema marker). `domains` and `tags` MUST be YAML LISTS on every page (block form `- value`, never inline scalar) so `contains(domains, X)` resolves in Dataview. Each page needs a minimal valid body (an H1 + a TL;DR section is enough; fixtures need not carry [prov:] markers since they live under examples/ which lint never walks, but include a short TL;DR so they render in Obsidian).
    NEUTRALITY (D-12) — POSITIVE-MARKER discipline (REVIEWS HIGH: check-neutrality.sh PRUNES examples/, so the gate is BLIND to these files; author discipline is the ONLY real defense and MUST be positively asserted): every fixture `id` MUST start with the literal prefix `fixture-` (e.g. `fixture-entity-1`, `fixture-source-2`); every `domains`/`tags` value MUST be an obviously-synthetic token drawn ONLY from {`alpha`, `beta`, `gamma`} for domains and {`tag-x`, `tag-y`, `tag-z`} for tags. NEVER a real wiki slug/term. These synthetic tokens make a leak structurally impossible AND give the acceptance a positive `^fixture-` / synthetic-token assertion to check (since the gate cannot).
    PER-PAGE ATTRIBUTE MATRIX + COUNT DERIVATION (REVIEWS HIGH: counts must be derived deterministically, not eyeballed): build a table — one row per fixture page, columns {id, type, status, epistemic_status, privacy-present?, domains-list}. Derive each of the five query counts MECHANICALLY from that matrix. Emit BOTH the per-page matrix AND a machine-consumable fenced count matrix into the SUMMARY for Plan 04:
      ```
      | query | expected_count |
      |-------|----------------|
      | active-entities | N1 |
      | sources-by-domain-alpha | N2 |
      | stale-pages | N3 |
      | missing-privacy | N4 |
      | pages-in-domain-alpha-active | N5 |
      ```
  </action>
  <acceptance_criteria>
    - `ls examples/dataview-fixtures/*.md | wc -l` is between 8 and 12
    - Every fixture carries example:true: `grep -L 'example: true' examples/dataview-fixtures/*.md` lists NOTHING (empty)
    - POSITIVE NEUTRALITY: every fixture id starts with `fixture-`: `grep -L '^id: fixture-' examples/dataview-fixtures/*.md` lists NOTHING (empty)
    - TARGETED DENYLIST over the fixtures (the repo gate prunes examples/, so grep here directly): `! grep -rilE 'kahneman|prospect-theory|loss-aversion|personal-decision|salary' examples/dataview-fixtures/` exits 0 (no match)
    - domains/tags are LISTS (block form) on every page: `! grep -E '^(domains|tags):[[:space:]]*\[' examples/dataview-fixtures/*.md` (no inline-array scalars) AND every page has a `^domains:` followed by at least one `^  - ` item
    - Source-shaped fixtures are schema-valid: every `type: source` fixture carries the source tail fields — `for f in $(grep -l '^type: source' examples/dataview-fixtures/*.md); do grep -q '^content_hash:' "$f" && grep -q '^ingested_at:' "$f" && grep -q '^source_type:' "$f" || { echo "INVALID SOURCE FIXTURE: $f"; exit 1; }; done`
    - `grep -lc 'bootstrap_stage: bootstrapped' examples/dataview-fixtures/*.md | wc -l` >= 2 (at least two bootstrap_stage pages)
    - Query 3 nonzero: `grep -l 'epistemic_status: stale' examples/dataview-fixtures/*.md | wc -l` >= 1
    - Query 4 nonzero (missing-privacy = OMITTED privacy line): at least 2 files have NO `^privacy:` line — `grep -L '^privacy:' examples/dataview-fixtures/*.md | wc -l` >= 2
    - Query 1 count: `grep -l '^type: entity' examples/dataview-fixtures/*.md | xargs grep -l '^status: active' | wc -l` >= 2
    - The SUMMARY contains the fenced `| query | expected_count |` matrix (machine-consumable handoff to Plan 04)
    - Lint did not regress on the LIVE wiki (NOT a proof-of-correctness for the fixtures — lint never walks examples/): `bash bin/lint.sh --category yaml,orphan wiki` exits clean. Do NOT claim "zero findings via example:true" — the operative skip is the examples/ path-prefix (EXCLUDE_DIRS), so a fixtures-scoped lint run is vacuous by construction.
    - `bash bin/check-neutrality.sh` exits 0 (REPO-WIDE backstop; reminder: it PRUNES examples/, so it does NOT scan these fixtures — the `^fixture-` + synthetic-token + targeted-denylist assertions above are the real proof)
  </acceptance_criteria>
  <verify>
    <automated>n=$(ls examples/dataview-fixtures/*.md | wc -l); [ "$n" -ge 8 ] && [ "$n" -le 12 ] && [ -z "$(grep -L 'example: true' examples/dataview-fixtures/*.md)" ] && [ -z "$(grep -L '^id: fixture-' examples/dataview-fixtures/*.md)" ] && ! grep -rilE 'kahneman|prospect-theory|loss-aversion|personal-decision|salary' examples/dataview-fixtures/ && ! grep -E '^(domains|tags):[[:space:]]*\[' examples/dataview-fixtures/*.md && [ "$(grep -l 'bootstrap_stage: bootstrapped' examples/dataview-fixtures/*.md | wc -l)" -ge 2 ] && bash bin/lint.sh --category yaml,orphan wiki && bash bin/check-neutrality.sh</automated>
  </verify>
  <done>8-12 neutral fixtures exist (all id=fixture-*, synthetic alpha/beta domains, list-shaped domains/tags), all example:true, >=2 with bootstrap_stage, source fixtures schema-valid, stale + missing-privacy queries each nonzero, targeted denylist clean (gate prunes examples/), repo-wide neutrality backstop exits 0. The machine-consumable count matrix is recorded in SUMMARY for Plan 04 to re-derive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → `examples/` (template-public) | Fixture content is published in the neutral template; must use synthetic neutral placeholders, no private terms. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.1-03 | Information Disclosure | `examples/dataview-fixtures/*.md` | mitigate | D-12: `check-neutrality.sh` PRUNES examples/ (the gate is BLIND here), so the real defense is positive write-time discipline: every id is `fixture-*`, every domain/tag is a synthetic token (alpha/beta/gamma, tag-x/y/z), and a TARGETED denylist grep over the fixtures must find no real wiki terms. The repo gate is only a coarse backstop for the rest of the tree. |
| T-13.1-04 | Tampering | lint health-check false positives | accept | Fixtures are excluded from lint because `bin/lint.sh` NEVER WALKS examples/ — the operative mechanism is the PATH-PREFIX `EXCLUDE_DIRS={'maintenance','examples'}`, NOT the per-page `example: true` marker (proven: examples/kahneman/ pages carry no example:true yet are never linted). `example: true` remains the correct §5/D-04 schema marker on these pages regardless. A fixtures-scoped lint run would be vacuous, so it is not used as a proof. |
</threat_model>

<verification>
- `bash bin/lint.sh --category yaml,orphan wiki` exits clean (LIVE wiki did not regress — NOT a fixtures proof; lint never walks examples/, so the skip is the examples/ path-prefix, not example:true)
- POSITIVE neutrality: all ids `^fixture-`, all domains/tags synthetic tokens, targeted denylist grep over the fixtures is clean (the gate prunes examples/)
- `bash bin/check-neutrality.sh` exits 0 (repo-wide backstop)
- domains/tags are YAML lists; source fixtures carry the source tail; per-query counts derived from a per-page attribute matrix and emitted as a machine-consumable fenced `| query | expected_count |` matrix for Plan 04
</verification>

<success_criteria>
- `examples/dataview-fixtures/` exists with 8-12 example:true pages (ids `fixture-*`, synthetic alpha/beta domains as LISTS) spanning types/domains/statuses (DEBT-01 prep)
- >=2 pages carry bootstrap_stage (D-05)
- Each canonical query has a non-trivial deterministic count derived from a per-page attribute matrix; stale + missing-privacy LIST queries have nonzero hits; machine-consumable count matrix emitted for Plan 04
- Live wiki lint did not regress; neutrality backstop clean; fixtures-scoped neutrality proven by positive markers (gate prunes examples/)
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-02-SUMMARY.md` (MUST record the final per-query expected-count matrix for Plan 04).
</output>
