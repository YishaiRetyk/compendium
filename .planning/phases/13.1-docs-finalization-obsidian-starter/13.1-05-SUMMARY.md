---
phase: 13.1-docs-finalization-obsidian-starter
plan: 05
subsystem: docs
tags: [docs, accuracy-pass, brownfield, obsidian, neutrality]
requires:
  - docs/reference/obsidian-starter.md (Plan 01)
  - docs/reference/agent-parity.md (Plan 04)
provides:
  - reconciled docs/reference/index.md (all 12 reference files cataloged)
  - drift-corrected brownfield.md (five subcommands, real script names)
  - truthful .obsidianignore + quickstart.md (.obsidianignore convention claim)
affects:
  - docs/reference/index.md
  - docs/reference/brownfield.md
  - docs/quickstart.md
  - .obsidianignore
tech-stack:
  added: []
  patterns: [accuracy-pass-edit-on-drift, anchored-link-coverage-check, neutrality-placeholder-only]
key-files:
  created: []
  modified:
    - docs/reference/brownfield.md
    - docs/quickstart.md
    - .obsidianignore
    - docs/reference/index.md
decisions:
  - "ci.md / release.md / three-layer-model.md / dataview-queries.md verified against source-of-truth and found NO DRIFT — left byte-unchanged (accuracy-pass discipline)"
  - "CONTRIBUTING.md merge-conflict recipes verified accurate against committed .gitattributes — left unchanged; index pointer strengthened instead of creating merge-conflicts.md (D-11)"
  - "brownfield.md line 356 'Phase 10 + Phase 11 requirements' kept as legitimate historical authorship attribution (not stale future-work framing)"
metrics:
  duration: ~12min
  tasks: 2
  files: 4
  completed: 2026-06-01
---

# Phase 13.1 Plan 05: Accuracy-Pass Substantial Docs + Reconcile Index Summary

Accuracy-passed the five substantial reference docs (editing only the one that drifted — `brownfield.md`), corrected the self-contradicting `.obsidianignore`/`quickstart.md` claims, and reconciled `docs/reference/index.md` to catalog all 12 reference files (5 previously-missing additions), with the CONTRIBUTING.md merge-conflict recipes verified accurate.

## What Was Done

### Task 1 — Accuracy-pass the five substantial docs (commit ecb013d)

**brownfield.md (DRIFTED — edited):**

- **Line 3 (the single-phrase gate):** rewrote the stub framing. WAS:
  > "...ships two complete subcommands — `scan` ... and `bootstrap` ... — plus two stubs (`suggest`, `verify`) that ship in Phase 11."

  NOW names all **FIVE** subcommands per AGENTS.md §11.5:
  > "...ships five subcommands — `scan` ..., `bootstrap` ..., `suggest` ..., `review-typing` ..., and `verify` .... See AGENTS.md §11.5 for the authoritative five-subcommand contract."

  (Deliberately wrote FIVE, never "four" — the T-13.1-12 trap. The doc's own `## review-typing` and `## verify` sections already define all five.)

- **Wrong script name (was line 140):** corrected `04-privacy-classification.sh` → `04-privacy-review.sh` (the real shipped name; the doc was internally inconsistent — line 308 already used the correct name). Reworded the surrounding "Bootstrap will NEVER" list from "Phase 11's `0N-*.sh`" future-work framing to shipped-migration framing.

- **Phase 11 audit (~15 refs):** reworded every ref that implied `suggest`/`review-typing`/`verify`/migrations are unshipped future work to shipped tense:
  - line 87 "`suggest` / `lint`'s job in Phase 11" → "`suggest` / `lint`'s job"
  - line 95 "handoff to Phase 11 `suggest/01-page-typing.sh`" → "handoff to the `suggest` / `01-page-typing.sh` migration path"
  - line 148 "user works through Phase 11 migrations" → "...the suggest/review-typing/verify migrations"
  - line 154 "complete Phase 11 migrations" → "...the suggest/review-typing/verify migrations"
  - bootstrap_stage table (165/166): "complete Phase 11 migrations" / "reviewed against Phase 11's suggest/verify workflow" → shipped-workflow wording (verified = passed `verify --promote` 5-gate)
  - line 333 "Phase 11's `01-page-typing.sh` adds inbound-link context" → "The `01-page-typing.sh` migration adds..."
  - line 334 "belong in Phase 11's suggest/verify workflow" → "...the suggest/review-typing/verify workflow"
  - See-also "Brownfield Workflow (populated in Phase 11)" → "(the authoritative five-subcommand contract)"
  - **KEPT (accurate history):** line 356 "REQUIREMENTS.md §BRWN — Phase 10 + Phase 11 requirements" is a legitimate historical-authorship attribution (brownfield requirements genuinely span those phases), so it remains.

- **Every `0[1-4]-*.sh` ref is a real script** — verified all four (`01-page-typing.sh`, `02-provenance-bootstrap.sh`, `03-cross-link-inference.sh`, `04-privacy-review.sh`) exist under `schema/brownfield/migrations/`. No phantoms.

**quickstart.md (DRIFTED — edited, ~line 35):** corrected the false claim. WAS:
> "The `examples/kahneman/` cluster (hidden from Obsidian by default via `.obsidianignore`) is a reference example..."

NOW:
> "The `examples/kahneman/` cluster is a reference example you can browse for shape — note that `.obsidianignore` is a release-manifest + graph-hygiene convention (consumed by `bin/release.sh` and docs), NOT an Obsidian-enforced exclusion; Obsidian does not read it, so `examples/` is indexed by Dataview by default. To hide it from your own vault, use Settings → Files & Links → Excluded files."

**.obsidianignore (SELF-FALSIFYING — edited header):** rewrote the header so the shipped file no longer claims to "hide examples/ from the Obsidian graph view + content indexing." New header truthfully states Obsidian does NOT read the file, that it is a release-manifest + graph-hygiene convention consumed by `bin/release.sh` + docs, that `examples/` IS Dataview-indexed, and that the real exclusion is Settings → Files & Links → Excluded files. Kept the file, kept the `examples/` body line, kept it in the `bin/release.sh` allowlist. Also removed the bare `Kahneman` token from the header prose (neutrality), replacing it with "reference cluster (examples/)".

**ci.md (NO DRIFT — unchanged):** verified the §11.3 severity-remap table matches the CLAUDE.md §11.3 dispatch table verbatim (`yaml`/`orphan`/`crossref`/`provenance` → error; `stale`/`gap`/`contradiction`/`contradiction-sync`/`drift`/`contributor` → warning; `autofix`/`skip-count` → info) and that the `--staged` (Phase 12.2) contract (D-10 only, requires-`--strict`, exemption ordering, `--no-verify`-only bypass) matches §11.3. No edit.

**release.md (NO DRIFT — unchanged):** the documented allowlist matches `bin/release.sh` ALLOWLIST exactly (`AGENTS.md`, `CLAUDE.md`, `README.md`, `LICENSE`, `PRIVACY.md`, `.gitignore`, `.gitattributes`, `.obsidianignore`, `.neutrality-denylist.txt`, `bin/`, `schema/`, `docs/`, `wiki/index.md`, `wiki/log.md`, `wiki/decisions/`, `examples/kahneman/`, `.github/`, `.githooks/`, `tests/phase-07/`). No edit.

**three-layer-model.md (NO DRIFT — unchanged):** the decision-record link `wiki/decisions/dr-2026-05-01-complementary-systems-boundary.md` resolves (file exists). No edit.

**dataview-queries.md (NO DRIFT — unchanged):** 5 `dataview` blocks present; the canonical query bodies match the §16 Appendix A descriptions and the source-of-truth examples in CLAUDE.md §6 (source registry `TABLE source_type, ingested_at, content_hash FROM "wiki/sources"`) and §15 (`TABLE summary, epistemic_status FROM "wiki/entities" WHERE status = "active"`). No edit.

### Task 2 — Reconcile index.md + verify CONTRIBUTING.md (commit 1929838)

- **index.md** now lists all 12 `docs/reference/*.md` files (excluding index.md itself), each as an anchored markdown link. Added the 5 previously-missing entries: `dataview-queries.md`, `commit-examples.md`, `setup-prerequisites.md`, `agent-parity.md` (Plan 04), `obsidian-starter.md` (Plan 01). Dropped the "(Phase 12) populated-later" framing from the now-authored `schema-tour` / `privacy-model` / `examples` descriptions and gave accurate one-liners.
- **CONTRIBUTING.md pointer strengthened** in the Contributing section of index.md to name the verified `wiki/log.md` (append-only) and `wiki/index.md` (category listings) merge-conflict recipes. No new `merge-conflicts.md` (D-11).

**CONTRIBUTING.md merge-conflict recipes — VERIFIED ACCURATE (quoted, not rubber-stamped):**

The `wiki/log.md` recipe's `merge=union` opt-in claim is accurate. CONTRIBUTING.md states:
> ```gitattributes
> # Add this to .gitattributes LOCALLY (not committed by default).
> # Git will union-merge append-only conflicts in log.md automatically.
> wiki/log.md merge=union
> ```
> "We deliberately do NOT commit this as a default `.gitattributes` line."

Verified: `grep -n 'merge=union' .gitattributes` returns NO match — the committed `.gitattributes` indeed does not carry `merge=union`, so the "not committed by default" claim is true.

The `wiki/index.md` recipe is accurate:
> "Resolution — keep both, alphabetize within category, re-run lint:"
> ```bash
> git checkout main -- wiki/index.md
> git checkout your-branch -- wiki/index.md
> ...
> bash bin/lint.sh wiki/                     # confirms no broken links
> ```
> "`merge=union` is NOT recommended for `wiki/index.md` — it would collide headers and produce duplicate category sections."

Verified accurate: `wiki/index.md` is organized by `## Category` headers (per AGENTS.md §12), so `merge=union` would indeed produce duplicate category sections — the warning is correct. Both recipes left unchanged.

## Verification Results

| Gate | Result |
|------|--------|
| brownfield.md no stub-ship phrasing | PASS (`! grep -qi 'stubs.*ship in Phase 11'`) |
| brownfield.md FIVE subcommands incl review-typing, not "four" | PASS |
| brownfield.md `04-privacy-classification` → `04-privacy-review` | PASS |
| every `0[1-4]-*.sh` ref is a real migration script | PASS (4/4 real) |
| quickstart.md false claim removed | PASS |
| .obsidianignore header truthful + still in release allowlist | PASS |
| ci.md `--staged` present; dataview-queries 5+ blocks | PASS |
| index.md lists every reference file (anchored to link form) | PASS (12/12) |
| 5 additions present as links | PASS |
| no `Status: stub` in Plan-05-owned files | PASS |
| no `Status: stub` anywhere in docs/reference/ (Plan-04 precondition) | PASS (none remain) |
| index.md points at CONTRIBUTING.md for merge-conflict recipes | PASS |
| `bash bin/check-neutrality.sh` | PASS (exit 0) |
| `bash bin/sync-claude.sh --check` | PASS (exit 0) |
| `bash bin/lint.sh` (live wiki not regressed) | PASS (exit 0; only pre-existing contradiction-candidate warnings) |

## Deviations from Plan

**1. [Rule 2 - Neutrality] Removed bare `Kahneman` token from .obsidianignore header.**
- **Found during:** Task 1 (.obsidianignore header rewrite)
- **Issue:** The original header prose said "the bundled Kahneman reference cluster" — a bare topical token. Per CLAUDE.md §3 / D-12, template-public files at repo root must use placeholders, and `.obsidianignore` is not in `PUBLIC_PATHS` so `bin/check-neutrality.sh` does not scan it (a leak would be silent).
- **Fix:** Replaced with the neutral "reference cluster (examples/)". Targeted denylist grep `! grep -iE 'kahneman' .obsidianignore` now passes.
- **Files modified:** .obsidianignore
- **Commit:** ecb013d

Otherwise the plan executed as written. Four of the five substantial docs (ci.md, release.md, three-layer-model.md, dataview-queries.md) had no drift and were left byte-unchanged per accuracy-pass discipline.

## Self-Check: PASSED

- FOUND: docs/reference/brownfield.md (modified, committed ecb013d)
- FOUND: docs/quickstart.md (modified, committed ecb013d)
- FOUND: .obsidianignore (modified, committed ecb013d)
- FOUND: docs/reference/index.md (modified, committed 1929838)
- FOUND commit: ecb013d (accuracy-pass brownfield.md + .obsidianignore)
- FOUND commit: 1929838 (reconcile reference index)
