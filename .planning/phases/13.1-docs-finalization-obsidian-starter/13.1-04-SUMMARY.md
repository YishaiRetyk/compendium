---
phase: 13.1-docs-finalization-obsidian-starter
plan: 04
subsystem: docs-reference
tags: [docs, reference, schema, privacy, examples, agent-parity, debt-02, neutrality]
requires:
  - "13.1-02 (dataview-fixtures query|expected_count matrix)"
  - "13.1-03 (DEBT-02 structural-diff evidence in 13.1-VERIFICATION.md)"
provides:
  - "docs/reference/schema-tour.md — full §5 frontmatter + §6 provenance walkthrough"
  - "docs/reference/privacy-model.md — §13 tiers/precedence + check-privacy CI gate"
  - "docs/reference/examples.md — examples/ usage + fixture count matrix + corrected .obsidianignore"
  - "docs/reference/agent-parity.md — DEBT-02 structural-equivalence rubric + Codex re-run procedure"
affects:
  - "Plan 05 (index.md reconciliation registers agent-parity.md + drops 'populated later' framing)"
tech_stack_added: []
patterns:
  - "Reference-doc house style (Shared Pattern B): H1 + > blockquote + ## TL;DR + source-of-truth pointer + ## sections + ## See also"
  - "Path-only golden references (Shared Pattern D/E): examples/kahneman/... paths + prospect-theory hyphenated slug; never a bare kahneman token, never spaced topical denylist phrases"
key_files_created:
  - docs/reference/agent-parity.md
key_files_modified:
  - docs/reference/schema-tour.md
  - docs/reference/privacy-model.md
  - docs/reference/examples.md
decisions:
  - "agent-parity.md Codex column rendered 'pending — deferred to 13.2' (blocked-on-runtime), never a fabricated MATCH — mirrors 13.1-VERIFICATION.md honestly"
  - "Added the spaced phrase 'structural equivalence' to the agent-parity TL;DR so the grep -qi acceptance criterion resolves (doc otherwise used the hyphenated 'structural-equivalence' form)"
  - "active-entities fixture count re-derived live (=3) and asserted equal to the documented value"
metrics:
  duration: "~15 min"
  completed: 2026-06-01
  tasks: 3
  files: 4
---

# Phase 13.1 Plan 04: Docs Finalization Summary

Fully authored the three never-populated `docs/reference/` stubs (schema-tour, privacy-model, examples) against the shipped v1.1 surface and created the new `agent-parity.md` (the DEBT-02 deliverable). examples.md folds in the `examples/dataview-fixtures/` expected-count matrix and corrects the stale `.obsidianignore` indexing claim; agent-parity.md ships the structural-equivalence rubric + the Codex parity re-run procedure, mirroring the recorded (Codex-blocked) outcome honestly.

## What Was Built

- **schema-tour.md** (stub → 155 lines): page-type section-order table, full §5 base frontmatter block (placeholders), the `domains` vs `knowledge_domain` and `status` vs `epistemic_status` distinctions, per-type additions (source tail + decision asymmetry), the §5 validation checklist, the §6 `[prov:...]` grammar + locator table + support types, the `<!-- page: N -->` page-marker convention, claim-level `[epistemic::]` markers, and the staleness decay model. Source-of-truth pointer to AGENTS.md §5/§6.
- **privacy-model.md** (stub → 72 lines): `local_only`/`cloud_safe` tiers, three-level precedence, fail-closed conflict resolution, the §13 7-row decision table reproduced VERBATIM, wiki-page privacy inheritance (strictest-wins), and CI enforcement via `bin/check-privacy.sh` (cross-linked to ci.md). Source-of-truth pointer to AGENTS.md §13.
- **examples.md** (stub → full): how `examples/` works + the `example: true` lint-skip convention; the CORRECTED `.obsidianignore` description (release-manifest/graph-hygiene convention, NOT Obsidian-native; real mechanism = Settings → Files & Links → Excluded files / `userIgnoreFilters`); the `examples/dataview-fixtures/` inventory + five fixture-scoped (`FROM "examples/dataview-fixtures"`) Dataview query blocks each annotated with its expected static grep-proxy count (3/2/2/2/5) + the two Dataview-vs-grep divergence notes; one count re-derived live and asserted equal.
- **agent-parity.md** (NEW): structural-equivalence tolerance rubric (page-set / page-types / prov source-IDs / prov locator-targets / frontmatter-valid PASS; prose may differ freely), golden inventory (1 entity, 3 concepts, 1 comparison, 1 overview, 2 sources; single-source compares vs the prospect-theory SUBSET), codex = cloud-egress-by-default + fail-closed seed guard, the `codex exec` invocation + `pagetypes()`/`provtargets()` structural-diff helpers, and the diff-presentation table with the Codex column rendered `pending — deferred to 13.2` per the recorded blocked-on-runtime outcome.

## Expected-Count Matrix Carried Into examples.md

| query | expected_count | live re-derivation |
|-------|----------------|--------------------|
| active-entities | 3 | `grep -l '^type: entity' examples/dataview-fixtures/*.md \| xargs grep -l '^status: active' \| wc -l` → 3 ✓ |
| sources-by-domain-alpha | 2 | (from 13.1-02 matrix) |
| stale-pages | 2 | (from 13.1-02 matrix) |
| missing-privacy | 2 | (from 13.1-02 matrix) |
| pages-in-domain-alpha-active | 5 | (from 13.1-02 matrix) |

Re-derivation command output for active-entities = **3**, equal to the documented value (REVIEWS HIGH "counts never validated" closed for ≥1 count).

## DEBT-02 Honesty Mirror

agent-parity.md mirrors `13.1-VERIFICATION.md` exactly: Claude-side prov-locator-target set EXACT-matches the golden prospect-theory subset (all 6 `#sec:` targets); the Codex side was blocked on a host bubblewrap user-namespace sandbox pathology (0 wiki pages) and is recorded as `pending — deferred to 13.2`, NOT as a measured MATCH. No parity that was never measured is asserted.

## Deviations from Plan

- **[Rule 3 — Blocking issue] agent-parity.md "structural equivalence" grep.** The Task-3 automated check is `grep -qi 'structural equivalence'` (spaced). The doc initially used only the hyphenated `structural-equivalence` form and `structurally equivalent`, so the grep returned 0. Added the spaced phrase to the TL;DR ("This structural equivalence bar is the DEBT-02 tolerance contract") to satisfy the acceptance criterion without altering meaning. Fixed before the Task-3 commit (`39e47fb`).

No other deviations — all three tasks executed as written.

## Neutrality Discipline

All four docs use placeholders only; the golden is referenced ONLY by `examples/kahneman/...` PATH + type + count. The sole bare token used is the hyphenated `prospect-theory` SLUG (not on the denylist). No bare `kahneman` token survives the gate's path-scrub anywhere; no spaced topical denylist phrases ("prospect theory" / "loss aversion" / "cognitive biases"). `bash bin/check-neutrality.sh` → exit 0 after each task.

## Verification Evidence

- No `Status: stub` lines remain in schema-tour.md, privacy-model.md, examples.md (grep → none).
- schema-tour.md `## See also` present + `prov:` present (6); ≥25 lines (155).
- privacy-model.md `## See also` + `local_only` (17) + `cloud_safe` (10) + `check-privacy` (3); ≥25 lines (72).
- examples.md: `examples/dataview-fixtures` present; `example: true` present; `Excluded files`/`userIgnoreFilters` present; NO "obsidianignore...hides...index" assertion; `FROM "examples/dataview-fixtures"` blocks = 6 (≥5); `expected` present; live active-entities count = 3 = documented.
- agent-parity.md exists; `structural equivalence` present; `codex exec` present (4); `examples/kahneman` present (4); `thinking-fast-and-slow-part1` absent; diff-table dimensions present; `## See also` present.
- `bash bin/check-neutrality.sh` → exit 0 (real gate; docs/ is in PUBLIC_PATHS).
- NOTE: `bin/lint.sh` walks `wiki/` only and is a NO-OP for docs/reference/, so it is not used as proof here (per plan verification note).

## Commits

- `eb7b419` docs(13.1-04): author schema-tour and privacy-model reference docs
- `8060c7d` docs(13.1-04): author examples.md with fixture count matrix and obsidianignore correction
- `39e47fb` docs(13.1-04): create agent-parity.md (DEBT-02 deliverable)

## Self-Check: PASSED

All 4 deliverable files verified present; all 3 task commits verified in git log.
