---
phase: 09.1
plan: 02
type: execute
wave: 1
depends_on:
  - "09.1-01"
files_modified:
  - AGENTS.md
  - CLAUDE.md
  - schema/AGENTS.template.md
  - schema/fixtures/canonical-AGENTS.md
  - schema/examples/entity.md
  - schema/examples/concept.md
  - schema/examples/source-summary.md
  - schema/examples/comparison.md
  - schema/examples/overview.md
  - schema/examples/decision.md
  - docs/reference/dataview-queries.md
  - docs/reference/commit-examples.md
  - wiki/decisions/dr-2026-04-16-progressive-disclosure-extraction.md
  - wiki/index.md
  - wiki/log.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "I-1: cmp -s AGENTS.md CLAUDE.md returns 0 (byte-equality preserved after edits)"
    - "I-2: schema/fixtures/canonical-AGENTS.md matches python3 str.replace render of schema/AGENTS.template.md (Phase-08 gate stays green)"
    - "I-3: docs/reference/examples.md untouched (Phase-07 stub assertion holds)"
    - "I-4: schema/examples/ contains exactly 6 .md files: entity, concept, source-summary, comparison, overview, decision"
    - "I-5: Each schema/examples/*.md has valid YAML frontmatter with example: true, privacy: cloud_safe, and type enum matching filename (source-summary.md → type: source)"
    - "I-6: AGENTS.md §4 has exactly 6 bare `See: schema/examples/<type>.md for a concrete filled-in instance.` pointers at column 0, no markdown-link / bold / list-bullet forms; former worked-example content absent"
    - "I-7: AGENTS.md §16 Appendix A has pointer to docs/reference/dataview-queries.md; former TABLE/dataview block absent"
    - "I-8: AGENTS.md §16 Appendix B has pointer to docs/reference/commit-examples.md; former reflect(q1-review) commit example absent"
    - "I-9: AGENTS.md §16 Appendix C heading + all 10 QRC rules preserved verbatim"
    - "I-10: docs/reference/dataview-queries.md exists with exactly 5 direct ```dataview fenced blocks (no outer ```markdown wrappers per R2 review consensus) and no YAML frontmatter"
    - "I-11: docs/reference/commit-examples.md exists with a code block containing the 5 representative commit-message samples and no YAML frontmatter"
    - "I-12: wiki/decisions/dr-2026-04-16-progressive-disclosure-extraction.md exists with trigger_type: schema-update, affected_pages: [] EXACTLY (R3 review consensus), all 7 required sections, Why section names adopted framing and what it replaced, Alternatives Considered lists ≥1 alternative"
    - "I-13: wiki/index.md has `## Decisions` heading and bullet referencing dr-2026-04-16-progressive-disclosure-extraction"
    - "I-14 (neutrality): bin/check-neutrality.sh exits 0 — no Kahneman/personal-domain tokens in new docs/reference files (body-text scan, independent of R6 frontmatter-absence argument)"
    - "I-15 (privacy): bin/check-privacy.sh exits 0 — no privacy: local_only frontmatter leaks into docs/reference or other public paths (frontmatter-only scanner, so frontmatter-free new files trivially pass)"
    - "Template parity: AGENTS.md §4 and §16 bodies remain byte-identical to schema/AGENTS.template.md at offset-adjusted line ranges post-extraction (R5 preflight enforces this as precondition before edits)"
    - "R5 preflight: bash tests/phase-09.1/test_template_parity.sh exits 0 BEFORE any AGENTS.md or schema/AGENTS.template.md edit; if it fails, task aborts"
    - "R11 log entry: wiki/log.md gains exactly one new reflect entry '## [2026-04-16] reflect | progressive disclosure extraction' at EOF (append-only per AGENTS.md §12)"
    - "R2 dataview-fence shape: docs/reference/dataview-queries.md contains zero ```markdown outer wrappers (only direct ```dataview fences); enforced by test_dataview_fences.sh from Plan 09.1-01"
    - "R10 commit subject: uses unscoped `schema:` prefix per AGENTS.md §3 commit-conventions table (scoped `schema(NN):` is undocumented)"
  artifacts:
    - path: "schema/examples/entity.md"
      provides: "Fully worked Entity page-type instance (Geoffrey Hinton); extracted from AGENTS.md §4.1 body lines 146-194; frontmatter adds example: true"
      contains: "type: entity"
    - path: "schema/examples/concept.md"
      provides: "Fully worked Concept instance (Attention Mechanism); extracted from AGENTS.md §4.2 body lines 208-263"
      contains: "type: concept"
    - path: "schema/examples/source-summary.md"
      provides: "Fully worked Source Summary (Vaswani et al.); extracted from AGENTS.md §4.3 body lines 287-341; filename hyphenated, frontmatter type enum is `source`"
      contains: "type: source"
    - path: "schema/examples/comparison.md"
      provides: "Fully worked Comparison (RNNs vs Transformers); extracted from AGENTS.md §4.4 body lines 355-408"
      contains: "type: comparison"
    - path: "schema/examples/overview.md"
      provides: "Fully worked Overview (Deep Learning); extracted from AGENTS.md §4.5 body lines 422-478"
      contains: "type: overview"
    - path: "schema/examples/decision.md"
      provides: "Fully worked Decision record (dr-2026-04-14-phase6-decision-type illustrative); extracted from AGENTS.md §4.6 body lines 524-557 with truncation preserved per D-07"
      contains: "type: decision"
    - path: "docs/reference/dataview-queries.md"
      provides: "5 Dataview query patterns extracted from AGENTS.md §16 Appendix A (lines 1704-1756); H1 + blockquote framing + TL;DR + body + See also footer per docs/reference/ci.md shape. R2 transformation: outer ```markdown fence is STRIPPED when extracting; only the inner ```dataview``` block is preserved (standalone cookbook shape, not illustrative-inline shape)."
      contains: "```dataview"
    - path: "docs/reference/commit-examples.md"
      provides: "Representative commit messages extracted from AGENTS.md §16 Appendix B (lines 1760-1770); same shape as dataview-queries.md"
      contains: "schema: define base frontmatter"
    - path: "wiki/decisions/dr-2026-04-16-progressive-disclosure-extraction.md"
      provides: "Tier-1 inline decision record per §11.4 + D-14; trigger_type: schema-update, affected_pages: [], 7 required sections"
      contains: "trigger_type: schema-update"
    - path: "AGENTS.md"
      provides: "Extracted spec: §4.1-4.6 worked example blocks replaced by D-08 residue shape; §16 Appendix A/B replaced by pointer residue; §16 Appendix C verbatim; all other sections unchanged"
      contains: "See: schema/examples/entity.md for a concrete filled-in instance."
    - path: "schema/AGENTS.template.md"
      provides: "Mirror of AGENTS.md edits at offset-adjusted line ranges (+2 for §4, -62 for §16); byte-identical body regions to AGENTS.md"
      contains: "See: schema/examples/entity.md for a concrete filled-in instance."
    - path: "schema/fixtures/canonical-AGENTS.md"
      provides: "Regenerated via verbatim Plan 08-01 python3 str.replace render routine after template edit"
      contains: "See: schema/examples/entity.md for a concrete filled-in instance."
    - path: "wiki/index.md"
      provides: "Adds new ## Decisions section (did not exist before — PATTERNS Note 2) listing the new DR plus existing DRs"
      contains: "## Decisions"
    - path: "wiki/log.md"
      provides: "R11 review consensus: new reflect-typed log entry for 2026-04-16 capturing the extraction activity per AGENTS.md §9/§12 formal-operations vocabulary. Appended at EOF (append-only). Complements the Tier-1 DR: DR is the structural decision record (why); log entry is the activity record (what/when)."
      contains: "## [2026-04-16] reflect | progressive disclosure extraction"
    - path: "CLAUDE.md"
      provides: "Byte-identical mirror of post-extraction AGENTS.md (auto-synced by .githooks/pre-commit)"
      contains: "See: schema/examples/entity.md for a concrete filled-in instance."
  key_links:
    - from: "AGENTS.md §4.1-4.6"
      to: "schema/examples/*.md"
      via: "bare-prefix See: pointer on its own line"
      pattern: "^See: schema/examples/(entity|concept|source-summary|comparison|overview|decision)\\.md for a concrete filled-in instance\\.$"
    - from: "AGENTS.md §16 Appendix A/B"
      to: "docs/reference/{dataview-queries,commit-examples}.md"
      via: "inline backtick-wrapped path reference in one-line prose"
      pattern: "docs/reference/(dataview-queries|commit-examples)\\.md"
    - from: "schema/fixtures/canonical-AGENTS.md"
      to: "schema/AGENTS.template.md"
      via: "python3 str.replace render with 4 wizard placeholder substitutions (Plan 08-01 routine)"
      pattern: "canonical-AGENTS\\.md"
    - from: "CLAUDE.md"
      to: "AGENTS.md"
      via: ".githooks/pre-commit → bin/sync-claude.sh (cp-based byte copy)"
      pattern: "bin/sync-claude\\.sh"
    - from: "wiki/decisions/dr-2026-04-16-progressive-disclosure-extraction.md"
      to: "AGENTS.md §11.4 Tier 1"
      via: "inline decision-record obligation for schema-updates (D-14)"
      pattern: "trigger_type: schema-update"
    - from: "wiki/index.md"
      to: "wiki/decisions/dr-2026-04-16-progressive-disclosure-extraction.md"
      via: "bulleted wikilink under ## Decisions heading"
      pattern: "dr-2026-04-16-progressive-disclosure-extraction"
    - from: "Task 2 STEP 0 preflight"
      to: "tests/phase-09.1/test_template_parity.sh"
      via: "R5 enforced precondition: bash tests/phase-09.1/test_template_parity.sh must exit 0 before any AGENTS.md or schema/AGENTS.template.md edit; task aborts on non-zero"
      pattern: "test_template_parity\\.sh"
    - from: "wiki/log.md reflect entry"
      to: "AGENTS.md §12 activity-log conventions"
      via: "R11 review consensus: append '## [2026-04-16] reflect | progressive disclosure extraction' per §12 newest-at-bottom append-only log"
      pattern: "^## \\[2026-04-16\\] reflect"
---

<objective>
Perform the atomic Wave-1 extraction: run the R5 preflight (`bash tests/phase-09.1/test_template_parity.sh`) and abort if parity drifted, then move AGENTS.md §4 worked examples to `schema/examples/<type>.md` (6 files), move §16 Appendix A/B to `docs/reference/{dataview-queries,commit-examples}.md` (2 files — `dataview-queries.md` uses DIRECT ```dataview fences per R2, not the nested ```markdown wrapper from AGENTS.md §16 Appendix A), replace with D-08/D-13 residue, mirror all edits byte-identically to `schema/AGENTS.template.md`, regenerate `schema/fixtures/canonical-AGENTS.md` via the verbatim Plan 08-01 python3 routine, create the inline Tier-1 decision record, add `## Decisions` section to `wiki/index.md`, append the R11 reflect entry to `wiki/log.md`, and let `.githooks/pre-commit` auto-sync `CLAUDE.md`. Single commit with unscoped `schema:` subject per R10. All 8 RED tests from Plan 09.1-01 flip to GREEN; 3 canary tests stay GREEN.

Purpose: Close out Phase 09.1 by executing the extraction in one atomic transaction. The dual-file edit (AGENTS.md + schema/AGENTS.template.md) prevents wizard/manual-track drift (R-1). The verbatim canonical render routine prevents setup-parity CI regression (R-2 from RESEARCH — distinct from R2 review item). The byte-equality pre-commit hook prevents CLAUDE.md drift (R-3 from RESEARCH). The goal-backward must-haves above lock all 15 RESEARCH.md §7 invariants PLUS the four review-consensus items (R2 dataview-fence shape, R3 affected_pages exactness, R5 preflight, R11 log entry).

Output: 6 new `schema/examples/*.md` files, 2 new `docs/reference/*.md` files (dataview-queries.md with direct dataview fences per R2), 1 new DR, 5 modified files (AGENTS.md, CLAUDE.md auto, schema/AGENTS.template.md, schema/fixtures/canonical-AGENTS.md, wiki/index.md, wiki/log.md). `docs/reference/index.md` explicitly NOT touched per R12. AGENTS.md shrinks from 1,785 lines to ~1,435 (actual ~350 line reduction — RESEARCH.md §2 honest arithmetic vs D-01 headline ~500, documented in the DR).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.1-progressive-disclosure-extraction/09.1-CONTEXT.md
@.planning/phases/09.1-progressive-disclosure-extraction/09.1-RESEARCH.md
@.planning/phases/09.1-progressive-disclosure-extraction/09.1-PATTERNS.md
@.planning/phases/09.1-progressive-disclosure-extraction/09.1-VALIDATION.md
@.planning/phases/09.1-progressive-disclosure-extraction/09.1-01-PLAN.md
@AGENTS.md
@schema/AGENTS.template.md
@schema/fixtures/canonical-AGENTS.md
@wiki/decisions/dr-2026-04-14-phase6-decision-type.md
@wiki/index.md
@docs/reference/ci.md
@docs/reference/privacy-model.md
@schema/templates/entity.md
@.githooks/pre-commit
@bin/sync-claude.sh

<interfaces>
<!-- Key interfaces, line ranges, and verbatim strings extracted from RESEARCH.md §2/§3/§5/§6 and PATTERNS.md. Executor uses these directly. -->

From RESEARCH.md §2.1 (AGENTS.md extraction ranges — verified via direct sed/diff inspection):
- §4.1 Entity kept-inline: 135-142; worked-example REPLACED: 143-195 (body lines 146-194 go to schema/examples/entity.md)
- §4.2 Concept kept: 197-205; REPLACED: 206-264 (body 208-263 → concept.md)
- §4.3 Source Summary kept: 266-284 (incl. "Additional frontmatter fields" table per D-08 rule 4); REPLACED: 285-342 (body 287-341 → source-summary.md)
- §4.4 Comparison kept: 344-352; REPLACED: 353-409 (body 355-408 → comparison.md)
- §4.5 Overview kept: 411-419; REPLACED: 420-479 (body 422-478 → overview.md)
- §4.6 Decision kept: 481-521 (incl. Directory/File naming/ID/Frontmatter table/Section ordering/Epistemic/decision_history — all normative); REPLACED: 522-558 (body 524-557 → decision.md, truncation preserved per D-07)
- §16 Appendix A REPLACED: 1702-1756 → docs/reference/dataview-queries.md (body 1704-1756)
- §16 Appendix B REPLACED: 1758-1770 → docs/reference/commit-examples.md (body 1760-1770)
- §16 Appendix C UNCHANGED: 1772-1785

From RESEARCH.md §2.2 (schema/AGENTS.template.md mirror line ranges — offset-adjusted):
- §4 template: lines 133-561 (offset +2 from AGENTS.md due to wizard header at line 32)
- §16 template: lines 1638-1723 (offset -62 due to Phase-9 template lag; OUT OF SCOPE to reconcile — see R-7)

From RESEARCH.md §3 (verbatim Plan 08-01 canonical render routine — COPY EXACTLY):
```bash
python3 -c '
import sys
src = open("schema/AGENTS.template.md", encoding="utf-8").read()
subs = {
  "{{AGENT_FILENAME}}":  "CLAUDE.md",
  "{{PRIMARY_DOMAIN}}":  "personal-knowledge",
  "{{DEFAULT_PRIVACY}}": "cloud_safe",
  "{{DECAY_PROFILE}}":   "default",
}
for k, v in subs.items():
  src = src.replace(k, v)
import re
leftover = re.findall(r"\{\{[A-Z_]+\}\}", src)
if leftover:
  sys.exit(f"ERROR: leftover placeholders: {leftover}")
open("schema/fixtures/canonical-AGENTS.md", "w", encoding="utf-8", newline="\n").write(src)
'
```

From RESEARCH.md §5 (§4 residue template — EXACT SHAPE):
```markdown
**Example:** {EXAMPLE_SUBJECT}. Demonstrates {CAPSULE_BULLET_1}; {CAPSULE_BULLET_2}; {CAPSULE_BULLET_3}.

See: schema/examples/{TYPE_SLUG}.md for a concrete filled-in instance.
```
(Replaces the `**Worked example:**` + fenced ```markdown block. Blank line between `**Example:**` line and `See:` line. `See:` starts at column 0 — NO bullet, NO bold, NO markdown link.)

From RESEARCH.md §6 (§16 residue shape):
Appendix A residue (replaces AGENTS.md 1702-1756):
```markdown
### Appendix A: Dataview Query Examples

See `docs/reference/dataview-queries.md` for five Dataview query patterns (active entities, sources by domain, stale pages, missing privacy classification, pages in a domain).
```
Appendix B residue (replaces AGENTS.md 1758-1770):
```markdown
### Appendix B: Commit Message Examples

See `docs/reference/commit-examples.md` for representative commit messages per workflow type (schema/ingest/query/lint/reflect).
```

From PATTERNS.md Shared Pattern 6 (frontmatter addition for examples):
Every schema/examples/*.md gets `example: true` as last line of frontmatter before closing `---`. Matches AGENTS.md §5 field definition and examples/kahneman/ precedent. DO NOT copy the `<!-- FORBIDDEN PATTERNS ... -->` HTML comment from schema/templates/*.md.

From PATTERNS.md Shared Pattern 4 (R-1 dual-file edit rule):
CRITICAL: Edit AGENTS.md AND schema/AGENTS.template.md in the same commit. §4/§16 bodies are byte-identical between the two files today. Make the same textual edit at each file's respective line range. DO NOT commit AGENTS.md alone.

From PATTERNS.md Note 2 (wiki/index.md):
Current wiki/index.md has NO `## Decisions` heading. Wave 1 must CREATE this section. Listing all three existing DRs (phase6-decision-type, kahneman-to-examples, progressive-disclosure-extraction) is more complete than listing only the new one.

From Note 1 (AGENTS.md line 133):
Current preamble says "Five page types exist." Now 6 (decision was added Phase 6). Per D-07 this is NOT required but is a one-word polish. RECOMMENDED fix: `Five` → `Six` (add to DR Consequences if fixed).

From .githooks/pre-commit behavior (D-16 / R-3):
When AGENTS.md changes and CLAUDE.md is stale, pre-commit:
1. Runs bin/sync-claude.sh --check
2. On drift: runs bin/sync-claude.sh (cp AGENTS.md CLAUDE.md)
3. git add CLAUDE.md
4. Exits 1 with "Re-run commit." message
5. This is EXPECTED. Simply re-run `git commit -C HEAD` or stage CLAUDE.md manually first via `bash bin/sync-claude.sh && git add CLAUDE.md` before the single commit.

From wiki/decisions/dr-2026-04-14-phase6-decision-type.md (DR shape analog):
- 24-line frontmatter block with base fields + knowledge_domain: software + trigger_type: schema-update + affected_pages: []
- Includes the `<!-- FORBIDDEN PATTERNS ... -->` HTML comment between frontmatter and TL;DR
- 7 sections in order: TL;DR, Decision, Why, Alternatives Considered, Consequences, Affected Pages, Sources
- "## Why" names adopted framing AND prior framing explicitly
- "## Alternatives Considered" uses bulleted list with rationale per alternative
- "## Affected Pages" = "None. This is the inaugural decision record…" for infrastructure DRs
- "## Sources" = "None. This is an internal schema decision; the decision record itself is the source of truth."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 6 schema/examples/*.md + 2 docs/reference/*.md + decision record</name>
  <files>schema/examples/entity.md, schema/examples/concept.md, schema/examples/source-summary.md, schema/examples/comparison.md, schema/examples/overview.md, schema/examples/decision.md, docs/reference/dataview-queries.md, docs/reference/commit-examples.md, wiki/decisions/dr-2026-04-16-progressive-disclosure-extraction.md</files>

  <read_first>
    - .planning/phases/09.1-progressive-disclosure-extraction/09.1-CONTEXT.md (D-04 through D-14)
    - .planning/phases/09.1-progressive-disclosure-extraction/09.1-RESEARCH.md §2.3, §2.4 (extraction content sources, filename/type-enum asymmetry for source-summary)
    - .planning/phases/09.1-progressive-disclosure-extraction/09.1-PATTERNS.md Shared Pattern 6 (example: true), lines 45-97 (schema/examples/* shape), lines 99-152 (docs/reference/* shape), lines 155-207 (DR shape)
    - AGENTS.md lines 143-195 (§4.1 Entity worked-example block — extract body 146-194 for entity.md)
    - AGENTS.md lines 206-264 (§4.2 Concept — extract 208-263 for concept.md)
    - AGENTS.md lines 285-342 (§4.3 Source Summary — extract 287-341 for source-summary.md)
    - AGENTS.md lines 353-409 (§4.4 Comparison — extract 355-408 for comparison.md)
    - AGENTS.md lines 420-479 (§4.5 Overview — extract 422-478 for overview.md)
    - AGENTS.md lines 522-558 (§4.6 Decision worked example — extract body 524-557 for decision.md; preserve the `(...remaining sections: Alternatives Considered, Consequences, Affected Pages, Sources.)` truncation per D-07)
    - AGENTS.md lines 1702-1756 (§16 Appendix A — 5 ```dataview blocks verbatim for dataview-queries.md body)
    - AGENTS.md lines 1758-1770 (§16 Appendix B — single code block with commit examples verbatim for commit-examples.md body)
    - wiki/decisions/dr-2026-04-14-phase6-decision-type.md (verbatim frontmatter + section shape analog for the new DR)
    - docs/reference/ci.md lines 1-15 (H1 + blockquote + TL;DR shape for new docs/reference files)
    - docs/reference/privacy-model.md (See also footer shape)
    - schema/templates/entity.md (frontmatter key ordering — do NOT copy `<!-- FORBIDDEN PATTERNS ... -->` HTML comment into examples/*)
  </read_first>

  <action>
STEP 1 — Create `schema/examples/entity.md` by copying the body of AGENTS.md §4.1 fenced block (lines 146-194 — the YAML frontmatter + Hinton content between the ```markdown and ``` fences, WITHOUT the fence lines themselves) and adding `example: true` as the last frontmatter key before the closing `---`. Exact body (copy verbatim from AGENTS.md lines 146-194, then add the single new line):

```
---
id: geoffrey-hinton
title: Geoffrey Hinton
type: entity
status: active
summary: "British-Canadian computer scientist, pioneer of deep learning and neural networks."
created_at: 2026-04-08
updated_at: 2026-04-08
sources:
  - src-2026-03-20-hinton-interview
epistemic_status: sourced
tags:
  - researcher
  - deep-learning
domains:
  - ai-research
supersedes:
superseded_by:
privacy: cloud_safe
aliases:
  - Geoff Hinton
example: true
---

## TL;DR

Geoffrey Hinton is a pioneer of [[Deep Learning]] and co-inventor of [[Backpropagation]]. He shared the 2018 Turing Award with [[Yoshua Bengio]] and [[Yann LeCun]].

## Key Facts

- Co-invented backpropagation algorithm for training neural networks [prov:src-2026-03-20-hinton-interview#sec:early-work]
- Pioneered deep belief networks and restricted Boltzmann machines [prov:src-2026-03-20-hinton-interview#sec:contributions|direct]
- Left Google in 2023 citing concerns about AI safety [prov:src-2026-03-20-hinton-interview#sec:google-departure|direct|2026-04-08]

## Detail

Geoffrey Hinton spent decades working on neural networks when they were considered a dead end by much of the AI research community. His persistence led to breakthroughs in deep belief networks (2006) and later contributed to the deep learning revolution. His backpropagation work with David Rumelhart and Ronald Williams provided the foundational training algorithm still used today.

In 2023, Hinton resigned from Google to speak freely about AI safety risks, particularly the potential for AI systems to become more intelligent than humans. He has since been vocal about the need for regulation and safety research.

## Related Pages

- [[Deep Learning]]
- [[Backpropagation]]
- [[Neural Networks]]
- [[AI Safety]]

## Sources

- [[src-2026-03-20-hinton-interview]]: "Geoffrey Hinton Interview on AI Safety" (2026-03-20)
```

Ensure trailing newline (single `\n`). Do NOT include any `<!-- FORBIDDEN PATTERNS ... -->` HTML comment (that belongs to templates, not examples).

STEP 2 — Create `schema/examples/concept.md` by copying AGENTS.md §4.2 body (lines 208-263) verbatim, adding `example: true` after the `aliases:` list. The YAML structure is:

```
---
id: attention-mechanism
title: Attention Mechanism
type: concept
status: active
summary: "A neural network component that allows models to focus on relevant parts of the input sequence."
created_at: 2026-04-08
updated_at: 2026-04-08
sources:
  - src-2026-03-15-vaswani-attention
  - src-2026-04-01-bahdanau-alignment
epistemic_status: sourced
tags:
  - machine-learning
  - transformers
  - deep-learning
domains:
  - ai-research
supersedes:
superseded_by:
privacy: cloud_safe
aliases:
  - Attention
  - Self-Attention
example: true
---
```

Follow with body verbatim from AGENTS.md lines 234-263 (the ## TL;DR through ## Sources block). Trailing LF.

STEP 3 — Create `schema/examples/source-summary.md`. Filename IS `source-summary.md` (hyphen, matches schema/templates/source-summary.md sibling) BUT frontmatter `type: source` (singular, enum value per AGENTS.md §5). Copy AGENTS.md §4.3 body (lines 287-341) verbatim, add `example: true` as the last frontmatter key before the closing `---`. The body includes extended source-type fields (`path`, `url`, `content_hash`, `ingested_at`, `source_type`) and sections TL;DR → Key Takeaways → Extracted Claims → Notes → Source Metadata. Trailing LF.

STEP 4 — Create `schema/examples/comparison.md` from AGENTS.md lines 355-408. `type: comparison`, add `example: true`. Section order: TL;DR → Bottom Line → Comparison Table → Detailed Comparison → Sources. Trailing LF.

STEP 5 — Create `schema/examples/overview.md` from AGENTS.md lines 422-478. `type: overview`, add `example: true`. Section order: TL;DR → Key Facts → Detail → Related Pages → Sources. Trailing LF.

STEP 6 — Create `schema/examples/decision.md` from AGENTS.md lines 524-557. `type: decision`, add `example: true`. Preserve the truncation line `(...remaining sections: Alternatives Considered, Consequences, Affected Pages, Sources.)` EXACTLY per D-07 (no rewrite). Sections in order: TL;DR, Decision, Why. Frontmatter includes `trigger_type: schema-update` and `affected_pages: []`. Trailing LF.

STEP 7 — Create `docs/reference/dataview-queries.md`. NO YAML frontmatter (docs/reference/ convention per PATTERNS.md Shared Pattern 2 rule 5). Shape mirrors docs/reference/ci.md.

**CRITICAL TRANSFORMATION (R2 review consensus — DIFFERS from the AGENTS.md §16 Appendix A source verbatim):**

AGENTS.md §16 Appendix A wraps each inner ` ```dataview ``` ` block inside an outer ` ```markdown ``` ` fence. That outer wrapper is CORRECT inside AGENTS.md because the intent there is illustrative — "here is how an Obsidian markdown page embeds a Dataview query" — so the outer markdown fence shows the reader the verbatim markdown syntax they'd write. In a standalone cookbook file (`docs/reference/dataview-queries.md`), the outer wrapper is WRONG: readers want direct, copy-pasteable ` ```dataview ``` ` snippets, not markdown-escaped examples that require unwrapping. Cross-AI review (Gemini + Codex) flagged this as a HIGH-severity extraction bug.

**Transformation to apply when copying from AGENTS.md §16 Appendix A:**
- STRIP the outer ` ```` ` `markdown ` ` ```` ` opening fence and its matching closing fence around each example.
- PRESERVE the inner ` ``` ` `dataview ` ` ``` ` block and its closing fence exactly as-is.
- PRESERVE the surrounding prose labels ("**List all active entity pages:**", etc.) exactly as-is.

**Exact body to write** (direct `dataview` fences, no outer wrappers):

```markdown
# Dataview Query Examples

> Reference documentation for the five canonical Dataview query patterns used across the wiki. Extracted from AGENTS.md §16 Appendix A for progressive-disclosure readability.

## TL;DR

The wiki uses Dataview to query frontmatter fields across pages. These five patterns cover the common cases: listing active entities, finding sources by domain, surfacing stale pages, spotting missing privacy classification, and enumerating pages in a specific domain. See [AGENTS.md §5](../../AGENTS.md) for the underlying frontmatter schema these queries read.

## Query Patterns

**List all active entity pages:**

<INNER `dataview` BLOCK FROM AGENTS.md LINES ~1710-1716, STRIPPED OF OUTER ` ```markdown ` WRAPPER>

**List all sources by domain:**

<INNER `dataview` BLOCK FROM AGENTS.md LINES ~1722-1728, STRIPPED OF OUTER ` ```markdown ` WRAPPER>

**List stale pages across the entire wiki:**

<INNER `dataview` BLOCK FROM AGENTS.md LINES ~1734-1740, STRIPPED OF OUTER ` ```markdown ` WRAPPER>

**Find pages missing privacy classification:**

<INNER `dataview` BLOCK FROM AGENTS.md LINES ~1746-1750, STRIPPED OF OUTER ` ```markdown ` WRAPPER>

**List all pages in a specific domain:**

<INNER `dataview` BLOCK FROM AGENTS.md LINES ~1756-1762, STRIPPED OF OUTER ` ```markdown ` WRAPPER>

## See also

- [AGENTS.md](../../AGENTS.md)
- [../README.md](../README.md)
```

**Acceptance gates (enforced by `tests/phase-09.1/test_dataview_fences.sh` created in Plan 09.1-01 STEP 8):**
- `grep -cE '^```dataview$' docs/reference/dataview-queries.md` MUST equal 5 (five direct dataview fence opens).
- `grep -cE '^```markdown$' docs/reference/dataview-queries.md` MUST equal 0 (zero outer markdown wrappers — standalone cookbook does not need them).

Trailing LF.

STEP 8 — Create `docs/reference/commit-examples.md`. Same shape, no frontmatter. Body:

```markdown
# Commit Message Examples

> Reference documentation for representative commit messages per wiki workflow type. Extracted from AGENTS.md §16 Appendix B for progressive-disclosure readability.

## TL;DR

All wiki commits use conventional-commit format with a workflow-type prefix: `schema`, `ingest(<source-slug>)`, `query(<topic>)`, `lint(<scope>)`, or `reflect(<scope>)`. See [AGENTS.md §3](../../AGENTS.md) for the full commit-convention rules and the one-commit-per-logical-operation discipline.

## Examples

<VERBATIM FENCED BLOCK FROM AGENTS.md LINES 1762-1770 — the 9 commit-message lines>

## See also

- [AGENTS.md](../../AGENTS.md)
- [../README.md](../README.md)
```

The verbatim body block from AGENTS.md must contain exactly these lines (per AGENTS.md 1762-1770):

```
schema: define base frontmatter fields and page type conventions
ingest(hinton-interview): add source summary and update entity pages
ingest(vaswani-attention): create source summary with 3 extracted claims, update attention mechanism page
query(attention-mechanisms): synthesize comparison of attention variants
query(ai-safety-timeline): create overview page from 4 existing sources
lint(wiki): fix 3 orphan pages and 2 broken provenance references
lint(entities): update 5 stale epistemic_status markers
reflect(q1-review): restructure AI safety domain after new sources
reflect(domain-split): separate neuroscience from ai-research domain
```

Wrap in a plain ` ``` ` fenced code block (no language tag). Trailing LF.

STEP 9 — Create `wiki/decisions/dr-2026-04-16-progressive-disclosure-extraction.md` using `wiki/decisions/dr-2026-04-14-phase6-decision-type.md` as the shape analog. Exact frontmatter:

```yaml
---
id: dr-2026-04-16-progressive-disclosure-extraction
title: "Extract §4 Worked Examples and §16 Appendices A+B to Deep References"
type: decision
status: active
summary: "AGENTS.md §4 page-type worked examples move to schema/examples/<type>.md and §16 Appendices A (Dataview queries) and B (commit examples) move to docs/reference/dataview-queries.md and docs/reference/commit-examples.md. §16 Appendix C (Quick Reference Card) and all §4 normative prose (purpose, when-to-use, section order, frontmatter-required tables) remain inline."
created_at: 2026-04-16
updated_at: 2026-04-16
sources: []
epistemic_status: sourced
tags:
  - meta
  - schema
domains:
  - wiki-infrastructure
supersedes:
superseded_by:
privacy: cloud_safe
aliases: []
has_contradictions: false
knowledge_domain: software
trigger_type: schema-update
affected_pages: []
---

<!-- FORBIDDEN PATTERNS (see AGENTS.md section 3):
     - No wikilinks in frontmatter (use plain string IDs in sources[], affected_pages[], supersedes, etc.)
     - No display aliases: write [[Page Title]] not [[Page Title|Alias]]
     - Link each page only on first mention in the body -->
```

Then all 7 required sections. Each section must have substantive content (not a placeholder). Suggested content:

`## TL;DR` — "AGENTS.md §4 worked examples and §16 Appendices A and B move out of the core spec into deep-reference files under schema/examples/ and docs/reference/; the core spec retains per-type purpose/section-order/frontmatter prose inline and §16 Appendix C verbatim. Net: ~350 lines (~20%) removed from AGENTS.md; no meaning lost."

`## Decision` — Explicit enumeration of what moved where, the residue shape used, and the parallel edits. Must name: (a) the 6 new schema/examples/*.md files; (b) the 2 new docs/reference/*.md files; (c) the D-08 residue shape for §4 (purpose + section-order + when-to-use + type-specific-frontmatter-table + 2-3-bullet capsule + `See: schema/examples/<type>.md` pointer); (d) the D-13 one-line pointer residue for §16 A/B; (e) Appendix C kept verbatim; (f) schema/AGENTS.template.md edited in parallel at offset-adjusted line ranges; (g) schema/fixtures/canonical-AGENTS.md regenerated via Plan 08-01 python3 str.replace; (h) wiki/index.md gains its first ## Decisions section.

`## Why` — MUST explicitly state adopted framing AND replaced framing (AGENTS.md §4.6 line 511 requirement). Suggested framing: **Adopted:** "spec + linked deep references" — the sole authoritative specification remains AGENTS.md, and `schema/examples/*.md` / `docs/reference/*.md` are deep extensions that the spec points to, not alternative specs. **Replaced:** "sole authoritative specification as a single monolithic file" — the prior convention forced every reader (LLM or human) to hold the full 1,785-line document in context even when they only needed the per-type shape or a commit-message reference. Progressive disclosure means Claude Code, Codex, and other agents scan the normative residue first and dereference the fully worked examples only when needed, preserving the "sole authoritative specification" claim while reducing the load-bearing context cost.

`## Alternatives Considered` — MUST list ≥1 bulleted alternative with rationale for rejection. Include at least these (from RESEARCH.md §8 and CONTEXT.md deferred ideas):
- **Bare-pointer-only residue** (no 2-3-bullet capsule on §4 sub-sections): Rejected per D-08. Per-type residue includes a capsule summary so the core spec stays self-contained for scanning readers; line-delta savings drop from ~500 to ~350, which D-02 accepts as the readability tradeoff.
- **Combine A+B into a single `docs/reference/appendices.md` grab-bag**: Rejected per D-12. Topic-per-file is the established `docs/reference/` pattern (brownfield.md, ci.md, privacy-model.md, etc.); a grab-bag file would break discoverability.
- **Reuse `docs/reference/examples.md` for the §4 worked examples**: Rejected per D-06. That file is reserved for Phase 12 DEBT scope (documenting how `examples/` works in Obsidian, .obsidianignore, safe copy-into-wiki) and Phase-07 `tests/phase-07/test_reference_stubs.sh` asserts it remains a stub.
- **Reuse `examples/kahneman/` for §4 bodies**: Rejected per D-05. Subject matter differs (§4 uses Hinton/Vaswani/deep-learning; kahneman cluster uses Kahneman-specific content); `examples/kahneman/` stays as the existing test-fixture cluster for §§11.1/11.2/12 operational illustrations.
- **Extract §9 Structured Operations and §11 Workflows in the same wave**: Deferred per `.planning/seeds/workflows-operations-to-skills.md`. Those sections require a portable runtime mirror that does not yet exist; pulling them into v1.1 risks the Phase 12 Codex agent-parity merge gate.

`## Consequences` — list outcomes. Suggested bullets (the executor should draft the actual Consequences prose using these points as source material):
- AGENTS.md shrinks from 1,785 lines to ~1,435 (actual ~350-line reduction; RESEARCH.md §2 honest arithmetic). The CONTEXT.md D-01 headline predicted ~500 lines; the ~150-line shortfall is the cost of D-08's capsule-and-frontmatter-table-inline choice and is acceptable per D-02 (no hard ceiling).
- CLAUDE.md remains byte-identical to AGENTS.md via `.githooks/pre-commit` → `bin/sync-claude.sh` auto-sync.
- Codex and other non-Claude agents can still follow AGENTS.md normatively; the `See: schema/examples/<type>.md for a concrete filled-in instance.` pointer (**singular "a concrete filled-in instance"** — matches §§11.1/11.2 precedent exactly, per cross-AI review R1 consensus) is the canonical indirection and uses the same bare-prefix shape as the pre-existing `See: examples/kahneman/<path>.md` convention — no new reader-convention introduced, no drift from established phrasing.
- Phase-08 setup-parity CI stays green because `schema/AGENTS.template.md` is edited in parallel and `schema/fixtures/canonical-AGENTS.md` is regenerated via the Plan 08-01 python3 str.replace routine in the same commit.
- Phase-07 neutrality and Phase-09 privacy gates pass cleanly by two **independent** arguments (cross-AI review R6 — do NOT conflate them): (a) **Privacy** — `schema/examples/*.md` carry `privacy: cloud_safe` explicitly and `bin/lint.sh` skips them because of `example: true` per AGENTS.md §5; the new `docs/reference/*.md` files have no YAML frontmatter at all, and `bin/check-privacy.sh` is a frontmatter-only scanner per D-14, so there is nothing for it to flag. (b) **Neutrality** — `bin/check-neutrality.sh` is a body-text scanner (not frontmatter-only); it passes on the new files because the Hinton / Vaswani / deep-learning / attention-mechanism content is pre-vetted neutral (D-07) and was already passing the denylist while inline in AGENTS.md §§4/§16. Moving that same body text to `schema/examples/*.md` and `docs/reference/*.md` does not change what the neutrality scanner sees.
- `wiki/index.md` gains its first `## Decisions` section, listing this DR plus the two pre-existing ones (`dr-2026-04-14-phase6-decision-type`, `dr-2026-04-15-kahneman-to-examples`) for forward-consistency per PATTERNS.md Note 2.
- `wiki/log.md` gains a new `## [2026-04-16] reflect | progressive disclosure extraction` entry (R11 review consensus) per AGENTS.md §9/§12 formal-operations vocabulary; the DR is the structural decision record (why), the log entry is the activity record (what/when).
- The §4 preamble (AGENTS.md line 133) was updated from "Five page types exist" to "Six page types exist" as a one-word polish reflecting the §4.6 Decision addition from Phase 6 (R8 promoted to mandatory because the Consequences bullet above would otherwise contradict a stale preamble).
- The `docs/reference/dataview-queries.md` extraction strips the outer ```markdown wrapper that §16 Appendix A uses to illustrate "this is how an Obsidian page embeds a Dataview query"; the standalone cookbook emits direct ```dataview blocks so readers can copy-paste the queries (R2 review consensus).
- §9 Structured Operations and §11 Workflows extraction remains deferred per the seed; the AGENTS.md size may be revisited in v1.2 if the progressive-disclosure fitness still feels load-bearing.

`## Affected Pages` — "None. This is an infrastructure/schema change; no pre-existing wiki content pages are restructured. The AGENTS.md spec itself is modified, but AGENTS.md is not a wiki content page (it is the canonical spec). The `affected_pages: []` list reflects this: no page under `wiki/` gains a `decision_history` back-link from this DR."

`## Sources` — "None. This is an internal schema decision; the decision record itself is the source of truth."

Trailing LF.

STEP 10 — Verify none of the 9 new files accidentally carry `privacy: local_only`. Run: `! grep -rE '^privacy:[[:space:]]*local_only' schema/examples/ docs/reference/dataview-queries.md docs/reference/commit-examples.md wiki/decisions/dr-2026-04-16-progressive-disclosure-extraction.md`. Expected: no matches.
  </action>

  <verify>
    <automated>bash tests/phase-09.1/test_schema_examples_exist.sh && bash tests/phase-09.1/test_schema_examples_frontmatter.sh && bash tests/phase-09.1/test_docs_reference_new.sh && bash tests/phase-09.1/test_decision_record.sh</automated>
  </verify>

  <acceptance_criteria>
    - All 9 new files exist: `for f in schema/examples/entity.md schema/examples/concept.md schema/examples/source-summary.md schema/examples/comparison.md schema/examples/overview.md schema/examples/decision.md docs/reference/dataview-queries.md docs/reference/commit-examples.md wiki/decisions/dr-2026-04-16-progressive-disclosure-extraction.md; do test -f "$f" || { echo MISSING:$f; exit 1; }; done`
    - Each schema/examples/*.md has `example: true`: `for f in schema/examples/*.md; do grep -q '^example: true$' "$f" || { echo MISSING_EXAMPLE_TRUE:$f; exit 1; }; done`
    - source-summary.md uses type enum `source` (singular) even though filename is hyphenated: `grep -q '^type: source$' schema/examples/source-summary.md`
    - Every schema/examples/*.md has exactly 1 `privacy: cloud_safe` line: `for f in schema/examples/*.md; do [ "$(grep -c '^privacy: cloud_safe$' "$f")" = "1" ] || { echo "WRONG_PRIVACY_COUNT: $f"; exit 1; }; done`
    - None of the new files have `privacy: local_only`: `! grep -rE '^privacy:[[:space:]]*local_only' schema/examples/ docs/reference/dataview-queries.md docs/reference/commit-examples.md wiki/decisions/dr-2026-04-16-progressive-disclosure-extraction.md`
    - docs/reference/dataview-queries.md has exactly 5 ```dataview fences: `[ "$(grep -c '^```dataview$' docs/reference/dataview-queries.md)" = "5" ]`
    - docs/reference/commit-examples.md contains all 5 representative commit samples from AGENTS.md §16 B: `for s in 'schema: define base frontmatter' 'ingest(hinton-interview)' 'query(attention-mechanisms)' 'lint(wiki): fix 3 orphan pages' 'reflect(q1-review)'; do grep -Fq "$s" docs/reference/commit-examples.md || { echo MISSING_SAMPLE:$s; exit 1; }; done`
    - Neither docs/reference new file has YAML frontmatter: `! head -1 docs/reference/dataview-queries.md | grep -q '^---$' && ! head -1 docs/reference/commit-examples.md | grep -q '^---$'`
    - DR frontmatter has required fields: `grep -q '^trigger_type: schema-update$' wiki/decisions/dr-2026-04-16-progressive-disclosure-extraction.md && grep -q '^affected_pages: \[\]$' wiki/decisions/dr-2026-04-16-progressive-disclosure-extraction.md && grep -q '^knowledge_domain: software$' wiki/decisions/dr-2026-04-16-progressive-disclosure-extraction.md`
    - DR has all 7 required sections: `for sec in '^## TL;DR' '^## Decision' '^## Why' '^## Alternatives Considered' '^## Consequences' '^## Affected Pages' '^## Sources'; do grep -qE "$sec" wiki/decisions/dr-2026-04-16-progressive-disclosure-extraction.md || { echo MISSING_SECTION:$sec; exit 1; }; done`
    - DR Why names replaced framing (contains word matching `replac|prior|previously|former|before`): `python3 -c 'import re, pathlib; t=pathlib.Path("wiki/decisions/dr-2026-04-16-progressive-disclosure-extraction.md").read_text(); m=re.search(r"^## Why\s*\n(.*?)(?=^## )", t, re.DOTALL|re.MULTILINE); import sys; sys.exit(0 if m and re.search(r"\b(replac|prior|previously|former|before)\b", m.group(1), re.I) else 1)'`
    - DR Alternatives Considered has ≥1 bulleted alternative: `python3 -c 'import re, pathlib; t=pathlib.Path("wiki/decisions/dr-2026-04-16-progressive-disclosure-extraction.md").read_text(); m=re.search(r"^## Alternatives Considered\s*\n(.*?)(?=^## )", t, re.DOTALL|re.MULTILINE); import sys; sys.exit(0 if m and len([ln for ln in m.group(1).splitlines() if re.match(r"^\s*-\s", ln)]) >= 1 else 1)'`
    - Wave-1 RED tests for file-creation portion FLIP to GREEN: `bash tests/phase-09.1/test_schema_examples_exist.sh && bash tests/phase-09.1/test_schema_examples_frontmatter.sh && bash tests/phase-09.1/test_docs_reference_new.sh && bash tests/phase-09.1/test_decision_record.sh`
  </acceptance_criteria>

  <done>9 new files created: 6 schema/examples/*.md (with example: true + cloud_safe), 2 docs/reference/*.md (no frontmatter), 1 DR (all 7 sections, Why names replaced framing, Alternatives Considered lists ≥1 item). 4 of the Plan 09.1-01 RED tests flip to GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Edit AGENTS.md + schema/AGENTS.template.md residue; update wiki/index.md; regenerate canonical fixture; sync CLAUDE.md; commit atomically</name>
  <files>AGENTS.md, schema/AGENTS.template.md, schema/fixtures/canonical-AGENTS.md, wiki/index.md, CLAUDE.md</files>

  <read_first>
    - .planning/phases/09.1-progressive-disclosure-extraction/09.1-CONTEXT.md (D-08, D-13, D-15, D-16, D-17 — extraction residue shape + canonical render + byte-equality + release allowlist)
    - .planning/phases/09.1-progressive-disclosure-extraction/09.1-RESEARCH.md §2 (file-by-file inventory with exact line ranges), §3 (verbatim canonical render routine), §5 (§4 residue template), §6 (§16 residue template), §8 (R-1 dual-file rule, R-3 pre-commit hook behavior)
    - .planning/phases/09.1-progressive-disclosure-extraction/09.1-PATTERNS.md Shared Patterns 1 (See: pointer), 2 (§4 residue), 3 (§16 residue), 4 (R-1 enforcement), 5 (canonical render verbatim), Note 1 (line 133 Five→Six polish), Note 2 (wiki/index.md Decisions section creation), Note 4 (docs/reference/index.md pointer)
    - AGENTS.md lines 131-559 (§4 — the extraction target regions)
    - AGENTS.md lines 1700-1785 (§16 — A/B/C)
    - schema/AGENTS.template.md lines 133-561 (§4 mirror, offset +2) and lines 1638-1723 (§16 mirror, offset -62)
    - .githooks/pre-commit (to understand CLAUDE.md auto-sync + re-stage behavior)
    - bin/sync-claude.sh (cp-based sync routine)
  </read_first>

  <action>
CRITICAL DISCIPLINE: This task is an atomic dual-file edit (AGENTS.md + schema/AGENTS.template.md) followed by canonical-fixture regen and wiki/index.md update and atomic commit. If pre-commit hook fires and modifies CLAUDE.md, stage CLAUDE.md and retry the commit (R-3 is EXPECTED behavior, not a failure).

STEP 0 — PREFLIGHT (R5 review consensus — ENFORCED PRECONDITION, not a documented assumption):

Before touching `AGENTS.md` or `schema/AGENTS.template.md`, run:

```bash
bash tests/phase-09.1/test_template_parity.sh
```

If this exits non-zero, ABORT the task. Do NOT proceed with the offset-based dual-file edits. The entire extraction strategy (mirror §4/§16 edits from AGENTS.md to schema/AGENTS.template.md at offset-adjusted line ranges) depends on the two files having byte-identical §4 and §16 bodies TODAY. If that parity has drifted between plan generation and execution, the dual-file edit strategy is unsafe and the phase needs replanning. Rationale per R5: the parity premise is load-bearing; promote it from "documented in RESEARCH.md §2.2" to "mechanically enforced before any edit." Expected on the current repo state: exit 0 (test created by Plan 09.1-01 as a regression canary).

If preflight PASSES, proceed to STEP 1.

STEP 1 — Edit AGENTS.md §4 residue. For each of the 6 sub-sections (§4.1 through §4.6), replace the `**Worked example:**` label line + fenced ```markdown ... ``` block with the D-08 residue shape. Keep everything ABOVE the `**Worked example:**` label line (purpose/when-to-use/section-order/required-frontmatter prose) UNCHANGED.

EXACT residue shape (6 bullets per D-08):
```
**Example:** {EXAMPLE_SUBJECT}. Demonstrates {CAPSULE_BULLET_1}; {CAPSULE_BULLET_2}; {CAPSULE_BULLET_3}.

See: schema/examples/{TYPE_SLUG}.md for a concrete filled-in instance.
```

Where `See:` starts at column 0 — NO bullet, NO bold, NO markdown link. Blank line between `**Example:**` and `See:`. Trailing blank line before the next `###` heading (matches existing section cadence).

Per-section values (executor drafts capsule prose based on what each extracted example actually demonstrates — these are suggestive starting points from RESEARCH.md §5; refine to fit the real content but keep to 2-3 semicolon-separated demonstrates clauses):

§4.1 Entity:
```
**Example:** Geoffrey Hinton. Demonstrates minimal aliases (single "Geoff Hinton"), mixed-locator Key Facts (bare sec: / sec:+direct / sec:+direct+checked_at), and Related-Pages link-on-first-mention convention.

See: schema/examples/entity.md for a concrete filled-in instance.
```

§4.2 Concept:
```
**Example:** Attention Mechanism. Demonstrates multi-source sourced claims (two sources in frontmatter), three-level locator pattern (sec:, p5, sec:self-attention), and Related-Pages cross-referencing to entity and concept siblings.

See: schema/examples/concept.md for a concrete filled-in instance.
```

§4.3 Source Summary (retain the "Additional frontmatter fields" table ABOVE the residue block per D-08 rule 4 — lines 266-284 of AGENTS.md stay untouched):
```
**Example:** "Vaswani et al. - Attention Is All You Need" (type: source; filename: source-summary.md matches the schema/templates/ sibling — readers should NOT expect schema/examples/source.md). Demonstrates full path/url/content_hash/ingested_at/source_type population, Extracted Claims with direct-quote provenance, and Source Metadata authors/published block.

See: schema/examples/source-summary.md for a concrete filled-in instance.
```

§4.4 Comparison:
```
**Example:** RNNs vs Transformers. Demonstrates Bottom Line + Comparison Table + Detailed Comparison three-tier structure, mixed-source sources list in frontmatter, and domain-spanning dimension rows.

See: schema/examples/comparison.md for a concrete filled-in instance.
```

§4.5 Overview:
```
**Example:** Deep Learning. Demonstrates synthesis across 3 sources (frontmatter + body provenance), four-claim Key Facts with domain-internal wikilinks to entity and concept siblings, and Related-Pages as navigation hub.

See: schema/examples/overview.md for a concrete filled-in instance.
```

§4.6 Decision (keep all normative prose ABOVE the residue block — Directory/File naming/ID convention/Frontmatter table/Section ordering/Epistemic pattern/decision_history paragraph all stay, per D-08 rule 4 and RESEARCH.md §2.1):
```
**Example:** Introduce Decision Record Page Type (dr-2026-04-14-phase6-decision-type). Demonstrates trigger_type: schema-update classification, empty affected_pages for an infrastructure record, and the Why-names-replaced-framing section pattern.

See: schema/examples/decision.md for a concrete filled-in instance.
```

Use `Edit` / `Read` to perform these replacements. DO NOT remove the lines BEFORE the `**Worked example:**` label in each sub-section. The `**Worked example:**` label line itself GOES AWAY entirely; the residue `**Example:**` line replaces it.

STEP 2 — MANDATORY polish (PATTERNS.md Note 1): Change AGENTS.md line 133 preamble from `Five page types exist.` to `Six page types exist.`. Mirror to `schema/AGENTS.template.md` at its parallel line (line 135, offset +2). The §4.6 Decision type was added in Phase 6 but the preamble was never updated. This is mandatory — the DR Consequences section (Task 1 STEP 9) references this change.

STEP 3 — Edit AGENTS.md §16. Replace lines 1702-1756 (Appendix A body) with the D-13 pointer shape:

```
### Appendix A: Dataview Query Examples

See `docs/reference/dataview-queries.md` for five Dataview query patterns (active entities, sources by domain, stale pages, missing privacy classification, pages in a domain).
```

Replace lines 1758-1770 (Appendix B body) with:

```
### Appendix B: Commit Message Examples

See `docs/reference/commit-examples.md` for representative commit messages per workflow type (schema/ingest/query/lint/reflect).
```

Appendix C (lines 1772-1785): DO NOT TOUCH. The `### Appendix C: Quick Reference Card` heading and its 10 numbered bullets must remain exactly as-is per D-03.

STEP 4 — Mirror all edits from STEP 1, STEP 2, STEP 3 to `schema/AGENTS.template.md` at offset-adjusted line ranges:
- §4 edits at template lines 133-561 (offset +2 from AGENTS.md)
- §16 edits at template lines 1638-1723 (offset -62 from AGENTS.md)
- Line-133 Five→Six polish: in template, this line is at 135 (offset +2)

The §4/§16 body content is currently byte-identical between AGENTS.md and schema/AGENTS.template.md (verified by RESEARCH.md §2.2 `diff` inspection). Apply the SAME textual replacements to the template at the offset-adjusted locations. After editing, verify with the template-parity test from Plan 01: `bash tests/phase-09.1/test_template_parity.sh` must PASS.

**Tip (executor discretion):** Rather than hand-editing the template at offset-adjusted line numbers, you can run `diff -u AGENTS.md schema/AGENTS.template.md` after editing AGENTS.md only, identify the exact textual deltas you just applied to AGENTS.md, then apply the same deltas to the template via `Edit` with string matching (not line numbers). The template's wizard-placeholder-bearing regions (lines 32, 56, 588-589, 848) are fully outside §4 and §16 per RESEARCH.md §1 key finding #2 — the edits will not touch placeholders.

STEP 5 — Regenerate `schema/fixtures/canonical-AGENTS.md` by running the VERBATIM Plan 08-01 python3 str.replace routine (from RESEARCH.md §3 — do not modify the script):

```bash
python3 -c '
import sys
src = open("schema/AGENTS.template.md", encoding="utf-8").read()
subs = {
  "{{AGENT_FILENAME}}":  "CLAUDE.md",
  "{{PRIMARY_DOMAIN}}":  "personal-knowledge",
  "{{DEFAULT_PRIVACY}}": "cloud_safe",
  "{{DECAY_PROFILE}}":   "default",
}
for k, v in subs.items():
  src = src.replace(k, v)
import re
leftover = re.findall(r"\{\{[A-Z_]+\}\}", src)
if leftover:
  sys.exit(f"ERROR: leftover placeholders: {leftover}")
open("schema/fixtures/canonical-AGENTS.md", "w", encoding="utf-8", newline="\n").write(src)
'
```

Run from repo root. Expected exit code: 0. Verify immediately after:
```bash
! grep -E '\{\{[A-Z_]+\}\}' schema/fixtures/canonical-AGENTS.md                        # no leftover placeholders
! grep -E '^privacy:[[:space:]]*local_only' schema/fixtures/canonical-AGENTS.md        # no privacy leak
[ "$(tail -c 1 schema/fixtures/canonical-AGENTS.md | xxd -p)" = "0a" ]                  # LF terminator
bash tests/phase-08/test_canonical_byte_equality.sh                                     # Phase-08 gate green
```

STEP 6 — Update `wiki/index.md` AND `wiki/log.md` (two wiki-layer mutations kept together for one atomic wiki-state update).

**Part A — `wiki/index.md` (PATTERNS.md Note 2):** The current index has no `## Decisions` section; Wave 1 creates it. Append the following to the current EOF (after line 23 `See AGENTS.md §2 Directory Structure for layout conventions.`):

```markdown

## Decisions

- [[dr-2026-04-14-phase6-decision-type]] — Introduce Decision Record Page Type (schema-update, 2026-04-14)
- [[dr-2026-04-15-kahneman-to-examples]] — Move Kahneman Cluster from wiki/ to examples/ (schema-update, 2026-04-15)
- [[dr-2026-04-16-progressive-disclosure-extraction]] — Extract §4 Worked Examples and §16 Appendices (schema-update, 2026-04-16)
```

(The leading blank line separates the new section from the existing paragraph. Bullets use the existing DR ids — verify both ids exist in `wiki/decisions/` before writing. `dr-2026-04-14-phase6-decision-type.md` and `dr-2026-04-15-kahneman-to-examples.md` both exist today per `ls wiki/decisions/`.)

**Part B — `wiki/log.md` (R11 review consensus — formal-operations log entry per AGENTS.md §9/§12):** Append a reflect-typed entry to `wiki/log.md` (newest-at-bottom append-only per AGENTS.md §12). Exact append:

```markdown

## [2026-04-16] reflect | progressive disclosure extraction

result: extracted §4 worked examples (6 files) to schema/examples/ and §16 appendices A, B to docs/reference/; DR dr-2026-04-16-progressive-disclosure-extraction records the framing shift.
reason: reduce spec context size while preserving "sole authoritative specification" framing via uniform `See:` pointers.
```

Rationale: AGENTS.md §9 says "all wiki mutations use a formal operations vocabulary" and §12 documents the log-entry shape. The Tier-1 inline DR captures the structural decision (why), but the `reflect` log entry captures the activity (what happened, when). Both are needed for full auditability. Entry goes at EOF (append-only — never rewrite existing log entries per §11.1 step 9 precedent).

**Acceptance gate:** After Part B, `grep -c '^## \[2026-04-16\] reflect | progressive disclosure extraction$' wiki/log.md` MUST equal 1 (exactly one new reflect entry added; idempotent if re-run).

STEP 7 — Sync CLAUDE.md. Run `bash bin/sync-claude.sh` (without `--check`) to produce `CLAUDE.md` byte-identical to the post-edit AGENTS.md. Verify: `cmp -s AGENTS.md CLAUDE.md` exits 0.

STEP 8 — (REMOVED per R12 review consensus) — the optional `docs/reference/index.md` pointer update has been dropped from the atomic-commit scope. It is harmless but it weakens the "atomic extraction only" boundary and adds one more file that future reviewers have to reason about. If the pointer addition is desired, it can land as a follow-up `docs:` commit outside Phase 09.1. Documented in `<deferred>` of CONTEXT.md as post-extraction polish.

STEP 9 — Run the full Phase-09.1 test suite locally: `bash tests/phase-09.1/run.sh`. Expected: 11/11 tests PASS (3 canaries that were GREEN pre-extraction + 8 RED tests now flipped to GREEN). Aggregator exits 0.

STEP 10 — Also run the phase-07 + phase-08 + phase-09 aggregators as regression safety: `bash tests/phase-07/run.sh && bash tests/phase-08/run.sh && bash tests/phase-09/run.sh`. All must exit 0. In particular:
- `tests/phase-07/test_reference_stubs.sh` must pass (docs/reference/examples.md still stub)
- `tests/phase-08/test_canonical_byte_equality.sh` must pass (fixture regenerated correctly)
- `bin/check-neutrality.sh` (invoked via phase-07 aggregator) must exit 0 (no new personal terms in new docs/reference files — body-text scan, independent of the frontmatter-absence argument used for privacy)
- `bin/check-privacy.sh` (invoked via phase-09 aggregator) must exit 0 (no local_only leaks; `bin/check-privacy.sh` is frontmatter-only per D-14 so frontmatter-free docs/reference files are trivially safe)

**R6 review-consensus clarification on why the new files are safe for the public-path guards:**

- **Privacy (`bin/check-privacy.sh`)**: the two new `docs/reference/*.md` files have NO YAML frontmatter (per `docs/reference/` convention — PATTERNS.md Shared Pattern 2 rule 5). `bin/check-privacy.sh` scans ONLY frontmatter (per D-14), so a file with no frontmatter is trivially safe — nothing to flag.
- **Neutrality (`bin/check-neutrality.sh`)**: independent argument. The scanner reads BODY TEXT across public paths (not frontmatter). The new docs/reference files are safe because the Hinton / Vaswani / deep-learning / attention-mechanism content they carry is already denylist-passing per the existing `.neutrality-denylist.txt` rules (the SAME content was passing the neutrality gate while inline in AGENTS.md §16; moving it to a new file does not change what the scanner sees). Do NOT conflate the two arguments — "no frontmatter means safe" is true ONLY for privacy.

STEP 11 — Commit atomically. Stage the 14 files (the original scope minus the dropped STEP 8 optional update):

```bash
git add AGENTS.md CLAUDE.md schema/AGENTS.template.md schema/fixtures/canonical-AGENTS.md \
        schema/examples/entity.md schema/examples/concept.md schema/examples/source-summary.md \
        schema/examples/comparison.md schema/examples/overview.md schema/examples/decision.md \
        docs/reference/dataview-queries.md docs/reference/commit-examples.md \
        wiki/decisions/dr-2026-04-16-progressive-disclosure-extraction.md \
        wiki/index.md wiki/log.md
# tests/phase-09.1/ is already committed by Plan 09.1-01 Wave 0; do NOT stage it here.
# docs/reference/index.md is explicitly NOT staged per R12 (optional scope dropped).
```

Create the commit with message (use HEREDOC for formatting). **R10 review consensus: use the UNSCOPED `schema:` prefix documented in AGENTS.md §3 commit-conventions table** — scoped `schema(NN):` is undocumented and risks a downstream lint-category mismatch.

```
schema: extract §4 worked examples and §16 appendices A,B to deep references (progressive disclosure)

Move AGENTS.md §4.1-4.6 worked examples to schema/examples/<type>.md (6 files)
and §16 Appendices A/B to docs/reference/{dataview-queries,commit-examples}.md
(2 files). §16 Appendix C (Quick Reference Card) and all §4 normative prose
(purpose, when-to-use, section order, frontmatter-required tables) remain
inline. Net: ~350 lines (~20%) removed from AGENTS.md.

Decision record: wiki/decisions/dr-2026-04-16-progressive-disclosure-extraction.md
Log entry: wiki/log.md reflect entry for 2026-04-16
Closes: Phase 09.1 Progressive Disclosure Extraction
```

**Expected pre-commit behavior (R-3):** If the hook fires and modifies CLAUDE.md despite Step 7 having synced it, you'll see a `Re-run commit.` message and the commit will exit 1. This is NORMAL. Run `git add CLAUDE.md && git commit -C HEAD` or just re-run the same `git commit` — the second attempt lands cleanly.

STEP 12 — After the commit succeeds, run `bash tests/phase-09.1/run.sh` one final time to confirm all Phase-09.1 tests GREEN. Then run `bash tests/phase-08/run.sh` to confirm setup-parity CI would pass remotely. Both must exit 0.
  </action>

  <verify>
    <automated>bash tests/phase-09.1/run.sh && bash tests/phase-08/run.sh && cmp -s AGENTS.md CLAUDE.md && ! grep -E '^privacy:[[:space:]]*local_only' schema/fixtures/canonical-AGENTS.md && [ "$(grep -cE '^See: schema/examples/(entity|concept|source-summary|comparison|overview|decision)\.md for a concrete filled-in instance\.$' AGENTS.md)" = "6" ] && grep -q 'docs/reference/dataview-queries\.md' AGENTS.md && grep -q 'docs/reference/commit-examples\.md' AGENTS.md && grep -q '^### Appendix C: Quick Reference Card' AGENTS.md && grep -q '^## Decisions' wiki/index.md</automated>
  </verify>

  <acceptance_criteria>
    - R5 preflight ran successfully before edits: log trace shows `bash tests/phase-09.1/test_template_parity.sh` exited 0 before any AGENTS.md / schema/AGENTS.template.md modification (documented by executor; mechanical proxy: `git log -1 --format=%B` contains no signal of aborted preflight)
    - AGENTS.md §4 residue: exactly 6 bare-prefix `See:` pointers at column 0 — `[ "$(grep -cE '^See: schema/examples/(entity|concept|source-summary|comparison|overview|decision)\.md for a concrete filled-in instance\.$' AGENTS.md)" = "6" ]`
    - AGENTS.md §4 anti-patterns absent: `! grep -qE '^\s*-\s+See: schema/examples/' AGENTS.md && ! grep -qE '\*\*See:\*\* schema/examples/' AGENTS.md && ! grep -qE 'See: \[schema/examples/' AGENTS.md`
    - Former §4 worked-example content removed: `! grep -Fq 'British-Canadian computer scientist, pioneer of deep learning' AGENTS.md && ! grep -Fq 'Seminal paper introducing the Transformer architecture based entirely on attention' AGENTS.md`
    - R8 preamble polish applied: `grep -F -q 'Six page types exist.' AGENTS.md && ! grep -F -q 'Five page types exist.' AGENTS.md`
    - AGENTS.md §16 A/B pointers present AND former content absent: `grep -q 'docs/reference/dataview-queries\.md' AGENTS.md && grep -q 'docs/reference/commit-examples\.md' AGENTS.md && ! grep -q 'TABLE summary, epistemic_status, updated_at' AGENTS.md && ! grep -q 'reflect(q1-review): restructure AI safety domain' AGENTS.md`
    - AGENTS.md §16 Appendix C preserved (R7 fixed-string grep, no brittle regex): `grep -F -q '### Appendix C: Quick Reference Card' AGENTS.md && grep -F -q '**Read `wiki/index.md` first, always.**' AGENTS.md && grep -F -q '**Operations: UPDATE, MERGE, SUPERSEDE, ARCHIVE.**' AGENTS.md`
    - Byte-equality AGENTS.md ≡ CLAUDE.md: `cmp -s AGENTS.md CLAUDE.md`
    - Template parity: `bash tests/phase-09.1/test_template_parity.sh` passes (post-extraction)
    - Canonical fixture regenerated correctly: `bash tests/phase-08/test_canonical_byte_equality.sh` passes
    - No placeholders leaked in fixture: `! grep -E '\{\{[A-Z_]+\}\}' schema/fixtures/canonical-AGENTS.md`
    - Fixture ends with LF: `[ "$(tail -c 1 schema/fixtures/canonical-AGENTS.md | xxd -p)" = "0a" ]`
    - No privacy leak in fixture: `! grep -E '^privacy:[[:space:]]*local_only' schema/fixtures/canonical-AGENTS.md`
    - R2 dataview fences: `[ "$(grep -cE '^```dataview$' docs/reference/dataview-queries.md)" = "5" ] && [ "$(grep -cE '^```markdown$' docs/reference/dataview-queries.md)" = "0" ]`
    - R2 test_dataview_fences.sh passes: `bash tests/phase-09.1/test_dataview_fences.sh`
    - R3 DR affected_pages exactness: `grep -F -q 'affected_pages: []' wiki/decisions/dr-2026-04-16-progressive-disclosure-extraction.md`
    - R11 log.md reflect entry appended (exactly once): `[ "$(grep -cF '## [2026-04-16] reflect | progressive disclosure extraction' wiki/log.md)" = "1" ]`
    - wiki/index.md has Decisions section with all 3 DRs: `grep -q '^## Decisions' wiki/index.md && grep -q 'dr-2026-04-14-phase6-decision-type' wiki/index.md && grep -q 'dr-2026-04-15-kahneman-to-examples' wiki/index.md && grep -q 'dr-2026-04-16-progressive-disclosure-extraction' wiki/index.md`
    - R12 docs/reference/index.md NOT modified in this commit: `git log -1 --name-only | grep -qv '^docs/reference/index\.md$' || true` (i.e., the pointer-list update was dropped from atomic scope)
    - AGENTS.md shrunk: `[ "$(wc -l < AGENTS.md)" -lt 1600 ]` (target ~1,435; sanity bound at 1,600 accommodates capsule-prose length variation)
    - Phase-09.1 aggregator all green (11/11): `bash tests/phase-09.1/run.sh` exits 0
    - Phase-08 aggregator all green (setup-parity CI proxy): `bash tests/phase-08/run.sh` exits 0
    - Phase-07 stub test still green: `bash tests/phase-07/test_reference_stubs.sh` exits 0
    - Neutrality guard passes (body-text scan is clean per R6 — Hinton/Vaswani/deep-learning content pre-vetted): `bash bin/check-neutrality.sh` exits 0
    - Privacy guard passes (frontmatter-only scan vacuously clean on frontmatter-free new docs files per R6): `bash bin/check-privacy.sh` exits 0
    - R10 commit subject uses unscoped schema prefix: `git log -1 --format='%s' | grep -qE '^schema: '`
    - Single atomic commit touching the 15 files in files_modified: `[ "$(git log -1 --name-only --format='' | grep -cvE '^$')" -ge 14 ]` (14-15 depending on whether wiki/log.md landed as distinct-file; both acceptable)
  </acceptance_criteria>

  <done>Atomic extraction complete. R5 preflight ran successfully before edits. AGENTS.md §4/§16 residues written; schema/AGENTS.template.md mirrored; canonical fixture regenerated; wiki/index.md has new Decisions section; wiki/log.md has R11 reflect entry; CLAUDE.md byte-equal (auto-synced). docs/reference/dataview-queries.md emits direct ```dataview fences per R2. Commit subject uses unscoped `schema:` prefix per R10. All 11 Phase-09.1 tests GREEN (3 canaries + 8 flipped from RED). All cross-phase regression gates (phase-07 stub, phase-08 canonical byte-equality, neutrality, privacy) pass. Phase 09.1 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Codex/other LLM agents ↔ AGENTS.md | Non-Claude agents read AGENTS.md as the sole authoritative specification. The `See: schema/examples/<type>.md` pointer must be comprehensible without Claude-specific indirection. Mitigation: pointer shape is plain-filesystem-path English, matches pre-existing `See: examples/kahneman/…` convention at §§11.1/11.2/12. |
| public repo ↔ release.sh | `bin/release.sh` orphan-branch publish must not leak creator-private content via new files. Mitigation: all 8 new files are Hinton/Vaswani/deep-learning neutral (pre-vetted per D-07); `bin/release.sh` ALLOWLIST already includes `schema/` and `docs/` (D-17 confirmed); `bin/check-neutrality.sh` passes on new files. |
| pre-commit hook ↔ committer | `.githooks/pre-commit` auto-syncs CLAUDE.md on drift. Silent modification could mask errors. Mitigation: R-3 documented explicitly in task action; executor instructed to expect re-stage behavior; final `cmp -s AGENTS.md CLAUDE.md` check confirms sync. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-01 | T (Tampering) | schema/examples/*.md | mitigate | All content lifted verbatim from AGENTS.md (already committed, already reviewed). `example: true` frontmatter isolates from `bin/lint.sh` (AGENTS.md §5 field definition). No new prose authored. |
| T-09.1-02 | T (Tampering) | docs/reference/dataview-queries.md, commit-examples.md | mitigate | Content lifted verbatim from AGENTS.md §16 A/B (with R2 transformation for dataview-queries.md: strip outer ```markdown wrapper so cookbook emits direct ```dataview``` snippets). **R6 split:** (a) `bin/check-privacy.sh` is a frontmatter-only scanner per D-14 → frontmatter-free docs files are trivially safe. (b) `bin/check-neutrality.sh` is a body-text scanner → safe ONLY because the Hinton/Vaswani/deep-learning content is already denylist-passing per existing `.neutrality-denylist.txt` rules (same content passed while inline in AGENTS.md §16). Do NOT claim one argument covers both guards. Dataview fence count verified by `test_dataview_fences.sh`: 5 ```dataview + 0 ```markdown wrappers. |
| T-09.1-03 | T (Tampering) | AGENTS.md §4 residue, §16 residue | mitigate | Dual-file edit (AGENTS.md + schema/AGENTS.template.md) enforced by Task 2 step 4 instruction AND by `tests/phase-09.1/test_template_parity.sh`. R-1 mitigated mechanically. |
| T-09.1-04 | I (Information disclosure) | schema/fixtures/canonical-AGENTS.md | mitigate | Regenerated via verbatim Plan 08-01 python3 str.replace; no new content introduced. Post-regen checks: no leftover placeholders, no `privacy: local_only`, LF terminator. |
| T-09.1-05 | R (Repudiation) | wiki/decisions/dr-2026-04-16-progressive-disclosure-extraction.md | mitigate | DR required per §11.4 Tier 1 + D-14. All 7 sections + trigger_type: schema-update + affected_pages: [] validated by `tests/phase-09.1/test_decision_record.sh`. Audit trail complete. |
| T-09.1-06 | D (Denial-of-service) | Pre-commit hook loop | accept | R-3: pre-commit fires once on CLAUDE.md drift. Second commit attempt lands. Not a loop. Max 2 commit attempts. |
| T-09.1-07 | I | CLAUDE.md auto-sync | accept | Byte-copy of AGENTS.md via `cp`; no content transformation. If AGENTS.md is safe, CLAUDE.md is safe. |

No HIGH severity threats. Security enforcement gate (ASVS L1, block on high) passes.
</threat_model>

<verification>
**Phase-level checks (run after Task 2 commit):**

- `bash tests/phase-09.1/run.sh` — Phase 09.1 aggregator green (11/11 tests PASS, exit 0)
- `bash tests/phase-08/run.sh` — Setup-parity CI proxy green (canonical byte-equality holds)
- `bash tests/phase-07/run.sh` — Stub assertion green
- `bash tests/phase-09/run.sh` — CI lint + privacy-leak guard green
- `cmp -s AGENTS.md CLAUDE.md` — byte-equality
- `wc -l AGENTS.md` — target ~1,435 lines (actual should be ≥1,400 and ≤1,600)
- `git log -1 --name-only` — single commit touches 14-15 files atomically; `docs/reference/index.md` NOT among them (R12)
- `bash bin/check-neutrality.sh` — body-text scan clean (R6: independent of frontmatter-absence argument)
- `bash bin/check-privacy.sh` — frontmatter-only scan clean (R6: vacuously passes on frontmatter-free docs/reference new files)
- `grep -cF '## [2026-04-16] reflect | progressive disclosure extraction' wiki/log.md` returns 1 (R11 log entry)
- `git log -1 --format='%s' | grep -qE '^schema: '` (R10 unscoped commit subject)
</verification>

<success_criteria>
- **All 15 RESEARCH.md §7 invariants hold** (I-1 through I-15) — verified by the 11 tests in `tests/phase-09.1/` plus the neutrality/privacy guards
- **All four review-consensus items addressed** (R2 dataview-fence shape, R3 affected_pages exactness, R5 preflight, R11 log entry) — verified by dedicated assertions in `<acceptance_criteria>` and by the R2 test `test_dataview_fences.sh`
- **AGENTS.md reduced** from 1,785 lines to ~1,435 lines (actual reduction ~350 lines per RESEARCH.md §2 honest arithmetic)
- **§4/§16 template parity preserved**: `schema/AGENTS.template.md` §4 and §16 bodies byte-identical to AGENTS.md (R-1 mitigated; R5 preflight enforced it as precondition)
- **Canonical fixture regenerated**: `schema/fixtures/canonical-AGENTS.md` produced by the verbatim Plan 08-01 python3 str.replace routine; Phase-08 `test_canonical_byte_equality.sh` green (R-2 from RESEARCH mitigated)
- **CLAUDE.md byte-equal**: auto-synced by pre-commit hook; `cmp -s AGENTS.md CLAUDE.md` exits 0 (R-3 from RESEARCH handled)
- **Decision record present**: `wiki/decisions/dr-2026-04-16-progressive-disclosure-extraction.md` with `trigger_type: schema-update`, `affected_pages: []` EXACTLY (R3 review consensus), 7 required sections, Why section names replaced framing, Alternatives Considered lists ≥1 alternative (R-5 from RESEARCH mitigated via `test_decision_record.sh`)
- **wiki/index.md updated**: new `## Decisions` section listing all 3 DRs (PATTERNS Note 2)
- **wiki/log.md updated** (R11 review consensus): new `## [2026-04-16] reflect | progressive disclosure extraction` entry appended at EOF per AGENTS.md §12 newest-at-bottom append-only
- **docs/reference/examples.md untouched**: Phase-07 `test_reference_stubs.sh` green (D-06/D-18/R-4 from RESEARCH)
- **docs/reference/index.md untouched** (R12 review consensus — optional pointer-list update dropped from atomic scope)
- **Pointer convention uniform**: 6 bare-prefix `See: schema/examples/<slug>.md for a concrete filled-in instance.` pointers in AGENTS.md §4 at column 0 (D-09/R-6 from RESEARCH)
- **example: true on all 6 schema/examples files**: prevents lint noise from red wikilinks (R-9 from RESEARCH)
- **R8 preamble polish applied**: AGENTS.md §4 preamble reads "Six page types exist." (not "Five")
- **All Phase-09.1 tests GREEN** (`bash tests/phase-09.1/run.sh` exit 0, 11/11)
- **Single atomic commit with unscoped `schema:` subject** (R10 review consensus) touches AGENTS.md + CLAUDE.md + schema/AGENTS.template.md + schema/fixtures/canonical-AGENTS.md + 6 schema/examples/*.md + 2 docs/reference/*.md + 1 DR + wiki/index.md + wiki/log.md. docs/reference/index.md explicitly NOT touched. tests/phase-09.1/* is NOT touched by this commit — it was committed by Plan 09.1-01 Wave 0.
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-progressive-disclosure-extraction/09.1-02-SUMMARY.md`
</output>
