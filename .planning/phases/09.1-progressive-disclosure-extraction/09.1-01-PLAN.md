---
phase: 09.1
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/phase-09.1/run.sh
  - tests/phase-09.1/lib.sh
  - tests/phase-09.1/test_byte_equality.sh
  - tests/phase-09.1/test_phase_07_stub_untouched.sh
  - tests/phase-09.1/test_schema_examples_exist.sh
  - tests/phase-09.1/test_schema_examples_frontmatter.sh
  - tests/phase-09.1/test_agents_section_4_residue.sh
  - tests/phase-09.1/test_agents_section_16.sh
  - tests/phase-09.1/test_docs_reference_new.sh
  - tests/phase-09.1/test_dataview_fences.sh
  - tests/phase-09.1/test_decision_record.sh
  - tests/phase-09.1/test_wiki_index_decisions.sh
  - tests/phase-09.1/test_template_parity.sh
autonomous: true
requirements: []

must_haves:
  truths:
    - "tests/phase-09.1/run.sh exists and iterates test_*.sh files with a pass/fail tally"
    - "tests/phase-09.1/lib.sh exports REPO_ROOT and assert_exit_code helpers"
    - "All 11 test_*.sh files are executable; 8 fail RED initially (target files not yet extracted), 3 pass GREEN (regression canaries)"
    - "test_template_parity.sh asserts §4/§16 body equality between AGENTS.md and schema/AGENTS.template.md — passes TODAY on pre-extraction state"
    - "test_byte_equality.sh runs cmp -s AGENTS.md CLAUDE.md and re-invokes the Phase-08 canonical byte-equality test (the pre-commit hook already enforces bin/sync-claude.sh --check at commit time per D-16, so the test harness does not duplicate that check)"
    - "test_phase_07_stub_untouched.sh re-invokes tests/phase-07/test_reference_stubs.sh as a wrapper"
    - "test_dataview_fences.sh asserts docs/reference/dataview-queries.md uses DIRECT ```dataview fences (not outer ```markdown wrappers) — per R2 review consensus (RED until Wave 1)"
    - "test_agents_section_4_residue.sh includes ratchet grep asserting AGENTS.md §4 preamble reads 'Six page types exist.' (not 'Five') — R8 review consensus"
    - "test_decision_record.sh asserts affected_pages: [] EXACTLY (no None fallback) — R3 review consensus"
    - "test_agents_section_16.sh uses grep -F fixed-string matches for Appendix C literal text (no regex metachars) — R7 review consensus"
    - "Aggregator exits non-zero when any test fails (enabling RED gate for Wave 1 GREEN transition)"
  artifacts:
    - path: "tests/phase-09.1/run.sh"
      provides: "Test aggregator mirroring tests/phase-09/run.sh (pass/fail tally, failed-tests list, phase label)"
    - path: "tests/phase-09.1/lib.sh"
      provides: "Minimal shared helpers: REPO_ROOT resolution + assert_exit_code"
    - path: "tests/phase-09.1/test_byte_equality.sh"
      provides: "Invariants I-1 (AGENTS.md ≡ CLAUDE.md via cmp -s) and I-2 (canonical fixture byte-equality via Phase-08 wrapper). Script performs EXACTLY those two checks — see R4 consensus fix."
    - path: "tests/phase-09.1/test_phase_07_stub_untouched.sh"
      provides: "Invariant I-3 (docs/reference/examples.md stub assertion preserved via wrapper)"
    - path: "tests/phase-09.1/test_schema_examples_exist.sh"
      provides: "Invariant I-4 (6 files under schema/examples/)"
    - path: "tests/phase-09.1/test_schema_examples_frontmatter.sh"
      provides: "Invariant I-5 (YAML validity, example: true, privacy: cloud_safe, type enum matches filename)"
    - path: "tests/phase-09.1/test_agents_section_4_residue.sh"
      provides: "Invariant I-6 (6 bare-prefix `See: schema/examples/<type>.md` pointers; former §4 body content absent; AGENTS.md §4 preamble reads 'Six page types exist.' per R8)"
    - path: "tests/phase-09.1/test_agents_section_16.sh"
      provides: "Invariants I-7, I-8, I-9 (§16 A/B pointers present; former content absent; Appendix C verbatim via grep -F fixed-string match per R7)"
    - path: "tests/phase-09.1/test_docs_reference_new.sh"
      provides: "Invariants I-10 (5 dataview code blocks) and I-11 (commit-examples.md body)"
    - path: "tests/phase-09.1/test_dataview_fences.sh"
      provides: "R2 review-consensus enforcement: docs/reference/dataview-queries.md uses DIRECT ```dataview fences with zero outer ```markdown wrappers (appropriate for standalone cookbook, NOT the AGENTS.md §16 inline-illustration shape)"
    - path: "tests/phase-09.1/test_decision_record.sh"
      provides: "Invariant I-12 (DR file, trigger_type: schema-update, affected_pages: [] EXACTLY per R3, all 7 sections)"
    - path: "tests/phase-09.1/test_wiki_index_decisions.sh"
      provides: "Invariant I-13 (wiki/index.md has `## Decisions` heading + DR bullet)"
    - path: "tests/phase-09.1/test_template_parity.sh"
      provides: "R-1 mitigation: AGENTS.md §4/§16 bodies byte-identical to schema/AGENTS.template.md at offset-adjusted line ranges"
  key_links:
    - from: "tests/phase-09.1/run.sh"
      to: "tests/phase-09.1/test_*.sh"
      via: "shopt -s nullglob + for loop iteration"
      pattern: "for t in .SCRIPT_DIR./test_\\*\\.sh"
    - from: "tests/phase-09.1/test_*.sh"
      to: "tests/phase-09.1/lib.sh"
      via: "source $SCRIPT_DIR/lib.sh"
      pattern: "source.*SCRIPT_DIR.*lib\\.sh"
    - from: "tests/phase-09.1/test_phase_07_stub_untouched.sh"
      to: "tests/phase-07/test_reference_stubs.sh"
      via: "bash wrapper invocation"
      pattern: "bash.*tests/phase-07/test_reference_stubs\\.sh"
    - from: "tests/phase-09.1/test_byte_equality.sh"
      to: "tests/phase-08/test_canonical_byte_equality.sh"
      via: "bash wrapper invocation"
      pattern: "bash.*tests/phase-08/test_canonical_byte_equality\\.sh"
    - from: "tests/phase-09.1/test_dataview_fences.sh"
      to: "docs/reference/dataview-queries.md"
      via: "grep -c '^```dataview' and grep -c '^```markdown' assertions"
      pattern: "docs/reference/dataview-queries\\.md"
---

<objective>
Scaffold the Phase 09.1 test harness + 11 test files (8 RED + 3 GREEN canaries) asserting the 15 structural invariants defined in RESEARCH.md §7 and VALIDATION.md plus the R2 dataview-fence-shape invariant from the cross-AI review consensus. Every RED test should PASS on its Wave-1 GREEN target state but FAIL today — EXCEPT `test_template_parity.sh`, `test_phase_07_stub_untouched.sh`, and `test_byte_equality.sh`, which assert CURRENT-state invariants (pre-extraction parity, stub preservation, byte-equality) and MUST pass today so we detect regressions introduced during Wave 1.

Purpose: Establish a mechanical gate so the Wave-1 atomic extraction either passes all 11 tests or the commit is abandoned. No interpretation, no drift, no silent scope shift. The test file count grew from 10 to 11 when the cross-AI review (R2) promoted the dataview-fence-shape assertion from inline acceptance criterion to a first-class test, so standalone-cookbook readers get direct ```dataview``` snippets rather than nested-markdown-wrapped snippets.

Output: `tests/phase-09.1/` directory with aggregator, lib, and 11 test files (13 files total including lib.sh and run.sh). After commit, `bash tests/phase-09.1/run.sh` exits non-zero (8 tests RED, 3 canaries GREEN — this is expected until Plan 09.1-02 runs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.1-progressive-disclosure-extraction/09.1-CONTEXT.md
@.planning/phases/09.1-progressive-disclosure-extraction/09.1-RESEARCH.md
@.planning/phases/09.1-progressive-disclosure-extraction/09.1-PATTERNS.md
@.planning/phases/09.1-progressive-disclosure-extraction/09.1-VALIDATION.md
@tests/phase-09/run.sh
@tests/phase-09/lib.sh
@tests/phase-09/test_agents_section_11_1.sh
@tests/phase-09/test_agents_section_12.sh
@tests/phase-09/test_ci_docs.sh
@tests/phase-08/test_canonical_byte_equality.sh
@tests/phase-07/test_reference_stubs.sh

<interfaces>
<!-- Key shell-scaffolding patterns extracted from analogs. Executor uses these directly — no exploration needed. -->

From tests/phase-09/run.sh (aggregator skeleton):
- Uses set -euo pipefail, shopt -s nullglob
- SCRIPT_DIR resolved via cd+dirname+BASH_SOURCE pattern
- Iterates $SCRIPT_DIR/test_*.sh, tallies PASS/FAIL/TOTAL, appends failing names to FAILED_TESTS array
- Emits "PHASE 09 TESTS: ${PASS}/${TOTAL}" at end, "Failed: ${FAILED_TESTS[*]}" on stderr when FAIL>0
- Exits 1 on any failure, 0 otherwise

From tests/phase-09/lib.sh (minimal helper subset Phase 09.1 needs):
- REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
- assert_exit_code expected actual desc → emits "FAIL: $desc (expected exit=$expected, got $actual)" on mismatch
- export -f assert_exit_code

From tests/phase-09/test_agents_section_12.sh (awk-section-scoped grep + CLAUDE.md sync check):
- A="$REPO_ROOT/AGENTS.md"
- (cd "$REPO_ROOT" && bash bin/sync-claude.sh --check) for byte-equality verification

From tests/phase-09/test_agents_section_11_1.sh (line-anchored grep pattern with unique strings)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create aggregator + lib + 2 regression-wrapper tests (currently GREEN)</name>
  <files>tests/phase-09.1/run.sh, tests/phase-09.1/lib.sh, tests/phase-09.1/test_byte_equality.sh, tests/phase-09.1/test_phase_07_stub_untouched.sh, tests/phase-09.1/test_template_parity.sh</files>

  <read_first>
    - .planning/phases/09.1-progressive-disclosure-extraction/09.1-CONTEXT.md (D-16, D-18 — byte-equality + stub assertion preservation)
    - .planning/phases/09.1-progressive-disclosure-extraction/09.1-RESEARCH.md §2.2 (line-range offsets for template parity: §4 AGENTS 131-559 ↔ template 133-561; §16 AGENTS 1700-1785 ↔ template 1638-1723)
    - .planning/phases/09.1-progressive-disclosure-extraction/09.1-PATTERNS.md Shared Pattern 4 (R-1 template parity)
    - tests/phase-09/run.sh (exact aggregator shape — copy verbatim with s/09/09.1/ rename)
    - tests/phase-09/lib.sh (minimal helper subset)
    - tests/phase-08/test_canonical_byte_equality.sh (for test_byte_equality.sh wrapper call)
    - tests/phase-07/test_reference_stubs.sh (for test_phase_07_stub_untouched.sh wrapper call)
  </read_first>

  <action>
STEP 1 — Create `tests/phase-09.1/run.sh` with mode 0755. Copy the aggregator verbatim from PATTERNS.md "tests/phase-09.1/run.sh" section (lines 216-264), which in turn mirrors tests/phase-09/run.sh with `PHASE 09 TESTS` → `PHASE 09.1 TESTS`. Exact contents:

```bash
#!/usr/bin/env bash
# tests/phase-09.1/run.sh -- Phase 09.1 test aggregator (progressive-disclosure extraction).
# Iterates tests/phase-09.1/test_*.sh, tallies pass/fail, exits non-zero on any failure.
set -euo pipefail

FULL=0
while [ "$#" -gt 0 ]; do
    case "$1" in
        --full) FULL=1; shift ;;
        --help|-h)
            cat <<'EOF'
Usage: tests/phase-09.1/run.sh [--full]
  --full   Reserved for future full-suite runs (no-op).
EOF
            exit 0
            ;;
        *) echo "ERROR: unknown option: $1" >&2; exit 1 ;;
    esac
done

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PASS=0
FAIL=0
TOTAL=0
FAILED_TESTS=()

shopt -s nullglob
for t in "$SCRIPT_DIR"/test_*.sh; do
    TOTAL=$((TOTAL + 1))
    name="$(basename "$t")"
    echo "--- Running $name ---"
    if bash "$t"; then
        PASS=$((PASS + 1))
        echo "--- PASS $name ---"
    else
        FAIL=$((FAIL + 1))
        FAILED_TESTS+=("$name")
        echo "--- FAIL $name ---"
    fi
done

echo ""
echo "PHASE 09.1 TESTS: ${PASS}/${TOTAL}"
if [ "$FAIL" -gt 0 ]; then
    echo "Failed: ${FAILED_TESTS[*]}" >&2
    exit 1
fi
exit 0
```

STEP 2 — Create `tests/phase-09.1/lib.sh` with mode 0644. Exact contents (minimal subset per PATTERNS.md lib.sh section):

```bash
#!/usr/bin/env bash
# tests/phase-09.1/lib.sh -- Phase 09.1 shared test helpers.
# Source this from tests/phase-09.1/test_*.sh:
#   SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
#   source "$SCRIPT_DIR/lib.sh"

REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"

# assert_exit_code <expected> <actual> <description>
assert_exit_code() {
    local expected="$1" actual="$2" desc="$3"
    if [ "$expected" != "$actual" ]; then
        echo "FAIL: $desc (expected exit=$expected, got $actual)" >&2
        return 1
    fi
}

export -f assert_exit_code
```

STEP 3 — Create `tests/phase-09.1/test_byte_equality.sh` with mode 0755. Invariants I-1 + I-2. This test CURRENTLY PASSES (regression canary). Per R4 review consensus, the script performs EXACTLY two checks: (a) `cmp -s AGENTS.md CLAUDE.md` for byte-equality, and (b) invokes the Phase-08 `test_canonical_byte_equality.sh` wrapper. It does NOT call `bin/sync-claude.sh --check` — the pre-commit hook already enforces that at commit time (D-16), and duplicating it in the test harness adds noise without safety benefit.

```bash
#!/usr/bin/env bash
# I-1 + I-2: AGENTS.md ≡ CLAUDE.md (byte-equality); schema/fixtures/canonical-AGENTS.md matches
# the python3 str.replace render of schema/AGENTS.template.md (Phase-08 canonical fixture gate).
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/lib.sh"

# I-1: AGENTS.md ≡ CLAUDE.md
cmp -s "$REPO_ROOT/AGENTS.md" "$REPO_ROOT/CLAUDE.md" \
    || { echo "FAIL: AGENTS.md and CLAUDE.md are not byte-equal (run bin/sync-claude.sh)" >&2; exit 1; }

# I-2: canonical-AGENTS.md matches Phase-08 byte-equality test
(cd "$REPO_ROOT" && bash tests/phase-08/test_canonical_byte_equality.sh) \
    || { echo "FAIL: Phase-08 canonical byte-equality test failed (schema/AGENTS.template.md drift)" >&2; exit 1; }

echo "PASS: byte-equality (AGENTS.md ≡ CLAUDE.md; canonical fixture matches template render)"
```

STEP 4 — Create `tests/phase-09.1/test_phase_07_stub_untouched.sh` with mode 0755. Invariant I-3. This test CURRENTLY PASSES (regression canary):

```bash
#!/usr/bin/env bash
# I-3: docs/reference/examples.md stays a stub. Per D-06, Phase 09.1 must NOT touch that file.
# Wrapper re-invokes Phase-07's tests/phase-07/test_reference_stubs.sh as defense-in-depth.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/lib.sh"

(cd "$REPO_ROOT" && bash tests/phase-07/test_reference_stubs.sh) \
    || { echo "FAIL: tests/phase-07/test_reference_stubs.sh failed — Phase 09.1 must not modify docs/reference/examples.md (D-06, D-18)" >&2; exit 1; }

echo "PASS: docs/reference/examples.md stub preserved per D-06/D-18"
```

STEP 5 — Create `tests/phase-09.1/test_template_parity.sh` with mode 0755. R-1 mitigation: §4 and §16 bodies byte-identical between AGENTS.md and schema/AGENTS.template.md. This test CURRENTLY PASSES (canary — must STAY passing after Wave 1):

```bash
#!/usr/bin/env bash
# R-1 mitigation: AGENTS.md §4 and §16 bodies byte-identical to schema/AGENTS.template.md
# at offset-adjusted line ranges (RESEARCH.md §2.2).
#
# PRE-EXTRACTION (today):   AGENTS.md §4 = lines 131-559; template §4 = lines 133-561.
#                           AGENTS.md §16 = lines 1700-1785; template §16 = lines 1638-1723.
# POST-EXTRACTION (Wave 1): End line-numbers shift because the worked-example fenced blocks
#                           shrink to residue. The two files must STAY byte-identical at
#                           whatever their new line ranges are.
#
# Strategy: Lock the START of each region and use a sentinel END pattern, extracting via
# awk from "## 4." through "## 5." (exclusive) for §4, and from "## 16." through EOF
# for §16. Compare the extracted blocks with diff.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/lib.sh"

A="$REPO_ROOT/AGENTS.md"
T="$REPO_ROOT/schema/AGENTS.template.md"

# Extract §4 body: from line matching '^## 4\. Page Types and Templates' through line
# before '^## 5\. Frontmatter Schema'. Use awk flag-based extraction (PATTERNS.md Note:
# flag-based awk replaces range-pair because range pair collapses to 1 line when start
# regex is a subset of end regex).
extract_section_4() {
    awk '
        /^## 5\. Frontmatter Schema/ { inside=0 }
        inside==1 { print }
        /^## 4\. Page Types and Templates/ { inside=1 }
    ' "$1"
}

# Extract §16 body: from '^## 16\. Appendices and Examples' through EOF.
extract_section_16() {
    awk '
        /^## 16\. Appendices and Examples/ { inside=1 }
        inside==1 { print }
    ' "$1"
}

diff <(extract_section_4 "$A") <(extract_section_4 "$T") > /tmp/phase-09.1-s4.diff \
    || { echo "FAIL: §4 body drift between AGENTS.md and schema/AGENTS.template.md" >&2; cat /tmp/phase-09.1-s4.diff >&2; exit 1; }

diff <(extract_section_16 "$A") <(extract_section_16 "$T") > /tmp/phase-09.1-s16.diff \
    || { echo "FAIL: §16 body drift between AGENTS.md and schema/AGENTS.template.md" >&2; cat /tmp/phase-09.1-s16.diff >&2; exit 1; }

echo "PASS: AGENTS.md §4 and §16 bodies byte-identical to schema/AGENTS.template.md"
```

STEP 6 — `chmod +x` on all four test files and run.sh. Verify with `ls -la tests/phase-09.1/`. Then run `bash tests/phase-09.1/run.sh`; expect 3/3 PASS right now (I-1 + I-2 via test_byte_equality.sh, I-3 via test_phase_07_stub_untouched.sh, R-1 parity via test_template_parity.sh all hold today — all three files listed are regression canaries, not RED tests; note: lib.sh and run.sh are excluded by the test_*.sh glob in the aggregator).
  </action>

  <verify>
    <automated>test -x tests/phase-09.1/run.sh && test -f tests/phase-09.1/lib.sh && bash tests/phase-09.1/test_byte_equality.sh && bash tests/phase-09.1/test_phase_07_stub_untouched.sh && bash tests/phase-09.1/test_template_parity.sh</automated>
  </verify>

  <acceptance_criteria>
    - `tests/phase-09.1/run.sh` is executable, contains literal string `PHASE 09.1 TESTS`, uses `shopt -s nullglob`, loops `$SCRIPT_DIR/test_*.sh` — verify: `grep -q 'PHASE 09\.1 TESTS' tests/phase-09.1/run.sh && grep -q 'shopt -s nullglob' tests/phase-09.1/run.sh`
    - `tests/phase-09.1/lib.sh` exports `assert_exit_code` and defines `REPO_ROOT` — verify: `grep -q 'export -f assert_exit_code' tests/phase-09.1/lib.sh && grep -q 'REPO_ROOT=' tests/phase-09.1/lib.sh`
    - All 3 test files source lib.sh — verify: `for f in tests/phase-09.1/test_byte_equality.sh tests/phase-09.1/test_phase_07_stub_untouched.sh tests/phase-09.1/test_template_parity.sh; do grep -q 'source.*lib\.sh' "$f" || exit 1; done`
    - test_byte_equality.sh calls `cmp -s` on AGENTS.md/CLAUDE.md and wraps `tests/phase-08/test_canonical_byte_equality.sh` — verify: `grep -q 'cmp -s.*AGENTS.md.*CLAUDE.md' tests/phase-09.1/test_byte_equality.sh && grep -q 'test_canonical_byte_equality\.sh' tests/phase-09.1/test_byte_equality.sh`
    - test_phase_07_stub_untouched.sh wraps `tests/phase-07/test_reference_stubs.sh` — verify: `grep -q 'test_reference_stubs\.sh' tests/phase-09.1/test_phase_07_stub_untouched.sh`
    - test_template_parity.sh uses flag-based awk extraction for §4 and §16, asserts diff is empty — verify: `grep -q 'extract_section_4' tests/phase-09.1/test_template_parity.sh && grep -q 'extract_section_16' tests/phase-09.1/test_template_parity.sh`
    - All 4 tests PASS on current (pre-extraction) repo state — verify: `bash tests/phase-09.1/test_byte_equality.sh && bash tests/phase-09.1/test_phase_07_stub_untouched.sh && bash tests/phase-09.1/test_template_parity.sh` (exit 0 for all three)
  </acceptance_criteria>

  <done>tests/phase-09.1/ contains run.sh + lib.sh + 3 canary test files, all executable, all passing on current repo state. Regression gate armed for Wave 1.</done>
</task>

<task type="auto">
  <name>Task 2: Create 8 RED test files asserting Wave-1 extraction invariants (I-4..I-13 + R2 dataview-fence shape)</name>
  <files>tests/phase-09.1/test_schema_examples_exist.sh, tests/phase-09.1/test_schema_examples_frontmatter.sh, tests/phase-09.1/test_agents_section_4_residue.sh, tests/phase-09.1/test_agents_section_16.sh, tests/phase-09.1/test_docs_reference_new.sh, tests/phase-09.1/test_dataview_fences.sh, tests/phase-09.1/test_decision_record.sh, tests/phase-09.1/test_wiki_index_decisions.sh</files>

  <read_first>
    - .planning/phases/09.1-progressive-disclosure-extraction/09.1-RESEARCH.md §7 (invariants I-4..I-13 with verification commands lines 406-471)
    - .planning/phases/09.1-progressive-disclosure-extraction/09.1-PATTERNS.md Shared Pattern 1 (bare-prefix See: convention), Shared Pattern 3 (§16 residue shape), and test file analog sections
    - tests/phase-09/test_agents_section_11_1.sh (awk-section-scoped grep analog)
    - tests/phase-09/test_agents_section_12.sh (CLAUDE.md sync check analog)
    - tests/phase-09/test_ci_docs.sh (python3 + file-exists analog)
    - wiki/decisions/dr-2026-04-14-phase6-decision-type.md (section heading analog for test_decision_record.sh)
  </read_first>

  <action>
All 7 tests below should FAIL today (RED) and PASS after Plan 09.1-02 Wave-1 commit (GREEN). Create each with mode 0755.

STEP 1 — Create `tests/phase-09.1/test_schema_examples_exist.sh` (I-4):

```bash
#!/usr/bin/env bash
# I-4: schema/examples/ contains exactly 6 files: entity.md, concept.md, source-summary.md,
# comparison.md, overview.md, decision.md.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/lib.sh"

D="$REPO_ROOT/schema/examples"
test -d "$D" || { echo "FAIL: $D missing" >&2; exit 1; }

for t in entity concept source-summary comparison overview decision; do
    test -f "$D/$t.md" || { echo "FAIL: $D/$t.md missing" >&2; exit 1; }
done

count="$(find "$D" -maxdepth 1 -type f -name '*.md' | wc -l)"
# Accept >=6; if planner opts in to schema/examples/README.md that is fine (PATTERNS Note 3).
[ "$count" -ge 6 ] || { echo "FAIL: expected >=6 .md files in $D, found $count" >&2; exit 1; }

echo "PASS: schema/examples/ has all 6 page-type examples"
```

STEP 2 — Create `tests/phase-09.1/test_schema_examples_frontmatter.sh` (I-5). Use the verbatim python3 heredoc from PATTERNS.md test_schema_examples_frontmatter.sh section (PATTERNS.md lines 395-449):

```bash
#!/usr/bin/env bash
# I-5: Each schema/examples/<type>.md has valid YAML frontmatter with example: true,
# privacy: cloud_safe, and matching type: enum.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/lib.sh"

cd "$REPO_ROOT"

python3 - <<'PYEOF'
import pathlib, sys
try:
    import yaml
except ImportError:
    sys.exit("FAIL: python3-yaml missing; tests/phase-08 pins this dependency")

EXPECTED_TYPES = {
    "entity.md": "entity",
    "concept.md": "concept",
    "source-summary.md": "source",   # filename uses hyphen, type enum is `source` (RESEARCH §2.3)
    "comparison.md": "comparison",
    "overview.md": "overview",
    "decision.md": "decision",
}

root = pathlib.Path("schema/examples")
if not root.is_dir():
    sys.exit("FAIL: schema/examples/ directory missing")

errors = []
for name, expected_type in EXPECTED_TYPES.items():
    p = root / name
    if not p.is_file():
        errors.append(f"{p}: missing"); continue
    parts = p.read_text().split("---", 2)
    if len(parts) < 3:
        errors.append(f"{p}: malformed frontmatter"); continue
    try:
        fm = yaml.safe_load(parts[1])
    except yaml.YAMLError as e:
        errors.append(f"{p}: YAML parse error -- {e}"); continue
    if fm.get("type") != expected_type:
        errors.append(f"{p}: type={fm.get('type')!r}, expected {expected_type!r}")
    if fm.get("example") is not True:
        errors.append(f"{p}: example != True (got {fm.get('example')!r})")
    if fm.get("privacy") != "cloud_safe":
        errors.append(f"{p}: privacy != cloud_safe (got {fm.get('privacy')!r})")

if errors:
    for e in errors: print("FAIL:", e, file=sys.stderr)
    sys.exit(1)
print("PASS: schema/examples/ frontmatter valid")
PYEOF
```

STEP 3 — Create `tests/phase-09.1/test_agents_section_4_residue.sh` (I-6). Use the verbatim shape from PATTERNS.md (lines 320-354):

```bash
#!/usr/bin/env bash
# I-6: AGENTS.md §4.1 through §4.6 each have exactly ONE `See: schema/examples/<type>.md` line
# in the D-08 residue shape. Also: the former worked-example fenced blocks are GONE.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/lib.sh"

A="$REPO_ROOT/AGENTS.md"

# D-09 pointer convention: bare `See: schema/examples/<slug>.md for a concrete filled-in instance.`
# Matches the AGENTS.md §§11.1 (line 880), 11.2 (line 1323), 12 (line 1553) precedent.
for t in entity concept source-summary comparison overview decision; do
    grep -q "^See: schema/examples/$t\.md for a concrete filled-in instance\.$" "$A" \
        || { echo "FAIL: §4 missing bare pointer 'See: schema/examples/$t.md for a concrete filled-in instance.'" >&2; exit 1; }
done

# Exactly 6 such pointers — not 5, not 7
count="$(grep -cE '^See: schema/examples/(entity|concept|source-summary|comparison|overview|decision)\.md for a concrete filled-in instance\.$' "$A")"
[ "$count" -eq 6 ] || { echo "FAIL: expected 6 §4 pointers, found $count" >&2; exit 1; }

# Anti-patterns per R-6: reject markdown-link / bold / list-bullet forms
! grep -qE '^\s*-?\s*\*\*See:\*\*' "$A" || { echo "FAIL: found **See:** (bold) — D-09 violation" >&2; exit 1; }
! grep -qE 'See: \[schema/examples/' "$A" || { echo "FAIL: found markdown-link See pointer — D-09 violation" >&2; exit 1; }

# Former §4 worked-example content ABSENT. Unique verbatim strings from each current block.
for old in \
    "British-Canadian computer scientist, pioneer of deep learning" \
    "A neural network component that allows models to focus" \
    "Seminal paper introducing the Transformer architecture based entirely on attention" \
    "Comparison of recurrent neural networks and Transformer architectures" \
    "High-level overview of deep learning: history, key architectures"
do
    if grep -Fq "$old" "$A"; then
        echo "FAIL: AGENTS.md still contains former §4 worked-example content: '$old'" >&2
        exit 1
    fi
done

# R8 review consensus: ratchet the §4 preamble from "Five page types exist." (pre-Phase-6 drift)
# to "Six page types exist." The DR Consequences section references this polish; the change is
# mandatory (not optional) because the DR prose would contradict a stale "Five" preamble.
grep -F -q 'Six page types exist.' "$A" \
    || { echo "FAIL: §4 preamble must read 'Six page types exist.' (polish promoted to mandatory per R8/DR Consequences); found outdated 'Five page types exist.' or missing" >&2; exit 1; }
! grep -F -q 'Five page types exist.' "$A" \
    || { echo "FAIL: §4 preamble still has outdated 'Five page types exist.' — R8 polish not applied" >&2; exit 1; }

echo "PASS: AGENTS.md §4 residue correctly extracted"
```

STEP 4 — Create `tests/phase-09.1/test_agents_section_16.sh` (I-7 + I-8 + I-9):

```bash
#!/usr/bin/env bash
# I-7: AGENTS.md §16 Appendix A has pointer to docs/reference/dataview-queries.md AND
#      the former A content (TABLE summary... Dataview block) is absent.
# I-8: AGENTS.md §16 Appendix B has pointer to docs/reference/commit-examples.md AND
#      the former B content (reflect(q1-review) commit example) is absent.
# I-9: AGENTS.md §16 Appendix C retains all 10 numbered QRC rules verbatim.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/lib.sh"

A="$REPO_ROOT/AGENTS.md"

# I-7: Appendix A pointer present, former content absent
grep -q 'docs/reference/dataview-queries\.md' "$A" \
    || { echo "FAIL: §16 missing docs/reference/dataview-queries.md pointer" >&2; exit 1; }
! grep -q 'TABLE summary, epistemic_status, updated_at' "$A" \
    || { echo "FAIL: §16 still contains former Appendix A Dataview block" >&2; exit 1; }

# I-8: Appendix B pointer present, former content absent
grep -q 'docs/reference/commit-examples\.md' "$A" \
    || { echo "FAIL: §16 missing docs/reference/commit-examples.md pointer" >&2; exit 1; }
! grep -q 'reflect(q1-review): restructure AI safety domain' "$A" \
    || { echo "FAIL: §16 still contains former Appendix B commit example" >&2; exit 1; }

# I-9: Appendix C preserved verbatim. R7 review consensus: use grep -F fixed-string matches
# (no regex metachars) to pin the EXACT literal text from AGENTS.md §16. Brittle regex-based
# greps (e.g. 'Read .wiki/index\.md. first, always') risked false positives/negatives because
# the literal text includes backticks and asterisks that regex treats as metachars.
grep -F -q '**Read `wiki/index.md` first, always.**' "$A" \
    || { echo "FAIL: §16 Appendix C rule 1 ('Read wiki/index.md first, always.') missing or modified" >&2; exit 1; }
grep -F -q '**Operations: UPDATE, MERGE, SUPERSEDE, ARCHIVE.**' "$A" \
    || { echo "FAIL: §16 Appendix C rule 9 ('Operations: UPDATE, MERGE, SUPERSEDE, ARCHIVE.') missing or modified" >&2; exit 1; }
grep -F -q 'See Section 3 "What Agents Must NOT Do"' "$A" \
    || { echo "FAIL: §16 Appendix C rule 10 ('See Section 3 What Agents Must NOT Do') missing or modified" >&2; exit 1; }

# Appendix C heading itself — line-anchored, no regex metachars in target literal
grep -F -q '### Appendix C: Quick Reference Card' "$A" \
    || { echo "FAIL: §16 Appendix C heading missing" >&2; exit 1; }

# CLAUDE.md sync check (defense in depth; pre-commit hook auto-syncs but verify local state)
(cd "$REPO_ROOT" && bash bin/sync-claude.sh --check) \
    || { echo "FAIL: CLAUDE.md drifted from AGENTS.md after §16 edit" >&2; exit 1; }

echo "PASS: AGENTS.md §16 A/B extracted; C preserved; CLAUDE.md synced"
```

STEP 5 — Create `tests/phase-09.1/test_docs_reference_new.sh` (I-10 + I-11):

```bash
#!/usr/bin/env bash
# I-10: docs/reference/dataview-queries.md exists with 5 ```dataview code blocks.
# I-11: docs/reference/commit-examples.md exists with a code block containing >=9 commit lines.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/lib.sh"

DQ="$REPO_ROOT/docs/reference/dataview-queries.md"
CE="$REPO_ROOT/docs/reference/commit-examples.md"

# I-10
test -f "$DQ" || { echo "FAIL: $DQ missing" >&2; exit 1; }
dataview_count="$(grep -cE '^```dataview$' "$DQ")"
[ "$dataview_count" -eq 5 ] || { echo "FAIL: expected 5 '```dataview' blocks in dataview-queries.md, found $dataview_count" >&2; exit 1; }

# I-11
test -f "$CE" || { echo "FAIL: $CE missing" >&2; exit 1; }
# The commit-examples body has 1 fenced block containing >=9 commit-message lines.
# Representative commits from current AGENTS.md Appendix B (lines 1762-1770).
for sample in \
    'schema: define base frontmatter fields and page type conventions' \
    'ingest(hinton-interview): add source summary' \
    'query(attention-mechanisms): synthesize comparison' \
    'lint(wiki): fix 3 orphan pages' \
    'reflect(q1-review): restructure AI safety domain'
do
    grep -Fq "$sample" "$CE" || { echo "FAIL: commit-examples.md missing sample: $sample" >&2; exit 1; }
done

# Neither file has YAML frontmatter (docs/reference/ convention per PATTERNS Shared Pattern 2 rule 5)
! grep -q '^---$' "$DQ" || { echo "FAIL: docs/reference/dataview-queries.md should NOT have YAML frontmatter" >&2; exit 1; }
! grep -q '^---$' "$CE" || { echo "FAIL: docs/reference/commit-examples.md should NOT have YAML frontmatter" >&2; exit 1; }

# Both have H1 + See also footer (docs/reference/ci.md shape)
grep -q '^# ' "$DQ" || { echo "FAIL: dataview-queries.md missing H1" >&2; exit 1; }
grep -q '^# ' "$CE" || { echo "FAIL: commit-examples.md missing H1" >&2; exit 1; }

echo "PASS: docs/reference/dataview-queries.md (5 dataview blocks) + commit-examples.md present"
```

STEP 6 — Create `tests/phase-09.1/test_decision_record.sh` (I-12):

```bash
#!/usr/bin/env bash
# I-12: wiki/decisions/dr-2026-04-16-progressive-disclosure-extraction.md exists with
# trigger_type: schema-update, affected_pages: [], and all 7 required sections
# (TL;DR, Decision, Why, Alternatives Considered, Consequences, Affected Pages, Sources).
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/lib.sh"

DR="$REPO_ROOT/wiki/decisions/dr-2026-04-16-progressive-disclosure-extraction.md"
test -f "$DR" || { echo "FAIL: $DR missing" >&2; exit 1; }

# Frontmatter: type, trigger_type, affected_pages
python3 - "$DR" <<'PYEOF'
import sys, yaml, pathlib
p = pathlib.Path(sys.argv[1])
parts = p.read_text().split("---", 2)
if len(parts) < 3:
    sys.exit(f"FAIL: {p}: malformed frontmatter")
fm = yaml.safe_load(parts[1])
errors = []
if fm.get("type") != "decision":
    errors.append(f"type != decision (got {fm.get('type')!r})")
if fm.get("trigger_type") != "schema-update":
    errors.append(f"trigger_type != schema-update (got {fm.get('trigger_type')!r})")
ap = fm.get("affected_pages")
# R3 review consensus: require affected_pages: [] EXACTLY (D-14). No None fallback.
if ap != []:
    errors.append(f"affected_pages != [] exactly (got {ap!r}); D-14 requires empty list")
if fm.get("status") != "active":
    errors.append(f"status != active (got {fm.get('status')!r})")
if fm.get("epistemic_status") != "sourced":
    errors.append(f"epistemic_status != sourced (got {fm.get('epistemic_status')!r})")
if errors:
    for e in errors: print("FAIL:", e, file=sys.stderr)
    sys.exit(1)
PYEOF

# All 7 required sections present (AGENTS.md §4.6 section ordering)
for section in '^## TL;DR' '^## Decision' '^## Why' '^## Alternatives Considered' '^## Consequences' '^## Affected Pages' '^## Sources'; do
    grep -qE "$section" "$DR" \
        || { echo "FAIL: DR missing section matching '$section'" >&2; exit 1; }
done

# Why section must state adopted framing AND what it replaced (AGENTS.md §4.6 line 511 requirement)
# Mechanical proxy: require at least one "replaced" or "replacing" or "prior" or "previously" token
# in the body of the Why section.
python3 - "$DR" <<'PYEOF'
import sys, pathlib, re
text = pathlib.Path(sys.argv[1]).read_text()
# Extract Why section
m = re.search(r'^## Why\s*\n(.*?)(?=^## )', text, flags=re.DOTALL | re.MULTILINE)
if not m:
    sys.exit("FAIL: DR ## Why section empty or unbounded")
body = m.group(1)
if len(body.strip()) < 50:
    sys.exit("FAIL: DR ## Why section too short (<50 chars)")
if not re.search(r'\b(replac|prior|previously|former|before)\b', body, re.IGNORECASE):
    sys.exit("FAIL: DR ## Why must state what framing was replaced (AGENTS.md §4.6 requirement)")
PYEOF

# Alternatives Considered must list >=1 alternative
python3 - "$DR" <<'PYEOF'
import sys, pathlib, re
text = pathlib.Path(sys.argv[1]).read_text()
m = re.search(r'^## Alternatives Considered\s*\n(.*?)(?=^## )', text, flags=re.DOTALL | re.MULTILINE)
if not m:
    sys.exit("FAIL: DR ## Alternatives Considered section empty or unbounded")
body = m.group(1)
# Require >=1 bulleted alternative
bullets = [ln for ln in body.splitlines() if re.match(r'^\s*-\s', ln)]
if len(bullets) < 1:
    sys.exit("FAIL: DR ## Alternatives Considered must list >=1 alternative (AGENTS.md §4.6 requirement)")
PYEOF

echo "PASS: wiki/decisions/dr-2026-04-16-progressive-disclosure-extraction.md valid"
```

STEP 7 — Create `tests/phase-09.1/test_wiki_index_decisions.sh` (I-13):

```bash
#!/usr/bin/env bash
# I-13: wiki/index.md contains ## Decisions heading AND references the new DR.
# Note: wiki/index.md currently has no ## Decisions section (PATTERNS.md Note 2);
# Wave 1 must CREATE this section.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/lib.sh"

IDX="$REPO_ROOT/wiki/index.md"
test -f "$IDX" || { echo "FAIL: $IDX missing" >&2; exit 1; }

grep -q '^## Decisions' "$IDX" \
    || { echo "FAIL: wiki/index.md missing '## Decisions' heading (PATTERNS Note 2: Wave 1 must create it)" >&2; exit 1; }

grep -q 'dr-2026-04-16-progressive-disclosure-extraction' "$IDX" \
    || { echo "FAIL: wiki/index.md missing dr-2026-04-16-progressive-disclosure-extraction reference" >&2; exit 1; }

echo "PASS: wiki/index.md has Decisions section with new DR entry"
```

STEP 8 — Create `tests/phase-09.1/test_dataview_fences.sh` (R2 cross-AI review consensus). This test is RED today (the target file does not yet exist) and GREEN after Wave-1 produces `docs/reference/dataview-queries.md` with DIRECT ```dataview``` fences (no outer ```markdown wrapper). Rationale: the AGENTS.md §16 Appendix A source-of-truth wraps each inner ```dataview``` block in an outer ```markdown fence because inside AGENTS.md the intent is "here is how an Obsidian markdown page embeds a Dataview query" (illustrative). In a standalone cookbook file (`docs/reference/dataview-queries.md`), readers want copy-pasteable query snippets, not markdown-escaped examples — so the outer ```markdown wrapper must be stripped and only the inner ```dataview block preserved. See R2 in 09.1-REVIEWS.md for full reasoning.

```bash
#!/usr/bin/env bash
# R2: docs/reference/dataview-queries.md uses DIRECT ```dataview fences.
# The AGENTS.md §16 Appendix A wraps each inner ```dataview``` block in an outer ```markdown
# fence (illustrative inline-markdown-showing-how-obsidian-renders-a-dataview-page). That outer
# wrapper is WRONG in a standalone cookbook — it turns copy-pasteable queries into
# markdown-escaped examples. Cookbook must emit inner fences only.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/lib.sh"

DQ="$REPO_ROOT/docs/reference/dataview-queries.md"
test -f "$DQ" || { echo "FAIL: $DQ missing (expected after Wave 1 extraction)" >&2; exit 1; }

# Positive: 5 inner ```dataview``` fence opens at column 0
dataview_opens="$(grep -cE '^```dataview$' "$DQ")"
[ "$dataview_opens" -eq 5 ] \
    || { echo "FAIL: expected 5 '```dataview' fence opens in $DQ, found $dataview_opens" >&2; exit 1; }

# Negative: ZERO outer ```markdown fences. These are appropriate inside AGENTS.md (illustrative)
# but forbidden in the standalone cookbook.
markdown_opens="$(grep -cE '^```markdown$' "$DQ")"
[ "$markdown_opens" -eq 0 ] \
    || { echo "FAIL: found $markdown_opens outer '```markdown' fence(s) in $DQ; standalone cookbook must strip the AGENTS.md §16 Appendix A outer wrapper (R2 review consensus)" >&2; exit 1; }

echo "PASS: docs/reference/dataview-queries.md uses direct ```dataview``` fences (no outer markdown wrappers)"
```

STEP 9 — `chmod +x` on all 8 new test files from this task. Run the aggregator: `bash tests/phase-09.1/run.sh`. Expect PHASE 09.1 TESTS to show 3/11 PASS (the 3 canaries from Task 1: test_byte_equality.sh + test_phase_07_stub_untouched.sh + test_template_parity.sh). The 8 new tests from this task all FAIL RED. Aggregator exits 1. This is expected and confirms the gate is armed. Note: run.sh and lib.sh are not counted by the aggregator (test_*.sh glob excludes them), so TOTAL=11.
  </action>

  <verify>
    <automated>test -x tests/phase-09.1/test_schema_examples_exist.sh && test -x tests/phase-09.1/test_schema_examples_frontmatter.sh && test -x tests/phase-09.1/test_agents_section_4_residue.sh && test -x tests/phase-09.1/test_agents_section_16.sh && test -x tests/phase-09.1/test_docs_reference_new.sh && test -x tests/phase-09.1/test_dataview_fences.sh && test -x tests/phase-09.1/test_decision_record.sh && test -x tests/phase-09.1/test_wiki_index_decisions.sh && bash tests/phase-09.1/run.sh 2>&1 | grep -qE 'PHASE 09\.1 TESTS: 3/11'</automated>
  </verify>

  <acceptance_criteria>
    - All 8 new test files executable: `for f in tests/phase-09.1/test_schema_examples_exist.sh tests/phase-09.1/test_schema_examples_frontmatter.sh tests/phase-09.1/test_agents_section_4_residue.sh tests/phase-09.1/test_agents_section_16.sh tests/phase-09.1/test_docs_reference_new.sh tests/phase-09.1/test_dataview_fences.sh tests/phase-09.1/test_decision_record.sh tests/phase-09.1/test_wiki_index_decisions.sh; do test -x "$f" || exit 1; done`
    - Each test sources lib.sh: `for f in tests/phase-09.1/test_schema_examples_*.sh tests/phase-09.1/test_agents_section_*.sh tests/phase-09.1/test_docs_reference_new.sh tests/phase-09.1/test_dataview_fences.sh tests/phase-09.1/test_decision_record.sh tests/phase-09.1/test_wiki_index_decisions.sh; do grep -q 'source.*lib\.sh' "$f" || { echo MISSING_SOURCE:$f; exit 1; }; done`
    - test_schema_examples_frontmatter.sh uses python3 heredoc with yaml.safe_load: `grep -q 'yaml.safe_load' tests/phase-09.1/test_schema_examples_frontmatter.sh`
    - test_agents_section_4_residue.sh asserts exactly 6 bare `See:` pointers: `grep -q 'count.*-eq 6' tests/phase-09.1/test_agents_section_4_residue.sh`
    - test_agents_section_4_residue.sh rejects anti-patterns (bold/markdown-link See): `grep -q 'D-09 violation' tests/phase-09.1/test_agents_section_4_residue.sh`
    - test_agents_section_4_residue.sh includes R8 ratchet grep asserting 'Six page types exist.': `grep -F -q 'Six page types exist.' tests/phase-09.1/test_agents_section_4_residue.sh`
    - test_agents_section_16.sh uses grep -F fixed-string match for Appendix C literal (R7): `grep -F -q "grep -F -q '**Read \`wiki/index.md\` first, always.**'" tests/phase-09.1/test_agents_section_16.sh`
    - test_docs_reference_new.sh asserts NO frontmatter in new docs/reference files: `grep -q 'should NOT have YAML frontmatter' tests/phase-09.1/test_docs_reference_new.sh`
    - test_dataview_fences.sh (R2) asserts 5 ```dataview fences AND 0 ```markdown fences in the extracted cookbook: `grep -q '```dataview' tests/phase-09.1/test_dataview_fences.sh && grep -q '```markdown' tests/phase-09.1/test_dataview_fences.sh && grep -q 'R2 review consensus' tests/phase-09.1/test_dataview_fences.sh`
    - test_decision_record.sh requires affected_pages == [] EXACTLY (R3 — no None fallback): `grep -q 'affected_pages != \[\] exactly' tests/phase-09.1/test_decision_record.sh`
    - test_decision_record.sh validates all 7 section headings + `## Why` content + `## Alternatives Considered` bullet count: `grep -q 'Alternatives Considered must list' tests/phase-09.1/test_decision_record.sh`
    - test_wiki_index_decisions.sh asserts `## Decisions` heading AND DR reference: `grep -q 'PATTERNS Note 2' tests/phase-09.1/test_wiki_index_decisions.sh`
    - Aggregator emits RED state today (3 GREEN canaries + 8 RED tests = 3/11 passing): `bash tests/phase-09.1/run.sh 2>&1 | grep -qE 'PHASE 09\.1 TESTS: 3/11'`
    - Aggregator exits non-zero today (8 RED tests): `bash tests/phase-09.1/run.sh; [ $? -eq 1 ]`
  </acceptance_criteria>

  <done>11 test files + run.sh + lib.sh under tests/phase-09.1/ (13 files total). 3 GREEN canaries (test_byte_equality.sh, test_phase_07_stub_untouched.sh, test_template_parity.sh) + 8 RED tests asserting Wave-1 extraction invariants + R2 dataview-fence shape. Aggregator TOTAL=11 (run.sh/lib.sh excluded by test_*.sh glob); exits 1 with 3/11 passing. Gate armed for Plan 09.1-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → CI | Test files execute in CI via .github/workflows/lint.yml (none of the new tests are yet wired into CI; that happens out-of-band after Wave 1 if desired — for now they're local-only gates) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-01 | T (Tampering) | tests/phase-09.1/test_*.sh | mitigate | Tests sourced only from repo-owned PATTERNS.md snippets and the RESEARCH.md invariants. No external input. Scripts use `set -euo pipefail` to catch unset-variable injection. |
| T-09.1-02 | D (Denial-of-service) | tests/phase-09.1/run.sh | accept | Aggregator iterates a bounded list of tests with no external network calls. Runtime <10s per VALIDATION.md. No DoS surface. |
| T-09.1-03 | I (Information disclosure) | tests/phase-09.1/*.sh | accept | Tests read AGENTS.md / CLAUDE.md / schema/ / wiki/ / docs/reference/ — all already-committed public content. No private paths touched. Privacy guard (bin/check-privacy.sh) already excludes the new paths by design (schema/ outside PUBLIC_PATHS; docs/reference new files have no frontmatter). |

No HIGH severity threats. Phase 09.1 gate (`security_enforcement: block on high`) passes cleanly.
</threat_model>

<verification>
- `test -x tests/phase-09.1/run.sh && bash tests/phase-09.1/run.sh 2>&1 | grep -q 'PHASE 09\.1 TESTS:'` — aggregator functional
- All 11 test files + lib.sh + run.sh exist in `tests/phase-09.1/` (R9 review-consensus count correction): `[ "$(ls tests/phase-09.1/*.sh | wc -l)" -eq 13 ]` (1 lib + 1 run + 11 test files = 13 .sh files total)
- 3 canary tests PASS on current state: `bash tests/phase-09.1/test_byte_equality.sh && bash tests/phase-09.1/test_phase_07_stub_untouched.sh && bash tests/phase-09.1/test_template_parity.sh`
- 8 RED tests fail predictably before Wave 1 (target files/sections don't exist yet)
- Aggregator emits 3/11 on current state: `bash tests/phase-09.1/run.sh 2>&1 | grep -qE 'PHASE 09\.1 TESTS: 3/11'`
- Aggregator exits 1 when run today: `bash tests/phase-09.1/run.sh || echo EXIT_NONZERO_OK`
</verification>

<success_criteria>
- `tests/phase-09.1/` contains 13 files: `run.sh`, `lib.sh`, `test_byte_equality.sh`, `test_phase_07_stub_untouched.sh`, `test_template_parity.sh`, `test_schema_examples_exist.sh`, `test_schema_examples_frontmatter.sh`, `test_agents_section_4_residue.sh`, `test_agents_section_16.sh`, `test_docs_reference_new.sh`, `test_dataview_fences.sh`, `test_decision_record.sh`, `test_wiki_index_decisions.sh` — 1 aggregator + 1 lib + 11 test files (R2 added test_dataview_fences.sh per cross-AI review; R9 count corrected from prior draft)
- `bash tests/phase-09.1/run.sh` runs to completion, emits `PHASE 09.1 TESTS: 3/11` tally on pre-extraction state, exits 1 (RED state confirmed)
- `test_template_parity.sh`, `test_byte_equality.sh`, `test_phase_07_stub_untouched.sh` ALL PASS on current pre-extraction state (3 canaries must stay green through Wave 1)
- Every test sources `lib.sh`, uses `set -euo pipefail`, exits 0 on PASS and 1 on FAIL with a stderr message prefixed `FAIL:`
- Commit includes all 13 files; `.githooks/pre-commit` may trigger but no AGENTS.md/CLAUDE.md edits in this plan so it should exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-progressive-disclosure-extraction/09.1-01-SUMMARY.md`
</output>
