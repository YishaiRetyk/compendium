---
phase: 15-privacy-architecture
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - bin/lint.sh
  - bin/release.sh
  - .claude/settings.cloud.json
  - docs/reference/privacy-model.md
  - tests/phase-15/test_lint_xtier_link.sh
  - tests/phase-15/test_cloud_deny_profile.sh
autonomous: true
requirements: [PRIV-03, PRIV-05]
must_haves:
  truths:
    - "A wiki-cloud/ page linking to a wiki-local/ target is a lint linkres ERROR (cloud->local forbidden, D-09) — this is the only NET-NEW lint logic; it is additive (does not break a green tree), so it correctly lives outside Plan 01's security-atomic commit"
    - "A concrete cloud deny-profile artifact exists (.claude/settings.cloud.json) labeled fail-open/convenience, plus an HONEST fail-direction table + separate-repo fail-closed runbook in docs"
    - "The fail-direction table names all 4 surfaces with their TRUE direction (Read DENIED, Bash file-cmd DENIED, python open NOT denied, git show NOT denied); docs never claim track-in-main is fail-closed"
    - "The behavioral cloud-deny verification test (or its static-assertion + manual-runbook fallback per RESEARCH A2) pins the Read/Bash/git-show fail-direction so a future harness loosening is caught"
    - "NOTE (review HIGH #6): the privacy PREDICATE re-keys (privacy_resolve collapse, check-neutrality leak-source, check-privacy structural, audit FAITH-04) and their tests moved to Plan 01's security-atomic commit. This plan no longer touches them — it carries only the additive D-09 check + the enforcement artifacts + the verification test"
  artifacts:
    - path: ".claude/settings.cloud.json"
      provides: "PRIV-03 cloud-scoped deny-read profile"
      contains: "Read(./wiki-local/**)"
    - path: "docs/reference/privacy-model.md"
      provides: "Fail-direction table + separate-repo fail-closed runbook + raw-source structural rule (D-12/D-13)"
      contains: "fail-open"
    - path: "bin/lint.sh"
      provides: "Net-new D-09 cloud->local linkres check (additive; LINT_VERSION minor bump)"
      contains: "cloud->local"
  key_links:
    - from: "bin/lint.sh linkres"
      to: "page_tier map (cloud|local)"
      via: "PIPED_LINK_RE resolution + cross-tier finding"
      pattern: "cloud->local"
    - from: ".claude/settings.cloud.json"
      to: "docs/reference/privacy-model.md"
      via: "fail-direction table documents the deny-profile's true (fail-open) coverage"
      pattern: "settings.cloud.json"
---

<objective>
Ship the NET-NEW, ADDITIVE enforcement artifacts on top of Plan 01's migrated + predicate-re-keyed tree (PRIV-03, PRIV-05 remainder). This plan: (1) adds the D-09 asymmetric cloud→local link check to `bin/lint.sh` linkres (net-new, additive — does not break a green tree, so it correctly lives outside Plan 01's security-atomic commit); (2) ships the `.claude/settings.cloud.json` cloud deny-profile; (3) authors the HONEST fail-direction table + separate-repo fail-closed runbook + the documented raw-source structural rule in `docs/reference/privacy-model.md`; (4) implements the cloud-deny verification test (or its static-assertion + manual-runbook fallback per RESEARCH A2). The phase's own purpose is to close an irreversible-leak gap, so every enforcement claim is labeled with its TRUE fail direction (D-12 — MUST NOT ship false assurance).

**Revised per cross-AI review (REVIEWS.md, review HIGH #6 — D-02 atomicity).** The privacy PREDICATE re-keys (privacy_resolve collapse, check-neutrality leak-source, check-privacy structural, audit FAITH-04) and the lint required-field/enum removal + generated-frontmatter strips were MOVED INTO Plan 01's security-atomic commit, because leaving them here created a committed intermediate where a privacy guard was dead. This plan now carries ONLY the genuinely net-new / additive / non-atomic-critical work: the D-09 link check (additive lint logic), the deny-profile artifact, the fail-direction docs, and the verification test. `test_check_privacy_rekey.sh` and `test_neutrality_leak_source_rekey.sh` are turned GREEN by Plan 01's predicate re-keys; this plan only asserts they still pass over the additive changes.

Purpose: Make the remaining privacy enforcement structural (a directory boundary + a harness flag) and honestly labeled, on top of the already-secured tree.
Output: D-09 lint check, the deny-profile artifact, the fail-direction + raw-source-rule docs, GREEN PRIV-03 + the additive PRIV-05 (D-09) tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-privacy-architecture/15-CONTEXT.md
@.planning/phases/15-privacy-architecture/15-RESEARCH.md
@.planning/phases/15-privacy-architecture/15-PATTERNS.md

<interfaces>
<!-- linkres machinery to REUSE for D-09 (do NOT rebuild — RESEARCH Don't Hand-Roll): -->
From bin/lint.sh:
  PIPED_LINK_RE = re.compile(r'\[\[([^\]|]+)\|([^\]]+)\]\]')   # line 391 (target, display)
  known_ids set + norm_map built over all_pages              # lines 1899-1926
  linkres_scan worklist of (rel, abs_path, full_text)        # lines 1936-1950
  mask_markdown(raw)                                         # line ~1970 (frontmatter+fence masking already correct)
  add_finding('error', 'linkres', rel, "...")               # lines 1979-1990
  'linkres': 'error' severity remap                          # line 321 (already exists)
  EXCLUDE_DIRS = {'maintenance', 'examples'}                 # line 435 (path-prefix-exemption template)
  # TWO-ROOT DISCOVERY (review MEDIUM #3): all_pages discovery is rooted in WIKI_DIR (one dir).
  # The D-09 check resolves a cloud-page link to a LOCAL target, so all_pages/known_ids/page_tier
  # MUST be built over BOTH wiki-cloud/ AND wiki-local/ (else local targets are unknown and the
  # cross-tier link looks like an ordinary red link, not a D-09 error). Build the page universe
  # over both roots; tag each id's tier from its path prefix.

<!-- NOTE: the predicate re-keys (privacy_resolve collapse, audit FAITH-04, check-neutrality -->
<!-- leak-source, check-privacy structural) MOVED to Plan 01's security-atomic commit (review -->
<!-- HIGH #6). They are NOT in this plan. This plan only touches the ADDITIVE D-09 lint check, -->
<!-- the deny-profile artifact, the docs, and the verification test. -->

<!-- the deny-profile artifact (RESEARCH Code Examples, D-12): -->
.claude/settings.cloud.json:
  { "permissions": { "deny": ["Read(./wiki-local/**)"] } }
  # loaded via: claude --settings ./.claude/settings.cloud.json
  # LABEL: policy/convenience, FAIL-OPEN, single-clone low-stakes only
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the net-new, additive D-09 cloud→local link check to lint linkres (two-root discovery)</name>
  <files>bin/lint.sh, tests/phase-15/test_lint_xtier_link.sh</files>
  <read_first>
    - bin/lint.sh:1890-2025 (the linkres block — RESEARCH-confirmed range; known_ids/norm_map/linkres_scan/PIPED_LINK_RE/add_finding)
    - bin/lint.sh:94,1899-1926 (WIKI_DIR root + the all_pages/known_ids build — must extend to a TWO-ROOT page universe over wiki-cloud/ AND wiki-local/, review MEDIUM #3)
    - bin/lint.sh:26-29,313-321,435 (--category help + severity remap dict + EXCLUDE_DIRS — for the subcategory-vs-new-category decision)
    - .planning/phases/15-privacy-architecture/15-PATTERNS.md lines 187-205 (linkres-anchored asymmetric-link check, with exact line anchors)
    - .planning/phases/15-privacy-architecture/15-RESEARCH.md "Pattern 3" + "Don't Hand-Roll" + Open Q3/A4 (subcategory vs new category)
  </read_first>
  <action>
    Add ONLY the net-new, ADDITIVE D-09 asymmetric link check (the predicate re-keys for privacy_resolve / check-neutrality / check-privacy / audit FAITH-04 are in Plan 01's security-atomic commit — review HIGH #6; this plan does NOT touch them). This check is additive: it adds a new finding on a previously-undetected condition; it does not break an otherwise-green tree, so it correctly lives outside Plan 01's atomic commit.

    bin/lint.sh — D-09 asymmetric link check: REUSE the existing linkres machinery (do NOT rebuild).
      (1) TWO-ROOT page universe (review MEDIUM #3): the current `all_pages`/`known_ids`/`norm_map` build (lines ~1899-1926) is rooted in a single `WIKI_DIR`. The D-09 check must resolve a `wiki-cloud/` link whose TARGET lives in sibling `wiki-local/` — so the page universe MUST be built over BOTH roots (wiki-cloud/ AND wiki-local/). Without this, a cloud→local link resolves to an UNKNOWN id and surfaces as an ordinary red link, not a D-09 cross-tier error. Extend the discovery to walk both roots (Plan 01 already re-keyed WIKI_DIR default to wiki-cloud/; here add the wiki-local/ root to the page-universe build, guarded by `os.path.isdir` so a vault with no wiki-local/ content still works).
      (2) Build a parallel `page_tier[id] = 'cloud'|'local'` map from each page's path prefix (`wiki-cloud/` → cloud, `wiki-local/` → local) during the same walk.
      (3) Inside the existing `for rel, abs_path, raw in linkres_scan:` loop (line ~1969), when the LINKING file is under `wiki-cloud/` AND a resolved `PIPED_LINK_RE` target's id maps to a `local` page, emit:
        `add_finding('error', 'linkres', rel, "cloud->local link forbidden (PRIV-05/D-09): wiki-cloud page links to a wiki-local target (would break for cloud sessions + leak existence)")`
      Reuse `mask_markdown` (links in code fences/frontmatter ignored) and the existing `linkres`→`error` remap (line 321). Per RESEARCH Open Q3/A4, prefer a logical subcategory tag inside `linkres` findings (least churn, inherits the error remap); escalate to a top-level `xtier` category ONLY if reporting clarity demands it (that adds the ~3 registration edits at lines 26-29, 313-321, and the should_run path). Bump LINT_VERSION minor (1.6.0 → 1.7.0) for the additive check.

      DO NOT re-introduce the `privacy` field anywhere; DO NOT touch privacy_resolve.py / check-neutrality.sh / check-privacy.sh / audit-claims.sh (Plan 01 owns those). This task is purely the additive lint rule + its two-root discovery.
  </action>
  <verify>
    <automated>bash tests/phase-15/test_lint_xtier_link.sh && bash bin/lint.sh wiki-cloud/ && echo "D-09 live + wiki-cloud lint still green"</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/phase-15/test_lint_xtier_link.sh` exits 0 (cloud→local link is a linkres error)
    - `grep -n 'cloud->local\|D-09' bin/lint.sh` shows the new check
    - two-root discovery present (review MEDIUM #3): `grep -nE "wiki-local" bin/lint.sh` shows the page-universe walk includes the local root (not only the cloud root)
    - `grep -q "LINT_VERSION=\"1.7" bin/lint.sh` (additive minor bump)
    - `bash bin/lint.sh wiki-cloud/` exits 0 (the D-09 check does not false-fire on the real migrated tree)
    - this task did NOT touch the predicates (Plan 01 owns them): `git diff --name-only HEAD -- bin/lib/privacy_resolve.py bin/check-neutrality.sh bin/check-privacy.sh bin/audit-claims.sh | wc -l` == 0 for THIS task's commit
  </acceptance_criteria>
  <done>Net-new additive D-09 cloud→local link check live with two-root discovery; LINT_VERSION minor-bumped; predicates untouched (Plan 01 owns them).</done>
</task>

<task type="auto">
  <name>Task 2: Ship the cloud deny-profile + the HONEST fail-direction/raw-source docs + the verification test (asserts Plan 01's predicate re-keys still green)</name>
  <files>bin/release.sh, .claude/settings.cloud.json, docs/reference/privacy-model.md, tests/phase-15/test_cloud_deny_profile.sh</files>
  <read_first>
    - bin/release.sh:18-52 (ALLOWLIST — confirm wiki-local/ excluded by absence; Plan 01 already re-keyed wiki/→wiki-cloud/ here, this task only RE-CONFIRMS wiki-local/ is absent)
    - .claude/settings.json (the structural sibling — JSON shape only; the deny goes in settings.cloud.json, NEVER here)
    - .planning/phases/15-privacy-architecture/15-RESEARCH.md "Code Examples" (deny-profile JSON + verification-test skeleton + the 4 surfaces) + Assumptions A2 (headless feasibility) + Open Q2 (the A2 fallback ladder)
    - .planning/phases/15-privacy-architecture/15-PATTERNS.md lines 151-153,258-272 (test_cloud_deny_profile + settings.cloud.json sibling)
    - docs/reference/privacy-model.md (current §13 mirror — the natural home for the fail-direction table + separate-repo runbook + the raw-source structural rule per D-12)
    - .planning/phases/15-privacy-architecture/15-CONTEXT.md D-12/D-13/D-14 (honest labeling, two regimes, unified-graph preservation) + the raw-source structural rule Plan 01 Task 6 defined (raw sources/ cloud-safe-only; local sources live as their summary under wiki-local/sources/)
  </read_first>
  <action>
    Ship the honest enforcement artifact set (the predicate re-keys are Plan 01's; this task asserts they stayed green, and adds the NET-NEW artifacts).

    (a) Create `.claude/settings.cloud.json` (NEW artifact) with exactly:
      `{ "permissions": { "deny": ["Read(./wiki-local/**)"] } }`
    JSON forbids comments, so document the labeling (policy/convenience, FAIL-OPEN, single-clone low-stakes only; loaded via `claude --settings ./.claude/settings.cloud.json`) in docs/reference/privacy-model.md, not inline. The deny MUST NOT be added to the committed `.claude/settings.json` (deny-beats-allow would block local sessions too — Pitfall 2). Leave `.claude/settings.json` permissive.

    (b) docs/reference/privacy-model.md — author the HONEST fail-direction table + the separate-repo fail-closed runbook + the raw-source structural rule (D-12/D-13/D-14):
      - Fail-direction table — name the 4 surfaces with their TRUE direction: Read tool → DENIED (fail-open profile covers it); `cat`/`grep`/`head` via Bash → DENIED (recognized file cmds); `python3 -c open(...)` → NOT denied (fail-open gap); `git show HEAD:wiki-local/…` → NOT denied (git-object gap).
      - State plainly: the deny-profile is convenience/fail-open; the separate-repo regime (D-13: wiki-local/ as its own gitignored nested repo with its own private remote) is the fail-closed guarantee; the OS sandbox is the OS-level fail-closed alternative. Document the separate-repo runbook (when real local_only content arrives) and note it PRESERVES the unified Obsidian graph (D-14).
      - RAW-SOURCE STRUCTURAL RULE (review HIGH #2 — document the rule Plan 01 Task 6 defined): raw `sources/` is cloud-safe-only; a source that must be local lives as its SOURCE-SUMMARY page under `wiki-local/sources/`; the FAITH-04 resolver keys off the summary-page tier, never the raw `sources/...` path. This is the documented hard rule the reviewer's Suggestion #2 calls for.
      - check-privacy SCOPE HONESTY (review MEDIUM #2): note that `check-privacy.sh` is a PATH/release-allowlist guard (catches `docs/wiki-local/...` path leaks), NOT a content-equivalence scanner; copied-content leaks (a `wiki-local/` term pasted into `docs/private.md`) are caught by `check-neutrality.sh`'s term-scan, not check-privacy.
      - Add the forward-reference that the init-wizard interactive privacy-tier prompt is deferred to Phase D (per CONTEXT deferred). MUST NOT claim sparse-worktree or the settings-deny makes track-in-main fail-closed (the false-assurance trap).

    (c) CONFIRM `bin/release.sh` ALLOWLIST excludes `wiki-local/*` (belt-and-suspenders; Plan 01 already re-keyed the wiki/→wiki-cloud/ entries). If any wiki-local path is in the allowlist, remove it.

    (d) tests/phase-15/test_cloud_deny_profile.sh — implement the assertions the Wave-0 stub declared. Static-assert settings.cloud.json shape (`deny == ["Read(./wiki-local/**)"]`) + the fail-direction table presence in privacy-model.md (grep fail-open / git show / python). Per RESEARCH A2 + Open Q2, SPIKE the headless `claude -p --settings ./.claude/settings.cloud.json` three-surface behavioral assertion (Read DENIED, Bash cat DENIED, git show NOT denied); if the headless assertion is brittle/non-deterministic in this harness, fall back to the static-assertion + the documented manual runbook (VALIDATION.md Manual-Only row) and leave a clearly-commented `# MANUAL:` block — either path satisfies Success Criterion #3 (concrete artifact) + D-12.4 (regression tripwire exists).

    (e) ASSERT Plan 01's predicate re-keys are still green over this plan's additive changes (no regression introduced by the D-09 check / docs): run `tests/phase-15/test_check_privacy_rekey.sh` + `test_neutrality_leak_source_rekey.sh` + the phase-13 audit suite. These were turned GREEN by Plan 01; this task only confirms they remain green (it does NOT re-implement the predicates).
  </action>
  <verify>
    <automated>bash tests/phase-15/test_cloud_deny_profile.sh && python3 -c "import json; d=json.load(open('.claude/settings.cloud.json')); assert d['permissions']['deny']==['Read(./wiki-local/**)'], d; print('deny-profile OK')" && grep -qi 'fail-open' docs/reference/privacy-model.md && grep -qi 'git show' docs/reference/privacy-model.md && bash tests/phase-15/test_check_privacy_rekey.sh && bash tests/phase-15/test_neutrality_leak_source_rekey.sh && for t in tests/phase-13/test_*.sh; do bash "$t" >/dev/null 2>&1 || { echo "REGRESSION: $t"; exit 1; }; done && echo "phase-13 audit suite still green"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .claude/settings.cloud.json` exits 0 and the python json-load assertion above prints "deny-profile OK"
    - `! grep -q 'wiki-local' .claude/settings.json` (deny NOT in the committed permissive profile — Pitfall 2)
    - `grep -qi 'fail-open' docs/reference/privacy-model.md && grep -qi 'fail-closed' docs/reference/privacy-model.md` exits 0
    - `grep -qi 'git show' docs/reference/privacy-model.md && grep -qi 'separate.repo' docs/reference/privacy-model.md` exits 0 (object-leak gap + fail-closed regime documented)
    - raw-source rule documented (review HIGH #2): `grep -qiE 'raw .*sources.*cloud|wiki-local/sources' docs/reference/privacy-model.md` exits 0
    - check-privacy scope honesty (review MEDIUM #2): `grep -qiE 'path.*guard|not a content' docs/reference/privacy-model.md` exits 0
    - `bash tests/phase-15/test_cloud_deny_profile.sh` exits 0
    - `! grep -q 'wiki-local' bin/release.sh` (allowlist excludes local tier by absence)
    - Plan-01 predicate gates still green (no regression): `bash tests/phase-15/test_check_privacy_rekey.sh` exits 0 AND `bash tests/phase-15/test_neutrality_leak_source_rekey.sh` exits 0 AND the phase-13 for-loop prints "phase-13 audit suite still green"
  </acceptance_criteria>
  <done>cloud deny-profile shipped + honestly labeled; fail-direction table + separate-repo runbook + raw-source rule + check-privacy scope honesty in docs; verification test (or A2 fallback) pins the fail-direction; Plan-01 predicate gates confirmed still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cloud session → wiki-local/ git objects | The git-object store is reachable regardless of working-tree/sparse state — the residual fail-open surface this plan must label honestly, never paper over |
| cloud session → wiki-local/ via Bash subprocess | `python -c open()` / arbitrary subprocesses bypass the Read-tool deny — the second residual fail-open surface |
| wiki-local/ content → PUBLIC_PATHS / release stage | check-privacy.sh (path guard) + check-neutrality.sh leak-source term-scan + release allowlist are the guards preventing local content reaching template-public surfaces. NOTE: the predicate re-keys for check-privacy + check-neutrality landed in Plan 01's security-atomic commit (review HIGH #6); this plan only re-confirms they stay green + adds the deny-profile/docs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-leak | Information disclosure | Cloud session reading wiki-local/ (the irreversible-leak gap this phase exists to close) | mitigate (partial) + accept (residual) | Mitigate the Read-tool + recognized-Bash-file-cmd surface via the .claude/settings.cloud.json deny; ACCEPT + DOCUMENT the residual python/subprocess + git-object fail-open surfaces in the honest fail-direction table; the FAIL-CLOSED guarantee is the separate-repo regime (D-13), documented as the path for real local_only content. The verification test PINS current Read/Bash behavior so a future harness change that loosens it is caught. |
| T-15-leak-2 | Information disclosure | False assurance — docs claiming track-in-main is fail-closed | mitigate | D-12: docs MUST label the deny-profile fail-open and name the separate-repo regime as the only fail-closed path; acceptance criteria grep for 'fail-open' + 'git show' + 'separate-repo'; MUST NOT ship the sparse-worktree-is-safe claim |
| T-15-leak-3 | Information disclosure | wiki-local/ content leaking into PUBLIC_PATHS / the public release | mitigate (predicate in Plan 01) | check-privacy.sh re-keyed (Plan 01 Task 6) to flag wiki-local/ under PUBLIC_PATHS — a PATH guard, NOT a content scanner (review MEDIUM #2: copied-content leaks are caught by check-neutrality's term-scan, also Plan 01); release allowlist excludes wiki-local/ by absence (this plan re-confirms). This plan documents the path-vs-content scope split honestly in privacy-model.md. |
| T-15-leak-4 | Information disclosure | Neutrality guard silently losing its wiki-local leak source (the W1 regression): the dead `privacy: local_only` regex matches nothing post-strip → one of three CI-gated leak sources goes dark | mitigate (in Plan 01 — review HIGH #6) | The leak-source predicate re-key (walk wiki-local/ by prefix, drop the dead regex) is in PLAN 01's security-atomic commit, NOT here — so there is no committed intermediate where the predicate is dead. test_neutrality_leak_source_rekey.sh is RED in Wave 0 and turned green by Plan 01; this plan re-confirms it stays green over the additive D-09 change. |
| T-15-05 | Tampering | cloud→local wikilink (would break for cloud sessions + leak the existence of private pages) | mitigate | D-09 linkres check emits an error on any wiki-cloud→wiki-local link; the two-root page universe (review MEDIUM #3) ensures the local target is KNOWN (else it looks like a red link); test_lint_xtier_link asserts exit 1 |
| T-15-06 | Spoofing | A regression in the audit FAITH-04 partition silently un-withholding local claims | mitigate (predicate in Plan 01) | The FAITH-04 predicate re-key + source-summary-path fix landed in Plan 01 (review HIGH #2/#3); this plan re-confirms the phase-13 audit suite stays green over the additive D-09/docs changes (the partition control-flow was preserved, only the predicate swapped). |
</threat_model>

<verification>
- `bash tests/phase-15/test_lint_xtier_link.sh test_cloud_deny_profile.sh` — the two tests this plan implements GREEN
- `.claude/settings.cloud.json` exists with the exact deny entry; `.claude/settings.json` stays permissive (no wiki-local deny)
- `docs/reference/privacy-model.md` carries the honest fail-direction table (4 surfaces) + separate-repo fail-closed runbook + the raw-source structural rule + the check-privacy path-vs-content scope honesty
- D-09 check uses two-root discovery (review MEDIUM #3): a wiki-cloud→wiki-local link resolves to a KNOWN local target (a linkres error), not an unknown red link
- Plan-01 predicate gates still green over this plan's additive changes: `test_check_privacy_rekey.sh` + `test_neutrality_leak_source_rekey.sh` + phase-13 audit suite all green (this plan does NOT re-implement them — review HIGH #6)
- `for t in tests/phase-15/test_*.sh; do bash "$t"; done` — full phase-15 suite GREEN
- `bash bin/lint.sh wiki-cloud/` exits 0 (D-09 check does not false-fire on the real tree)
</verification>

<success_criteria>
- D-09 cloud→local link check live in lint (error severity) with two-root discovery; LINT_VERSION minor-bumped
- Concrete enforcement artifact set shipped: .claude/settings.cloud.json (fail-open, labeled) + honest fail-direction table + separate-repo fail-closed runbook + raw-source structural rule + check-privacy scope honesty
- No false assurance: every enforcement claim labeled with its true fail direction (D-12)
- The privacy PREDICATE re-keys live in Plan 01's security-atomic commit (review HIGH #6); this plan confirms they stay green
- PRIV-03 + the additive PRIV-05 (D-09) tests green
</success_criteria>

<output>
After completion, create `.planning/phases/15-privacy-architecture/15-02-SUMMARY.md`
</output>
