---
phase: 15-privacy-architecture
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - bin/lint.sh
  - bin/check-privacy.sh
  - bin/check-neutrality.sh
  - bin/lib/privacy_resolve.py
  - bin/audit-claims.sh
  - .claude/settings.cloud.json
  - docs/reference/privacy-model.md
  - tests/phase-15/test_lint_xtier_link.sh
  - tests/phase-15/test_check_privacy_rekey.sh
  - tests/phase-15/test_neutrality_leak_source_rekey.sh
  - tests/phase-15/test_cloud_deny_profile.sh
autonomous: true
requirements: [PRIV-03, PRIV-05]
must_haves:
  truths:
    - "A wiki-cloud/ page linking to a wiki-local/ target is a lint linkres ERROR (cloud->local forbidden, D-09)"
    - "bin/check-privacy.sh is re-keyed to the structural model: it flags wiki-local/ content appearing under PUBLIC_PATHS / the release stage, not a frontmatter field"
    - "bin/check-neutrality.sh source_local_only_wiki() is re-keyed from the privacy: local_only frontmatter regex to the structural model (walk wiki-local/ by path prefix); the neutrality guard keeps all three leak sources"
    - "privacy_resolve effective-claim predicate collapses to: local_only iff page OR any contributing source lives under wiki-local/ (the 3-level ladder is gone)"
    - "audit FAITH-04 chokepoint still withholds effective-local claims, now via the structural predicate, with no behavioral regression (phase-13 suite green)"
    - "A concrete cloud deny-profile artifact exists (.claude/settings.cloud.json) labeled fail-open/convenience, plus an HONEST fail-direction table + separate-repo fail-closed runbook in docs"
  artifacts:
    - path: ".claude/settings.cloud.json"
      provides: "PRIV-03 cloud-scoped deny-read profile"
      contains: "Read(./wiki-local/**)"
    - path: "docs/reference/privacy-model.md"
      provides: "Fail-direction table + separate-repo fail-closed runbook (D-12/D-13)"
      contains: "fail-open"
    - path: "bin/check-privacy.sh"
      provides: "Re-keyed structural leak guard (PRIV-05)"
    - path: "bin/check-neutrality.sh"
      provides: "Re-keyed leak-source predicate (W1 — privacy: local_only regex -> wiki-local/ path-prefix walk)"
    - path: "bin/lib/privacy_resolve.py"
      provides: "Collapsed single-predicate effective-privacy resolver (PRIV-05)"
  key_links:
    - from: "bin/lint.sh linkres"
      to: "page_tier map (cloud|local)"
      via: "PIPED_LINK_RE resolution + cross-tier finding"
      pattern: "cloud->local"
    - from: "bin/audit-claims.sh"
      to: "bin/lib/privacy_resolve.py"
      via: "resolve_effective_claim_privacy import (line 142)"
      pattern: "resolve_effective_claim_privacy"
    - from: "bin/check-neutrality.sh source_local_only_wiki()"
      to: "wiki-local/ path prefix"
      via: "os.walk(wiki-local) replacing the privacy: local_only frontmatter regex"
      pattern: "wiki-local"
    - from: ".claude/settings.cloud.json"
      to: "docs/reference/privacy-model.md"
      via: "fail-direction table documents the deny-profile's true (fail-open) coverage"
      pattern: "settings.cloud.json"
---

<objective>
Ship the net-new enforcement artifacts and the tooling LOGIC re-keys (PRIV-03, PRIV-05) on top of the migrated tree. This plan: (1) adds the D-09 asymmetric cloud→local link check to `bin/lint.sh` linkres; (2) re-keys `bin/check-privacy.sh` from the frontmatter-grep to the structural `wiki-local/`-leak model AND re-keys `bin/check-neutrality.sh`'s leak-SOURCE predicate from the now-dead `privacy: local_only` regex to the structural `wiki-local/` path-prefix walk (W1 — without this the neutrality guard silently loses one of its three CI-gated leak sources); (3) collapses `bin/lib/privacy_resolve.py` to the single structural predicate and re-keys the `bin/audit-claims.sh` FAITH-04 chokepoint with no behavioral regression; (4) ships the `.claude/settings.cloud.json` cloud deny-profile + the HONEST fail-direction table + separate-repo fail-closed runbook in `docs/reference/privacy-model.md` + the verification tests. The phase's own purpose is to close an irreversible-leak gap, so every enforcement claim is labeled with its TRUE fail direction (D-12 — MUST NOT ship false assurance).

Purpose: Make privacy enforcement structural (a directory boundary + a harness flag), not a remembered agent rule, while honestly labeling the fail-open vs fail-closed paths. These are the PREDICATE/LOGIC re-keys (the privacy field is gone after Plan 01; every check that depended on the field must now key off the wiki-local/ path prefix).
Output: D-09 lint check, re-keyed guards (check-privacy + check-neutrality predicate + privacy_resolve + audit), the deny-profile artifact, the fail-direction docs, GREEN PRIV-03/PRIV-05 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-privacy-architecture/15-CONTEXT.md
@.planning/phases/15-privacy-architecture/15-RESEARCH.md
@.planning/phases/15-privacy-architecture/15-PATTERNS.md

<interfaces>
<!-- linkres machinery to REUSE for D-09 (do NOT rebuild — RESEARCH Don't Hand-Roll): -->
From bin/lint.sh:
  PIPED_LINK_RE = re.compile(r'\[\[([^\]|]+)\|([^\]]+)\]\]')   # line 391 (target, display)
  known_ids set + norm_map built over all_pages              # lines 1899-1926
  linkres_scan worklist of (rel, abs_path, full_text)        # lines 1936-1950
  mask_markdown(raw)                                         # line ~1970 (frontmatter+fence masking already correct)
  add_finding('error', 'linkres', rel, "...")               # lines 1979-1990
  'linkres': 'error' severity remap                          # line 321 (already exists)
  EXCLUDE_DIRS = {'maintenance', 'examples'}                 # line 435 (path-prefix-exemption template)

<!-- audit chokepoint to RE-KEY (keep control-flow, swap predicate): -->
From bin/audit-claims.sh:
  from privacy_resolve import resolve_source_privacy, resolve_effective_claim_privacy   # line 142
  effective_priv = resolve_effective_claim_privacy(...)                                 # line 745
  if effective_priv == 'local_only' and not ALLOW_LOCAL: add_finding('skipped-privacy', ...)  # 745-762

<!-- privacy_resolve to COLLAPSE (single predicate, NOT the 3-level ladder): -->
From bin/lib/privacy_resolve.py:
  def resolve_source_privacy(source_fm, source_path)                       # line 37
  def resolve_effective_claim_privacy(page_fm, page_path, source_fm, ...)  # line 64
  # New predicate: effective local_only IFF page_path OR any contributing source_path is under wiki-local/

<!-- check-privacy to RE-KEY (preserve CLI surface + exit codes 0/1/2): -->
From bin/check-privacy.sh:
  PUBLIC_PATHS=(examples docs AGENTS.md CLAUDE.md README.md PRIVACY.md .github)   # line 71
  PRIVACY_LOCAL_ONLY_RE = re.compile(r'^privacy:\s*local_only\s*$', re.M)         # line 87 (target GONE post-strip)

<!-- check-neutrality leak-source predicate to RE-KEY (W1 — A3 mis-classified this as pure-path): -->
From bin/check-neutrality.sh:
  source_local_only_wiki()                                   # line 262 — leak source (1) of 3
  wiki_dir = os.path.join(ROOT, "wiki")                      # line 265 — walk dir (now wiki-local/)
  if fm and re.search(r"^privacy:\s*local_only\b", fm, re.M):# line 279 — DEAD predicate post-strip (matches nothing)
  suggest() group label "wiki local_only" + dispatch         # line 368 — one of three CI-gated leak sources
  # Plan 01 already re-keyed lines 8/47 comments + line-94 PUBLIC_PATHS `wiki`->`wiki-cloud`; this plan owns the PREDICATE.
  # Lines 308-309 git-history paths stay literal `wiki/` (pre-migration commits via git log --all) — DO NOT touch.

<!-- the deny-profile artifact (RESEARCH Code Examples, D-12): -->
.claude/settings.cloud.json:
  { "permissions": { "deny": ["Read(./wiki-local/**)"] } }
  # loaded via: claude --settings ./.claude/settings.cloud.json
  # LABEL: policy/convenience, FAIL-OPEN, single-clone low-stakes only
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add D-09 cloud→local link check to lint linkres + re-key privacy_resolve + audit FAITH-04</name>
  <files>bin/lint.sh, bin/lib/privacy_resolve.py, bin/audit-claims.sh, tests/phase-15/test_lint_xtier_link.sh</files>
  <read_first>
    - bin/lint.sh:1890-2025 (the linkres block — RESEARCH-confirmed range; known_ids/norm_map/linkres_scan/PIPED_LINK_RE/add_finding)
    - bin/lint.sh:26-29,313-321,435 (--category help + severity remap dict + EXCLUDE_DIRS — for the subcategory-vs-new-category decision)
    - bin/lib/privacy_resolve.py:37-87 (the 3-level resolver being COLLAPSED — both functions)
    - bin/audit-claims.sh:140-142,745-762 (the import + the FAITH-04 partition gate — keep control-flow, swap predicate)
    - .planning/phases/15-privacy-architecture/15-PATTERNS.md lines 155-226 (linkres-anchored asymmetric-link check + privacy_resolve collapse + audit re-key, with exact line anchors)
    - .planning/phases/15-privacy-architecture/15-RESEARCH.md "Pattern 3" + "Don't Hand-Roll" + Open Q3/A4 (subcategory vs new category)
  </read_first>
  <action>
    Three tooling logic re-keys (the migration's path-string churn already landed in Plan 01; this is the predicate/check LOGIC).

    (a) bin/lint.sh — D-09 asymmetric link check: REUSE the existing linkres machinery (do NOT rebuild). During the `all_pages` walk that builds `known_ids` (lines ~1899-1907), build a parallel `page_tier[id] = 'cloud'|'local'` map from each page's path prefix (`wiki-cloud/` → cloud, `wiki-local/` → local). Then inside the existing `for rel, abs_path, raw in linkres_scan:` loop (line ~1969), when the LINKING file is under `wiki-cloud/` AND a resolved `PIPED_LINK_RE` target's id maps to a `local` page, emit:
      `add_finding('error', 'linkres', rel, "cloud->local link forbidden (PRIV-05/D-09): wiki-cloud page links to a wiki-local target (would break for cloud sessions + leak existence)")`
    Reuse `mask_markdown` (so links in code fences/frontmatter are ignored) and the existing `linkres`→`error` remap (line 321). Per RESEARCH Open Q3/A4, prefer a logical subcategory tag inside `linkres` findings (least churn, inherits the error remap); escalate to a top-level `xtier` category ONLY if reporting clarity demands it (that adds the ~3 registration edits at lines 26-29, 313-321, and the should_run path). Bump LINT_VERSION minor (1.6.0 → 1.7.0) for the additive check.

    (b) bin/lib/privacy_resolve.py — COLLAPSE to the single structural predicate (do NOT port the 3-level ladder — RESEARCH Don't Hand-Roll). `resolve_effective_claim_privacy(...)` returns `'local_only'` IFF the claim's page path OR any contributing source path is under `wiki-local/`, else `'cloud_safe'`. Keep the exported function NAMES `resolve_source_privacy` + `resolve_effective_claim_privacy` stable (the audit imports them by name at line 142) — OR update the import+call site in audit-claims.sh in lockstep (this same task). The new predicate keys off PATH (`wiki-local/` prefix), so the signature MAY simplify to path arguments; if you change arity, update audit-claims.sh:142 + :745 in the same commit.

    (c) bin/audit-claims.sh — re-key the FAITH-04 consumer: keep the partition CONTROL-FLOW at lines 745-762 (`if effective_priv == 'local_only' and not ALLOW_LOCAL: add_finding('skipped-privacy', ...)` withholding from BOTH the verifier subprocess AND `--emit-worklist`) UNCHANGED in semantics; only the predicate underneath swaps to the structural one. The withhold/skipped-privacy/`--allow-local` opt-in/verifier-locality model are UNCHANGED (CONTEXT: re-keys, does not change enforcement semantics).
  </action>
  <verify>
    <automated>bash tests/phase-15/test_lint_xtier_link.sh && python3 -c "import sys; sys.path.insert(0,'bin/lib'); from privacy_resolve import resolve_effective_claim_privacy" && for t in tests/phase-13/test_*.sh; do bash "$t" >/dev/null 2>&1 || { echo "REGRESSION: $t"; exit 1; }; done && echo "phase-13 audit suite green"</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/phase-15/test_lint_xtier_link.sh` exits 0 (cloud→local link is a linkres error)
    - `grep -n 'cloud->local\|D-09\|wiki-local' bin/lint.sh` shows the new check
    - `grep -q "LINT_VERSION=\"1.7" bin/lint.sh` (additive minor bump)
    - `! grep -qi 'three-level\|stricter.of\|fail-closed' bin/lib/privacy_resolve.py` (3-level ladder collapsed)
    - audit imports still resolve: `python3 -c "import sys; sys.path.insert(0,'bin/lib'); from privacy_resolve import resolve_effective_claim_privacy" ` exits 0
    - phase-13 audit suite green (no FAITH-04 regression): the for-loop above prints "phase-13 audit suite green"
  </acceptance_criteria>
  <done>D-09 link check live; privacy_resolve collapsed to single predicate; audit FAITH-04 re-keyed with no regression.</done>
</task>

<task type="auto">
  <name>Task 2: Re-key check-privacy.sh + check-neutrality.sh leak-source predicate; ship the cloud deny-profile + fail-direction docs + verification tests</name>
  <files>bin/check-privacy.sh, bin/check-neutrality.sh, bin/release.sh, .claude/settings.cloud.json, docs/reference/privacy-model.md, tests/phase-15/test_check_privacy_rekey.sh, tests/phase-15/test_neutrality_leak_source_rekey.sh, tests/phase-15/test_cloud_deny_profile.sh</files>
  <read_first>
    - bin/check-privacy.sh:24-33,67-87,101-140 (PUBLIC_PATHS + frontmatter-grep predicate + exit codes 0/1/2 + the env-export-to-python idiom — preserve the CLI surface, swap only the scan predicate)
    - bin/check-neutrality.sh:262-283,365-382 (source_local_only_wiki() leak source (1) of 3 + the suggest() dispatch list — W1 PREDICATE re-key target; Plan 01 already re-keyed the line-8/47 comments + line-94 PUBLIC_PATHS, so here ONLY the predicate)
    - bin/check-neutrality.sh:305-343 (source_git_history() — the lines 308-309 hardcoded paths reference PRE-migration `wiki/` commits via `git log --all`; DO NOT re-key them, they must stay literal `wiki/`)
    - bin/release.sh:18-52 (ALLOWLIST — confirm wiki-local/ excluded by absence; this is the belt to check-privacy's suspenders)
    - .claude/settings.json (the structural sibling — JSON shape only; the deny goes in settings.cloud.json, NEVER here)
    - .planning/phases/15-privacy-architecture/15-RESEARCH.md "Code Examples" (deny-profile JSON + verification-test skeleton + the 4 surfaces) + "check-privacy.sh re-key" block + Assumptions A2 (headless feasibility) + A3 (the mis-classification the W1 predicate re-key corrects) + Open Q1 (thin-guard recommendation)
    - .planning/phases/15-privacy-architecture/15-PATTERNS.md lines 151-173,258-272 (test_cloud_deny_profile + check-privacy re-key + settings.cloud.json sibling)
    - docs/reference/privacy-model.md (current §13 mirror — the natural home for the fail-direction table + separate-repo runbook per D-12)
  </read_first>
  <action>
    Ship the structural leak guard re-keys (check-privacy + the check-neutrality PREDICATE) + the honest enforcement artifact set.

    (a) bin/check-privacy.sh — re-key the scan predicate (preserve the CLI surface `--root`/`--format text|json`, exit codes 0/1/2, the CI `privacy-leak` job name, and the env-export-to-python heredoc idiom). Change from "grep PUBLIC_PATHS frontmatter for `privacy: local_only`" to the structural check: "assert no `wiki-local/` content appears under PUBLIC_PATHS / would be copied into the release stage." Since `wiki-local/` is never itself in PUBLIC_PATHS, the primary guard becomes: flag any path under a PUBLIC_PATH that is inside (or copies) a `wiki-local/` tree. Keep it a THIN re-keyed guard (RESEARCH Open Q1 recommendation — preserves the branch-protection required check). Also CONFIRM `bin/release.sh` ALLOWLIST excludes `wiki-local/*` (belt-and-suspenders) — if any wiki-local path is in the allowlist, remove it.

    (b) bin/check-neutrality.sh — re-key the leak-SOURCE predicate `source_local_only_wiki()` (W1). The current function (i) walks `os.path.join(ROOT, "wiki")` and (ii) filters pages by `re.search(r"^privacy:\s*local_only\b", fm)`. After D-01 strips the `privacy` field repo-wide, that regex matches NOTHING — the neutrality guard silently loses one of its three CI-gated leak sources (this guard is invoked by neutrality.yml + setup-parity.yml; PRIV-05 forbids behavioral regression beyond the structural model swap, and A3 mis-classified this file as pure-path — it is FALSE). RE-KEY to the structural model, parallel to the privacy_resolve/check-privacy collapse: walk `os.path.join(ROOT, "wiki-local")` and treat EVERY page under it as local-only (no frontmatter predicate — the directory IS the classifier). Concretely: change `wiki_dir = os.path.join(ROOT, "wiki")` → `os.path.join(ROOT, "wiki-local")`, DELETE the `if fm and re.search(r"^privacy:\s*local_only\b", fm, re.M):` guard at line ~279 so every `.md` under `wiki-local/` contributes its tokens (keep the `os.path.isdir` early-return for the common no-local-content case, and keep tokenizing full text). Leave the `suggest()` group label (it can stay "wiki local_only" or become "wiki-local content" — your call) and the 3-source dispatch intact. DO NOT touch `source_git_history()` lines 308-309 — those hardcoded paths are pre-migration `wiki/` commit paths read via `git log --all`; they must stay literal `wiki/` (they reference history, not the working tree).

    (c) Create `.claude/settings.cloud.json` (NEW artifact) with exactly:
      `{ "permissions": { "deny": ["Read(./wiki-local/**)"] } }`
    JSON forbids comments, so document the labeling (policy/convenience, FAIL-OPEN, single-clone low-stakes only; loaded via `claude --settings ./.claude/settings.cloud.json`) in docs/reference/privacy-model.md, not inline. The deny MUST NOT be added to the committed `.claude/settings.json` (deny-beats-allow would block local sessions too — Pitfall 2). Leave `.claude/settings.json` permissive.

    (d) docs/reference/privacy-model.md — author the HONEST fail-direction table + the separate-repo fail-closed runbook (D-12/D-13). The fail-direction table must name the 4 surfaces with their TRUE direction: Read tool → DENIED (fail-open profile covers it); `cat`/`grep`/`head` via Bash → DENIED (recognized file cmds); `python3 -c open(...)` → NOT denied (fail-open gap); `git show HEAD:wiki-local/…` → NOT denied (git-object gap). State plainly: the deny-profile is convenience/fail-open; the separate-repo regime (D-13: wiki-local/ as its own gitignored nested repo with its own private remote) is the fail-closed guarantee; the OS sandbox is the OS-level fail-closed alternative. Document the separate-repo runbook (when real local_only content arrives) and note it PRESERVES the unified Obsidian graph (D-14). Add the forward-reference that the init-wizard interactive privacy-tier prompt is deferred to Phase D (per CONTEXT deferred). MUST NOT claim sparse-worktree or the settings-deny makes track-in-main fail-closed (the false-assurance trap).

    (e) tests/phase-15/test_cloud_deny_profile.sh + test_check_privacy_rekey.sh + test_neutrality_leak_source_rekey.sh — implement the assertions the Wave-0 stubs declared.
      - test_check_privacy_rekey.sh: build a clean fixture → check-privacy exit 0; build a fixture copying wiki-local/ content into docs/ → exit 2.
      - test_neutrality_leak_source_rekey.sh (W1 regression tripwire): (i) static-assert the dead regex is GONE — `! grep -q 'privacy:\\\\s\\*local_only' bin/check-neutrality.sh` within source_local_only_wiki (or repo-wide, since Plan 01 re-keyed the comments); (ii) behavioral: in a bare-repo fixture, drop a `wiki-local/sources/x.md` page carrying a personal term AND copy that page's term into a PUBLIC path (e.g. docs/) → assert `bin/check-neutrality.sh` (the leak-source suggest/scan path that consumes source_local_only_wiki) still SURFACES the wiki-local/ page as a leak source (i.e. the term is caught) — proving the guard did NOT silently drop its wiki-local leak source.
      - test_cloud_deny_profile.sh: static-assert settings.cloud.json shape (`deny == ["Read(./wiki-local/**)"]`) + the fail-direction table presence in privacy-model.md (grep fail-open / git show / python). Per RESEARCH A2, SPIKE the headless `claude -p --settings ./.claude/settings.cloud.json` three-surface behavioral assertion (Read DENIED, Bash cat DENIED, git show NOT denied); if the headless assertion is brittle/non-deterministic in this harness, fall back to the static-assertion + the documented manual runbook (VALIDATION.md Manual-Only row) and leave a clearly-commented `# MANUAL:` block — either path satisfies Success Criterion #3 (concrete artifact) + D-12.4 (regression tripwire exists).
  </action>
  <verify>
    <automated>bash tests/phase-15/test_check_privacy_rekey.sh && bash tests/phase-15/test_neutrality_leak_source_rekey.sh && bash tests/phase-15/test_cloud_deny_profile.sh && python3 -c "import json; d=json.load(open('.claude/settings.cloud.json')); assert d['permissions']['deny']==['Read(./wiki-local/**)'], d; print('deny-profile OK')" && grep -qi 'fail-open' docs/reference/privacy-model.md && grep -qi 'git show' docs/reference/privacy-model.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .claude/settings.cloud.json` exits 0 and the python json-load assertion above prints "deny-profile OK"
    - `! grep -q 'wiki-local' .claude/settings.json` (deny NOT in the committed permissive profile — Pitfall 2)
    - `grep -qi 'fail-open' docs/reference/privacy-model.md && grep -qi 'fail-closed' docs/reference/privacy-model.md` exits 0
    - `grep -qi 'git show' docs/reference/privacy-model.md && grep -qi 'separate.repo' docs/reference/privacy-model.md` exits 0 (object-leak gap + fail-closed regime documented)
    - `bash tests/phase-15/test_check_privacy_rekey.sh` exits 0 (structural leak guard)
    - check-neutrality predicate re-keyed (W1): `grep -q 'wiki-local' bin/check-neutrality.sh` exits 0 AND `! grep -q 'privacy:\\\\s\\*local_only' bin/check-neutrality.sh` (dead frontmatter regex gone from the leak-source function) AND `bash tests/phase-15/test_neutrality_leak_source_rekey.sh` exits 0 (a wiki-local/ page in a public path is still caught)
    - check-neutrality git-history paths preserved: `grep -q 'wiki/overviews/personal-decision-patterns.md' bin/check-neutrality.sh` exits 0 (lines 308-309 pre-migration paths untouched)
    - `bash tests/phase-15/test_cloud_deny_profile.sh` exits 0
    - `! grep -q 'wiki-local' bin/release.sh` (allowlist excludes local tier by absence)
  </acceptance_criteria>
  <done>check-privacy re-keyed; check-neutrality leak-source predicate re-keyed (W1 — guard keeps all three leak sources); cloud deny-profile shipped + honestly labeled; fail-direction table + separate-repo runbook in docs; PRIV-03/PRIV-05 tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cloud session → wiki-local/ git objects | The git-object store is reachable regardless of working-tree/sparse state — the residual fail-open surface this plan must label honestly, never paper over |
| cloud session → wiki-local/ via Bash subprocess | `python -c open()` / arbitrary subprocesses bypass the Read-tool deny — the second residual fail-open surface |
| wiki-local/ content → PUBLIC_PATHS / release stage | check-privacy.sh + check-neutrality.sh leak-source scan + release allowlist are the guards preventing local content reaching template-public surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-leak | Information disclosure | Cloud session reading wiki-local/ (the irreversible-leak gap this phase exists to close) | mitigate (partial) + accept (residual) | Mitigate the Read-tool + recognized-Bash-file-cmd surface via the .claude/settings.cloud.json deny; ACCEPT + DOCUMENT the residual python/subprocess + git-object fail-open surfaces in the honest fail-direction table; the FAIL-CLOSED guarantee is the separate-repo regime (D-13), documented as the path for real local_only content. The verification test PINS current Read/Bash behavior so a future harness change that loosens it is caught. |
| T-15-leak-2 | Information disclosure | False assurance — docs claiming track-in-main is fail-closed | mitigate | D-12: docs MUST label the deny-profile fail-open and name the separate-repo regime as the only fail-closed path; acceptance criteria grep for 'fail-open' + 'git show' + 'separate-repo'; MUST NOT ship the sparse-worktree-is-safe claim |
| T-15-leak-3 | Information disclosure | wiki-local/ content leaking into PUBLIC_PATHS / the public release | mitigate | check-privacy.sh re-keyed to flag wiki-local/ under PUBLIC_PATHS; release allowlist excludes wiki-local/ by absence (belt-and-suspenders); both asserted in acceptance criteria |
| T-15-leak-4 | Information disclosure | Neutrality guard silently losing its wiki-local leak source (the W1 regression): A3 mis-classified check-neutrality.sh as pure-path, so a mechanical-only re-key leaves the `privacy: local_only` regex matching nothing → one of three CI-gated leak sources (neutrality.yml + setup-parity.yml) goes dark, letting personal terms reach template-public paths undetected | mitigate | W1 predicate re-key: source_local_only_wiki() walks `wiki-local/` by path prefix (the dir IS the classifier) and drops the dead frontmatter regex; test_neutrality_leak_source_rekey.sh is a regression tripwire asserting (a) the old regex is gone AND (b) a wiki-local/ page whose term reaches a public path is still SURFACED; the predicate re-key depends_on [01] so it lands after the field strip (never silently dropped) |
| T-15-05 | Tampering | cloud→local wikilink (would break for cloud sessions + leak the existence of private pages) | mitigate | D-09 linkres check emits an error on any wiki-cloud→wiki-local link; test_lint_xtier_link asserts exit 1 |
| T-15-06 | Spoofing | A regression in the audit FAITH-04 partition silently un-withholding local claims | mitigate | phase-13 audit suite re-run green is an acceptance criterion; the partition control-flow is preserved, only the predicate swaps |
</threat_model>

<verification>
- `bash tests/phase-15/test_lint_xtier_link.sh test_check_privacy_rekey.sh test_neutrality_leak_source_rekey.sh test_cloud_deny_profile.sh` — all GREEN
- `.claude/settings.cloud.json` exists with the exact deny entry; `.claude/settings.json` stays permissive (no wiki-local deny)
- `docs/reference/privacy-model.md` carries the honest fail-direction table (4 surfaces) + separate-repo fail-closed runbook
- check-neutrality.sh leak-source predicate re-keyed: dead `privacy: local_only` regex gone; `wiki-local/` path-prefix walk live; the git-history paths (lines 308-309) preserved literal
- phase-13 audit suite re-run green (FAITH-04 no regression)
- `for t in tests/phase-15/test_*.sh; do bash "$t"; done` — full phase-15 suite GREEN
- `bash bin/lint.sh wiki-cloud/` exits 0 (D-09 check does not false-fire on the real tree)
</verification>

<success_criteria>
- D-09 cloud→local link check live in lint (error severity); LINT_VERSION minor-bumped
- check-privacy.sh re-keyed to the structural model; check-neutrality.sh leak-source predicate re-keyed (W1 — three leak sources intact); privacy_resolve collapsed to the single predicate; audit FAITH-04 re-keyed with no regression
- Concrete enforcement artifact set shipped: .claude/settings.cloud.json (fail-open, labeled) + honest fail-direction table + separate-repo fail-closed runbook
- No false assurance: every enforcement claim labeled with its true fail direction (D-12)
- PRIV-03 + PRIV-05 tests green
</success_criteria>

<output>
After completion, create `.planning/phases/15-privacy-architecture/15-02-SUMMARY.md`
</output>
