---
phase: 15-privacy-architecture
plan: 01
type: execute
wave: 1
depends_on: [00]
files_modified:
  - CLAUDE.md
  - AGENTS.md
  - schema/AGENTS.template.md
  - schema/fixtures/canonical-AGENTS.md
  - schema/templates/entity.md
  - schema/templates/concept.md
  - schema/templates/comparison.md
  - schema/templates/overview.md
  - schema/templates/source-summary.md
  - schema/templates/decision.md
  - schema/examples/entity.md
  - schema/examples/concept.md
  - schema/examples/comparison.md
  - schema/examples/overview.md
  - schema/examples/source-summary.md
  - schema/examples/decision.md
  - bin/lint.sh
  - bin/init-wizard.sh
  - bin/brownfield.sh
  - bin/release.sh
  - bin/audit-claims.sh
  - bin/search.sh
  - bin/ingest.sh
  - bin/validate-op.sh
  - bin/check-neutrality.sh
  - bin/check-privacy.sh
  - bin/lib/privacy_resolve.py
  - .github/workflows/lint.yml
  - tests/phase-13/test_privacy_resolve_precedence.sh
  - tests/phase-13/test_claim_page_privacy.sh
  - tests/phase-13/test_raw_source_privacy.sh
  - wiki-cloud/
  - wiki-local/
  - tests/
autonomous: true
requirements: [PRIV-01, PRIV-02, PRIV-04, PRIV-05, PRIV-06, PRIV-07]
must_haves:
  truths:
    - "wiki/ no longer exists; wiki-cloud/ holds the 54 content pages; wiki-local/maintenance/ holds the 2 audit files"
    - "No page in wiki-cloud/ or wiki-local/ carries a privacy frontmatter key; the generated audit-state/report (bin/audit-claims.sh) and lint-report (bin/lint.sh) templates emit NO privacy: key (review HIGH #4)"
    - "bin/lint.sh BASE_FIELDS no longer lists 'privacy' and the VALID_PRIVACY enum check is removed — stripped pages do NOT yaml-error (review HIGH #5; makes the D-02 lockstep invariant satisfiable)"
    - "CLAUDE.md §13 is the asymmetric two-dir model; the 7-row precedence table is removed (not relocated); §13 resident core is a one-line structural pointer and the fail-closed/3-level-precedence/inheritance machinery is REMOVED not relocated — privacy leaves the always-loaded MUST-list (D-16/PRIV-07)"
    - "CLAUDE.md ≡ AGENTS.md byte-equal; canonical-AGENTS.md regenerated; wizard byte-equality test green"
    - "ALL privacy PREDICATE re-keys land in THIS lockstep commit (review HIGH #6 D-02 atomicity): privacy_resolve collapsed to the single structural predicate, bin/check-neutrality.sh source_local_only_wiki() re-keyed to the wiki-local/ path-prefix walk, bin/audit-claims.sh FAITH-04 chokepoint re-keyed, bin/check-privacy.sh re-keyed structural — NO committed intermediate where any privacy guard is dead"
    - "bin/audit-claims.sh writes audit-state/report to wiki-local/maintenance/ (NOT wiki-cloud/) — the control-plane stays local (review HIGH #3 / PRIV-01 / D-08)"
    - "source_registry stores the source SUMMARY repo path (summary_rel_path), and the FAITH-04 resolver call passes it so a source under wiki-local/sources/ makes its claims effective-local (review HIGH #2/#3 — no silent source-tier-privacy regression)"
    - "The raw-source structural privacy rule is DEFINED and documented (review HIGH #2): raw sources/ is cloud-safe-only; local raw sources live as their SUMMARY page under wiki-local/sources/ (the resolver keys off the summary page tier, never the raw sources/ path)"
    - "The 3 phase-13 resolver tests (test_privacy_resolve_precedence, test_claim_page_privacy, test_raw_source_privacy) are REWRITTEN to structural equivalents (or retired with replacements) so 'phase-13 audit suite green' actually holds (review HIGH #1)"
    - "bin/lint.sh wiki-cloud/ exits clean (no privacy-field yaml errors); the whole migration is ONE commit with no lint-red intermediate"
    - "The execution-time DR exists at wiki-cloud/decisions/dr-2026-06-04-privacy-asymmetric-two-dir.md (trigger_type: schema-update) recording the three weighed options (per-page mixed / per-vault fully separate / asymmetric two-dir) and why asymmetric won (D-15/PRIV-06)"
    - "Pass C is manifest-driven: each prior-phase fixture is classified (cloud page / local audit control-plane / legacy old-privacy resolver test / obsolete) and re-keyed accordingly across ALL 9 phase dirs (94 .sh files, NOT ~40) — audit-maintenance fixtures go to wiki-local/maintenance/, not wiki-cloud/ (review HIGH #7)"
  artifacts:
    - path: "wiki-cloud/decisions/dr-2026-06-04-privacy-asymmetric-two-dir.md"
      provides: "PRIV-06 execution-time decision record"
      contains: "trigger_type: schema-update"
    - path: "wiki-local/maintenance/audit-state.md"
      provides: "Relocated audit control-plane (local tier)"
    - path: "wiki-cloud/index.md"
      provides: "Cloud-tier content index (renamed from wiki/index.md)"
    - path: "CLAUDE.md"
      provides: "Rewritten §2/§3/§5/§8/§13"
      contains: "wiki-cloud"
  key_links:
    - from: "CLAUDE.md"
      to: "AGENTS.md"
      via: "bin/sync-claude.sh --check"
      pattern: "wiki-cloud"
    - from: "schema/AGENTS.template.md"
      to: "schema/fixtures/canonical-AGENTS.md"
      via: "wizard regen (tests/phase-08/test_canonical_byte_equality.sh)"
      pattern: "wiki-cloud"
    - from: "migration"
      to: "bin/lib/brownfield_yaml.py"
      via: "read_fm_body -> del fm['privacy'] -> write_roundtrip"
      pattern: "write_roundtrip"
---

<objective>
Execute the ONE SECURITY-ATOMIC LOCKSTEP COMMIT (D-02) that converts the per-page privacy model to the asymmetric two-directory model. This single atomic commit: routes-then-strips the 56 pages into `wiki-cloud/` + `wiki-local/`, rewrites CLAUDE.md/AGENTS.md §2/§3/§5/§8/§13 (+ template mirror + fixture regen), re-keys every mechanical `wiki/`→`wiki-cloud/` path string across `bin/`/CI/templates/tests, re-keys EVERY privacy PREDICATE that depends on the now-deleted field (privacy_resolve collapse, check-neutrality leak-source, check-privacy structural, audit FAITH-04), removes `privacy` from lint's required-field set + enum, strips `privacy` from the generated maintenance templates, routes the audit control-plane to `wiki-local/maintenance/`, authors the execution-time decision record, and lands the tree lint-GREEN. There must be NO intermediate commit where lint errors on the whole tree OR where any privacy guard is dead (D-02/D-03/D-05).

**Revised per cross-AI review (REVIEWS.md, 8 HIGH + 3 MEDIUM, all code-verified).** The reviewer's top finding (HIGH #6) is that the original two-plan split violated D-02 atomicity: Plan 01 committed a migrated tree while check-neutrality's leak-source predicate was knowingly dead until Plan 02 — a committed intermediate security regression. Per the reviewer's own recommendation, the PREDICATE re-keys (privacy_resolve, check-neutrality leak-source, check-privacy, audit FAITH-04) and the lint required-field/enum removal + generated-frontmatter strips are PULLED INTO this lockstep commit. Plan 02 retains ONLY the genuinely net-new / additive / non-atomic-critical artifacts (the D-09 cloud→local link check, the `.claude/settings.cloud.json` deny-profile, the fail-direction docs, the behavioral verification tests). Other addressed concerns: HIGH #1 (rewrite phase-13 resolver tests), HIGH #2/#3 (raw-source structural rule + source_registry summary path), HIGH #3 (audit control-plane → wiki-local), HIGH #4 (generated frontmatter), HIGH #5 (lint required field), HIGH #7 (manifest-driven Pass C across 9 dirs / 94 files).

Purpose: §13 must be in its rewritten asymmetric form before Phase 16 can extract it (PRIV-07 → REF-06). The migration is light on data, heavy on schema/tooling/docs path churn — and the privacy guards must never be dead at any commit boundary.
Output: Renamed two-dir tree, rewritten schema, re-keyed tooling paths AND predicates, the DR, all in one security-atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-privacy-architecture/15-CONTEXT.md
@.planning/phases/15-privacy-architecture/15-RESEARCH.md
@.planning/phases/15-privacy-architecture/15-PATTERNS.md

<interfaces>
<!-- ruamel frontmatter helper the strip MUST use (D-04, never sed). Verified signatures: -->
From bin/lib/brownfield_yaml.py:
  read_fm_body(path) -> (CommentedMap|None, body, raw_yaml_block|None)   # line 186
  write_roundtrip(path, fm, body, raw_yaml_block=None) -> None          # line 420; atomic tmp+rename, LF-normalized

<!-- Migration caller idiom (copy from this apply-class analog): -->
From schema/brownfield/migrations/02-provenance-bootstrap.sh:106-111:
  export BROWNFIELD_ROOT BROWNFIELD_LIB_DIR APPLY OP_HASH
  python3 - <<'PYEOF'
  import os, sys, pathlib
  sys.path.insert(0, os.environ['BROWNFIELD_LIB_DIR'])
  from brownfield_yaml import read_fm_body, write_roundtrip

<!-- The per-page strip (RESEARCH Pattern 1): -->
  fm, body, raw = read_fm_body(path)
  if fm is not None and 'privacy' in fm:
      del fm['privacy']
      write_roundtrip(path, fm, body, raw)

<!-- Schema anchors (CLAUDE.md line numbers, current state): -->
  §2 Directory Structure   -> line 34
  §3 MUST-NOT local_only   -> line 130 ("DO NOT send local_only content to cloud LLM APIs")
  §5 base privacy row      -> line 274 ("privacy: local_only|cloud_safe")
  §5 privacy field desc    -> line 300
  §5 checklist item #5     -> line 380 ("5. `privacy` is one of...")
  §8 Wikilink Conventions  -> line 651
  §13 Privacy Routing      -> lines 1550-1593 (7-row "Privacy Decision Table" + "Three-Level Precedence" + inheritance — ALL REMOVED)
  §14 (next section)       -> line 1594
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route-then-strip the 56 pages into wiki-cloud/ + wiki-local/ (helper also bulk-re-keys the ~40 prior-phase fixtures)</name>
  <files>wiki-cloud/ (renamed from wiki/), wiki-local/maintenance/audit-report.md, wiki-local/maintenance/audit-state.md, bin/migrate-privacy-dirs.sh (one-off helper), tests/phase-07..13/*.sh (mechanical path-default re-key, performed BY the helper — Pass C)</files>
  <read_first>
    - schema/brownfield/migrations/02-provenance-bootstrap.sh:106-179 (apply-class ruamel-mutator caller idiom — lib-resolution header + per-page loop)
    - bin/lib/brownfield_yaml.py:186,420 (read_fm_body / write_roundtrip signatures)
    - .planning/phases/15-privacy-architecture/15-PATTERNS.md lines 230-254 (route-then-strip ordering D-03, per-page mutate idiom, anti-pattern: never sed)
    - .planning/phases/15-privacy-architecture/15-RESEARCH.md "Recommended migration script structure" (Pass A/B) + "Pattern 2: Route-then-strip ordering"
    - .planning/phases/15-privacy-architecture/15-VALIDATION.md "Wave 0 Requirements" fixture-update task (the ~40 prior-phase test-fixture path updates the helper must mechanically cover — W3)
  </read_first>
  <action>
    Write a one-off migration helper (e.g. `bin/migrate-privacy-dirs.sh` or an inline python3 block — your discretion; it may be deleted post-migration or kept) that performs Pass A then Pass B per D-03 — ROUTE THEN STRIP, never strip blind — AND a Pass C that bulk-re-keys the prior-phase test fixtures so the executor never hand-edits 40 files (W3):

    Pass 0: verify clean working tree (`git status --porcelain` empty), capture HEAD.

    Pass A (ROUTE): for each `wiki/**/*.md`, read its `privacy` frontmatter value generically (do NOT hardcode the 2 known files — read the field so the script survives a vault with other local content). If `privacy: local_only` → `git mv` to the mirror path under `wiki-local/` (the 2 audit files: `wiki/maintenance/audit-report.md` → `wiki-local/maintenance/audit-report.md`, `audit-state.md` likewise). Else → `git mv` to the same relative path under `wiki-cloud/` (e.g. `wiki/concepts/X.md` → `wiki-cloud/concepts/X.md`, `wiki/index.md` → `wiki-cloud/index.md`, `wiki/log.md` → `wiki-cloud/log.md`, `wiki/maintenance/lint-report.md` → `wiki-cloud/maintenance/lint-report.md`). After Pass A the `wiki/` dir must be empty and removed.

    Pass B (STRIP): for each moved page under `wiki-cloud/` and `wiki-local/`, apply the ruamel strip (`read_fm_body` → `del fm['privacy']` if present → `write_roundtrip`). Idempotent guard `if 'privacy' in fm`. NEVER `sed` (corrupts prose `privacy:` lines — D-04).

    Pass C (MANIFEST-DRIVEN FIXTURE RE-KEY — W3; REVISED per review HIGH #7): the original blanket `wiki/`→`wiki-cloud/` rewrite is UNSAFE and UNDER-SCOPED. Code-verified scope: 94 `.sh` files across 9 phase dirs reference bare `wiki/` (phase-07:6, phase-08:6, phase-09:12, phase-09.1:3, phase-10:6, phase-11:18, phase-12.1:1, phase-12.2:11, phase-13:31) — NOT the ~40 the original estimated. A blanket re-key is wrong for two reasons: (a) some references are audit MAINTENANCE paths that must become `wiki-local/maintenance/`, NOT `wiki-cloud/maintenance/` (e.g. `tests/phase-13/test_report_privacy_local_only.sh`, `test_checkpoint_advance.sh`); (b) the 3 phase-13 resolver tests intentionally test the OLD privacy-resolver semantics and must be REWRITTEN structurally (Task 5), not mechanically re-keyed.

    The helper MUST classify each fixture before transforming. For each `tests/phase-*/*.sh` file with a bare `wiki/` reference, classify per-occurrence into one of four buckets:
      1. **cloud page reference** (`wiki/concepts/`, `wiki/index.md`, `wiki/decisions/`, `wiki/entities/`, etc.) → re-key to `wiki-cloud/`.
      2. **local audit control-plane** (`wiki/maintenance/audit-report.md`, `wiki/maintenance/audit-state.md`) → re-key to `wiki-local/maintenance/` (the audit control-plane is the LOCAL tier per D-08; lint-report stays `wiki-cloud/maintenance/`).
      3. **legacy old-privacy resolver test** (the 3 phase-13 resolver tests in Task 5) → DO NOT mechanically re-key; Task 5 rewrites them structurally.
      4. **git-history / pre-migration commit path** (a literal read via `git log --all`/`git show HEAD:` referencing a pre-rename path) → leave literal `wiki/` (it references history, not the working tree).
    Mechanical rule per occurrence: bucket 1 → `wiki-cloud/`; bucket 2 → `wiki-local/maintenance/`; buckets 3+4 → skip. Idempotent (skip lines already reading `wiki-cloud/`/`wiki-local/`). Do NOT touch `tests/phase-15/` (those already assert the new tiers). The helper emits a per-file classification manifest (which occurrences went to which bucket) so the executor reviews the CLASSIFICATION, not 94 individual edits. The bin/ + CI path-string re-keys remain Task 3.

    **Pass C acceptance widens to ALL 9 phase dirs** (the original validated only 4): after Pass C, no bucket-1/bucket-2 bare-`wiki/` occurrence remains in any of phase-07/08/09/09.1/10/11/12.1/12.2/13 (allowing bucket-3/bucket-4 literals).

    Do NOT touch `examples/` page moves here (examples stay in place; their `privacy` strip is Task 4). Do NOT edit `.obsidian/` (workspace.json self-heals; dataview main.js strings are coincidental — Pitfall 5).
  </action>
  <verify>
    <automated>test ! -d wiki && test -d wiki-cloud && test -f wiki-local/maintenance/audit-state.md && test -f wiki-local/maintenance/audit-report.md && python3 -c "import glob,sys; sys.path.insert(0,'bin/lib'); from brownfield_yaml import read_fm_body; bad=[p for p in glob.glob('wiki-cloud/**/*.md',recursive=True)+glob.glob('wiki-local/**/*.md',recursive=True) if (read_fm_body(p)[0] or {}).get('privacy') is not None]; print('PAGES_WITH_PRIVACY:',bad); sys.exit(1 if bad else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -d wiki` exits 0 (old dir gone)
    - `find wiki-cloud -name '*.md' | wc -l` == 54
    - `test -f wiki-local/maintenance/audit-report.md && test -f wiki-local/maintenance/audit-state.md` exits 0
    - Zero pages under wiki-cloud/ or wiki-local/ retain a `privacy` frontmatter key (the python3 check above exits 0)
    - `git status --porcelain | grep -c '^R'` shows renames (git mv preserved history)
    - Pass C ran across ALL 9 phase dirs (review HIGH #7): for d in phase-07 phase-08 phase-09 phase-09.1 phase-10 phase-11 phase-12.1 phase-12.2 phase-13, every bucket-1/bucket-2 bare-`wiki/` occurrence is re-keyed (the helper emits a classification manifest; bucket-3 = the 3 phase-13 resolver tests deferred to Task 5; bucket-4 = git-history literals stay). Spot-check: `grep -rl 'wiki-local/maintenance' tests/phase-13/` shows the audit-maintenance fixtures (e.g. test_report_privacy_local_only.sh, test_checkpoint_advance.sh) routed LOCAL, not cloud.
    - The 3 phase-13 resolver tests are NOT mechanically re-keyed here (Task 5 owns their structural rewrite): the helper's manifest classifies them bucket-3.
  </acceptance_criteria>
  <done>56 pages routed by privacy value then stripped of the field; wiki/ removed; 54 cloud + 2 local; the prior-phase fixtures (94 .sh across 9 dirs) manifest-classified and re-keyed by bucket (cloud→wiki-cloud, audit-maintenance→wiki-local, resolver-tests deferred to Task 5, git-history literals preserved).</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite CLAUDE.md/AGENTS.md §2/§3/§5/§8/§13; sync; regen fixture</name>
  <files>CLAUDE.md, AGENTS.md, schema/AGENTS.template.md, schema/fixtures/canonical-AGENTS.md</files>
  <read_first>
    - CLAUDE.md:34-120 (§2 Directory Structure tree + permitted-dirs + source/wiki dir rules — rewrite to wiki-cloud/ + wiki-local/)
    - CLAUDE.md:130 (§3 MUST-NOT local_only line — reconcile to structural)
    - CLAUDE.md:252-385 (§5 base block line 274, field desc line 300, checklist item #5 line 380 — REMOVE privacy)
    - CLAUDE.md:651-720 (§8 Wikilink Conventions — add cloud→local forbidden rule D-09)
    - CLAUDE.md:1550-1593 (§13 — full rewrite; REMOVE 7-row table + Three-Level Precedence + inheritance)
    - .planning/phases/15-privacy-architecture/15-RESEARCH.md "§13 Rewrite Content Map (PRIV-02)" (the 8-point content map — authoritative)
    - .planning/phases/15-privacy-architecture/15-CONTEXT.md D-16 (the one-line pointer wording)
    - bin/sync-claude.sh:1-30 (the --check byte-equality contract)
  </read_first>
  <action>
    Rewrite the schema in CLAUDE.md (then sync to AGENTS.md + mirror to schema/AGENTS.template.md). Apply ALL edits in BOTH CLAUDE.md and AGENTS.md and the template — they must stay byte-equal.

    §2 Directory Structure: rewrite the tree from `wiki/` to `wiki-cloud/` (the cloud-safe tier, six page-type subdirs + index.md + log.md + maintenance/) and add `wiki-local/` (local-only tier; mirrors the same six page types + maintenance/; subdirs created on demand per D-06; index.md/log.md created lazily per D-07; creator vault starts as just `wiki-local/maintenance/{audit-report,audit-state}.md` per D-08). Update Permitted top-level directories list (`wiki/` → `wiki-cloud/` + `wiki-local/`). Update the "Wiki directory rules" subsection. State the per-tier index.md/log.md partition (D-07) and the mirror-convention (D-06).

    §3 "What Agents Must NOT Do": rewrite line 130 from "DO NOT send local_only content to cloud LLM APIs" to the structural form: "DO NOT read `wiki-local/` from a cloud session — the tier boundary is structural (directory + harness permission), not a per-turn rule. local→cloud is the forbidden leak direction." Reconcile the line-132 neutrality rule's `privacy: local_only` reference to `wiki-local/` paths.

    §5 Frontmatter Schema: DELETE the `privacy:` base-field line (274), its field-description row (300), and validation-checklist item #5 (380) — RENUMBER the checklist. Remove `privacy` from the BASE_FIELDS narrative everywhere it's listed in §5.

    §8 Wikilink Conventions: add the asymmetric cross-tier link rule (D-09): `wiki-local`→`wiki-cloud` links are fine; `wiki-cloud`→`wiki-local` links are FORBIDDEN (they break for cloud sessions AND leak the existence of private pages). Note this is lint-enforced (linkres).

    §13 Privacy Routing — FULL REWRITE per the RESEARCH §13 Rewrite Content Map:
      1. State the asymmetric model: two dirs `wiki-cloud/` (cloud-safe) + `wiki-local/` (local-only), one unified Obsidian vault; one-way permeability (local runs read both; cloud runs MUST NOT read `wiki-local/`); local→cloud is the forbidden leak.
      2. REMOVE the 7-row "Privacy Decision Table", the "Three-Level Precedence" narrative, the conflict-resolution/stricter-wins block, and the wiki-page inheritance block (D-16: removed, NOT relocated).
      3. State enforcement HONESTLY: the cloud deny-profile (`--settings .claude/settings.cloud.json`) is convenience/FAIL-OPEN; OS sandbox or separate-repo (D-13) is the FAIL-CLOSED guarantee. Reference the fail-direction table in `docs/reference/privacy-model.md` (authored in Plan 02).
      Then reduce the RESIDENT §13 core obligation to a ONE-LINE structural pointer (D-16/PRIV-07): e.g. "Vault tier is structural; cloud sessions cannot read `wiki-local/` — see `docs/reference/privacy-model.md`." Privacy leaves the always-loaded MUST-list. (Note: full §13→schema/reference/privacy.md extraction is Phase 16; here §13 is rewritten in place to its asymmetric form with the machinery removed.)

      Also reconcile the §11.2 / §9 privacy-inheritance prose (lines ~788, 793, 990-996) and §11.7/FAITH-04 (lines 1440-1444) wording to the structural model — here align the PROSE; the §11.7 effective-privacy PREDICATE logic re-key is Task 6 of THIS plan (same security-atomic commit — review HIGH #6), so the §13/§11.7 prose and the predicate land together and cannot contradict.

    Then run `bash bin/sync-claude.sh` to sync AGENTS.md, regenerate `schema/fixtures/canonical-AGENTS.md` via the Phase-08 wizard-render routine (pinned WIZARD_GENERATED_AT / WIZARD_TEMPLATE_SHA per the 11-05 precedent), and confirm `bash bin/sync-claude.sh --check` exits 0.
  </action>
  <verify>
    <automated>bash bin/sync-claude.sh --check && bash tests/phase-15/test_schema_13_rewritten.sh && bash tests/phase-15/test_priv_resident_reduced.sh && bash tests/phase-15/test_privacy_field_stripped.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash bin/sync-claude.sh --check` exits 0 (CLAUDE.md ≡ AGENTS.md)
    - `! grep -q 'Privacy Decision Table' CLAUDE.md` (7-row table removed)
    - `! grep -q 'Three-Level Precedence' CLAUDE.md` (precedence narrative removed)
    - `grep -qi 'wiki-local' CLAUDE.md && grep -qi 'asymmetric\|one-way' CLAUDE.md` exits 0
    - `! grep -q '^privacy: local_only|cloud_safe' CLAUDE.md` (§5 base row removed)
    - `bash tests/phase-15/test_schema_13_rewritten.sh` exits 0
    - `bash tests/phase-15/test_priv_resident_reduced.sh` exits 0
  </acceptance_criteria>
  <done>Schema rewritten in CLAUDE.md+AGENTS.md+template; fixture regenerated; byte-equal; PRIV-02/04/07 schema tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Re-key mechanical wiki/ → wiki-cloud/ path strings in bin/ and CI; route audit control-plane to wiki-local/; remove privacy from lint required-field + enum; strip privacy from generated maintenance templates</name>
  <files>bin/lint.sh, bin/init-wizard.sh, bin/brownfield.sh, bin/release.sh, bin/audit-claims.sh, bin/search.sh, bin/ingest.sh, bin/validate-op.sh, bin/check-neutrality.sh, .github/workflows/lint.yml</files>
  <read_first>
    - .planning/phases/15-privacy-architecture/15-RESEARCH.md "Path-Reference Inventory (D-05)" (the full lockstep surface with per-file counts + the ^wiki/-anchor caveat A3 — A3 CORRECTED: check-neutrality.sh carries a PREDICATE re-keyed in Task 6, NOT pure-path)
    - bin/lint.sh:94,185,217,228,249,255,277,435 (WIKI_DIR default + EXCLUDE_DIRS + report path — re-key default to wiki-cloud/)
    - bin/lint.sh:375,378-383,1063-1067 (VALID_PRIVACY set line 375 + BASE_FIELDS list line 381 including 'privacy' + the enum validation at 1065-1066 — REMOVE privacy from both, review HIGH #5)
    - bin/lint.sh:2430-2436 (the lint-report template heredoc emitting `privacy: cloud_safe` at line ~2434 — STRIP, review HIGH #4)
    - bin/release.sh:18-52 (ALLOWLIST entries wiki/index.md, wiki/log.md, wiki/decisions, wiki/{entities,...} → wiki-cloud/*; confirm wiki-local/ NOT in allowlist)
    - bin/audit-claims.sh:156,227-229,506,538,713,835-837 (WIKI_DIR base + the walk + git diff -- wiki/ scope; CRITICAL review HIGH #3: state_path/report default at :538/:713/:837 must route to wiki-local/maintenance/, NOT wiki-cloud/)
    - bin/audit-claims.sh:860-940 (the audit-state + audit-report template heredocs emitting `privacy: local_only` at lines ~863, ~936 — STRIP, review HIGH #4)
    - bin/check-neutrality.sh:8,47,94 (doc-comment "wiki/** pages" refs at lines 8/47 + PUBLIC_PATHS line-94 `wiki` entry — MECHANICAL path re-key here; the source_local_only_wiki() leak-source PREDICATE at lines 262-283 is Task 6 of THIS plan; the lines 308-309 git-history paths stay literal `wiki/`)
    - .github/workflows/lint.yml lines ~55-56 (privacy-leak job wiki/** exclusion comment)
  </read_first>
  <action>
    Re-key the mechanical `wiki/` → `wiki-cloud/` PATH STRINGS, route the audit control-plane LOCAL, and make the lint-required-field + generated-frontmatter changes that the D-02 lockstep invariant requires (the privacy PREDICATE re-keys for privacy_resolve/check-neutrality-leak-source/check-privacy/audit-FAITH-04 are Task 6 of THIS plan — same commit, review HIGH #6). Per RESEARCH A3, grep each file in context before rewriting; watch for `^wiki/`-anchored regexes that would break against `wiki-cloud/`. (The prior-phase fixture re-keys are handled by the Task 1 helper's manifest-driven Pass C.)

    bin/lint.sh:
      (path) `WIKI_DIR="${WIKI_ROOT:-wiki/}"` (line 94) → `wiki-cloud/`; the `wiki/{entities,concepts,...}` prefix checks; the `git diff -- wiki/` scopes; the `wiki/maintenance/lint-report.md` report path. (The D-09 net-new linkres check is Plan 02 — leave the linkres LOGIC alone here, only fix path strings.)
      (required-field, review HIGH #5) REMOVE `'privacy'` from the BASE_FIELDS list (line ~381) so a stripped page no longer yaml-errors "missing required field: privacy". REMOVE the VALID_PRIVACY enum check at lines ~1065-1066 (`if 'privacy' in fm and fm['privacy'] not in VALID_PRIVACY: add_finding('error','yaml',...)`) and the `VALID_PRIVACY = {...}` definition at line ~375 (now unused). Without this, stripping 54 pages produces 54 yaml errors and the D-02 lockstep "no lint-red intermediate" invariant is unsatisfiable.
      (generated-frontmatter, review HIGH #4) STRIP `privacy: cloud_safe` from the lint-report template heredoc (line ~2434) so the next lint run does NOT re-introduce the field on `wiki-cloud/maintenance/lint-report.md`.

    bin/init-wizard.sh (22 refs): scaffold emits `wiki/` dir + index/log creation → `wiki-cloud/`; document the `wiki-local/` lazy-mirror. (Coordinate-with-Phase-D note: leave a comment that the interactive privacy-tier prompt is deferred to Phase D.)

    bin/brownfield.sh (11), bin/search.sh (5), bin/ingest.sh, bin/validate-op.sh: re-key path defaults `wiki/` → `wiki-cloud/`. Verify each in context (A3).

    bin/check-neutrality.sh — MECHANICAL path re-key ONLY here (the leak-source PREDICATE is Task 6 of THIS plan):
      (i) doc comments at lines 8 + 47: re-point the `wiki/` path token (note wiki-cloud is the public scan target). Task 6 finalizes the predicate-semantics wording.
      (ii) PUBLIC_PATHS line 94: re-key `wiki` → `wiki-cloud` (the cloud-safe tier is the public surface; `wiki-local` MUST NOT be added — it is the leak SOURCE, not a public target).
      (iii) DO NOT touch `source_local_only_wiki()` (lines 262-283) here — Task 6 of THIS plan re-keys its walk-dir + drops the dead regex.
      (iv) DO NOT touch the lines 308-309 git-history paths — pre-migration commit paths read via `git log --all`; stay literal `wiki/`.

    bin/release.sh (9): ALLOWLIST `wiki/*` → `wiki-cloud/*`; CONFIRM `wiki-local/` is NOT added (excluded by absence per D-13).

    bin/audit-claims.sh:
      (path) `WIKI_DIR = os.path.join(REPO_ROOT, 'wiki')` (line ~156) + the `git diff -- wiki/` scope (~506) → wiki-cloud/ for the CLAIM-PAGE walk (the audit reads claims from the cloud tier + local tier).
      (CONTROL-PLANE ROUTING, review HIGH #3 / PRIV-01 / D-08) CRITICAL: the audit-state + audit-report are LOCAL control-plane. Route `state_path` (line ~538), the report default `'path': 'wiki/maintenance/audit-report.md'` (line ~713), and the maint/state_path derivation (lines ~835-837) to `wiki-local/maintenance/`, NOT `wiki-cloud/maintenance/`. A blanket `wiki/`→`wiki-cloud/` re-key would regenerate the local-only audit control-plane on the CLOUD side — a privacy violation. Use a SEPARATE `LOCAL_MAINT = os.path.join(REPO_ROOT, 'wiki-local', 'maintenance')` for these three sites; keep the claim-walk root on `wiki-cloud/` (plus a `wiki-local/` walk per the two-tier universe in Task 6).
      (generated-frontmatter, review HIGH #4) STRIP `privacy: local_only` from the audit-state template (line ~863) and the audit-report template (line ~936) heredocs. The control-plane is local BY DIRECTORY now (it lives under `wiki-local/`), so the field is both redundant and a re-introduction of the stripped key.

    .github/workflows/lint.yml: re-key the `privacy-leak` job comment referencing `wiki/**` and any path args.

    Do NOT edit `.obsidian/workspace.json` or `.obsidian/plugins/dataview/main.js` (Pitfall 5).
  </action>
  <verify>
    <automated>grep -rIl --include='*.sh' --include='*.yml' -e 'wiki/' bin/ .github/ 2>/dev/null | grep -v 'wiki-cloud\|wiki-local' | sed 's/^/RESIDUAL: /'; echo "scan-done"; grep -n "wiki-local/maintenance\|wiki-local', 'maintenance" bin/audit-claims.sh | head</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "WIKI_ROOT:-wiki-cloud/" bin/lint.sh` exits 0 (default re-keyed)
    - lint required-field removed (review HIGH #5): `! grep -nE "'privacy'" bin/lint.sh | grep -q BASE_FIELDS` — i.e. the BASE_FIELDS list no longer contains 'privacy' AND `! grep -q 'Invalid privacy' bin/lint.sh` AND `! grep -q 'VALID_PRIVACY' bin/lint.sh`; `bash tests/phase-15/test_lint_required_field_dropped.sh` exits 0
    - lint-report generated frontmatter clean (review HIGH #4): `! grep -nE '^privacy: cloud_safe' bin/lint.sh` (the line ~2434 template no longer emits the field)
    - audit control-plane routed LOCAL (review HIGH #3): `grep -q "wiki-local" bin/audit-claims.sh` exits 0 AND the state/report default paths resolve under `wiki-local/maintenance/` — `grep -nE "wiki-local.{0,12}maintenance" bin/audit-claims.sh` shows the state + report sites; `! grep -nE "wiki-cloud/maintenance/audit-(state|report)" bin/audit-claims.sh` (audit control-plane is NOT on the cloud tier)
    - audit generated frontmatter clean (review HIGH #4): `! grep -nE '^privacy: local_only' bin/audit-claims.sh` within the audit-state/report templates (lines ~863/~936 no longer emit it); `bash tests/phase-15/test_generated_frontmatter_clean.sh` exits 0
    - `! grep -E '\bwiki/' bin/release.sh` (allowlist re-keyed; no bare wiki/ path)
    - `! grep -q 'wiki-local' bin/release.sh` (local tier excluded by absence)
    - bin/check-neutrality.sh PUBLIC_PATHS re-keyed: `grep -qE 'PUBLIC_PATHS=.*wiki-cloud' bin/check-neutrality.sh` exits 0 AND `! grep -qE 'PUBLIC_PATHS=\(.*[ (]wiki[ )]' bin/check-neutrality.sh` (bare `wiki` PUBLIC entry replaced; `wiki-local` NOT added)
    - bin/check-neutrality.sh predicate re-key deferred to Task 6 of THIS plan: `grep -q 'def source_local_only_wiki' bin/check-neutrality.sh` exits 0 (function present; its walk-dir + regex re-key happens in Task 6, same commit)
    - `.obsidian/` untouched: `git diff --name-only | grep -c '.obsidian'` == 0
  </acceptance_criteria>
  <done>Mechanical wiki/ path strings in bin/ + CI re-keyed; audit control-plane routed to wiki-local/maintenance/ (review HIGH #3); privacy removed from lint BASE_FIELDS + enum (HIGH #5); generated audit/lint templates strip privacy (HIGH #4); check-neutrality PUBLIC_PATHS + comments re-keyed (its leak-source predicate is Task 6, same commit); .obsidian/ untouched.</done>
</task>

<task type="auto">
  <name>Task 5: Rewrite the 3 phase-13 resolver tests to structural equivalents (review HIGH #1)</name>
  <files>tests/phase-13/test_privacy_resolve_precedence.sh, tests/phase-13/test_claim_page_privacy.sh, tests/phase-13/test_raw_source_privacy.sh</files>
  <read_first>
    - tests/phase-13/test_privacy_resolve_precedence.sh (FULL — it calls `resolve_source_privacy` with `{'privacy': ...}` dicts and asserts the 7-row Decision Table; ALL of these assertions are deleted by the collapse and MUST be replaced with structural equivalents)
    - tests/phase-13/test_claim_page_privacy.sh:1-30 (the resolver-direct-call + the FAITH-04 withhold behavioral test — the HIGH-A row asserts `resolve_effective_claim_privacy({'privacy':'local_only'}, 'wiki/concepts/x.md', ...) == 'local_only'`; rewrite to the path-prefix contract: a page under `wiki-local/` is local)
    - tests/phase-13/test_raw_source_privacy.sh:1-40 (the raw-source-folds-in test — the load-bearing review HIGH #2 case: a cloud page citing a cloud summary whose RAW source says local_only currently folds to local. Under the structural rule raw `sources/` is cloud-safe-only, so this test is REFRAMED: a cloud page citing a source whose SUMMARY lives under `wiki-local/sources/` resolves local)
    - bin/lib/privacy_resolve.py:37-87 (the resolver being collapsed in Task 6 — its NEW path-prefix contract is what these tests now assert)
    - .planning/phases/15-privacy-architecture/15-CONTEXT.md specifics (FAITH-04 single predicate) + the raw-source structural rule defined in Task 6
  </read_first>
  <action>
    Rewrite (do NOT mechanically path-re-key) the 3 phase-13 tests that unit-assert the DELETED 3-level/stricter-wins resolver semantics, so "phase-13 audit suite green" (the Plan 02 / phase-gate acceptance) actually holds. These tests pass `privacy` frontmatter dicts directly into `resolve_source_privacy` / `resolve_effective_claim_privacy` and assert precedence outcomes the collapse removes (code-verified). They must be REWRITTEN to the structural path-prefix contract that Task 6 ships:

    - `test_privacy_resolve_precedence.sh`: the 7-row Decision-Table assertions (`r({'privacy':'cloud_safe'}, 'sources/cloud-safe/x.md') == 'cloud_safe'`, etc.) are GONE. Replace with structural assertions over the new contract: a source SUMMARY path under `wiki-local/` → `local_only`; a summary under `wiki-cloud/` → `cloud_safe`; raw `sources/` paths are cloud-safe-only (the raw path is NEVER the local signal — the SUMMARY tier is). Keep the file as a phase-13 test (it is a phase-13 acceptance gate) but assert the post-collapse behavior. Update the header comment from "honors §13 three-level precedence" to the structural contract.
    - `test_claim_page_privacy.sh`: keep the FAITH-04-withhold behavioral half (a local CLAIM is withheld from cloud-facing worklist without `--allow-local`) but re-fixture it: the page that drives "local" is now a page UNDER `wiki-local/` (not a page carrying `privacy: local_only`). Rewrite the direct resolver unit assertion to `resolve_effective_claim_privacy(...page under wiki-local/...) == 'local_only'`.
    - `test_raw_source_privacy.sh`: REFRAME to the structural rule. The old case (raw-source frontmatter `privacy: local_only` folds in) is gone with the field. The replacement asserts the SOURCE-TIER privacy that review HIGH #2 demands survives: a cloud page citing a source whose SUMMARY page lives under `wiki-local/sources/` → effective `local_only` (and is withheld). This is the behavioral twin of Wave-0's `test_resolver_structural.sh` case (ii).

    IMPORTANT: these are bucket-3 in Task 1's Pass C manifest (NOT mechanically re-keyed). They land in THIS lockstep commit alongside the Task 6 resolver collapse, so the phase-13 suite is green at the commit boundary (no intermediate red).
  </action>
  <verify>
    <automated>for t in test_privacy_resolve_precedence test_claim_page_privacy test_raw_source_privacy; do bash tests/phase-13/$t.sh; echo "$t exit=$?"; done</automated>
  </verify>
  <acceptance_criteria>
    - The 3 tests no longer pass `{'privacy': ...}` precedence dicts: `! grep -q "sources/cloud-safe\|sources/local-only" tests/phase-13/test_privacy_resolve_precedence.sh` (old Decision-Table dir signals gone)
    - `grep -q 'wiki-local' tests/phase-13/test_raw_source_privacy.sh` exits 0 (reframed to the source-SUMMARY-tier rule)
    - All 3 exit 0 AFTER Task 6 lands (run in Task 4(c)): `for t in test_privacy_resolve_precedence test_claim_page_privacy test_raw_source_privacy; do bash tests/phase-13/$t.sh || exit 1; done`
  </acceptance_criteria>
  <done>The 3 phase-13 resolver tests rewritten to the structural path-prefix contract; "phase-13 audit suite green" is now actually achievable (review HIGH #1).</done>
</task>

<task type="auto">
  <name>Task 6: Collapse privacy_resolve + re-key check-neutrality leak-source + check-privacy + audit FAITH-04 (the PREDICATE re-keys, IN the lockstep commit — review HIGH #6); define the raw-source structural rule + store source-summary repo path (review HIGH #2/#3)</name>
  <files>bin/lib/privacy_resolve.py, bin/check-neutrality.sh, bin/check-privacy.sh, bin/audit-claims.sh</files>
  <read_first>
    - bin/lib/privacy_resolve.py:37-87 (resolve_source_privacy + resolve_effective_claim_privacy — the 3-level ladder being COLLAPSED to the single structural predicate; do NOT port the ladder)
    - bin/audit-claims.sh:140-142,246-250,462,526,743-762 (the import; source_registry storage `source_registry[sfm['id']] = sfm` at :250 — stores ONLY frontmatter, NOT a repo path; the FAITH-04 call at :745 passes `(sfm or {}).get('path','')` which is a RAW `sources/...` path, NOT a summary path — review HIGH #2/#3)
    - bin/check-neutrality.sh:262-283 (source_local_only_wiki() — walk_dir `os.path.join(ROOT, "wiki")` at :265 + the DEAD `re.search(r"^privacy:\s*local_only\b", fm)` at :279) + bin/check-neutrality.sh:305-312 (git-history paths — STAY literal wiki/)
    - bin/check-privacy.sh:24-33,67-87,121-140 (PUBLIC_PATHS + the `PRIVACY_LOCAL_ONLY_RE` frontmatter-grep predicate that loses its target post-strip + exit codes 0/1/2 — preserve CLI surface, swap predicate)
    - .planning/phases/15-privacy-architecture/15-RESEARCH.md "check-privacy.sh re-key" + "Pattern 3" + Assumptions A3 + the §13 Rewrite Content Map item 7 (FAITH-04 = single predicate)
    - .planning/phases/15-privacy-architecture/15-CONTEXT.md specifics (FAITH-04: "a claim is effective-local_only iff its page OR any contributing source lives under wiki-local/")
  </read_first>
  <action>
    Land EVERY privacy PREDICATE re-key in THIS lockstep commit (review HIGH #6 — the original two-plan split left check-neutrality's leak-source predicate DEAD between Plan 01 and Plan 02, a committed intermediate security regression). The privacy field is gone after Tasks 1-4; every check that keyed off it must now key off the `wiki-local/` path prefix, atomically.

    **First, DEFINE the raw-source structural rule (review HIGH #2 — the architecture gap CONTEXT left open).** Raw sources live under `sources/` with NO local tier (verified: no `sources/local-only/`). The collapsed predicate "page OR contributing source under wiki-local/" otherwise makes EVERY source-derived claim cloud_safe — a silent FAITH-04 regression. RESOLUTION (document this in privacy_resolve.py module docstring + carry into the §13 prose Task 2 already wrote, and into docs/reference/privacy-model.md authored by Plan 02): **raw `sources/` is cloud-safe-only; a source that must be local lives as its SOURCE-SUMMARY page under `wiki-local/sources/`. The resolver keys off the summary-page tier, never the raw `sources/...` path.** This is the documented hard rule the reviewer's Suggestion #2 calls for ("a hard rule that raw `sources/` is cloud-safe only, documented and tested").

    (a) bin/lib/privacy_resolve.py — COLLAPSE both functions to the single structural predicate (do NOT port the ladder):
      - `resolve_source_privacy(summary_rel_path)` → `'local_only'` iff the source SUMMARY page path is under `wiki-local/`, else `'cloud_safe'`.
      - `resolve_effective_claim_privacy(page_path, source_summary_path)` → `'local_only'` iff `page_path` OR `source_summary_path` is under `wiki-local/`, else `'cloud_safe'`. The signature keys off the SUMMARY page path (the source-tier signal), NEVER the raw `sources/...` path. Keep the two exported NAMES stable; if you change arity, update audit-claims.sh:142 + :745 in THIS same task (you do — see (c)).
      - Module docstring states the raw-source rule above.

    (b) bin/check-neutrality.sh — re-key the leak-SOURCE predicate `source_local_only_wiki()` (this was the dead-predicate intermediate the reviewer flagged as the D-02 violation). Change `wiki_dir = os.path.join(ROOT, "wiki")` (:265) → `os.path.join(ROOT, "wiki-local")`; DELETE the `if fm and re.search(r"^privacy:\s*local_only\b", fm, re.M):` guard (:279) so EVERY `.md` under `wiki-local/` contributes its tokens (the directory IS the classifier). Keep the `os.path.isdir` early-return (common no-local-content case) and keep tokenizing full text. Leave the `suggest()` group label + the 3-source dispatch intact (all three leak sources stay live). DO NOT touch `source_git_history()` lines 308-309 (pre-migration `wiki/` commit paths via `git log --all` — stay literal).

    (c) bin/audit-claims.sh — re-key the FAITH-04 chokepoint + fix source_registry (review HIGH #2/#3):
      - source_registry (line ~250) currently stores `source_registry[sfm['id']] = sfm` (frontmatter only). ADD the summary page's repo-relative path so the resolver can see its tier: store `source_registry[sfm['id']] = {'fm': sfm, 'summary_rel_path': rel}` (or a parallel `source_summary_path[sfm['id']] = rel`), where `rel` is the summary page's path relative to REPO_ROOT (captured during the page walk at lines ~227-250). Update the consumers at :462 / :526 / :743 accordingly.
      - the FAITH-04 call at :745 currently passes `(sfm or {}).get('path','')` — a RAW `sources/...` path. CHANGE it to pass the source SUMMARY repo path (`summary_rel_path`) so a source whose summary is under `wiki-local/sources/` makes its claims effective-local. Keep the partition CONTROL-FLOW at :745-762 (`if effective_priv == 'local_only' and not ALLOW_LOCAL: add_finding('skipped-privacy', ...)` withholding from BOTH the verifier subprocess AND `--emit-worklist`) UNCHANGED in semantics — only the predicate + the path argument swap.
      - the claim-page universe must include BOTH tiers: walk `wiki-cloud/` AND `wiki-local/` for claim pages (the two-tier page universe the reviewer asks for), so a claim ON a `wiki-local/` page is itself recognized as local.

    (d) bin/check-privacy.sh — re-key the scan predicate (preserve CLI surface `--root`/`--format text|json`, exit codes 0/1/2, the CI `privacy-leak` job name, the env-export-to-python idiom). Change from "grep PUBLIC_PATHS frontmatter for `privacy: local_only`" to the structural check: flag any path under a PUBLIC_PATH that is inside a `wiki-local/` tree OR copies `wiki-local/` content into a PUBLIC_PATH (e.g. `docs/wiki-local/...`). Keep it a THIN re-keyed guard. NOTE (review MEDIUM #2): a path-only scanner catches `docs/wiki-local/...` but NOT arbitrary copied content pasted into `docs/private.md`. STATE this limitation explicitly in the script header + docs (Plan 02 authors the doc): check-privacy is a PATH/release-allowlist guard, not a content-equivalence scanner. The content-leak surface is covered by check-neutrality's term-scan (re-keyed in (b)) — which DOES catch copied terms. Do not over-promise check-privacy.
  </action>
  <verify>
    <automated>python3 -c "import sys; sys.path.insert(0,'bin/lib'); import privacy_resolve; print('import ok')" && bash tests/phase-15/test_resolver_structural.sh && bash tests/phase-15/test_neutrality_leak_source_rekey.sh && python3 -c "import sys; sys.path.insert(0,'bin/lib'); from privacy_resolve import resolve_effective_claim_privacy"</automated>
  </verify>
  <acceptance_criteria>
    - privacy_resolve collapsed (review HIGH #6): `! grep -qi 'three-level\|stricter.of\|fail-closed' bin/lib/privacy_resolve.py` (ladder gone) AND the module docstring states the raw-source rule: `grep -qi 'raw.*sources.*cloud\|summary.*tier\|wiki-local/sources' bin/lib/privacy_resolve.py`
    - `bash tests/phase-15/test_resolver_structural.sh` exits 0 (source-derived-local survives — review HIGH #2)
    - check-neutrality leak-source re-keyed (review HIGH #6): `grep -q 'wiki-local' bin/check-neutrality.sh` exits 0 within source_local_only_wiki AND `! grep -q 'privacy:\\\\s\\*local_only' bin/check-neutrality.sh` (dead regex gone) AND `bash tests/phase-15/test_neutrality_leak_source_rekey.sh` exits 0
    - check-neutrality git-history preserved: `grep -q 'wiki/overviews/personal-decision-patterns.md' bin/check-neutrality.sh` exits 0 (lines 308-309 untouched)
    - source_registry stores the summary path (review HIGH #3): `grep -qE 'summary_rel_path|source_summary_path' bin/audit-claims.sh` exits 0
    - FAITH-04 call no longer passes the raw source path: the resolver is called with a summary-tier path, not `(sfm or {}).get('path','')` — `! grep -nE "resolve_effective_claim_privacy\(.*\.get\('path'" bin/audit-claims.sh` (the raw-path arg at :745 is replaced)
    - audit imports still resolve: `python3 -c "import sys; sys.path.insert(0,'bin/lib'); from privacy_resolve import resolve_effective_claim_privacy"` exits 0
    - check-privacy re-keyed structural + honest scope: `bash tests/phase-15/test_check_privacy_rekey.sh` exits 0 AND the script header states it is a path/release guard (not a content scanner): `grep -qi 'path\|release' bin/check-privacy.sh`
  </acceptance_criteria>
  <done>privacy_resolve collapsed; check-neutrality leak-source + check-privacy + audit FAITH-04 all re-keyed in THIS lockstep commit (no dead-predicate intermediate — review HIGH #6); raw-source structural rule defined + tested (HIGH #2); source_registry stores summary path + FAITH-04 keys off it (HIGH #3).</done>
</task>

<task type="auto">
  <name>Task 4: Strip privacy from templates + examples; author DR; lint-green; ONE security-atomic commit</name>
  <files>schema/templates/*.md, schema/examples/*.md, examples/kahneman/**, examples/dataview-fixtures/**, wiki-cloud/decisions/dr-2026-06-04-privacy-asymmetric-two-dir.md, wiki-cloud/index.md, wiki-cloud/log.md</files>
  <read_first>
    - schema/templates/decision.md:15 + wiki-cloud/decisions/dr-2026-06-03-uniform-piped-links.md:1-93 (DR template + the DR-at-execution precedent — frontmatter to mirror, alternatives-numbered format lines 76-93)
    - .planning/phases/15-privacy-architecture/15-CONTEXT.md D-15 (three options to record + why asymmetric won) + D-01 (strip examples/ too)
    - .planning/milestones/v1.2-MILESTONE-BRIEF.md lines 60-75 (the three weighed options, verbatim, for the DR's Alternatives section)
    - CLAUDE.md §4.6 (decision page type — 7 required sections)
  </read_first>
  <action>
    **This is the FINAL task of the plan — it runs AFTER Tasks 1, 2, 3, 5, 6 have all accumulated their changes in the working tree (no prior commit).** Strip the `privacy` key from the 12 page templates + examples, author the DR, run the full green gate, then land EVERYTHING as ONE security-atomic commit.

    (a) Strip `privacy:` from all `schema/templates/*.md` (entity, concept, comparison, overview, source-summary, decision) and all `schema/examples/*.md` (entity, concept, comparison, overview, source-summary, decision) using the ruamel helper (NOT sed). For `examples/kahneman/**` and `examples/dataview-fixtures/**` (D-01 says drop privacy from examples/): strip the `privacy` key there too via ruamel. Note: these examples carry `example: true` so lint skips them, but D-01 is explicit — the field is gone repo-wide. The decision-template's `privacy: cloud_safe` row (decision.md:15) is removed in this same commit so the DR authored below correctly OMITS the privacy row.

    (b) Author the execution-time DR at `wiki-cloud/decisions/dr-2026-06-04-privacy-asymmetric-two-dir.md` (`trigger_type: schema-update`, `affected_pages` listing the key schema/page IDs touched — non-empty). Mirror the frontmatter shape from dr-2026-06-03-uniform-piped-links.md (type: decision, status: active, epistemic_status: sourced, knowledge_domain: software, tags: [meta, schema], domains: [wiki-infrastructure]) — but OMIT the `privacy` row (stripped this commit). Fill all 7 §4.6 sections:
      - `## Why` must name the framing REPLACED: the implicit per-page §13 precedence/inheritance/fail-closed model.
      - `## Alternatives Considered` must list the THREE weighed options (≥2 rejected bullets): (1) per-page mixed [status quo], (2) per-vault fully separate, (3) asymmetric two-dir [CHOSEN] — and why asymmetric won (dominates status quo on safety + simplicity, keeps the only synthesis direction worth having; the forbidden direction local→cloud is exactly the leak).
      - `## Consequences` should note the raw-source structural rule (raw `sources/` is cloud-safe-only; local sources live as their SUMMARY under `wiki-local/sources/`) defined in Task 6.
    Add the DR to `wiki-cloud/index.md` under Decisions. Append a `## [2026-06-04] reflect | privacy asymmetric two-dir` entry to `wiki-cloud/log.md` (newest-at-bottom).

    (c) GREEN GATE — the security-atomic invariant (review HIGH #6: no committed intermediate where lint is red OR any privacy guard is dead). Run and require ALL green BEFORE committing:
      - `bash bin/lint.sh wiki-cloud/` exits 0 (no privacy-field yaml errors — depends on Task 3's BASE_FIELDS/enum removal; D-02 lockstep invariant).
      - `bash bin/sync-claude.sh --check` + `bash tests/phase-08/test_canonical_byte_equality.sh` (byte-equality + wizard regen).
      - The Wave-1-covered phase-15 tests: `test_layout_migrated`, `test_privacy_field_stripped`, `test_schema_13_rewritten`, `test_priv_resident_reduced`, `test_decision_record`, `test_resolver_structural`, `test_neutrality_leak_source_rekey`, `test_generated_frontmatter_clean`, `test_lint_required_field_dropped` — all GREEN (Tasks 3+6 turned the predicate/required-field/generated-frontmatter gates green).
      - The 3 rewritten phase-13 resolver tests (Task 5) + the full phase-13 audit suite: `for t in tests/phase-13/test_*.sh; do bash "$t" || exit 1; done` GREEN (review HIGH #1 — "phase-13 audit suite green" actually holds now).
      - Regression: re-run `tests/phase-07..13/*` over the renamed tree (Pass C re-keyed the fixtures by bucket).

    (d) Commit EVERYTHING from Tasks 1, 2, 3, 5, 6, 4 as a SINGLE security-atomic commit (D-02/HIGH #6): `schema: asymmetric two-dir privacy (wiki/→wiki-cloud/+wiki-local/), strip per-page privacy field, re-key all privacy predicates`. There must be NO prior partial commit — every task accumulates in the working tree and lands together here, so no commit boundary leaves a dead privacy guard. Verify `git log --oneline -1` shows exactly this migration commit on top of the Wave-0 scaffold commit.
  </action>
  <verify>
    <automated>bash bin/lint.sh wiki-cloud/ && bash bin/sync-claude.sh --check && bash tests/phase-08/test_canonical_byte_equality.sh && for t in tests/phase-15/test_layout_migrated test_privacy_field_stripped test_schema_13_rewritten test_priv_resident_reduced test_decision_record test_resolver_structural test_neutrality_leak_source_rekey test_generated_frontmatter_clean test_lint_required_field_dropped; do bash tests/phase-15/$t.sh || exit 1; done && for t in tests/phase-13/test_*.sh; do bash "$t" || exit 1; done && echo "ALL GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - `bash bin/lint.sh wiki-cloud/` exits 0 (lockstep lint-green invariant, D-02)
    - `test -f wiki-cloud/decisions/dr-2026-06-04-privacy-asymmetric-two-dir.md` exits 0
    - `grep -q 'trigger_type: schema-update' wiki-cloud/decisions/dr-2026-06-04-privacy-asymmetric-two-dir.md` exits 0
    - `bash tests/phase-15/test_decision_record.sh` exits 0 (3 options recorded)
    - `! grep -rl 'privacy:' schema/templates/ schema/examples/` (templates/examples stripped)
    - phase-13 audit suite green (review HIGH #1): `for t in tests/phase-13/test_*.sh; do bash "$t" || exit 1; done` exits 0
    - all predicate-dependent phase-15 tests green: `for t in test_resolver_structural test_neutrality_leak_source_rekey test_generated_frontmatter_clean test_lint_required_field_dropped; do bash tests/phase-15/$t.sh || exit 1; done` exits 0
    - `git log --oneline -1 | grep -q 'asymmetric two-dir privacy'` (single security-atomic migration commit landed)
  </acceptance_criteria>
  <done>Templates/examples stripped; DR authored; lint + phase-13 + predicate gates green; entire migration (Tasks 1,2,3,5,6,4) in ONE security-atomic commit; PRIV-01/02/04/05/06/07 Wave-1 tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local-tier page → cloud session | `wiki-local/` content must never become readable by a cloud-model run; the directory boundary is the new structural control |
| migration script → page frontmatter | A sed-based strip would cross into prose body text and corrupt documentation (the prose-corruption boundary) |
| commit boundary | A partial mid-migration commit would leave the tree lint-red and the regression suite broken (the atomicity boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-leak | Information disclosure | A page that should be local_only routed to wiki-cloud/ (mis-route) | mitigate | D-03 route-THEN-strip reads each page's `privacy` value before any strip; the 2 known local_only audit files are asserted present under wiki-local/maintenance/; the python3 verify enumerates any page that still carries privacy (would signal an un-stripped/mis-handled page) |
| T-15-01 | Tampering | Raw `sed '/privacy:/d'` corrupting prose `privacy:` lines in CLAUDE.md/docs | mitigate | D-04: ruamel `read_fm_body`→`del`→`write_roundtrip` scoped to the `---` fences only; sed is explicitly forbidden; .obsidian/ excluded from the sweep |
| T-15-02 | Denial of service (CI) | Lint-red intermediate state breaking the regression suite mid-migration | mitigate | D-02 lockstep: all of Tasks 1,2,3,5,6,4 land in ONE commit; `bin/lint.sh wiki-cloud/` must exit 0 (requires Task 3's BASE_FIELDS/enum removal — review HIGH #5) before the commit; no partial commit boundary |
| T-15-03 | Repudiation | Schema rewrite with no recorded rationale (why asymmetric over the rejected options) | mitigate | PRIV-06 execution-time DR records the three weighed options + why asymmetric won; trigger_type: schema-update; test_decision_record asserts ≥2 alternatives |
| T-15-04 | Tampering | CLAUDE.md / AGENTS.md drift (edit lands in one, not the other) | mitigate | `bin/sync-claude.sh --check` (pre-commit + acceptance criterion) + canonical fixture regen + byte-equality test |
| T-15-07 | Information disclosure | check-neutrality leak-source coverage gap — the `privacy: local_only` regex goes DEAD after the field strip, silently dropping one of three CI-gated leak sources | mitigate (REVISED — review HIGH #6) | The leak-SOURCE predicate re-key (walk `wiki-local/`, drop the `privacy:` regex) is now Task 6 of THIS plan, landing in the SAME security-atomic commit as the field strip — NOT deferred to a later plan. There is no committed intermediate where the predicate is dead. `test_neutrality_leak_source_rekey.sh` (RED in Wave 0) is the regression tripwire asserting the re-key + that a `wiki-local/` term reaching a public path is still surfaced |
| T-15-08 | Information disclosure | FAITH-04 source-tier privacy lost — the path-prefix collapse keyed off the RAW `sources/...` path (never under wiki-local/) makes every source-derived claim cloud_safe | mitigate (review HIGH #2/#3) | Task 6 stores the source SUMMARY repo path in source_registry and the FAITH-04 call keys off the summary tier, not the raw path; the raw-source structural rule (raw sources/ cloud-safe-only; local sources live as their summary under wiki-local/sources/) is defined + documented; `test_resolver_structural.sh` (RED in Wave 0) pins the source-derived-local case |
| T-15-09 | Tampering | Generated maintenance frontmatter re-introduces the stripped `privacy` field on the next tool run | mitigate (review HIGH #4) | Task 3 strips `privacy: local_only` from the audit-state/report templates + `privacy: cloud_safe` from the lint-report template, in the same commit; `test_generated_frontmatter_clean.sh` (RED in Wave 0) gates it |
| T-15-10 | Information disclosure | Audit control-plane (local-only) regenerated on the CLOUD tier by a blanket wiki/→wiki-cloud/ re-key | mitigate (review HIGH #3) | Task 3 routes the audit state/report default paths to `wiki-local/maintenance/` via a separate LOCAL_MAINT root; acceptance asserts the control-plane is NOT under wiki-cloud/maintenance/ |
</threat_model>

<verification>
- `test ! -d wiki && test -d wiki-cloud && test -d wiki-local/maintenance` — tree migrated
- `bash bin/lint.sh wiki-cloud/` exits 0 — lockstep lint-green invariant (D-02; requires Task 3 BASE_FIELDS/enum removal)
- `bash bin/sync-claude.sh --check` exits 0 — byte-equality
- `bash tests/phase-08/test_canonical_byte_equality.sh` exits 0 — wizard fixture regenerated
- All Wave-1-covered phase-15 tests GREEN: `test_layout_migrated`, `test_schema_13_rewritten`, `test_privacy_field_stripped`, `test_priv_resident_reduced`, `test_decision_record`, `test_resolver_structural`, `test_neutrality_leak_source_rekey`, `test_generated_frontmatter_clean`, `test_lint_required_field_dropped`
- phase-13 audit suite green (review HIGH #1): `for t in tests/phase-13/test_*.sh; do bash "$t"; done` all exit 0 (the 3 resolver tests were rewritten in Task 5, not mechanically re-keyed)
- audit control-plane is LOCAL (review HIGH #3): the regenerated audit-state/report land under `wiki-local/maintenance/`, never `wiki-cloud/maintenance/`
- `git log --oneline -2` — exactly one security-atomic migration commit atop the Wave-0 scaffold commit (no partial commits; no dead-predicate intermediate — review HIGH #6)
- Regression: re-run `tests/phase-07..13/*` — prior-phase suites green over the renamed tree (Pass C re-keyed the fixtures by manifest bucket across all 9 dirs — review HIGH #7)
</verification>

<success_criteria>
- wiki-cloud/ (54 pages) + wiki-local/maintenance/ (2 audit files) exist; wiki/ gone; no privacy frontmatter key anywhere (pages, generated maintenance templates, lint required-field, enum)
- §13 rewritten to asymmetric model; 7-row table + 3-level precedence + inheritance REMOVED; §13 resident core = one-line pointer
- CLAUDE.md ≡ AGENTS.md ≡ template; canonical fixture regenerated
- ALL privacy predicates re-keyed in this commit (privacy_resolve collapse, check-neutrality leak-source, check-privacy structural, audit FAITH-04) — no dead-predicate intermediate (review HIGH #6)
- Raw-source structural rule defined + tested; source_registry stores summary path; FAITH-04 source-tier privacy survives (review HIGH #2/#3)
- The 3 phase-13 resolver tests rewritten structurally; phase-13 audit suite green (review HIGH #1)
- Execution-time DR authored recording the three options; trigger_type: schema-update
- The entire migration is ONE lint-green, predicate-green, security-atomic commit (D-02 atomicity preserved)
</success_criteria>

<output>
After completion, create `.planning/phases/15-privacy-architecture/15-01-SUMMARY.md`
</output>
