---
phase: 15-privacy-architecture
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/phase-15/lib.sh
  - tests/phase-15/test_layout_migrated.sh
  - tests/phase-15/test_schema_13_rewritten.sh
  - tests/phase-15/test_privacy_field_stripped.sh
  - tests/phase-15/test_priv_resident_reduced.sh
  - tests/phase-15/test_decision_record.sh
  - tests/phase-15/test_lint_xtier_link.sh
  - tests/phase-15/test_check_privacy_rekey.sh
  - tests/phase-15/test_cloud_deny_profile.sh
autonomous: true
requirements: [PRIV-01, PRIV-02, PRIV-03, PRIV-04, PRIV-05, PRIV-06, PRIV-07]
must_haves:
  truths:
    - "All 9 phase-15 test files exist and are RED (fail) before structural change"
    - "tests/phase-15/lib.sh exports make_bare_repo, assert_exit_code, write_page with phase15- mktemp prefix"
    - "Each PRIV requirement has a dedicated failing test that goes GREEN only after Wave 1/2 lands"
  artifacts:
    - path: "tests/phase-15/lib.sh"
      provides: "Test harness (4 core helpers copied from phase-13)"
      contains: "make_bare_repo"
    - path: "tests/phase-15/test_layout_migrated.sh"
      provides: "PRIV-01 RED test"
    - path: "tests/phase-15/test_schema_13_rewritten.sh"
      provides: "PRIV-02 RED test"
    - path: "tests/phase-15/test_cloud_deny_profile.sh"
      provides: "PRIV-03 three-surface fail-direction RED test"
    - path: "tests/phase-15/test_privacy_field_stripped.sh"
      provides: "PRIV-04 RED test"
    - path: "tests/phase-15/test_lint_xtier_link.sh"
      provides: "PRIV-05 cloud->local link RED test"
    - path: "tests/phase-15/test_check_privacy_rekey.sh"
      provides: "PRIV-05 check-privacy re-key RED test"
    - path: "tests/phase-15/test_decision_record.sh"
      provides: "PRIV-06 DR RED test"
    - path: "tests/phase-15/test_priv_resident_reduced.sh"
      provides: "PRIV-07 RED test"
  key_links:
    - from: "tests/phase-15/lib.sh"
      to: "tests/phase-13/lib.sh"
      via: "verbatim copy + prefix bump"
      pattern: "make_bare_repo"
---

<objective>
Build the Wave 0 RED test scaffold for Phase 15 BEFORE any structural change. Per VALIDATION.md the Nyquist contract requires every PRIV requirement to have a failing automated test that the migration (Wave 1) and enforcement (Wave 2) plans turn GREEN. This plan creates `tests/phase-15/lib.sh` plus 9 `test_*.sh` files. No production code, no schema edits, no page moves.

Purpose: Establish the verification gate first so the lockstep migration in Wave 1 has a deterministic GREEN target and cannot silently regress.
Output: 1 harness + 9 RED test files under `tests/phase-15/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-privacy-architecture/15-VALIDATION.md
@.planning/phases/15-privacy-architecture/15-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy test harness + write the four schema/layout grep-idiom RED tests</name>
  <files>tests/phase-15/lib.sh, tests/phase-15/test_layout_migrated.sh, tests/phase-15/test_schema_13_rewritten.sh, tests/phase-15/test_privacy_field_stripped.sh, tests/phase-15/test_priv_resident_reduced.sh</files>
  <read_first>
    - tests/phase-13/lib.sh:14-58 (the 4 core helpers to copy VERBATIM — make_bare_repo, assert_exit_code, cleanup_fixture_repo, write_page)
    - tests/phase-09.1/test_decision_record.sh:1-75 (grep + python3-yaml-parse assertion idioms — the skeleton for all grep/yaml tests)
    - .planning/phases/15-privacy-architecture/15-PATTERNS.md lines 35-126 (lib.sh + the 4 grep/yaml test analog assignments)
    - .planning/phases/15-privacy-architecture/15-RESEARCH.md "§13 Rewrite Content Map" (what §13 must contain/lack — drives the grep assertions)
    - CLAUDE.md:1550-1593 (current §13 — the 7-row table + 3-level precedence the tests assert is GONE post-migration)
  </read_first>
  <action>
    Create `tests/phase-15/lib.sh` by copying `tests/phase-13/lib.sh` VERBATIM, bumping the mktemp prefix from `phase13-` to `phase15-` (line ~50). Keep only the 4 core helpers (make_bare_repo, assert_exit_code, cleanup_fixture_repo, write_page) + the `export -f` line; the audit-specific verifier helpers are not needed.

    Then write these 4 RED tests using the `source "$SCRIPT_DIR/lib.sh"` → REPO_ROOT-relative path → grep/python3-yaml assertion → `echo PASS` skeleton:

    - `tests/phase-15/test_layout_migrated.sh` (PRIV-01): assert `$REPO_ROOT/wiki-cloud/` exists AND `$REPO_ROOT/wiki-local/maintenance/` exists AND `$REPO_ROOT/wiki/` does NOT exist (`test ! -d`) AND the 2 audit files are at `wiki-local/maintenance/audit-report.md` + `audit-state.md`. Today this FAILS (wiki/ still present, wiki-cloud/ absent).

    - `tests/phase-15/test_schema_13_rewritten.sh` (PRIV-02): grep CLAUDE.md §13 region for asymmetric-model language PRESENT (`grep -qi 'wiki-local'` AND `grep -qi 'asymmetric\|one-way permeab\|cloud sessions\|cannot read'`) AND the 7-row precedence table ABSENT (assert `grep -q 'Privacy Decision Table'` returns non-zero → fail-if-present) AND the "Three-Level Precedence" heading ABSENT. Today FAILS (table + precedence still present at CLAUDE.md:1558-1591).

    - `tests/phase-15/test_privacy_field_stripped.sh` (PRIV-04): embed a `python3 - <<'PYEOF'` that walks `wiki-cloud/` + `wiki-local/` `*.md`, parses frontmatter, and asserts NO `privacy` key in any page. ALSO assert CLAUDE.md no longer contains the base-field line `privacy: local_only|cloud_safe` (line 274 today) and checklist item `5. \`privacy\` is one of` (line 380 today). Today FAILS (pages under wiki/, field present).

    - `tests/phase-15/test_priv_resident_reduced.sh` (PRIV-07): assert CLAUDE.md §13 body (between `## 13` and `## 14`) is reduced to a one-line structural pointer — assert the section is SHORT (e.g. line-count between `^## 13` and `^## 14` is < 15) AND contains a pointer phrase (`grep -qi 'schema/reference/privacy'` OR `grep -qi 'see.*privacy'`) AND does NOT contain `fail-closed` / `Three-Level` / `inheritance`. Today FAILS (§13 is ~44 lines with full machinery).

    Each test must `exit 0` on PASS, `exit 1` on FAIL (via the FAIL-echo-to-stderr + exit idiom from phase-09.1).
  </action>
  <verify>
    <automated>bash tests/phase-15/lib.sh; for t in test_layout_migrated test_schema_13_rewritten test_privacy_field_stripped test_priv_resident_reduced; do bash tests/phase-15/$t.sh; echo "$t exit=$?"; done</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/phase-15/lib.sh` exits 0
    - `grep -q 'phase15-' tests/phase-15/lib.sh` exits 0 (prefix bumped)
    - `grep -q 'make_bare_repo' tests/phase-15/lib.sh && grep -q 'write_page' tests/phase-15/lib.sh` exits 0
    - Each of the 4 tests exits NON-zero today (RED): `bash tests/phase-15/test_layout_migrated.sh; test $? -ne 0`
    - `grep -q 'wiki-cloud' tests/phase-15/test_layout_migrated.sh` exits 0
    - `grep -q 'wiki-local' tests/phase-15/test_privacy_field_stripped.sh` exits 0
  </acceptance_criteria>
  <done>lib.sh + 4 grep/yaml RED tests exist; all 4 fail today; harness helpers exported.</done>
</task>

<task type="auto">
  <name>Task 2: Write the DR, lint-link, check-privacy, and cloud-deny RED tests</name>
  <files>tests/phase-15/test_decision_record.sh, tests/phase-15/test_lint_xtier_link.sh, tests/phase-15/test_check_privacy_rekey.sh, tests/phase-15/test_cloud_deny_profile.sh</files>
  <read_first>
    - tests/phase-09.1/test_decision_record.sh:1-75 (the DR test to MIRROR — change DR path + trigger_type + strengthen alternatives to >=2)
    - .planning/phases/15-privacy-architecture/15-PATTERNS.md lines 85-153 (test_decision_record / lint_xtier / check_privacy / cloud_deny analog assignments)
    - tests/phase-13/lib.sh:10-12,71-78 (write_page usage docstring + signature — used to build inline wiki-cloud/wiki-local fixtures)
    - bin/lint.sh:321,388-391,1890-1925 (linkres severity remap + PIPED_LINK_RE + known_ids — the machinery test_lint_xtier_link asserts against)
    - bin/check-privacy.sh:24-33,67-87,121-140 (PUBLIC_PATHS + frontmatter-grep + exit codes — the contract test_check_privacy_rekey asserts)
    - .planning/phases/15-privacy-architecture/15-RESEARCH.md Code Examples "Verification test skeleton" (the 4 surfaces: Read DENIED / Bash cat DENIED / python open NOT denied / git show NOT denied) + Assumptions A2 (headless feasibility fallback)
  </read_first>
  <action>
    Write the remaining 5 RED tests:

    - `tests/phase-15/test_decision_record.sh` (PRIV-06): MIRROR `tests/phase-09.1/test_decision_record.sh`. Change `DR="$REPO_ROOT/wiki-cloud/decisions/dr-2026-06-04-privacy-asymmetric-two-dir.md"`. Keep the python3-yaml frontmatter assertion (`type: decision`, `trigger_type: schema-update`, `status: active`, `epistemic_status: sourced`); change the `affected_pages == []` exact-empty check to "is a list" (this DR has non-empty affected_pages). Keep the 7-section grep loop verbatim (`^## TL;DR`...`^## Sources`). Keep the Why-section `\b(replac|prior|previously|former|before)\b` proxy. STRENGTHEN the alternatives check to require `len(bullets) >= 2` (the three weighed options = chosen + 2 rejected). Today FAILS (DR file absent).

    - `tests/phase-15/test_lint_xtier_link.sh` (PRIV-05): use `make_bare_repo` → `write_page "$repo" "wiki-cloud/concepts/foo.md"` with body `[[bar|Bar]]` linking to → `write_page "$repo" "wiki-local/concepts/bar.md"`. Run `( cd "$repo" && "$REPO_ROOT/bin/lint.sh" --ci --category linkres wiki-cloud/ ); rc=$?` and `assert_exit_code 1 "$rc" "cloud->local link must be a linkres error"`. Today FAILS (the D-09 check does not exist; lint either passes or errors for the wrong reason — verify it does NOT exit 1 for the cloud→local reason; a precise assertion may also grep the lint output for `cloud->local` / `D-09`).

    - `tests/phase-15/test_check_privacy_rekey.sh` (PRIV-05): build a fixture and invoke `bin/check-privacy.sh`. Assert the re-keyed structural behavior: a clean tree (no `wiki-local/` content under PUBLIC_PATHS) → exit 0; a tree where `wiki-local/` content is copied into a PUBLIC_PATH (e.g. `docs/`) → exit 2 (leak). Today FAILS (current check-privacy keys on the `privacy: local_only` frontmatter field, not on `wiki-local/` path).

    - `tests/phase-15/test_cloud_deny_profile.sh` (PRIV-03): per RESEARCH A2 the headless `claude -p --settings` behavioral assertion is MEDIUM-risk. Implement the AUTOMATABLE static portion now (RED): assert `$REPO_ROOT/.claude/settings.cloud.json` exists, is valid JSON (`python3 -c 'import json; json.load(...)'`), and contains `permissions.deny == ["Read(./wiki-local/**)"]`. ALSO assert `docs/reference/privacy-model.md` contains a fail-direction table naming the 4 surfaces (`grep -qi 'fail-open'` AND `grep -qi 'git show'` AND `grep -qi 'python'`). Leave a clearly-commented `# MANUAL / headless-spike:` block documenting the 3 live surfaces (Read DENIED, Bash cat DENIED, git show NOT denied) for the Wave 2 executor to attempt to automate or convert to the manual-runbook fallback. Today FAILS (settings.cloud.json + fail-direction table absent).

    Each test exits 0 on PASS / non-zero on FAIL.
  </action>
  <verify>
    <automated>for t in test_decision_record test_lint_xtier_link test_check_privacy_rekey test_cloud_deny_profile; do bash tests/phase-15/$t.sh; echo "$t exit=$?"; done</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files exist: `for t in test_decision_record test_lint_xtier_link test_check_privacy_rekey test_cloud_deny_profile; do test -f tests/phase-15/$t.sh || exit 1; done`
    - Each exits NON-zero today (RED): `bash tests/phase-15/test_decision_record.sh; test $? -ne 0`
    - `grep -q 'dr-2026-06-04-privacy-asymmetric-two-dir' tests/phase-15/test_decision_record.sh` exits 0
    - `grep -q 'settings.cloud.json' tests/phase-15/test_cloud_deny_profile.sh` exits 0
    - `grep -q 'git show' tests/phase-15/test_cloud_deny_profile.sh` exits 0 (the object-leak surface is pinned)
    - `grep -q 'wiki-local' tests/phase-15/test_lint_xtier_link.sh` exits 0
  </acceptance_criteria>
  <done>9 total test files RED; all PRIV requirements have a failing automated gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → real repo tree | Tests read the real CLAUDE.md / wiki tree; a buggy test that passes today would mask the migration gap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-W0-01 | Tampering | A RED test that accidentally passes today (false-GREEN) | mitigate | Acceptance criteria explicitly assert each test exits NON-zero today; the harness commit is the proof the gate is real before Wave 1 |
| T-15-W0-02 | Information disclosure | Test fixtures embedding real local_only content | accept | Fixtures use synthetic `foo`/`bar` pages in mktemp dirs; no real vault content; cleaned via cleanup_fixture_repo |
</threat_model>

<verification>
- All 9 test files + lib.sh exist under `tests/phase-15/`
- `for t in tests/phase-15/test_*.sh; do bash "$t"; echo "$t exit=$?"; done` — every test exits NON-zero (RED) today
- No production code, schema, or page changes in this plan (`git diff --name-only` touches only `tests/phase-15/`)
</verification>

<success_criteria>
- 1 harness + 9 RED tests exist; each maps to a PRIV requirement per VALIDATION.md Per-Task map
- Every test is RED today and will go GREEN only after Wave 1 (migration) / Wave 2 (enforcement)
</success_criteria>

<output>
After completion, create `.planning/phases/15-privacy-architecture/15-00-SUMMARY.md`
</output>
