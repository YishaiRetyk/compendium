---
phase: 07-neutral-template-foundation
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - examples/kahneman/README.md
  - examples/kahneman/log.md
  - examples/kahneman/entities/daniel-kahneman.md
  - examples/kahneman/concepts/prospect-theory.md
  - examples/kahneman/concepts/loss-aversion.md
  - examples/kahneman/concepts/cognitive-biases.md
  - examples/kahneman/comparisons/system-1-vs-system-2.md
  - examples/kahneman/overviews/decision-making.md
  - examples/kahneman/sources/src-2026-04-09-thinking-fast-and-slow-part1.md
  - examples/kahneman/sources/src-2026-04-10-kahneman-prospect-theory.md
  - wiki/entities/daniel-kahneman.md
  - wiki/concepts/prospect-theory.md
  - wiki/concepts/loss-aversion.md
  - wiki/concepts/cognitive-biases.md
  - wiki/comparisons/system-1-vs-system-2.md
  - wiki/overviews/decision-making.md
  - wiki/overviews/personal-decision-patterns.md
  - wiki/sources/src-2026-04-09-thinking-fast-and-slow-part1.md
  - wiki/sources/src-2026-04-10-kahneman-prospect-theory.md
  - wiki/sources/src-2026-04-10-personal-decision-journal.md
  - wiki/index.md
  - wiki/log.md
  - wiki/maintenance/lint-report.md
  - wiki/maintenance/reflect-state.md
  - wiki/decisions/dr-2026-04-15-kahneman-to-examples.md
  - bin/lint.sh
  - tests/phase-07/test_kahneman_moved.sh
  - tests/phase-07/test_kahneman_readme.sh
  - tests/phase-07/test_lint_exclude.sh
  - tests/phase-07/test_wiki_skeleton.sh
  - tests/phase-07/test_no_stale_kahneman_paths.sh
  - tests/phase-07/fixtures/lint-exclude/examples/sample.md
  - tests/phase-07/fixtures/lint-exclude/wiki/marked.md
autonomous: true
requirements:
  - NEUT-01
  - NEUT-04
  - NEUT-05
  - NEUT-07
  - TMPL-05
must_haves:
  truths:
    - "All 7 Kahneman content pages live under examples/kahneman/ with internal wikilinks intact"
    - "wiki/ contains only index.md + log.md skeletons and decisions/ (no Kahneman content under entities|concepts|comparisons|overviews|sources, no local_only content, no maintenance/)"
    - "SUPERSEDE decision record dr-2026-04-15-kahneman-to-examples.md exists, conforms to AGENTS.md §4.6 type: decision schema (id, type, status, summary, created_at, updated_at, epistemic_status, tags, domains, trigger_type, affected_pages) with all 7 required body sections"
    - "bin/lint.sh skips examples/ by default and skips any page with example: true frontmatter"
    - "examples/kahneman/README.md documents the cluster per D-13"
    - "No remaining file under public surfaces (README, AGENTS.md, CLAUDE.md, docs/, .github/, bin/, wiki/index.md, wiki/log.md) references the old wiki/{entities,concepts,comparisons,overviews,sources}/...kahneman... paths"
  artifacts:
    - path: examples/kahneman/
      provides: "Preserved Kahneman reference example"
    - path: wiki/decisions/dr-2026-04-15-kahneman-to-examples.md
      provides: "NEUT-07 decision record (canonical type: decision schema)"
      contains: "trigger_type: schema-update"
    - path: bin/lint.sh
      provides: "EXCLUDE_DIRS extended; example: true honored"
      contains: "'examples'"
  key_links:
    - from: examples/kahneman/entities/daniel-kahneman.md
      to: examples/kahneman/concepts/prospect-theory.md
      via: "wikilink [[prospect-theory]]"
      pattern: "\\[\\[prospect-theory"
    - from: wiki/index.md
      to: "(no Kahneman)"
      via: "grep -L kahneman wiki/index.md"
      pattern: "skeleton-only"
---

<objective>
Relocate the Kahneman cluster from `wiki/` to `examples/kahneman/`, remove `local_only` personal content from `wiki/`, **delete `wiki/maintenance/` entirely** (TMPL-05 says skeleton-only), reduce `wiki/` to TMPL-05 skeleton, extend `bin/lint.sh` to honor `examples/` and `example: true`, commit a NEUT-07 decision record that conforms exactly to the canonical `AGENTS.md §4.6 type: decision` schema, and run a repo-wide scan that asserts no remaining public file references the old `wiki/.../kahneman/...` paths.

Purpose: This is the foundation for every later neutrality task. AGENTS.md neutralization (plan 03) and CI gates (plan 05) both depend on `wiki/` being Kahneman-free and `examples/kahneman/` being the single sanctioned home for Kahneman content. Also removes `privacy: local_only` creator content that must never ship publicly (NEUT-08 target, surfaced by PITFALLS.md C-1).
Output: 7 Kahneman pages + 2 Kahneman sources relocated with wikilinks preserved; personal-journal + personal-decision-patterns deleted; `wiki/maintenance/` deleted; wiki reduced to skeleton; lint.sh extended; canonical-schema decision record committed; stale-path scan test added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-neutral-template-foundation/07-CONTEXT.md
@.planning/phases/07-neutral-template-foundation/07-RESEARCH.md
@.planning/phases/07-neutral-template-foundation/07-VALIDATION.md
@.planning/phases/07-neutral-template-foundation/07-REVIEWS.md
@AGENTS.md
@bin/lint.sh
</context>

<interfaces>
**Current wiki/ layout (verified 2026-04-15):**
```
wiki/index.md
wiki/log.md
wiki/comparisons/system-1-vs-system-2.md
wiki/concepts/cognitive-biases.md
wiki/concepts/loss-aversion.md
wiki/concepts/prospect-theory.md
wiki/entities/daniel-kahneman.md
wiki/overviews/decision-making.md
wiki/overviews/personal-decision-patterns.md           # privacy: local_only — DELETE
wiki/sources/src-2026-04-09-thinking-fast-and-slow-part1.md
wiki/sources/src-2026-04-10-kahneman-prospect-theory.md
wiki/sources/src-2026-04-10-personal-decision-journal.md  # privacy: local_only — DELETE
wiki/maintenance/lint-report.md                        # DELETE — TMPL-05 skeleton-only
wiki/maintenance/reflect-state.md                      # DELETE — TMPL-05 skeleton-only
```

**bin/lint.sh L178-180 (verified) — extension point for NEUT-04:**
```python
# BEFORE:
EXCLUDE_DIRS = {'maintenance'}
# AFTER:
EXCLUDE_DIRS = {'maintenance', 'examples'}
```
Plus a per-file frontmatter check at the per-file loop (around L222) that skips any file with `example: true` in frontmatter.

`'maintenance'` stays in EXCLUDE_DIRS as a forward-compatible default for downstream forks that may regenerate maintenance reports locally; the public template ships without the directory.

**AGENTS.md §4.6 — canonical `type: decision` schema (HIGH-severity revision per REVIEWS.md):**
The decision record MUST use these EXACT frontmatter keys (no `date`, no `class: SUPERSEDE` — those are NOT in the schema):

Required base fields: `id`, `title`, `type: decision`, `status: active`, `summary` (single-line quoted), `created_at: 2026-04-15`, `updated_at: 2026-04-15`, `sources: []`, `epistemic_status: sourced`, `tags: [meta, schema]`, `domains: [wiki-infrastructure]`, `privacy: cloud_safe`, `knowledge_domain: software`.
Required decision-specific fields: `trigger_type: schema-update`, `affected_pages: [...]` (YAML list of page IDs, kebab-case strings without `.md`).

Required body sections (all 7, in this order, per AGENTS.md §4.6):
1. `## TL;DR`
2. `## Decision`
3. `## Why`
4. `## Alternatives Considered`
5. `## Consequences`
6. `## Affected Pages`
7. `## Sources`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Relocate Kahneman cluster, delete local_only + wiki/maintenance/, shrink wiki to skeleton, commit canonical NEUT-07 record, scan for stale paths</name>
  <files>examples/kahneman/README.md, examples/kahneman/log.md, examples/kahneman/entities/daniel-kahneman.md, examples/kahneman/concepts/prospect-theory.md, examples/kahneman/concepts/loss-aversion.md, examples/kahneman/concepts/cognitive-biases.md, examples/kahneman/comparisons/system-1-vs-system-2.md, examples/kahneman/overviews/decision-making.md, examples/kahneman/sources/src-2026-04-09-thinking-fast-and-slow-part1.md, examples/kahneman/sources/src-2026-04-10-kahneman-prospect-theory.md, wiki/entities/daniel-kahneman.md, wiki/concepts/prospect-theory.md, wiki/concepts/loss-aversion.md, wiki/concepts/cognitive-biases.md, wiki/comparisons/system-1-vs-system-2.md, wiki/overviews/decision-making.md, wiki/overviews/personal-decision-patterns.md, wiki/sources/src-2026-04-09-thinking-fast-and-slow-part1.md, wiki/sources/src-2026-04-10-kahneman-prospect-theory.md, wiki/sources/src-2026-04-10-personal-decision-journal.md, wiki/index.md, wiki/log.md, wiki/maintenance/lint-report.md, wiki/maintenance/reflect-state.md, wiki/decisions/dr-2026-04-15-kahneman-to-examples.md, tests/phase-07/test_kahneman_moved.sh, tests/phase-07/test_kahneman_readme.sh, tests/phase-07/test_wiki_skeleton.sh, tests/phase-07/test_no_stale_kahneman_paths.sh</files>
  <read_first>
    - wiki/entities/daniel-kahneman.md (source content + its wikilinks)
    - wiki/concepts/prospect-theory.md (source content + wikilinks)
    - wiki/concepts/loss-aversion.md
    - wiki/concepts/cognitive-biases.md
    - wiki/comparisons/system-1-vs-system-2.md
    - wiki/overviews/decision-making.md
    - wiki/overviews/personal-decision-patterns.md (confirm privacy: local_only before deleting)
    - wiki/sources/src-2026-04-09-thinking-fast-and-slow-part1.md
    - wiki/sources/src-2026-04-10-kahneman-prospect-theory.md
    - wiki/sources/src-2026-04-10-personal-decision-journal.md (confirm privacy: local_only)
    - wiki/index.md, wiki/log.md (to know what to strip)
    - **AGENTS.md §4.6 (lines 473–550) — the canonical `type: decision` schema. The decision record below MUST conform field-for-field. Section ordering is enumerated at lines 499–507.**
    - .planning/phases/07-neutral-template-foundation/07-CONTEXT.md §D-13 (README content requirements), §D-09 (lint behavior)
    - .planning/phases/07-neutral-template-foundation/07-REVIEWS.md (HIGH-severity findings #1 and #2 on this plan)
    - .planning/phases/07-neutral-template-foundation/07-RESEARCH.md §Runtime State Inventory (lines 397–422) — the move inventory
  </read_first>
  <action>
    Sub-steps, in order:

    **1. Verify privacy: local_only on files slated for deletion.**
    ```bash
    grep -E '^privacy:\s*local_only' wiki/overviews/personal-decision-patterns.md wiki/sources/src-2026-04-10-personal-decision-journal.md
    ```
    Both must match. If not, HALT and report — it means the classification changed and the delete is unsafe.

    **2. Create target directory structure:**
    ```bash
    mkdir -p examples/kahneman/{entities,concepts,comparisons,overviews,sources}
    ```

    **3. `git mv` the 7 Kahneman pages + 2 Kahneman sources to preserve history:**
    ```bash
    git mv wiki/entities/daniel-kahneman.md examples/kahneman/entities/daniel-kahneman.md
    git mv wiki/concepts/prospect-theory.md examples/kahneman/concepts/prospect-theory.md
    git mv wiki/concepts/loss-aversion.md examples/kahneman/concepts/loss-aversion.md
    git mv wiki/concepts/cognitive-biases.md examples/kahneman/concepts/cognitive-biases.md
    git mv wiki/comparisons/system-1-vs-system-2.md examples/kahneman/comparisons/system-1-vs-system-2.md
    git mv wiki/overviews/decision-making.md examples/kahneman/overviews/decision-making.md
    git mv wiki/sources/src-2026-04-09-thinking-fast-and-slow-part1.md examples/kahneman/sources/src-2026-04-09-thinking-fast-and-slow-part1.md
    git mv wiki/sources/src-2026-04-10-kahneman-prospect-theory.md examples/kahneman/sources/src-2026-04-10-kahneman-prospect-theory.md
    ```

    **4. Delete `privacy: local_only` personal content (must not ship):**
    ```bash
    git rm wiki/overviews/personal-decision-patterns.md
    git rm wiki/sources/src-2026-04-10-personal-decision-journal.md
    ```

    **4b. Delete `wiki/maintenance/` entirely (REVIEWS.md HIGH finding #2 — TMPL-05 says skeleton-only).**
    ```bash
    git rm wiki/maintenance/lint-report.md wiki/maintenance/reflect-state.md
    rmdir wiki/maintenance 2>/dev/null || true
    ```
    `bin/lint.sh` regenerates these locally on demand; they MUST NOT ship in the public template. Downstream forks running lint locally will recreate any reports they need.

    **5. Verify wikilinks still resolve.** Obsidian-style `[[slug]]` wikilinks resolve by filename anywhere in the vault, so in-cluster links (`[[prospect-theory]]` etc.) continue to work as long as all Kahneman pages moved together. Run:
    ```bash
    python3 - <<'PY'
    import re, pathlib
    root = pathlib.Path("examples/kahneman")
    slugs = {p.stem for p in root.rglob("*.md")}
    bad = []
    for p in root.rglob("*.md"):
        for m in re.finditer(r"\[\[([a-z0-9-]+)(?:\|[^\]]*)?\]\]", p.read_text()):
            target = m.group(1)
            if target not in slugs and target not in {"index","log"}:
                bad.append((str(p), target))
    if bad:
        for b in bad: print("UNRESOLVED:", b)
        raise SystemExit(1)
    print("All in-cluster wikilinks resolve.")
    PY
    ```

    **6. Create `examples/kahneman/log.md`** — extract any Kahneman ingest entries from `wiki/log.md` (preserving AGENTS.md §12 append-only format and log entry timestamps). Filter rule: an entry migrates only if it explicitly names a Kahneman page slug (`daniel-kahneman`, `prospect-theory`, `loss-aversion`, `cognitive-biases`, `system-1-vs-system-2`, `decision-making`, `thinking-fast-and-slow`, `src-2026-04-09-thinking-fast-and-slow-part1`, `src-2026-04-10-kahneman-prospect-theory`). Personal-domain entries (e.g., journal) do NOT migrate — they get deleted along with their source pages.

    **7. Shrink `wiki/index.md` to skeleton (TMPL-05):**
    Replace full contents with:
    ```markdown
    ---
    id: index
    title: Index
    type: overview
    status: active
    summary: "Skeleton index — ingested content will appear here organized by knowledge domain."
    created_at: 2026-04-15
    updated_at: 2026-04-15
    sources: []
    epistemic_status: sourced
    tags: [meta]
    domains: [wiki-infrastructure]
    privacy: cloud_safe
    knowledge_domain: software
    ---

    # Index

    This is a skeleton index. Ingested content will appear here organized by knowledge domain.

    See `AGENTS.md §2 Directory Structure` for layout conventions.
    ```

    **8. Shrink `wiki/log.md` to skeleton.** `log.md` is an append-only operations log, not a normal wiki page. Use minimal frontmatter that satisfies `bin/lint.sh` without imposing the full page schema:
    ```markdown
    ---
    title: Log
    privacy: cloud_safe
    ---

    # Log

    Append ingest entries here, newest at bottom, per AGENTS.md §12.
    ```
    If `bin/lint.sh` rejects this minimal form, fall back to adding `id: log`, `type: overview`, `status: active`, `summary: "Append-only operations log."`, `created_at: 2026-04-15`, `updated_at: 2026-04-15`, `sources: []`, `epistemic_status: sourced`, `tags: [meta]`, `domains: [wiki-infrastructure]`, `knowledge_domain: software` — same minimal-meta pattern as index.md.

    **9. (REMOVED — was "Regenerate or stub `wiki/maintenance/lint-report.md`".) `wiki/maintenance/` is deleted in step 4b. There is nothing to regenerate or stub here.**

    **10. Create `examples/kahneman/README.md` (NEUT-05, D-13) — MUST contain these sections (level-2 headers exactly):**
      - `## What this cluster demonstrates` (list: ingest, synthesis, cross-linking, provenance, contradiction handling)
      - `## Why it is preserved` (rationale: reference example; agents and readers benefit from a concrete filled instance)
      - `## How to read it` (entry points: start with `entities/daniel-kahneman.md`, walk outward)
      - `## Which AGENTS.md sections it illustrates` (mapped list: §2 structure, §5 frontmatter, §6 provenance, §11 operations)
      - `## Do not edit — reference material` (explicit note)
      Frontmatter MUST include `example: true` and `privacy: cloud_safe`.

    **11. Create NEUT-07 decision record at `wiki/decisions/dr-2026-04-15-kahneman-to-examples.md` — CANONICAL `type: decision` SCHEMA (REVIEWS.md HIGH finding #1).**

    Frontmatter MUST be EXACTLY (no `date` field, no `class:` field — those are NOT in AGENTS.md §4.6):
    ```yaml
    ---
    id: dr-2026-04-15-kahneman-to-examples
    title: "Move Kahneman Cluster from wiki/ to examples/"
    type: decision
    status: active
    summary: "Relocate the 7 Kahneman pages + 2 Kahneman source summaries from wiki/ to examples/kahneman/ to neutralize the public template surface; delete privacy: local_only personal-domain content; delete wiki/maintenance/."
    created_at: 2026-04-15
    updated_at: 2026-04-15
    sources: []
    epistemic_status: sourced
    tags:
      - meta
      - schema
      - neutralization
    domains:
      - wiki-infrastructure
    privacy: cloud_safe
    knowledge_domain: software
    trigger_type: schema-update
    affected_pages:
      - daniel-kahneman
      - prospect-theory
      - loss-aversion
      - cognitive-biases
      - system-1-vs-system-2
      - decision-making
      - src-2026-04-09-thinking-fast-and-slow-part1
      - src-2026-04-10-kahneman-prospect-theory
      - personal-decision-patterns
      - src-2026-04-10-personal-decision-journal
      - lint-report
      - reflect-state
    ---
    ```

    Body MUST follow the exact 7-section order from AGENTS.md §4.6 lines 499–507:

    ```markdown
    ## TL;DR

    To ship a public, neutral template (v1.1 milestone), the creator-domain Kahneman cluster moves to `examples/kahneman/` (preserved as a reference example), `privacy: local_only` personal-domain content is deleted, and `wiki/maintenance/` is deleted (TMPL-05 ships skeleton-only).

    ## Decision

    Relocate all 7 Kahneman wiki pages and 2 Kahneman source summaries from `wiki/{entities,concepts,comparisons,overviews,sources}/` to `examples/kahneman/{...}/`. Delete `wiki/overviews/personal-decision-patterns.md` and `wiki/sources/src-2026-04-10-personal-decision-journal.md` (`privacy: local_only`, must never ship). Delete `wiki/maintenance/lint-report.md` and `wiki/maintenance/reflect-state.md` (regenerated locally on demand by `bin/lint.sh`). Reduce `wiki/` to TMPL-05 skeleton: `index.md`, `log.md`, and `decisions/`.

    ## Why

    The framing adopted is "the public template ships with an empty wiki and a preserved reference example, distinct directories with distinct semantics." The framing it replaces is "the wiki is the creator's personal-domain cluster plus that creator's private notes." NEUT-01, NEUT-05, NEUT-07, TMPL-05 all derive from this framing. Pitfall C-1 (creator-content leakage) is mitigated by removing creator content from public paths in a single auditable commit.

    ## Alternatives Considered

    - **Leave Kahneman cluster in `wiki/` and document it as "starter content."** Rejected: a stranger cloning the template would inherit Daniel Kahneman's biography as their own knowledge base; any new ingest would be polluted by an unrelated domain.
    - **Delete the Kahneman cluster entirely.** Rejected: the cluster is a high-quality, internally consistent demonstration of the schema's full feature surface (entities, concepts, comparisons, overviews, sources, provenance). NEUT-05 keeps it as a reference.
    - **Keep `wiki/maintenance/` with stubbed reports.** Rejected: TMPL-05 says skeleton-only. Maintenance reports are tooling output, not authored content; downstream forks regenerate them on demand.

    ## Consequences

    - `wiki/` reduces to TMPL-05 skeleton (index.md + log.md + decisions/).
    - `examples/kahneman/README.md` (NEUT-05) explains the cluster's role.
    - `bin/lint.sh EXCLUDE_DIRS` extended to skip `examples/` (NEUT-04).
    - `AGENTS.md` illustrative content rewritten in plan 07-03 (NEUT-02/03) to use generic placeholders + `See: examples/kahneman/...` pointers.
    - The personal-decision-patterns / personal-decision-journal content is irrecoverable from the public release (orphan-branch publish in plan 07-05 ensures git history is also unreachable).
    - Maintenance reports must be regenerated locally; documented in `docs/reference/release.md`.

    ## Affected Pages

    Moved to `examples/kahneman/`:
    - [[daniel-kahneman]] — relocated to examples/kahneman/entities/
    - [[prospect-theory]] — relocated to examples/kahneman/concepts/
    - [[loss-aversion]] — relocated to examples/kahneman/concepts/
    - [[cognitive-biases]] — relocated to examples/kahneman/concepts/
    - [[system-1-vs-system-2]] — relocated to examples/kahneman/comparisons/
    - [[decision-making]] — relocated to examples/kahneman/overviews/
    - [[src-2026-04-09-thinking-fast-and-slow-part1]] — relocated to examples/kahneman/sources/
    - [[src-2026-04-10-kahneman-prospect-theory]] — relocated to examples/kahneman/sources/

    Deleted (creator-private, never to ship):
    - personal-decision-patterns — `privacy: local_only`
    - src-2026-04-10-personal-decision-journal — `privacy: local_only`

    Deleted (TMPL-05 skeleton-only):
    - lint-report (regenerate locally via `bash bin/lint.sh`)
    - reflect-state (regenerate locally via reflect workflow)

    ## Sources

    - AGENTS.md §4.6 — `type: decision` schema this record conforms to.
    - .planning/REQUIREMENTS.md — TMPL-05, NEUT-01, NEUT-05, NEUT-07 definitions.
    - .planning/phases/07-neutral-template-foundation/07-CONTEXT.md — locked decisions D-09, D-13.
    ```

    **12. Create Wave 0 tests.**

    `tests/phase-07/test_kahneman_moved.sh`:
      - Asserts `examples/kahneman/entities/daniel-kahneman.md` exists.
      - Asserts each of the 7 Kahneman page filenames + 2 source filenames exists under `examples/kahneman/`.
      - **Scoped Kahneman-string check (REVIEWS.md HIGH finding #3): asserts that `wiki/entities/`, `wiki/concepts/`, `wiki/comparisons/`, `wiki/overviews/`, `wiki/sources/` contain ZERO files matching `kahneman|prospect|loss-aversion|cognitive-biases|system-1-vs-system-2|decision-making|thinking-fast`. The check explicitly excludes `wiki/decisions/` (where the NEUT-07 record legitimately names the move) and excludes `wiki/maintenance/` (which is now deleted). Use:**
        ```bash
        for d in wiki/entities wiki/concepts wiki/comparisons wiki/overviews wiki/sources; do
          [ -d "$d" ] && find "$d" -name '*.md' | xargs -r grep -lEi 'kahneman|prospect-theory|loss-aversion|cognitive-biases|system-1-vs-system-2|decision-making|thinking-fast' && exit 1
        done
        exit 0
        ```
      - Asserts in-cluster wikilinks resolve (runs the python3 block from step 5).
      - Asserts `wiki/overviews/personal-decision-patterns.md` and `wiki/sources/src-2026-04-10-personal-decision-journal.md` do NOT exist.
      - Asserts `wiki/maintenance/` does NOT exist (`test ! -d wiki/maintenance`).

    `tests/phase-07/test_kahneman_readme.sh`:
      - Asserts `examples/kahneman/README.md` exists.
      - Asserts each of the 5 required H2 sections present: `## What this cluster demonstrates`, `## Why it is preserved`, `## How to read it`, `## Which AGENTS.md sections it illustrates`, `## Do not edit`.
      - Asserts frontmatter contains `example: true` and `privacy: cloud_safe`.
      - Asserts the NEUT-07 decision record conforms to the canonical schema:
        ```bash
        DR=wiki/decisions/dr-2026-04-15-kahneman-to-examples.md
        grep -q '^type: decision' "$DR" || exit 1
        grep -q '^status: active' "$DR" || exit 1
        grep -q '^trigger_type: schema-update' "$DR" || exit 1
        grep -q '^epistemic_status: sourced' "$DR" || exit 1
        grep -q '^created_at: 2026-04-15' "$DR" || exit 1
        grep -q '^updated_at: 2026-04-15' "$DR" || exit 1
        grep -q '^affected_pages:' "$DR" || exit 1
        # Forbidden non-canonical fields (REVIEWS.md HIGH finding #1):
        grep -q '^date:' "$DR" && exit 1
        grep -q '^class:' "$DR" && exit 1
        # All 7 required sections in order:
        for s in '## TL;DR' '## Decision' '## Why' '## Alternatives Considered' '## Consequences' '## Affected Pages' '## Sources'; do
          grep -qF "$s" "$DR" || { echo "missing: $s"; exit 1; }
        done
        exit 0
        ```

    `tests/phase-07/test_wiki_skeleton.sh` (TMPL-05 — strictly enforced):
      - Asserts `wiki/` top level contains ONLY `index.md`, `log.md`, and `decisions/` (no entities, concepts, comparisons, overviews, sources, **maintenance** subdirectories at all):
        ```bash
        UNEXPECTED=$(find wiki -mindepth 1 -maxdepth 1 ! -name 'index.md' ! -name 'log.md' ! -name 'decisions' -print)
        [ -z "$UNEXPECTED" ] || { echo "UNEXPECTED in wiki/: $UNEXPECTED"; exit 1; }
        ```
      - Asserts `wiki/index.md` is ≤ 30 lines total (frontmatter + body).

    **`tests/phase-07/test_no_stale_kahneman_paths.sh` (NEW — REVIEWS.md HIGH finding #4):**
    Repo-wide scan asserting NO public file references the old `wiki/{entities,concepts,comparisons,overviews,sources}/...kahneman...` paths or any of the 7 moved page slugs as a `wiki/...` path:
    ```bash
    #!/usr/bin/env bash
    set -euo pipefail
    # Search public surfaces (not examples/, not .planning/, not .git/, not wiki/decisions/ where the
    # NEUT-07 record legitimately names the move).
    HITS=$(grep -rIn -E 'wiki/(entities|concepts|comparisons|overviews|sources)/(daniel-kahneman|prospect-theory|loss-aversion|cognitive-biases|system-1-vs-system-2|decision-making|thinking-fast|src-2026-04-09-thinking-fast-and-slow-part1|src-2026-04-10-kahneman-prospect-theory)' \
      --include='*.md' --include='*.sh' --include='*.yml' --include='*.yaml' \
      --exclude-dir=examples --exclude-dir=.planning --exclude-dir=.git --exclude-dir=node_modules \
      README.md AGENTS.md CLAUDE.md PRIVACY.md docs/ .github/ bin/ wiki/index.md wiki/log.md 2>/dev/null || true)
    if [ -n "$HITS" ]; then
      echo "STALE wiki/.../kahneman/... path references found:"
      echo "$HITS"
      exit 1
    fi
    echo "No stale Kahneman path references in public surfaces."
    exit 0
    ```
    Note: CLAUDE.md, README.md, docs/ may not exist yet during early Wave 2 execution; the test must guard each path with `[ -e ]` before grepping (use `find ... -path ...` or check existence per-target before invoking grep). Acceptable if the test harness skips non-existent targets cleanly.

    Commit: `refactor(07-02): move Kahneman cluster to examples/, delete local_only + wiki/maintenance/, commit canonical NEUT-07 record, scan stale paths`.
  </action>
  <verify>
    <automated>bash tests/phase-07/test_kahneman_moved.sh && bash tests/phase-07/test_kahneman_readme.sh && bash tests/phase-07/test_wiki_skeleton.sh && bash tests/phase-07/test_no_stale_kahneman_paths.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -f examples/kahneman/entities/daniel-kahneman.md`
    - `test -f examples/kahneman/concepts/prospect-theory.md`
    - `test -f examples/kahneman/concepts/loss-aversion.md`
    - `test -f examples/kahneman/concepts/cognitive-biases.md`
    - `test -f examples/kahneman/comparisons/system-1-vs-system-2.md`
    - `test -f examples/kahneman/overviews/decision-making.md`
    - `test -f examples/kahneman/sources/src-2026-04-09-thinking-fast-and-slow-part1.md`
    - `test -f examples/kahneman/sources/src-2026-04-10-kahneman-prospect-theory.md`
    - `test ! -e wiki/entities/daniel-kahneman.md`
    - `test ! -e wiki/concepts/prospect-theory.md`
    - `test ! -e wiki/overviews/personal-decision-patterns.md`
    - `test ! -e wiki/sources/src-2026-04-10-personal-decision-journal.md`
    - `test ! -d wiki/maintenance` (REVIEWS.md HIGH #2 — TMPL-05 enforcement)
    - `find wiki -mindepth 1 -maxdepth 1 ! -name 'index.md' ! -name 'log.md' ! -name 'decisions' | wc -l` returns 0
    - `grep -c 'kahneman\|prospect-theory\|loss-aversion' wiki/index.md wiki/log.md` returns 0
    - `test -f wiki/decisions/dr-2026-04-15-kahneman-to-examples.md`
    - Decision record uses canonical schema (REVIEWS.md HIGH #1):
      - `grep -q '^type: decision' wiki/decisions/dr-2026-04-15-kahneman-to-examples.md`
      - `grep -q '^trigger_type: schema-update' wiki/decisions/dr-2026-04-15-kahneman-to-examples.md`
      - `grep -q '^epistemic_status: sourced' wiki/decisions/dr-2026-04-15-kahneman-to-examples.md`
      - `! grep -q '^date:' wiki/decisions/dr-2026-04-15-kahneman-to-examples.md` (forbidden non-canonical field)
      - `! grep -q '^class:' wiki/decisions/dr-2026-04-15-kahneman-to-examples.md` (forbidden non-canonical field)
      - All 7 required body sections present in order: `grep -nF '## TL;DR\|## Decision\|## Why\|## Alternatives Considered\|## Consequences\|## Affected Pages\|## Sources' wiki/decisions/dr-2026-04-15-kahneman-to-examples.md` returns 7 hits
    - `test -f examples/kahneman/README.md`
    - `grep -q '^example: true' examples/kahneman/README.md`
    - `grep -c '^## ' examples/kahneman/README.md | awk '{exit ($1 >= 5) ? 0 : 1}'` (at least 5 H2 sections)
    - `bash tests/phase-07/test_kahneman_moved.sh` exits 0
    - `bash tests/phase-07/test_kahneman_readme.sh` exits 0
    - `bash tests/phase-07/test_wiki_skeleton.sh` exits 0
    - `bash tests/phase-07/test_no_stale_kahneman_paths.sh` exits 0
  </acceptance_criteria>
  <done>Kahneman cluster relocated with wikilinks intact; local_only content deleted; `wiki/maintenance/` deleted; wiki skeleton-only; canonical-schema NEUT-07 record + NEUT-05 README committed; scoped Kahneman-string test passes; repo-wide stale-path scan passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend bin/lint.sh EXCLUDE_DIRS and honor example: true (NEUT-04)</name>
  <files>bin/lint.sh, tests/phase-07/test_lint_exclude.sh, tests/phase-07/fixtures/lint-exclude/examples/sample.md, tests/phase-07/fixtures/lint-exclude/wiki/marked.md, tests/phase-07/fixtures/lint-exclude/wiki/normal.md</files>
  <read_first>
    - bin/lint.sh (the ENTIRE file — need to understand EXCLUDE_DIRS at L178-180 AND the per-file iteration loop; current patterns are mandatory to respect)
    - .planning/phases/07-neutral-template-foundation/07-CONTEXT.md §D-09 (authority hierarchy)
    - .planning/phases/07-neutral-template-foundation/07-RESEARCH.md §Code Examples → Example 4 (lines 640–656)
  </read_first>
  <behavior>
    - Test 1: Fixture page under `examples/` is NOT linted (skipped via EXCLUDE_DIRS).
    - Test 2: Fixture page with `example: true` in frontmatter under `wiki/` is NOT linted (skipped via per-file check).
    - Test 3: Fixture page under `wiki/` with no `example` flag IS linted normally.
    - Test 4: Running `bash bin/lint.sh` on the real (post-relocation) repo does not report findings for any file under `examples/`.
  </behavior>
  <action>
    **1. Create fixtures:**

    `tests/phase-07/fixtures/lint-exclude/examples/sample.md`:
    ```
    ---
    type: concept
    title: Sample Under Examples
    privacy: cloud_safe
    ---
    # Sample
    Intentionally missing sources / malformed enough to trip lint if it ran.
    ```

    `tests/phase-07/fixtures/lint-exclude/wiki/marked.md`:
    ```
    ---
    type: concept
    title: Marked Example
    example: true
    privacy: cloud_safe
    ---
    # Marked
    Intentionally malformed; example: true must suppress lint.
    ```

    `tests/phase-07/fixtures/lint-exclude/wiki/normal.md`:
    ```
    ---
    type: concept
    title: Normal Page
    privacy: cloud_safe
    sources: []
    knowledge_domain: test
    epistemic_status: settled
    ---
    # Normal
    Content.
    ```

    **2. Modify `bin/lint.sh`:**

    At L178-180, replace `EXCLUDE_DIRS = {'maintenance'}` with `EXCLUDE_DIRS = {'maintenance', 'examples'}`. Keep `'maintenance'` for forward compatibility with downstream forks that regenerate maintenance reports locally; the public template ships without the directory.

    Locate the per-file iteration loop (the python block around L222 where each page's frontmatter is parsed). After frontmatter parse, insert:
    ```python
    if isinstance(frontmatter, dict) and frontmatter.get('example') is True:
        continue  # NEUT-04 (D-09 fallback): example: true suppresses all health checks
    ```
    Use the exact variable name lint.sh uses for the parsed frontmatter (grep the file first — may be `fm`, `frontmatter`, `meta`).

    Add a `WIKI_ROOT` override: lint.sh must respect an env var `WIKI_ROOT` (default `wiki`) so fixtures can be linted without touching the real tree. If the current script hardcodes `wiki/`, change it to `os.environ.get("WIKI_ROOT", "wiki")` in the python block. Preserve default behavior (no env var = lints wiki/).

    **3. Create `tests/phase-07/test_lint_exclude.sh`:**
      - `t1`: `WIKI_ROOT=tests/phase-07/fixtures/lint-exclude bash bin/lint.sh | grep -c 'examples/sample.md'` → must be 0.
      - `t2`: same run + `grep -c 'wiki/marked.md'` → must be 0.
      - `t3`: same run MAY report findings on `wiki/normal.md` (but the test does not require that — only asserts the exclude logic).
      - `t4`: Run `bash bin/lint.sh` on the real repo and assert `grep -c 'examples/'` in stdout is 0.

    Update `tests/phase-07/run.sh` to source this new test file (confirm the aggregator's glob picks it up — it should, since test_*.sh).

    Commit: `feat(07-02): lint.sh EXCLUDE_DIRS honors examples/ and example: true (NEUT-04)`.
  </action>
  <verify>
    <automated>bash tests/phase-07/test_lint_exclude.sh && bash tests/phase-07/run.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "'examples'" bin/lint.sh`
    - `grep -q "EXCLUDE_DIRS = {'maintenance', 'examples'}" bin/lint.sh || grep -qE "EXCLUDE_DIRS\s*=\s*\{.*'examples'.*'maintenance'|'maintenance'.*'examples'" bin/lint.sh`
    - `grep -q "example.*True\|example.*is True\|example.*true" bin/lint.sh` (frontmatter check)
    - `grep -q 'WIKI_ROOT' bin/lint.sh` (env var support)
    - `test -f tests/phase-07/fixtures/lint-exclude/examples/sample.md`
    - `test -f tests/phase-07/fixtures/lint-exclude/wiki/marked.md`
    - `test -f tests/phase-07/fixtures/lint-exclude/wiki/normal.md`
    - `WIKI_ROOT=tests/phase-07/fixtures/lint-exclude bash bin/lint.sh 2>&1 | grep -c 'examples/sample.md'` returns 0
    - `WIKI_ROOT=tests/phase-07/fixtures/lint-exclude bash bin/lint.sh 2>&1 | grep -c 'wiki/marked.md'` returns 0
    - `bash bin/lint.sh 2>&1 | grep -c 'examples/'` returns 0
    - `bash tests/phase-07/run.sh` exits 0
  </acceptance_criteria>
  <done>lint.sh honors examples/ + example: true; fixtures + test pass; real repo lint produces no examples/ findings.</done>
</task>

</tasks>

<verification>
Automated: `bash tests/phase-07/run.sh` exits 0 across all accumulated tests (requirements-sync + kahneman-moved + kahneman-readme + wiki-skeleton + no-stale-kahneman-paths + lint-exclude).

Manual (deferred to phase-gate): Open the repo in Obsidian (if available) and confirm `examples/kahneman/` wikilinks resolve. Full Obsidian verification is Phase 12 DEBT-01 — Phase 7 only needs the file-level assertion that slugs align.
</verification>

<success_criteria>
- NEUT-01: 7 Kahneman pages + 2 Kahneman sources under `examples/kahneman/`; wikilinks resolve.
- NEUT-04: lint.sh honors examples/ + `example: true`.
- NEUT-05: examples/kahneman/README.md has all 5 required sections.
- NEUT-07: Decision record committed using canonical AGENTS.md §4.6 schema (no `date`, no `class:`; all 7 required sections present).
- TMPL-05: wiki/ reduced to skeleton (index.md + log.md + decisions/ ONLY — no maintenance/, no content directories).
- local_only creator content (personal-decision-patterns, personal-decision-journal) deleted.
- No public file outside `examples/` and `wiki/decisions/` references the old `wiki/.../kahneman/...` paths.
</success_criteria>

<output>
Create `.planning/phases/07-neutral-template-foundation/07-02-SUMMARY.md` with a file-move table (source → destination), the deletion list (including `wiki/maintenance/`), the lint.sh diff summary, the NEUT-07 record path with the canonical-schema field list, and the result of the repo-wide stale-path scan.
</output>
</content>
</invoke>