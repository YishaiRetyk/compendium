---
phase: 12.1-neut-08-personal-term-denylist-curation
plan: 03
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - .planning/phases/12.1-neut-08-personal-term-denylist-curation/calibration-script.py
  - .planning/phases/12.1-neut-08-personal-term-denylist-curation/calibration-decisions.txt
  - .planning/phases/12.1-neut-08-personal-term-denylist-curation/extrapolation-draft.md
  - .planning/phases/12.1-neut-08-personal-term-denylist-curation/post-merge-evidence.txt
  - .neutrality-denylist.txt
autonomous: false
requirements:
  - NEUT-08
must_haves:
  truths:
    - "Calibration script (Python, seed=42, proportional-stratified across 3 source clusters per CONTEXT.md D-01 + D-02) exists and is reproducible: re-running with the same seed produces the same 20-line sample"
    - "User runs calibration script interactively; calibration-decisions.txt contains 20 lines of binary keep/skip decisions per CONTEXT.md D-03 (no free-text rationale collected)"
    - "Claude produces extrapolation-draft.md grouped by decision (kept-list with one-phrase rationale per term + rejected-list with reject-class labels) per CONTEXT.md D-04"
    - "User reviews/edits the extrapolation draft (this is the only LLM-mediated decision; final review is human per CONTEXT.md D-01 step 3)"
    - "After user review, the kept-list is appended verbatim under a single new unified header `# Category: Personal-vault terms expanded curation (NEUT-08, 2026-05-02)` per CONTEXT.md D-07. No per-cluster sub-headers (D-08); no per-term inline comments (load_denylist parser does not strip them — RESEARCH.md D-08 root cause)"
    - "N > 0 entries are appended (SPEC requirement #1 acceptance: N > 0; RESEARCH.md Pitfall 3 mitigation if calibration yields 0 keeps)"
    - "Every appended line either lives under a # Category: header OR is a # comment line; no bare uncategorized lines in the diff (SPEC acceptance criterion #1)"
    - "Curation rubric (SPEC §Constraints) honored: (a) proper noun / personal name / project codename, OR (b) multi-word phrase / hyphenated identifier unique to vault. Generic single-word English vocabulary rejected."
    - "After append, bash bin/check-neutrality.sh exits 0 against the live tree (validates Plan 02 + Plan 03 are mutually compatible — newly promoted terms must not introduce false positives across legitimately neutral wiki/docs/AGENTS.md prose)"
    - "tests/phase-12.1/run.sh continues to PASS (the contract test from Plan 01 finds the new unified header's first term as anchor and validates wiring)"
  artifacts:
    - path: ".planning/phases/12.1-neut-08-personal-term-denylist-curation/calibration-script.py"
      provides: "Reproducible 20-line stratified sampler (seed=42); workpaper utility, NOT under tests/ or bin/"
      contains: "SEED = 42"
    - path: ".planning/phases/12.1-neut-08-personal-term-denylist-curation/calibration-decisions.txt"
      provides: "20 binary keep/skip decisions from interactive calibration pass (workpaper)"
      min_lines: 22
    - path: ".planning/phases/12.1-neut-08-personal-term-denylist-curation/extrapolation-draft.md"
      provides: "Claude-extrapolated kept-list + rejected-list with reject-class labels per CONTEXT.md D-04 (workpaper, user-reviewed); rejected terms grouped into bullet sub-lists ≤20 terms per bullet per WARNING #6 reviewability bound"
      contains: "## Kept"
      max_lines: 200
    - path: ".neutrality-denylist.txt"
      provides: "Curated NEUT-08 personal-vault terms appended under unified Category header (single header, many terms per D-07)"
      contains: "# Category: Personal-vault terms expanded curation (NEUT-08, 2026-05-02)"
  key_links:
    - from: ".neutrality-denylist.txt unified NEUT-08 header"
      to: "tests/phase-12.1/test_neut08_live_denylist_contract.sh anchor extraction"
      via: "awk strict-match on the literal `# Category: Personal-vault terms expanded curation (NEUT-08, 2026-05-02)` header (per Plan 01 BLOCKER #1 fix; SPEC #3 fidelity)"
      pattern: "Personal-vault terms expanded curation \\(NEUT-08, 2026-05-02\\)"
    - from: ".planning/backlog-neutrality-denylist-candidate.txt"
      to: "calibration-script.py parse_clusters()"
      via: "# source: header parsing"
      pattern: "# source:"
    - from: "calibration-decisions.txt"
      to: "extrapolation-draft.md (Claude inputs)"
      via: "operator hands off file path; no automation"
      pattern: "calibration-decisions"
---

<objective>
Execute the calibration-then-extrapolation curation pattern locked in CONTEXT.md D-01..D-04 and append a vetted subset of `.planning/backlog-neutrality-denylist-candidate.txt` (855 candidate lines across 3 source clusters) into `.neutrality-denylist.txt` under a single new unified `# Category:` header per CONTEXT.md D-07.

The pattern is deliberately three-step: (1) deterministic stratified sampler (Python, seed=42) draws 20 representative lines; (2) user makes binary keep/skip per line in an interactive prompt; (3) Claude reads the candidate file + calibration log + SPEC rubric, produces a grouped kept/rejected draft, user reviews/edits, and the final kept-list is appended to the live denylist.

This plan has TWO checkpoints (steps 2 and 4 below): the calibration interaction REQUIRES the user, and the final review/edit of Claude's draft REQUIRES the user before the merge. `autonomous: false`. The user is the sole authority on the curation rubric judgment; Claude's role is sampling deterministically, extrapolating the rubric pattern from binary signals, and mechanically appending the user-approved kept-list.

Purpose: Closes SPEC requirement #1 (curated denylist merge) with the lightest-friction high-rigor approach selected in CONTEXT.md (rejected pure-editor as too tedious for 855 lines, rejected pure-Claude as security-adjacent decision, rejected pure-interactive as slowest). Independent of Plan 02 (lint-report.md frontmatter); both can land in either order. Plan 04 verification gates on completion of BOTH this plan AND Plan 02.

Output: 1 calibration script (Python workpaper), 1 calibration-decisions.txt (workpaper), 1 extrapolation-draft.md (workpaper, user-reviewed), and 1 mutation to `.neutrality-denylist.txt` (the only mutation that touches the live denylist).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-CONTEXT.md
@.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-SPEC.md
@.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-RESEARCH.md
@.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-PATTERNS.md
@.planning/backlog-neutrality-denylist-candidate.txt
@.neutrality-denylist.txt
@bin/check-neutrality.sh

<interfaces>
<!-- Inputs and parser contracts the executor needs. -->

.planning/backlog-neutrality-denylist-candidate.txt structure (verified 2026-05-02):
  - 861 total lines
  - 855 token lines under 3 # source: headers:
    * agents-md-size-risk.md (19 tokens, mostly generic English; expected ~0 promotions per SPEC)
    * git:wiki/overviews/personal-decision-patterns.md (437 tokens)
    * git:wiki/sources/src-2026-04-10-personal-decision-journal.md (399 tokens)
  - Header format: lines starting with `# source: <cluster-name>`
  - Token lines: bare lowercased strings, one per line, no inline comments

.neutrality-denylist.txt parser contract (bin/check-neutrality.sh load_denylist() lines 147-157):
  - Reads each line, strips whitespace, skips empty lines and lines starting with `#`
  - Does NOT strip trailing inline `# ...` comments (CRITICAL — RESEARCH.md D-08 root cause)
  - Implication: per-term inline source attribution like `term # from: cluster-name` would be loaded as the LITERAL string `term # from: cluster-name` (lowercased) and never match anything in scanned files. SILENT NEUTERING. Use ONLY bare term lines.

.neutrality-denylist.txt current state (verified 2026-05-02, 25 lines):
  - 2 existing # Category: headers + 17 active terms
  - Trailing newline present after final term `pre-commitment` (line 25)
  - Wave 1b appends starting on line 26 with a blank-line separator before the new header

Stratified arithmetic (per RESEARCH.md A2 verified):
  - round(20*437/855) = 10 (personal-decision-patterns)
  - round(20*399/855) = 9 (personal-decision-journal)
  - round(20*19/855)  = 0 → max(1, ...) floor lifts to 1 (agents-md-size-risk)
  - Total = 20

Existing # Category: header byte-format (verified in .neutrality-denylist.txt lines 1, 12):
  # Category: <Title> (<scope-tag>)
  # <free-text rationale, optional, can span multiple lines>
  <bare-term-1>
  <bare-term-2>
  ...
  (blank line)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author calibration-script.py per RESEARCH.md skeleton (lines 300-369)</name>
  <files>.planning/phases/12.1-neut-08-personal-term-denylist-curation/calibration-script.py</files>
  <read_first>.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-RESEARCH.md (lines 300-369: complete script skeleton; lines 21-25: D-01..D-03 invariants); .planning/backlog-neutrality-denylist-candidate.txt (verify line 1 starts with `# source:` and confirm cluster header format)</read_first>
  <action>
    Create `.planning/phases/12.1-neut-08-personal-term-denylist-curation/calibration-script.py` per the verbatim skeleton in RESEARCH.md lines 300-369. Key invariants to preserve (per PATTERNS.md "calibration-script.py" section):

    - Shebang: `#!/usr/bin/env python3`
    - Module docstring naming D-02 (seed=42 reproducibility) and D-03 (binary keep/skip, no free-text)
    - `from pathlib import Path` (stdlib only — zero new dependencies per CONTEXT.md Standard Stack)
    - `SEED = 42` constant at module scope (load-bearing per D-02)
    - `random.Random(SEED)` instance (NOT module-level `random.seed()` — deterministic per-instance regardless of any other random.* calls in the same process)
    - `parse_clusters()` recognizing `# source:` headers (line.startswith("# source:"))
    - `stratified_sample()` with proportional rule `quota = max(1, round(total * len(terms) / grand)) if terms else 0`, then `quota = min(quota, len(terms))`, then `rng.sample(terms, quota)`. Deterministic trim if rounding overshoots: `out[:total]`
    - Interactive `input()` loop accepting `keep|k|y` or `skip|s|n` (case-insensitive via `.strip().lower()`)
    - Tab-separated output: `<decision>\t<term>\t<source-cluster>` per line, prefixed by 2 `#` header lines
    - `if __name__ == "__main__": sys.exit(main())` exit-code discipline

    Make executable: `chmod +x .planning/phases/12.1-neut-08-personal-term-denylist-curation/calibration-script.py`.

    DO NOT run the script in this task. Task 2 is the user-checkpoint that runs it interactively.

    Per CONTEXT.md D-01..D-03 (calibration mechanics) + D-02 (seed=42 + proportional stratification) + Discretion (Python language choice).
  </action>
  <verify>
    <automated>test -x .planning/phases/12.1-neut-08-personal-term-denylist-curation/calibration-script.py && grep -qE "^SEED = 42$" .planning/phases/12.1-neut-08-personal-term-denylist-curation/calibration-script.py && grep -qE "random\\.Random\\(SEED\\)" .planning/phases/12.1-neut-08-personal-term-denylist-curation/calibration-script.py && grep -qE "# source:" .planning/phases/12.1-neut-08-personal-term-denylist-curation/calibration-script.py && python3 -c "import ast; ast.parse(open('.planning/phases/12.1-neut-08-personal-term-denylist-curation/calibration-script.py').read())"</automated>
  </verify>
  <done>
    Script exists, is executable, parses as valid Python 3 syntax, contains the load-bearing constants (SEED=42, random.Random(SEED) per-instance), and recognizes the `# source:` header format from the candidate file.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Run calibration script interactively (USER ACTION — produces calibration-decisions.txt)</name>
  <what-built>The Python calibration script from Task 1. It draws a deterministic 20-line proportional-stratified sample from `.planning/backlog-neutrality-denylist-candidate.txt` and prompts the user for binary keep/skip per line.</what-built>
  <how-to-verify>
    1. From the repo root, run:
       ```
       python3 .planning/phases/12.1-neut-08-personal-term-denylist-curation/calibration-script.py
       ```
    2. The script presents 20 prompts of the form `[<cluster>] '<term>'  keep/skip:`. Type `keep` or `k` or `y` to keep, `skip` or `s` or `n` to skip. Apply the SPEC §Constraints rubric:
       - **KEEP if** the term is (a) a proper noun / personal name / project codename, OR (b) a multi-word phrase or hyphenated identifier unique to your personal vault.
       - **SKIP if** the term is generic English vocabulary (the dominant content of all 3 candidate clusters).
       - When in doubt: SKIP. The phase is acceptable with N=1 keeps (per SPEC acceptance criterion #1: N > 0).
    3. After 20 decisions, the script writes `.planning/phases/12.1-neut-08-personal-term-denylist-curation/calibration-decisions.txt` with the format:
       ```
       # Phase 12.1 calibration decisions (seed=42, n=20)
       # Format: <decision>\t<term>\t<source-cluster>
       keep<TAB>articulate-the-want<TAB>git:wiki/overviews/personal-decision-patterns.md
       skip<TAB>accept<TAB>git:wiki/overviews/personal-decision-patterns.md
       ...
       ```
    4. Verify the output:
       - `wc -l .planning/phases/12.1-neut-08-personal-term-denylist-curation/calibration-decisions.txt` should report ≥ 22 (2 header + 20 decisions)
       - `grep -cE '^(keep|skip)\b' .planning/phases/12.1-neut-08-personal-term-denylist-curation/calibration-decisions.txt` should return 20

    Edge case (RESEARCH.md Pitfall 3): if you type `skip` 20 times (zero keeps), proceed anyway — Task 3 has a fallback path. Do NOT artificially keep terms that fail the rubric just to get a non-zero N.
  </how-to-verify>
  <resume-signal>Type "calibration complete" once `.planning/phases/12.1-neut-08-personal-term-denylist-curation/calibration-decisions.txt` exists with ≥ 20 keep/skip decisions.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Produce Claude extrapolation draft per CONTEXT.md D-04 schema</name>
  <files>.planning/phases/12.1-neut-08-personal-term-denylist-curation/extrapolation-draft.md</files>
  <read_first>.planning/backlog-neutrality-denylist-candidate.txt (all 861 lines), .planning/phases/12.1-neut-08-personal-term-denylist-curation/calibration-decisions.txt (the 20 binary signals), .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-SPEC.md (§Constraints — the binding rubric), .neutrality-denylist.txt (existing 17 entries — style reference for what "looks promotable")</read_first>
  <action>
    Read all 4 inputs end-to-end. Then produce `.planning/phases/12.1-neut-08-personal-term-denylist-curation/extrapolation-draft.md` matching the D-04 schema verbatim (per RESEARCH.md lines 444-471):

    ```markdown
    # Phase 12.1 — Claude-extrapolated curation draft

    **Source:** .planning/backlog-neutrality-denylist-candidate.txt (855 candidate lines)
    **Calibration anchor:** .planning/phases/12.1-neut-08-personal-term-denylist-curation/calibration-decisions.txt (20 binary signals)
    **Rubric:** SPEC §Constraints (proper noun / personal name / project codename / multi-word phrase / hyphenated identifier unique to vault; generic single-word English rejected)

    ## Kept (N terms)

    <one-phrase rationale per term, e.g.:>
    - `articulate-the-want` — hyphenated personal-vault identifier
    - `flinch-level` — hyphenated vault-coined term
    - `pre-commitment` — multi-word vault concept
    - ... (one rationale phrase per term, per D-04)

    ## Rejected (M terms)

    ### Class: generic-english (X terms)
    - accept, action, addition, after, ...

    ### Class: ambiguous-not-vault-specific (Y terms)
    - anchoring, articulate, applying, ...

    ### Class: off-rubric (Z terms)
    - <any others — single-word English that resembles codename but lacks vault uniqueness>

    ## Summary
    - Total candidates: 855
    - Kept: N
    - Rejected: M (generic-english=X + ambiguous-not-vault-specific=Y + off-rubric=Z; X+Y+Z = M)
    - Per-cluster contribution to kept-list: agents-md-size-risk.md=A, personal-decision-patterns.md=B, personal-decision-journal.md=C; A+B+C = N
    - Note: per RESEARCH.md A1, agents-md-size-risk.md cluster expected to yield ~0 promotions
    ```

    Inference procedure:
    1. Read the calibration-decisions.txt to identify the operative rubric the user is applying. The SPEC rubric is the prior; calibration decisions resolve borderline cases. If the user kept hyphenated terms like `articulate-the-want` and skipped generic single words like `accept`, the rubric is being applied as written.
    2. Walk all 855 candidate lines. For each, classify:
       - KEEP if it matches the rubric
       - REJECT into one of the three classes (generic-english | ambiguous-not-vault-specific | off-rubric)
    3. Group output as specified in the schema. Kept terms get one-phrase rationale per term. Rejected terms grouped by class. **Reviewability formatting bound (WARNING #6):** within each `### Class:` subsection, alphabetize terms and split into bullet sub-lists of AT MOST ~20 terms per bullet (e.g., one bullet for `a*-c*` words, next bullet for `d*-f*` words, etc., or strict 20-per-chunk if alphabetic clustering yields uneven spans). This caps the draft at ~200 lines total even for ~840 rejected terms — keeps the user's Task 4 review feasible. Total file size SHOULD NOT exceed 200 lines (per artifact `max_lines: 200`); if it does, tighten the chunking.
    4. Bias toward fewer terms — when in doubt about a token, REJECT. The SPEC explicitly forbids generic English; false positives across legitimately neutral wiki/docs prose are the failure mode.

    Edge case handling per RESEARCH.md Pitfall 3:
    - If calibration produced 0 keeps: Claude scans the 855 lines for hyphenated identifiers / multi-word phrases / proper nouns directly (those are categorically the targets). Lines like `articulate-the-want`, `flinch-level`, `pre-commitment`, `after-the-fact` already appear in the candidate file under personal-decision-patterns cluster (RESEARCH.md A1 names ~5-10 such candidates as plausibly present). Promote them to the draft kept-list with the standard rubric labels.
    - If after both calibration AND direct scan, zero terms qualify: STOP and escalate to user via Task 4 with an explicit "no qualifying terms found under SPEC rubric" note. The SPEC acceptance #1 still requires N > 0; if N = 0 the phase has uncovered a gap in the rubric or the candidate file and needs human resolution before merge.

    DO NOT mutate `.neutrality-denylist.txt` in this task. Task 5 (after user review in Task 4) is the only mutation to the live denylist.

    Per CONTEXT.md D-01 step 3 (Claude-extrapolated draft) + D-04 (schema) + RESEARCH.md Pitfall 3 mitigation.
  </action>
  <verify>
    <automated>test -f .planning/phases/12.1-neut-08-personal-term-denylist-curation/extrapolation-draft.md && grep -qE "^## Kept" .planning/phases/12.1-neut-08-personal-term-denylist-curation/extrapolation-draft.md && grep -qE "^## Rejected" .planning/phases/12.1-neut-08-personal-term-denylist-curation/extrapolation-draft.md && grep -qE "^## Summary" .planning/phases/12.1-neut-08-personal-term-denylist-curation/extrapolation-draft.md && grep -qE "^### Class:" .planning/phases/12.1-neut-08-personal-term-denylist-curation/extrapolation-draft.md && [ "$(wc -l < .planning/phases/12.1-neut-08-personal-term-denylist-curation/extrapolation-draft.md)" -le 200 ]</automated>
  </verify>
  <done>
    Draft exists with the four required sections (Kept / Rejected / Class: subsections / Summary). Each kept term has a one-phrase rationale. Rejected terms are grouped by class with bullet sub-lists of ≤20 terms each (WARNING #6 reviewability bound). Total file ≤200 lines. Summary contains explicit N (kept count) and M (rejected count) integers, with class counts that sum correctly.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: User reviews/edits extrapolation-draft.md (USER CHECKPOINT — final rubric authority)</name>
  <what-built>The Claude-extrapolated kept-list + rejected-list at `.planning/phases/12.1-neut-08-personal-term-denylist-curation/extrapolation-draft.md`. Claude inferred the operative rubric from your 20 calibration decisions and walked all 855 candidate lines.</what-built>
  <how-to-verify>
    1. Open `.planning/phases/12.1-neut-08-personal-term-denylist-curation/extrapolation-draft.md`.
    2. Review the **Kept** section. For each term, ask:
       - Does this match the SPEC rubric (proper noun / personal name / project codename / multi-word phrase / hyphenated identifier unique to your personal vault)?
       - If NO → delete the bullet from the Kept section, move it under an appropriate Rejected class.
    3. Review the **Rejected** section by class (use grep/skim). For each class:
       - `generic-english` → most should stay rejected. Spot-check a sample.
       - `ambiguous-not-vault-specific` → spot-check; promote to Kept ONLY if the term truly is vault-unique.
       - `off-rubric` → review any unusual classifications.
    4. (Optional) Reorder Kept terms (alphabetical, by-cluster, or clustered-by-similarity — Claude's Discretion per CONTEXT.md). User preference governs.
    5. Update the Summary block at the bottom: ensure the integer counts (N, M, X, Y, Z, A, B, C) match the actual edited lists. Discrepancies between Summary and actual lists will fail Plan 04's verification artifact authoring.
    6. **Decision point — ESCALATE if N = 0:**
       - If your final Kept list is empty, STOP. Per SPEC acceptance criterion #1, N > 0 is required. Either (a) revisit the rubric with the user — perhaps a borderline term should be promoted; (b) abort the phase and document the rubric gap.
       - If N > 0 (even N = 1 satisfies SPEC), proceed to Task 5.

    Approval signal: confirm the Kept list reflects your final intent. The next task (Task 5) appends the Kept list verbatim to `.neutrality-denylist.txt` — no further review opportunity before merge.
  </how-to-verify>
  <resume-signal>Type "approved" with the final integer N (kept-term count) for Task 5 to use, OR type "escalate: <reason>" if N = 0 and rubric needs revision.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Append user-approved kept-list under unified Category header to .neutrality-denylist.txt + verify gate exit 0 + test passes</name>
  <files>.neutrality-denylist.txt</files>
  <read_first>.neutrality-denylist.txt (current 25-line state — append target), .planning/phases/12.1-neut-08-personal-term-denylist-curation/extrapolation-draft.md (user-approved Kept list — source of terms), .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-CONTEXT.md (D-07 + D-08 — header invariants)</read_first>
  <action>
    Append to `.neutrality-denylist.txt` (NEVER rewrite the existing 25 lines — pure append):

    1. One blank line separator after the existing final term `pre-commitment` (line 25).
    2. The unified Category header block:
       ```
       # Category: Personal-vault terms expanded curation (NEUT-08, 2026-05-02)
       # Curated subset of .planning/backlog-neutrality-denylist-candidate.txt under
       # the SPEC rubric (proper noun / personal name / project codename / multi-word
       # phrase / hyphenated identifier unique to vault). Generic English vocabulary
       # rejected per false-positive cascade analysis. See .planning/phases/12.1-
       # neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md for kept/
       # rejected counts and rubric-class breakdown.
       ```
    3. Each user-approved Kept term from `extrapolation-draft.md`, ONE per line, BARE (no inline comments — the load_denylist parser at `bin/check-neutrality.sh:147-157` does NOT strip trailing comments and would silently neuter the entry — RESEARCH.md D-08 root cause). Lowercase form (matches existing convention in the file).
    4. Trailing newline at EOF (POSIX text-file convention; matches current file state).

    Format invariants (per PATTERNS.md ".neutrality-denylist.txt" + CONTEXT.md D-07/D-08):
    - Single header line starting `# Category: `
    - Optional `#`-prefix rationale lines below the header (no parser meaning; prose-only)
    - Bare term lines: lowercase, one term per line, NO inline `#` comments, NO trailing whitespace
    - Blank line between categories (preserve existing blank-line separator pattern)
    - NO sub-headers per cluster (forbidden by D-07/D-08; per-cluster traceability lives in extrapolation-draft.md + Plan 04 VERIFICATION.md)
    - NO per-term rubric-class tags (forbidden by Deferred Ideas — promoted terms are bare)

    After appending, run two verification commands and capture verbatim output to a workpaper:
    1. `bash bin/check-neutrality.sh; echo "exit: $?"` (must exit 0 — newly promoted terms must NOT trip on legitimately neutral wiki/docs/AGENTS.md prose; if exit ≠ 0, the executor STOPS, identifies the false-positive term(s), removes them from the curated set, and re-runs)
    2. `bash tests/phase-12.1/run.sh` (must report `PHASE 12.1 TESTS: 1/1` — Plan 01's strict-header awk anchors ONLY on the literal `# Category: Personal-vault terms expanded curation (NEUT-08, 2026-05-02)` header per BLOCKER #1 / SPEC #3 fidelity, so the test was RED before this task and flips to GREEN as soon as the unified header + first kept term land in `.neutrality-denylist.txt`)

    Capture both outputs to `.planning/phases/12.1-neut-08-personal-term-denylist-curation/post-merge-evidence.txt`:
    ```
    { echo "=== bash bin/check-neutrality.sh ===" ; bash bin/check-neutrality.sh; echo "exit: $?" ; echo "=== bash tests/phase-12.1/run.sh ===" ; bash tests/phase-12.1/run.sh ; } > .planning/phases/12.1-neut-08-personal-term-denylist-curation/post-merge-evidence.txt 2>&1
    ```

    The post-merge evidence file is consumed by Plan 04's verification artifact per D-10.

    EDGE CASE: If `bash bin/check-neutrality.sh` returns exit ≠ 0 after the append, parse the output for the offending term(s) and file path(s). Re-open `extrapolation-draft.md`, demote the term to Rejected with class `false-positive-on-neutral-text`, regenerate the appended block, and re-run. This loop is acceptable; it is the curation-loop in action.

    Per CONTEXT.md D-07 (unified header) + D-08 (no inline comments) + SPEC requirement #1 acceptance + Pitfall 5 (count must match git-diff at Plan 04 time).
  </action>
  <verify>
    <automated>grep -cE "^# Category: Personal-vault terms expanded curation \\(NEUT-08, 2026-05-02\\)$" .neutrality-denylist.txt | grep -q "^1$" && [ "$(awk '/^# *Category: Personal-vault terms expanded curation/,0' .neutrality-denylist.txt | grep -v '^#' | grep -vE '^[[:space:]]*$' | wc -l)" -gt 0 ] && bash bin/check-neutrality.sh && bash tests/phase-12.1/run.sh && test -f .planning/phases/12.1-neut-08-personal-term-denylist-curation/post-merge-evidence.txt && grep -q "^exit: 0$" .planning/phases/12.1-neut-08-personal-term-denylist-curation/post-merge-evidence.txt && grep -qE "^PHASE 12\\.1 TESTS: 1/1$" .planning/phases/12.1-neut-08-personal-term-denylist-curation/post-merge-evidence.txt</automated>
  </verify>
  <done>
    Unified `# Category: Personal-vault terms expanded curation (NEUT-08, 2026-05-02)` header exists exactly once. At least one bare term line follows it under the same category block. `bash bin/check-neutrality.sh` exits 0 (newly promoted terms do not introduce false positives). `bash tests/phase-12.1/run.sh` reports `PHASE 12.1 TESTS: 1/1`. Post-merge evidence file captured for Plan 04 consumption.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate file → curation rubric → live denylist | LLM-mediated extrapolation step bridges raw token list and security-adjacent live denylist; human review is the trust anchor |
| live denylist → public-path scanning | New terms enforce against AGENTS.md, CLAUDE.md, README.md, docs/, .github/, examples/, bin/ paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.1-01 | Tampering | calibration script seed | mitigate | `SEED = 42` constant + `random.Random(SEED)` per-instance ensures reproducibility — any operator (or future audit) can re-run and produce the same 20-line sample. Documented inline in script. |
| T-12.1-03 | Denial of Service | Generic English promotion → false-positive cascade | mitigate | Curation rubric (SPEC §Constraints) explicitly rejects generic English; user reviews Claude's draft before merge (Task 4); Task 5 immediately re-runs `bash bin/check-neutrality.sh` after append — exit ≠ 0 triggers explicit demote-and-retry loop. |
| T-12.1-05 | Information Disclosure | calibration-decisions.txt + extrapolation-draft.md → committed to git | accept | Decisions are operational ephemera; the kept terms ARE the denylist (already public via `.neutrality-denylist.txt`); the rejected terms are bare English vocabulary. Workpapers stay under `.planning/phases/12.1-...` per Open Question 2 recommendation. |
</threat_model>

<verification>
After all 5 tasks land:

1. `.planning/phases/12.1-neut-08-personal-term-denylist-curation/calibration-script.py` exists, executable, parses as Python 3, contains `SEED = 42` and `random.Random(SEED)`
2. `.planning/phases/12.1-neut-08-personal-term-denylist-curation/calibration-decisions.txt` exists with `grep -cE '^(keep|skip)\b'` = 20
3. `.planning/phases/12.1-neut-08-personal-term-denylist-curation/extrapolation-draft.md` exists with the 4 required sections (Kept, Rejected, Class:, Summary)
4. `.neutrality-denylist.txt` contains exactly one `# Category: Personal-vault terms expanded curation (NEUT-08, 2026-05-02)` header line
5. At least one bare term line exists under that header (N > 0 per SPEC acceptance #1)
6. No bare uncategorized lines exist anywhere in the appended diff (SPEC acceptance criterion #1)
7. `bash bin/check-neutrality.sh; echo "exit: $?"` returns `exit: 0` (gate clean post-merge — REQUIRES Plan 02 has also landed; if Plan 02 not yet landed, expect exit 2 from lint-report.md hits, which is OK because Plan 04 verification gates on BOTH plans complete)
8. `bash tests/phase-12.1/run.sh` returns `PHASE 12.1 TESTS: 1/1`
9. Post-merge evidence captured at `.planning/phases/12.1-neut-08-personal-term-denylist-curation/post-merge-evidence.txt`

ORDERING NOTE: If Plan 02 is run BEFORE Plan 03, verification step 7 passes immediately. If Plan 03 lands BEFORE Plan 02, expect `exit: 2` from the lint-report.md hits — capture as `expected pre-Plan-02 baseline` and proceed. Plan 04 gates on both plans landed.
</verification>

<success_criteria>
- All 4 workpaper artifacts exist (calibration-script.py, calibration-decisions.txt, extrapolation-draft.md, post-merge-evidence.txt)
- `.neutrality-denylist.txt` carries exactly one new unified `# Category:` header per CONTEXT.md D-07 with N > 0 bare term lines under it
- All promoted terms satisfy the SPEC rubric (proper noun / personal name / project codename / multi-word phrase / hyphenated identifier unique to vault); zero generic single-word English vocabulary lines added
- Post-merge `bash tests/phase-12.1/run.sh` reports `PHASE 12.1 TESTS: 1/1`
- Post-merge `bash bin/check-neutrality.sh` returns exit 0 once Plan 02 has also landed (the two plans together restore live-tree gate exit 0)
- Zero modifications to `bin/check-neutrality.sh`, `.github/workflows/neutrality.yml`, or any other Phase 7 infrastructure (SPEC out-of-scope)
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-03-SUMMARY.md` documenting:
- Final integer N (kept-term count) — REQUIRED for Plan 04 verification artifact (SPEC acceptance criterion #5: N must match `git diff <phase-base>..HEAD -- .neutrality-denylist.txt | grep -cE '^\+[^#+]'`)
- Per-cluster contribution to N (A=agents-md-size-risk, B=personal-decision-patterns, C=personal-decision-journal; A+B+C=N) per RESEARCH.md content checklist line 572
- Rejection class breakdown (X=generic-english, Y=ambiguous-not-vault-specific, Z=off-rubric; X+Y+Z = 855 - N) per CONTEXT.md D-10 + Plan 04 consumption
- Output of `bash bin/check-neutrality.sh; echo "exit: $?"` post-merge (verbatim from post-merge-evidence.txt)
- Output of `bash tests/phase-12.1/run.sh` post-merge (verbatim from post-merge-evidence.txt)
- Note any false-positive demote-loop iterations (if any term initially failed `bin/check-neutrality.sh` exit 0 and was demoted)
- Confirmation that `git diff <phase-base>..HEAD -- bin/check-neutrality.sh` is empty (no source modifications) — phase-base SHA from STATE.md or `git log --format=%H -n 1 -- .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-SPEC.md`
</output>
