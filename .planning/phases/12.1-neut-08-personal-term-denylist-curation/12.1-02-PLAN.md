---
phase: 12.1-neut-08-personal-term-denylist-curation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - wiki/maintenance/lint-report.md
  - .planning/phases/12.1-neut-08-personal-term-denylist-curation/pre-fix-check-neutrality.txt
  - .planning/phases/12.1-neut-08-personal-term-denylist-curation/post-fix-check-neutrality.txt
autonomous: true
requirements:
  - NEUT-08
must_haves:
  truths:
    - "wiki/maintenance/lint-report.md frontmatter contains neutrality_exempt: true (mirrors 3 in-repo precedents per CONTEXT.md Discretion + RESEARCH.md Pitfall 2)"
    - "Pre-fix evidence captured first: bash bin/check-neutrality.sh; echo $? prints exit: 2 with hits at wiki/maintenance/lint-report.md:38 (kahneman) and :39 (personal-decision-journal). This is REQUIRED evidence for the Plan 04 verification artifact per CONTEXT.md D-10."
    - "Post-fix evidence: bash bin/check-neutrality.sh; echo $? prints exit: 0 in isolation (independent of any curation work in Plan 03)"
    - "wiki/maintenance/lint-report.md body is unchanged (SPEC out-of-scope: no rewriting/filtering the body of lint-report.md itself)"
    - "bin/check-neutrality.sh source is unchanged (SPEC out-of-scope: no modification to gate semantics)"
  artifacts:
    - path: "wiki/maintenance/lint-report.md"
      provides: "frontmatter neutrality_exempt: true exemption with rationale comment matching 3 precedents"
      contains: "neutrality_exempt: true"
    - path: ".planning/phases/12.1-neut-08-personal-term-denylist-curation/pre-fix-check-neutrality.txt"
      provides: "Verbatim pre-state evidence (exit 2 + 2 hits) for Plan 04 verification artifact (D-10)"
      contains: "exit: 2"
    - path: ".planning/phases/12.1-neut-08-personal-term-denylist-curation/post-fix-check-neutrality.txt"
      provides: "Verbatim post-state evidence (exit 0) for Plan 04 verification artifact (D-10)"
      contains: "exit: 0"
    - path: ".planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-02-SUMMARY.md"
      provides: "Captured pre-fix and post-fix bin/check-neutrality.sh exit-code evidence (verbatim) for Plan 04 verification artifact (D-10)"
      contains: "exit: 2"
  key_links:
    - from: "wiki/maintenance/lint-report.md frontmatter"
      to: "bin/check-neutrality.sh has_neutrality_exempt() at lines 180-185"
      via: "regex ^neutrality_exempt:\\s*true\\b (case-insensitive)"
      pattern: "neutrality_exempt:\\s*true"
---

<objective>
Restore `bin/check-neutrality.sh` to exit 0 against the live tree by adding `neutrality_exempt: true` to the frontmatter of `wiki/maintenance/lint-report.md`. The file currently legitimately names `kahneman` (line 38) and `personal-decision-journal` (line 39) inside drift-detection findings; the established remedy across three other in-repo precedents (`wiki/index.md:17`, `wiki/decisions/dr-2026-04-15-kahneman-to-examples.md:23`, `wiki/decisions/dr-2026-04-16-progressive-disclosure-extraction.md:22`) is per-file frontmatter exemption.

CRITICAL ORDERING: This plan MUST capture pre-fix `bash bin/check-neutrality.sh; echo $?` evidence BEFORE applying the frontmatter edit. Per CONTEXT.md D-10 + RESEARCH.md Pitfall 2, the verification artifact (Plan 04) requires both pre-state (showing the lint-report.md hits) and post-state (showing exit 0) to be captured verbatim. If the pre-state is captured AFTER the frontmatter edit, evidence is lost and the verification artifact cannot be authored faithfully.

Purpose: Independent surgical fix — addresses SPEC requirement #2 (live-tree gate exit 0) in isolation from the curation work in Plan 03. Plans 02 and 03 are independent (zero shared file modifications) and can land in either order.

Output: One frontmatter-line edit in `wiki/maintenance/lint-report.md` (no body edits) + two evidence workpapers + a SUMMARY.md capturing pre/post evidence for downstream consumption by Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-CONTEXT.md
@.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-SPEC.md
@.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-RESEARCH.md
@.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-PATTERNS.md
@wiki/maintenance/lint-report.md
@wiki/index.md
@wiki/decisions/dr-2026-04-15-kahneman-to-examples.md
@wiki/decisions/dr-2026-04-16-progressive-disclosure-extraction.md
@bin/check-neutrality.sh

<interfaces>
<!-- Three in-repo precedents (verified) for the neutrality_exempt frontmatter pattern. -->
<!-- Format invariants: single-line YAML key, value true (no quotes), inline # comment after 2+ spaces, -->
<!-- closing parenthetical (check-neutrality.sh) referencing the scanner. -->

Precedent 1 — wiki/index.md:17:
  neutrality_exempt: true  # The ## Decisions section must reference dr-2026-04-15-kahneman-to-examples by its canonical slug; the wikilink target is structurally fixed and §8 rule 3 forbids display aliases. Narrow exemption for the DR catalog.

Precedent 2 — wiki/decisions/dr-2026-04-15-kahneman-to-examples.md:23:
  neutrality_exempt: true  # This record IS the history of the Kahneman-to-examples relocation; it legitimately names what it relocated. Scanner skips this file (check-neutrality.sh).

Precedent 3 — wiki/decisions/dr-2026-04-16-progressive-disclosure-extraction.md:22:
  neutrality_exempt: true  # Why-section legitimately names the rejected `examples/kahneman/` reuse and the prior dr-2026-04-15-kahneman-to-examples decision; mirrors the dr-2026-04-15 precedent (check-neutrality.sh).

bin/check-neutrality.sh has_neutrality_exempt() at lines 180-185:
  Regex anchors at ^neutrality_exempt:, case-insensitive, allows whitespace before true.
  Pattern: ^neutrality_exempt:\s*true\b — accepts ANY whitespace (spaces, tabs, multiple) between `:` and `true`.
  Any placement within the existing --- delimited frontmatter block satisfies the regex.
  Recommended placement: after knowledge_domain: "" line 21, before closing ---.

  IMPORTANT (BLOCKER #2 fix): the verify regex MUST mirror the loader contract's
  whitespace tolerance — match `[[:space:]]*` between `:` and `true`, not a literal
  two-space sequence. A typo producing one space, a tab, or three spaces would still
  satisfy the gate but would fail an over-strict verify. The post-fix exit-0
  capture is the authoritative behavior assertion.

wiki/maintenance/lint-report.md current frontmatter (verified, 22 lines):
  ---
  id: lint-report
  title: Lint Report
  type: overview
  status: active
  summary: "Wiki health-check findings from most recent lint run."
  created_at: 2026-04-30
  updated_at: 2026-05-01
  sources: []
  epistemic_status: sourced
  tags:
    - meta
    - maintenance
  domains: []
  supersedes:
  superseded_by:
  privacy: cloud_safe
  aliases:
    - Lint Report
  has_contradictions: false
  knowledge_domain: ""
  ---

  Body lines 38-39 contain (verified) the strings `kahneman` and `personal-decision-journal`
  inside drift-detection findings. These MUST NOT be removed or edited (SPEC out-of-scope).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture pre-fix bin/check-neutrality.sh evidence (MUST happen BEFORE Task 2)</name>
  <files>.planning/phases/12.1-neut-08-personal-term-denylist-curation/pre-fix-check-neutrality.txt</files>
  <read_first>.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-RESEARCH.md (Pitfall 2 evidence at lines 270-276 documents the exact expected output)</read_first>
  <action>
    Run `bash bin/check-neutrality.sh; echo "exit: $?"` (capturing both stdout and stderr) and write the verbatim output to `.planning/phases/12.1-neut-08-personal-term-denylist-curation/pre-fix-check-neutrality.txt`.

    Exact command:
    ```
    { bash bin/check-neutrality.sh; echo "exit: $?"; } > .planning/phases/12.1-neut-08-personal-term-denylist-curation/pre-fix-check-neutrality.txt 2>&1
    ```

    Expected content per RESEARCH.md Pitfall 2 (verified 2026-05-02):
    ```
    wiki/maintenance/lint-report.md:38: kahneman
    wiki/maintenance/lint-report.md:39: personal-decision-journal
    exit: 2
    ```

    If the captured output does NOT show `exit: 2` with hits at `wiki/maintenance/lint-report.md:38` and `wiki/maintenance/lint-report.md:39`, STOP — the live-tree state has drifted from the research baseline and the executor must investigate before proceeding (additional hits would imply more files need exemption; zero hits would imply the regression has already been fixed by another commit).

    This file is the verbatim pre-state evidence consumed by Plan 04's verification artifact per D-10. Do NOT edit or summarize the captured output — paste verbatim.

    CRITICAL: this task captures evidence FROM THE LIVE TREE BEFORE Task 2 mutates it. If Task 2 runs first, the evidence is lost (exit code becomes 0 due to the frontmatter fix, masking the original regression).
  </action>
  <verify>
    <automated>test -f .planning/phases/12.1-neut-08-personal-term-denylist-curation/pre-fix-check-neutrality.txt && grep -q "^exit: 2$" .planning/phases/12.1-neut-08-personal-term-denylist-curation/pre-fix-check-neutrality.txt && grep -qE "wiki/maintenance/lint-report\\.md:38: kahneman" .planning/phases/12.1-neut-08-personal-term-denylist-curation/pre-fix-check-neutrality.txt && grep -qE "wiki/maintenance/lint-report\\.md:39: personal-decision-journal" .planning/phases/12.1-neut-08-personal-term-denylist-curation/pre-fix-check-neutrality.txt</automated>
  </verify>
  <done>
    Pre-fix evidence file exists, contains `exit: 2`, and shows both expected hits. Live-tree baseline matches the research baseline (no unexpected drift).
  </done>
</task>

<task type="auto">
  <name>Task 2: Insert neutrality_exempt: true frontmatter into wiki/maintenance/lint-report.md + verify post-fix exit 0</name>
  <files>wiki/maintenance/lint-report.md, .planning/phases/12.1-neut-08-personal-term-denylist-curation/post-fix-check-neutrality.txt</files>
  <read_first>wiki/maintenance/lint-report.md (current frontmatter, lines 1-22), wiki/decisions/dr-2026-04-15-kahneman-to-examples.md (precedent 2 line 23 — closest analog by phrasing)</read_first>
  <action>
    Use the Edit tool to insert exactly ONE new line into `wiki/maintenance/lint-report.md` immediately after `knowledge_domain: ""` (line 21) and immediately before the closing `---` (line 22). The new line is verbatim:

    ```
    neutrality_exempt: true  # Lint reports enumerate drift findings by file path; legitimately naming `kahneman` and `personal-decision-journal` (lines 38-39) is unavoidable when summarizing what the lint detected. Scanner skips this file (check-neutrality.sh).
    ```

    Format invariants (per PATTERNS.md "Per-file frontmatter exemption" + 3 precedents):
    - Key: `neutrality_exempt:` (lowercase, snake_case per CLAUDE.md §3)
    - Value: `true` (bare boolean, no quotes)
    - Two spaces between value and the inline `#` comment (matches the precedents — but the gate's loader regex `^neutrality_exempt:\s*true\b` accepts ANY whitespace, so the gate-side check is whitespace-tolerant)
    - Comment names WHY the file legitimately discusses denylisted terms
    - Comment closes with the parenthetical `(check-neutrality.sh)` referencing the scanner script (mirrors precedents 2 and 3 exactly)
    - Backticks around `kahneman` and `personal-decision-journal` per established style of precedent 3

    Do NOT touch any other line in the file. Do NOT touch the body (SPEC out-of-scope: no rewriting/filtering of lint-report.md body content). Do NOT touch any other file. Do NOT touch `bin/check-neutrality.sh` (SPEC out-of-scope).

    After the edit, capture post-fix evidence:
    ```
    { bash bin/check-neutrality.sh; echo "exit: $?"; } > .planning/phases/12.1-neut-08-personal-term-denylist-curation/post-fix-check-neutrality.txt 2>&1
    ```

    Expected post-fix content: `exit: 0` (no hits, gate clean). Per CONTEXT.md D-10 the verification artifact will paste both files verbatim.

    Per RESEARCH.md Implementation Order Wave 1a + RESEARCH.md Pattern 2 + CONTEXT.md Discretion (rationale wording).
  </action>
  <verify>
    <automated>grep -cE "^neutrality_exempt:[[:space:]]*true\\b" wiki/maintenance/lint-report.md | grep -q "^1$" && grep -qF "(check-neutrality.sh)" wiki/maintenance/lint-report.md && test -f .planning/phases/12.1-neut-08-personal-term-denylist-curation/post-fix-check-neutrality.txt && grep -q "^exit: 0$" .planning/phases/12.1-neut-08-personal-term-denylist-curation/post-fix-check-neutrality.txt && bash bin/check-neutrality.sh; rc=$?; [ "$rc" -eq 0 ] && [ "$(git diff --name-only HEAD -- bin/check-neutrality.sh | wc -l)" -eq 0 ]</automated>
  </verify>
  <done>
    `wiki/maintenance/lint-report.md` frontmatter contains exactly one `neutrality_exempt: true` line (matched by the gate loader's whitespace-tolerant regex `^neutrality_exempt:[[:space:]]*true\b`) with the rationale comment ending in `(check-neutrality.sh)`. Post-fix capture file shows `exit: 0` AND a fresh invocation of `bash bin/check-neutrality.sh` exits 0 (authoritative behavior assertion). `bin/check-neutrality.sh` source unchanged (zero diff from HEAD). Body of `wiki/maintenance/lint-report.md` is unchanged (only one line added in the frontmatter region).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontmatter exemption → gate bypass | Per-file exemption could in principle suppress legitimate leaks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.1-02 | Elevation of Privilege | wiki/maintenance/lint-report.md → bin/check-neutrality.sh exemption bypass | mitigate | wiki/maintenance/ is documented as a control-plane directory in CLAUDE.md (excluded from wiki/index.md); the file enumerates drift findings BY DESIGN, mirroring 3 prior in-repo precedents (wiki/index.md, dr-2026-04-15, dr-2026-04-16). The exemption is narrow (one file) and the contract test (Plan 01) still fires on PUBLIC paths under tests/phase-12.1/. The scanner continues to enforce on docs/, AGENTS.md, CLAUDE.md, README.md, .github/, examples/, bin/ paths unchanged. |
| T-12.1-10 | Tampering | Verify regex over-strict on whitespace | mitigate | Verify uses `^neutrality_exempt:[[:space:]]*true\b` (loader-mirrored, whitespace-tolerant) plus authoritative `bash bin/check-neutrality.sh` exit-0 assertion — typos producing 1/3 spaces or a tab still pass verify if the gate accepts them (BLOCKER #2 fix from checker). |
</threat_model>

<verification>
After both tasks land:

1. `grep -cE "^neutrality_exempt:[[:space:]]*true\b" wiki/maintenance/lint-report.md` returns 1 (exactly one match; whitespace-tolerant per the gate loader contract)
2. `bash bin/check-neutrality.sh; echo "exit: $?"` returns `exit: 0` against the live tree (authoritative behavior assertion)
3. `git diff --name-only HEAD -- bin/check-neutrality.sh` returns empty (no source modifications)
4. `git diff HEAD -- wiki/maintenance/lint-report.md | grep -cE "^[+-][^+-]"` returns 1 (exactly one line added; zero lines removed)
5. Pre-fix and post-fix evidence files exist under `.planning/phases/12.1-neut-08-personal-term-denylist-curation/` for Plan 04 consumption
</verification>

<success_criteria>
- `wiki/maintenance/lint-report.md` frontmatter contains `neutrality_exempt: true` with the precedent-aligned rationale comment ending in `(check-neutrality.sh)`
- `bash bin/check-neutrality.sh; echo $?` prints `exit: 0` against the post-fix tree (authoritative; whitespace-tolerant verify regex backs it up)
- `git diff <phase-base>..HEAD -- bin/check-neutrality.sh` is empty (gate source unchanged — verified by Plan 04)
- `wiki/maintenance/lint-report.md` body lines 38-39 still contain `kahneman` and `personal-decision-journal` strings (NOT removed; only frontmatter mutated)
- Pre-fix and post-fix evidence captured verbatim under `.planning/phases/12.1-neut-08-personal-term-denylist-curation/{pre,post}-fix-check-neutrality.txt` for Plan 04
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-02-SUMMARY.md` documenting:
- The single-line frontmatter edit (paste the new line verbatim)
- The two evidence files (paths) and their contents (verbatim)
- Confirmation that `bin/check-neutrality.sh` source is unchanged (`git diff` output)
- Confirmation that body lines 38-39 of `wiki/maintenance/lint-report.md` are unchanged
- Note: this plan independently restores live-tree gate exit 0; Plan 03 (curation) addresses the separate SPEC requirement #1 (curated denylist merge)
</output>
</output>
