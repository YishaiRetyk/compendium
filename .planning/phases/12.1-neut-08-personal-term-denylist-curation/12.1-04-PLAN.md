---
phase: 12.1-neut-08-personal-term-denylist-curation
plan: 04
type: execute
wave: 3
depends_on:
  - 01
  - 02
  - 03
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md
  - .planning/phases/12.1-neut-08-personal-term-denylist-curation/sync-evidence.txt
  - .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-04-SUMMARY.md
autonomous: true
requirements:
  - NEUT-08
must_haves:
  truths:
    - "REQUIREMENTS.md NEUT-08 bullet (line 36) flipped from `[ ]` to `[x]`; status note rewritten to `Complete (Phase 12.1)` while preserving the Phase 7 → Phase 12.1 reassignment lineage (SPEC requirement #4)"
    - "REQUIREMENTS.md traceability matrix row (line 215) flipped from `| NEUT-08 | Phase 12.1 | Pending |` to `| NEUT-08 | Phase 12.1 | Complete |` (SPEC requirement #4 acceptance grep)"
    - "12.1-VERIFICATION.md exists at .planning/phases/12.1-neut-08-personal-term-denylist-curation/ with Phase 7 mirror frontmatter + Phase 12 newer phase_base_sha/anchor shape per CONTEXT.md D-09 + PATTERNS.md preference"
    - "Verification artifact body contains: 5-criterion Goal-Achievement table mapping ROADMAP.md success criteria to evidence; Required Artifacts list (9 items per RESEARCH.md content checklist); Behavioral Spot-Checks table; Curation Statistics block (kept N + rejected M + per-cluster + rubric-class breakdown per CONTEXT.md D-10); REQUIREMENTS.md before/after diff for NEUT-08; both bin/requirements-sync.sh invocations with exit codes"
    - "Verification artifact records pre-curation `bash bin/check-neutrality.sh; echo $?` showing exit 2 + the lint-report.md hits (sourced from .planning/phases/12.1-.../pre-fix-check-neutrality.txt produced by Plan 02 Task 1) AND post-curation showing exit 0 (sourced from post-merge-evidence.txt produced by Plan 03 Task 5) — per CONTEXT.md D-10 + RESEARCH.md Pitfall 2"
    - "bash bin/requirements-sync.sh --strict --phase 12.1 exits 0 (zero drift between REQUIREMENTS.md and 12.1-VERIFICATION.md)"
    - "bash bin/requirements-sync.sh --require-complete --phase 12.1 exits 0 (Phase 12.1 is the first consumer of this flag, delivered 2026-05-01 via quick task 260501-g5n)"
    - "git diff <phase-base>..HEAD -- bin/check-neutrality.sh is empty (gate source unchanged across the entire phase per SPEC out-of-scope)"
    - "N (kept-term count) is computed once via `git diff <phase-base>..HEAD -- .neutrality-denylist.txt | grep -cE '^\\+[^#+]'` (where `<phase-base>` = `git log --format=%H -n 1 -- .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-SPEC.md`) and used as the SINGLE SOURCE OF TRUTH in REQUIREMENTS.md status note, 12.1-VERIFICATION.md Goal Achievement row 1, Required Artifacts row 4, Curation Statistics, and 12.1-04-SUMMARY.md — no other source of N is consulted (kept_count in 12.1-03-SUMMARY.md is sanity-check only; if it differs, log divergence in 12.1-04-SUMMARY.md but use the diff-derived N everywhere). Per MEDIUM #3 from REVIEWS.md."
    - "After Task 2 completes, no `<placeholder>` markers remain in 12.1-VERIFICATION.md (verified by `! grep -qE \"<[a-zA-Z_][a-zA-Z0-9_ -]*>\" .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md`). Per MEDIUM #4 from REVIEWS.md."
    - "All verbatim CLI outputs pasted into 12.1-VERIFICATION.md Behavioral Spot-Checks table are sourced from the four pre/post evidence files at execution time (pre-fix-check-neutrality.txt, post-fix-check-neutrality.txt, post-merge-evidence.txt, sync-evidence.txt) — no hand-typed reproductions. Per LOW #6 from REVIEWS.md."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "NEUT-08 status flip — bullet checkbox + status-note + traceability matrix row"
      contains: "[x] **NEUT-08**"
    - path: ".planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md"
      provides: "Phase 12.1 verification artifact in Phase 7 mirror format with D-09 + D-10 evidence; consumed by bin/requirements-sync.sh"
      contains: "phase_base_sha:"
      min_lines: 80
  key_links:
    - from: ".planning/REQUIREMENTS.md NEUT-08 line 36"
      to: ".planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md"
      via: "Status: Complete (Phase 12.1) — see VERIFICATION.md"
      pattern: "Complete \\(Phase 12.1\\)"
    - from: "12.1-VERIFICATION.md frontmatter"
      to: "bin/requirements-sync.sh"
      via: "YAML parse + checkbox sync"
      pattern: "phase: 12.1"
    - from: "12.1-VERIFICATION.md REQUIREMENTS.md before/after diff block"
      to: "Plan 04 evidence chain"
      via: "diff captures the REQUIREMENTS.md mutation made earlier in this plan"
      pattern: "^- \\[x\\] \\*\\*NEUT-08\\*\\*"
---

<objective>
Close Phase 12.1 by flipping NEUT-08 from `Pending` to `Complete (Phase 12.1)` in `.planning/REQUIREMENTS.md`, authoring `12.1-VERIFICATION.md` per Phase 7 mirror format with the D-09 + D-10 evidence checklist, and proving zero drift via `bin/requirements-sync.sh --strict --phase 12.1` exit 0 AND `--require-complete --phase 12.1` exit 0 (latter is the closure-mode flag delivered 2026-05-01 via quick task 260501-g5n; Phase 12.1 is its first consumer).

Per RESEARCH.md Pitfall 5, this plan AUTHORS THE VERIFICATION ARTIFACT AFTER PLANS 02 + 03 ARE LOCKED. The kept-term count in the artifact must exactly match `git diff <phase-base>..HEAD -- .neutrality-denylist.txt | grep -cE '^\+[^#+]'` — author-then-edit-denylist would cause count desync. **Per MEDIUM #3 from REVIEWS.md, this diff-derived N is the SINGLE SOURCE OF TRUTH** across all four downstream consumers (REQUIREMENTS.md status note, 12.1-VERIFICATION.md, 12.1-04-SUMMARY.md, plus the 12.1-VERIFICATION.md Goal Achievement and Required Artifacts tables). Plan 03's 12.1-03-SUMMARY.md kept_count is sanity-check only.

Purpose: Closes SPEC requirements #4 (REQUIREMENTS.md status flip) and #5 (verification artifact + zero-drift sync), satisfying the Phase 12.1 closure gate. Once this plan lands, NEUT-08 is the first v1.1 partial requirement to flip to Complete, clearing the path for Phase 13.2 (v1.1 closure verification).

Output: 1 mutation to `.planning/REQUIREMENTS.md` (2 surgical line edits — bullet + matrix row), 1 new `12.1-VERIFICATION.md` artifact. Plan 04 is `autonomous: true` (no user checkpoints) — all evidence sources are produced by Plans 02 + 03 and consumed mechanically.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-CONTEXT.md
@.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-SPEC.md
@.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-RESEARCH.md
@.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-PATTERNS.md
@.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VALIDATION.md
@.planning/phases/12.1-neut-08-personal-term-denylist-curation/pre-fix-check-neutrality.txt
@.planning/phases/12.1-neut-08-personal-term-denylist-curation/post-fix-check-neutrality.txt
@.planning/phases/12.1-neut-08-personal-term-denylist-curation/post-merge-evidence.txt
@.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-03-SUMMARY.md
@.planning/phases/07-neutral-template-foundation/07-VERIFICATION.md
@.planning/phases/12-complementary-systems-boundary-gtd-alignment/12-VERIFICATION.md
@bin/requirements-sync.sh

<interfaces>
<!-- Phase-base SHA for verification frontmatter (per Phase 12 precedent line 12). -->
<!-- Computed via: git log --format=%H -n 1 -- .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-SPEC.md -->
<!-- Verified at 2026-05-02 = 6a2bf5cb2be83079b3d81a05e5d66c6a7e21e950 (short: 6a2bf5c). -->
<!-- The executor MUST re-run this command at execution time — newer commits to SPEC.md (rare in this phase) would change the SHA. -->

REQUIREMENTS.md current state (verified 2026-05-02):
  Line 36: `- [ ] **NEUT-08** *(promoted to Phase 12.1)*: CI personal-content denylist ... **Status:** Infrastructure shipped in Phase 7 ...; personal-term denylist curation promoted from backlog 999.2 to active **Phase 12.1** on 2026-05-01 to clear before the Phase 13.2 closure gate. Candidate material at `.planning/backlog-neutrality-denylist-candidate.txt` (861-line deterministic --suggest-denylist output).`
  Line 215: `| NEUT-08 | Phase 12.1 | Pending |`

  Surrounding precedent for the bullet form (lines 30-35 — completed NEUT bullets):
  - [x] **NEUT-06**: CI neutrality gate: ... (runs on every PR) (pitfall C-1)
  - [x] **NEUT-07**: Decision record `dr-YYYY-MM-DD-kahneman-to-examples.md` committed ...

  Surrounding precedent for the matrix row (lines 210-214):
  | NEUT-04 | Phase 7 | Complete |
  | NEUT-05 | Phase 7 | Complete |
  | NEUT-06 | Phase 7 | Complete |
  | NEUT-07 | Phase 7 | Complete |
  | NEUT-08 | Phase 12.1 | Pending |  ← Line 215 (target)

bin/requirements-sync.sh accepted flags (verified):
  --phase <N>           Scope to single phase (e.g., --phase 12.1)
  --strict              Drift-only: fail on REQUIREMENTS.md ↔ VERIFICATION.md mismatch
  --require-complete    Closure mode: fail when any in-scope REQ-ID has status != Complete in REQUIREMENTS.md
                        Delivered 2026-05-01 via quick task 260501-g5n; Phase 12.1 is first consumer.

Phase 7 verification frontmatter pattern (analog primary per CONTEXT.md D-09):
  ---
  phase: 07-neutral-template-foundation
  verified: 2026-04-15T00:00:00Z
  status: verified
  score: 20/20 must-haves verified; ...
  human_verification_resolved: [...]
  human_verification_deferred: [...]
  ---

Phase 12 verification frontmatter pattern (newer, richer — per PATTERNS.md adopt for 12.1):
  ---
  phase: 12-complementary-systems-boundary-gtd-alignment
  verified: 2026-05-01T00:00:00Z
  status: passed
  score: 5/5 success criteria verified (3/3 BOUND requirements satisfied)
  overrides_applied: 0
  re_verification:
    previous_status: passed
    previous_score: 3/3 requirements verified
    gaps_closed: []
    gaps_remaining: []
    regressions: []
  phase_base_sha: ef3afec61fe211c88f3b965b83e96d67dd0b609d
  phase_base_sha_short: ef3afec
  phase_base_anchor: "git log --format=%H -n 1 -- .planning/phases/12-complementary-systems-boundary-gtd-alignment/12-SPEC.md"
  ---

Goal-Achievement table format (Phase 12 lines 30-38 shape):
  | # | Truth | Status | Evidence |
  |---|-------|--------|----------|
  | 1 | <success criterion 1> | VERIFIED | <command + observed output + file:line cite> |

Required Artifacts table (Phase 7 lines 44-66 OR Phase 12 lines 53-58 — Phase 12's 6-column shape preferred):
  | # | Artifact | Expected | Exists | Substantive | Wired | Data Flows | Status |

Behavioral Spot-Checks table (Phase 7 lines 84-93):
  | Behavior | Command | Result | Status |

REQUIREMENTS.md before/after diff block (per CONTEXT.md D-10):
  ```diff
  --- a/.planning/REQUIREMENTS.md
  +++ b/.planning/REQUIREMENTS.md
  @@ line 36 @@
  -- [ ] **NEUT-08** *(promoted to Phase 12.1)*: ...
  +- [x] **NEUT-08** *(completed in Phase 12.1)*: ...
  @@ line 215 @@
  -| NEUT-08 | Phase 12.1 | Pending |
  +| NEUT-08 | Phase 12.1 | Complete |
  ```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surgical 2-line edits to .planning/REQUIREMENTS.md (NEUT-08 bullet flip + matrix row flip) — N derived from git-diff (single source of truth)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md (lines 30-36 for NEUT bullet precedent + line 36 current state; lines 210-216 for matrix precedent + line 215 current state); .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-03-SUMMARY.md (sanity-check kept_count ONLY — see substitution rules below)</read_first>
  <action>
    First, compute the authoritative kept-term integer N at execution time. Per MEDIUM #3 from REVIEWS.md, N is derived MECHANICALLY from the live git diff and is the SINGLE SOURCE OF TRUTH for all downstream substitutions in this plan:

    ```
    PHASE_BASE_SHA=$(git log --format=%H -n 1 -- .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-SPEC.md)
    N=$(git diff "${PHASE_BASE_SHA}..HEAD" -- .neutrality-denylist.txt | grep -cE '^\+[^#+]')
    ```

    Sanity-check (NOT authoritative): cross-reference `N` against the kept_count recorded in `.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-03-SUMMARY.md`. If they differ, the diff-derived N WINS — use it for ALL substitutions in this task and in Task 2; record the divergence in 12.1-04-SUMMARY.md (the per-plan SUMMARY emitted at the end of this plan) but do NOT alter the diff-derived value. The kept_count in 12.1-03-SUMMARY.md is a workpaper sanity-check only and may legitimately drift if Plan 03 Task 5 demote-loop iterations occurred between the SUMMARY write and the final commit.

    Use the Edit tool TWICE on `.planning/REQUIREMENTS.md`:

    **Edit 1 — line 36 bullet flip:**
    Replace the entire current line 36 (which begins `- [ ] **NEUT-08** *(promoted to Phase 12.1)*: CI personal-content denylist check ...`) with a Complete-tense form preserving the Phase 7 → Phase 12.1 reassignment lineage and citing the verification artifact. Use the form recommended in PATTERNS.md lines 414-415 (Discretion):

    ```
    - [x] **NEUT-08** *(completed in Phase 12.1)*: CI personal-content denylist check covering domain terms from the creator's private vault, runs on PR diff restricted to public control-plane paths (excludes `examples/kahneman/**`) (pitfall C-1 mitigation). **Status:** Complete (Phase 12.1) — N personal-vault terms curated from `.planning/backlog-neutrality-denylist-candidate.txt` (855 candidate lines) and merged into `.neutrality-denylist.txt` under the unified `# Category: Personal-vault terms expanded curation (NEUT-08, 2026-05-02)` header. Live-tree gate exit 0 verified; contract test at `tests/phase-12.1/`. See `.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md`.
    ```

    Substitute `N` with the diff-derived integer computed at execution time via `git diff $(git log --format=%H -n 1 -- .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-SPEC.md)..HEAD -- .neutrality-denylist.txt | grep -cE '^\+[^#+]'`. The diff-derived value is authoritative across REQUIREMENTS.md, 12.1-VERIFICATION.md, and 12.1-04-SUMMARY.md (single source of truth per MEDIUM #3 from REVIEWS.md). The kept_count in 12.1-03-SUMMARY.md is a sanity check only — if it differs from the diff-derived N, log the divergence in 12.1-04-SUMMARY.md and use the diff-derived N everywhere.

    **Edit 2 — line 215 matrix row flip:**
    Replace `| NEUT-08 | Phase 12.1 | Pending |` with `| NEUT-08 | Phase 12.1 | Complete |`.

    Single-character status change in the third column. Phase column unchanged (preserves the Phase 7 → Phase 12.1 reassignment record).

    DO NOT touch any other line in `.planning/REQUIREMENTS.md`. The file is the project-wide requirements registry; surgical 2-line edits only.

    Per SPEC requirement #4 + acceptance criteria 8 + PATTERNS.md "REQUIREMENTS.md surgical 2-line edits" pattern + MEDIUM #3 from REVIEWS.md (diff-derived N as single source of truth).
  </action>
  <verify>
    <automated>grep -cE "^- \\[x\\] \\*\\*NEUT-08\\*\\*" .planning/REQUIREMENTS.md | grep -q "^1$" && grep -cE "^\\| NEUT-08 \\| Phase 12.1 \\| Complete \\|" .planning/REQUIREMENTS.md | grep -q "^1$" && ! grep -qE "^- \\[ \\] \\*\\*NEUT-08\\*\\*" .planning/REQUIREMENTS.md && ! grep -qE "^\\| NEUT-08 \\| Phase 12.1 \\| Pending \\|" .planning/REQUIREMENTS.md && grep -qE "Complete \\(Phase 12.1\\)" .planning/REQUIREMENTS.md && grep -qE "12.1-VERIFICATION\\.md" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    `grep -cE "^- \[x\] \*\*NEUT-08\*\*" .planning/REQUIREMENTS.md` returns 1 (exactly one match). `grep -cE "^\| NEUT-08 \| Phase 12.1 \| Complete \|" .planning/REQUIREMENTS.md` returns 1 (exactly one match). The old `[ ] **NEUT-08**` bullet and `Pending` matrix row no longer exist. Status note contains `Complete (Phase 12.1)` and references `12.1-VERIFICATION.md`. Surrounding NEUT-06/NEUT-07/DEBT-03 lines and other matrix rows untouched. The integer substituted for `N` in the status note matches the diff-derived value (single source of truth per MEDIUM #3).
  </done>
</task>

<task type="auto">
  <name>Task 2: Author 12.1-VERIFICATION.md per Phase 7 mirror + Phase 12 newer frontmatter shape (D-09 + D-10 evidence checklist) — N from diff is single source of truth; placeholder-leak guard at end</name>
  <files>.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md</files>
  <read_first>.planning/phases/07-neutral-template-foundation/07-VERIFICATION.md (Phase 7 mirror — primary analog per CONTEXT.md D-09; lines 1-18 frontmatter, lines 30-38 Goal-Achievement table, lines 42-66 Required Artifacts, lines 84-93 Behavioral Spot-Checks); .planning/phases/12-complementary-systems-boundary-gtd-alignment/12-VERIFICATION.md (newer frontmatter shape per PATTERNS.md preference — phase_base_sha + phase_base_anchor); .planning/phases/12.1-.../pre-fix-check-neutrality.txt + post-fix-check-neutrality.txt + post-merge-evidence.txt + 12.1-03-SUMMARY.md (verbatim evidence sources)</read_first>
  <action>
    Compute the phase-base SHA at execution time:
    ```
    PHASE_BASE_SHA=$(git log --format=%H -n 1 -- .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-SPEC.md)
    PHASE_BASE_SHORT=${PHASE_BASE_SHA:0:7}
    ```
    Verified value at planning time (2026-05-02): `6a2bf5cb2be83079b3d81a05e5d66c6a7e21e950` (short: `6a2bf5c`). The executor must re-run the command since later commits to SPEC.md would shift the SHA (unlikely but possible).

    Compute kept-term count N at execution time (SAME diff-derived value used by Task 1; recompute for confidence — the value MUST be identical):
    ```
    N=$(git diff "${PHASE_BASE_SHA}..HEAD" -- .neutrality-denylist.txt | grep -cE '^\+[^#+]')
    ```
    This is the SPEC requirement #5 acceptance — N must be derived from the actual git-diff, not from a stale workpaper count. Per RESEARCH.md Pitfall 5 + MEDIUM #3 from REVIEWS.md (single source of truth).

    Create `.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md` with this exact structure:

    ```markdown
    ---
    phase: 12.1-neut-08-personal-term-denylist-curation
    verified: <ISO 8601 UTC timestamp at execution time, e.g., 2026-05-02T19:00:00Z>
    status: passed
    score: 5/5 success criteria verified (1/1 NEUT-08 requirement satisfied)
    overrides_applied: 0
    re_verification:
      previous_status: pending
      previous_score: 0/1 requirements verified
      gaps_closed: [NEUT-08]
      gaps_remaining: []
      regressions: []
    phase_base_sha: <full SHA from git log command above>
    phase_base_sha_short: <7-char prefix>
    phase_base_anchor: "git log --format=%H -n 1 -- .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-SPEC.md"
    ---

    # Phase 12.1 Verification — NEUT-08 Personal-Term Denylist Curation

    **Phase:** 12.1-neut-08-personal-term-denylist-curation
    **Status:** Complete (passed)
    **Verified:** YYYY-MM-DD
    **Phase-base SHA (anchor for diff acceptance check):** SHORT (full: FULL)
    **Phase-base anchor command:** `git log --format=%H -n 1 -- .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-SPEC.md`

    ## Goal Achievement (5 ROADMAP.md success criteria → evidence)

    | # | Truth | Status | Evidence |
    |---|-------|--------|----------|
    | 1 | Curated subset of `.planning/backlog-neutrality-denylist-candidate.txt` merged into `.neutrality-denylist.txt` with category annotations | VERIFIED | `git diff PHASE_BASE..HEAD -- .neutrality-denylist.txt \| grep -cE '^\+[^#+]'` = N (where N = INTEGER); new header `# Category: Personal-vault terms expanded curation (NEUT-08, 2026-05-02)` at `.neutrality-denylist.txt:LINE_NUM` |
    | 2 | `bin/check-neutrality.sh` exits 0 on live tree post-merge | VERIFIED | `bash bin/check-neutrality.sh; echo "exit: $?"` → `exit: 0` (output below in Behavioral Spot-Checks); `git diff PHASE_BASE..HEAD -- bin/check-neutrality.sh` empty (no source modifications) |
    | 3 | Neutrality-leak fixture proves new entries wired | VERIFIED | `bash tests/phase-12.1/run.sh` → `PHASE 12.1 TESTS: 1/1`; positive case = exit 2 with anchor hit; negative-control = exit 0 |
    | 4 | NEUT-08 flips to Complete in REQUIREMENTS.md | VERIFIED | `grep -cE "^- \\[x\\] \\*\\*NEUT-08\\*\\*" .planning/REQUIREMENTS.md` = 1; matrix row reads `\| NEUT-08 \| Phase 12.1 \| Complete \|` (line 215) |
    | 5 | Phase verification artifact + zero-drift sync | VERIFIED | This file exists; `bin/requirements-sync.sh --strict --phase 12.1` exit 0; `--require-complete --phase 12.1` exit 0 |

    ## Required Artifacts

    | # | Artifact | Expected | Exists | Substantive | Wired | Status |
    |---|----------|----------|--------|-------------|-------|--------|
    | 1 | `tests/phase-12.1/run.sh` | Per-phase aggregator (mirror of phase-07) | YES (~50 lines) | YES (`PHASE 12.1 TESTS:` header) | YES (Plan 04 evidence: `bash tests/phase-12.1/run.sh` → 1/1) | VERIFIED |
    | 2 | `tests/phase-12.1/test_neut08_live_denylist_contract.sh` | Any-NEUT-08-term contract (D-06) | YES | YES (vacuous-pass guard, mktemp -d, grep -qF, Python str.replace negative-control per HIGH #1) | YES (anchored to LIVE `.neutrality-denylist.txt`) | VERIFIED |
    | 3 | `wiki/maintenance/lint-report.md` (frontmatter fix) | `neutrality_exempt: true` + rationale comment | YES (1 line added in frontmatter) | YES (mirrors 3 in-repo precedents) | YES (`bin/check-neutrality.sh` `has_neutrality_exempt()` consumer at lines 180-185) | VERIFIED |
    | 4 | `.neutrality-denylist.txt` (curation merge) | N new entries under unified NEUT-08 header (N = INTEGER from diff) | YES (N=INTEGER entries) | YES (curation rubric honored — see Curation Statistics below) | YES (`bin/check-neutrality.sh` `load_denylist()` consumer at lines 147-157) | VERIFIED |
    | 5 | `.planning/REQUIREMENTS.md` (status flip) | bullet `[x]`; matrix `Complete` | YES (2 lines flipped) | YES (Complete (Phase 12.1) status note + lineage preserved) | YES (`bin/requirements-sync.sh` consumer) | VERIFIED |
    | 6 | `bin/check-neutrality.sh` | UNCHANGED (SPEC out-of-scope) | YES | UNCHANGED | UNCHANGED | UNCHANGED (verified) |
    | 7 | `.planning/phases/12.1-.../calibration-script.py` | Python workpaper (seed=42, stratified) | YES | YES (SEED=42, random.Random(SEED), parse_clusters, stratified_sample) | YES (consumed by Plan 03 Task 2 user run) | VERIFIED |
    | 8 | `.planning/phases/12.1-.../calibration-decisions.txt` | 20 binary keep/skip decisions | YES (20 lines + 2 header) | YES (`grep -cE '^(keep\|skip)\\b'` = 20) | YES (consumed by Plan 03 Task 3 Claude extrapolation) | VERIFIED |
    | 9 | `.planning/phases/12.1-.../extrapolation-draft.md` | Grouped kept/rejected per CONTEXT.md D-04 | YES | YES (Kept + Rejected by class + Summary sections) | YES (consumed by Plan 03 Task 5 append) | VERIFIED |
    | 10 | `.planning/phases/12.1-.../12.1-VERIFICATION.md` | This file (Phase 7 mirror + Phase 12 frontmatter) | YES | YES (D-09 + D-10 evidence) | YES (`bin/requirements-sync.sh` consumer) | VERIFIED |

    ## Behavioral Spot-Checks

    | Behavior | Command | Result | Status |
    |----------|---------|--------|--------|
    | Pre-Plan-02 regression repro (live tree exit 2) | `bash bin/check-neutrality.sh; echo "exit: $?"` | VERBATIM_PRE_FIX_OUTPUT (must include `wiki/maintenance/lint-report.md:38: kahneman` and `wiki/maintenance/lint-report.md:39: personal-decision-journal` and `exit: 2`) | PASS (regression confirmed pre-fix) |
    | Post-Plan-02 frontmatter exemption (live tree exit 0) | `bash bin/check-neutrality.sh; echo "exit: $?"` | VERBATIM_POST_FIX_OUTPUT (must show `exit: 0`) | PASS |
    | Post-Plan-03 curation merge (live tree exit 0 with new terms) | `bash bin/check-neutrality.sh; echo "exit: $?"` | VERBATIM_POST_MERGE_OUTPUT (must show `exit: 0`) | PASS |
    | Phase 12.1 contract test green | `bash tests/phase-12.1/run.sh` | VERBATIM_TEST_OUTPUT (must show `PHASE 12.1 TESTS: 1/1`) | PASS |
    | requirements-sync strict mode | `bash bin/requirements-sync.sh --strict --phase 12.1; echo "exit: $?"` | VERBATIM_SYNC_STRICT_OUTPUT (run NOW; must show `exit: 0`) | PASS |
    | requirements-sync require-complete mode (Phase 12.1 first consumer) | `bash bin/requirements-sync.sh --require-complete --phase 12.1; echo "exit: $?"` | VERBATIM_SYNC_REQCOMP_OUTPUT (run NOW; must show `exit: 0`) | PASS |
    | Gate source unchanged across entire phase | `git diff PHASE_BASE..HEAD -- bin/check-neutrality.sh` | empty output | PASS (SPEC out-of-scope honored) |

    ## Curation Statistics (per CONTEXT.md D-10)

    - **Total candidates:** 855 (across 3 source clusters)
      - `agents-md-size-risk.md`: 19 tokens
      - `git:wiki/overviews/personal-decision-patterns.md`: 437 tokens
      - `git:wiki/sources/src-2026-04-10-personal-decision-journal.md`: 399 tokens
    - **Kept:** N = INTEGER (from git-diff; single source of truth per MEDIUM #3)
    - **Rejected:** M = 855 - N
      - Class `generic-english`: X (from `.planning/phases/12.1-.../extrapolation-draft.md` Summary)
      - Class `ambiguous-not-vault-specific`: Y
      - Class `off-rubric`: Z
      - Verification: X + Y + Z = M = 855 - N
    - **Per-cluster contribution to kept-list:**
      - `agents-md-size-risk.md`: A (expected ~0 per RESEARCH.md A1)
      - `personal-decision-patterns.md`: B
      - `personal-decision-journal.md`: C
      - Verification: A + B + C = N

    ## REQUIREMENTS.md Diff (NEUT-08 status flip — per CONTEXT.md D-10)

    ```diff
    --- a/.planning/REQUIREMENTS.md  (line 36)
    +++ b/.planning/REQUIREMENTS.md
    @@ -36 +36 @@
    -- [ ] **NEUT-08** *(promoted to Phase 12.1)*: CI personal-content denylist check covering domain terms ...
    +- [x] **NEUT-08** *(completed in Phase 12.1)*: CI personal-content denylist check ... **Status:** Complete (Phase 12.1) — N personal-vault terms curated ... See `.planning/phases/12.1-.../12.1-VERIFICATION.md`.
    @@ -215 +215 @@
    -| NEUT-08 | Phase 12.1 | Pending |
    +| NEUT-08 | Phase 12.1 | Complete |
    ```
    (Paste the full before/after text of both lines verbatim. Use `git diff PHASE_BASE..HEAD -- .planning/REQUIREMENTS.md` to source.)

    ## Requirements Coverage

    | Requirement | Source Plan | Description | Status | Evidence |
    |-------------|-------------|-------------|--------|----------|
    | NEUT-08 | 12.1-03 (curation) + 12.1-02 (frontmatter fix) + 12.1-01 (contract test) + 12.1-04 (status flip + verification) | CI personal-content denylist check covering creator's private vault terms | VERIFIED | N entries under unified NEUT-08 header; gate exit 0; contract test green (PHASE 12.1 TESTS: 1/1); status flip Complete; sync exit 0 (both modes) |
    ```

    Substitute every UPPER_CASE_PLACEHOLDER token (INTEGER, FULL, SHORT, LINE_NUM, VERBATIM_*_OUTPUT, PHASE_BASE, etc.) with the actual computed value at execution time. Paste verbatim outputs from the four evidence files (pre-fix-check-neutrality.txt, post-fix-check-neutrality.txt, post-merge-evidence.txt, sync-evidence.txt — produced by Plans 02 / 03 / Task 3 of this plan). DO NOT summarize or paraphrase the evidence — verbatim only (per RESEARCH.md "Don't Hand-Roll" table line 239 + LOW #6 from REVIEWS.md). All angle-bracket tokens MUST be substituted before commit; the placeholder-leak guard below is the deterministic enforcement.

    PLACEHOLDER-LEAK GUARD (MEDIUM #4 from REVIEWS.md): As the FINAL step of this task, after all substitutions, verify that no `<placeholder>` patterns survive in the file. Run:
    ```
    ! grep -qE "<[a-zA-Z_][a-zA-Z0-9_ -]*>" .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md
    ```
    This must succeed (i.e., grep finds zero matches). The narrow regex matches only angle-bracket tokens that look like `<word>` or `<words with spaces>` — it does NOT flag the diff-block markers (`---`, `+++`, `@@`), the parenthetical `(check-neutrality.sh)`, or the literal Phase-12.1 unified header `# Category: Personal-vault terms expanded curation (NEUT-08, 2026-05-02)`. If grep finds matches, identify each matching line, replace the placeholder with the computed value, and re-run the guard. The phrase "no `<placeholder>` markers remain" in the verification list (step 10) is now backed by this grep check, not just prose.

    Per CONTEXT.md D-09 (Phase 7 mirror) + D-10 (D-10 evidence list) + RESEARCH.md content checklist lines 527-585 + PATTERNS.md "12.1-VERIFICATION.md" section + MEDIUM #3 (diff-derived N as single source of truth) + MEDIUM #4 (placeholder-leak guard) + LOW #6 (verbatim CLI sourcing) from REVIEWS.md.
  </action>
  <verify>
    <automated>test -f .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md && python3 -c "import yaml; yaml.safe_load(open('.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md').read().split('---')[1])" && grep -qE "^phase: 12\\.1-neut-08-personal-term-denylist-curation$" .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md && grep -qE "^phase_base_sha:" .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md && grep -qE "^status: passed$" .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md && grep -qE "^## Goal Achievement" .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md && grep -qE "^## Required Artifacts$" .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md && grep -qE "^## Behavioral Spot-Checks$" .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md && grep -qE "^## Curation Statistics" .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md && grep -qE "^## REQUIREMENTS.md Diff" .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md && grep -qE "wiki/maintenance/lint-report\\.md:38: kahneman" .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md && grep -qE "PHASE 12\\.1 TESTS: 1/1" .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md && ! grep -qE "<[a-zA-Z_][a-zA-Z0-9_ -]*>" .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md</automated>
  </verify>
  <done>
    `12.1-VERIFICATION.md` exists. Frontmatter parses as valid YAML and contains `phase`, `verified`, `status: passed`, `score`, `phase_base_sha`, `phase_base_sha_short`, `phase_base_anchor`. Body contains all 6 required sections (Goal Achievement, Required Artifacts, Behavioral Spot-Checks, Curation Statistics, REQUIREMENTS.md Diff, Requirements Coverage). Pre-state evidence (`wiki/maintenance/lint-report.md:38: kahneman`) and post-state evidence (`PHASE 12.1 TESTS: 1/1`, `exit: 0`) appear verbatim in the Behavioral Spot-Checks table. Curation Statistics block contains the integers N, M, X, Y, Z, A, B, C with self-consistency (X+Y+Z = M, A+B+C = N). N matches the diff-derived value (single source of truth per MEDIUM #3). Placeholder-leak guard passes: `! grep -qE "<[a-zA-Z_][a-zA-Z0-9_ -]*>" 12.1-VERIFICATION.md` returns 0 (no `<placeholder>` markers remain — MEDIUM #4 from REVIEWS.md).
  </done>
</task>

<task type="auto">
  <name>Task 3: Run bin/requirements-sync.sh in both modes; capture exit-0 evidence; verify zero gate-source modifications</name>
  <files>.planning/phases/12.1-neut-08-personal-term-denylist-curation/sync-evidence.txt</files>
  <read_first>bin/requirements-sync.sh (verify the --phase + --strict + --require-complete flag combinations); .planning/REQUIREMENTS.md (post-Task-1 state); .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md (post-Task-2 state)</read_first>
  <action>
    Run the two requirements-sync invocations in sequence and capture verbatim output:
    ```
    {
      echo "=== bash bin/requirements-sync.sh --strict --phase 12.1 ==="
      bash bin/requirements-sync.sh --strict --phase 12.1
      echo "exit: $?"
      echo ""
      echo "=== bash bin/requirements-sync.sh --require-complete --phase 12.1 ==="
      bash bin/requirements-sync.sh --require-complete --phase 12.1
      echo "exit: $?"
      echo ""
      echo "=== git diff <phase-base>..HEAD -- bin/check-neutrality.sh ==="
      PHASE_BASE_SHA=$(git log --format=%H -n 1 -- .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-SPEC.md)
      git diff "${PHASE_BASE_SHA}..HEAD" -- bin/check-neutrality.sh
      echo "diff-line-count: $(git diff "${PHASE_BASE_SHA}..HEAD" -- bin/check-neutrality.sh | wc -l)"
    } > .planning/phases/12.1-neut-08-personal-term-denylist-curation/sync-evidence.txt 2>&1
    ```

    Required outcomes:
    - `--strict --phase 12.1` returns `exit: 0` (zero drift between REQUIREMENTS.md and 12.1-VERIFICATION.md)
    - `--require-complete --phase 12.1` returns `exit: 0` (NEUT-08 has status `Complete` in REQUIREMENTS.md)
    - `git diff <phase-base>..HEAD -- bin/check-neutrality.sh` is empty AND `diff-line-count: 0` (gate source unchanged)

    If `--strict` fails: read the error output, identify the drift (likely a REQ-ID listed in 12.1-VERIFICATION.md but not in REQUIREMENTS.md, or vice versa). For Phase 12.1, the only in-scope REQ-ID is NEUT-08; if drift is reported, check Task 1's REQUIREMENTS.md edits and Task 2's VERIFICATION.md Requirements Coverage table.

    If `--require-complete` fails: NEUT-08 is still listed as `Pending` somewhere. Re-check Task 1 — both line 36 bullet AND line 215 matrix row must be flipped.

    If `git diff` returns non-empty: a previous plan or task accidentally touched `bin/check-neutrality.sh`. STOP and investigate; SPEC out-of-scope is binding.

    After capture, paste the verbatim outputs INTO `12.1-VERIFICATION.md`'s Behavioral Spot-Checks table rows for `requirements-sync strict mode`, `requirements-sync require-complete mode`, and `Gate source unchanged across entire phase`. Use the Edit tool to overwrite the placeholder `VERBATIM_SYNC_*_OUTPUT` markers with the captured output. After this paste, re-run the placeholder-leak guard on 12.1-VERIFICATION.md (per MEDIUM #4) to confirm no angle-bracket placeholders survive — the file must satisfy `! grep -qE "<[a-zA-Z_][a-zA-Z0-9_ -]*>" 12.1-VERIFICATION.md`.

    Per SPEC requirement #5 acceptance + RESEARCH.md "Implementation Order Wave 3 steps 4-5" + PATTERNS.md "12.1-VERIFICATION.md" lines 308-310 + MEDIUM #4 from REVIEWS.md (placeholder-leak guard).
  </action>
  <verify>
    <automated>test -f .planning/phases/12.1-neut-08-personal-term-denylist-curation/sync-evidence.txt && grep -qE "^=== bash bin/requirements-sync\\.sh --strict --phase 12\\.1 ===$" .planning/phases/12.1-neut-08-personal-term-denylist-curation/sync-evidence.txt && grep -qE "^=== bash bin/requirements-sync\\.sh --require-complete --phase 12\\.1 ===$" .planning/phases/12.1-neut-08-personal-term-denylist-curation/sync-evidence.txt && [ "$(grep -c '^exit: 0$' .planning/phases/12.1-neut-08-personal-term-denylist-curation/sync-evidence.txt)" -eq 2 ] && grep -qE "^diff-line-count: 0$" .planning/phases/12.1-neut-08-personal-term-denylist-curation/sync-evidence.txt && bash bin/requirements-sync.sh --strict --phase 12.1 && bash bin/requirements-sync.sh --require-complete --phase 12.1 && [ "$(git diff $(git log --format=%H -n 1 -- .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-SPEC.md)..HEAD -- bin/check-neutrality.sh | wc -l)" -eq 0 ] && grep -qE "^\\| requirements-sync strict mode \\|.*\\| PASS \\|$" .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md && ! grep -qE "<[a-zA-Z_][a-zA-Z0-9_ -]*>" .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md</automated>
  </verify>
  <done>
    Both sync invocations exit 0. `git diff <phase-base>..HEAD -- bin/check-neutrality.sh` returns empty (zero modifications to gate source). `12.1-VERIFICATION.md` Behavioral Spot-Checks table now contains the verbatim sync output (no remaining `VERBATIM_*` or `<placeholder>` markers — MEDIUM #4 placeholder-leak guard passes deterministically).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 12.1-VERIFICATION.md ↔ REQUIREMENTS.md | Sync drift would cause closure-gate false-positive |
| Status flip → Phase 13.2 closure trigger | Premature flip without complete evidence would compromise milestone integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.1-06 | Tampering | Kept-term count drift between VERIFICATION.md and live denylist | mitigate | Task 1 AND Task 2 both compute N at execution time via `git diff $(git log ...)..HEAD -- .neutrality-denylist.txt | grep -cE '^\+[^#+]'` (single source of truth per MEDIUM #3 from REVIEWS.md). The kept_count in 12.1-03-SUMMARY.md is sanity-check only; if it differs from the diff-derived N, Plan 04 wins and the divergence is logged in 12.1-04-SUMMARY.md. RESEARCH.md Pitfall 5 + Author-VERIFICATION-after-denylist-locked ordering. |
| T-12.1-07 | Tampering | Stealth modification of bin/check-neutrality.sh during the phase | mitigate | Task 3 explicitly captures `git diff <base>..HEAD -- bin/check-neutrality.sh` and asserts `diff-line-count: 0`; SPEC out-of-scope is binding. |
| T-12.1-08 | Repudiation | Pre-state evidence not captured before frontmatter fix | mitigate | Plan 02 Task 1 captures pre-fix evidence first (mandatory ordering); this plan's Task 2 pastes that pre-fix evidence verbatim into VERIFICATION.md per RESEARCH.md Pitfall 2. |
| T-12.1-11 | Repudiation | Unfilled `<placeholder>` markers surviving into VERIFICATION.md | mitigate | Task 2 final step + Task 3 final step both run `! grep -qE "<[a-zA-Z_][a-zA-Z0-9_ -]*>" 12.1-VERIFICATION.md` — narrow regex catches angle-bracket tokens like `<word>` and `<words with spaces>` without false-positives on diff markers (`---`, `+++`, `@@`) or the parenthetical `(check-neutrality.sh)`. Per MEDIUM #4 from REVIEWS.md. |
</threat_model>

<verification>
After all 3 tasks land:

1. `grep -cE "^- \[x\] \*\*NEUT-08\*\*" .planning/REQUIREMENTS.md` returns 1
2. `grep -cE "^\| NEUT-08 \| Phase 12.1 \| Complete \|" .planning/REQUIREMENTS.md` returns 1
3. `test -f .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md`
4. `python3 -c "import yaml; yaml.safe_load(open('.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md').read().split('---')[1])"` succeeds (frontmatter is valid YAML)
5. `bash bin/requirements-sync.sh --strict --phase 12.1` exits 0
6. `bash bin/requirements-sync.sh --require-complete --phase 12.1` exits 0
7. `git diff $(git log --format=%H -n 1 -- .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-SPEC.md)..HEAD -- bin/check-neutrality.sh` is empty
8. `bash bin/check-neutrality.sh; echo $?` returns `0` (full live tree clean)
9. `bash tests/phase-12.1/run.sh` returns `PHASE 12.1 TESTS: 1/1`
10. Verification artifact contains pre-state (exit 2 + lint-report.md hits) AND post-state (exit 0 + 1/1 tests) AND sync-evidence (both --strict and --require-complete exit 0) verbatim — no `<placeholder>` markers remain (verified deterministically by `! grep -qE "<[a-zA-Z_][a-zA-Z0-9_ -]*>" .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md` per MEDIUM #4 from REVIEWS.md)
11. N (kept-term count) used in REQUIREMENTS.md status note, 12.1-VERIFICATION.md (Goal Achievement row 1, Required Artifacts row 4, Curation Statistics), and 12.1-04-SUMMARY.md is identical and equals `git diff <phase-base>..HEAD -- .neutrality-denylist.txt | grep -cE '^\+[^#+]'` — single source of truth per MEDIUM #3 from REVIEWS.md
</verification>

<success_criteria>
- `.planning/REQUIREMENTS.md` shows NEUT-08 as `[x]` with `Complete (Phase 12.1)` status note; traceability matrix row reads `| NEUT-08 | Phase 12.1 | Complete |` (SPEC requirement #4)
- `.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md` exists with Phase 7 mirror + Phase 12 newer frontmatter shape (CONTEXT.md D-09); contains all 6 required body sections; D-10 evidence is verbatim from the four pre/post evidence files
- `bin/requirements-sync.sh --strict --phase 12.1` exits 0 (zero drift)
- `bin/requirements-sync.sh --require-complete --phase 12.1` exits 0 (Phase 12.1 first consumer of closure-mode flag)
- `git diff <phase-base>..HEAD -- bin/check-neutrality.sh` is empty (zero gate-source modifications across entire phase)
- N (kept-term count) is computed once via `git diff <phase-base>..HEAD -- .neutrality-denylist.txt | grep -cE '^\+[^#+]'` and used as the SINGLE SOURCE OF TRUTH in REQUIREMENTS.md status note, 12.1-VERIFICATION.md (Goal Achievement row 1, Required Artifacts row 4, Curation Statistics), and 12.1-04-SUMMARY.md (RESEARCH.md Pitfall 5 + MEDIUM #3 from REVIEWS.md). The kept_count in 12.1-03-SUMMARY.md is sanity-check only.
- After Task 2, no `<placeholder>` markers remain in 12.1-VERIFICATION.md (deterministic guard: `! grep -qE "<[a-zA-Z_][a-zA-Z0-9_ -]*>" .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md` succeeds — MEDIUM #4 from REVIEWS.md)
- All verbatim CLI outputs in 12.1-VERIFICATION.md Behavioral Spot-Checks are sourced from pre-fix-check-neutrality.txt, post-fix-check-neutrality.txt, post-merge-evidence.txt, and sync-evidence.txt at execution time — no hand-typed reproductions (LOW #6 from REVIEWS.md)
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-04-SUMMARY.md` documenting:
- The 2 surgical edits to `.planning/REQUIREMENTS.md` (line 36 bullet form before/after; line 215 matrix row before/after)
- Path to `12.1-VERIFICATION.md` and verbatim copies of its three exit-code-evidence rows (pre-fix exit 2, post-merge exit 0, both sync exits 0)
- Computed phase_base_sha (full + short) at execution time
- Final integer N (kept-term count) — the AUTHORITATIVE diff-derived value used everywhere in REQUIREMENTS.md, 12.1-VERIFICATION.md, and this file. Confirm it equals `git diff <phase-base>..HEAD -- .neutrality-denylist.txt | grep -cE '^\+[^#+]'` per MEDIUM #3 from REVIEWS.md (single source of truth).
- If 12.1-03-SUMMARY.md kept_count differs from this diff-derived N: log the divergence here as `kept_count_divergence: yes (Plan-03 reported X, Plan-04 diff-derived Y, Plan-04 wins)`. If they match: log `kept_count_divergence: no`.
- Confirmation that `! grep -qE "<[a-zA-Z_][a-zA-Z0-9_ -]*>" 12.1-VERIFICATION.md` returns 0 — no `<placeholder>` markers survive in the final verification artifact (MEDIUM #4 placeholder-leak guard passed)
- Confirmation that NEUT-08 is now the first v1.1 partial-requirement closure (clears the path for Phase 13.2 v1.1 closure verification gate)
- Phase 12.1 status: COMPLETE
</output>
</content>
</invoke>