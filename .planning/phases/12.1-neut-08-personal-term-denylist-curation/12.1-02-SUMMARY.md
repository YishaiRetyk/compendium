---
phase: 12.1-neut-08-personal-term-denylist-curation
plan: 02
subsystem: testing
tags: [neutrality-gate, frontmatter-exemption, evidence-capture, lint-report]

# Dependency graph
requires:
  - phase: 07-neutral-template-foundation
    provides: bin/check-neutrality.sh + has_neutrality_exempt() per-file frontmatter exemption mechanism
provides:
  - wiki/maintenance/lint-report.md frontmatter neutrality_exempt:true exemption (4th in-repo precedent)
  - Verbatim pre-fix evidence (exit 2 + 2 hits) for Plan 04 verification artifact
  - Verbatim post-fix evidence (exit 0) for Plan 04 verification artifact
  - Live-tree gate restored to exit 0 in isolation from Plan 03 curation work
affects: [12.1-04-VERIFICATION (consumes both evidence files), 12.1-03-curation (independent; no shared file modifications)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Per-file frontmatter neutrality_exempt: true exemption (4th repo instance)"
    - "Pre/post evidence-capture pattern with verbatim stdout+stderr+exit redirection for verification artifacts"

key-files:
  created:
    - .planning/phases/12.1-neut-08-personal-term-denylist-curation/pre-fix-check-neutrality.txt
    - .planning/phases/12.1-neut-08-personal-term-denylist-curation/post-fix-check-neutrality.txt
    - .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-02-SUMMARY.md
  modified:
    - wiki/maintenance/lint-report.md (frontmatter only; one line added)

key-decisions:
  - "Mirrored 3 existing precedents verbatim for the rationale-comment style (closing parenthetical (check-neutrality.sh); backticks around denylisted terms)"
  - "Inserted neutrality_exempt: true between knowledge_domain: \"\" and closing --- per CONTEXT.md interfaces guidance"
  - "Captured evidence as verbatim stdout+stderr+exit-code via { ...; echo \"exit: $?\"; } > file 2>&1 to preserve exact gate output for Plan 04"
  - "Pre-fix evidence captured BEFORE frontmatter edit (Task ordering Plan 02 → Task 1 mandatory); reverse ordering would lose the exit:2 regression evidence forever"

patterns-established:
  - "Pattern 1: Pre/post-fix paired evidence files. When a fix changes a tool's exit code, capture the pre-state output verbatim before the fix, then re-run after the fix. Both files become inputs to a downstream verification artifact."
  - "Pattern 2: neutrality_exempt rationale-comment style. Single-line YAML, value `true` (no quotes), two spaces, inline `#` comment naming WHY (which denylisted terms appear and why they belong), closing parenthetical `(check-neutrality.sh)` referencing the scanner. Backticks around term tokens."

requirements-completed: [NEUT-08]

# Metrics
duration: ~5min
completed: 2026-05-03
---

# Phase 12.1 Plan 02: lint-report.md Neutrality Exemption + Evidence Capture Summary

**Per-file `neutrality_exempt: true` exemption on `wiki/maintenance/lint-report.md` (mirrors 3 in-repo precedents) restores `bin/check-neutrality.sh` to exit 0 against the live tree; pre/post-fix evidence captured verbatim for downstream Plan 04 verification artifact (CONTEXT.md D-10).**

## Performance

- **Duration:** ~5 min
- **Completed:** 2026-05-03
- **Tasks:** 2 (Task 1: pre-fix evidence; Task 2: frontmatter edit + post-fix evidence)
- **Files modified:** 1 (`wiki/maintenance/lint-report.md` frontmatter only)
- **Files created:** 3 (2 evidence files + this summary)

## Accomplishments

- Captured verbatim pre-fix `bin/check-neutrality.sh` output proving exit 2 with hits at `wiki/maintenance/lint-report.md:38` (`kahneman`) and `:39` (`personal-decision-journal`) — required evidence for Plan 04 per CONTEXT.md D-10. Captured BEFORE the frontmatter edit, preserving regression state.
- Added `neutrality_exempt: true` (with precedent-aligned rationale comment) to `wiki/maintenance/lint-report.md` frontmatter — single-line insertion between `knowledge_domain: ""` and the closing `---`. Mirrors 3 in-repo precedents byte-for-byte (style, parenthetical, backtick-quoting of term tokens).
- Captured verbatim post-fix `bin/check-neutrality.sh` output proving exit 0 — confirming the gate accepts the new exemption via the `^neutrality_exempt:\s*true\b` regex in `has_neutrality_exempt()` at `bin/check-neutrality.sh:185`.
- Confirmed body of `wiki/maintenance/lint-report.md` is unchanged (drift-finding lines 38-39 still contain `kahneman` and `personal-decision-journal` — out-of-scope to filter per SPEC).
- Confirmed `bin/check-neutrality.sh` source is unchanged (out-of-scope to modify gate semantics per SPEC).

## Task Commits

Each task was committed atomically with `--no-verify` (parallel-executor mode):

1. **Task 1: Capture pre-fix bin/check-neutrality.sh evidence** — `b1667ef` (test)
2. **Task 2 (part a): Insert neutrality_exempt frontmatter** — `df16007` (fix)
3. **Task 2 (part b): Capture post-fix bin/check-neutrality.sh evidence** — `003b521` (test)

The single Task 2 in the plan was split across two atomic commits because conventional-commit semantics differ for the frontmatter edit (`fix`) vs. the post-fix evidence file (`test`). The plan's verify clause checks both outputs together — both commits land before any verifier runs.

## Files Created/Modified

- `wiki/maintenance/lint-report.md` — Added single line `neutrality_exempt: true  # ...` to frontmatter between `knowledge_domain: ""` (line 21) and closing `---` (now line 23). Body unchanged.
- `.planning/phases/12.1-neut-08-personal-term-denylist-curation/pre-fix-check-neutrality.txt` — Verbatim pre-fix output of `bash bin/check-neutrality.sh; echo "exit: $?"`.
- `.planning/phases/12.1-neut-08-personal-term-denylist-curation/post-fix-check-neutrality.txt` — Verbatim post-fix output of same command.
- `.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-02-SUMMARY.md` — This file.

## Frontmatter Edit (Verbatim)

Inserted line in `wiki/maintenance/lint-report.md`:

```
neutrality_exempt: true  # Lint reports enumerate drift findings by file path; legitimately naming `kahneman` and `personal-decision-journal` (lines 38-39) is unavoidable when summarizing what the lint detected. Scanner skips this file (check-neutrality.sh).
```

Diff (frontmatter region only):

```diff
@@ -19,6 +19,7 @@ aliases:
   - Lint Report
 has_contradictions: false
 knowledge_domain: ""
+neutrality_exempt: true  # Lint reports enumerate drift findings by file path; legitimately naming `kahneman` and `personal-decision-journal` (lines 38-39) is unavoidable when summarizing what the lint detected. Scanner skips this file (check-neutrality.sh).
 ---
```

## Pre-Fix Evidence (Verbatim)

`.planning/phases/12.1-neut-08-personal-term-denylist-curation/pre-fix-check-neutrality.txt`:

```
wiki/maintenance/lint-report.md:38: kahneman
wiki/maintenance/lint-report.md:39: personal-decision-journal
exit: 2
```

This output exactly matches the research baseline documented in `12.1-RESEARCH.md` Pitfall 2. No drift from the pre-execution live-tree state (no extra files needed exemption; no other gate had already fixed the regression).

## Post-Fix Evidence (Verbatim)

`.planning/phases/12.1-neut-08-personal-term-denylist-curation/post-fix-check-neutrality.txt`:

```
exit: 0
```

Gate clean against the live tree. No hits. No stderr.

## Confirmations

- `git diff 178b63e52fad89fe0c1d8d48f635caafff1fbaeb..HEAD -- bin/check-neutrality.sh` is empty (gate source unchanged — `wc -l` returned 0).
- `git diff 178b63e52fad89fe0c1d8d48f635caafff1fbaeb..HEAD -- wiki/maintenance/lint-report.md` shows exactly one `+` line, zero `-` lines (frontmatter-only insertion; body unchanged).
- `grep -cE "^neutrality_exempt:[[:space:]]*true\\b" wiki/maintenance/lint-report.md` returns `1` (exactly one match; whitespace-tolerant per gate loader contract).
- `bash bin/check-neutrality.sh; echo $?` exits 0 in current worktree state (authoritative behavior assertion).

## Decisions Made

- None beyond Claude's-discretion items called out in CONTEXT.md (rationale-comment wording matched precedent style; placement after `knowledge_domain: ""` per `<interfaces>` guidance).

## Deviations from Plan

None — plan executed exactly as written. The only mechanical deviation worth noting: Task 2 in the plan bundles the frontmatter edit with post-fix evidence capture; I split this across two atomic commits to keep `fix` and `test` conventional-commit types separated. The `<verify>` clause of Task 2 still passes because both commits land before any verifier runs — the file artifacts and exit-code assertions are identical to the single-commit alternative.

## Issues Encountered

None.

## Next Plan Readiness

- **Plan 03 (curation) — independent, can land in either order.** Plans 02 and 03 modify zero shared files. Plan 03's denylist curation does not depend on this exemption being present (Plan 03 reads `.neutrality-denylist.txt` and `.planning/backlog-neutrality-denylist-candidate.txt`).
- **Plan 04 (verification artifact) — consumes both evidence files.** Per CONTEXT.md D-10, the verification artifact pastes both `pre-fix-check-neutrality.txt` (exit 2 + 2 hits) and `post-fix-check-neutrality.txt` (exit 0) verbatim. Both artifacts are now in place under `.planning/phases/12.1-neut-08-personal-term-denylist-curation/`.

## Self-Check: PASSED

Verified in current worktree state:

- `wiki/maintenance/lint-report.md` exists and contains exactly 1 line matching `^neutrality_exempt:[[:space:]]*true\b`. **PASS**
- Pre-fix evidence file exists and contains literal `exit: 2`, `wiki/maintenance/lint-report.md:38: kahneman`, `wiki/maintenance/lint-report.md:39: personal-decision-journal`. **PASS**
- Post-fix evidence file exists and contains literal `exit: 0`. **PASS**
- `bash bin/check-neutrality.sh; echo $?` exits 0 in current state (authoritative). **PASS**
- `bin/check-neutrality.sh` source is unchanged from base `178b63e52fad89fe0c1d8d48f635caafff1fbaeb` (zero diff). **PASS**
- `wiki/maintenance/lint-report.md` body lines 38-39 still contain `kahneman` and `personal-decision-journal` (not removed; only frontmatter mutated). **PASS**
- All 3 task commits (`b1667ef`, `df16007`, `003b521`) found in `git log --oneline` since base. **PASS**

---
*Phase: 12.1-neut-08-personal-term-denylist-curation*
*Plan: 02 (lint-report.md neutrality exemption + evidence)*
*Completed: 2026-05-03*
