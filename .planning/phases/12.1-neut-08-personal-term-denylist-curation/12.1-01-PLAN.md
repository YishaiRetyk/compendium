---
phase: 12.1-neut-08-personal-term-denylist-curation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase-12.1/run.sh
  - tests/phase-12.1/test_neut08_live_denylist_contract.sh
autonomous: true
requirements:
  - NEUT-08
must_haves:
  truths:
    - "tests/phase-12.1/run.sh exists and follows the per-phase aggregator convention (mirrors tests/phase-07/run.sh shape)"
    - "tests/phase-12.1/test_neut08_live_denylist_contract.sh exists implementing the Any-NEUT-08-term contract per CONTEXT.md D-05 and D-06, anchored STRICTLY to the Phase-12.1 unified header literal `# Category: Personal-vault terms expanded curation (NEUT-08, 2026-05-02)` per SPEC requirement #3 (`a term promoted in this phase`)"
    - "Wave 0 RED state: with no Phase-12.1 unified header present yet in .neutrality-denylist.txt, the test MUST fail fast (exit 1) with the vacuous-pass-guard message: `FAIL: no term found under '# Category: Personal-vault terms expanded curation (NEUT-08, 2026-05-02)' header in .neutrality-denylist.txt — Phase 12.1 curation has not landed yet (or header was renamed)` (per RESEARCH.md Pitfall 1 — RED is the desired state at Wave 0 commit)"
    - "Wave 2 GREEN flip: AFTER Plan 03 commits the unified Phase-12.1 header + N kept terms, re-running `bash tests/phase-12.1/run.sh` reports `PHASE 12.1 TESTS: 1/1` with no further test-file edits required (the strict-anchor shape automatically picks the first kept term once the unified header lands)"
    - "Test self-locates the anchor by parsing the LIVE .neutrality-denylist.txt at runtime (per D-06 self-locating shape), but anchors STRICTLY on the Phase-12.1 unified header literal (NOT a broad NEUT-08 substring match — this is the SPEC #3 fidelity fix that guarantees the anchor is a term promoted IN THIS PHASE, not the Phase-7 starter category carryover)"
    - "Test seeds the anchor in a hermetic mktemp -d fixture under a public-path subdir, points --root at the fixture but --denylist at the LIVE .neutrality-denylist.txt (Phase 12.1 critical delta vs Phase 7 N3)"
    - "Test asserts positive case (exit 2 + grep -F anchor in output) AND negative-control case (anchor replaced with REDACTED → exit 0)"
  artifacts:
    - path: "tests/phase-12.1/run.sh"
      provides: "Per-phase test aggregator (Wave 0 harness); mirrors tests/phase-07/run.sh"
      contains: "PHASE 12.1 TESTS:"
    - path: "tests/phase-12.1/test_neut08_live_denylist_contract.sh"
      provides: "Any-NEUT-08-term contract test (D-06) anchored on Phase-12.1 unified header (SPEC #3 fidelity)"
      contains: "Personal-vault terms expanded curation (NEUT-08, 2026-05-02)"
  key_links:
    - from: "tests/phase-12.1/run.sh"
      to: "tests/phase-12.1/test_*.sh"
      via: "shopt -s nullglob; for t in $SCRIPT_DIR/test_*.sh"
      pattern: "for t in .*/test_\\*\\.sh"
    - from: "tests/phase-12.1/test_neut08_live_denylist_contract.sh"
      to: ".neutrality-denylist.txt"
      via: "awk anchor extraction strictly under the Phase-12.1 unified header"
      pattern: "Personal-vault terms expanded curation \\(NEUT-08, 2026-05-02\\)"
    - from: "tests/phase-12.1/test_neut08_live_denylist_contract.sh"
      to: "bin/check-neutrality.sh"
      via: "bash bin/check-neutrality.sh --root <mktemp> --denylist .neutrality-denylist.txt"
      pattern: "bin/check-neutrality\\.sh.*--root.*--denylist"
---

<objective>
Land the Wave 0 test scaffold for Phase 12.1 in a deliberately RED state. The Any-NEUT-08-term contract test and its per-phase aggregator must exist and execute; the test MUST fail today with a clear vacuous-pass-guard message because no Phase-12.1 unified header exists in `.neutrality-denylist.txt` yet (Plan 03 adds it). This RED state proves the contract is wired correctly AND honors SPEC requirement #3's "a term promoted in this phase" fidelity: the test will go GREEN automatically in Wave 2 once the Phase-12.1 unified header lands with at least one kept term, with no further test-file edits required.

Purpose: Lock in the test-as-contract before any curation work. The test self-locates its anchor at runtime (lightest contract per CONTEXT.md D-06), but anchors STRICTLY on the Phase-12.1 unified header literal — not the broad NEUT-08 substring match. This strict-anchor shape is the SPEC #3 fidelity fix: the contract test MUST verify a term promoted IN THIS PHASE, not silently anchor on the Phase-7 NEUT-08 starter category carryover (`personal-decision-patterns` etc.). The vacuous-pass guard is the failsafe.

Output: `tests/phase-12.1/run.sh` (aggregator, mirrors `tests/phase-07/run.sh`) and `tests/phase-12.1/test_neut08_live_denylist_contract.sh` (contract test). Both executable. Plan 01 is independent of Plan 02 (lint-report.md frontmatter) — they can land in either order. Plan 03 (curation) depends on Plan 01 because the test goes GREEN as soon as the Phase-12.1 unified header lands; Plan 04 (verification + status flip) consumes the GREEN result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-CONTEXT.md
@.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-SPEC.md
@.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-RESEARCH.md
@.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-PATTERNS.md
@.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VALIDATION.md
@tests/phase-07/run.sh
@tests/phase-07/test_denylist_gate.sh
@bin/check-neutrality.sh
@.neutrality-denylist.txt

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

bin/check-neutrality.sh accepted flags (verified in source):
  --root <dir>          Override scan root (default: repo root). Plan 12.1-01 uses for hermetic mktemp -d fixture.
  --denylist <path>     Override denylist path (default: .neutrality-denylist.txt). Plan 12.1-01 uses LIVE root denylist.
  --format <text|json>  Output format. Plan 12.1-01 uses default text mode.

bin/check-neutrality.sh exit codes:
  0 = no hits
  2 = at least one denylist hit (gate fires)

bin/check-neutrality.sh load_denylist() at lines 147-157 (CONSTRAINT — DO NOT modify):
  Reads each line, strips whitespace, skips empty lines and lines starting with `#`.
  Does NOT strip trailing inline `# ...` comments. Implication: per-term inline
  source attribution would be loaded as the literal string `term # comment` and
  never match anything in scanned files. Only `# Category:` block headers and
  bare term lines are valid in .neutrality-denylist.txt.

bin/check-neutrality.sh has_neutrality_exempt() at lines 180-185:
  Reads frontmatter, returns True iff `^neutrality_exempt:\s*true\b`
  (case-insensitive). Per-file exemption mechanism. Plan 12.1-02 uses this.

.neutrality-denylist.txt current shape (verified 2026-05-02, 25 lines):
  - Two existing # Category: headers:
    * Line 1:  # Category: Kahneman reference example terms (NEUT-06)
    * Line 12: # Category: Personal-decision content archived from wiki/ (NEUT-08)
              ^ This is the Phase 7 starter category. SPEC #3 says the contract test
              must anchor on a term "promoted in this phase" — i.e., NOT this
              starter, NOT these 8 carryover terms. The strict-header anchor below
              is the SPEC #3 fidelity guarantee.
  - Bare term lines (lowercased; no inline trailing comments)
  - Blank line separates categories
  - NO Phase-12.1 unified header exists yet — Plan 03 Task 5 appends the
    `# Category: Personal-vault terms expanded curation (NEUT-08, 2026-05-02)`
    header at EOF (line 26+). Wave 0 RED state today; Wave 2 GREEN after Plan 03 commits.

SPEC #3 fidelity (CRITICAL — read before writing test):
  SPEC requirement #3 acceptance reads: "A test under tests/phase-12.1/ ... seeds a
  public-path file with at least one term PROMOTED IN THIS PHASE, runs
  bin/check-neutrality.sh, and asserts exit code 2 plus a hit on the seeded term."

  The phrase "promoted in this phase" is binding. The test MUST anchor on a term
  that landed via Phase-12.1 curation work — not a Phase-7 NEUT-08 starter carryover
  (the existing `personal-decision-patterns` block at lines 12-25). A broad
  `NEUT-08` substring match in the awk anchor would silently pick the FIRST
  NEUT-08-tagged term in the file (today: `personal-decision-patterns` from line 16),
  which violates SPEC #3.

  Solution: anchor STRICTLY on the literal Phase-12.1 unified header string. This
  guarantees the test is RED today (Wave 0; no such header exists yet → vacuous-pass
  guard fires with exit 1) and GREEN after Plan 03 commits (the awk extracts the
  first kept term that lands under that header → positive case fires).

tests/phase-07/run.sh aggregator pattern (verified in file, 49 lines):
  - Shebang + set -euo pipefail
  - --full / --help argument loop
  - SCRIPT_DIR via dirname BASH_SOURCE
  - shopt -s nullglob; for t in test_*.sh
  - Per-test bash invocation, PASS/FAIL tally
  - Final "PHASE 07 TESTS: ${PASS}/${TOTAL}" line, exit 1 on any FAIL
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/phase-12.1/run.sh aggregator (mechanical mirror of tests/phase-07/run.sh)</name>
  <files>tests/phase-12.1/run.sh</files>
  <read_first>tests/phase-07/run.sh (full file — this is a byte-clone with two string substitutions only)</read_first>
  <action>
    Create the file `tests/phase-12.1/run.sh` as a byte-clone of `tests/phase-07/run.sh` with EXACTLY these substitutions:
    - Replace the comment `# tests/phase-07/run.sh -- Phase 07 test aggregator (Wave 0 harness).` with `# tests/phase-12.1/run.sh -- Phase 12.1 test aggregator (Wave 0 harness).`
    - Replace `# Iterates tests/phase-07/test_*.sh, ...` with `# Iterates tests/phase-12.1/test_*.sh, tallies pass/fail, exits non-zero on any failure.`
    - Replace `Usage: tests/phase-07/run.sh [--full]` with `Usage: tests/phase-12.1/run.sh [--full]`
    - Replace `echo "PHASE 07 TESTS: ${PASS}/${TOTAL}"` with `echo "PHASE 12.1 TESTS: ${PASS}/${TOTAL}"`

    Preserve everything else verbatim: the `set -euo pipefail`, `--full`/`--help` argument loop, SCRIPT_DIR resolution via `cd "$(dirname "${BASH_SOURCE[0]}")" && pwd`, the `shopt -s nullglob; for t in "$SCRIPT_DIR"/test_*.sh` aggregation loop, PASS/FAIL/TOTAL counters, FAILED_TESTS array, and the final exit-1-on-any-FAIL guard.

    Make executable: `chmod +x tests/phase-12.1/run.sh`. Per CONTEXT.md D-05 + RESEARCH.md Pattern 1.
  </action>
  <verify>
    <automated>test -x tests/phase-12.1/run.sh && grep -qE "^echo \"PHASE 12\\.1 TESTS:" tests/phase-12.1/run.sh && grep -qE "^# tests/phase-12\\.1/run\\.sh -- Phase 12\\.1 test aggregator" tests/phase-12.1/run.sh && grep -qE "^Usage: tests/phase-12\\.1/run\\.sh \\[--full\\]" tests/phase-12.1/run.sh && bash -n tests/phase-12.1/run.sh</automated>
  </verify>
  <done>
    File exists and is executable. PASS/TOTAL header echoes "PHASE 12.1 TESTS:". `bash -n` (parse-only) confirms no shell parse errors. The aggregator's actual exit code is left to Task 2 to determine — once Task 2 lands, the aggregator will exit 1 (Wave 0 RED, vacuous-pass guard fires).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create tests/phase-12.1/test_neut08_live_denylist_contract.sh (Any-NEUT-08-term contract per CONTEXT.md D-06 + SPEC #3 strict-anchor fidelity)</name>
  <files>tests/phase-12.1/test_neut08_live_denylist_contract.sh</files>
  <read_first>tests/phase-07/test_denylist_gate.sh (analog), .planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-RESEARCH.md (lines 372-433 has the verbatim skeleton; the awk anchor is REPLACED below per SPEC #3 fidelity), .neutrality-denylist.txt (verify the Phase-12.1 unified header literal does NOT yet exist)</read_first>
  <action>
    Create `tests/phase-12.1/test_neut08_live_denylist_contract.sh` per the RESEARCH.md skeleton at lines 372-433, with ONE SURGICAL DEVIATION: the awk anchor extraction is REPLACED with a strict-header literal match (SPEC #3 fidelity — see Interfaces "SPEC #3 fidelity" block above). Everything else mirrors the skeleton verbatim.

    Required structure (in this order, no deviation):

    1. Shebang `#!/usr/bin/env bash` + header comment block: `# tests/phase-12.1/test_neut08_live_denylist_contract.sh -- Phase 12.1 (NEUT-08).` and `# Any-NEUT-08-term contract per CONTEXT.md D-06 + SPEC #3 fidelity: parse live` and `# .neutrality-denylist.txt for first term under the Phase-12.1 unified header,` and `# seed it in a hermetic public-path fixture, assert gate fires (exit 2 + hit);` and `# negative-control: remove term, assert exit 0. Wave 0 RED until Plan 03 lands.`

    2. `set -euo pipefail`

    3. SCRIPT_DIR + REPO_ROOT resolution + `cd "$REPO_ROOT"` per `tests/phase-07/test_denylist_gate.sh` lines 6-8:
       ```
       SCRIPT_DIR=$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)
       REPO_ROOT=$(cd "$SCRIPT_DIR/../.." && pwd)
       cd "$REPO_ROOT"
       ```

    4. Constants:
       ```
       DENYLIST=".neutrality-denylist.txt"
       SCRIPT="bin/check-neutrality.sh"
       ```

    5. Anchor extraction via awk — STRICT match on the Phase-12.1 unified header literal (NOT a broad NEUT-08 substring match — this is the SPEC #3 fidelity fix):
       ```
       ANCHOR=$(awk '
         /^# *Category:.*Personal-vault terms expanded curation \(NEUT-08, 2026-05-02\)/ { in_block=1; next }
         in_block && /^# *Category:/  { exit }
         in_block && /^[[:space:]]*$/ { next }
         in_block && /^#/             { next }
         in_block && NF               { print; exit }
       ' "$DENYLIST")
       ```

       Anchor literal: `Personal-vault terms expanded curation (NEUT-08, 2026-05-02)`. This is the EXACT string Plan 03 Task 5 appends per CONTEXT.md D-07. If the executor of Plan 03 ever changes the date or wording, this awk MUST be updated in lockstep — that is the intentional coupling.

       NO FALLBACK to broad NEUT-08 substring match. Fallback would silently pick the Phase-7 starter category's `personal-decision-patterns` term, violating SPEC #3 ("a term promoted in this phase").

    6. Vacuous-pass guard immediately after anchor extraction (per Pitfall 4 — distinct error message; exit 1):
       ```
       if [ -z "$ANCHOR" ]; then
         echo "FAIL: no term found under '# Category: Personal-vault terms expanded curation (NEUT-08, 2026-05-02)' header in $DENYLIST — Phase 12.1 curation has not landed yet (or header was renamed)" >&2
         exit 1
       fi
       echo "anchor term: $ANCHOR"
       ```

    7. Hermetic fixture creation via `mktemp -d` + `trap 'rm -rf "$FIX"' EXIT` (per Shared Pattern in PATTERNS.md lines 431-439):
       ```
       FIX=$(mktemp -d)
       trap 'rm -rf "$FIX"' EXIT
       mkdir -p "$FIX/wiki" "$FIX/docs"
       printf '# Test fixture\nThis page contains the term %s for leak testing.\n' "$ANCHOR" > "$FIX/wiki/leak.md"
       ```
       (Use `printf` not `echo -e` to keep escapes portable — `\n` interpretation differs across shells.)

    8. Positive case: invoke gate with `--root "$FIX"` and `--denylist "$REPO_ROOT/$DENYLIST"`. Wrap in `set +e ... set -e` (Shared Pattern lines 442-449) because gate exits 2 expectedly. Assert RC=2 AND `grep -qF` (fixed-string, NOT regex — anchor may contain regex metachars from candidate cluster) anchor present in output:
       ```
       set +e
       OUT=$(bash "$SCRIPT" --root "$FIX" --denylist "$REPO_ROOT/$DENYLIST" 2>&1)
       RC=$?
       set -e
       if [ "$RC" -ne 2 ] || ! printf '%s' "$OUT" | grep -qF "$ANCHOR"; then
         echo "FAIL positive: expected exit 2 with hit on '$ANCHOR'; got exit $RC" >&2
         echo "$OUT" >&2
         exit 1
       fi
       echo "PASS positive: gate fires on '$ANCHOR'"
       ```

    9. Negative-control: replace anchor with `REDACTED` (use `sed -i.bak ... && rm` for portability across GNU/BSD sed). Re-invoke gate, assert RC=0:
       ```
       sed -i.bak "s|$ANCHOR|REDACTED|g" "$FIX/wiki/leak.md" && rm "$FIX/wiki/leak.md.bak"
       set +e
       OUT2=$(bash "$SCRIPT" --root "$FIX" --denylist "$REPO_ROOT/$DENYLIST" 2>&1)
       RC2=$?
       set -e
       if [ "$RC2" -ne 0 ]; then
         echo "FAIL negative-control: expected exit 0 after removal; got exit $RC2" >&2
         echo "$OUT2" >&2
         exit 1
       fi
       echo "PASS negative-control: gate clean after removal"
       ```

    10. Final summary line + exit 0:
        ```
        echo "test_neut08_live_denylist_contract: 2 pass / 0 fail"
        ```

    Make executable: `chmod +x tests/phase-12.1/test_neut08_live_denylist_contract.sh`.

    CONSTRAINT — sed delimiter: use `|` (pipe) as the sed substitution delimiter (`s|$ANCHOR|REDACTED|g`) so anchor terms containing `/` (e.g., paths from candidate clusters) do not break the regex. Forward slash is the more common candidate term content; pipe is the safe choice.

    CONSTRAINT — grep -F (fixed-string): the anchor MAY contain regex metachars in future curation cycles (hyphens are safe; periods or brackets would not be). `grep -qF` is mandatory; `grep -q` would silently mismatch on metachar-containing anchors.

    CONSTRAINT — strict header anchor (NO fallback): the awk extracts ONLY under the literal `# Category: Personal-vault terms expanded curation (NEUT-08, 2026-05-02)` header. Adding a fallback to broad NEUT-08 substring match would re-introduce the SPEC #3 violation that BLOCKER #1 fixes. Wave 0 RED is the desired state (vacuous-pass guard fires); Wave 2 GREEN flip happens automatically after Plan 03 commits.

    Per CONTEXT.md D-06 (Any-NEUT-08-term contract, lightest of three options) + SPEC #3 ("a term promoted in this phase" fidelity) + RESEARCH.md Pitfall 1 (RED at Wave 0 is the intended state) + Pitfall 4 (anchor extraction silent-mask guard).
  </action>
  <verify>
    <automated>test -x tests/phase-12.1/test_neut08_live_denylist_contract.sh && grep -qF 'Personal-vault terms expanded curation (NEUT-08, 2026-05-02)' tests/phase-12.1/test_neut08_live_denylist_contract.sh && grep -qE 'Phase 12\.1 curation has not landed yet' tests/phase-12.1/test_neut08_live_denylist_contract.sh && grep -qE 'mktemp -d' tests/phase-12.1/test_neut08_live_denylist_contract.sh && grep -qE 'grep -qF' tests/phase-12.1/test_neut08_live_denylist_contract.sh && grep -qE '\\-\\-denylist "\\$REPO_ROOT/\\$DENYLIST"' tests/phase-12.1/test_neut08_live_denylist_contract.sh && bash -n tests/phase-12.1/test_neut08_live_denylist_contract.sh && OUT=$(bash tests/phase-12.1/run.sh 2>&1 || true) && echo "$OUT" | grep -qE "^PHASE 12\\.1 TESTS: 0/1$" && echo "$OUT" | grep -qF "Phase 12.1 curation has not landed yet"</automated>
  </verify>
  <done>
    File exists and is executable. Contains the strict Phase-12.1-unified-header literal, the vacuous-pass-guard message including the substring `Phase 12.1 curation has not landed yet`, `mktemp -d` for hermetic fixture, `grep -qF` for anchor matching (NOT bare `grep -q`), and `--denylist "$REPO_ROOT/$DENYLIST"` (LIVE denylist, not fixture-local). At Wave 0 commit time (no Phase-12.1 unified header in `.neutrality-denylist.txt`), `bash tests/phase-12.1/run.sh` reports `PHASE 12.1 TESTS: 0/1` and the test stderr contains the vacuous-pass-guard string — this is the intended Wave 0 RED state per RESEARCH.md Pitfall 1 and SPEC #3. After Plan 03 commits the unified header + N kept terms, re-running the same command flips to `PHASE 12.1 TESTS: 1/1` with no test-file edits.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LIVE .neutrality-denylist.txt → contract test | Test reads live data; future curation could introduce regex-metachar terms that break naive grep |
| mktemp fixture → bin/check-neutrality.sh | Hermetic temp dir; deleted via trap; no path traversal risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.1-01 | Tampering | LIVE denylist anchor → grep | mitigate | Use `grep -qF` (fixed-string mode) so any future curated term containing regex metachars (`.`, `[`, `]`, `*`) still matches reliably. Embedded constraint in Task 2 action. |
| T-12.1-04 | Denial of Service | mktemp fixture cleanup | mitigate | `trap 'rm -rf "$FIX"' EXIT` ensures cleanup on success, failure, or signal. |
| T-12.1-09 | Tampering | Anchor scope drift (silent fallback to Phase-7 starter) | mitigate | Strict-header awk pattern with NO fallback to broad NEUT-08 substring match — guarantees the anchor is a Phase-12.1-promoted term per SPEC #3 fidelity (BLOCKER #1 fix from checker). |
</threat_model>

<verification>
After both tasks land at Wave 0 commit time (Plan 03 not yet landed):

1. `test -x tests/phase-12.1/run.sh` (executable bit set)
2. `test -x tests/phase-12.1/test_neut08_live_denylist_contract.sh` (executable bit set)
3. `bash tests/phase-12.1/run.sh` exits 1 with output containing both `PHASE 12.1 TESTS: 0/1` AND the vacuous-pass-guard substring `Phase 12.1 curation has not landed yet` — this is the intended Wave 0 RED state per RESEARCH.md Pitfall 1 + SPEC #3 fidelity (BLOCKER #1 fix)
4. `grep -qF "Personal-vault terms expanded curation (NEUT-08, 2026-05-02)" tests/phase-12.1/test_neut08_live_denylist_contract.sh` returns 0 (strict-header anchor present)
5. `git diff --name-only HEAD -- bin/check-neutrality.sh` is empty (no source modifications to gate; this plan does not touch bin/)

After Plan 03 commits (Wave 2 verification — re-run from Plan 04):

6. `bash tests/phase-12.1/run.sh` exits 0 with output `PHASE 12.1 TESTS: 1/1` (the strict-anchor awk extracts the first kept term under the Phase-12.1 unified header)

Confirm `.github/workflows/neutrality.yml` does NOT walk `tests/phase-12.1/run.sh` (verified in research: workflow line 43 invokes only `tests/phase-07/run.sh`). Wave 0 commit therefore does not perturb CI even though the new aggregator exits 1 in isolation.
</verification>

<success_criteria>
- Both files exist and are executable
- Wave 0 commit time: `bash tests/phase-12.1/run.sh` exits 1 with `PHASE 12.1 TESTS: 0/1` and the vacuous-pass-guard message visible in stderr (RED is the desired state per Pitfall 1)
- Wave 2 (after Plan 03 commits): `bash tests/phase-12.1/run.sh` exits 0 with `PHASE 12.1 TESTS: 1/1` (no test-file edits required between Wave 0 and Wave 2)
- Test contains all six mandated constructs: strict Phase-12.1 unified-header literal anchor (NO fallback to broad NEUT-08), vacuous-pass guard with the `Phase 12.1 curation has not landed yet` message, `mktemp -d` hermetic fixture, `grep -qF` (not bare `grep -q`), `--denylist "$REPO_ROOT/$DENYLIST"` (LIVE), `--root "$FIX"` (hermetic)
- Zero modification to `bin/check-neutrality.sh`
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-01-SUMMARY.md` documenting:
- Files created (paths + line counts)
- Wave 0 RED state confirmed: paste verbatim output of `bash tests/phase-12.1/run.sh 2>&1 || true` showing `PHASE 12.1 TESTS: 0/1` + the vacuous-pass-guard message — kept as Wave 0 RED evidence baseline for Plan 04 verification artifact
- Confirmation that `.github/workflows/neutrality.yml` does NOT pick up the new test directory (no CI perturbation despite the local exit-1)
- Note: no anchor term is selected at Wave 0 (the strict-header awk returns empty); the first selected anchor will appear in Plan 03's post-merge evidence after the unified header lands
</output>
</output>
