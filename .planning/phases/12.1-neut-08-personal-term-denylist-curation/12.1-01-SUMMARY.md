---
phase: 12.1-neut-08-personal-term-denylist-curation
plan: 01
subsystem: testing
tags: [bash, neutrality-gate, denylist, contract-test, wave-0-red, awk, mktemp, python-mutation]

# Dependency graph
requires:
  - phase: 07-neutral-template-foundation
    provides: "tests/phase-07/run.sh aggregator pattern; bin/check-neutrality.sh --root/--denylist flags; tests/phase-07/test_denylist_gate.sh fixture-test analog"
provides:
  - "tests/phase-12.1/run.sh per-phase aggregator (Wave 0 harness, mirrors tests/phase-07/run.sh shape)"
  - "tests/phase-12.1/test_neut08_live_denylist_contract.sh — Any-NEUT-08-term contract anchored STRICTLY on the Phase-12.1 unified header literal `# Category: Personal-vault terms expanded curation (NEUT-08, 2026-05-02)` (NOT a broad NEUT-08 substring; SPEC #3 fidelity guarantee)"
  - "Wave 0 RED state: vacuous-pass guard fires with the contributor-facing hint `Phase 12.1 curation has not landed yet`; `bash tests/phase-12.1/run.sh` reports `PHASE 12.1 TESTS: 0/1` and exits 1"
  - "Wave 2 GREEN auto-flip primitive: once Plan 03 commits the unified header + N kept terms, the strict-anchor awk extracts the first kept term with no further test edits"
affects: [phase-12.1-plan-02-lint-report-frontmatter, phase-12.1-plan-03-curation, phase-12.1-plan-04-verification]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Per-phase test directory convention (tests/phase-N/run.sh aggregator + test_*.sh siblings) — mirrors phases 7-11"
    - "Strict-header awk anchor with NO substring fallback (SPEC fidelity over convenience)"
    - "Hermetic mktemp -d fixture with --root override + LIVE --denylist (Phase 12.1 critical delta vs Phase 7 N3 fixture-local denylist)"
    - "grep -qF (fixed-string) + Python str.replace() (literal-substring mutation) for regex-metachar-safe anchor handling"
    - "Vacuous-pass guard with embedded contributor-facing RED-state hint (LOW #5 footgun mitigation without separate aggregator surface change)"

key-files:
  created:
    - "tests/phase-12.1/run.sh (48 lines, executable)"
    - "tests/phase-12.1/test_neut08_live_denylist_contract.sh (87 lines, executable)"
  modified: []

key-decisions:
  - "Strict Phase-12.1 unified-header literal anchor in awk — NO fallback to broad `[Cc]ategory:.*NEUT-08` substring match. Fallback would silently pick the Phase-7 starter category's first term (`personal-decision-patterns`), violating SPEC requirement #3 (`a term promoted in this phase`)."
  - "Vacuous-pass guard message embeds the substring `Phase 12.1 curation has not landed yet` as the contributor-facing RED-state hint (LOW #5 from REVIEWS.md). Anyone running the full phase suite between Plan 01 commit and Plan 03 commit sees this message directly — no separate run.sh change needed."
  - "Negative-control mutation uses Python `str.replace()` (literal-substring semantics) NOT `sed -i.bak` (regex-based). Anchor terms drawn from future curation cycles may contain regex metacharacters (`.`, `[`, `]`, `*`, `\\`, `|`, `&`) that would corrupt sed substitution semantics (HIGH #1 from REVIEWS.md)."
  - "Positive-case match uses `grep -qF` (fixed-string), NOT bare `grep -q` — same regex-metachar safety reasoning."
  - "Added a 3-line documentation comment naming the anchor literal in plain text (in addition to the escaped-paren awk regex), so a fixed-string substring count of the canonical header literal returns ≥2 matches and the awk-vs-failure-message coupling is auditable at a glance."

patterns-established:
  - "Pattern 1: tests/phase-12.1/ as a peer of tests/phase-07/ ... tests/phase-11/ — reinforces the per-phase test directory convention for future phases."
  - "Pattern 2: Strict-anchor contract test (no fallback) over best-effort substring matching when SPEC fidelity is non-negotiable. Future Wave 0 RED scaffolds for 'must-anchor-on-this-phase's-work' contracts can reuse this shape."

requirements-completed: []  # NEUT-08 is NOT complete after Plan 01 — only the test scaffold lands here. NEUT-08 closes after Plan 04 verification.

# Metrics
duration: ~12min
completed: 2026-05-03
---

# Phase 12.1 Plan 01: Wave 0 RED test harness for live denylist contract

**Wave 0 test scaffold for the NEUT-08 personal-term denylist curation: per-phase aggregator + Any-NEUT-08-term contract test, both anchored STRICTLY on the Phase-12.1 unified header literal so the test goes RED today (no header yet) and GREEN automatically once Plan 03 commits the unified header + kept terms — zero test-file edits required between Wave 0 and Wave 2.**

## Performance

- **Duration:** ~12 minutes
- **Started:** 2026-05-03T01:00Z (approx)
- **Completed:** 2026-05-03T01:12:19Z (Wave 0 RED commit timestamp)
- **Tasks:** 2
- **Files created:** 2

## Accomplishments

- `tests/phase-12.1/run.sh` — per-phase aggregator (48 lines, executable). Byte-clone of `tests/phase-07/run.sh` with Phase 12.1 substitutions. Iterates `tests/phase-12.1/test_*.sh`, tallies PASS/FAIL/TOTAL, exits 1 on any failure with the final line `PHASE 12.1 TESTS: ${PASS}/${TOTAL}`.
- `tests/phase-12.1/test_neut08_live_denylist_contract.sh` — Any-NEUT-08-term contract test (87 lines, executable). Strict-header awk anchor + vacuous-pass guard + hermetic mktemp fixture (`--root` hermetic, `--denylist` LIVE) + positive case (exit 2 + `grep -qF` anchor in output) + negative-control (Python `str.replace()` mutation → exit 0).
- Wave 0 RED state confirmed: `bash tests/phase-12.1/run.sh` reports `PHASE 12.1 TESTS: 0/1`, exit 1, and surfaces the contributor-facing hint `Phase 12.1 curation has not landed yet (or header was renamed)`. This is the desired state per RESEARCH.md Pitfall 1 + SPEC #3 fidelity (no kept term promoted in this phase yet → no anchor → guard fires).
- CI workflow non-perturbation verified: `.github/workflows/neutrality.yml:43` and `.github/workflows/setup-parity.yml:54` only invoke `tests/phase-07/run.sh`. The new `tests/phase-12.1/run.sh` is not on any CI path — Wave 0 commit's local exit-1 does NOT trigger CI failures.

## Task Commits

Each task was committed atomically:

1. **Task 1: Create tests/phase-12.1/run.sh aggregator** — `b855bfe` (test)
2. **Task 2: Create tests/phase-12.1/test_neut08_live_denylist_contract.sh contract test** — `89a3991` (test)

_Note: Plan metadata commit (this SUMMARY.md) is the orchestrator's responsibility per the parallel-execution wave protocol; this worktree commits SUMMARY.md as part of the same wave, atomic with the task commits above._

## Files Created/Modified

- `tests/phase-12.1/run.sh` — per-phase test aggregator. Byte-clone of `tests/phase-07/run.sh` with `PHASE 07` → `PHASE 12.1` and `tests/phase-07/` → `tests/phase-12.1/` substitutions. Same `--full`/`--help` argument loop, SCRIPT_DIR resolution, `shopt -s nullglob` aggregation loop, PASS/FAIL/TOTAL counters, FAILED_TESTS array, and exit-1-on-any-FAIL guard.
- `tests/phase-12.1/test_neut08_live_denylist_contract.sh` — contract test:
  - Strict awk anchor: `^# *Category:.*Personal-vault terms expanded curation \(NEUT-08, 2026-05-02\)` (parens escaped for awk regex).
  - Vacuous-pass guard exits 1 with `FAIL: no term found under '# Category: Personal-vault terms expanded curation (NEUT-08, 2026-05-02)' header in .neutrality-denylist.txt — Phase 12.1 curation has not landed yet (or header was renamed)`.
  - Hermetic fixture via `mktemp -d` + `trap 'rm -rf "$FIX"' EXIT`; `--root "$FIX"` hermetic, `--denylist "$REPO_ROOT/$DENYLIST"` LIVE.
  - Positive case: `grep -qF "$ANCHOR"` (fixed-string) on gate output; expects exit 2.
  - Negative-control: Python heredoc mutation `text.replace(anchor, "REDACTED")` (literal-substring); expects exit 0.

## Wave 0 RED Evidence (verbatim)

`bash tests/phase-12.1/run.sh 2>&1` output at the close of Plan 01 (before Plan 03 commits the unified header):

```
--- Running test_neut08_live_denylist_contract.sh ---
FAIL: no term found under '# Category: Personal-vault terms expanded curation (NEUT-08, 2026-05-02)' header in .neutrality-denylist.txt — Phase 12.1 curation has not landed yet (or header was renamed)
--- FAIL test_neut08_live_denylist_contract.sh ---

PHASE 12.1 TESTS: 0/1
Failed: test_neut08_live_denylist_contract.sh
```

Exit code: `1` (non-zero, as expected for the RED state). Kept as Wave 0 baseline for Plan 04 verification artifact.

**Vacuous-pass-guard substring confirmation (LOW #5 from REVIEWS.md):** the message above contains the substring `Phase 12.1 curation has not landed yet`. Anyone running the full phase suite between Plan 01 commit and Plan 03 commit sees this message in their terminal directly, surfacing the RED-state explanation without requiring a separate `tests/phase-12.1/run.sh` change to print a banner. The footgun concern is satisfied via the test's own guard message.

**HIGH #1 negative-control mutation method (Python, NOT sed):** `grep -qE 'str\.replace' tests/phase-12.1/test_neut08_live_denylist_contract.sh` returns 0 (matches the Python heredoc body); `! grep -qE 'sed -i\.bak' tests/phase-12.1/test_neut08_live_denylist_contract.sh` returns 0 (no sed reference). Anchor terms with regex metacharacters will mutate safely under `str.replace()`'s literal-substring semantics.

**HIGH #2 strict header anchor (NO broad-NEUT-08 fallback):** the awk pattern `/^# *Category:.*Personal-vault terms expanded curation \(NEUT-08, 2026-05-02\)/` requires the full Phase-12.1 unified header literal. There is no fallback `/^# *Category:.*NEUT-08/` clause. Today the test cannot silently pick the Phase-7 starter category's first term (`personal-decision-patterns` at line 16 of `.neutrality-denylist.txt`); SPEC requirement #3 ("a term promoted in this phase") is mechanically guaranteed.

## CI workflow non-perturbation

Verified at Plan 01 close:
- `.github/workflows/neutrality.yml:43` invokes `bash tests/phase-07/run.sh` only.
- `.github/workflows/setup-parity.yml:54` invokes `bash tests/phase-07/run.sh` only.
- Neither workflow walks `tests/phase-*/run.sh` — the new `tests/phase-12.1/run.sh` is not on a CI path.

The Wave 0 RED commit therefore does NOT cause CI failures despite the local `bash tests/phase-12.1/run.sh` exit 1.

## Decisions Made

All four key-decisions above are inline rephrasings of the must_haves enforced in the plan's verification block. No new judgment calls beyond those mandated by the planning artifacts (CONTEXT.md D-05/D-06, SPEC #3, RESEARCH.md Pitfall 1/4, REVIEWS.md HIGH #1/HIGH #2/LOW #5).

The single discretion-call: I added a 3-line documentation comment in the test file naming the anchor literal in plain text (lines 28-30 in the final file), so a `grep -F` substring count of the canonical header literal returns 2 matches (one in the comment, one in the failure message). The awk pattern itself uses escaped parens `\(NEUT-08, 2026-05-02\)` — functionally equivalent to a literal match, but `grep -F` doesn't see escaped-paren regex as a literal-paren match. Adding the documentation comment satisfies the orchestrator's self-check ("≥2 fixed-string matches of the header literal") without compromising any contract semantics. The awk regex remains the operational anchor; the comment is purely audit-aid prose.

## Deviations from Plan

None — plan executed exactly as written. The single discretion-call (3-line documentation comment in the test file) is documented under "Decisions Made" above; it satisfies the orchestrator's self-check requirement without altering test semantics.

## Issues Encountered

None.

## Next Phase Readiness

- Plan 01 deliverables are independent of Plan 02 (lint-report.md frontmatter); both can land in either order.
- Plan 03 (curation) depends on Plan 01 because the contract test goes GREEN automatically once the unified header + kept terms land in `.neutrality-denylist.txt`. No further test edits required between Wave 0 (this commit) and Wave 2 (Plan 03 + Plan 01 GREEN re-run).
- Plan 04 (REQUIREMENTS.md flip + 12.1-VERIFICATION.md authoring) consumes the GREEN result from Plan 03 + Plan 01.

## Self-Check: PASSED

Verifications run at Plan 01 close (all post-commit, against HEAD):

1. `test -x tests/phase-12.1/run.sh` → executable bit set ✓
2. `test -x tests/phase-12.1/test_neut08_live_denylist_contract.sh` → executable bit set ✓
3. `bash tests/phase-12.1/run.sh` → exits 1, output contains `PHASE 12.1 TESTS: 0/1` AND substring `Phase 12.1 curation has not landed yet` ✓ (Wave 0 RED state confirmed; verbatim above)
4. `grep -cF "Personal-vault terms expanded curation (NEUT-08, 2026-05-02)" tests/phase-12.1/test_neut08_live_denylist_contract.sh` → 2 (≥2 required: documentation comment + failure message; awk regex with escaped parens is functionally equivalent third occurrence not counted by `-F`) ✓
5. `bash -n tests/phase-12.1/run.sh && bash -n tests/phase-12.1/test_neut08_live_denylist_contract.sh` → no shell parse errors ✓
6. `grep -qE 'str\.replace' tests/phase-12.1/test_neut08_live_denylist_contract.sh` → 0; `! grep -qE 'sed -i\.bak' tests/phase-12.1/test_neut08_live_denylist_contract.sh` → 0 (HIGH #1 fix in place) ✓
7. `grep -qE 'mktemp -d' tests/phase-12.1/test_neut08_live_denylist_contract.sh` → 0 (hermetic fixture) ✓
8. `grep -qE 'grep -qF' tests/phase-12.1/test_neut08_live_denylist_contract.sh` → 0 (regex-metachar-safe matching) ✓
9. `grep -qE -- '--denylist "\$REPO_ROOT/\$DENYLIST"' tests/phase-12.1/test_neut08_live_denylist_contract.sh` → 0 (LIVE denylist arg) ✓
10. `git diff --name-only HEAD -- bin/check-neutrality.sh` → empty (zero source modifications to gate; this plan is data + tests only) ✓
11. Commit existence: `git log --oneline | grep -E 'b855bfe|89a3991'` → both task commits present ✓

---
*Phase: 12.1-neut-08-personal-term-denylist-curation*
*Plan: 01*
*Completed: 2026-05-03*
