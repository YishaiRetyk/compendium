---
phase: 12.1
slug: neut-08-personal-term-denylist-curation
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-02
---

# Phase 12.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Sourced from `12.1-RESEARCH.md` §Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Bash test aggregator (`tests/phase-N/run.sh` per-phase pattern; no third-party framework) |
| **Config file** | None — convention-based: `tests/phase-12.1/run.sh` walks `tests/phase-12.1/test_*.sh` siblings |
| **Quick run command** | `bash tests/phase-12.1/run.sh` |
| **Full suite command** | `bash tests/phase-12.1/run.sh` (single-phase scope; CI also runs `tests/phase-07/run.sh` per current `.github/workflows/neutrality.yml`) |
| **Estimated runtime** | < 5 seconds (bash + python3 + grep only; no network, no DB) |

---

## Sampling Rate

- **After every task commit:** `bash tests/phase-12.1/run.sh` (after Wave 1b lands a NEUT-08 term, this should be GREEN)
- **After every plan wave:** `bash tests/phase-12.1/run.sh` AND `bash bin/check-neutrality.sh; echo $?` (after Waves 1a + 1b both land, both must be PASS / exit 0)
- **Before `/gsd-verify-work`:** All five rows in the Per-Task Verification Map below must be PASS, AND `bash bin/requirements-sync.sh --strict --phase 12.1` exit 0 AND `--require-complete --phase 12.1` exit 0
- **Max feedback latency:** ~5 seconds (full suite is small)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 12.1-01-01 | 01 | 1a | NEUT-08 (#2) | — | `wiki/maintenance/lint-report.md` carries `neutrality_exempt: true`; `bash bin/check-neutrality.sh` exits 0 (post-fix, pre-merge) | smoke + grep | `grep -c "^neutrality_exempt: true$" wiki/maintenance/lint-report.md` (=1); `bash bin/check-neutrality.sh; echo $?` (=0) | ✅ — invocation only | ⬜ pending |
| 12.1-02-01 | 02 | 0 | NEUT-08 (#3) | — | Test aggregator + contract test scaffold land RED (positive case fails because no NEUT-08 term yet) | unit | `bash tests/phase-12.1/run.sh` (expect FAIL with "no NEUT-08-tagged terms" guard message) | ❌ W0 | ⬜ pending |
| 12.1-03-01 | 03 | 1b | NEUT-08 (#1) | — | Calibration script + log produced; user binary keep/skip decisions for 20 stratified-random sample lines persisted | manual + grep | `test -f .planning/phases/12.1-.../calibration-decisions.txt` AND `grep -cE "^(keep\|skip)\b" .planning/phases/12.1-.../calibration-decisions.txt` (=20) | ❌ W1b | ⬜ pending |
| 12.1-03-02 | 03 | 1b | NEUT-08 (#1) | — | Claude extrapolation draft produced (kept-list + rejected-list with reject-class labels); user reviewed/edited; merged into `.neutrality-denylist.txt` under unified `# Category: Personal-vault terms expanded curation (NEUT-08, 2026-05-02)` header; N>0 entries; no bare uncategorized lines | grep | `awk '/^# *Category:.*NEUT-08/,/^# *Category:|^$/' .neutrality-denylist.txt \| grep -cE "^[^#]"` (>0); `awk '/^[^#[:space:]]/ && !in_cat {print; exit 1} /^# *Category:/ {in_cat=1}' .neutrality-denylist.txt` (exit 0) | ❌ W1b | ⬜ pending |
| 12.1-02-02 | 02 | 2 | NEUT-08 (#3) | — | Contract test now GREEN (positive: seeded NEUT-08 term in temp fixture → exit 2 + hit; negative-control: term removed → exit 0) | unit | `bash tests/phase-12.1/run.sh` (expect PASS) | ❌ W2 | ⬜ pending |
| 12.1-04-01 | 04 | 3 | NEUT-08 (#4) | — | REQUIREMENTS.md NEUT-08 bullet flipped to `[x]` with `Complete (Phase 12.1)`; traceability matrix row reads `\| NEUT-08 \| Phase 12.1 \| Complete \|` | grep | `grep -cE "^- \[x\] \*\*NEUT-08\*\*" .planning/REQUIREMENTS.md` (=1); `grep -cE "^\| NEUT-08 \| Phase 12.1 \| Complete \|" .planning/REQUIREMENTS.md` (=1) | ✅ — invocation only | ⬜ pending |
| 12.1-04-02 | 04 | 3 | NEUT-08 (#5) | — | `12.1-VERIFICATION.md` exists, parses as YAML+md, contains kept-term count + rejection-class breakdown + fixture evidence + pre/post `check-neutrality.sh` exits + REQUIREMENTS.md before/after diff (per D-09 + D-10 + Phase 7 mirror) | smoke + python | `python3 -c "import yaml; yaml.safe_load(open('.planning/phases/12.1-neut-08-personal-term-denylist-curation/12.1-VERIFICATION.md').read().split('---')[1])"` (no exception) | ❌ W3 | ⬜ pending |
| 12.1-04-03 | 04 | 3 | NEUT-08 (#5) | — | Both sync invocations exit 0 against phase-closing HEAD | smoke | `bash bin/requirements-sync.sh --strict --phase 12.1; echo $?` (=0); `bash bin/requirements-sync.sh --require-complete --phase 12.1; echo $?` (=0) | ✅ — invocation only | ⬜ pending |
| 12.1-04-04 | 04 | 3 | NEUT-08 (#2) | — | `bin/check-neutrality.sh` source unchanged from phase-base | git-diff | `git diff <phase-base>..HEAD -- bin/check-neutrality.sh` (must be empty) | ✅ — invocation only | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Task IDs above are illustrative — the planner assigns final IDs in PLAN.md frontmatter.*

---

## Wave 0 Requirements

- [ ] `tests/phase-12.1/run.sh` — aggregator skeleton (mirror of `tests/phase-07/run.sh`); echoes `PHASE 12.1 TESTS: ${PASS}/${TOTAL}`
- [ ] `tests/phase-12.1/test_neut08_live_denylist_contract.sh` — Any-NEUT-08-term contract per CONTEXT.md D-06 (parses live `.neutrality-denylist.txt`, locates first term under any `# Category:` header containing `NEUT-08` substring, seeds a temp public-path fixture via `mktemp -d`, runs `bin/check-neutrality.sh --root <fixture> --denylist .neutrality-denylist.txt`, asserts positive=exit-2+hit and negative-control=exit-0). Fails fast with clear message if zero NEUT-08-tagged terms exist (vacuous-pass guard).
- No shared fixture file required — test self-seeds via `mktemp -d`; no static fixture under `tests/phase-12.1/fixtures/` needed.
- No framework install required — bash + python3 already present in repo.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Curation rubric judgment per term — proper noun / personal name / project codename / multi-word phrase / hyphenated identifier vs generic English | NEUT-08 (#1) | Rubric application requires human judgment that no automated test can replicate (LLM-extrapolated, but final review is human per CONTEXT.md D-01 step 3) | (1) Run calibration script on 20-line stratified-random sample (seed=42); record binary keep/skip per line. (2) Review Claude's extrapolated kept-list + rejected-list against SPEC rubric §Constraints. (3) Edit before merge. (4) Confirm no generic single-word English vocabulary survives. |
| Wording of `neutrality_exempt: true` rationale comment on `wiki/maintenance/lint-report.md` | NEUT-08 (#2) | Comment style is editorial; matches established precedent (`wiki/index.md:17`, two `wiki/decisions/dr-*.md:22-23`) but exact wording is per-file | Mirror precedent comment-rationale style (e.g., `# legitimately names denylisted terms in lint findings`); review against the 3 precedent files before commit. |
| REQUIREMENTS.md status-note rewording when flipping NEUT-08 from `Pending` to `Complete (Phase 12.1)` | NEUT-08 (#4) | Editorial — preserves Phase 7 → Phase 12.1 reassignment context while marking complete | Manual prose edit; preserve the existing "infrastructure shipped in Phase 7" lineage note. |
| Pre/post `bin/check-neutrality.sh; echo $?` evidence captured in `12.1-VERIFICATION.md` | NEUT-08 (#5) | Captures point-in-time evidence (commit shas) for verification artifact; not re-runnable post-merge | Run before Wave 1a (expect exit 2), run after Waves 1a + 2 (expect exit 0); capture full output in VERIFICATION.md fenced block. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (`tests/phase-12.1/run.sh` + `test_neut08_live_denylist_contract.sh`)
- [ ] No watch-mode flags (single-shot bash invocations only)
- [ ] Feedback latency < 5s
- [ ] `nyquist_compliant: true` set in frontmatter (after planner finalizes plan IDs and re-checks coverage)

**Approval:** pending
