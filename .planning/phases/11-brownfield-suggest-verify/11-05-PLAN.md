---
phase: 11-brownfield-suggest-verify
plan: 05
type: execute
wave: 5
depends_on:
  - 11-04
files_modified:
  - AGENTS.md
  - CLAUDE.md
  - schema/AGENTS.template.md
  - schema/fixtures/canonical-AGENTS.md
  - docs/reference/brownfield.md
  - .planning/REQUIREMENTS.md
  - wiki/decisions/dr-2026-04-20-brownfield-apply-vs-advisory.md
  - wiki/index.md
  - wiki/log.md
requirements:
  - BRWN-11
  - BRWN-12
  - BRWN-17
  - BRWN-18
  - BRWN-19
  - BRWN-20
  - BRWN-22

autonomous: true

must_haves:
  truths:
    - "`AGENTS.md §11.5 Brownfield Workflow` is populated (thin authoritative contract matching §11.1–11.4 shape); former `§11.5 Release Workflow (Orphan-Branch Publish)` is renumbered to `§11.6` (Option C per RESEARCH Pitfall 1)."
    - "AGENTS.md §11.5 documents the **apply-vs-advisory architectural boundary** (D-01) AND the **per-script applied.log variance** (review item 10 contract): 01 apply-block has 2-line paired-inputs; 02 apply-block has 1-line literal `- (vault walk — no candidate inputs; 02 is direct-apply)`; 03/04 advisory-blocks omit `inputs:` and include `mutations: none` + `report_section:` pointing at REPORT.md anchors."
    - "AGENTS.md §11.5 documents the **paired immutable inputs contract for 01-page-typing** (review item 2): candidates.yaml (cluster membership) + decisions.yaml (policy) are both required; apply does NOT re-classify."
    - "AGENTS.md §11.5 documents the **script-location root resolution** for all four migration scripts (review item 1): BROWNFIELD_ROOT defaults to the parent of the `.brownfield/` containing the script, not to `$(pwd)`."
    - "AGENTS.md §11.5 documents the **D-09 operational enforcement** (review item 9): `verify` WARNs on stale candidate artifacts when `source_script_hash` drifts."
    - "AGENTS.md §5 `bootstrap_stage` row's forward-ref to `§11.5 Brownfield Workflow` is now CORRECT (no longer pointing at Release Workflow — Phase 10 WR-03 opportunistic fix closed)."
    - "`schema/AGENTS.template.md` §11.5 + §11.6 mirror AGENTS.md byte-equivalent (modulo wizard placeholders — §11.5 + §11.6 have none); Phase 9.1 template-parity test still passes."
    - "`schema/fixtures/canonical-AGENTS.md` regenerated via Phase 8-01 python3 routine; Phase 8-01 byte-equality test `test_canonical_byte_equality.sh` still passes."
    - "`CLAUDE.md` auto-syncs byte-identical to AGENTS.md via `.githooks/pre-commit` hook; staged cleanly per Phase 9.1 R-3 precedent (`bash bin/sync-claude.sh && git add CLAUDE.md` pre-commit)."
    - "`docs/reference/brownfield.md` `## suggest subcommand`, `## review-typing subcommand` (NEW), `## verify subcommand` sections are populated per D-17 (cluster UX description, AI-handoff template reference, candidate-file shapes with examples, full lifecycle walkthrough, troubleshooting table); existing `## scan subcommand`, `## bootstrap subcommand`, `## Rollback` sections preserved. Troubleshooting table includes entries for the item-4 EOF behavior and the item-9 stale-artifact WARN."
    - "`.planning/REQUIREMENTS.md` BRWN-12 wording is amended (`04-privacy-classification` → `04-privacy-review`); NEW requirement BRWN-22 added covering `review-typing` subcommand (small-batch TTY + EOF handling per item 4 + label validation per item 11, large-batch AI handoff, manifest-write-back, no-LLM-in-CLI constraint); traceability table updated; `v1.1 requirements` count bumped from 78 → 79."
    - "`wiki/decisions/dr-2026-04-20-brownfield-apply-vs-advisory.md` Tier-1 decision record exists per D-21 with `trigger_type: schema-update` and populated sections (TL;DR, Decision, Why, Alternatives Considered, Consequences, Affected Pages, Sources). The DR documents the review-feedback-driven hardenings (items 1, 2, 3, 4, 5, 7, 8, 9, 10, 11) as part of the Consequences section — they are NOT separate decisions, but follow-on details of the apply-vs-advisory architecture."
    - "`wiki/index.md` Decisions section includes the new DR entry; `wiki/log.md` has a `reflect` entry appended at EOF per AGENTS.md §12 newest-at-bottom append-only convention."
    - "All remaining Plan 11-01 RED tests tagged `EXPECTED_BY: 11-05` flip GREEN: `bash tests/phase-11/run.sh --expected-by 11-05` reports pass == total."
    - "The unfiltered aggregator `bash tests/phase-11/run.sh` ALSO reports pass == total at the end of Plan 11-05 (since all plan-specific subsets are now GREEN). This closes the per-plan-gate-split loop from item 6."
    - "All phase-wide test aggregators pass: `tests/phase-07/` (22/22), `tests/phase-08/` (21/21), `tests/phase-09/` (28/28), `tests/phase-09.1/` (11/11), `tests/phase-10/` (32/32), `tests/phase-11/` (~38/~38 unfiltered)."
  artifacts:
    - path: AGENTS.md
      provides: §11.5 Brownfield Workflow (new content — includes paired-inputs contract, root-resolution contract, per-script applied.log variance, stale-artifact WARN documented); §11.6 Release Workflow (renumbered); §5 bootstrap_stage forward-ref corrected (WR-03 closed)
      contains: "### 11.5 Brownfield Workflow"
    - path: docs/reference/brownfield.md
      provides: Complete suggest + review-typing + verify sections; lifecycle walkthrough; troubleshooting table with item-4 (EOF) + item-9 (stale artifact) entries
      min_lines_added: 280
    - path: .planning/REQUIREMENTS.md
      provides: BRWN-12 rename amendment; BRWN-22 (new) review-typing requirement including item-4 EOF + item-11 label validation; traceability flips
    - path: wiki/decisions/dr-2026-04-20-brownfield-apply-vs-advisory.md
      provides: Tier-1 decision record per D-21; trigger_type=schema-update
      min_lines: 100
  key_links:
    - from: AGENTS.md §11.5 Brownfield Workflow
      to: docs/reference/brownfield.md
      via: "See: docs/reference/brownfield.md pointer at end of §11.5"
      pattern: "See: docs/reference/brownfield.md"
    - from: AGENTS.md §11.5
      to: schema/brownfield/migrations/README.md (per-script applied.log variance, item 10)
      via: "pointer to the per-script variance doc"
      pattern: "applied.log block shapes"
    - from: wiki/decisions/dr-2026-04-20-brownfield-apply-vs-advisory.md
      to: wiki/index.md Decisions section
      via: wikilink entry
      pattern: "dr-2026-04-20-brownfield-apply-vs-advisory"
    - from: AGENTS.md
      to: CLAUDE.md
      via: .githooks/pre-commit auto-sync (bash bin/sync-claude.sh && git add CLAUDE.md)
      pattern: "bin/sync-claude.sh"
    - from: AGENTS.md §11.5 + §11.6
      to: schema/AGENTS.template.md §11.5 + §11.6
      via: byte-equivalent mirror enforced by Phase 9.1 template-parity test
      pattern: "schema/AGENTS.template.md"
    - from: AGENTS.md §5 bootstrap_stage row
      to: AGENTS.md §11.5 Brownfield Workflow
      via: "See `§11.5 Brownfield Workflow`" pointer (correct after Plan 11-05)
      pattern: "§11.5 Brownfield Workflow"
---

<review_feedback_applied>
This plan was revised per `.planning/phases/11-brownfield-suggest-verify/11-REVIEWS.md`:

- **Item 2 contract documentation:** AGENTS.md §11.5 subsection "11.5.1 suggest" explicitly documents the paired-immutable-inputs contract for 01-page-typing (candidates.yaml + decisions.yaml both required; apply does NOT re-classify). Matches Plan 11-03's implementation. Tracks the item 2 contract decision.
- **Item 6 contract documentation:** No direct documentation change; item 6 is a test-harness concern (aggregator gate split via `--expected-by`). But this plan IS the one that turns the unfiltered aggregator fully GREEN (because it closes the last plan-specific subset — `EXPECTED_BY: 11-05`). Success criteria clarified accordingly: after Plan 11-05, both `--expected-by <any>` AND the unfiltered `bash tests/phase-11/run.sh` report pass == total.
- **Item 9 contract documentation:** AGENTS.md §11.5.3 verify subsection documents the stale-candidate-artifact WARN as normative behavior: "verify compares each candidate YAML's `source_script_hash:` against the current body-post-op_hash-strip sha256 of the corresponding `.brownfield/migrations/*.sh` byte-copy; mismatches are reported as WARN on stderr without blocking promotion."
- **Item 10 contract documentation:** AGENTS.md §11.5 adds a new subsection "11.5.4 applied.log per-script shapes" documenting the per-script variance (01 2-line paired; 02 1-line literal advisory; 03/04 no inputs + report_section). Points at `schema/brownfield/migrations/README.md` for the authoritative schema.
- **docs/reference/brownfield.md troubleshooting table:** Add two new rows:
  * `review-typing hangs` → `You piped empty stdin; the CLI now detects EOF cleanly (item 4 fix). If you see hang, check you're not on an older pre-item-4 build.`
  * `verify WARNs 'stale candidate artifact'` → `Your .brownfield/migrations/*.sh byte-copy has drifted from the canonical script. Re-run bin/brownfield.sh suggest to refresh.`
- **Items 1, 3, 5, 7, 8, 11, 12, 14 contract documentation:** Rolled into the DR's "Consequences" section as implementation details. No new AGENTS.md lines — these are implementation hardenings of the existing D-01/D-02/D-04/D-10 decisions, not new architectural choices.
</review_feedback_applied>

<objective>
Finalize Phase 11 by populating the authoritative schema contract (AGENTS.md §11.5), the operator runbook (docs/reference/brownfield.md), the requirements traceability (REQUIREMENTS.md amendments), and the Tier-1 decision record that captures the apply-vs-advisory architecture + review-feedback hardenings. Opportunistically closes Phase 10 WR-03 (the §5 bootstrap_stage forward-ref typo) since we're editing §5 and §11.x at the same time.

Purpose: Without this plan, Phase 11's code works but is undocumented in the schema. §11.5 is the "sole authoritative specification" entry point for future agents; docs are the operator runbook; the DR captures the "why" of the review-manifest architecture + review-feedback hardenings for future maintainers.

Output: All ~7 remaining Plan 11-01 RED tests tagged `EXPECTED_BY: 11-05` flip GREEN; REQUIREMENTS.md traceability table flips BRWN-11..20 + BRWN-22 to `Complete`; `bash tests/phase-11/run.sh` emits fully GREEN (unfiltered AND every `--expected-by <plan>` subset); all prior-phase test aggregators still pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/11-brownfield-suggest-verify/11-CONTEXT.md
@.planning/phases/11-brownfield-suggest-verify/11-RESEARCH.md
@.planning/phases/11-brownfield-suggest-verify/11-PATTERNS.md
@.planning/phases/11-brownfield-suggest-verify/11-REVIEWS.md
@.planning/phases/11-brownfield-suggest-verify/11-04-SUMMARY.md
@AGENTS.md
@CLAUDE.md
@schema/AGENTS.template.md
@docs/reference/brownfield.md
@bin/sync-claude.sh

<interfaces>
<!-- AGENTS.md §11.5 normative contract per D-16 + RESEARCH Q10 + review feedback -->

AGENTS.md §11.5 template shape (EXACT match to §11.1–11.4 preamble + numbered Steps + Abort conditions — per D-16 + review-feedback hardenings):

```markdown
### 11.5 Brownfield Workflow

The brownfield workflow onboards existing Obsidian vaults into the wiki compiler
schema. It is the mechanical counterpart to ingest (§11.1) — where ingest creates
wiki pages from sources, brownfield transforms pre-existing vault pages into
schema-compliant form. The workflow operates through five subcommands plus the
`bootstrap_stage` lifecycle gate.

**Core principle:** *Review may be interactive and AI-guided; apply must always be deterministic.*

**Architectural boundary — apply class vs advisory class:**

| Subcommand / script | Class | What it does |
|---------------------|-------|--------------|
| `bin/brownfield.sh scan` | inventory | Dry-run classification; writes `.brownfield/REPORT.md`; zero vault mutation (Phase 10) |
| `bin/brownfield.sh bootstrap` | apply | Mechanical frontmatter injection with `bootstrap_stage: bootstrapped` (Phase 10) |
| `bin/brownfield.sh suggest` | generator | Byte-copies canonical migration scripts + generates candidate data files |
| `bin/brownfield.sh review-typing` | orchestrator | TTY small-batch cluster prompts OR large-batch AI-handoff via prompt.md |
| `bin/brownfield.sh verify [--promote]` | gate | Read-only lint wrapper + stale-artifact WARN; `--promote` flips `bootstrap_stage` on passing pages |
| `.brownfield/migrations/01-page-typing.sh` | apply | Page typing from paired manifests (candidates + decisions) |
| `.brownfield/migrations/02-provenance-bootstrap.sh` | apply | TL;DR + Key Facts top-level-bullet `[epistemic:: inferred]` tagging |
| `.brownfield/migrations/03-cross-link-inference.sh` | advisory | Cross-link candidates report |
| `.brownfield/migrations/04-privacy-review.sh` | advisory | Privacy-sensitive findings report |

**Root resolution (all four migration scripts):** each script derives its vault
root from its own filesystem location — specifically, the parent of the
`.brownfield/` directory containing the script. Migration scripts do NOT default
to `$(pwd)`; `BROWNFIELD_ROOT` is an explicit env-var override for advanced use.
This prevents cross-tree mutation when a script is invoked via absolute path
from an unrelated cwd.

#### bootstrap_stage Lifecycle

\```
(absent) ──[bin/brownfield.sh bootstrap --apply]──> bootstrapped
bootstrapped ──[bin/brownfield.sh verify --promote, passes gate]──> verified
bootstrapped ──[normal ingest via bin/ingest.sh]──> (stripped per BRWN-10)
verified     ──[no automatic downgrade]──> (manual edit only)
raw          ──[reserved for future import workflows]──> (no writer in v1.1)
\```

#### 11.5.1 suggest

\```
Trigger:  User completes bootstrap and wants to migrate typing / provenance /
          cross-links / privacy.
Inputs:   Vault (current state) + bin/lib/brownfield_classify.py rule set.
Outputs:  .brownfield/migrations/*.sh (byte-copies with op_hash headers on lines
          2 and 3, shebang preserved on line 1),
          .brownfield/*.yaml candidate data files (each opening with a D-09
          metadata header including source_script_hash for stale-artifact
          detection by `verify`),
          REPORT.md advisory sections.
Commit:   N/A (.brownfield/ is gitignored per TMPL-04).
\```

**Steps:**

1. Byte-copy canonical scripts from `schema/brownfield/migrations/*.sh` into
   `.brownfield/migrations/*.sh`. Prepend `# op_hash: sha256:<hex>` (line 2) +
   `# op_hash_scope: canonical-script-body + data-schema-version` (line 3),
   preserving the shebang on line 1.
2. Walk vault using `bin/lib/brownfield_walk.py walk_vault_respecting_ignore()` —
   the SAME helper used by `scan`. Honors `.brownfield-ignore` patterns.
3. Cluster classifications via `cluster_by_signals()`. Write
   `.brownfield/page-typing-candidates.yaml` with metadata header per D-09.
4. Write `.brownfield/page-typing-decisions.yaml` with every cluster
   `decision: pending`. High-confidence clusters auto-approve when EITHER the
   frontmatter signal is an explicit valid type enum OR 3+ non-frontmatter
   signals agree with the proposed label (D-03 verbatim).
5. Generate `.brownfield/provenance-bootstrap-report.yaml`,
   `cross-link-candidates.yaml`, `privacy-findings.yaml`.
6. Append `## Cross-link candidates` + `## Privacy review` sections to
   `.brownfield/REPORT.md`.

**Paired immutable inputs contract:** `page-typing-candidates.yaml` (cluster
membership — which pages belong to which cluster) and `page-typing-decisions.yaml`
(policy — which clusters are approved, rejected, or pending, with optional
per-page overrides) are consumed TOGETHER by `01-page-typing.sh --apply`.
`--apply` never re-classifies at apply time; candidates.yaml is read strictly
for the cluster-id → page-list lookup. Both files are required — deleting
either before apply causes a hard error.

**Abort conditions:**

- `schema/brownfield/migrations/` missing from repo.
- Vault root does not exist.

#### 11.5.2 review-typing

\```
Trigger:  User resolves pending clusters in page-typing-decisions.yaml.
Inputs:   .brownfield/page-typing-candidates.yaml (read)
          .brownfield/page-typing-decisions.yaml (read+write)
Outputs:  Updated decisions.yaml with resolved decisions; OR
          .brownfield/review-typing-prompt.md (large-batch AI handoff).
Commit:   N/A (.brownfield/ is gitignored).
\```

**Steps:**

1. Count pending clusters in decisions.yaml.
2. **If pending count < N (default 20) AND stdout is a TTY:** TTY prompts
   cluster-by-cluster with primitives `approve all / reject all / inspect /
   override / skip`. Override labels are validated against the `type:` enum
   before being persisted to the decisions manifest.
3. **If pending count ≥ N OR non-TTY:** emit `.brownfield/review-typing-prompt.md`
   — directive template pointing at candidates + decisions YAMLs. Tell user to
   open in AI session (Claude Code, Codex, etc.) and edit the decisions manifest.
4. On stdin EOF (e.g., piped `</dev/null`): write back any decisions made so
   far and exit cleanly. Never loop indefinitely.
5. Write decisions.yaml with ruamel.yaml round-trip (preserves user comments).

The CLI never calls an LLM. The AI-handoff template operates on the file
artifact from outside the CLI; the user runs a deterministic apply script afterward.

**Abort conditions:**

- `.brownfield/page-typing-decisions.yaml` not found → run `suggest` first.
- All clusters already resolved → report cleanly and exit 0.

#### 11.5.3 verify [--promote]

\```
Trigger:  User checks vault passes lint after migration scripts have run.
Inputs:   Vault + `.brownfield/page-typing-decisions.yaml` (for Gate 5) +
          .brownfield/*.yaml metadata headers (for stale-artifact detection).
Outputs:  stdout summary of blockers + optional WARN on stale candidate
          artifacts; (with --promote) `bootstrap_stage` flips on eligible pages.
Commit:   N/A (frontmatter mutations via ruamel round-trip; user commits separately).
\```

**Steps (read-only mode):**

1. For each `.brownfield/*.yaml` candidate file, compare its D-09
   `source_script_hash:` header against the current body-post-op_hash-strip
   sha256 of the corresponding `.brownfield/migrations/*.sh`. Emit a stderr
   WARN for each mismatch (`stale candidate artifact detected: <cand>; re-run
   `bin/brownfield.sh suggest` to refresh`).
2. Run `bin/lint.sh --ci --format json --category yaml,provenance,orphan,crossref,brownfield`.
3. Print summary grouped by severity; list paths blocking promotion.
4. Exit 0 regardless of findings (stale WARN and lint findings are diagnostic,
   not gating).

**Steps (`--promote`):**

1. Run steps 1–3 above (stale-artifact WARN + lint).
2. Read `.brownfield/page-typing-decisions.yaml`; build pending-pages set.
3. Walk vault; for each `bootstrap_stage: bootstrapped` page, apply the 5-gate
   pass-list: (a) currently bootstrapped; (b) `type:` is a valid enum per §4;
   (c) zero error-severity lint findings for the page; (d) type-specific
   required fields present (e.g., `path`, `content_hash`, `ingested_at`,
   `source_type` for `type: source`); (e) not in pending-pages set.
4. If all 5 gates pass: flip `bootstrap_stage: verified` via `write_roundtrip`.
5. Print summary: `N pages promoted; M pages blocked`.

Privacy is not checked here — `bin/check-privacy.sh` handles the public-paths
leak guard (see §13 and Phase 9).

**Abort conditions:**

- `bin/lint.sh` not found or exits with runtime error.
- No `bootstrap_stage: bootstrapped` pages found → nothing to verify.

#### 11.5.4 applied.log per-script shapes

`.brownfield/applied.log` is append-only; one block per meaningful execution.
Apply-class (01, 02) append on `--apply` only (dry-run never appends); advisory-
class (03, 04) append on findings. The block shape is **per-script** — each
script's exact schema is documented verbatim in `schema/brownfield/migrations/README.md`
and summarized here:

- **01-page-typing.sh (apply):** `inputs:` is a two-item list — hashed candidates.yaml + hashed decisions.yaml. `summary:` has `approved_clusters`, `overridden_pages`, `pending_pages_remaining`.
- **02-provenance-bootstrap.sh (apply):** `inputs:` is a one-item literal string `- (vault walk — no candidate inputs; 02 is direct-apply)` — 02 is direct-apply, no candidate manifest. `summary:` has `pages_with_eligible_bullets`, `pages_with_no_eligible_bullets`.
- **03-cross-link-inference.sh (advisory):** no `inputs:` field. `mutations: none` + `report_section: REPORT.md#cross-link-candidates`.
- **04-privacy-review.sh (advisory):** no `inputs:` field. `mutations: none` + `report_section: REPORT.md#privacy-review`.

All four share the invariant fields: `## <script> @ <UTC ISO>` header, `mode:`, `op_hash:`, `exit_code:`, `prereq_check:`, `summary:`. Field variance beyond these is intentional and documented (NOT drift).

See: docs/reference/brownfield.md for operator runbook, troubleshooting, and the
full lifecycle walkthrough.
```

**Template parity requirement (Phase 9.1 R-3):** schema/AGENTS.template.md §11.5 + §11.6 must mirror AGENTS.md byte-equivalent (no placeholders in these sections).

**CLAUDE.md byte-sync:** .githooks/pre-commit runs bin/sync-claude.sh; after editing AGENTS.md, run:
```bash
bash bin/sync-claude.sh && git add CLAUDE.md
```
BEFORE `git commit` to avoid the two-attempt path (Phase 9.1 R-3 precedent).

**Canonical fixture regeneration (Phase 8-01):** After schema/AGENTS.template.md edit, regenerate schema/fixtures/canonical-AGENTS.md:
```bash
export WIZARD_GENERATED_AT="2026-04-16T00:00:00Z"
export WIZARD_TEMPLATE_SHA="<frozen-fixture>"
bash bin/init-wizard.sh --answers-file schema/fixtures/canonical-answers.yaml --render-to /tmp/wz-regen
cp /tmp/wz-regen/AGENTS.md schema/fixtures/canonical-AGENTS.md
```

**REQUIREMENTS.md amendments:**

BRWN-12 (rename) — change line 97:
FROM: `- [ ] **BRWN-12**: Four staged migration script classes: 01-page-typing.sh, 02-provenance-bootstrap.sh, 03-cross-link-inference.sh, 04-privacy-classification.sh`
TO:   `- [ ] **BRWN-12**: Four staged migration script classes: 01-page-typing.sh, 02-provenance-bootstrap.sh, 03-cross-link-inference.sh, 04-privacy-review.sh (renamed from 04-privacy-classification.sh in Phase 11 per D-07 — fail-closed preserved; script is advisory-only, never flips privacy:)`

BRWN-22 (new, added after BRWN-21 at line 106) — revised wording incorporating review items 4 + 11:
`- [ ] **BRWN-22**: `bin/brownfield.sh review-typing` orchestrator — small-batch TTY cluster-by-cluster prompts (approve/reject/inspect/override/skip) when pending clusters < N (default 20) AND stdout is a TTY; large-batch AI-handoff via `.brownfield/review-typing-prompt.md` when pending ≥ N OR non-TTY. Both modes write to `.brownfield/page-typing-decisions.yaml` via ruamel.yaml round-trip (comments preserved). Small-batch prompt loop handles stdin EOF cleanly (aborts session without hanging — review item 4). Override labels are validated against the `type:` enum at entry time (review item 11). No LLM calls inside `bin/brownfield.sh` (BRWN-16); AI-handoff prompt is a static markdown artifact consumed outside the CLI.`

Traceability table update (lines 228-237):
- Flip BRWN-11..20 Status from `Pending` to `Complete` (pending Plan 11-05 verification close).
- Add new row: `| BRWN-22 | Phase 11 | Complete |`.

Coverage line update (line 243):
FROM: `v1.1 requirements: 78 total (TMPL: 11, NEUT: 8, WZRD: 11, MANUAL: 6, COLAB: 8, CI: 9, BRWN: 21, DEBT: 4)`
TO:   `v1.1 requirements: 79 total (TMPL: 11, NEUT: 8, WZRD: 11, MANUAL: 6, COLAB: 8, CI: 9, BRWN: 22, DEBT: 4)`
Line 244 update: `Mapped to phases: 79 (..., Phase 11: 11, ...)`.

**Tier-1 DR content scope (D-21 + review-feedback hardenings):**

File: wiki/decisions/dr-2026-04-20-brownfield-apply-vs-advisory.md

Frontmatter (per AGENTS.md §4.6):
```yaml
---
id: dr-2026-04-20-brownfield-apply-vs-advisory
title: "Brownfield Apply-vs-Advisory Architecture + Review-Manifest Pattern"
type: decision
trigger_type: schema-update
affected_pages: []
status: active
summary: "Captures the apply-class vs advisory-class split, review-manifest pattern for 01-page-typing, bootstrap_stage lifecycle gate, and cross-AI review-feedback hardenings (items 1–11) introduced in Phase 11."
created_at: 2026-04-20
updated_at: 2026-04-20
sources: []
epistemic_status: sourced
tags: [brownfield, architecture, apply-class, advisory-class, review-manifest]
domains: [schema-evolution]
supersedes: null
superseded_by: null
privacy: cloud_safe
aliases: []
has_contradictions: false
knowledge_domain: ""
---
```

Required sections (AGENTS.md §4.6):
1. TL;DR — one paragraph stating the decision.
2. Decision — what was chosen.
3. Why — what framing was adopted and what it replaced.
4. Alternatives Considered — must list alternatives with rejection rationale.
5. Consequences — what changes downstream, INCLUDING the review-feedback hardenings (items 1-11) as implementation details.
6. Affected Pages — enumerate (AGENTS.md §11.5, docs/reference/brownfield.md, each migration script, the REQUIREMENTS.md amendments).
7. Sources — CONTEXT/RESEARCH/PATTERNS/REVIEWS references from Phase 11 planning artifacts.

**docs/reference/brownfield.md content (per D-17 + review feedback items 4/9):**

Current state: scan + bootstrap sections populated (Phase 10); `## suggest subcommand` + `## verify subcommand` stubs; NO `## review-typing subcommand` section.

Phase 11 Plan 11-05 replacements:
- Replace `## suggest subcommand` stub with full section (Section 2a below).
- INSERT new `## review-typing subcommand` section between suggest and verify.
- Replace `## verify subcommand` stub with full section (including stale-artifact-WARN description per item 9).
- Preserve existing `## Rollback` section (BRWN-19 already shipped in Phase 10).
- Add a `## Lifecycle walkthrough` + `## Troubleshooting` section at the end (before Rollback). Troubleshooting table includes rows for item 4 (EOF) + item 9 (stale artifact WARN) + all pre-review entries.

Section shapes (match existing scan/bootstrap section pattern — Flags table + Output contract + subsections).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AGENTS.md §11.5 Brownfield Workflow populate (includes paired-inputs + root-resolution + per-script applied.log variance + stale-artifact WARN per review items 1/2/9/10) + §11.6 Release Workflow renumber + §5 bootstrap_stage forward-ref fix (Option C / WR-03 close) + template mirror + canonical fixture regen + CLAUDE.md sync</name>
  <files>
    AGENTS.md,
    schema/AGENTS.template.md,
    schema/fixtures/canonical-AGENTS.md,
    CLAUDE.md
  </files>
  <read_first>
    - AGENTS.md §11.1 Ingest Workflow (lines ~866-902) — structure template (Trigger/Inputs/Outputs/Commit block + numbered Steps + Abort conditions)
    - AGENTS.md §11.5 Release Workflow (lines ~1171-1175) — CURRENT content to renumber to §11.6
    - AGENTS.md §5 bootstrap_stage row (line ~302) — has forward-ref typo to §11.5 (Phase 10 WR-03); point at §11.5 after Option C renumber (ref still reads "§11.5" but now points at the new Brownfield Workflow, closing WR-03)
    - schema/AGENTS.template.md §11.5 (line ~1138) — parallel current Release Workflow content; mirror in lockstep
    - schema/fixtures/canonical-AGENTS.md — read header to find canonical-answers.yaml inputs; regeneration recipe per Phase 8-01
    - tests/phase-08/test_canonical_byte_equality.sh (lines 7-40) — regeneration recipe invocation
    - bin/sync-claude.sh — verify it is byte-identity (cp AGENTS.md CLAUDE.md)
    - .planning/phases/11-brownfield-suggest-verify/11-RESEARCH.md Pitfall 1 (lines 478–491) + Q10 (lines 1152–1268) — §11.5 template shape and renumbering Option C rationale
    - .planning/phases/11-brownfield-suggest-verify/11-REVIEWS.md (items 1, 2, 9, 10 to anchor in §11.5 body)
    - tests/phase-11/test_agents_section_11_5.sh (Plan 11-01) — assertion shape the edit must satisfy
  </read_first>
  <behavior>
    - Option C renumber per RESEARCH Pitfall 1: current §11.5 "Release Workflow (Orphan-Branch Publish)" → §11.6; new content "Brownfield Workflow" occupies §11.5.
    - AGENTS.md §5 bootstrap_stage row currently reads `See §11.5 Brownfield Workflow (populated in Phase 11).` — after renumber, this string is STILL correct (points at the newly-populated §11.5 content), so WR-03 is closed by construction.
    - schema/AGENTS.template.md gets the SAME structural edit (renumber + populate). Phase 9.1 template-parity test asserts byte-equivalence modulo wizard placeholders — §11.5 + §11.6 have none.
    - schema/fixtures/canonical-AGENTS.md is REGENERATED via bin/init-wizard.sh using the pinned env vars per Phase 8-01 test.
    - CLAUDE.md is auto-synced via pre-commit hook; run `bash bin/sync-claude.sh && git add CLAUDE.md` BEFORE git commit to collapse the documented 2-attempt R-3 path.
    - Body of new §11.5 follows the exact structure from `<interfaces>` block above — including the four sub-sections 11.5.1 suggest / 11.5.2 review-typing / 11.5.3 verify / 11.5.4 applied.log per-script shapes.
    - Review-feedback anchors embedded in §11.5 text:
      * Paired immutable inputs contract for 01 (item 2) — explicit paragraph under 11.5.1 Steps.
      * Root resolution from script location (item 1) — explicit paragraph after the subcommand table.
      * Stale-artifact WARN in verify (item 9) — Step 1 of 11.5.3.
      * Per-script applied.log variance (item 10) — new sub-section 11.5.4.
    - Docs pointer at end of §11.5: `See: docs/reference/brownfield.md` for operator runbook — matches §§11.1/11.2/12 convention.
  </behavior>
  <action>
    ## 1a. AGENTS.md edits

    ### Step 1 — Renumber existing §11.5 Release Workflow to §11.6

    Find the line `### 11.5 Release Workflow (Orphan-Branch Publish)` in AGENTS.md (~line 1171). Change to `### 11.6 Release Workflow (Orphan-Branch Publish)`. Leave the 3-line body intact.

    ### Step 2 — Insert §11.5 Brownfield Workflow BEFORE the (newly-renumbered) §11.6

    Place the full §11.5 Brownfield Workflow content from the `<interfaces>` block above verbatim BEFORE the `### 11.6 Release Workflow` line. The content includes:
    - Preamble paragraph + core principle quote (verbatim from D-01).
    - Architectural boundary table (9 rows — 5 subcommands + 4 migration scripts).
    - Root-resolution paragraph (item 1 contract documented).
    - bootstrap_stage Lifecycle code fence (D-15 verbatim).
    - Sub-section **§11.5.1 suggest** — Trigger/Inputs/Outputs/Commit preamble + numbered Steps + **Paired immutable inputs contract** paragraph (item 2) + Abort conditions.
    - Sub-section **§11.5.2 review-typing** — Trigger/Inputs/Outputs/Commit preamble + numbered Steps (including Step 4 EOF-safe behavior per item 4) + Abort conditions.
    - Sub-section **§11.5.3 verify [--promote]** — Trigger/Inputs/Outputs/Commit preamble + numbered Steps (including stale-artifact-WARN per item 9) + Abort conditions.
    - Sub-section **§11.5.4 applied.log per-script shapes** — per-script variance per item 10.
    - Final `See:` pointer to docs.

    ### Step 3 — Verify §5 bootstrap_stage forward-ref now resolves correctly

    Line ~302 in AGENTS.md currently reads:
    `... See \`§11.5 Brownfield Workflow\` (populated in Phase 11). |`

    After Option C renumbering, §11.5 IS Brownfield Workflow → the existing forward-ref is NOW CORRECT. WR-03 closed opportunistically. NO EDIT needed to §5 line 302 text. (Verify grep confirms this before committing.)

    ## 1b. schema/AGENTS.template.md — mirror the AGENTS.md edit

    Make the EXACT same structural edits to schema/AGENTS.template.md:
    - Rename `### 11.5 Release Workflow` → `### 11.6 Release Workflow` (around line 1138).
    - Insert the same §11.5 Brownfield Workflow content BEFORE §11.6.
    - §5 bootstrap_stage row is mirrored from AGENTS.md — no template-specific edit here.

    Phase 9.1 template-parity test (tests/phase-09.1/test_template_parity.sh) asserts byte-equivalence modulo wizard placeholders. §11.5 + §11.6 introduce NO placeholders (no `{{...}}` tokens) — so the body blocks match AGENTS.md verbatim.

    ## 1c. schema/fixtures/canonical-AGENTS.md — regenerate via bin/init-wizard.sh

    Use the pinned env vars per Phase 8-01 test regeneration recipe:

    ```bash
    export WIZARD_GENERATED_AT="2026-04-16T00:00:00Z"
    export WIZARD_TEMPLATE_SHA="$(git log -1 --format=%H schema/AGENTS.template.md)"
    WIZARD_OUT=$(mktemp -d -t wz-regen-XXXXXX)
    bash bin/init-wizard.sh --answers-file schema/fixtures/canonical-answers.yaml --render-to "$WIZARD_OUT"
    cp "$WIZARD_OUT/AGENTS.md" schema/fixtures/canonical-AGENTS.md
    rm -rf "$WIZARD_OUT"
    ```

    Verify `tests/phase-08/test_canonical_byte_equality.sh` passes after regeneration. If it fails, check:
    - `WIZARD_TEMPLATE_SHA` pin matches the new post-edit template SHA.
    - `schema/fixtures/canonical-answers.yaml` metadata.generated_at still `2026-04-16T00:00:00Z` (Phase 8-01 pin).

    ## 1d. CLAUDE.md — sync from AGENTS.md via pre-commit hook precedent

    Run BEFORE git commit to collapse the documented 2-attempt R-3 path (Phase 9.1):
    ```bash
    bash bin/sync-claude.sh
    git add CLAUDE.md
    ```

    `bin/sync-claude.sh` copies AGENTS.md to CLAUDE.md byte-identically. The pre-commit hook (`.githooks/pre-commit`) would do this automatically on commit, but running it manually stages cleanly in one attempt.
  </action>
  <verify>
    <automated>
      grep -c '^### 11.5 Brownfield Workflow' AGENTS.md | grep -q '^1$' &&
      grep -c '^### 11.5 Release Workflow' AGENTS.md | grep -q '^0$' &&
      grep -c '^### 11.6 Release Workflow' AGENTS.md | grep -q '^1$' &&
      grep -q 'Review may be interactive and AI-guided; apply must always be deterministic\.' AGENTS.md &&
      grep -q '§11.5 Brownfield Workflow' AGENTS.md &&
      # Item 2 anchor: paired immutable inputs paragraph present
      grep -q 'Paired immutable inputs contract' AGENTS.md &&
      # Item 1 anchor: root resolution paragraph present
      grep -q 'parent of the' AGENTS.md &&
      grep -q 'Migration scripts do NOT default' AGENTS.md &&
      # Item 9 anchor: stale-artifact WARN documented in 11.5.3
      grep -q 'stale candidate artifact detected' AGENTS.md &&
      # Item 10 anchor: per-script applied.log shapes subsection present
      grep -q '#### 11.5.4 applied.log per-script shapes' AGENTS.md &&
      grep -q '02 is direct-apply' AGENTS.md &&
      grep -c '^### 11.5 Brownfield Workflow' schema/AGENTS.template.md | grep -q '^1$' &&
      grep -c '^### 11.6 Release Workflow' schema/AGENTS.template.md | grep -q '^1$' &&
      cmp -s AGENTS.md CLAUDE.md &&
      bash tests/phase-08/test_canonical_byte_equality.sh >/dev/null &&
      bash tests/phase-11/test_agents_section_11_5.sh >/dev/null &&
      bash tests/phase-11/test_agents_template_parity_11_5.sh >/dev/null &&
      bash tests/phase-11/test_canonical_agents_byte_equality.sh >/dev/null &&
      bash tests/phase-09.1/run.sh 2>&1 | tail -5 | grep -qE 'PHASE 09.1 TESTS: [0-9]+/[0-9]+$'
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^### 11.5 Brownfield Workflow' AGENTS.md` returns 1.
    - `grep -c '^### 11.5 Release Workflow' AGENTS.md` returns 0 (renumbered to §11.6).
    - `grep -c '^### 11.6 Release Workflow' AGENTS.md` returns 1.
    - The D-01 design principle `Review may be interactive and AI-guided; apply must always be deterministic.` appears verbatim in AGENTS.md.
    - The bootstrap_stage lifecycle diagram (D-15) appears in §11.5.
    - **Item 2 anchor:** §11.5 text contains the phrase "Paired immutable inputs contract" explicitly.
    - **Item 1 anchor:** §11.5 explicitly documents root resolution via script location (parent of `.brownfield/`), not `$(pwd)`.
    - **Item 9 anchor:** §11.5.3 verify documents stale-artifact-WARN (`stale candidate artifact detected:` phrase present in the Steps body).
    - **Item 10 anchor:** §11.5.4 applied.log per-script shapes subsection exists, documents per-script variance (01 paired, 02 literal advisory line, 03/04 no inputs).
    - AGENTS.md §5 `bootstrap_stage` row forward-ref is correct: `grep -q '§11.5 Brownfield Workflow' AGENTS.md` succeeds. WR-03 closed.
    - `cmp -s AGENTS.md CLAUDE.md` returns 0 (byte-identical — CLAUDE.md synced).
    - `bash tests/phase-09.1/run.sh` emits `PHASE 09.1 TESTS: 11/11` (template-parity test still green).
    - `bash tests/phase-08/test_canonical_byte_equality.sh` passes (canonical-AGENTS regenerated correctly).
    - Plan 11-01 RED tests flip GREEN: `test_agents_section_11_5.sh`, `test_agents_template_parity_11_5.sh`, `test_canonical_agents_byte_equality.sh`.
  </acceptance_criteria>
  <done>
    AGENTS.md §11.5 + §11.6 populated with review-feedback anchors; template + fixture + CLAUDE.md synchronized; WR-03 closed; all AGENTS-related tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: docs/reference/brownfield.md populate suggest/review-typing/verify + troubleshooting rows for item 4 (EOF) and item 9 (stale artifact) + REQUIREMENTS.md amendments + Tier-1 DR (covers review-feedback hardenings items 1-11 as consequences) + wiki/index.md + wiki/log.md reflect entry</name>
  <files>
    docs/reference/brownfield.md,
    .planning/REQUIREMENTS.md,
    wiki/decisions/dr-2026-04-20-brownfield-apply-vs-advisory.md,
    wiki/index.md,
    wiki/log.md
  </files>
  <read_first>
    - docs/reference/brownfield.md (FULL file; study existing scan + bootstrap section shape for consistency; find the Phase-10 stubs at `## suggest subcommand` + `## verify subcommand`)
    - AGENTS.md §4.6 Decision record spec (full section)
    - schema/templates/decision.md (decision-record template; use as starting point)
    - wiki/decisions/ (list existing decision records for ID-naming precedent + section ordering)
    - wiki/index.md (Decisions section — find pattern for adding a new DR wikilink)
    - wiki/log.md (EOF — understand newest-at-bottom append-only convention)
    - .planning/REQUIREMENTS.md lines 96–106, 228–245 (BRWN section; traceability table + coverage summary)
    - .planning/phases/11-brownfield-suggest-verify/11-CONTEXT.md D-21 (DR scope + content mandate)
    - .planning/phases/11-brownfield-suggest-verify/11-RESEARCH.md Q9 (verify performance budget — used in troubleshooting docs)
    - .planning/phases/11-brownfield-suggest-verify/11-REVIEWS.md (items 1-15 — the hardenings to document in DR Consequences)
  </read_first>
  <behavior>
    - docs are operator-oriented: examples, flag tables, troubleshooting. Do NOT duplicate AGENTS.md §11.5 normative contract; link to it.
    - Stubs at `## suggest subcommand` and `## verify subcommand` are REPLACED (not appended to); `## review-typing subcommand` is INSERTED between them.
    - Troubleshooting table includes rows for item 4 (EOF behavior) + item 9 (stale artifact WARN) + all pre-review entries.
    - REQUIREMENTS.md changes: BRWN-12 wording amendment + BRWN-22 new row (mentioning items 4 + 11) + traceability flips + coverage count bump.
    - DR filename uses 2026-04-20 date to match `wiki/decisions/dr-YYYY-MM-DD-slug.md` convention (Phase 6 D-06 precedent).
    - DR is a wiki page (type: decision); it belongs in the wiki/ tree and must obey AGENTS.md §4.6 + §5 validation checklist.
    - DR's Consequences section documents the review-feedback hardenings (items 1-11) as implementation details — NOT separate decisions, but follow-on details of the apply-vs-advisory + review-manifest architecture.
    - wiki/log.md entry uses the `reflect` operation type per AGENTS.md §12.
  </behavior>
  <action>
    ## 2a. docs/reference/brownfield.md populate

    ### Step 1 — Replace `## suggest subcommand` stub (currently lines ~170-174 per Phase 10's incomplete placeholder)

    ```markdown
    > See AGENTS.md §11.5 Brownfield Workflow for the authoritative contract. This section covers operator workflow, examples, and troubleshooting — it does not restate normative semantics.

    ## suggest subcommand

    `bin/brownfield.sh suggest` generates the migration scripts + candidate data files needed for page typing, provenance bootstrap, cross-link inference, and privacy review. It is a generator — it writes to `.brownfield/` (gitignored) but never mutates vault pages.

    ### Flags

    | Flag | Default | Effect |
    |------|---------|--------|
    | `--root DIR` | `.` | Vault root |
    | `--help` | — | Print help and exit 0 |

    ### Output contract

    After `suggest` runs:

    - `<root>/.brownfield/migrations/` contains byte-copies of the four canonical migration scripts, each carrying an `# op_hash: sha256:<hex>` header (line 2) + `# op_hash_scope: canonical-script-body + data-schema-version` (line 3) prepended at copy time. The shebang stays on line 1.
    - `<root>/.brownfield/` contains five candidate/data YAML files, each opening with a D-09 metadata header (including `source_script_hash:` — consumed by `verify` for stale-artifact detection):
      - `page-typing-candidates.yaml` — clusters + signals per page typing decisions.
      - `page-typing-decisions.yaml` — policy manifest with `decision: pending` on all clusters except auto-approved high-confidence (explicit frontmatter type OR 3+ non-frontmatter signals agree — D-03 verbatim).
      - `provenance-bootstrap-report.yaml` — dry-run preview of 02's eligible-TOP-LEVEL-bullet counts per page.
      - `cross-link-candidates.yaml` — advisory cross-link proposals for 03.
      - `privacy-findings.yaml` — advisory privacy-sensitive findings for 04.
    - `<root>/.brownfield/REPORT.md` gains `## Cross-link candidates` and `## Privacy review` sections (appended; does not overwrite Phase 10 sections).

    ### Scan scope

    `suggest` reuses the Phase 10 `.brownfield-ignore` walker (`bin/lib/brownfield_walk.py walk_vault_respecting_ignore()`) — the SAME helper `scan` uses. Control-plane files (`docs/`, `schema/`, `examples/`, `AGENTS.md`, `CLAUDE.md`, `README.md`, `.github/`) listed in `.brownfield-ignore` are never classified. This prevents sweeping control-plane material into page-typing candidates when suggest is run at repo root.

    ### Metadata header example

    Each candidate YAML opens with the D-09 block:

    ```yaml
    # ---
    # schema_version: 1
    # tool_version: 1.1.0
    # generated_at: 2026-04-20T00:00:00Z
    # vault_root: /home/user/vault
    # source_script_hash: sha256:...
    # ---
    clusters:
      - cluster_id: cluster_1
        ...
    ```

    The `generated_at` field is pinned to `BROWNFIELD_FIXTURE_TODAY` when that env var is set (useful for fixture determinism); otherwise uses wall-clock UTC. `source_script_hash` lets `verify` detect stale review artifacts when the canonical script changes.

    ## review-typing subcommand

    `bin/brownfield.sh review-typing` is the orchestrator that resolves pending clusters in `.brownfield/page-typing-decisions.yaml`. It branches on pending-cluster count:

    - **Small batch (< threshold, default 20) AND TTY available:** cluster-by-cluster prompts with primitives `[a]pprove all / [r]eject all / [i]nspect / [o]verride / [s]kip`. On stdin EOF (piped `</dev/null`) the session aborts cleanly; partial progress is saved. Override labels are validated against the `type:` enum at entry time.
    - **Large batch (≥ threshold) OR non-TTY:** emits `.brownfield/review-typing-prompt.md` — a directive template pointing at the candidates + decisions manifests. Open the prompt in your AI session (Claude Code, Codex, etc.). The AI assists with tradeoff articulation + cluster merge/split suggestions but edits ONLY the decisions manifest.

    ### Flags

    | Flag | Default | Effect |
    |------|---------|--------|
    | `--root DIR` | `.` | Vault root |
    | `--threshold N` | 20 | Pending-cluster count threshold for large-batch mode |
    | `--help` | — | Print help and exit 0 |

    ### Small-batch TTY session example

    ```
    $ bash bin/brownfield.sh review-typing
    review-typing: 3 pending clusters; TTY mode.

    === cluster_2 (4 pages, confidence=medium) ===
    Proposed label: concept
    Signals: {'frontmatter': 'none', 'filename': 'kebab', 'heading': 'concept-like', 'inbound': 'inbound-light', 'links': 'outbound-heavy'}
    Sample pages (first 5):
      - wiki/concepts/attention-mechanism.md
      - wiki/concepts/transformer.md
      ...

    [a]pprove all / [r]eject all / [i]nspect / [o]verride / [s]kip: a
    -> approved (all 4 pages -> concept)
    ...
    ```

    Color output honors `NO_COLOR` env var (Phase 8 D-20 precedent).

    ### Large-batch AI handoff

    When pending ≥ threshold or when stdout is not a TTY, review-typing writes `.brownfield/review-typing-prompt.md` with instructions for your AI session. The CLI itself never calls an LLM — the prompt.md artifact is a plain markdown file that you open in your assistant's context. The assistant edits the decisions manifest; you then run `bash .brownfield/migrations/01-page-typing.sh --apply`.

    **Decision boundary:** *Review may be interactive and AI-guided; apply must always be deterministic.*

    ## verify subcommand

    `bin/brownfield.sh verify` wraps `bin/lint.sh` with brownfield-appropriate severity thresholds AND detects stale candidate artifacts. By default it is read-only — it prints a summary of blockers and stale-artifact WARNs, then exits 0 regardless of findings. `--promote` is the mutation path that flips `bootstrap_stage: bootstrapped → verified` on pages that pass the 5-gate list.

    ### Flags

    | Flag | Default | Effect |
    |------|---------|--------|
    | `--root DIR` | `.` | Vault root |
    | `--promote` | off | Flip `bootstrap_stage` on passing pages |
    | `--help` | — | Print help and exit 0 |

    ### Stale candidate artifact WARN

    Before running lint, verify compares each `.brownfield/*.yaml` candidate file's D-09 `source_script_hash:` header against the current body-post-op_hash-strip sha256 of the corresponding `.brownfield/migrations/*.sh` byte-copy. If they differ (meaning the byte-copy has been hand-edited or the canonical script has changed since suggest was last run), verify emits a stderr WARN:

    ```
    verify: stale candidate artifact detected: page-typing-candidates.yaml (source_script_hash in header sha256:abc... != current sha256:def... for migrations/01-page-typing.sh); re-run `bin/brownfield.sh suggest` to refresh.
    ```

    The WARN is non-blocking — verify exits 0 regardless. Re-run `bin/brownfield.sh suggest` to regenerate the candidate files against the current scripts.

    ### The 5-gate pass-list (D-14)

    A page is promoted to `bootstrap_stage: verified` iff ALL of:

    1. **Currently bootstrapped** — `bootstrap_stage: bootstrapped` (not already `verified` / `archived`).
    2. **Valid type:** — `type:` is one of `entity | concept | source | comparison | overview | decision` per §4.
    3. **Zero blocking lint findings** — no `severity: error` lint findings for this page path in the 5 checked categories (`yaml`, `provenance`, `orphan`, `crossref`, `brownfield`).
    4. **Type-specific required fields present** — e.g., `type: source` requires `path`, `content_hash`, `ingested_at`, `source_type`.
    5. **No pending review decision** — page is not listed in any cluster with `decision: pending` in `.brownfield/page-typing-decisions.yaml`.

    Privacy is NOT a gate — `bin/check-privacy.sh` handles the public-paths leak guard separately (Phase 9 D-15). The `--promote` run is the human sign-off for advisory work (privacy review especially): by running it, you assert you've examined advisory outputs.

    ### Performance

    RESEARCH Q9 budget: `verify --promote` completes in <20s on a 500-page vault. Bottleneck is the single lint subprocess invocation; gate evaluation itself is O(n) with O(1) per-page lookups.

    ## Lifecycle walkthrough

    The full happy-path after `bootstrap` ships:

    1. `bash bin/brownfield.sh suggest` — generate migration scripts + candidates.
    2. `bash bin/brownfield.sh review-typing` — resolve clusters (TTY or AI handoff).
    3. `bash .brownfield/migrations/01-page-typing.sh --apply` — commit typing decisions deterministically from paired immutable inputs (candidates.yaml + decisions.yaml).
    4. `bash .brownfield/migrations/02-provenance-bootstrap.sh --apply` — tag claim-like TOP-LEVEL TL;DR + Key Facts bullets (nested bullets untouched).
    5. `bash .brownfield/migrations/03-cross-link-inference.sh` — advisory cross-link report.
    6. `bash .brownfield/migrations/04-privacy-review.sh` — advisory privacy-sensitive findings report.
    7. `bash bin/brownfield.sh verify` — read-only lint check + stale-artifact WARN.
    8. `bash bin/brownfield.sh verify --promote` — flip `bootstrap_stage: verified` on pages passing all 5 gates.

    Each migration script appends one block to `.brownfield/applied.log` per meaningful execution (apply-class on `--apply` only; advisory-class on findings). Plain `--dry-run` does NOT append. See `schema/brownfield/migrations/README.md` for the per-script applied.log block shapes.

    ## Troubleshooting

    | Symptom | Cause | Fix |
    |---------|-------|-----|
    | `ImportError: No module named 'ruamel.yaml'` | ruamel.yaml not installed | `pip install ruamel.yaml` or set `PYTHONPATH=$HOME/.local/lib/python3/dist-packages` |
    | `suggest` warns "no bootstrapped pages found" | bootstrap hasn't run yet on this vault | Run `bash bin/brownfield.sh bootstrap --apply` first |
    | `01 --apply` skips everything | All clusters still `decision: pending` in decisions.yaml | Run `review-typing` to resolve, or manually edit decisions.yaml |
    | `01 --apply` errors "page-typing-candidates.yaml not found" | candidates.yaml deleted; 01 requires BOTH files as paired immutable inputs | Re-run `bash bin/brownfield.sh suggest` to regenerate both |
    | `02 --apply` emits `WARN: N bootstrapped pages still have empty type:...` | Soft D-12 prereq — most pages lack `type:` | Expected on fresh-bootstrap vault; proceeds anyway. Running 01 --apply first produces better results. |
    | `02 --apply` skipped nested bullets | By design (item 5) — 02 only tags TOP-LEVEL bullets under TL;DR + Key Facts | Nested bullets are intentionally untouched. Flatten to top-level if you want them tagged. |
    | `review-typing` hangs on piped stdin | Should not happen on current build | Check you're on post-Plan-11-04 code (EOF handling added per review item 4). Piping `</dev/null` aborts the session cleanly. |
    | `review-typing` rejects override label | Label must be one of `entity | concept | source | comparison | overview | decision` — validated at entry time per review item 11 | Retry with a valid label. |
    | `verify` WARNs `stale candidate artifact detected` | `source_script_hash` in candidate header drifted from current migration script body | Re-run `bash bin/brownfield.sh suggest` to refresh byte-copies and metadata headers. Non-blocking — you can still run --promote if you're confident the change is safe. |
    | `verify --promote` blocks most pages | Usually Gate 2 (unset type:) or Gate 5 (pending clusters) | Inspect the `[blocked] <path> — <reason>` list; fix the root cause and re-run. |
    | `applied.log` has no block after a migration ran | Migration invoked in `--dry-run` (the default) | Re-run with `--apply` for apply-class; advisory-class appends only when findings exist. |
    | `.brownfield/` contents look stale after code changes | Your schema/brownfield/migrations/*.sh changed after last suggest; verify will WARN on this | Re-run `suggest` to refresh byte-copies and regenerate op_hash headers. |

    ## Rollback

    (Preserve existing `## Rollback` section from Phase 10 BRWN-19 — it documents `git reset --hard <sha>` as the canonical undo.)
    ```

    ### Step 2 — Preserve existing `## scan subcommand`, `## bootstrap subcommand`, `## Fixture testing environment variables`, `## Rollback` sections UNTOUCHED.

    ## 2b. .planning/REQUIREMENTS.md amendments

    ### Step 1 — Amend BRWN-12 wording (line ~97)

    Replace:
    ```
    - [ ] **BRWN-12**: Four staged migration script classes: `01-page-typing.sh`, `02-provenance-bootstrap.sh`, `03-cross-link-inference.sh`, `04-privacy-classification.sh`
    ```

    With:
    ```
    - [ ] **BRWN-12**: Four staged migration script classes: `01-page-typing.sh`, `02-provenance-bootstrap.sh`, `03-cross-link-inference.sh`, `04-privacy-review.sh` (renamed from `04-privacy-classification.sh` in Phase 11 per D-07 — fail-closed `privacy: local_only` preserved; script is advisory-only and never flips `privacy:` frontmatter).
    ```

    ### Step 2 — Add BRWN-22 (new requirement) after BRWN-21 (line ~106)

    Insert a new line after BRWN-21:
    ```
    - [ ] **BRWN-22**: `bin/brownfield.sh review-typing` orchestrator resolves pending page-typing clusters with two modes: **small-batch TTY** (< threshold, default 20, and stdout is a TTY) with cluster primitives `approve all / reject all / inspect / override / skip`; **large-batch AI handoff** (≥ threshold OR non-TTY) via a static `.brownfield/review-typing-prompt.md` artifact consumed OUTSIDE the CLI. Both modes write back to `.brownfield/page-typing-decisions.yaml` via ruamel.yaml round-trip (preserves user comments). Small-batch prompt loop handles stdin EOF cleanly (aborts session without hanging). Override labels are validated against the `type:` enum at entry time. No LLM calls inside `bin/brownfield.sh` (BRWN-16 hard-lock).
    ```

    ### Step 3 — Flip traceability table (lines ~228-237)

    For each BRWN-11..20 row, change `Pending` to `Complete`. Add a new row for BRWN-22: `| BRWN-22 | Phase 11 | Complete |`.

    ### Step 4 — Update Coverage summary (lines 243-244)

    Replace:
    ```
    v1.1 requirements: 78 total (TMPL: 11, NEUT: 8, WZRD: 11, MANUAL: 6, COLAB: 8, CI: 9, BRWN: 21, DEBT: 4)
    Mapped to phases: 78 (Phase 7: 20, Phase 8: 17, Phase 9: 17, Phase 10: 11, Phase 11: 10, Phase 12: 3)
    ```

    With:
    ```
    v1.1 requirements: 79 total (TMPL: 11, NEUT: 8, WZRD: 11, MANUAL: 6, COLAB: 8, CI: 9, BRWN: 22, DEBT: 4)
    Mapped to phases: 79 (Phase 7: 20, Phase 8: 17, Phase 9: 17, Phase 10: 11, Phase 11: 11, Phase 12: 3)
    ```

    ## 2c. wiki/decisions/dr-2026-04-20-brownfield-apply-vs-advisory.md — Tier-1 DR

    Create the file with frontmatter per the `<interfaces>` block above + the following body:

    ```markdown
    ## TL;DR

    Phase 11 introduces the brownfield workflow's apply-vs-advisory architectural split, the review-manifest pattern for 01-page-typing, and the bootstrap_stage lifecycle gate via `verify --promote`. Apply-class scripts (01, 02) mutate vault pages deterministically; advisory-class scripts (03, 04) produce reports only. The review-manifest pattern isolates page-typing judgment (AI-guided or TTY) from deterministic apply, preserving BRWN-16's mechanical-tool boundary. Cross-AI review (Gemini + Codex) hardened the initial design with root-resolution safety (item 1), paired-immutable-inputs contract for 01 (item 2), shared `.brownfield-ignore` walker with scan (item 3), EOF-safe prompt loop in review-typing (item 4), top-level-bullets-only regex in 02 (item 5), aggregator per-plan gate split (item 6), widened D-03 auto-approve (item 7), Python hashlib replacing shell sha256sum (item 8), operational D-09 enforcement via stale-artifact WARN in verify (item 9), documented per-script applied.log variance (item 10), and override-label validation (item 11).

    ## Decision

    Adopt the following architecture for Phase 11 brownfield migration:

    1. **Apply class:** `01-page-typing.sh`, `02-provenance-bootstrap.sh`. Real `--apply` path; mutate frontmatter / body text deterministically.
    2. **Advisory class:** `03-cross-link-inference.sh`, `04-privacy-review.sh`. Report-only; never mutate pages; `--apply` is blocked.
    3. **Review-manifest pattern (01-page-typing only):** suggest emits `candidates.yaml` + `decisions.yaml` (pending); review-typing (TTY or AI-handoff) edits decisions; `01 --apply` reads both deterministically as PAIRED IMMUTABLE INPUTS (item 2 contract) — candidates.yaml provides cluster-membership lookup, decisions.yaml provides policy. Apply never re-classifies.
    4. **`bootstrap_stage` lifecycle gate:** `verify --promote` flips `bootstrapped → verified` via a 5-gate per-page pass-list (D-14). `verified` is manual-edit-only after that (no automatic downgrade).
    5. **Review-feedback hardenings (items 1–11 from 11-REVIEWS.md):** implementation details of the above, documented in Consequences below.

    Design principle: **Review may be interactive and AI-guided; apply must always be deterministic.**

    ## Why

    The replaced framing was "generate one `apply` chain-runner that does everything" (v1.0 imagination, captured in v2 backlog as BRWNAPPLY-01). The adopted framing separates mechanical work (deterministic, idempotent, testable via byte-equality fixtures) from judgment work (clustering decisions, cross-link inference, privacy classification) that genuinely requires human or AI-assisted interpretation.

    The review-manifest pattern exists because page-typing is the highest-leverage judgment call in the workflow: getting `type:` right affects lint, verify, and all downstream operations. Yet it is also the most cluster-friendly (PascalCase entities tend to cluster; kebab-case concepts tend to cluster). A review manifest captures both the mechanical cluster output AND the human policy decision in one file that the deterministic apply script can then consume without re-interpreting.

    Why 02/03/04 do NOT adopt the review-manifest pattern:
    - **02-provenance-bootstrap:** surface is narrow (top-level bullets under TL;DR + Key Facts per review item 5); eligibility rules are mechanical (8 skip classes per D-05); no manifest needed.
    - **03-cross-link-inference:** advisory-only in v1.1; manifest-backed apply deferred to future phase (see Deferred list). First-mention-only rule per AGENTS.md §8 needs per-user review; judgment-heavy.
    - **04-privacy-review:** advisory-only in v1.1; fail-closed `privacy: local_only` is preserved per AGENTS.md §13; no mutation path at all. Future: user-authored `.brownfield-privacy-cloudsafe.txt` allowlist as explicit human policy.

    ## Alternatives Considered

    1. **Auto-apply all migration scripts in a single `brownfield apply` command.** Rejected: judgment errors at scale silently corrupt the wiki; `git reset --hard` is too-blunt recovery for a 500-page vault. Deferred to v1.2 BRWNAPPLY-01 after the per-class dry-run path is battle-tested.

    2. **Per-page prompts for typing (no clustering).** Rejected: 500-page vaults produce 500 prompts; cognitive load makes the workflow unusable. Clustering by signal tuple (`cluster_by_signals` per RESEARCH Q1) reduces typical 500-page decisions to 20-50 cluster decisions.

    3. **Scanner-driven privacy promotion (04-privacy-review flips `privacy:` frontmatter).** Rejected: violates AGENTS.md §13 fail-closed semantics. Only a human may downgrade `local_only → cloud_safe`; regex-driven classification would produce false positives that silently promote sensitive content.

    4. **Embedded vault-specific logic in migration scripts (one-off rendered scripts per vault).** Rejected: violates byte-equality CI + git reviewability; re-running `suggest` on a changed vault would produce different script contents. Hybrid model (canonical scripts byte-copied; vault-specific data in YAML candidate files) keeps logic versioned and state ephemeral.

    5. **Interactive TUI with arrow keys + previews.** Rejected per FEATURES.md §Bucket 5 anti-feature list. CLI + manifest hits the same workflow need with less code and better CI scriptability.

    6. **Default `BROWNFIELD_ROOT=$(pwd)` for migration scripts.** Rejected (review item 1): cross-tree mutation risk when scripts are invoked via absolute path from unrelated cwd. Adopted: resolve from script's `.brownfield/` parent.

    7. **01-page-typing reads only decisions.yaml (candidates.yaml unused at apply time).** Rejected (review item 2): candidates.yaml provides load-bearing cluster-membership lookup. Adopted: paired immutable inputs.

    8. **Fork scan's walker for suggest.** Rejected (review item 3): drift risk + simplified-walker bug (would sweep docs/, schema/, etc. into candidates). Adopted: shared `bin/lib/brownfield_walk.py` called by both.

    9. **Shell `sha256sum` for input hashing.** Rejected (review item 8): macOS portability gap (not in POSIX; requires coreutils install). Adopted: Python `hashlib` via heredoc.

    10. **D-09 metadata decorative.** Rejected (review item 9): audit story is weaker if stale artifacts go undetected. Adopted: verify emits stale-artifact WARN when `source_script_hash` drifts. Non-blocking.

    11. **Unified applied.log schema across all four scripts.** Rejected (review item 10): 02's direct-apply has no candidate inputs to hash; forcing a unified schema would require a fake placeholder. Adopted: per-script variance, documented in `schema/brownfield/migrations/README.md` and AGENTS.md §11.5.4.

    ## Consequences

    - AGENTS.md §11.5 Brownfield Workflow is the new authoritative contract; §11.5 Release Workflow renumbered to §11.6 (opportunistic fix of Phase 10 WR-03 forward-ref typo).
    - `schema/brownfield/migrations/` becomes a new tracked directory of canonical migration scripts; byte-equality CI-enforced via `tests/phase-11/test_canonical_byte_equality.sh`.
    - `.brownfield/` gains the candidate/decisions/applied-log artifacts (all gitignored per TMPL-04).
    - `bootstrap_stage` gains the `verified` lifecycle state; `verify --promote` is the ONLY v1.1 writer of that state.
    - REQUIREMENTS.md BRWN-12 wording amended (04-privacy-classification → 04-privacy-review); new BRWN-22 covers `review-typing` (including items 4 + 11).
    - v1.2 roadmap entries: BRWNAPPLY-01 (chain-runner), manifest-backed apply for 03/04, `.brownfield/02-exclude.txt` page-level opt-out.

    **Review-feedback hardenings (implementation details of the above architecture):**

    - **Item 1 — Root resolution:** all four migration scripts derive `BROWNFIELD_ROOT` from the script's `.brownfield/` parent via `${BASH_SOURCE[0]}` resolution, not from `$(pwd)`. Prevents cross-tree mutation. Locked by `test_01_root_resolution.sh`.
    - **Item 2 — Paired immutable inputs:** 01-page-typing.sh requires BOTH candidates.yaml + decisions.yaml at apply time. Neither alone is sufficient. Locked by `test_01_paired_immutable_inputs.sh` + `test_01_apply_reads_decisions_only.sh`.
    - **Item 3 — Shared `.brownfield-ignore` walker:** scan + suggest call `bin/lib/brownfield_walk.py walk_vault_respecting_ignore()`; no fork. Locked by `test_suggest_respects_brownfield_ignore.sh` + Phase 10 regression suite staying green.
    - **Item 4 — EOF-safe review-typing loop:** `_EOF_SENTINEL` + `_MAX_REPROMPTS_PER_CLUSTER=5` prevent hangs; piped `</dev/null` aborts cleanly. Locked by `test_review_typing_eof_handling.sh` (`timeout 3` guard).
    - **Item 5 — Top-level bullets only:** `BULLET_TOP_LEVEL_RE = r'^-(?: |\\t)(.+)$'` rejects any leading whitespace. Locked by `test_02_top_level_bullets_only.sh` + `nested-bullets-vault` fixture.
    - **Item 6 — Per-plan gate split:** `tests/phase-11/run.sh --expected-by <plan>` filters test subset via `# EXPECTED_BY: <plan-id>` test-file tag. Each downstream plan asserts only its target subset.
    - **Item 7 — D-03 auto-approve widened:** `cluster_is_autoapproveable()` returns True for `confidence=high` + (explicit frontmatter type OR 3+ non-frontmatter signals agree). Locked by `test_01_highconf_multisignal_autoapprove.sh`.
    - **Item 8 — Python hashlib:** zero shell `sha256sum` in migration scripts or suggest branch. Locked by `test_hashlib_not_sha256sum.sh`.
    - **Item 9 — Operational D-09 enforcement:** `verify` WARNs on stale candidate artifacts (`source_script_hash` drift). Non-blocking. Locked by `test_verify_stale_artifact_warn.sh`.
    - **Item 10 — Documented per-script applied.log variance:** `schema/brownfield/migrations/README.md` + AGENTS.md §11.5.4 enumerate per-script block shapes. Locked by split `test_applied_log_apply_schema.sh` assertions.
    - **Item 11 — Override-label validation:** review-typing validates against `VALID_ENUMS['type']` at entry; invalid labels rejected before write. Locked by `test_review_typing_validates_override_label.sh`.

    ## Affected Pages

    The following artifacts are created or substantially modified by Phase 11 to implement this decision:

    - [[AGENTS.md]] §11.5 Brownfield Workflow (new authoritative contract, incorporating items 1, 2, 9, 10 anchors)
    - [[AGENTS.md]] §11.6 Release Workflow (renumbered from §11.5 — Option C per RESEARCH Pitfall 1)
    - CLAUDE.md (byte-sync mirror of AGENTS.md via `.githooks/pre-commit`)
    - schema/AGENTS.template.md (template mirror of §11.5 + §11.6)
    - schema/fixtures/canonical-AGENTS.md (regenerated canonical fixture)
    - docs/reference/brownfield.md (operator runbook: suggest + review-typing + verify sections populated; troubleshooting table includes item-4/item-9/item-11 entries)
    - schema/brownfield/migrations/01-page-typing.sh (new apply-class canonical script; paired inputs + root resolution)
    - schema/brownfield/migrations/02-provenance-bootstrap.sh (new apply-class; top-level-only regex + direct-apply applied.log variance)
    - schema/brownfield/migrations/03-cross-link-inference.sh (new advisory-class; root resolution)
    - schema/brownfield/migrations/04-privacy-review.sh (new advisory-class; root resolution)
    - schema/brownfield/migrations/README.md (per-script applied.log variance schema per item 10)
    - bin/brownfield.sh (new subcommands: suggest, review-typing, verify; stale-artifact WARN in verify; EOF-safe prompt loop in review-typing)
    - bin/lib/brownfield_classify.py (new `cluster_by_signals()` + `cluster_is_autoapproveable()`)
    - bin/lib/brownfield_provenance.py (new module for 02 eligibility heuristics; top-level-only regex)
    - bin/lib/brownfield_walk.py (new shared walker per item 3 — consumed by both scan and suggest)
    - .planning/REQUIREMENTS.md (BRWN-12 rename; BRWN-22 added with EOF + label-validation wording; traceability updated)

    ## Sources

    - .planning/phases/11-brownfield-suggest-verify/11-CONTEXT.md (21 locked decisions D-01..D-21)
    - .planning/phases/11-brownfield-suggest-verify/11-RESEARCH.md (Q1–Q11 research answers; Pitfall 1 renumbering analysis)
    - .planning/phases/11-brownfield-suggest-verify/11-PATTERNS.md (analog pattern map)
    - .planning/phases/11-brownfield-suggest-verify/11-REVIEWS.md (cross-AI review — Gemini + Codex; items 1–15 — HIGH 1-4, MEDIUM 5-10, LOW 11-15)
    - .planning/research/FEATURES.md §Bucket 5 (anti-feature list)
    - .planning/research/PITFALLS.md §C-2, §C-3 (frontmatter corruption + idempotency)
    ```

    ## 2d. wiki/index.md — add DR to Decisions section

    Find the `## Decisions` section. Add a new entry:
    ```
    - [[Brownfield Apply-vs-Advisory Architecture + Review-Manifest Pattern]] — Captures the apply-class vs advisory-class split, review-manifest pattern, bootstrap_stage lifecycle gate, and cross-AI review-feedback hardenings (items 1–11) (sourced, 2026-04-20)
    ```

    Use the DR's `title` field as the wikilink target per AGENTS.md §8 exact-title rule. Obsidian resolves via aliases; the DR ID is the frontmatter `id` field.

    ## 2e. wiki/log.md — append reflect entry at EOF

    Append to EOF (newest-at-bottom per AGENTS.md §12):

    ```markdown

    ## [2026-04-20] reflect | Phase 11 brownfield apply-vs-advisory architecture + review-feedback hardenings

    Structural reasoning captured in Tier-1 decision record [[Brownfield Apply-vs-Advisory Architecture + Review-Manifest Pattern]] (`dr-2026-04-20-brownfield-apply-vs-advisory`). Documents the apply-class vs advisory-class split (D-01), review-manifest pattern for 01-page-typing (D-02, D-04), bootstrap_stage lifecycle gate via verify --promote (D-13, D-14, D-15), and review-feedback hardenings: root resolution (item 1), paired immutable inputs (item 2), shared walker (item 3), EOF-safe review-typing (item 4), top-level-bullets-only regex (item 5), aggregator per-plan gate split (item 6), widened D-03 auto-approve (item 7), hashlib portability (item 8), operational D-09 enforcement (item 9), per-script applied.log variance (item 10), override-label validation (item 11). Alternatives rejected (chain-runner, per-page prompts, scanner-driven privacy promotion, $(pwd) root default, decisions-only-without-candidates, shell sha256sum, decorative D-09 metadata, unified applied.log schema).
    ```

    Staging note: since the DR has `privacy: cloud_safe` and lives in the wiki layer, ensure it does NOT contain any local_only content. The DR is a schema-architecture record — safe for public.
  </action>
  <verify>
    <automated>
      # docs populated
      awk '/^## suggest subcommand/,/^## /' docs/reference/brownfield.md | wc -l | awk '$1 >= 20 {exit 0} {exit 1}' &&
      awk '/^## review-typing subcommand/,/^## /' docs/reference/brownfield.md | wc -l | awk '$1 >= 20 {exit 0} {exit 1}' &&
      awk '/^## verify subcommand/,/^## /' docs/reference/brownfield.md | wc -l | awk '$1 >= 20 {exit 0} {exit 1}' &&
      grep -q 'mechanical' docs/reference/brownfield.md &&
      grep -q 'judgment' docs/reference/brownfield.md &&
      grep -q '5-gate' docs/reference/brownfield.md &&
      grep -q 'git reset --hard' docs/reference/brownfield.md &&
      # New troubleshooting anchors per items 4, 9, 11
      grep -q 'hangs on piped stdin' docs/reference/brownfield.md &&
      grep -q 'stale candidate artifact detected' docs/reference/brownfield.md &&
      grep -q 'rejects override label' docs/reference/brownfield.md &&
      # REQUIREMENTS.md amendments
      grep -q '04-privacy-review.sh' .planning/REQUIREMENTS.md &&
      grep -q '**BRWN-22**' .planning/REQUIREMENTS.md &&
      grep -q 'stdin EOF cleanly' .planning/REQUIREMENTS.md &&
      grep -q 'validated against the' .planning/REQUIREMENTS.md &&
      grep -q '| BRWN-22 | Phase 11 |' .planning/REQUIREMENTS.md &&
      # DR exists + valid frontmatter
      test -f wiki/decisions/dr-2026-04-20-brownfield-apply-vs-advisory.md &&
      grep -q '^type: decision' wiki/decisions/dr-2026-04-20-brownfield-apply-vs-advisory.md &&
      grep -q '^trigger_type: schema-update' wiki/decisions/dr-2026-04-20-brownfield-apply-vs-advisory.md &&
      grep -q '^## TL;DR' wiki/decisions/dr-2026-04-20-brownfield-apply-vs-advisory.md &&
      grep -q '^## Decision' wiki/decisions/dr-2026-04-20-brownfield-apply-vs-advisory.md &&
      grep -q '^## Why' wiki/decisions/dr-2026-04-20-brownfield-apply-vs-advisory.md &&
      grep -q '^## Alternatives Considered' wiki/decisions/dr-2026-04-20-brownfield-apply-vs-advisory.md &&
      grep -q '^## Consequences' wiki/decisions/dr-2026-04-20-brownfield-apply-vs-advisory.md &&
      grep -q '^## Affected Pages' wiki/decisions/dr-2026-04-20-brownfield-apply-vs-advisory.md &&
      grep -q '^## Sources' wiki/decisions/dr-2026-04-20-brownfield-apply-vs-advisory.md &&
      # DR documents all 11 review-feedback items in Consequences
      grep -q 'Item 1' wiki/decisions/dr-2026-04-20-brownfield-apply-vs-advisory.md &&
      grep -q 'Item 11' wiki/decisions/dr-2026-04-20-brownfield-apply-vs-advisory.md &&
      grep -q 'dr-2026-04-20-brownfield-apply-vs-advisory\|Brownfield Apply-vs-Advisory' wiki/index.md &&
      tail -20 wiki/log.md | grep -q '\[2026-04-20\] reflect | Phase 11' &&
      # Phase 11 test aggregator fully green (unfiltered)
      bash tests/phase-11/run.sh 2>&1 | tail -5 | grep -qE 'PHASE 11 TESTS: [0-9]+/[0-9]+$' &&
      (bash tests/phase-11/run.sh 2>&1 | tail -1 | awk -F'[/ ]' '{exit !($3 == $4)}')
    </automated>
  </verify>
  <acceptance_criteria>
    - `docs/reference/brownfield.md` has `## suggest subcommand` section ≥ 20 lines (populated, not stub).
    - `docs/reference/brownfield.md` has `## review-typing subcommand` section ≥ 20 lines.
    - `docs/reference/brownfield.md` has `## verify subcommand` section ≥ 20 lines mentioning `--promote` and 5-gate pass-list AND stale-artifact WARN.
    - Troubleshooting table includes rows for "hangs on piped stdin" (item 4), "stale candidate artifact detected" (item 9), "rejects override label" (item 11).
    - Existing scan/bootstrap/Rollback sections preserved.
    - `.planning/REQUIREMENTS.md` BRWN-12 uses `04-privacy-review.sh`; BRWN-22 added mentioning EOF + label validation; traceability table has BRWN-11..20 + BRWN-22 as Complete; coverage summary shows 79 total.
    - Tier-1 DR exists with valid frontmatter: `type: decision`, `trigger_type: schema-update`, `status: active`, `epistemic_status: sourced`, `has_contradictions: false`.
    - DR has all 7 required sections per AGENTS.md §4.6.
    - DR content mentions D-01 design principle verbatim, lists at least 5 alternatives with rejection rationale (original 5 + new alternatives 6-11 for review-feedback decisions).
    - DR's Consequences section enumerates all 11 review-feedback items with their locking tests.
    - `wiki/index.md` Decisions section lists the new DR wikilink.
    - `wiki/log.md` EOF has a `reflect` entry with header `## [2026-04-20] reflect | Phase 11 brownfield apply-vs-advisory architecture + review-feedback hardenings` — mentions items 1-11.
    - `bash tests/phase-11/run.sh` exits 0 and emits `PHASE 11 TESTS: N/N` (fully GREEN — unfiltered).
    - `bash tests/phase-11/run.sh --expected-by 11-05` reports pass == total.
    - All prior-phase aggregators still green: phase-07 (22/22), phase-08 (21/21), phase-09 (28/28), phase-09.1 (11/11), phase-10 (32/32).
  </acceptance_criteria>
  <done>
    Docs populated; REQUIREMENTS amended with BRWN-12 rename + BRWN-22 addition (item 4 + 11 wording); Tier-1 DR committed with review-feedback hardenings in Consequences; wiki index/log updated; all Phase 11 tests GREEN unfiltered; no regression across phases 7-10.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AGENTS.md ↔ schema/AGENTS.template.md | Phase 9.1 parity test enforces byte-equivalence modulo `{{...}}` placeholders (§11.5 + §11.6 have none) |
| AGENTS.md ↔ CLAUDE.md | `.githooks/pre-commit` auto-syncs; Phase 9.1 R-3 recommends pre-stage via `bash bin/sync-claude.sh && git add CLAUDE.md` |
| schema/AGENTS.template.md ↔ schema/fixtures/canonical-AGENTS.md | Phase 8-01 regeneration recipe; pinned env vars for determinism |
| DR page privacy: cloud_safe | Tier-1 DR is cloud_safe (schema-level record, no vault-specific data); contains no PII or local_only content |
| docs/reference/brownfield.md ↔ examples | Safe to cite `@example.com` and fake SSNs in troubleshooting table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | Tampering | AGENTS.md section renumber breaks external references | mitigate | Option C renumber chosen precisely because RESEARCH confirmed only `docs/reference/release.md` references §11.5 Release (links to the docs page, not §11.5 directly). |
| T-11-05-02 | Tampering | Template-parity drift (AGENTS.md edited but schema/AGENTS.template.md not mirrored) | mitigate | Phase 9.1 `tests/phase-09.1/test_template_parity.sh` asserts byte-equivalence. |
| T-11-05-03 | Tampering | canonical-AGENTS.md stale after template edit | mitigate | Phase 8-01 `tests/phase-08/test_canonical_byte_equality.sh` asserts. |
| T-11-05-04 | Data Protection | DR leaks local_only content | mitigate | DR `privacy: cloud_safe`; content is schema-architecture only. |
| T-11-05-05 | Information Disclosure | Tier-1 DR body mentions real vault paths | accept | All path references are either repo-relative or generic (`/home/user/vault`). |
| T-11-05-06 | Repudiation | REQUIREMENTS.md traceability flip without verification | mitigate | Plan 11-05 verify steps include running the phase-11 test aggregator BOTH filtered (`--expected-by 11-05`) AND unfiltered; traceability flip happens AFTER all tests GREEN. |
| T-11-05-07 | V1 Architecture | DR documents an architecture not actually implemented | mitigate | DR is committed LAST in the phase; its `affected_pages` section enumerates artifacts that are all present and tested at the time of commit. Consequences section cross-references each item (1-11) with its locking test file. |
| T-11-05-08 | Input Validation | wiki/log.md entry with wrong date ordering | mitigate | AGENTS.md §12 convention: newest at bottom, append-only. |
| T-11-05-09 | V11 Business Logic | wiki/index.md DR entry missing or malformed | mitigate | Test `test_agents_section_11_5.sh` and DR-specific tests would catch if the DR's `[[Title]]` wikilink does not resolve. |
| T-11-05-10 | V5 Input Validation | Tier-1 DR frontmatter validation | mitigate | DR uses valid enum values (trigger_type: schema-update; type: decision; status: active; epistemic_status: sourced; privacy: cloud_safe); all required base fields per AGENTS.md §5 validation checklist present; grep-verified. |
</threat_model>

<verification>
- `bash tests/phase-11/run.sh --expected-by 11-05` — reports pass == total for the 11-05 subset.
- `bash tests/phase-11/run.sh` (unfiltered) — exits 0; emits `PHASE 11 TESTS: N/N` with pass = total.
- `bash tests/phase-10/run.sh` — `PHASE 10 TESTS: 32/32` (non-regression).
- `bash tests/phase-09.1/run.sh` — `PHASE 09.1 TESTS: 11/11` (template parity + canonical fixture).
- `bash tests/phase-09/run.sh` — `PHASE 09 TESTS: 28/28`.
- `bash tests/phase-08/run.sh` — `PHASE 08 TESTS: 21/21`.
- `bash tests/phase-07/run.sh` — `PHASE 07 TESTS: 22/22`.
- `cmp -s AGENTS.md CLAUDE.md` (0 exit).
- `.planning/REQUIREMENTS.md` shows BRWN-11..20 + BRWN-22 as Complete.
- Tier-1 DR exists with valid schema and documents items 1-11 in Consequences.
- `wiki/index.md` Decisions lists the new DR.
- `wiki/log.md` EOF has the reflect entry mentioning items 1-11.
- No security/lint regressions: `bash bin/lint.sh --ci --format json --category yaml,provenance,orphan,crossref,brownfield` returns zero error-severity findings.
</verification>

<success_criteria>
- All Plan 11-01 RED tests (~38) now GREEN (unfiltered AND every `--expected-by <plan>` subset).
- Phases 7-10 test aggregators still GREEN.
- AGENTS.md §11.5 authoritatively documents the brownfield workflow contract INCLUDING the item-2/1/9/10 review-feedback anchors.
- docs/reference/brownfield.md is the operator runbook (suggest + review-typing + verify sections populated, lifecycle walkthrough, troubleshooting with item-4/9/11 entries).
- REQUIREMENTS.md reflects BRWN-12 rename + BRWN-22 addition (with items 4 + 11 wording); traceability table flipped; coverage = 79.
- Tier-1 DR committed captures the apply-vs-advisory + review-manifest + lifecycle decisions + review-feedback hardenings.
- Phase 10 WR-03 (§5 bootstrap_stage forward-ref typo) closed opportunistically via Option C renumber.
- All ten BRWN-11..20 REQ-IDs for Phase 11 traced through plans + tests + docs.
</success_criteria>

<output>
After completion, create `.planning/phases/11-brownfield-suggest-verify/11-05-SUMMARY.md` capturing:
- Final Phase 11 test tally: `PHASE 11 TESTS: N/N` unfiltered AND `PHASE 11 TESTS: N/N (expected-by 11-05)`.
- Non-regression confirmation: tests/phase-07 through tests/phase-10 aggregators all green.
- WR-03 closure verification (`grep -c '§11.5 Brownfield Workflow' AGENTS.md` ≥ 1, `grep -c '§11.5 Release Workflow' AGENTS.md` == 0).
- BRWN-12 amendment diff (before/after one-line summary).
- BRWN-22 wording (as committed) — includes item 4 + item 11 anchors.
- Tier-1 DR ID + affected-pages enumeration (for audit) — confirm items 1-11 documented in Consequences.
- CLAUDE.md sync confirmation (`cmp -s AGENTS.md CLAUDE.md` exit 0).
- Any discovered drift or fix-ups (e.g., Phase 10 WR-01/WR-02 — note whether opportunistically fixed or deferred).
- Trigger the final phase-11 VERIFICATION workflow (/gsd-verify-phase) to cut `.planning/phases/11-brownfield-suggest-verify/11-VERIFICATION.md` recording REQ-ID status.
</output>
