---
phase: 11-brownfield-suggest-verify
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase-11/run.sh
  - tests/phase-11/lib.sh
  - tests/phase-11/fixtures/small-vault-ambiguous/
  - tests/phase-11/fixtures/large-vault-ambiguous/
  - tests/phase-11/fixtures/pre-typed-vault/
  - tests/phase-11/fixtures/privacy-sensitive-vault/
  - tests/phase-11/fixtures/already-tagged-vault/
  - tests/phase-11/fixtures/README.md
  - tests/phase-11/test_migration_script_names.sh
  - tests/phase-11/test_suggest_byte_copies_migrations.sh
  - tests/phase-11/test_suggest_candidate_metadata_header.sh
  - tests/phase-11/test_canonical_byte_equality.sh
  - tests/phase-11/test_op_hash_stable.sh
  - tests/phase-11/test_op_hash_header_shape.sh
  - tests/phase-11/test_applied_log_apply_schema.sh
  - tests/phase-11/test_applied_log_advisory_schema.sh
  - tests/phase-11/test_applied_log_dryrun_no_append.sh
  - tests/phase-11/test_01_dryrun_default.sh
  - tests/phase-11/test_01_apply_reads_decisions_only.sh
  - tests/phase-11/test_01_idempotent.sh
  - tests/phase-11/test_02_dryrun_default.sh
  - tests/phase-11/test_02_apply_eligible_bullets.sh
  - tests/phase-11/test_02_no_magic_strings.sh
  - tests/phase-11/test_02_skips_already_tagged.sh
  - tests/phase-11/test_02_honest_no_eligible.sh
  - tests/phase-11/test_02_soft_prereq_warn.sh
  - tests/phase-11/test_02_idempotent.sh
  - tests/phase-11/test_03_advisory_only.sh
  - tests/phase-11/test_04_advisory_only.sh
  - tests/phase-11/test_04_never_flips_privacy.sh
  - tests/phase-11/test_review_typing_tty_small.sh
  - tests/phase-11/test_review_typing_ai_handoff.sh
  - tests/phase-11/test_review_typing_decisions_roundtrip.sh
  - tests/phase-11/test_verify_lint_wrapper.sh
  - tests/phase-11/test_verify_readonly_default.sh
  - tests/phase-11/test_verify_promote_5_gates.sh
  - tests/phase-11/test_verify_promote_gate5_pending.sh
  - tests/phase-11/test_no_llm_calls.sh
  - tests/phase-11/test_agents_section_11_5.sh
  - tests/phase-11/test_agents_template_parity_11_5.sh
  - tests/phase-11/test_canonical_agents_byte_equality.sh
  - tests/phase-11/test_docs_mechanical_judgment.sh
  - tests/phase-11/test_docs_suggest_section.sh
  - tests/phase-11/test_docs_review_typing_section.sh
  - tests/phase-11/test_docs_verify_section.sh
  - tests/phase-11/test_end_to_end_happy_path.sh
  - schema/brownfield/migrations/01-page-typing.sh
  - schema/brownfield/migrations/02-provenance-bootstrap.sh
  - schema/brownfield/migrations/03-cross-link-inference.sh
  - schema/brownfield/migrations/04-privacy-review.sh
  - schema/brownfield/migrations/README.md
autonomous: true
requirements:
  - BRWN-11
  - BRWN-12
  - BRWN-13
  - BRWN-14
  - BRWN-15
  - BRWN-16
  - BRWN-17
  - BRWN-18
  - BRWN-19
  - BRWN-20

must_haves:
  truths:
    - "Wave-0 test aggregator `tests/phase-11/run.sh` runs and emits `PHASE 11 TESTS: K/N` with N ≈ 30 RED tests covering the full D-19 contract suite."
    - "Five fixtures exist per D-20 (small-vault-ambiguous, large-vault-ambiguous, pre-typed-vault, privacy-sensitive-vault, already-tagged-vault) with byte-frozen content under `fixtures/<name>/input/` and golden `fixtures/<name>/expected/` directories."
    - "Four canonical migration script skeletons exist at `schema/brownfield/migrations/*.sh` with stable byte-equality anchors (valid bash, `--help` exit 0, contract phrases present) but NO implementation body — implementation lands in Plan 11-03."
    - "Four script filenames are exactly `01-page-typing.sh`, `02-provenance-bootstrap.sh`, `03-cross-link-inference.sh`, `04-privacy-review.sh` (D-07 rename locked; test asserts no `04-privacy-classification.sh` anywhere in `schema/brownfield/migrations/`)."
    - "Every RED test in the list fails today with a diagnostic message that names the missing capability (not a test-infra crash) — `bash tests/phase-11/run.sh` exits non-zero showing RED count, enabling GREEN progression in Plans 11-02/03/04/05."
    - "The end-to-end happy-path test file exists and is RED until Plans 11-02..11-04 land (it asserts final vault state matches `fixtures/end-to-end-golden/`)."
  artifacts:
    - path: tests/phase-11/run.sh
      provides: Phase 11 test aggregator (clone of Phase 10 run.sh with 10→11 rename)
      contains: "PHASE 11 TESTS"
    - path: tests/phase-11/lib.sh
      provides: Shared test helpers (make_fixture_repo, assert_byte_equal, assert_exit_code, assert_file_exists, assert_grep, assert_canonical_scripts_byte_identical)
      contains: "make_fixture_repo"
    - path: tests/phase-11/fixtures/README.md
      provides: Fixture-layout contract + regeneration recipes per D-20
      min_lines: 40
    - path: schema/brownfield/migrations/01-page-typing.sh
      provides: Canonical 01-page-typing skeleton (--help exits 0, signals contract phrase + class)
      contains: "Review may be interactive and AI-guided; apply must always be deterministic"
    - path: schema/brownfield/migrations/02-provenance-bootstrap.sh
      provides: Canonical 02 skeleton with contract phrase + WARN phrasing anchors
      contains: "Marks top-level bullets under"
    - path: schema/brownfield/migrations/03-cross-link-inference.sh
      provides: Canonical 03 skeleton (advisory-only signaled in --help)
      contains: "advisory-only"
    - path: schema/brownfield/migrations/04-privacy-review.sh
      provides: Canonical 04 skeleton with D-07 contract phrase
      contains: "04-privacy-review classifies findings for review priority, not for frontmatter mutation"
    - path: schema/brownfield/migrations/README.md
      provides: Directory-scope README documenting the four scripts + op_hash policy
      min_lines: 20
    - path: tests/phase-11/test_end_to_end_happy_path.sh
      provides: End-to-end happy-path RED test (suggest → review-typing → 01 → 02 → 03 → 04 → verify → verify --promote)
      min_lines: 60
  key_links:
    - from: tests/phase-11/run.sh
      to: tests/phase-11/test_*.sh
      via: "shopt -s nullglob; for t in \"$SCRIPT_DIR\"/test_*.sh"
      pattern: "test_\\*.sh"
    - from: tests/phase-11/test_*.sh
      to: tests/phase-11/lib.sh
      via: "source \"$SCRIPT_DIR/lib.sh\""
      pattern: "source .*lib\\.sh"
    - from: tests/phase-11/test_migration_script_names.sh
      to: schema/brownfield/migrations/*.sh
      via: filesystem glob (asserts exact four filenames including 04-privacy-review.sh, asserts NO 04-privacy-classification.sh)
      pattern: "04-privacy-review.sh"
    - from: tests/phase-11/test_canonical_byte_equality.sh
      to: schema/brownfield/migrations/*.sh + .brownfield/migrations/*.sh (post-suggest)
      via: "cmp of body-post-op_hash-header-strip"
      pattern: "cmp -s"
---

<objective>
Wave-0 foundation for Phase 11: test harness, five byte-frozen fixtures, four canonical migration-script skeletons, and the full RED-test contract suite (~30 tests covering D-19's 18 distinct assertions + the end-to-end happy-path). This plan LOCKS THE CONTRACT — subsequent plans (11-02/03/04/05) turn tests GREEN.

Purpose: Pattern-twin Phase 10 (32 tests) and Phase 9 (28 tests). RED-first discipline prevents scope drift during implementation; the canonical scripts' `schema/brownfield/migrations/` directory must also exist in Wave 0 so Plan 11-02's byte-copy logic has something to copy, and Plan 11-03 fills in real bodies. Empty/skeleton scripts in Wave 0 keep the byte-equality CI test stable from day one — Plan 11-03 replacing the body does not break the test shape, only changes the checksum.

Output: Complete `tests/phase-11/` aggregator + lib + fixtures + RED tests; skeleton canonical scripts under `schema/brownfield/migrations/`; `tests/phase-11/run.sh` emits `PHASE 11 TESTS: K/N` with K < N (RED state) and exits non-zero.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-brownfield-suggest-verify/11-CONTEXT.md
@.planning/phases/11-brownfield-suggest-verify/11-RESEARCH.md
@.planning/phases/11-brownfield-suggest-verify/11-PATTERNS.md
@tests/phase-10/run.sh
@tests/phase-10/lib.sh
@tests/phase-10/fixtures/README.md
@bin/brownfield.sh
@bin/lib/brownfield_classify.py

<interfaces>
<!-- Key contracts the executor must honor. Extracted from existing codebase + D-19 locked contracts. -->

From tests/phase-10/lib.sh (lib.sh helpers to clone for Phase 11):
```bash
REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
make_fixture_repo() { ... }      # mktemp + git init + copy non-README content
assert_byte_equal()  { ... }     # cmp -s with regeneration-recipe on failure
assert_exit_code()   { ... }
assert_file_exists() { ... }
assert_grep()        { ... }
export -f make_fixture_repo assert_byte_equal assert_exit_code assert_file_exists assert_grep
```

From tests/phase-10/run.sh (aggregator to clone for Phase 11):
```bash
#!/usr/bin/env bash
set -euo pipefail
# ...
shopt -s nullglob
for t in "$SCRIPT_DIR"/test_*.sh; do
    TOTAL=$((TOTAL + 1))
    # ... run and tally PASS/FAIL ...
done
echo "PHASE 10 TESTS: ${PASS}/${TOTAL}"   # for Phase 11: "PHASE 11 TESTS: ..."
```

From CONTEXT.md D-07 (RENAMED): `04-privacy-classification.sh` → `04-privacy-review.sh` throughout phase.

From CONTEXT.md D-09 (candidate-file metadata header — every .brownfield/*.yaml starts with this):
```yaml
# ---
# schema_version: 1
# tool_version: <suggest version constant>
# generated_at: <UTC ISO timestamp>
# vault_root: <absolute path>
# source_script_hash: <sha256 of canonical script>
# ---
```

From CONTEXT.md D-10 (op_hash header shape — injected by suggest into .brownfield/migrations/*.sh):
```bash
# op_hash: sha256:<64-hex>
# op_hash_scope: canonical-script-body + data-schema-version
```

From RESEARCH Q2 (op_hash derivation — canonical scripts ship WITHOUT op_hash header; suggest prepends at copy time):
- Byte-equality test compares body-post-op_hash-header-strip between schema/brownfield/migrations/*.sh and .brownfield/migrations/*.sh.
- Stripping removes only lines starting with `# op_hash:` and `# op_hash_scope:`.

From CONTEXT.md specifics (applied.log apply-block normative schema):
```markdown
## 01-page-typing.sh @ 2026-04-19T14:22:31Z
mode: apply
op_hash: sha256:abc123...
exit_code: 0
prereq_check: pass
inputs:
- .brownfield/page-typing-candidates.yaml @ sha256:def456...
- .brownfield/page-typing-decisions.yaml @ sha256:789abc...
files_touched: 12
files_created: 0
files_updated: 12
files_skipped: 4
changes:
- wiki/concepts/foo.md | updated | type: "" -> concept
summary:
- approved_clusters: 3
- overridden_pages: 2
- pending_pages_remaining: 5
```

From CONTEXT.md specifics (applied.log advisory-block normative schema):
```markdown
## 04-privacy-review.sh @ 2026-04-19T14:31:02Z
mode: advisory
op_hash: sha256:...
exit_code: 0
prereq_check: pass
mutations: none
report_section: REPORT.md#privacy-review
summary:
- pages_scanned: 184
- findings: 17
- high_risk_findings: 3
```

From CONTEXT.md specifics (02-provenance-bootstrap contract phrase — verbatim in script --help):
"Marks top-level bullets under `## TL;DR` and `## Key Facts` with `[epistemic:: inferred]` when the bullet looks claim-like, is not already tagged, and is not a link-only, source-list, question, task, or placeholder bullet. Never touches Detail. Reports honestly when no eligible bullets are found."

From CONTEXT.md specifics (04-privacy-review contract phrase — verbatim in script --help):
"04-privacy-review classifies findings for review priority, not for frontmatter mutation. Fail-closed `privacy: local_only` is preserved; only a human (via frontmatter edit) may downgrade."

From CONTEXT.md D-01 (design principle — verbatim in AGENTS.md §11.5 + docs + DR):
"Review may be interactive and AI-guided; apply must always be deterministic."

From CONTEXT.md specifics (02 soft state-based prereq WARN phrasing — verbatim):
"WARN: {N} bootstrapped pages still have empty type:. 02-provenance-bootstrap works best after page typing review or on pages with existing valid type. Proceeding anyway."

From bin/brownfield.sh HEAD (current suggest/verify exit-2 gate — replaced in Plan 11-02/11-04):
```bash
case "$SUBCOMMAND" in
    scan|bootstrap) ;;
    suggest|verify)
        echo "ERROR: '$SUBCOMMAND' not yet implemented — see Phase 11 (BRWN-11..20)" >&2
        exit 2 ;;
    *)
        echo "ERROR: unknown subcommand: $SUBCOMMAND" >&2
        usage >&2; exit 1 ;;
esac
```

From RESEARCH Q11 (Nyquist sampling — ~30 tests target):
| Test Type | Count | Runtime |
|-----------|-------|---------|
| Unit (Python function contracts — called via python3 -c) | 8–10 | <0.5s |
| Integration (bash script invocation) | 20–25 | 1–3s each |
| End-to-end happy-path | 1 | 10–15s |
Target total: ~30 RED tests; Phase 10 baseline 32; Phase 9 baseline 28.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test harness (run.sh + lib.sh) + five fixtures + canonical-script skeletons + schema/brownfield/migrations/README.md</name>
  <files>
    tests/phase-11/run.sh,
    tests/phase-11/lib.sh,
    tests/phase-11/fixtures/README.md,
    tests/phase-11/fixtures/small-vault-ambiguous/ (input/ + expected/),
    tests/phase-11/fixtures/large-vault-ambiguous/ (input/ + expected/),
    tests/phase-11/fixtures/pre-typed-vault/ (input/ + expected/),
    tests/phase-11/fixtures/privacy-sensitive-vault/ (input/ + expected/),
    tests/phase-11/fixtures/already-tagged-vault/ (input/ + expected/),
    schema/brownfield/migrations/01-page-typing.sh,
    schema/brownfield/migrations/02-provenance-bootstrap.sh,
    schema/brownfield/migrations/03-cross-link-inference.sh,
    schema/brownfield/migrations/04-privacy-review.sh,
    schema/brownfield/migrations/README.md
  </files>
  <read_first>
    - tests/phase-10/run.sh (FULL file; clone with 10→11 rename; output line 43 reads "PHASE 10 TESTS: ${PASS}/${TOTAL}" — for Phase 11 this becomes "PHASE 11 TESTS: ${PASS}/${TOTAL}")
    - tests/phase-10/lib.sh (FULL file; clone with 10→11 rename + add `assert_canonical_scripts_byte_identical`)
    - tests/phase-10/fixtures/README.md (reference; model Phase 11 fixture README on it — document the input/ + expected/ dual-golden shape per D-20)
    - tests/phase-10/fixtures/clean-frontmatter/ (inspect directory layout: input/ + expected/ + README.md documenting fixture intent)
    - .planning/phases/11-brownfield-suggest-verify/11-CONTEXT.md (D-01..D-21; specifics block — applied.log schemas, contract phrases, WARN phrasing)
    - .planning/phases/11-brownfield-suggest-verify/11-RESEARCH.md (Q1 clustering code, Q2 op_hash derivation, Q3 N=20 threshold, Q5 eligibility heuristics, Q7 privacy regex set, Q11 test map)
    - bin/brownfield.sh (current HEAD — read lines 1-65 for dispatcher shape; note line 53-56 suggest|verify exit-2 gate)
  </read_first>
  <behavior>
    <!-- Wave-0 skeleton testability contracts -->
    - run.sh exit semantics: exits 0 only when ALL tests in tests/phase-11/ pass; exits non-zero on any failure and prints `Failed: <names>` to stderr.
    - lib.sh portability: `REPO_ROOT` resolves correctly even when tests are invoked with `bash tests/phase-11/test_foo.sh` from any cwd.
    - Fixture layout contract: `tests/phase-11/fixtures/<name>/input/` is the starting vault content (deterministic); `tests/phase-11/fixtures/<name>/expected/` holds golden output artifacts that later plans produce; `README.md` inside each fixture documents intent.
    - Canonical-script skeletons: each `schema/brownfield/migrations/*.sh` is a valid bash file that exits 0 on `--help`, prints its class (apply / advisory), prints the relevant contract phrase to stdout, and exits 2 on any other invocation with "not yet implemented — Plan 11-03 pending" (so Plan 11-02's byte-copy can still succeed, but actual `--apply` is guarded).
    - op_hash posture: canonical skeletons DO NOT carry `# op_hash:` / `# op_hash_scope:` headers (per RESEARCH Q2 recommendation: suggest prepends at copy time).
    - Byte-equality anchor: script body content is deliberately stable (no dates, no dynamic text) so the checksum is deterministic.
  </behavior>
  <action>
    <!-- DATA DESIGN FIRST: build harness + fixtures + skeletons that every subsequent RED test can depend on. -->

    ## 1a. tests/phase-11/run.sh (clone phase-10 verbatim with 10→11 rename)

    Copy tests/phase-10/run.sh byte-for-byte, then replace:
    - Comment "tests/phase-10/run.sh" → "tests/phase-11/run.sh"
    - Comment "Phase 10 test aggregator" → "Phase 11 test aggregator"
    - Help usage "tests/phase-10/run.sh" → "tests/phase-11/run.sh"
    - Output "PHASE 10 TESTS:" → "PHASE 11 TESTS:"
    - Make it executable with `chmod +x tests/phase-11/run.sh`.

    ## 1b. tests/phase-11/lib.sh (clone phase-10 with 10→11 rename + NEW helper)

    Copy tests/phase-10/lib.sh byte-for-byte, then:
    - Replace "tests/phase-10" references with "tests/phase-11".
    - Replace "phase10-fixture-XXXXXX" mktemp template with "phase11-fixture-XXXXXX".
    - Replace regeneration-recipe comment in `assert_byte_equal` failure path to reference Phase 11 scripts:
      ```
      echo "To regenerate expected/ fixtures after Plan 11 ships:" >&2
      echo "  bash bin/brownfield.sh suggest <fixture-input-dir>" >&2
      echo "  bash <fixture-input-dir>/.brownfield/migrations/<script> --apply" >&2
      echo "  cp <produced-file> tests/phase-11/fixtures/<name>/expected/<file>" >&2
      ```
    - ADD a new helper function `assert_canonical_scripts_byte_identical <stage_root>` that compares `$REPO_ROOT/schema/brownfield/migrations/<script>` against `<stage_root>/.brownfield/migrations/<script>` for each of the four scripts. Compare the body-post-op_hash-header-strip (strip lines starting with `# op_hash:` and `# op_hash_scope:`). On drift: print diff -u head -50 + "FAIL: canonical drift on <script>" to stderr, return 1.
      Implementation hint (inline python3 for portable stripping):
      ```bash
      assert_canonical_scripts_byte_identical() {
          local stage_root="$1"
          local script
          for script in 01-page-typing.sh 02-provenance-bootstrap.sh 03-cross-link-inference.sh 04-privacy-review.sh; do
              python3 - "$REPO_ROOT/schema/brownfield/migrations/$script" "$stage_root/.brownfield/migrations/$script" <<'PYEOF' || return 1
      import sys, pathlib
      canon = pathlib.Path(sys.argv[1]).read_bytes()
      copy  = pathlib.Path(sys.argv[2]).read_bytes()
      def strip(b):
          return b"\n".join(ln for ln in b.split(b"\n")
                            if not ln.startswith(b"# op_hash:")
                            and not ln.startswith(b"# op_hash_scope:"))
      if strip(canon) != strip(copy):
          print(f"FAIL: canonical drift on {sys.argv[2].split('/')[-1]}", file=sys.stderr)
          sys.exit(1)
      PYEOF
          done
          return 0
      }
      export -f assert_canonical_scripts_byte_identical
      ```
    - Keep existing `export -f` line; extend to include `assert_canonical_scripts_byte_identical`.

    ## 1c. tests/phase-11/fixtures/README.md (new, ~60 lines)

    Document the fixture-layout contract:
    - Each fixture subdirectory has `input/` (starting vault content), `expected/` (golden outputs Plans 11-02..11-04 produce), and `README.md` (intent + what contract it exercises).
    - List the five fixtures per D-20 with one-paragraph descriptions each:
      * small-vault-ambiguous: 3–6 pages, <10 clusters → exercises TTY small-batch path (N=20 threshold per RESEARCH Q3).
      * large-vault-ambiguous: ≥25 pages, ≥20 clusters → exercises AI-handoff large-batch path.
      * pre-typed-vault: pages with existing valid `type:` frontmatter → proves 02 prereq is state-based (no WARN when vault mostly typed).
      * privacy-sensitive-vault: pages containing email-like (`jane@acme.com`), phone-like (`212-555-0199`), SSN-like (`123-45-6789`) strings → exercises 04-privacy-review pattern set.
      * already-tagged-vault: pages with `[epistemic:: sourced]` already present in TL;DR/Key Facts → proves 02 skips them.
    - Document the Phase-10-precedent env-var pins (`BROWNFIELD_FIXTURE_TODAY=2026-04-20`, `BROWNFIELD_FIXTURE_CREATED_AT=2026-04-20`) — re-used by Phase 11 fixtures to pin `generated_at` and `bootstrap_date` values for byte-equality determinism.
    - Document a new pin `BROWNFIELD_TOOL_VERSION=1.1.0` used in candidate-file metadata headers (D-09) for byte-equality.

    ## 1d. Five fixtures under tests/phase-11/fixtures/ (input/ only in Wave 0; expected/ directories exist but are empty — filled as GREEN tests land in 11-02..11-04)

    Create each fixture directory with `input/` populated and an empty `expected/` placeholder + per-fixture README.md.

    For **small-vault-ambiguous/input/**: create 4 bootstrapped pages under `wiki/` with `bootstrap_stage: bootstrapped`, `type: ""`, empty other mandatory fields, kebab-case and PascalCase filenames mixed, simple bodies with 2-3 TL;DR bullets each:
    - wiki/concepts/attention-mechanism.md (kebab-case; H1 "Attention Mechanism"; 2 TL;DR bullets; 2 Key Facts bullets)
    - wiki/concepts/Transformer.md (PascalCase; H1 "Transformer"; 2 TL;DR bullets)
    - wiki/entities/geoffrey-hinton.md (kebab-case; H1 "Geoffrey Hinton"; 2 TL;DR bullets)
    - wiki/overviews/deep-learning.md (kebab-case; H1 "Deep Learning"; 3 TL;DR bullets with wikilinks to the other three)

    For **large-vault-ambiguous/input/**: create 25 bootstrapped pages similar to above but distributed across wiki/entities/, wiki/concepts/, wiki/overviews/ to produce ≥20 distinct classifier-signal tuples (vary filename convention + H1 shape + inbound-link density). Use terse bodies (one TL;DR bullet each) to keep fixture small; the point is signal diversity, not content depth.

    For **pre-typed-vault/input/**: create 6 pages where 5 have valid `type:` frontmatter (mix of entity/concept/overview) and 1 has `type: ""` — proves 02's state-based WARN threshold (majority-untyped) does NOT fire.

    For **privacy-sensitive-vault/input/**: create 3 pages containing:
    - wiki/journal/2026-01-15.md: TL;DR bullet containing `jdoe@acme.com`.
    - wiki/concepts/contact-info.md: TL;DR bullet containing `212-555-0199`.
    - wiki/entities/sample-person.md: Body paragraph containing `SSN: 123-45-6789`.
    All three have `privacy: local_only` already set — proves 04 never flips it.

    For **already-tagged-vault/input/**: create 2 pages where every TL;DR + Key Facts bullet already has `[epistemic:: sourced]` or `[epistemic:: inferred]` — proves 02 emits honest "no eligible bullets" report.

    ALL fixtures use LF line endings (no CRLF); UTF-8; no BOM. Match Phase 9-01 fixture convention.

    Each fixture's per-fixture `README.md` (one paragraph): "This fixture exercises <D-XX> <contract>. Expected outputs live in `expected/`; input is frozen — do not edit without regenerating `expected/` (see tests/phase-11/fixtures/README.md)."

    ## 1e. Four canonical migration script skeletons under schema/brownfield/migrations/

    Each script is a valid bash file with the following structure. Keep bodies deliberately minimal to lock byte-equality anchors — Plan 11-03 replaces the "not-yet-implemented" branch with real implementation.

    **schema/brownfield/migrations/01-page-typing.sh** (≤40 lines):
    ```bash
    #!/usr/bin/env bash
    # 01-page-typing.sh — Apply-class migration script
    # Class:  apply
    # Scope:  Reads .brownfield/page-typing-decisions.yaml; mutates wiki page `type:` frontmatter via ruamel.yaml round-trip.
    # Design principle: Review may be interactive and AI-guided; apply must always be deterministic.
    # Filled in by Phase 11 Plan 11-03.
    set -euo pipefail

    usage() {
        cat <<'EOF'
    Usage: 01-page-typing.sh [--dry-run|--apply|--help]

    Apply-class migration: reads .brownfield/page-typing-decisions.yaml (produced
    by bin/brownfield.sh suggest + resolved via bin/brownfield.sh review-typing)
    and mutates wiki page `type:` frontmatter deterministically.

    Design principle: Review may be interactive and AI-guided; apply must always be deterministic.

    --dry-run  Default. Print plan; no mutations.
    --apply    Execute mutations.
    --help     Print this help and exit 0.
    EOF
    }

    if [ "${1:-}" = "--help" ] || [ "${1:-}" = "-h" ]; then usage; exit 0; fi
    echo "ERROR: 01-page-typing.sh not yet implemented — Plan 11-03 pending" >&2
    exit 2
    ```

    **schema/brownfield/migrations/02-provenance-bootstrap.sh** (≤35 lines): same shape as above. Usage body includes the D-05 contract phrase verbatim:
    ```
    Apply-class migration. Marks top-level bullets under `## TL;DR` and `## Key Facts`
    with [epistemic:: inferred] when the bullet looks claim-like, is not already
    tagged, and is not a link-only, source-list, question, task, or placeholder
    bullet. Never touches Detail. Reports honestly when no eligible bullets are found.
    ```

    **schema/brownfield/migrations/03-cross-link-inference.sh** (≤30 lines): same shape. Usage marks class as `advisory-only`; body includes: "Advisory-only: scans vault for exact-title + alias mentions; writes .brownfield/cross-link-candidates.yaml + ## Cross-link candidates section in REPORT.md. No --apply path."

    **schema/brownfield/migrations/04-privacy-review.sh** (≤30 lines): same shape. Usage marks class as `advisory-only`. Include the D-07 contract phrase verbatim:
    ```
    04-privacy-review classifies findings for review priority, not for frontmatter
    mutation. Fail-closed `privacy: local_only` is preserved; only a human (via
    frontmatter edit) may downgrade.
    ```

    All four scripts are `chmod +x` so they are directly invocable after byte-copy (test_suggest_byte_copies_migrations.sh asserts this).

    ## 1f. schema/brownfield/migrations/README.md (~25 lines)

    New file documenting the directory's role:
    - "Canonical migration scripts for `bin/brownfield.sh suggest`."
    - List each script with one-line class + purpose (match the script usage text).
    - Document the op_hash convention: canonical scripts ship WITHOUT `# op_hash:` / `# op_hash_scope:` headers; `bin/brownfield.sh suggest` prepends the computed header to `.brownfield/migrations/*.sh` at copy time (per D-10; RESEARCH Q2).
    - Document the byte-equality CI contract: `schema/brownfield/migrations/*.sh` body (post-op_hash-header-strip) MUST match `.brownfield/migrations/*.sh` body (post-strip); enforced by `tests/phase-11/test_canonical_byte_equality.sh`.
    - Pointer: "See AGENTS.md §11.5 for the authoritative workflow contract; docs/reference/brownfield.md for operator runbook."
  </action>
  <verify>
    <automated>
      bash -n tests/phase-11/run.sh &&
      bash -n tests/phase-11/lib.sh &&
      bash -n schema/brownfield/migrations/01-page-typing.sh &&
      bash -n schema/brownfield/migrations/02-provenance-bootstrap.sh &&
      bash -n schema/brownfield/migrations/03-cross-link-inference.sh &&
      bash -n schema/brownfield/migrations/04-privacy-review.sh &&
      test -x schema/brownfield/migrations/01-page-typing.sh &&
      test -x schema/brownfield/migrations/02-provenance-bootstrap.sh &&
      test -x schema/brownfield/migrations/03-cross-link-inference.sh &&
      test -x schema/brownfield/migrations/04-privacy-review.sh &&
      bash schema/brownfield/migrations/01-page-typing.sh --help | grep -q "Review may be interactive and AI-guided; apply must always be deterministic" &&
      bash schema/brownfield/migrations/02-provenance-bootstrap.sh --help | grep -q "Marks top-level bullets under" &&
      bash schema/brownfield/migrations/03-cross-link-inference.sh --help | grep -q "Advisory-only" &&
      bash schema/brownfield/migrations/04-privacy-review.sh --help | grep -q "04-privacy-review classifies findings for review priority, not for frontmatter mutation" &&
      ! ls schema/brownfield/migrations/04-privacy-classification.sh 2>/dev/null &&
      test -d tests/phase-11/fixtures/small-vault-ambiguous/input &&
      test -d tests/phase-11/fixtures/large-vault-ambiguous/input &&
      test -d tests/phase-11/fixtures/pre-typed-vault/input &&
      test -d tests/phase-11/fixtures/privacy-sensitive-vault/input &&
      test -d tests/phase-11/fixtures/already-tagged-vault/input &&
      test -f tests/phase-11/fixtures/README.md &&
      ! grep -q "^# op_hash:" schema/brownfield/migrations/01-page-typing.sh
    </automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/phase-11/run.sh --help` exits 0 and prints usage line.
    - `grep -c "^PHASE 11 TESTS:" tests/phase-11/run.sh` returns at least 1 (output line present in echo).
    - `bash -c 'source tests/phase-11/lib.sh && type -t assert_canonical_scripts_byte_identical'` prints `function`.
    - `bash -c 'source tests/phase-11/lib.sh && type -t make_fixture_repo && type -t assert_byte_equal && type -t assert_exit_code && type -t assert_file_exists && type -t assert_grep'` prints `function` five times.
    - Each of the four canonical scripts exits 0 on `--help` and exits 2 on other invocations (proving Wave-0 "not yet implemented" stub).
    - Zero canonical scripts contain `# op_hash:` or `# op_hash_scope:` comment lines (RESEARCH Q2 prepend-at-copy-time policy).
    - Exactly 4 files matching `schema/brownfield/migrations/*.sh` exist (no 04-privacy-classification.sh lingering).
    - Five fixture directories each contain non-empty `input/` and empty `expected/` subdirectories.
    - `cat tests/phase-11/fixtures/README.md | wc -l` is ≥ 40.
    - `cat schema/brownfield/migrations/README.md | wc -l` is ≥ 20 and contains the string "op_hash" (documenting the convention).
  </acceptance_criteria>
  <done>
    Harness + fixtures + canonical-script skeletons + dir README.md all committed and pass lightweight structural checks. `bash tests/phase-11/run.sh` runs but emits a low PASS/TOTAL ratio because the RED tests from Task 2 are not yet failing the RIGHT way — Task 2 fills that in.
  </done>
</task>

<task type="auto">
  <name>Task 2: RED test suite (~30 tests covering D-19 contract + end-to-end happy-path)</name>
  <files>
    tests/phase-11/test_migration_script_names.sh,
    tests/phase-11/test_suggest_byte_copies_migrations.sh,
    tests/phase-11/test_suggest_candidate_metadata_header.sh,
    tests/phase-11/test_canonical_byte_equality.sh,
    tests/phase-11/test_op_hash_stable.sh,
    tests/phase-11/test_op_hash_header_shape.sh,
    tests/phase-11/test_applied_log_apply_schema.sh,
    tests/phase-11/test_applied_log_advisory_schema.sh,
    tests/phase-11/test_applied_log_dryrun_no_append.sh,
    tests/phase-11/test_01_dryrun_default.sh,
    tests/phase-11/test_01_apply_reads_decisions_only.sh,
    tests/phase-11/test_01_idempotent.sh,
    tests/phase-11/test_02_dryrun_default.sh,
    tests/phase-11/test_02_apply_eligible_bullets.sh,
    tests/phase-11/test_02_no_magic_strings.sh,
    tests/phase-11/test_02_skips_already_tagged.sh,
    tests/phase-11/test_02_honest_no_eligible.sh,
    tests/phase-11/test_02_soft_prereq_warn.sh,
    tests/phase-11/test_02_idempotent.sh,
    tests/phase-11/test_03_advisory_only.sh,
    tests/phase-11/test_04_advisory_only.sh,
    tests/phase-11/test_04_never_flips_privacy.sh,
    tests/phase-11/test_review_typing_tty_small.sh,
    tests/phase-11/test_review_typing_ai_handoff.sh,
    tests/phase-11/test_review_typing_decisions_roundtrip.sh,
    tests/phase-11/test_verify_lint_wrapper.sh,
    tests/phase-11/test_verify_readonly_default.sh,
    tests/phase-11/test_verify_promote_5_gates.sh,
    tests/phase-11/test_verify_promote_gate5_pending.sh,
    tests/phase-11/test_no_llm_calls.sh,
    tests/phase-11/test_agents_section_11_5.sh,
    tests/phase-11/test_agents_template_parity_11_5.sh,
    tests/phase-11/test_canonical_agents_byte_equality.sh,
    tests/phase-11/test_docs_mechanical_judgment.sh,
    tests/phase-11/test_docs_suggest_section.sh,
    tests/phase-11/test_docs_review_typing_section.sh,
    tests/phase-11/test_docs_verify_section.sh,
    tests/phase-11/test_end_to_end_happy_path.sh
  </files>
  <read_first>
    - tests/phase-10/test_brownfield_bootstrap_idempotent.sh (pattern-twin for idempotency tests — re-run asserts zero-byte diff)
    - tests/phase-10/test_brownfield_bootstrap_apply_clean.sh (pattern-twin for apply + byte-equality test shape)
    - tests/phase-10/test_brownfield_scan_report_shape.sh (pattern-twin for REPORT.md shape assertions)
    - tests/phase-08/test_canonical_byte_equality.sh (pattern-twin for byte-equality test with regeneration hint)
    - .planning/phases/11-brownfield-suggest-verify/11-CONTEXT.md §Specific-Ideas (applied.log normative schemas + WARN phrasing + contract phrases)
    - .planning/phases/11-brownfield-suggest-verify/11-RESEARCH.md Q11 Phase Requirements → Test Map (authoritative test list)
    - .planning/phases/11-brownfield-suggest-verify/11-PATTERNS.md (analog citations for each new file)
  </read_first>
  <behavior>
    <!-- D-19 contract coverage — each test asserts a locked invariant -->
    - Every test MUST source `tests/phase-11/lib.sh` for helpers.
    - Every test MUST be runnable standalone (`bash tests/phase-11/test_foo.sh`) AND discoverable by `tests/phase-11/run.sh`.
    - Each test prints exactly one `PASS <name>` line on success and a clear `FAIL: <reason>` on failure.
    - RED state: every test MUST fail today with a diagnostic error mentioning the missing implementation (e.g., "suggest not yet implemented — Plan 11-02 pending"), NOT with a test-infra crash.
    - Pattern: tests that depend on `bin/brownfield.sh suggest` use the expected exit-2 output as the RED indicator; tests are written as if GREEN, so changing the stub to real implementation in Plans 11-02..11-04 flips PASS/FAIL automatically.
    - No sleep/retry loops; deterministic with Phase-10 env-var pins.
    - Tests set `export BROWNFIELD_FIXTURE_TODAY=2026-04-20`, `export BROWNFIELD_FIXTURE_CREATED_AT=2026-04-20`, `export BROWNFIELD_TOOL_VERSION=1.1.0` before invoking any Phase-11-produced command (for determinism).
  </behavior>
  <action>
    Create ~32 test scripts per RESEARCH Q11's test map. EACH test begins with the standard header:

    ```bash
    #!/usr/bin/env bash
    # tests/phase-11/<name>.sh — <one-line purpose>
    set -euo pipefail
    SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
    source "$SCRIPT_DIR/lib.sh"
    export BROWNFIELD_FIXTURE_TODAY=2026-04-20
    export BROWNFIELD_FIXTURE_CREATED_AT=2026-04-20
    export BROWNFIELD_TOOL_VERSION=1.1.0
    export PYTHONPATH="${PYTHONPATH:-}${PYTHONPATH:+:}$HOME/.local/lib/python3/dist-packages"
    NAME="$(basename "${BASH_SOURCE[0]}")"
    ```

    And ends with `echo "PASS $NAME"; exit 0`.

    ### Tests to create (grouped by REQ-ID; total ~32):

    **BRWN-12 (script naming — D-07 rename lock):**
    - `test_migration_script_names.sh`: Assert exactly 4 files match `schema/brownfield/migrations/*.sh`; assert each exists with its exact name; assert NO file named `04-privacy-classification.sh` anywhere in `schema/brownfield/migrations/` or `.brownfield/migrations/` (search under tmp fixture run). Use `find schema/brownfield/migrations -name "04-privacy-classification.sh"` returns empty.

    **BRWN-11 (suggest — D-08 hybrid generation):**
    - `test_suggest_byte_copies_migrations.sh`: make_fixture_repo small-vault-ambiguous; run `bash "$REPO_ROOT/bin/brownfield.sh" suggest "$TMP_REPO"`; assert `$TMP_REPO/.brownfield/migrations/01-page-typing.sh` exists, is executable (`test -x`); assert_canonical_scripts_byte_identical "$TMP_REPO".
    - `test_suggest_candidate_metadata_header.sh`: make_fixture_repo small-vault-ambiguous; run suggest; assert first 7 lines of each `.brownfield/{page-typing-candidates.yaml,page-typing-decisions.yaml,provenance-bootstrap-report.yaml,cross-link-candidates.yaml,privacy-findings.yaml}` contain the D-09 metadata block (`schema_version: 1`, `tool_version: 1.1.0`, `generated_at:`, `vault_root:`, `source_script_hash: sha256:`).

    **BRWN-14 (op_hash + applied.log):**
    - `test_canonical_byte_equality.sh`: make_fixture_repo small-vault-ambiguous; run suggest; assert_canonical_scripts_byte_identical "$TMP_REPO".
    - `test_op_hash_stable.sh`: make_fixture_repo small-vault-ambiguous → TMP1; make_fixture_repo large-vault-ambiguous → TMP2; run suggest on both; assert `grep -E '^# op_hash: sha256:[0-9a-f]{64}$' "$TMP1/.brownfield/migrations/01-page-typing.sh"` matches the same hash as in TMP2 (hash depends on canonical script + data-schema-version only, NOT vault content per D-10).
    - `test_op_hash_header_shape.sh`: run suggest; `grep -c '^# op_hash: sha256:' "$TMP/.brownfield/migrations/<each>.sh"` returns 1; `grep -c '^# op_hash_scope: canonical-script-body + data-schema-version$' "$TMP/.brownfield/migrations/<each>.sh"` returns 1.
    - `test_applied_log_apply_schema.sh`: run suggest; resolve decisions manifest to approve-all (scripted sed on small-vault fixture's decisions file — Plan 11-02 must produce a predictable shape); run `bash "$TMP/.brownfield/migrations/01-page-typing.sh" --apply`; grep `$TMP/.brownfield/applied.log` for block header `^## 01-page-typing\.sh @ .*Z$`, then `mode: apply`, `op_hash: sha256:`, `exit_code: 0`, `inputs:`, `files_touched:`, `changes:`, `summary:`.
    - `test_applied_log_advisory_schema.sh`: run suggest; run `bash "$TMP/.brownfield/migrations/04-privacy-review.sh"`; grep log for block header `^## 04-privacy-review\.sh @`, then `mode: advisory`, `mutations: none`, `report_section: REPORT.md#`.
    - `test_applied_log_dryrun_no_append.sh`: run suggest; run `bash "$TMP/.brownfield/migrations/01-page-typing.sh" --dry-run`; `test ! -f "$TMP/.brownfield/applied.log" -o $(grep -c '^## 01-page-typing' "$TMP/.brownfield/applied.log") -eq 0` (dry-run does NOT append).

    **BRWN-13 (per-script dry-run default + --apply gate + idempotency):**
    - `test_01_dryrun_default.sh`: run suggest; run `bash "$TMP/.brownfield/migrations/01-page-typing.sh"` (no flags); assert exit 0 AND no mutation (vault file mtimes unchanged OR git -C "$TMP" diff --quiet).
    - `test_01_apply_reads_decisions_only.sh`: run suggest; modify candidates.yaml to flag a bogus page (one Plan 11-02 DID NOT list); run `bash ...01-page-typing.sh --apply`; assert the bogus page is NOT mutated (apply reads DECISIONS manifest, not candidates). Plus the reverse: resolved-decision pages ARE mutated per manifest.
    - `test_01_idempotent.sh`: run suggest + decisions resolve-all + --apply; record git diff HEAD; run --apply a SECOND time; assert zero additional changes (git status --porcelain --no-renames | wc -l == 0 after second apply).
    - `test_02_dryrun_default.sh`: run suggest; `bash "$TMP/.brownfield/migrations/02-provenance-bootstrap.sh"`; assert no mutations.
    - `test_02_apply_eligible_bullets.sh`: use small-vault-ambiguous fixture; run suggest + `02-provenance-bootstrap.sh --apply`; assert TL;DR + Key Facts bullets gained ` [epistemic:: inferred]` suffix on eligible lines; assert Detail section bullets did NOT.
    - `test_02_no_magic_strings.sh`: after 02 --apply, `grep -rnE '\[prov:bootstrap\]|\[epistemic:: imported\]|\[epistemic:: bootstrapped\]' "$TMP/wiki/"` returns empty (BRWN-15: ONLY `[epistemic:: inferred]`).
    - `test_02_skips_already_tagged.sh`: use already-tagged-vault; run suggest; run 02 --apply; assert pages unchanged (no double-tagging) AND applied.log block reports honest count.
    - `test_02_honest_no_eligible.sh`: use already-tagged-vault OR a variant with only link-only bullets; run suggest; assert `provenance-bootstrap-report.yaml` records `eligible_bullets: 0` for those pages and the per-page entry includes a `note:` field with the string "no eligible claim bullets found" (honest reporting per D-05).
    - `test_02_soft_prereq_warn.sh`: use small-vault-ambiguous (majority `type: ""`); run suggest; capture stderr of `02-provenance-bootstrap.sh --apply`; assert stderr contains the EXACT phrase `WARN:` followed on same line by `bootstrapped pages still have empty type:. 02-provenance-bootstrap works best after page typing review or on pages with existing valid type. Proceeding anyway.`; assert exit code 0 (continues anyway).
    - `test_02_idempotent.sh`: run 02 --apply twice; assert zero byte diff between run 1 post-state and run 2 post-state.
    - `test_03_advisory_only.sh`: run suggest + 03-cross-link-inference.sh (no flags); assert `.brownfield/cross-link-candidates.yaml` exists; assert REPORT.md contains `## Cross-link candidates` section; assert NO vault mutations (git status --porcelain returns empty for `wiki/`); assert `03-cross-link-inference.sh --apply` exits non-zero OR no-ops (D-06).
    - `test_04_advisory_only.sh`: run suggest + 04-privacy-review.sh on privacy-sensitive-vault; assert `.brownfield/privacy-findings.yaml` exists with email/phone/SSN findings; assert REPORT.md contains `## Privacy review` section; assert NO vault mutations.
    - `test_04_never_flips_privacy.sh`: on privacy-sensitive-vault (all pages `privacy: local_only`), run 04; assert every page's `privacy:` field still reads `local_only` post-run (`grep -c '^privacy: local_only' $page` unchanged).

    **BRWN-22 (review-typing — new REQ-ID proposed in Plan 11-05):**
    - `test_review_typing_tty_small.sh`: on small-vault-ambiguous (pending clusters < 20); run suggest; pipe `echo -e 'a\na\na\n'` into `bash "$REPO_ROOT/bin/brownfield.sh" review-typing --root "$TMP"`; assert decisions.yaml now has all clusters `decision: approve`.
    - `test_review_typing_ai_handoff.sh`: on large-vault-ambiguous (pending clusters ≥ 20); run suggest; run `bash bin/brownfield.sh review-typing --root "$TMP"` (no stdin — force large-batch via cluster count; isatty will be false in CI per RESEARCH Pitfall 8); assert `$TMP/.brownfield/review-typing-prompt.md` exists; assert it contains the directive phrase `Edit ONLY the decisions manifest. Do not modify vault pages.` AND the run command `bash .brownfield/migrations/01-page-typing.sh --apply`.
    - `test_review_typing_decisions_roundtrip.sh`: add a comment line (`# human note: approved after team review`) to decisions.yaml; run review-typing TTY with 'approve all' input; assert the comment is preserved in the written-back file (ruamel round-trip invariant).

    **BRWN-17 (verify — D-13/D-14):**
    - `test_verify_lint_wrapper.sh`: run `bash bin/brownfield.sh verify --root "$TMP"`; assert stdout contains `lint --ci` OR a summary derived from lint findings; assert invocation used `--category yaml,provenance,orphan,crossref,brownfield` (NOT `privacy`).
    - `test_verify_readonly_default.sh`: on a fixture where some pages meet 5-gate pass-list; run `bash bin/brownfield.sh verify --root "$TMP"` (no --promote); assert zero `bootstrap_stage` flips occurred (git status --porcelain returns empty).
    - `test_verify_promote_5_gates.sh`: fixture with 3 pages bootstrapped: page A passes all 5 gates → gets flipped to `verified`; page B has `type: ""` (gate 2 fail) → remains bootstrapped; page C has a lint error (gate 3 fail) → remains bootstrapped. Run `verify --promote`; assert exactly A is flipped.
    - `test_verify_promote_gate5_pending.sh`: fixture where page has valid type + zero lint errors BUT appears in a `decision: pending` cluster in page-typing-decisions.yaml. Run `verify --promote`; assert the page is NOT flipped (Gate 5 blocks it).

    **BRWN-16 (no LLM calls):**
    - `test_no_llm_calls.sh`: `grep -rnE '(curl|wget|anthropic|openai|claude|gpt-|chatgpt)' bin/brownfield.sh bin/lib/brownfield_*.py schema/brownfield/migrations/ 2>/dev/null` returns empty (exit 1 from grep = no matches = PASS). Whitelist: documentation comments are permitted only inside the `.brownfield/review-typing-prompt.md` template text itself, which lives in the bash heredoc — tolerated only if heredoc boundaries are NOT curl/wget invocations.

    **BRWN-20 (AGENTS.md §11.5 shape — target of Plan 11-05):**
    - `test_agents_section_11_5.sh`: assert `grep -c '^### 11.5 Brownfield Workflow' AGENTS.md` == 1 AND `grep -c '^### 11.5 Release Workflow' AGENTS.md` == 0 (Option C renumber per RESEARCH Pitfall 1) AND `grep -c '^### 11.6 Release Workflow' AGENTS.md` == 1. Also assert the D-01 design-principle quote verbatim appears in §11.5 body: `grep -q 'Review may be interactive and AI-guided; apply must always be deterministic\.' AGENTS.md`.
    - `test_agents_template_parity_11_5.sh`: assert `grep -c '^### 11.5 Brownfield Workflow' schema/AGENTS.template.md` == 1; assert the §11.5 body block of AGENTS.md (between `^### 11.5` and the next `^### 11.`) is byte-identical to the §11.5 body block of `schema/AGENTS.template.md` (modulo template placeholders — but §11.5 has no placeholders per D-16, so compare exactly). Use flag-based awk extraction (pattern-twin Phase 9.1 `test_template_parity.sh`).
    - `test_canonical_agents_byte_equality.sh`: assert `cmp -s schema/fixtures/canonical-AGENTS.md <(bash bin/init-wizard.sh --answers-file schema/fixtures/canonical-answers.yaml --render-to /tmp/wz-phase11-check && cat /tmp/wz-phase11-check/AGENTS.md)` OR more simply: re-use Phase 8 test shape — confirms Plan 11-05's canonical-AGENTS.md regeneration was done.

    **BRWN-18 + BRWN-19 (docs — target of Plan 11-05):**
    - `test_docs_mechanical_judgment.sh`: assert `docs/reference/brownfield.md` contains the string `mechanical` AND `judgment` within the same section (grep `-A 20` to scope).
    - `test_docs_suggest_section.sh`: assert `## suggest subcommand` in docs/reference/brownfield.md is a populated section (≥ 20 lines between header and next `^## `) — NOT a stub. `awk '/^## suggest subcommand/,/^## /' docs/reference/brownfield.md | wc -l` ≥ 20.
    - `test_docs_review_typing_section.sh`: assert `## review-typing subcommand` exists and is populated (≥ 15 lines).
    - `test_docs_verify_section.sh`: assert `## verify subcommand` exists and is populated (≥ 15 lines) AND contains the string `--promote`.

    **End-to-end happy-path (one test):**
    - `test_end_to_end_happy_path.sh`: make_fixture_repo small-vault-ambiguous → TMP; run suggest; run review-typing with scripted-stdin approve-all; run 01 --apply; run 02 --apply; run 03 (advisory); run 04 (advisory); run verify; run verify --promote; assert final vault state matches a golden fixture under `tests/phase-11/fixtures/end-to-end-golden/` (path to be populated by Plan 11-04 GREEN work — test asserts the directory exists and performs byte-level comparison). Also assert `.brownfield/applied.log` has exactly N blocks (one per meaningful invocation — 01 apply, 02 apply, 03 advisory, 04 advisory = 4 blocks minimum).

    ## Final step

    Make all test scripts executable: `chmod +x tests/phase-11/test_*.sh`.

    Run `bash tests/phase-11/run.sh` — expect output like `PHASE 11 TESTS: 0/32` or similar RED state with exit 1 (Wave-0 gate: tests exist, execute, but fail clearly).
  </action>
  <verify>
    <automated>
      bash -n tests/phase-11/test_migration_script_names.sh &&
      bash -n tests/phase-11/test_suggest_byte_copies_migrations.sh &&
      bash -n tests/phase-11/test_02_no_magic_strings.sh &&
      bash -n tests/phase-11/test_verify_promote_5_gates.sh &&
      bash -n tests/phase-11/test_end_to_end_happy_path.sh &&
      test "$(ls tests/phase-11/test_*.sh | wc -l)" -ge 30 &&
      test -x tests/phase-11/test_migration_script_names.sh &&
      (bash tests/phase-11/test_migration_script_names.sh >/dev/null 2>&1 ; echo "$?") | grep -qE '^[01]$' &&
      (bash tests/phase-11/run.sh 2>&1 || true) | grep -qE 'PHASE 11 TESTS: [0-9]+/[0-9]+'
    </automated>
  </verify>
  <acceptance_criteria>
    - `ls tests/phase-11/test_*.sh | wc -l` returns a count ≥ 30 (Nyquist target per RESEARCH Q11).
    - Every test file passes `bash -n <file>` (syntactically valid).
    - Every test file is executable (`test -x` returns 0).
    - `bash tests/phase-11/run.sh 2>&1` output ends with a line matching `PHASE 11 TESTS: [0-9]+/[0-9]+` and exits 1 (RED state — tests exist but most fail because Plans 11-02..11-05 have not landed).
    - `test_no_llm_calls.sh` passes TODAY (greps existing code; no new Phase 11 code has added curl/wget/openai/anthropic calls yet).
    - `test_migration_script_names.sh` passes TODAY (skeleton scripts exist with correct names per Task 1).
    - `test_agents_section_11_5.sh` FAILS today (AGENTS.md §11.5 is still Release Workflow; Plan 11-05 fixes it).
    - `test_suggest_byte_copies_migrations.sh` FAILS today with the `suggest not yet implemented` stderr text from `bin/brownfield.sh` line 54 (exit-2 gate).
    - grep -l 'BROWNFIELD_FIXTURE_TODAY' tests/phase-11/test_*.sh returns every test file that invokes brownfield (deterministic env-var pinning).
  </acceptance_criteria>
  <done>
    ~30+ RED tests committed; aggregator runs and exits 1 with clear RED state; every subsequent plan (11-02/03/04/05) has a locked target: "flip K tests from FAIL to PASS; do not add new test contracts."
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness ↔ fixture repos | tests operate on throwaway `mktemp` git repos seeded from `tests/phase-11/fixtures/` — no state leaks back to real repo |
| canonical scripts ↔ user vault | canonical scripts ship tracked in `schema/brownfield/migrations/` and are BYTE-COPIED (never rendered with vault data); hybrid model isolates logic from state |
| fixture env vars ↔ wall clock | `BROWNFIELD_FIXTURE_TODAY` etc. pin dates for byte-equality determinism; fail-loud on malformed values (Phase 10 precedent) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | schema/brownfield/migrations/*.sh canonical scripts | mitigate | Skeleton scripts ship with stable byte-anchors and NO op_hash header (prevents self-reference in op_hash derivation per RESEARCH Q2); byte-equality CI test (Plan 11-02 consumes) asserts `.brownfield/migrations/*.sh` body-post-strip matches canonical. |
| T-11-01-02 | Tampering | tests/phase-11/fixtures/ input/ content | mitigate | Fixtures committed with LF line endings (`.gitattributes` already pins LF repo-wide); README.md documents regeneration recipe; `make_fixture_repo` copies via explicit `find -type f` + `cp` (no tar roundtrip) to prevent mode/mtime drift. |
| T-11-01-03 | Information Disclosure | fixtures (privacy-sensitive-vault) | accept | Fixture emails/phones/SSNs are obviously fake (`jdoe@acme.com`, `212-555-0199`, `123-45-6789`) and clearly marked as test data. Risk of real PII leakage is zero. |
| T-11-01-04 | Denial of Service | test aggregator infinite loop | mitigate | `set -euo pipefail` + no while loops in aggregator; each test has its own timeout via test-infra convention (bash test scripts terminate on assertion failure); no sleep/retry logic. |
| T-11-01-05 | Tampering | test scripts reading $PYTHONPATH from caller env | mitigate | Each test sets `export PYTHONPATH="${PYTHONPATH:-}${PYTHONPATH:+:}$HOME/.local/lib/python3/dist-packages"` so ruamel.yaml is available in isolation (idempotent append). |
| T-11-01-06 | Repudiation | test failures not attributable to specific REQ-ID | mitigate | Every test script's top-line comment names the REQ-ID(s) it covers; RESEARCH Q11 Phase Requirements → Test Map is the traceability source of truth. |
| T-11-01-07 | Input Validation | fixture YAML input with malformed frontmatter | accept | YAML parsing is handled by ruamel.yaml + PyYAML safe_load — both fail-loud on malformed input. Fixtures are BYTE-FROZEN so the input is known-good by construction. |
| T-11-01-08 | Elevation of Privilege | schema/brownfield/migrations/*.sh execution | mitigate | Skeleton scripts exit 2 on any invocation except `--help` (Wave-0 guard); users cannot accidentally run unimplemented `--apply` in Wave 0. Plan 11-03 replaces the exit-2 branch with real implementation. |
</threat_model>

<verification>
- All files under `tests/phase-11/` and `schema/brownfield/migrations/` committed.
- `bash -n` passes for every script.
- `bash tests/phase-11/run.sh` exits 1 (RED state) and prints `PHASE 11 TESTS: K/N` with K < N.
- Canonical scripts exit 0 on `--help` and 2 on real invocations.
- Zero `# op_hash:` headers in canonical scripts (prepended by suggest at copy time per D-10).
- Five fixtures present with `input/` + empty `expected/` + per-fixture README.
- `schema/brownfield/migrations/README.md` documents op_hash prepend policy.
- RED tests pass SYNTACTICALLY (`bash -n`) but FAIL SEMANTICALLY (expected — Plan 11-02..11-05 turn them GREEN).
- `test_no_llm_calls.sh` passes TODAY (whitelist preserved).
</verification>

<success_criteria>
All must_haves above hold. Specifically:
- `find tests/phase-11 -name 'test_*.sh' | wc -l` ≥ 30.
- `bash tests/phase-11/run.sh` emits `PHASE 11 TESTS: K/N` and exits 1 with K < N.
- `find schema/brownfield/migrations -name '*.sh' | sort` lists exactly 4 scripts, none named `04-privacy-classification.sh`.
- `find schema/brownfield/migrations -name '*.sh' | xargs grep -lE '^# op_hash:'` returns empty (no pre-baked headers in canonical scripts).
- Five fixture directories exist under `tests/phase-11/fixtures/` each with populated `input/` and empty `expected/` + per-fixture README.
- `tests/phase-11/fixtures/README.md` is ≥ 40 lines and documents env-var pinning + regeneration recipe.
</success_criteria>

<output>
After completion, create `.planning/phases/11-brownfield-suggest-verify/11-01-SUMMARY.md` capturing:
- Final test count (N).
- Fixture sizes (pages per fixture; cluster counts produced by the classifier — if any — on small vs large).
- Confirmation of Option C renumbering test shape (test_agents_section_11_5.sh) locked.
- Which tests are PASS today (whitelist) vs FAIL today (RED targets for Plans 11-02..11-05).
- Any calibration notes on N=20 threshold (do large-vault clusters actually reach ≥20?).
</output>
