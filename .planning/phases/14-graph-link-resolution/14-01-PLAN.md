---
phase: 14-graph-link-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - AGENTS.md
  - schema/AGENTS.template.md
  - schema/templates/entity.md
  - schema/templates/concept.md
  - schema/templates/overview.md
  - schema/templates/comparison.md
  - schema/templates/source-summary.md
  - schema/templates/decision.md
  - schema/obsidian/entity.md
  - schema/obsidian/concept.md
  - schema/obsidian/overview.md
  - schema/obsidian/comparison.md
  - schema/obsidian/source-summary.md
  - schema/obsidian/decision.md
  - wiki/decisions/dr-2026-06-03-uniform-piped-links.md
  - wiki/decisions/dr-2026-06-02-obsidian-filename-alias-resolution.md
  - wiki/index.md
  - wiki/log.md
autonomous: true
requirements:
  - LINK-01
  - LINK-02
  - LINK-03

must_haves:
  truths:
    - "CLAUDE.md §8 states Obsidian resolves [[X]] by filename/path ONLY (never aliases or title) and mandates uniform [[id|Exact Title]] links"
    - "The self-alias invariant (checklist items 18-19) is removed from §5 and §8"
    - "A new schema-update DR supersedes dr-2026-06-02-obsidian-filename-alias-resolution with supersedes/superseded_by set"
    - "AGENTS.md is byte-identical to CLAUDE.md (sync check passes)"
    - "schema/AGENTS.template.md mirrors the §5/§8 edits"
    - "All six schema/templates/*.md and schema/obsidian/*.md files reflect piped-link convention"
  artifacts:
    - path: "wiki/decisions/dr-2026-06-03-uniform-piped-links.md"
      provides: "Superseding DR documenting the real resolution rule and piped-link decision"
      contains: "trigger_type: schema-update"
    - path: "CLAUDE.md"
      provides: "Corrected §8 (piped links) + corrected §5 (self-alias items removed)"
      min_lines: 1600
    - path: "AGENTS.md"
      provides: "Byte-identical copy of CLAUDE.md"
  key_links:
    - from: "wiki/decisions/dr-2026-06-03-uniform-piped-links.md"
      to: "wiki/decisions/dr-2026-06-02-obsidian-filename-alias-resolution.md"
      via: "supersedes frontmatter field + superseded_by set on old DR"
      pattern: "supersedes.*dr-2026-06-02-obsidian-filename-alias-resolution"
    - from: "CLAUDE.md"
      to: "AGENTS.md"
      via: "bin/sync-claude.sh --check byte-equality"
      pattern: "bin/sync-claude.sh --check exits 0"
    - from: "schema/AGENTS.template.md"
      to: "CLAUDE.md §5/§8"
      via: "tests/phase-10 §5 parity test"
      pattern: "[[id|Exact Title]]"
---

<objective>
Correct the wiki schema convention to the ground truth about Obsidian link resolution.

Purpose: The prior Phase 14 run shipped CLAUDE.md/AGENTS.md §8/§5 still claiming the false
"filename stem + aliases" premise, mandating the self-alias invariant, and prohibiting display
aliases. All three must be inverted to the correct convention: Obsidian resolves [[X]] by
filename/path ONLY; every intra-wiki link MUST be the piped form [[id|Exact Title]]; the
self-alias invariant is removed (aliases reverts to optional). A new decision record supersedes
the wrong-premise DR.

Output: Corrected CLAUDE.md + AGENTS.md (§8 rules + §5 checklist + bad/good examples + §3 MUST NOT
list + §15 Obsidian note), corrected schema/templates/*.md + schema/obsidian/*.md (remove self-alias
comment block, add piped-link guidance), schema/AGENTS.template.md mirroring §5/§8, and a new
wiki/decisions/ DR that SUPERSEDES dr-2026-06-02-obsidian-filename-alias-resolution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-graph-link-resolution/14-CONTEXT.md

# The two key files being modified -- read them fully before starting
@CLAUDE.md
@schema/AGENTS.template.md
@schema/templates/entity.md
@schema/obsidian/entity.md
@wiki/decisions/dr-2026-06-02-obsidian-filename-alias-resolution.md
@wiki/index.md
</context>

<interfaces>
<!-- Key spans the executor needs before editing. -->

Current §8 lines in CLAUDE.md (approx lines 652-700 in current file):
  - Rule 3 says "DO NOT use display aliases"
  - Rule 4 says "Obsidian resolves [[X]] by matching the filename stem or an aliases entry"
  - Rule 4a mandates the self-alias invariant (LINK-02)
  - Bad/good examples show bare [[Title]] as GOOD and piped [[Title|alias]] as BAD
  - §5 validation checklist items 18-19 enforce self-alias invariant
  - §5 title field description says "Obsidian resolves [[X]] by filename stem + aliases"
  - §5 aliases field description says "Obsidian resolves [[Alias]] to the canonical page"

Current schema/templates/entity.md and schema/obsidian/entity.md (all 6 type files):
  - Have a comment block inside the aliases frontmatter section:
    "# Self-alias invariant (AGENTS.md §8): add your page title and id slug here..."
  - FORBIDDEN PATTERNS comment says "No display aliases: write [[Page Title]] not [[Page Title|Alias]]"

Current wiki/decisions/dr-2026-06-02-obsidian-filename-alias-resolution.md:
  - id: dr-2026-06-02-obsidian-filename-alias-resolution
  - status: active
  - superseded_by: (empty)
  - Documents the WRONG premise (filename stem + aliases)
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Correct CLAUDE.md §8/§5 to the piped-link convention</name>
  <files>CLAUDE.md</files>
  <read_first>
    Read CLAUDE.md lines 115-130 (§3 MUST NOT list — "DO NOT use display aliases"), lines 288-303
    (§5 title/aliases field descriptions), lines 376-397 (§5 frontmatter validation checklist items
    18-19), lines 652-700 (§8 full section — rules + bad/good examples + graph view implications),
    and lines 1647-1652 (§15 Obsidian section, "prohibits display aliases" note).
    Also read schema/AGENTS.template.md at the same line ranges to understand what mirrors here.
  </read_first>
  <action>
Edit CLAUDE.md. Make these targeted changes in-place (do NOT rewrite unrelated content):

**§3 MUST NOT list (around line 125):**
Replace:
  "- DO NOT use display aliases in wikilinks: write `[[Attention Mechanism]]` not `[[Attention Mechanism|attention]]`."
With:
  "- DO NOT write bare `[[Title]]` wikilinks. ALWAYS write `[[id|Exact Title]]` (target = page `id`; display = exact canonical `title`). Bare links without a pipe do not reliably resolve for multi-word-title pages in Obsidian (which resolves by filename/path ONLY, never by `aliases`)."

**§5 `title` field description (around line 288):**
Replace:
  "Human-readable canonical title. Used in page headings and the self-alias (§8). Obsidian resolves `[[X]]` by **filename stem + `aliases`**, not by this field."
With:
  "Human-readable canonical title. Used in page headings and as the display text in piped links `[[id|Title]]`. Obsidian resolves `[[X]]` by **filename/path ONLY** — never by this field and never by `aliases`."

**§5 `aliases` field description (around line 301):**
Replace:
  "Alternative names for Obsidian automatic resolution. Obsidian resolves `[[Alias]]` to the canonical page."
With:
  "OPTIONAL. Genuine alternate names (e.g. common abbreviations). Obsidian uses these for Quick Switcher / autocomplete and as display text in piped links `[[file|Alias]]`. They do NOT affect bare `[[X]]` resolution — that is always filename/path only."

**§5 frontmatter validation checklist items 18-19 (around lines 393-397):**
Delete BOTH items entirely (items 18 and 19 about the self-alias invariant). Renumber/reformat is
not required — simply remove those two bullet points. The checklist ends at item 17 (or a
preceding item).

Replace those two items with a single new item (maintaining the existing list numbering context):
  "18. Every intra-wiki body link MUST use the piped form `[[id|Exact Title]]` — target is the page `id` (= filename stem, always resolves in Obsidian); display is the exact canonical `title`. Bare `[[Title]]` links are a convention error."

**§8 Wikilink and Graph Conventions — Rules section (around lines 654-671):**
Replace the entire Rules section (numbered list items 1-8) with:

```
### Rules

1. Use `[[id|Exact Title]]` for ALL intra-wiki cross-references in page body text.
   The target before `|` is the page `id` (= filename stem — always resolves in Obsidian
   since Obsidian resolves `[[X]]` by **filename/path ONLY**, never by `title` and never
   by `aliases`). The display text after `|` is the exact canonical `title`.
2. Link on FIRST mention only per page. Subsequent mentions are plain text.
3. ALWAYS write `[[id|Exact Title]]` — bare `[[Title]]` links are a convention error.
   Examples: `[[bounded-context|Bounded Context]]`, `[[hack-agentive-stack|Hack (Agentive Stack)]]`.
4. The `aliases` frontmatter field is OPTIONAL — for genuine alternate names (Quick Switcher /
   autocomplete), NOT for link resolution. Obsidian resolves `[[X]]` by **filename/path ONLY**.
5. Red links (links to not-yet-existing page `id`s) are ALLOWED and intentional. They signal
   knowledge gaps for the lint workflow. Write them as `[[not-yet-existing-id|Display Text]]`
   where the `id` is the planned slug.
6. DO NOT put wikilinks in YAML frontmatter. Use string IDs in frontmatter, wikilinks in body text.
7. The `## Related Pages` section lists explicit wikilinks to connected pages.
8. The `## Sources` section in page body lists human-readable source references with wikilinks to
   source summary pages.
```

**§8 Bad vs. Good Wikilink Examples (around lines 673-692):**
Replace the entire example block with:

```
### Bad vs. Good Wikilink Examples

```
BAD:  sources: ["[[Vaswani et al]]"]                    (wikilink in frontmatter)
GOOD: sources: [src-2026-03-15-vaswani-attention]       (string ID in frontmatter)

BAD:  [[Attention Mechanism]]                           (bare link -- does not resolve for multi-word titles)
GOOD: [[attention-mechanism|Attention Mechanism]]       (piped: target=id, display=title)

BAD:  [[attention-mechanism|attention]]                 (display text is not the exact canonical title)
GOOD: [[attention-mechanism|Attention Mechanism]]       (display = exact title from page frontmatter)

BAD:  ...[[attention-mechanism|Attention Mechanism]] uses [[attention-mechanism|Attention Mechanism]] weights...  (linked twice)
GOOD: ...[[attention-mechanism|Attention Mechanism]] uses attention weights...  (linked once, plain after)

BAD:  [[Hack (Agentive Stack)]]                         (bare link, parens title, will not resolve)
GOOD: [[hack-agentive-stack|Hack (Agentive Stack)]]     (piped: id target resolves, parens in display)

BAD:  [[Bounded Contexts]]                              (bare plural link)
GOOD: [[bounded-context|Bounded Contexts]]              (id target resolves; plural cosmetic in display)
```
```

**§8 Graph View Implications (around lines 694-700):**
Replace the last bullet:
  "Red links appear in the graph as unresolved nodes, providing a visual map of knowledge gaps."
With:
  "Red links (piped links whose `id` target does not exist yet) appear as unresolved nodes, providing a visual map of knowledge gaps."

Also update the first bullet of Graph View Implications:
Replace:
  "Only wikilinks in page body text appear reliably in Obsidian's graph view."
With:
  "Only wikilinks in page body text appear in Obsidian's graph view. Piped links `[[id|Title]]` display the `title` in reading view while forming a graph edge to the `id` target."

Delete the third bullet about "First-mention linking prevents link noise" (keep content meaning but update wording):
Keep it as: "First-mention linking prevents link noise. A page that mentions a concept many times creates only one graph edge, not many."

**§15 Obsidian section (around lines 1647-1652):**
Replace:
  "- **Graph View:** Visualize the wiki's link structure. Only meaningful links appear because Section 8 enforces first-mention linking and prohibits display aliases."
With:
  "- **Graph View:** Visualize the wiki's link structure. All intra-wiki links use piped form `[[id|Title]]` — the `id` target resolves reliably in Obsidian (filename/path only), and the `title` displays in reading view."

Replace:
  "- **Aliases:** The `aliases` frontmatter field enables Obsidian to resolve alternative page names automatically, supporting the exact-title wikilink convention (Section 8)."
With:
  "- **Aliases:** The `aliases` frontmatter field is OPTIONAL — useful for Quick Switcher / autocomplete and as display text in piped links `[[file|Alias]]`, but NOT used for bare `[[X]]` resolution (filename/path only)."

**§16 Appendix C Quick Reference (around line 1682):**
Replace:
  "3. **`[[Exact Title]]` on first mention only.** No display aliases. No repeated links. No wikilinks in frontmatter."
With:
  "3. **`[[id|Exact Title]]` on first mention only.** Piped form only — target = page `id`, display = exact canonical `title`. No bare `[[Title]]` links. No repeated links. No wikilinks in frontmatter."
  </action>
  <verify>
    <automated>
      grep -c "filename/path ONLY" CLAUDE.md | grep -v "^0$"
      grep -c "\[\[id|Exact Title\]\]" CLAUDE.md | grep -v "^0$"
      grep -v '^#' CLAUDE.md | grep -c "title ∈ aliases\|self-alias invariant.*LINK-02\|aliases ⊇" | grep "^0$"
      grep -c "DO NOT use display aliases" CLAUDE.md | grep "^0$"
      grep -c "DO NOT write bare" CLAUDE.md | grep -v "^0$"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -c "filename/path ONLY" CLAUDE.md returns >= 3 (appears in §3, §5, §8)
    - grep -c "ALWAYS write" CLAUDE.md and the result includes "[[id|Exact Title]]" nearby
    - grep -c "self-alias invariant" CLAUDE.md returns 0 (removed from checklist + rules)
    - grep -c "title is a literal member of aliases" CLAUDE.md returns 0 (checklist item 18 removed)
    - grep -c "id slug is a literal member of aliases" CLAUDE.md returns 0 (checklist item 19 removed)
    - grep -c "DO NOT use display aliases" CLAUDE.md returns 0 (old §3 rule removed)
    - The §8 bad/good examples block contains "[[attention-mechanism|Attention Mechanism]]" as a GOOD example
    - The §8 bad/good examples block contains "[[hack-agentive-stack|Hack (Agentive Stack)]]" as a GOOD example
  </acceptance_criteria>
  <done>CLAUDE.md §3/§5/§8/§15/§16 reflect the piped-link convention with zero self-alias mandate language.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mirror §5/§8 edits into schema/AGENTS.template.md + all templates + sync AGENTS.md</name>
  <files>
    schema/AGENTS.template.md,
    schema/templates/entity.md,
    schema/templates/concept.md,
    schema/templates/overview.md,
    schema/templates/comparison.md,
    schema/templates/source-summary.md,
    schema/templates/decision.md,
    schema/obsidian/entity.md,
    schema/obsidian/concept.md,
    schema/obsidian/overview.md,
    schema/obsidian/comparison.md,
    schema/obsidian/source-summary.md,
    schema/obsidian/decision.md,
    AGENTS.md
  </files>
  <read_first>
    Read schema/AGENTS.template.md at the same line ranges as CLAUDE.md §5/§8 (the
    template is a near-copy of CLAUDE.md/AGENTS.md with {{PRIMARY_DOMAIN}} etc. tokens).
    Read schema/templates/entity.md and schema/obsidian/entity.md in full (they are short;
    confirm the self-alias comment block and FORBIDDEN PATTERNS text).
    Precedent: commit 016abe4 — every CLAUDE.md §5/§8/§11.3 edit is mirrored into
    schema/AGENTS.template.md at the corresponding sections; the tests/phase-10 §5 parity
    test and tests/phase-09.1 §4/§16 parity tests gate this.
  </read_first>
  <action>
**Step A — schema/AGENTS.template.md:**
Apply the IDENTICAL edits made to CLAUDE.md in Task 1 to schema/AGENTS.template.md.
The template uses {{PRIMARY_DOMAIN}}, {{DEFAULT_PRIVACY}}, etc. tokens in the frontmatter
section — those are irrelevant to §5/§8 and should be left unchanged. Only the §3 MUST NOT
list, §5 title/aliases descriptions, §5 checklist items 18-19, §8 rules, §8 bad/good examples,
§8 graph implications, §15 Obsidian note, and §16 quick reference need updating.

**Step B — schema/templates/*.md (all six files: entity.md, concept.md, overview.md,
comparison.md, source-summary.md, decision.md):**

For EACH file, make these two changes:

1. Remove the self-alias comment block inside the `aliases:` frontmatter section. The current
   text inside the frontmatter looks like:
   ```
   aliases:
     # Self-alias invariant (AGENTS.md §8): add your page title and id slug here
     # so [[Title]] resolves in Obsidian. See §8 rule 4a for the full explanation.
     # Example: if title is "<Page Title>" and id is "<page-id-slug>", add both:
     #   - "<Page Title>"
     #   - <page-id-slug>
   ```
   Replace with:
   ```
   aliases:
     # Optional: genuine alternate names for Quick Switcher / autocomplete (not needed for link resolution)
   ```

2. In the FORBIDDEN PATTERNS comment block, replace:
   "- No display aliases: write [[Page Title]] not [[Page Title|Alias]]"
   With:
   "- No bare [[Title]] links: ALWAYS write [[id|Exact Title]] (target=id, display=title)"

3. In the `## Related Pages` or `## Sources` section comments where they mention
   `[[Page Title]]` format, update to say `[[page-id|Page Title]]` format.
   - In entity.md, concept.md, overview.md "Related Pages" section comment:
     Replace: "Use [[Page Title]] format. Do NOT use display aliases like [[Page|Alias]]."
     With: "Use [[page-id|Page Title]] format — target=id, display=exact title."
   - In all files where the Sources section comment says:
     "Use [[Exact Page Title]] per AGENTS.md §8 rule 1 (NOT [[src-YYYY-MM-DD-slug]])."
     Replace with:
     "Use [[src-YYYY-MM-DD-slug|Exact Page Title]] per AGENTS.md §8 — piped: target=id, display=title."

**Step C — schema/obsidian/*.md (all six files):**
Apply the same two FORBIDDEN PATTERNS and Related Pages/Sources comment changes as Step B.
The obsidian templates are tokenized ({{title}}, {{date:YYYY-MM-DD}}) but have the same
FORBIDDEN PATTERNS comment block.

**Step D — sync AGENTS.md:**
Run: `bash bin/sync-claude.sh`
This copies CLAUDE.md → AGENTS.md, making them byte-identical.
Then run: `bash bin/sync-claude.sh --check` to verify exit 0.
  </action>
  <verify>
    <automated>
      grep -r "Self-alias invariant" schema/templates/ schema/obsidian/ | wc -l  # must be 0
      bash bin/sync-claude.sh --check; echo "sync exit: $?"  # must be "sync exit: 0"
      grep -c "filename/path ONLY" schema/AGENTS.template.md  # must be >= 3
      grep -c "self-alias invariant" schema/AGENTS.template.md  # must be 0
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -r "Self-alias invariant" schema/templates/ schema/obsidian/ returns empty (0 hits)
    - grep -r "No display aliases: write \[\[Page Title\]\]" schema/templates/ schema/obsidian/ returns 0 hits
    - grep -r "No bare \[\[Title\]\] links" schema/templates/ schema/obsidian/ returns >= 6 hits
    - bash bin/sync-claude.sh --check exits 0 (AGENTS.md byte-identical to CLAUDE.md)
    - grep -c "filename/path ONLY" schema/AGENTS.template.md returns >= 3
    - grep -c "self-alias invariant" schema/AGENTS.template.md returns 0
    - grep -c "title is a literal member of aliases" schema/AGENTS.template.md returns 0
  </acceptance_criteria>
  <done>All schema surfaces (AGENTS.template.md, 6 schema/templates/, 6 schema/obsidian/) reflect the piped-link convention; AGENTS.md is byte-identical to CLAUDE.md.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author superseding DR and mark old DR superseded</name>
  <files>
    wiki/decisions/dr-2026-06-03-uniform-piped-links.md,
    wiki/decisions/dr-2026-06-02-obsidian-filename-alias-resolution.md,
    wiki/index.md,
    wiki/log.md
  </files>
  <read_first>
    Read wiki/decisions/dr-2026-06-02-obsidian-filename-alias-resolution.md in full
    (to understand what it says, its id, and to set superseded_by on it).
    Read wiki/index.md to find where to add the new DR entry under "Decisions".
    Read wiki/log.md to find the append point (bottom of file).
    Precedent for DR structure: read wiki/decisions/dr-2026-04-14-phase6-decision-type.md
    as a canonical filled-in example.
  </read_first>
  <action>
**Step A — Create wiki/decisions/dr-2026-06-03-uniform-piped-links.md:**

Write the complete file with all required fields and sections. Use ONLY placeholder-neutral
language in the body (§3 neutrality: no real vault terms in template-public files).

NOTE: This decision record lives in wiki/ (not a template-public file), so it may reference
real page ids from the wiki. Specifically, it MUST reference the old DR it supersedes and
list affected wiki pages.

Frontmatter:
```yaml
---
id: dr-2026-06-03-uniform-piped-links
title: "Uniform Piped Links: Correcting the Obsidian Link Resolution Convention"
type: decision
status: active
summary: "Corrects CLAUDE.md/AGENTS.md §8 to state Obsidian resolves wikilinks by filename/path ONLY (never aliases); mandates uniform piped-link form [[id|Title]]; removes the self-alias invariant; supersedes the wrong-premise dr-2026-06-02-obsidian-filename-alias-resolution."
created_at: 2026-06-03
updated_at: 2026-06-03
sources: []
epistemic_status: sourced
tags:
  - meta
  - schema
domains:
  - wiki-infrastructure
supersedes: dr-2026-06-02-obsidian-filename-alias-resolution
superseded_by:
privacy: cloud_safe
aliases:
  - "Uniform Piped Links: Correcting the Obsidian Link Resolution Convention"
  - dr-2026-06-03-uniform-piped-links
has_contradictions: false
knowledge_domain: software
trigger_type: schema-update
affected_pages:
  - dr-2026-06-02-obsidian-filename-alias-resolution
---
```

Body sections (all 7 required sections):

```markdown
## TL;DR

Phase 14 (self-aliases) was built on the false premise that Obsidian resolves `[[X]]` by
filename stem + `aliases`. Confirmed false at v1.12.7 (intentional Obsidian design): resolution
is **filename/path ONLY**. The fix: adopt **uniform piped links** `[[id|Title]]` everywhere —
target = page `id` (= filename, always resolves), display = canonical `title`. §8 is inverted,
the self-alias invariant is removed, and `linkres` is re-pointed to enforce resolvable link targets.

## Decision

Adopt **uniform piped links** `[[id|Exact Title]]` as the mandatory intra-wiki link form:
- **Target** (before `|`): the page `id`, which equals the filename stem — always resolves in
  stock Obsidian because Obsidian resolves `[[X]]` by **filename/path ONLY**.
- **Display** (after `|`): the exact canonical `title` from the page's frontmatter.
- The convention is **unconditional** — every intra-wiki body link is piped, including
  single-word-title pages (e.g. `[[backpressure|Backpressure]]`).

`CLAUDE.md`/`AGENTS.md` §8 is rewritten to mandate this form and state the real resolution rule.
The self-alias invariant (`aliases ⊇ {title, id}`) is removed from §5 and §8 — `aliases` reverts
to an optional field for genuine alternate names (Quick Switcher / autocomplete), not for resolution.
`bin/lint.sh`'s `linkres` category is re-pointed to validate that every link target is a known page
`id` (bare `[[X]]` without a pipe is a `linkres` error).

## Why

The self-alias approach (Phase 14 plans 01/02/03) was invalidated at the LINK-10 human-verify gate:
after 53 self-aliases were added, the Obsidian graph still showed multi-word-title pages as orphans.
Decisive probe: clicking `[[Domain-Driven Design]]` in a linked page → unresolved (red). Quick
Switcher DID surface the alias — confirming aliases power autocomplete, not link resolution.

Obsidian moderator confirmation for v1.12.7: "not a bug… it's how it has always worked and it's an
intentional design decision." Source: forum.obsidian.md/t/wikilink-resolution-does-not-honor-frontmatter-aliases-1-12-7/113902

The piped-link approach was chosen because it:
1. Uses only the stable, documented filename/path mechanism — no undocumented API dependency.
2. Aligns with the existing `id == filename` invariant (§5).
3. Makes the §8 rule **unconditional** — agents never evaluate a per-link condition.
4. Makes `linkres` validation trivial: "is the target before `|` a known page `id`?" — exact match.
5. Dissolves the variant problem (plural/parens/casing live in cosmetic display text, not the target).
6. The rewrite cost (paid once by a script; humans see the clean display title in reading view).

## Alternatives Considered

1. **Self-aliases (original approach)** — REJECTED: does not connect the graph. Obsidian ignores
   `aliases` for bare `[[X]]` resolution. The 53 aliases added by Phase 14-03 are harmless but
   useless for connectivity; they stay as vestigial residue (Quick Switcher convenience).

2. **Bundle a small Obsidian resolver plugin** — REJECTED: violates §1 ("functions as plain markdown
   regardless of tooling"); the plugin injects into `metadataCache.uniqueFileLookup` — an undocumented
   internal API that breaks on Obsidian updates; "Obsidian plugin distribution" is v1.2-deferred per
   PROJECT.md. Adds an unofficial dependency to every clone of the template.

3. **Bare slug links `[[id]]` (no display text)** — REJECTED: resolves, but Obsidian displays the raw
   slug in reading view (e.g. "domain-driven-design" instead of "Domain-Driven Design") — unreadable.
   Previously rejected in `dr-2026-06-02-obsidian-filename-alias-resolution`.

4. **Rename wiki files to their titles** — REJECTED: breaks the `id == filename` invariant (§5),
   provenance source IDs (every `[prov:source_id#...]` marker), and all slug-based tooling.
   Previously rejected in `dr-2026-06-02-obsidian-filename-alias-resolution`.

## Consequences

- §8 is an unconditional rule: agents always write `[[id|Title]]`, never bare `[[Title]]`.
- `aliases` field is optional — removing the mandate does not break existing pages (the 53 self-
  aliases remain harmless; future pages omit them unless genuinely needed for alternate names).
- `linkres` re-pointed: errors on bare `[[X]]` and piped `[[target|...]]` where target is not a
  known `id`. Red links (not-yet-existing `id` targets) remain `gap`/info per §3.
- Body links in `wiki/` + `examples/` require a one-time rewrite to piped form (Phase 14 Wave 2).
- The v1.2 schema progressive-disclosure refactor (999.4) is correctly sequenced AFTER this fix.

## Affected Pages

- [[Obsidian Filename + Alias Resolution: Self-Alias Invariant]] — superseded by this decision;
  its `superseded_by` field is updated to `dr-2026-06-03-uniform-piped-links`.

## Sources

No external sources. The decision is grounded in:
- Obsidian moderator confirmation: https://forum.obsidian.md/t/wikilink-resolution-does-not-honor-frontmatter-aliases-1-12-7/113902
- Official Obsidian aliases docs: https://obsidian.md/help/aliases
- Phase 14 LINK-10 human-verify gate failure (2026-06-03)
```

**Step B — Update wiki/decisions/dr-2026-06-02-obsidian-filename-alias-resolution.md:**
Set these fields in the frontmatter:
- `status: superseded`
- `superseded_by: dr-2026-06-03-uniform-piped-links`

Add a redirect note at the top of the body (after frontmatter, before the FORBIDDEN PATTERNS comment):
```
> This decision record has been superseded by [[dr-2026-06-03-uniform-piped-links|Uniform Piped Links: Correcting the Obsidian Link Resolution Convention]]. The premise stated here (Obsidian resolves [[X]] by filename stem + aliases) was proven false at v1.12.7.
```

**Step C — Update wiki/index.md:**
Find the Decisions section and add the new DR entry:
`- [[dr-2026-06-03-uniform-piped-links|Uniform Piped Links: Correcting the Obsidian Link Resolution Convention]] -- Corrects §8 to mandate uniform [[id|Title]] piped links; supersedes the wrong-premise self-alias DR. (sourced, 2026-06-03)`

**Step D — Append to wiki/log.md:**
```
## [2026-06-03] reflect | Phase 14 schema-update DR

reflect recommended: schema-update — Phase 14 re-plan from corrected premise (piped links)
pages_affected: dr-2026-06-03-uniform-piped-links, dr-2026-06-02-obsidian-filename-alias-resolution
```
  </action>
  <verify>
    <automated>
      grep -c "supersedes: dr-2026-06-02-obsidian-filename-alias-resolution" wiki/decisions/dr-2026-06-03-uniform-piped-links.md
      grep "superseded_by" wiki/decisions/dr-2026-06-02-obsidian-filename-alias-resolution.md | grep "dr-2026-06-03"
      grep "trigger_type: schema-update" wiki/decisions/dr-2026-06-03-uniform-piped-links.md
      grep "dr-2026-06-03-uniform-piped-links" wiki/index.md
    </automated>
  </verify>
  <acceptance_criteria>
    - wiki/decisions/dr-2026-06-03-uniform-piped-links.md exists and has all 7 required sections (TL;DR, Decision, Why, Alternatives Considered, Consequences, Affected Pages, Sources)
    - grep "supersedes: dr-2026-06-02-obsidian-filename-alias-resolution" wiki/decisions/dr-2026-06-03-uniform-piped-links.md returns 1 hit
    - grep "superseded_by: dr-2026-06-03-uniform-piped-links" wiki/decisions/dr-2026-06-02-obsidian-filename-alias-resolution.md returns 1 hit
    - grep "status: superseded" wiki/decisions/dr-2026-06-02-obsidian-filename-alias-resolution.md returns 1 hit
    - grep "dr-2026-06-03-uniform-piped-links" wiki/index.md returns at least 1 hit
  </acceptance_criteria>
  <done>New DR exists, supersedes the old DR, both DRs have correct supersedes/superseded_by fields, DR is in wiki/index.md, log entry appended.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| text editor → CLAUDE.md | Text edits to spec; no untrusted input |
| template files → lint.sh parsing | Templates are developer-authored; no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Tampering | CLAUDE.md §5 checklist edit | mitigate | Pre-commit sync hook ensures AGENTS.md stays byte-identical; any divergence fails CI before merge |
| T-14-01-02 | Repudiation | DR supersession chain | mitigate | Both DRs carry bidirectional supersedes/superseded_by fields; wiki/log.md captures the reflect entry |
| T-14-01-03 | Information Disclosure | DR body mentioning real vault terms | accept | DR body correctly references real wiki page ids (not template-public); §3 neutrality only applies to template-public files |
| T-14-01-04 | Denial of Service | Incomplete checklist removal corrupting §5 | mitigate | Acceptance criteria verify by grep that both old items 18/19 are gone and the new item 18 is present |
</threat_model>

<verification>
After all three tasks:

1. `bash bin/sync-claude.sh --check` exits 0 — AGENTS.md byte-identical to CLAUDE.md
2. `grep -c "filename/path ONLY" CLAUDE.md` returns >= 3
3. `grep -c "self-alias invariant" CLAUDE.md` returns 0
4. `grep -c "ALWAYS write" CLAUDE.md | grep -v "^0$"` — the mandate appears
5. `grep -c "title is a literal member of aliases" CLAUDE.md` returns 0
6. `grep -c "id slug is a literal member of aliases" CLAUDE.md` returns 0
7. `grep -r "Self-alias invariant" schema/templates/ schema/obsidian/` returns empty
8. `grep "superseded_by: dr-2026-06-03" wiki/decisions/dr-2026-06-02-obsidian-filename-alias-resolution.md` returns a hit
9. `grep "dr-2026-06-03-uniform-piped-links" wiki/index.md` returns a hit
10. `diff <(grep -v "^$" CLAUDE.md) <(grep -v "^$" AGENTS.md)` returns empty (byte-identical)
</verification>

<success_criteria>
- §8 rule 1 mandates `[[id|Exact Title]]` and correctly states filename/path resolution
- §8 rule 3 says ALWAYS piped (replacing DO NOT display aliases)
- §8 bad/good examples show piped form as GOOD and bare form as BAD
- §5 checklist no longer contains the self-alias invariant (items 18-19 removed/replaced)
- schema/AGENTS.template.md mirrors all §5/§8 edits
- All 6 schema/templates/*.md and 6 schema/obsidian/*.md have updated FORBIDDEN PATTERNS + Related Pages comments
- AGENTS.md byte-identical to CLAUDE.md (bin/sync-claude.sh --check exits 0)
- New DR dr-2026-06-03-uniform-piped-links.md exists with all 7 required sections + all base frontmatter
- Old DR dr-2026-06-02-obsidian-filename-alias-resolution.md has status: superseded + superseded_by: dr-2026-06-03-uniform-piped-links
</success_criteria>

<output>
After completion, create `.planning/phases/14-graph-link-resolution/14-01-SUMMARY.md`
</output>
