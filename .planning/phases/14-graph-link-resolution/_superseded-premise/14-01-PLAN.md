---
phase: 14-graph-link-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AGENTS.md
  - CLAUDE.md
  - schema/templates/concept.md
  - schema/templates/entity.md
  - schema/templates/overview.md
  - schema/templates/comparison.md
  - schema/templates/source-summary.md
  - schema/templates/decision.md
  - schema/obsidian/concept.md
  - schema/obsidian/entity.md
  - schema/obsidian/overview.md
  - schema/obsidian/comparison.md
  - schema/obsidian/source-summary.md
  - schema/obsidian/decision.md
  - wiki/decisions/dr-2026-06-02-obsidian-filename-alias-resolution.md
  - wiki/index.md
  - wiki/log.md
autonomous: true
requirements:
  - LINK-01
  - LINK-02
  - LINK-03

must_haves:
  truths:
    - "AGENTS.md §8 states that Obsidian resolves [[X]] by filename stem + aliases, never by the title frontmatter field"
    - "AGENTS.md §8 rule 4 no longer claims Obsidian auto-resolves aliases to the canonical page; rule 4a mandates the LITERAL self-alias invariant (every page's `aliases` MUST include its `title` and `id` slug — matching LINK-02 and what `bin/lint.sh --fix` backfills)"
    - "AGENTS.md §5 frontmatter validation checklist items 18 and 19 require title and id to be LITERAL members of `aliases` (per LINK-02), and explain that this guarantees Obsidian resolution (since the alias is what Obsidian matches)"
    - "All 6 schema/templates/*.md files ship the self-alias invariant comment in their aliases block"
    - "All 6 schema/obsidian/*.md files seed the TITLE self-alias via `aliases: - {{title}}` (Obsidian/Templater fills the title on page creation; the literal id slug is added by the first `bin/lint.sh --fix` — the obsidian templates seed one of the two literal aliases, NOT the full invariant, since `{{title}}` cannot expand to the id-derived slug at template-creation time)"
    - "Decision record dr-2026-06-02-obsidian-filename-alias-resolution exists with trigger_type: schema-update and affected_pages: []"
    - "The decision record is listed in wiki/index.md under Decisions and appended to wiki/log.md"
    - "AGENTS.md §11.3 CI-mode severity-remap row lists linkres in the error group (yaml/orphan/crossref/provenance/linkres -> error), keeping the source-of-truth table consistent with docs/reference/ci.md (edited in Plan 02) — MEDIUM #4 schema-drift fix"
    - "bash bin/sync-claude.sh --check exits 0 (AGENTS.md byte-identical to CLAUDE.md)"
  artifacts:
    - path: "AGENTS.md"
      provides: "Corrected §8 Obsidian resolution rule + self-alias mandate + §5 checklist items 18–19"
      contains: "filename.*aliases"
    - path: "CLAUDE.md"
      provides: "Byte-identical mirror of AGENTS.md (copied by bin/sync-claude.sh)"
      contains: "filename.*aliases"
    - path: "schema/templates/concept.md"
      provides: "Self-alias comment in aliases block"
      contains: "Self-alias invariant"
    - path: "schema/obsidian/concept.md"
      provides: "aliases: - {{title}} auto-fill"
      contains: "{{title}}"
    - path: "wiki/decisions/dr-2026-06-02-obsidian-filename-alias-resolution.md"
      provides: "schema-update decision record for the Obsidian resolution convention fix"
      contains: "trigger_type: schema-update"
    - path: "wiki/index.md"
      provides: "Decision record entry under Decisions"
      contains: "dr-2026-06-02-obsidian-filename-alias-resolution"
    - path: "wiki/log.md"
      provides: "reflect log entry for this schema-update decision"
      contains: "schema-update"
  key_links:
    - from: "AGENTS.md §8 rule 4a"
      to: "schema/templates/*.md aliases block"
      via: "templates reference §8 in their self-alias comment"
      pattern: "Self-alias invariant"
    - from: "wiki/decisions/dr-2026-06-02-obsidian-filename-alias-resolution.md"
      to: "wiki/index.md"
      via: "index entry under Decisions section"
      pattern: "dr-2026-06-02-obsidian-filename-alias-resolution"
    - from: "AGENTS.md"
      to: "CLAUDE.md"
      via: "bash bin/sync-claude.sh byte-copy (AGENTS.md is source, CLAUDE.md is destination)"
      pattern: "bash bin/sync-claude.sh --check"
---

<objective>
Correct the false Obsidian resolution claim in AGENTS.md §8 and §5, mandate the self-alias invariant, update all 12 schema templates, author the schema-update decision record, register it, and sync CLAUDE.md.

Purpose: The wiki spec currently claims "Wikilinks resolve to this `title` value" — a statement that is false for Obsidian. Obsidian resolves [[X]] by filename stem + aliases, never by title. 31/49 wiki pages render as graph orphans because they have slug filenames (`domain-driven-design.md`) but no `title` alias. This plan fixes the spec so the truth is documented; Plan 02 (Wave 1, parallel) enforces it in the linter; Plan 03 (Wave 2) remediates the data.

Output: Corrected AGENTS.md §5 + §8, CLAUDE.md byte-identical mirror (via sync), 12 schema templates with self-alias guidance, and a `trigger_type: schema-update` decision record authored/registered/logged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key contracts the executor needs. -->

SYNC DIRECTION (mandatory read before any edit):
  bin/sync-claude.sh: SRC="AGENTS.md", DST="CLAUDE.md", cp "$SRC" "$DST"
  AGENTS.md is the mechanical source of truth.
  CLAUDE.md is the byte-copy destination.
  EDIT AGENTS.md — never CLAUDE.md directly.
  After editing AGENTS.md, propagate with: bash bin/sync-claude.sh && git add CLAUDE.md AGENTS.md

From AGENTS.md §8 (current — to be corrected):
```
Bad claim at line ~657: "Obsidian resolves aliases to the canonical page automatically"
Bad claim in §5 title row (~line 288): "Wikilinks resolve to this value."
```

Target §8 rule 4 correction (replace the misleading 2nd sentence) + rule 4a (insert after):
```
4. Use the `aliases` frontmatter field for alternate names. Obsidian resolves `[[X]]` by matching the filename stem or an `aliases` entry (case-insensitively) — never by the `title` frontmatter field.
4a. Every page MUST be self-aliased: its `aliases` list MUST include both its
    `title` and its `id` slug as literal entries (the self-alias invariant,
    LINK-02). Obsidian resolves `[[X]]` by **filename stem + `aliases`**, NEVER by
    the `title` frontmatter field — so listing the title as an alias is what makes
    `[[Title]]` resolve. Without the self-alias, `[[Title]]` renders as an
    unresolved red link even though a page with that title exists. `bin/lint.sh
    --fix` backfills both entries; `bin/lint.sh --category linkres` flags any page
    whose title or id is unreachable.
```

Target §5 title row correction:
```
| `title` | string | Human-readable canonical title. Used in page headings and the self-alias (§8).
  Obsidian resolves `[[X]]` by **filename stem + `aliases`**, not by this field. |
```

Target §5 checklist items 18–19 (append after current item 17). NOTE (Cycle-2 review,
orchestrator-confirmed): phrase as LITERAL ALIAS MEMBERSHIP — this matches LINK-02's locked
wording ("every page's `aliases` MUST include its `title` and `id` slug") AND what
`bin/lint.sh --fix` now backfills (literal title + id, computed against existing alias
membership, so the id is added even when `id == filename`). The Cycle-1 "reachability" phrasing
drifted from LINK-02 and would have left `--fix` and the checklist describing different
invariants; literal membership is the single source of truth:
```
18. The `title` is a LITERAL member of `aliases`: the exact `title` string appears
    (case-insensitively) in the `aliases` list. This guarantees `[[Title]]` resolves
    in Obsidian (Obsidian matches the alias). `bin/lint.sh --fix` backfills it.
19. The `id` slug is a LITERAL member of `aliases`: the exact `id` string appears in
    the `aliases` list — even though `id == filename` makes it stem-reachable, the
    literal self-alias invariant (LINK-02) requires it explicitly so `[[slug]]` and the
    self-referential graph edge are unambiguous. `bin/lint.sh --fix` backfills it.
    Together 18–19 are the self-alias invariant: `aliases ⊇ {title, id}`.
```

Target Bad/Good example to add in §8 (use abstract placeholders per neutrality rule):
```
BAD:  aliases: []                   (empty — [[<page-title>]] will not resolve in Obsidian)
GOOD: aliases:
        - "<Page Title>"
        - <page-id-slug>            (self-aliases guarantee Obsidian resolution)
```

schema/templates/*.md target (all 6 files, replace `aliases: []`):
```yaml
aliases:
  # Self-alias invariant (AGENTS.md §8): add your page title and id slug here
  # so [[Title]] resolves in Obsidian. See §8 rule 4a for the full explanation.
  # Example: if title is "<Page Title>" and id is "<page-id-slug>", add both:
  #   - "<Page Title>"
  #   - <page-id-slug>
```

schema/obsidian/*.md target (all 6 files, replace `aliases: []`):
```yaml
aliases:
  # Self-alias invariant (AGENTS.md §8 rule 4a): aliases MUST include both the page
  # title AND the id slug. The {{title}} placeholder below seeds only the TITLE alias
  # (Obsidian/Templater expands it on page creation); the literal id slug cannot be
  # expanded here, so it is added by the first `bin/lint.sh --fix`. These templates
  # therefore seed ONE of the two literal aliases, not the full invariant.
  - {{title}}
```
(Note: this is a deliberate, documented limitation — `{{title}}` is a Templater-style
title placeholder, and there is no equivalent placeholder that yields the id-derived
filename slug at template-creation time. `--fix` closes the gap on first lint. Do NOT
claim these obsidian templates ship the complete `aliases ⊇ {title, id}` invariant.)

Decision record slug: dr-2026-06-02-obsidian-filename-alias-resolution
Decision record frontmatter:
```yaml
trigger_type: schema-update
affected_pages: []
```

Sync command (MUST run after every edit to AGENTS.md):
```bash
bash bin/sync-claude.sh && git add CLAUDE.md AGENTS.md
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Correct AGENTS.md §5 + §8 and sync CLAUDE.md</name>
  <read_first>
    bin/sync-claude.sh (already verified: SRC="AGENTS.md", DST="CLAUDE.md", cp "$SRC" "$DST" — AGENTS.md is the source; edit AGENTS.md, never CLAUDE.md directly)
    AGENTS.md (full file — read before any edit to locate exact lines for §5 title-row, §5 checklist, §8 rules, §8 Bad/Good examples, §8 Graph View Implications)
    CLAUDE.md (read first line or sha to confirm it is currently byte-identical to AGENTS.md before editing — after sync they will diverge until sync runs again)
  </read_first>
  <files>AGENTS.md, CLAUDE.md</files>
  <action>
**SYNC RULE (MANDATORY):** AGENTS.md is the source of truth. bin/sync-claude.sh copies AGENTS.md → CLAUDE.md. Make ALL edits in AGENTS.md. After editing, run `bash bin/sync-claude.sh && git add CLAUDE.md AGENTS.md` so CLAUDE.md picks up the changes. Never edit CLAUDE.md directly.

**NEUTRALITY RULE (MANDATORY):** All added prose in AGENTS.md must use abstract placeholders (`<page-title>`, `<concept-slug>`, `<page-id-slug>`, `<Page Title>`) — NEVER real vault terms like "Domain-Driven Design", "Bounded Context", "hack-agentive-stack".

**Step 1 — Read AGENTS.md completely first.** Identify:
- The `| \`title\` | string | Human-readable canonical title. Wikilinks resolve to this value. |` row in §5 Field Descriptions table (approximately line 288)
- The end of §5 Frontmatter Validation Checklist (currently ends at item 17: `If decision_history is present...`)
- §8 rule 4: `Use the \`aliases\` frontmatter field for alternate names. Obsidian resolves aliases to the canonical page automatically.` — the second sentence is the false claim about Obsidian automatic resolution; keep the first sentence, add rule 4a after rule 4.
- §8 "Bad vs. Good Wikilink Examples" block — add the `aliases: []` bad/good example at the end of this block.
- §11.3 "CI mode" `--ci` severity-remap row (line ~1074) — the source-of-truth table; add `linkres` to the `error` group (Edit E).
- §8 Rule 4 (AGENTS.md line ~657): `Use the \`aliases\` frontmatter field for alternate names. Obsidian resolves aliases to the canonical page automatically.` — the SECOND sentence is the misleading claim RESEARCH flagged (it implies title-based auto-resolution). Edit C below corrects rule 4's second sentence AND inserts rule 4a, so the false framing is removed at its source, not merely neutralized downstream.

**Step 2 — Make four targeted edits to AGENTS.md:**

Edit A: §5 title row. Replace:
```
| `title` | string | Human-readable canonical title. Wikilinks resolve to this value. |
```
With:
```
| `title` | string | Human-readable canonical title. Used in page headings and the self-alias (§8). Obsidian resolves `[[X]]` by **filename stem + `aliases`**, not by this field. |
```

Edit B: §5 Frontmatter Validation Checklist. After item 17 (`If \`decision_history\` is present on any page: it is a YAML list of string IDs`), append (LITERAL ALIAS MEMBERSHIP wording — Cycle-2 review: matches LINK-02's locked invariant AND `bin/lint.sh --fix`, which backfills literal title+id; the Cycle-1 "reachability" phrasing drifted from LINK-02 and is replaced):
```
18. The `title` is a literal member of `aliases`: the exact `title` string appears (case-insensitively) in the `aliases` list, so `[[Title]]` resolves in Obsidian. `bin/lint.sh --fix` backfills it.
19. The `id` slug is a literal member of `aliases`: the exact `id` string appears in the `aliases` list — even though `id == filename` makes it stem-reachable, the self-alias invariant (LINK-02) requires it explicitly. Together 18–19 are the self-alias invariant: `aliases ⊇ {title, id}`. `bin/lint.sh --fix` backfills it.
```

Edit C: §8 "Rules" section. (i) First REPLACE the misleading rule 4 so its second sentence states the real mechanism. Replace:
```
4. Use the `aliases` frontmatter field for alternate names. Obsidian resolves aliases to the canonical page automatically.
```
With:
```
4. Use the `aliases` frontmatter field for alternate names. Obsidian resolves `[[X]]` by matching the filename stem or an `aliases` entry (case-insensitively) — never by the `title` frontmatter field.
```
(ii) Then insert new rule 4a immediately after the corrected rule 4:
```
4a. Every page MUST be self-aliased: its `aliases` list MUST include both its
    `title` and its `id` slug as literal entries (the self-alias invariant,
    LINK-02). Obsidian resolves `[[X]]` by **filename stem + `aliases`**, NEVER by
    the `title` frontmatter field — so listing the title as an alias is what makes
    `[[Title]]` resolve. Without the self-alias, `[[Title]]` renders as an
    unresolved red link even though a page with that title exists. `bin/lint.sh
    --fix` backfills both entries; `bin/lint.sh --category linkres` flags any page
    whose title or id is unreachable.
```

Edit D: §8 "Bad vs. Good Wikilink Examples" block. At the end of the existing examples block (after the last `GOOD:` entry), append:
```

BAD:  aliases: []                   (empty — [[<page-title>]] will not resolve in Obsidian)
GOOD: aliases:
        - "<Page Title>"
        - <page-id-slug>            (self-aliases guarantee Obsidian resolution)
```

Edit E: §11.3 "CI mode" severity-remap table (MEDIUM, Cycle-2 Codex — SCHEMA-DRIFT FIX). `docs/reference/ci.md` (edited in Plan 02, same wave) declares §11.3 "CI mode" the SOURCE OF TRUTH for the remap table; adding `linkres -> error` to `bin/lint.sh` + `ci.md` without updating §11.3 is schema drift. Plan 01 owns ALL AGENTS.md edits this wave (single-writer, avoids a parallel-write hazard with Plan 02), so the §11.3 row is edited HERE. Find the `--ci` row (line ~1074) and replace:
```
| `--ci` | Apply severity-remap dispatch table: `yaml`/`orphan`/`crossref`/`provenance` -> `error`; `stale`/`gap`/`contradiction`/`contradiction-sync`/`drift`/`contributor` -> `warning`; `autofix`/`skip-count` -> `info`. Default-skip `drift-external` category. Exit 1 iff any post-remap finding has severity `error`. |
```
With (adds `linkres` to the `error` group — only token added):
```
| `--ci` | Apply severity-remap dispatch table: `yaml`/`orphan`/`crossref`/`provenance`/`linkres` -> `error`; `stale`/`gap`/`contradiction`/`contradiction-sync`/`drift`/`contributor` -> `warning`; `autofix`/`skip-count` -> `info`. Default-skip `drift-external` category. Exit 1 iff any post-remap finding has severity `error`. |
```
This is part of the SAME AGENTS.md edit batch as Edits A–D, so the single `bash bin/sync-claude.sh` in Step 3 propagates all five edits to CLAUDE.md at once. NEUTRALITY: only the category token `linkres` is added to an existing rule list — no real vault terms.

**Step 3 — Propagate edits to CLAUDE.md via sync.** Run:
```bash
bash bin/sync-claude.sh && git add CLAUDE.md AGENTS.md
```
This copies the edited AGENTS.md → CLAUDE.md so both files are byte-identical and carry all four edits.

Verify: `bash bin/sync-claude.sh --check` must exit 0.

**Step 4 — Neutrality check.** Run:
```bash
bash bin/check-neutrality.sh
```
Must exit 0. If it exits non-zero, inspect the output, fix any leaked real vault terms in the edited sections of AGENTS.md (replace with abstract placeholders), then re-sync (`bash bin/sync-claude.sh && git add CLAUDE.md AGENTS.md`) and re-check.
  </action>
  <verify>
    <automated>
grep -q "filename stem" /home/yishai/Documents/compendium/AGENTS.md && echo "PASS: filename-stem claim present in AGENTS.md" || echo "FAIL"
grep -q "filename.*aliases" /home/yishai/Documents/compendium/AGENTS.md && echo "PASS: filename+aliases rule present in AGENTS.md" || echo "FAIL"
grep -q "Obsidian resolves aliases to the canonical page automatically" /home/yishai/Documents/compendium/AGENTS.md && echo "FAIL: misleading rule-4 sentence still present" || echo "PASS: misleading rule-4 auto-resolution sentence removed"
grep -n "title.*aliases\|aliases.*title" /home/yishai/Documents/compendium/AGENTS.md | head -5
grep "Apply severity-remap dispatch table" /home/yishai/Documents/compendium/AGENTS.md | grep -q "linkres" && echo "PASS: linkres in §11.3 --ci remap (MEDIUM #4)" || echo "FAIL: §11.3 remap row missing linkres"
bash /home/yishai/Documents/compendium/bin/sync-claude.sh --check && echo "PASS: AGENTS.md byte-identical to CLAUDE.md" || echo "FAIL: sync-claude check failed"
bash /home/yishai/Documents/compendium/bin/check-neutrality.sh && echo "PASS: neutrality clean" || echo "FAIL: neutrality violation"
    </automated>
  </verify>
  <done>AGENTS.md §5 title-row no longer claims "Wikilinks resolve to this value"; §5 checklist has items 18 and 19 phrased as LITERAL ALIAS MEMBERSHIP (`aliases ⊇ {title, id}`, matching LINK-02 and `bin/lint.sh --fix`); §8 rule 4's misleading "Obsidian resolves aliases to the canonical page automatically" sentence is REPLACED with the filename-stem-or-alias mechanism, and rule 4a mandates the LITERAL self-alias invariant; §8 Bad/Good examples include the aliases example with abstract placeholders; `bash bin/sync-claude.sh` has been run so CLAUDE.md is byte-identical to AGENTS.md; `bash bin/sync-claude.sh --check` exits 0; `bash bin/check-neutrality.sh` exits 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update 12 schema templates with self-alias guidance</name>
  <read_first>
    schema/templates/concept.md (read completely — locate the `aliases: []` line, confirm surrounding YAML context, check line count)
    schema/obsidian/concept.md (read completely — locate `aliases: []`, confirm `{{title}}` is already used for `title:` field above so the pattern is consistent)
    AGENTS.md §3 "What Agents Must NOT Do" (neutrality rule — placeholders only in template-public files)
  </read_first>
  <files>
    schema/templates/concept.md,
    schema/templates/entity.md,
    schema/templates/overview.md,
    schema/templates/comparison.md,
    schema/templates/source-summary.md,
    schema/templates/decision.md,
    schema/obsidian/concept.md,
    schema/obsidian/entity.md,
    schema/obsidian/overview.md,
    schema/obsidian/comparison.md,
    schema/obsidian/source-summary.md,
    schema/obsidian/decision.md
  </files>
  <action>
**NEUTRALITY RULE (MANDATORY):** These are template-public files. All examples in comments must use abstract placeholders `<Page Title>` and `<page-id-slug>` — never real vault terms.

**For all 6 `schema/templates/*.md` files** (concept.md, entity.md, overview.md, comparison.md, source-summary.md, decision.md):

In each file, locate the line:
```yaml
aliases: []
```
Replace it with:
```yaml
aliases:
  # Self-alias invariant (AGENTS.md §8): add your page title and id slug here
  # so [[Title]] resolves in Obsidian. See §8 rule 4a for the full explanation.
  # Example: if title is "<Page Title>" and id is "<page-id-slug>", add both:
  #   - "<Page Title>"
  #   - <page-id-slug>
```

**For all 6 `schema/obsidian/*.md` files** (concept.md, entity.md, overview.md, comparison.md, source-summary.md, decision.md):

In each file, locate the line:
```yaml
aliases: []
```
Replace it with:
```yaml
aliases:
  - {{title}}
```
(The `{{title}}` Obsidian template placeholder auto-fills the page title when the template is used, seeding the TITLE self-alias automatically. The literal id slug is NOT seeded here — `{{title}}` cannot expand to the id-derived filename slug at template-creation time — so the first `bin/lint.sh --fix` adds it to complete the `aliases ⊇ {title, id}` invariant. Note: the Obsidian templates already use `{{title}}` for the `title:` frontmatter field — this is consistent with that pattern.)

**Verification after each edit:** confirm the file does not contain `aliases: []` any more and does contain the replacement text.
  </action>
  <verify>
    <automated>
for f in /home/yishai/Documents/compendium/schema/templates/*.md; do
  grep -q "Self-alias invariant" "$f" && echo "PASS: $f has self-alias comment" || echo "FAIL: $f missing self-alias comment"
done
for f in /home/yishai/Documents/compendium/schema/obsidian/*.md; do
  grep -q "{{title}}" "$f" && echo "PASS: $f has {{title}} alias" || echo "FAIL: $f missing {{title}} alias"
done
bash /home/yishai/Documents/compendium/bin/check-neutrality.sh && echo "PASS: neutrality clean after template edits" || echo "FAIL"
    </automated>
  </verify>
  <done>All 6 `schema/templates/*.md` files have the self-alias invariant comment block replacing `aliases: []`; all 6 `schema/obsidian/*.md` files have `aliases:\n  - {{title}}` replacing `aliases: []`; no file still contains the bare `aliases: []` form; `bash bin/check-neutrality.sh` exits 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author decision record, register in index, log, and commit</name>
  <read_first>
    wiki/decisions/dr-2026-04-14-phase6-decision-type.md (analog DR: `trigger_type: schema-update`, `affected_pages: []`, FORBIDDEN PATTERNS comment, 7-section structure — read completely as the exact template to mirror)
    wiki/index.md (read completely — find the Decisions section to insert the new DR entry; confirm format: `- [[DR Title]] -- <one-line summary> (<epistemic_status>, <updated_at>)`)
    wiki/log.md (read the last 20 lines — confirm the append-only format; newest-at-bottom convention)
    AGENTS.md §4.6 (decision page type spec — 7 required sections, trigger_type enum, affected_pages field)
  </read_first>
  <files>
    wiki/decisions/dr-2026-06-02-obsidian-filename-alias-resolution.md,
    wiki/index.md,
    wiki/log.md
  </files>
  <action>
**NEUTRALITY RULE (MANDATORY):** The decision record is in `wiki/decisions/` — this is wiki data, not a template-public file. §3's neutrality prohibition is scoped to template-public paths, and `wiki/decisions/` is NOT one. Therefore real wiki structural terms (Obsidian, aliases, filename) AND real vault page slugs (e.g. `domain-driven-design.md`, `[[Domain-Driven Design]]`) are permitted as inline examples in the DR body — concrete examples that name the actually-remediated pages are encouraged here. (Neutrality placeholders remain MANDATORY in the template-public files edited by Tasks 1–2: AGENTS.md, CLAUDE.md, schema/templates/.)

**Step 1 — Create `wiki/decisions/dr-2026-06-02-obsidian-filename-alias-resolution.md`:**

Full frontmatter (mirror the analog `dr-2026-04-14-phase6-decision-type.md` shape exactly):
```yaml
---
id: dr-2026-06-02-obsidian-filename-alias-resolution
title: "Obsidian Filename + Alias Resolution: Self-Alias Invariant"
type: decision
status: active
summary: "Corrects AGENTS.md §8 to state Obsidian resolves wikilinks by filename stem + aliases (not title); mandates self-alias invariant; adds bin/lint.sh linkres enforcement."
created_at: 2026-06-02
updated_at: 2026-06-02
sources: []
epistemic_status: sourced
tags:
  - meta
  - schema
domains:
  - wiki-infrastructure
supersedes:
superseded_by:
privacy: cloud_safe
aliases:
  - "Obsidian Filename + Alias Resolution: Self-Alias Invariant"
  - dr-2026-06-02-obsidian-filename-alias-resolution
has_contradictions: false
knowledge_domain: software
trigger_type: schema-update
affected_pages: []
---
```

After the frontmatter, include the FORBIDDEN PATTERNS comment (copy from analog):
```markdown
<!-- FORBIDDEN PATTERNS (see AGENTS.md section 3):
     - No wikilinks in frontmatter (use plain string IDs in sources[], affected_pages[], supersedes, etc.)
     - No display aliases: write [[Page Title]] not [[Page Title|Alias]]
     - Link each page only on first mention in the body -->
```

Then the 7 required sections (all required per §4.6):

**## TL;DR**
One paragraph: Obsidian resolves `[[X]]` by filename stem + `aliases` frontmatter field, NEVER by the `title` field. The wiki spec falsely claimed otherwise; this decision corrects §8 + §5 checklist, mandates that every page include its title and id slug in `aliases` (the self-alias invariant), updates all 12 schema templates, and adds a `linkres` lint category to catch violations going forward.

**## Decision**
State the decision: (1) AGENTS.md §8 now states the real Obsidian resolution mechanism — filename stem + aliases; (2) self-alias invariant adopted — every page's `aliases` MUST include its `title` and `id` slug; (3) §5 checklist items 18–19 enforce this at authoring time; (4) `bin/lint.sh` gains a `linkres` category (CI-gating, error severity) enforced in Wave 1 (Plan 02) to prevent regression; (5) all `wiki/` and `examples/` pages are backfilled in Wave 2 (Plan 03).

**## Why**
Explain the root cause: the `title` frontmatter field in Obsidian is purely for display; Obsidian's link resolver uses the filename stem and the `aliases` frontmatter field. Pages named `domain-driven-design.md` with `title: "Domain-Driven Design"` and no alias are unreachable via `[[Domain-Driven Design]]` — Obsidian renders these as unresolved red links even though the page exists. This caused 31/49 wiki pages to appear as graph orphans. The framing adopted: filename + aliases is the ground truth; title is display metadata; self-aliases are the invariant bridging the two.

**## Alternatives Considered**
1. **Rename wiki files to spaced titles** (e.g., `Domain-Driven Design.md`) — rejected because it breaks the `id == filename` invariant (§5), provenance source IDs, and all tooling that relies on slug filenames.
2. **Rewrite body links to slug form** (e.g., `[[domain-driven-design]]`) — rejected because it abandons the readable `[[Exact Page Title]]` linking convention (§8) and makes prose harder to read.
3. **Accept orphans as a known limitation** — rejected because graph connectivity is the core value of the wiki's cross-reference layer; accepting disconnected pages defeats the purpose.

**## Consequences**
- `linkres` lint category (category `linkres`, severity `error` in `--ci` remap) prevents the same breakage from recurring.
- All 12 schema templates now ship the self-alias guidance, so new pages are correctly configured by default.
- 45 wiki/ pages + 8 lint-visible examples/ pages receive self-alias backfill in Wave 2 (mechanically via `--fix`). The 10 `examples/dataview-fixtures/` content pages are `example: true` (lint-skipped by `--fix`) but are still remediated by HAND-EDIT in Wave 2 (LINK-09: lint-skip means NOT-LINT-ENFORCED, not NOT-REMEDIATED). Only the 2 scaffolding files (kahneman `README.md` + `log.md`) are remediation-exempt.
- `bin/lint.sh --category linkres` exits 0 over `wiki/` after Wave 2 remediation.

**## Affected Pages**
None. This is an infrastructure-only schema correction targeting AGENTS.md, CLAUDE.md, and schema templates. No existing wiki topic page has its structure changed by this decision. The self-alias backfill in Wave 2 (Plan 03) is a data-level additive change (aliases added, nothing removed or restructured).

**## Sources**
None. Internal schema decision based on direct inspection of Obsidian's link-resolution behavior and enumeration of 31 orphaned wiki pages.

---

**Step 2 — Add entry to `wiki/index.md`** under the `## Decisions` section:
```
- [[Obsidian Filename + Alias Resolution: Self-Alias Invariant]] -- Corrects §8 Obsidian resolution rule; mandates self-alias invariant; adds linkres lint enforcement (sourced, 2026-06-02)
```

**Step 3 — Append entry to `wiki/log.md`** (newest at bottom):
```markdown
## [2026-06-02] reflect | schema-update: Obsidian filename + alias resolution

UPDATE wiki/decisions/dr-2026-06-02-obsidian-filename-alias-resolution.md
source: n/a (internal schema decision)
result: authored schema-update DR; corrected AGENTS.md §8 + §5 (self-alias invariant); updated 12 schema templates; registered in wiki/index.md
reason: Obsidian resolves [[X]] by filename stem + aliases, not title; 31/49 pages were graph orphans due to missing self-aliases; convention now correct and enforced by linkres lint category (Plan 02)
```

**Step 4 — Validate the structured operations (CLAUDE.md §9 Executor Model).** Before committing the index/log updates, run the deterministic operation validator (it MUST exit 0 before applying):
```bash
bash bin/validate-op.sh UPDATE wiki/index.md
bash bin/validate-op.sh UPDATE wiki/log.md
```
If either returns FAIL, do NOT commit — inspect the reported reason, fix the offending file, and re-run. (The new DR file is a page creation, not one of the four mutation ops, so it is not validated here; the index and log appends are the UPDATE operations §9 governs.)

**Step 5 — Commit all files atomically** (one commit per §3 one-commit-per-logical-operation). Run the add AND the literal commit (LOW Cycle-2 — show `git commit`, not just `git add`):
```bash
bash bin/sync-claude.sh && git add CLAUDE.md AGENTS.md
git add schema/templates/ schema/obsidian/ wiki/decisions/dr-2026-06-02-obsidian-filename-alias-resolution.md wiki/index.md wiki/log.md
git commit -m "reflect(obsidian-filename-alias-resolution): correct §8 resolution rule, mandate self-alias invariant, add schema-update DR"
```
  </action>
  <verify>
    <automated>
test -f /home/yishai/Documents/compendium/wiki/decisions/dr-2026-06-02-obsidian-filename-alias-resolution.md && echo "PASS: DR file exists" || echo "FAIL: DR file missing"
grep -q "trigger_type: schema-update" /home/yishai/Documents/compendium/wiki/decisions/dr-2026-06-02-obsidian-filename-alias-resolution.md && echo "PASS: trigger_type correct" || echo "FAIL"
grep -q "affected_pages: \[\]" /home/yishai/Documents/compendium/wiki/decisions/dr-2026-06-02-obsidian-filename-alias-resolution.md && echo "PASS: affected_pages empty list" || echo "FAIL"
grep -q "dr-2026-06-02-obsidian-filename-alias-resolution" /home/yishai/Documents/compendium/wiki/index.md && echo "PASS: DR in index" || echo "FAIL: DR not in index"
grep -q "schema-update.*obsidian\|obsidian.*schema-update" /home/yishai/Documents/compendium/wiki/log.md && echo "PASS: log entry present" || echo "FAIL: log entry missing"
bash /home/yishai/Documents/compendium/bin/sync-claude.sh --check && echo "PASS: byte-identical after DR commit" || echo "FAIL"
    </automated>
  </verify>
  <done>Decision record file exists at `wiki/decisions/dr-2026-06-02-obsidian-filename-alias-resolution.md` with `trigger_type: schema-update`, `affected_pages: []`, all 7 required sections, and a FORBIDDEN PATTERNS comment; it is listed in `wiki/index.md` under Decisions; `wiki/log.md` has a new append entry; `bin/sync-claude.sh --check` exits 0; all changes are in one commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AGENTS.md edit → CLAUDE.md | Both files are in-repo markdown; byte-copy via bin/sync-claude.sh (AGENTS.md → CLAUDE.md); no external input |
| schema/templates write | Template files are committed to repo; no runtime user input |
| wiki/decisions write | Decision record is authored by the agent; content is internally derived |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Tampering | AGENTS.md §8/§5 edit | mitigate | Neutrality rule enforced by `bash bin/check-neutrality.sh` exit 0 before commit; CLAUDE.md sync enforced by `.githooks/pre-commit` byte-equality check |
| T-14-01-02 | Tampering | schema/templates/*.md edit | mitigate | Abstract placeholders only (`<Page Title>`, `<page-id-slug>`); `bin/check-neutrality.sh` validates before commit |
| T-14-01-03 | Tampering | wiki/decisions DR file | accept | DR content is agent-authored wiki data (not template-public); wiki/ directory is excluded from check-neutrality.sh; content is factual/structural description only |
| T-14-01-04 | Denial of Service | CLAUDE.md diverging from AGENTS.md | mitigate | `.githooks/pre-commit` enforces byte-equality; `bin/sync-claude.sh --check` in task verify; commit rejected if divergent |
</threat_model>

<verification>
After all three tasks complete:
1. `grep -q "filename stem" AGENTS.md` — exits 0 (LINK-01)
2. `grep -c "title.*aliases" AGENTS.md` — count ≥ 2 (checklist items 18-19 present) (LINK-02)
3. `bash bin/sync-claude.sh --check` — exits 0 (CLAUDE.md byte-identical to AGENTS.md)
3b. `grep "Apply severity-remap dispatch table" AGENTS.md | grep -q linkres` — exits 0 (§11.3 source-of-truth remap lists linkres; MEDIUM #4 schema-drift fix)
4. `grep -q "trigger_type: schema-update" wiki/decisions/dr-2026-06-02-obsidian-filename-alias-resolution.md` — exits 0 (LINK-03)
5. `grep -q "dr-2026-06-02-obsidian-filename-alias-resolution" wiki/index.md` — exits 0
6. All 6 `schema/templates/*.md` files: `grep -q "Self-alias invariant" schema/templates/concept.md` — exits 0
7. All 6 `schema/obsidian/*.md` files: `grep -q "{{title}}" schema/obsidian/concept.md` — exits 0
8. `bash bin/check-neutrality.sh` — exits 0 (no vault terms in template-public files)
</verification>

<success_criteria>
- AGENTS.md §8 states Obsidian resolves [[X]] by filename stem + aliases (not title) — LINK-01 satisfied
- AGENTS.md §5 checklist items 18–19 require title and id to be LITERAL members of aliases (`aliases ⊇ {title, id}`, matching LINK-02 and `bin/lint.sh --fix`), and schema templates ship the self-alias — LINK-02 satisfied
- Decision record dr-2026-06-02-obsidian-filename-alias-resolution.md created with trigger_type: schema-update, registered in index, logged — LINK-03 satisfied
- `bash bin/sync-claude.sh --check` exits 0 (CLAUDE.md byte-identical to AGENTS.md)
- `bash bin/check-neutrality.sh` exits 0 (no vault term leaks in template-public files)
</success_criteria>

<output>
After completion, create `.planning/phases/14-graph-link-resolution/14-01-SUMMARY.md`
</output>
