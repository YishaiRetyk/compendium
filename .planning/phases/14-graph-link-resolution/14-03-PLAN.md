---
phase: 14-graph-link-resolution
plan: 03
type: execute
wave: 2
depends_on: [14-01, 14-02]
files_modified:
  - wiki/entities/*.md
  - wiki/concepts/*.md
  - wiki/overviews/*.md
  - wiki/comparisons/*.md
  - wiki/sources/*.md
  - wiki/decisions/*.md
  - examples/kahneman/concepts/*.md
  - examples/kahneman/entities/*.md
  - examples/kahneman/sources/*.md
  - examples/kahneman/comparisons/*.md
  - examples/kahneman/overviews/*.md
  - examples/dataview-fixtures/*.md
  - wiki/log.md
autonomous: false
requirements:
  - LINK-07
  - LINK-08
  - LINK-09
  - LINK-10

must_haves:
  truths:
    - "bash bin/lint.sh --category linkres wiki/ reports zero error-severity findings over wiki/ after self-alias backfill + variant reconciliation (residual warnings are acceptable, manual-triage)"
    - "The link text [[Bounded Contexts]] is reconciled to [[Bounded Context]]s (call site edited, plural-s outside the link per D-08)"
    - "Every wiki/ page needing a self-alias is backfilled by --fix; the `--fix` autofix-finding count is the source of truth for how many pages were modified (do NOT hard-code 45/4 — `--fix` now uses literal title+id membership per LINK-02, so even title==stem pages get the literal alias added and the prior '4 already compliant' framing no longer applies)"
    - "The 8 lint-visible examples pages (examples/kahneman/ cluster without example:true) are backfilled by --fix"
    - "The 10 dataview-fixture content pages (examples/dataview-fixtures/fixture-*.md, all example:true) get literal self-aliases (title + id) by HAND-EDIT — --fix cannot reach them (lint skips example:true at lint.sh:956), but LINK-09 requires examples pages carry self-aliases; lint-skip means not-lint-ENFORCED, not not-REMEDIATED"
    - "The 2 example:true scaffolding files (examples/kahneman/README.md, examples/kahneman/log.md) remain exempt — they mirror wiki/index.md + wiki/log.md, which are not self-aliased content nodes; documented as a deliberate exemption, not an oversight"
    - "The two colon-titled decision pages (dr-2026-05-01-complementary-systems-boundary, dr-2026-06-02-sc1-examples-isolable-subgraph) still re-parse as valid YAML after --fix, with their title alias as a STRING (HIGH BUG #2 canary)"
    - "wiki/overviews/domain-driven-design.md has both 'Domain-Driven Design' and 'domain-driven-design' in its aliases list"
    - "wiki/log.md has a lint log entry recording the --fix backfill (counts derived from actual run, not hard-coded) and variant reconciliation"
    - "Human-verified in Obsidian: domain-driven-design.md and other previously-orphaned pages are no longer graph orphans"
  artifacts:
    - path: "wiki/overviews/domain-driven-design.md"
      provides: "Canonical LINK-10 exemplar — self-alias backfilled so [[Domain-Driven Design]] resolves"
      contains: "Domain-Driven Design"
    - path: "wiki/log.md"
      provides: "Log entry for --fix backfill + variant reconciliation operation"
      contains: "linkres"
  key_links:
    - from: "wiki/ pages aliases field"
      to: "bin/lint.sh --category linkres check"
      via: "bin/lint.sh --fix writes aliases; --category linkres then finds 0 errors"
      pattern: "bin/lint.sh --category linkres wiki/"
    - from: "[[Bounded Context]]s call site edit"
      to: "wiki/concepts/bounded-context.md"
      via: "link text corrected to canonical title; plural-s kept outside link"
      pattern: "Bounded Context"
---

<objective>
Run `bin/lint.sh --fix` to mechanically backfill self-aliases across the lint-visible pages (45 wiki/ + 8 lint-visible examples/), hand-edit the 10 `example: true` dataview-fixture content pages to add literal self-aliases (`--fix` cannot reach them but LINK-09 requires them), manually reconcile the remaining body-link variant (the `[[Bounded Contexts]]` call-site edit that --fix cannot do), and human-verify the connected graph in Obsidian.

Purpose: Plan 01 fixed the convention; Plan 02 added enforcement. This plan remedies the existing data so `--category linkres wiki/` reports zero errors. After this plan, no wiki page is a graph orphan due to the missing-self-alias bug, and the `linkres` CI gate is clean for all future commits.

SCOPE CORRECTION (Cycle-2 review, orchestrator-confirmed ruling on the LINK-09 spec-fork): `bin/lint.sh:956` drops `example: true` pages from `all_pages` UNCONDITIONALLY, so `--fix` can only touch lint-visible pages. examples/ = 20 total = examples/kahneman/ (10) + examples/dataview-fixtures/ (10); of these, 12 are `example: true` and are NEVER seen by lint. The Cycle-2 reviewers diverged on what LINK-09 requires of the 12: Gemini read "lint-skip ⇒ exempt-from-remediation"; Codex (and the locked requirement text) read "lint skips them, but they STILL carry self-aliases — remediated by hand-edit". **Ruling (sides with Codex + LINK-09 literal text "examples/ pages carry self-aliases ... respecting example:true / lint-skip conventions"):** lint-skip means NOT-LINT-ENFORCED, not NOT-REMEDIATED. The remediation surface is therefore:
- **45 wiki/ pages** — self-alias backfill via `--fix` (mechanical)
- **8 lint-visible examples/kahneman/ pages** (no example:true) — self-alias backfill via `--fix` (mechanical)
- **10 dataview-fixture content pages** (examples/dataview-fixtures/fixture-*.md, all example:true) — literal self-alias (title + id) by **HAND-EDIT** (Task 1 Step 2b), since `--fix` cannot reach them; this is what satisfies LINK-09 literally
- **2 example:true scaffolding files** (examples/kahneman/README.md id `kahneman-readme`, examples/kahneman/log.md id `log`) — EXEMPT: they mirror wiki/index.md + wiki/log.md (the wiki's own non-self-aliased scaffolding); documenting them as a deliberate exemption respects the "respecting … conventions" clause.

Backfill total = 45 wiki/ + 8 kahneman + 10 fixtures (hand-edited) = 63 self-aliased pages; 2 scaffolding files exempt. NOT "65 via --fix" (mechanically impossible — `--fix` reaches only 53) and NOT "12 exempt" (only 2 are exempt; 10 are hand-remediated).

CI-RED-WINDOW STRATEGY (review F): once Plan 02 commits `'linkres':'error'`, full `--ci` over the repo surfaces ~45 wiki linkres errors and exits 1 — and stays red until THIS plan (human-gated, autonomous:false) finishes. Chosen strategy: land Plan 02 + Plan 03 in a SINGLE merge to any branch-protected ref (the working branch is NOT merged to main until this plan's human-verify checkpoint passes). During the window, `--strict` (diff-scoped — does NOT flag pre-existing pages for linkres reachability) remains the only safe REQUIRED gate; full `--ci` red is expected and not a regression. Do not push intermediate Plan-02-only state to a protected ref.

Output: 45 wiki/ + 8 kahneman pages self-aliased via `--fix`; 10 dataview-fixture pages self-aliased via hand-edit; `bin/lint.sh --category linkres wiki/` reports zero errors; `[[Bounded Contexts]]` call site reconciled; the 2 colon-titled decision pages verified as still-valid YAML; the 2 scaffolding files documented exempt; human-verify Obsidian checkpoint passed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-graph-link-resolution/14-01-SUMMARY.md
@.planning/phases/14-graph-link-resolution/14-02-SUMMARY.md

<interfaces>
<!-- Key facts the executor needs. From RESEARCH.md live enumeration. -->

**Quantified remediation surface (verified live by orchestrator):**
- wiki/ total: 49 pages
- wiki/ pages where title OR id is not already a LITERAL alias → backfilled by `--fix` (literal title+id membership per LINK-02; note: pages where title.lower()==stem still get the literal title/id alias added — `--fix` now uses literal membership, not stem-reachability, so the prior "4 already compliant" framing no longer applies cleanly. Treat the `--fix` autofix-finding count as the source of truth for how many wiki pages were modified; do NOT hard-code 45.)
- examples/ total: 20 pages = examples/kahneman/ (10) + examples/dataview-fixtures/ (10)
  - **8 lint-visible kahneman pages** (no example:true): daniel-kahneman.md, prospect-theory.md, loss-aversion.md, cognitive-biases.md, system-1-vs-system-2.md, decision-making.md, src-2026-04-09-thinking-fast-and-slow-part1.md, src-2026-04-10-kahneman-prospect-theory.md → backfilled by `--fix`
  - **10 dataview-fixture content pages** (examples/dataview-fixtures/fixture-*.md, all example:true, all `aliases: []`) → SKIPPED by lint at lint.sh:956 so `--fix` cannot reach them → HAND-EDITED in Step 2b to add literal self-aliases (title + id) per LINK-09
  - **2 example:true scaffolding files** (examples/kahneman/README.md id `kahneman-readme`; examples/kahneman/log.md id `log`) → EXEMPT (mirror wiki/index.md + wiki/log.md, the wiki's own non-self-aliased scaffolding)
- Self-aliased total = ~45 wiki/ + 8 kahneman (`--fix`) + 10 fixtures (hand-edit) = 63; 2 scaffolding files exempt

**Colon-titled decision pages (HIGH BUG #2 canary — verify post-fix):**
- wiki/decisions/dr-2026-05-01-complementary-systems-boundary.md — title "Complementary Systems Boundary: ..."
- wiki/decisions/dr-2026-06-02-sc1-examples-isolable-subgraph.md — title "SC1 Reframing: examples/ ..."
These have colon-space titles. Plan 02's `_yaml_quote_alias` must have landed first so `--fix` emits them quoted. After `--fix`, both MUST still re-parse as valid YAML with the title alias as a STRING (not a `{key: value}` dict). This is the safety net for the corruption bug.

**Body link variants (28 unique-match defects):**
- ~26 are title-exact links that resolve AFTER --fix adds the self-alias (they are NOT manual edits)
- 1 plural variant: `[[Bounded Contexts]]` → canonical is `[[Bounded Context]]s` (plural-s outside the link per D-08; edit the call site in the wiki page that contains this link)
- 1 parens variant: `[[Hack (Agentive Stack)]]` → this IS the canonical title; AFTER --fix adds "Hack (Agentive Stack)" as alias, this link resolves automatically (no manual edit needed)
- 1 source title variant: `[[Is this the only skill left?]]` → this may also resolve after --fix if the source summary page gets its title as a self-alias

**Canonical exemplar (LINK-10):** `wiki/overviews/domain-driven-design.md` — `id: domain-driven-design`, `title: "Domain-Driven Design"`. Currently `aliases: [DDD]` only. After --fix: `aliases: [DDD, Domain-Driven Design, domain-driven-design]`.

**Obsidian graph verification:** Open vault at repo root, enable `hideUnresolved` in Graph View settings, confirm domain-driven-design.md is no longer an isolated node.

**lint-skip for example: true pages:** The `linkres` check inherits the `all_pages` filter that skips `example: true` pages (lint.sh:956, verified live). So `--fix examples/` only reaches the 8 lint-visible kahneman pages (those WITHOUT `example: true`). The 12 `example: true` pages are never seen by `--fix`. Per the Cycle-2 LINK-09 ruling (objective SCOPE CORRECTION), lint-skip ≠ remediation-exempt: the 10 dataview-fixture content pages MUST still get literal self-aliases — added by HAND-EDIT in Step 2b (not `--fix`). Only the 2 scaffolding files (README.md, log.md) are remediation-exempt. Run `--fix` on wiki/ and examples/ (reaches 8 kahneman), then hand-edit the 10 fixtures. Do NOT claim "all 20 examples via --fix" or "12 exempt" — both are wrong (the prior revision's errors).

**key pages with inbound links to domain-driven-design.md:**
- wiki/index.md (has [[Domain-Driven Design]] entry)
- wiki/log.md (may have [[Domain-Driven Design]] references)
- wiki/concepts/ubiquitous-language.md (links to [[Domain-Driven Design]])
- wiki/concepts/programming-as-theory-building.md (links to [[Domain-Driven Design]])
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run --fix backfill across wiki/ and examples/ then reconcile variants</name>
  <read_first>
    wiki/overviews/domain-driven-design.md (read completely — confirm current aliases block shape; confirm title is "Domain-Driven Design"; verify this is the canonical LINK-10 exemplar)
    wiki/ directory listing (run: ls wiki/entities/ wiki/concepts/ wiki/overviews/ wiki/comparisons/ wiki/sources/ wiki/decisions/ to see all pages)
    examples/kahneman/ directory listing (run: ls examples/kahneman/concepts/ examples/kahneman/entities/ etc.)
    examples/dataview-fixtures/ listing (run: ls examples/dataview-fixtures/)
    CLAUDE.md §3 "What Agents Must NOT Do" — confirm: do NOT use real vault terms in template-public files (this task edits wiki/ data files, which ARE allowed to contain real terms)
    .planning/phases/14-graph-link-resolution/14-RESEARCH.md §Quantified Remediation Surface (the 28-variant breakdown + confirmed post-fix state)
  </read_first>
  <files>
    wiki/entities/*.md,
    wiki/concepts/*.md,
    wiki/overviews/*.md,
    wiki/comparisons/*.md,
    wiki/sources/*.md,
    wiki/decisions/*.md,
    examples/kahneman/concepts/*.md,
    examples/kahneman/entities/*.md,
    examples/kahneman/sources/*.md,
    examples/kahneman/comparisons/*.md,
    examples/kahneman/overviews/*.md,
    examples/dataview-fixtures/*.md
  </files>
  <action>
**Step 1 — Run --fix over wiki/ to backfill self-aliases:**
```bash
bash bin/lint.sh --fix --category linkres wiki/
```
This will:
- Add `title` to aliases for each page where title is not in aliases
- Add `id` slug to aliases for each page where id is not in aliases
- Preserve all existing aliases (DDD, Anthropic PBC, etc.)
- Emit one `autofix` finding per page modified

After running, spot-check `wiki/overviews/domain-driven-design.md` to confirm it now has (NOTE: `--fix` re-emits ALL aliases YAML-double-quoted via `_yaml_quote_alias` — existing entries included — so the block will look like this, with quotes; this is the actual writer style, LOW Cycle-2):
```yaml
aliases:
  - "DDD"                    # existing — preserved (re-emitted quoted)
  - "Domain-Driven Design"   # title added by --fix
  - "domain-driven-design"   # id slug added by --fix (literal membership, LINK-02)
```
(The exact pre-existing `DDD` alias is preserved per the review suggestion to verify alongside the new aliases.)

**Step 2 — Run --fix over examples/ to backfill self-aliases (lint-visible pages only):**
```bash
bash bin/lint.sh --fix --category linkres examples/
```
This `--fix` run backfills self-aliases for ONLY the 8 lint-visible kahneman pages (those without `example: true`). The 12 `example: true` pages (2 scaffolding + 10 dataview-fixtures) are skipped by `--fix` at lint.sh:972 — `--fix` mechanically cannot reach them. This does NOT make them remediation-exempt. Per the LINK-09 ruling (lint-skip means NOT-LINT-ENFORCED, not NOT-REMEDIATED), the 10 `examples/dataview-fixtures/` content pages MUST still receive literal self-aliases — added by HAND-EDIT in **Step 2b** below (not `--fix`). Only the 2 scaffolding files (`README.md`, `log.md`) are remediation-exempt. Do NOT skip Step 2b.

Confirm the lint-skip count after the run (this counts what `--fix` skipped — 10 of these 12 are still hand-edited in Step 2b; only 2 stay exempt):
```bash
# 12 example:true pages are skipped by --fix; Step 2b hand-edits 10 of them, 2 scaffolding files stay exempt
grep -rl "^example: true" examples/ | wc -l   # expect 12
```

**Step 2b — HAND-EDIT the 10 dataview-fixture content pages (LINK-09 literal remediation; Cycle-2 ruling):**

`--fix` cannot reach `example: true` pages (lint.sh:956 skips them), but LINK-09 requires examples pages to carry self-aliases. The 10 `examples/dataview-fixtures/fixture-*.md` content pages all currently have `aliases: []`. For EACH of these 10 files, replace the `aliases: []` line with a literal self-alias block containing the page's `title` and `id` (read each file's frontmatter first to get its exact title/id):

The 10 files (all under `examples/dataview-fixtures/`): `fixture-source-1.md`, `fixture-source-2.md`, `fixture-entity-1.md`, `fixture-entity-2.md`, `fixture-entity-3.md`, `fixture-concept-1.md`, `fixture-concept-2.md`, `fixture-overview-1.md`, `fixture-comparison-1.md`, `fixture-archived-1.md`.

For each file, transform (example shown for `fixture-concept-1.md`, id `fixture-concept-1`, title `Fixture Concept One`):
```yaml
# BEFORE:
aliases: []
# AFTER (title first, then id slug — both literal members, quote the title since it has spaces):
aliases:
  - "Fixture Concept One"
  - fixture-concept-1
```
Apply the same pattern to all 10 using each file's OWN `title` and `id` values (read them from each file — do NOT copy the example's values). Titles that contain a colon MUST be double-quoted (none of these 10 do, but verify). Preserve every other frontmatter line. Do NOT touch the 2 scaffolding files (README.md, log.md).

After editing, verify all 10 carry self-aliases and re-parse as valid YAML:
```bash
python3 - <<'PYEOF'
import yaml, glob, sys
fail = False
for p in sorted(glob.glob('examples/dataview-fixtures/fixture-*.md')):
    fm = next(yaml.safe_load_all(open(p)))
    aliases = [str(a) for a in (fm.get('aliases') or [])]
    title, pid = fm.get('title',''), fm.get('id','')
    if title not in aliases or pid not in aliases:
        print(f"FAIL: {p} missing self-alias (title={title!r} id={pid!r} aliases={aliases})"); fail = True
    else:
        print(f"PASS: {p} self-aliased")
sys.exit(1 if fail else 0)
PYEOF
```
Expect 10 PASS lines. (The `fixture-archived-1.md` page has `status: archived` — it still gets a self-alias; archived pages are still graph nodes.)

**Step 3 — Run --category linkres diagnostic over wiki/ to see remaining errors:**
```bash
bash bin/lint.sh --category linkres --dry-run wiki/ 2>&1
```
After --fix, the expected remaining errors are:
- `[[Bounded Contexts]]` variant (plural) — body link in some wiki page
- Possibly `[[Is this the only skill left?]]` if title-exact after --fix

Read the output carefully. For each remaining error, determine the remediation per D-07/D-08:
- Plural variant (`[[Bounded Contexts]]`): edit the call site → `[[Bounded Context]]s`
- Parens variant (`[[Hack (Agentive Stack)]]`): should have been fixed by --fix (title IS the canonical); if still appearing, check that --fix ran on that page and that `Hack (Agentive Stack)` is now in aliases
- Source title variant (`[[Is this the only skill left?]]`): find the page with that title; --fix should have added it as self-alias; if it still appears, check the source summary page's aliases

**Step 4 — Manual variant reconciliation (D-07/D-08):**

For `[[Bounded Contexts]]` (plural variant):
1. Find which wiki page(s) contain the text `[[Bounded Contexts]]`:
   ```bash
   grep -rn "\[\[Bounded Contexts\]\]" wiki/
   ```
2. For each occurrence, edit the link text: change `[[Bounded Contexts]]` to `[[Bounded Context]]s`
   - The canonical title is "Bounded Context" (singular)
   - The plural `s` goes OUTSIDE the link brackets per D-08
   - This is a call-site edit, NOT an alias addition (per D-08: "edit the link text, keep the s outside")

For any other variants still showing after --fix:
- If it is a real alternate name / acronym (not a plural/casing variant): add as alias to the target page
- If it is ambiguous: document in summary as "left for manual decision" with the full finding

**Step 5 — Run final --category linkres check to confirm 0 errors:**
```bash
bash bin/lint.sh --category linkres wiki/ --format json 2>/dev/null | python3 -c "
import json, sys
data = json.load(sys.stdin)
errs = [d for d in data if d['category'] == 'linkres' and d['severity'] == 'error']
if errs:
    print('FAIL: remaining linkres errors:')
    for e in errs:
        print(f'  {e[\"path\"]}: {e[\"message\"]}')
    sys.exit(1)
else:
    warns = [d for d in data if d['category'] == 'linkres' and d['severity'] == 'warning']
    print(f'PASS: 0 linkres errors. {len(warns)} warnings (ambiguous variants, manual triage).')
"
```

**Step 5b — Colon-title YAML canary (HIGH BUG #2 safety net).** Confirm `--fix` did NOT corrupt the two colon-titled decision pages — both must re-parse with their title alias as a STRING:
```bash
python3 - <<'PYEOF'
import yaml, sys
checks = [
    ("wiki/decisions/dr-2026-05-01-complementary-systems-boundary.md",
     "Complementary Systems Boundary: Compendium as Durable Wiki Memory in a Multi-System Stack"),
    ("wiki/decisions/dr-2026-06-02-sc1-examples-isolable-subgraph.md",
     "SC1 Reframing: examples/ Forms a Visually Isolable Sub-Graph"),
]
fail = False
for path, title in checks:
    fm = next(yaml.safe_load_all(open(path)))
    aliases = fm.get('aliases') or []
    if not all(isinstance(a, str) for a in aliases):
        print(f"FAIL: {path} has a non-string alias (colon corrupted YAML): {aliases}"); fail = True
    elif title not in aliases:
        print(f"FAIL: {path} missing quoted title alias: {aliases}"); fail = True
    else:
        print(f"PASS: {path} colon-title alias is a clean string")
sys.exit(1 if fail else 0)
PYEOF
```
If this FAILS, Plan 02's `_yaml_quote_alias` fix did not land or is wrong — STOP, do not commit, and fix Plan 02's helper before re-running `--fix`.

**Step 6 — Log the remediation in wiki/log.md** (append-only, newest at bottom). Derive counts from the ACTUAL `--fix` run output (count `autofix` findings), do NOT hard-code:
```markdown
## [2026-06-02] lint | linkres: self-alias backfill + variant reconciliation

result: ran `--fix` over wiki/ (<N_wiki> pages updated) + examples/ (<N_ex> lint-visible kahneman pages updated); hand-edited 10 dataview-fixture content pages (example:true, --fix-unreachable) to add literal self-aliases (LINK-09); 2 scaffolding files (README.md, log.md) exempt; manually reconciled [[Bounded Contexts]] → [[Bounded Context]]s (call-site edit, D-08)
pages_affected: wiki/overviews/domain-driven-design.md, the wiki page containing the [[Bounded Contexts]] call site, all wiki pages backfilled with self-aliases, 8 lint-visible kahneman pages, 10 examples/dataview-fixtures/fixture-*.md
write_back: NO-WRITE-BACK (remediation operation, not a query synthesis)
delta_compiled: none
```
CRITICAL (LOW Cycle-2 — Gemini): replace `<N_wiki>` and `<N_ex>` with the ACTUAL integer counts observed from the `--fix` autofix-finding output in Steps 1–2 BEFORE committing. Do NOT leave the angle-bracket placeholders in the committed `log.md` — grep the working-tree log.md for `<N_` and confirm zero matches (run this BEFORE `git add` in Step 7; greps the working tree, not the staged diff, so it catches placeholders even though staging happens later):
```bash
grep -q '<N_' wiki/log.md && echo "ERROR: placeholder left in log.md — replace with real count" || echo "OK: no placeholders"
```

**Step 7 — Commit all remediated pages** (one commit, one logical operation per §3). Run BOTH the add AND the commit (LOW Cycle-2 — show the literal `git commit`, not just `git add`):
```bash
git add wiki/ examples/ wiki/log.md
git commit -m "lint(wiki): backfill self-aliases across wiki/ + examples/ (LINK-07, LINK-09); reconcile [[Bounded Contexts]] call site (LINK-08)"
```
  </action>
  <verify>
    <automated>
bash /home/yishai/Documents/compendium/bin/lint.sh --category linkres /home/yishai/Documents/compendium/wiki/ --format json 2>/dev/null | python3 -c "
import json, sys
data = json.load(sys.stdin)
errs = [d for d in data if d['category'] == 'linkres' and d['severity'] == 'error']
if errs:
    print('FAIL: remaining linkres errors:')
    for e in errs: print(f'  {e[\"path\"]}: {e[\"message\"][:80]}')
    sys.exit(1)
print(f'PASS: 0 linkres errors over wiki/. Check complete.')
"
grep -q "Domain-Driven Design" /home/yishai/Documents/compendium/wiki/overviews/domain-driven-design.md && echo "PASS: DDD has title alias" || echo "FAIL: DDD missing title alias"
grep -rn "\[\[Bounded Contexts\]\]" /home/yishai/Documents/compendium/wiki/ && echo "FAIL: [[Bounded Contexts]] still present" || echo "PASS: [[Bounded Contexts]] reconciled"
# Colon-title YAML canary (HIGH BUG #2): both decision pages must re-parse with string aliases
python3 -c "import yaml; [print('PASS:',p) if all(isinstance(a,str) for a in (next(yaml.safe_load_all(open(p))).get('aliases') or [])) else print('FAIL:',p) for p in ['/home/yishai/Documents/compendium/wiki/decisions/dr-2026-05-01-complementary-systems-boundary.md','/home/yishai/Documents/compendium/wiki/decisions/dr-2026-06-02-sc1-examples-isolable-subgraph.md']]"
# LINK-09 fixtures hand-edit (Cycle-2): all 10 dataview-fixture content pages carry literal self-aliases (title + id)
python3 - <<'PYEOF'
import yaml, glob, sys
fail=False
for p in sorted(glob.glob('/home/yishai/Documents/compendium/examples/dataview-fixtures/fixture-*.md')):
    fm=next(yaml.safe_load_all(open(p))); al=[str(a) for a in (fm.get('aliases') or [])]
    if fm.get('title') in al and fm.get('id') in al: print('PASS:',p.split('/')[-1])
    else: print('FAIL:',p.split('/')[-1],al); fail=True
sys.exit(1 if fail else 0)
PYEOF
    </automated>
  </verify>
  <done>
`bash bin/lint.sh --category linkres wiki/` reports zero error-severity findings (LINK-07 + LINK-08 satisfied; residual warnings acceptable); `wiki/overviews/domain-driven-design.md` aliases include both "Domain-Driven Design" and "domain-driven-design"; `[[Bounded Contexts]]` link text no longer appears in wiki/ body text (replaced with `[[Bounded Context]]s`); the ~45 wiki/ + 8 lint-visible kahneman pages have self-aliases via `--fix`; the 10 dataview-fixture content pages have literal self-aliases via hand-edit (LINK-09); the 2 scaffolding files (README.md, log.md) are documented exempt; the two colon-titled decision pages re-parse as valid YAML with string aliases (HIGH BUG #2 canary passes); wiki/log.md has the remediation log entry with actual counts; all changes in one commit.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify connected graph in Obsidian (LINK-10)</name>
  <read_first>
    wiki/overviews/domain-driven-design.md (confirm self-aliases are present before opening Obsidian — checking the file confirms the mechanical part is done)
    wiki/index.md (confirm [[Domain-Driven Design]] entry is present — this is one of the inbound links that should now connect)
  </read_first>
  <what-built>
    Plan 01 corrected CLAUDE.md §8 (Obsidian resolution rule) and authored the schema-update DR.
    Plan 02 added the `linkres` lint category with `--fix` self-alias backfill.
    Task 1 (this plan) ran `--fix` across the 45 wiki/ + 8 lint-visible examples/ pages, hand-edited the 10 `examples/dataview-fixtures/` content pages to add literal self-aliases (LINK-09 — `--fix` cannot reach example:true pages but they are still remediated; only the 2 kahneman scaffolding files stay exempt), and reconciled the [[Bounded Contexts]] call site; the 2 colon-titled decision pages were verified as still-valid YAML.
    `bin/lint.sh --category linkres wiki/` now reports zero errors.
  </what-built>
  <how-to-verify>
    1. Open Obsidian at the repo root: `open /home/yishai/Documents/compendium` (or use the Obsidian app to open this folder as a vault).
    2. Navigate to Graph View (Cmd/Ctrl+G or sidebar icon).
    3. In Graph View settings, enable `hideUnresolved` (toggle "Hide unresolved links" ON). This makes red-link orphans from intentional knowledge gaps invisible, isolating graph-connectivity issues.
    4. Confirm that `domain-driven-design.md` (the page "Domain-Driven Design") is connected to its neighbors — it should appear as a node connected to: the index page, ubiquitous-language.md, programming-as-theory-building.md, and the sources that reference DDD.
    5. Spot-check 3-4 other previously-orphaned pages (e.g. concept pages like "Bounded Context", "Progressive Disclosure", entity pages). Confirm they are connected, not isolated nodes.
    6. Confirm the overall graph is substantially more connected than before (was 31/49 pages as orphans; should now show a largely connected cluster).
    7. Optional: disable `hideUnresolved` briefly to confirm the intentional red-link knowledge-gap nodes remain as unresolved links (they should — those were NOT touched by this phase).
  </how-to-verify>
  <resume-signal>
    Type "approved" if the graph is connected and domain-driven-design.md + other previously-orphaned pages are no longer isolated.
    Type "issue: [description]" if you see unexpected orphans remaining, and describe which page(s) are still disconnected.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bin/lint.sh --fix → wiki/ and examples/ file writes | --fix writes back to frontmatter aliases blocks; content derived from file's own YAML (no external input) |
| Manual call-site edit ([[Bounded Contexts]] → [[Bounded Context]]s) | Human-reviewed editorial decision; one file edit; low risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Tampering | --fix aliases write on wiki/ pages | mitigate | Frontmatter-section-only scope (fm_end fence); idempotency means re-run is safe; _apply_self_alias_fix exception-catches all errors; spot-check domain-driven-design.md aliases after --fix |
| T-14-03-02 | Tampering | [[Bounded Contexts]] call-site edit | accept | Single wiki page edit; grep-verified before and after; D-08 direction is explicit; human-reviewed in summary |
| T-14-03-03 | Tampering | --fix corrupts colon-titled / unusual-frontmatter pages | mitigate | CONFIRMED HIGH BUG #2 (verified live on 2 decision pages): Plan 02's `_yaml_quote_alias` emits YAML-double-quoted values so colon/bracket titles stay strings; ALIASES_RE confined to frontmatter section; regex mismatch leaves file unchanged + stderr warns; Step 5b canary re-parses both colon-titled pages and blocks commit if either is non-string |
| T-14-03-04 | Information Disclosure | Obsidian graph reveals vault structure to screen capture | accept | Local-only operation; graph view is user's own Obsidian instance; no external service involved |
</threat_model>

<verification>
After Task 1 (and after human verify in Task 2):
1. `bash bin/lint.sh --category linkres wiki/ --format json 2>/dev/null | python3 -c "import json,sys; d=json.load(sys.stdin); e=[x for x in d if x['category']=='linkres' and x['severity']=='error']; print('PASS' if not e else f'FAIL: {e}')"` — prints PASS (LINK-07, LINK-08)
2. `grep -q "Domain-Driven Design" wiki/overviews/domain-driven-design.md` — exits 0 (title self-alias present)
3. `grep -rn "\[\[Bounded Contexts\]\]" wiki/` — returns no matches (LINK-08 — plural variant reconciled)
4. Colon-title canary (HIGH BUG #2): both colon-titled decision pages re-parse with string aliases — `python3 -c "import yaml; assert all(isinstance(a,str) for p in ['wiki/decisions/dr-2026-05-01-complementary-systems-boundary.md','wiki/decisions/dr-2026-06-02-sc1-examples-isolable-subgraph.md'] for a in (next(yaml.safe_load_all(open(p))).get('aliases') or [])); print('PASS')"`
5. `bash bin/lint.sh --category yaml wiki/ --format json 2>/dev/null | python3 -c "import json,sys; d=json.load(sys.stdin); e=[x for x in d if x['severity']=='error']; print('PASS' if not e else f'FAIL yaml errors: {e[:3]}')"` — PASS (no new YAML errors from --fix)
6. `grep -rl "^example: true" examples/ | wc -l` — prints 12 (the example:true set; lint still skips all 12 — lint-skip intact)
7. LINK-09 fixtures hand-edited: all 10 `examples/dataview-fixtures/fixture-*.md` carry literal self-aliases (title + id) — `python3` glob check in Task 1 verify prints 10 PASS lines
8. Human verify: Obsidian graph connected, including the examples/dataview-fixtures sub-cluster (LINK-10, LINK-09)
</verification>

<success_criteria>
- `bin/lint.sh --category linkres wiki/` reports zero errors — LINK-07 and LINK-08 satisfied
- ~45 wiki/ + 8 kahneman pages self-aliased via `--fix`; 10 dataview-fixture content pages self-aliased via hand-edit (LINK-09 literal remediation, Cycle-2 ruling); 2 scaffolding files (README.md, log.md) documented exempt — LINK-09 satisfied (NOT "12 exempt" and NOT "20 via --fix")
- `[[Bounded Contexts]]` no longer appears in wiki/ body text (reconciled to `[[Bounded Context]]s`) — LINK-08 satisfied
- The two colon-titled decision pages re-parse as valid YAML with string aliases (HIGH BUG #2 did not corrupt them)
- Human checkpoint: Obsidian graph connected, domain-driven-design.md and other previously-orphaned pages are no longer isolated — LINK-10 satisfied
- `bash bin/lint.sh --category yaml wiki/` shows no new YAML errors from --fix writes (all frontmatter valid)
</success_criteria>

<output>
After completion, create `.planning/phases/14-graph-link-resolution/14-03-SUMMARY.md`
</output>
