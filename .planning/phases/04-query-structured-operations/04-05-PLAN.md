---
phase: 04-query-structured-operations
plan: 05
type: execute
wave: 2
depends_on:
  - 04-03
files_modified:
  - AGENTS.md
autonomous: true
requirements:
  - SOPS-01
  - SOPS-02
  - SOPS-03
  - SOPS-04
  - SOPS-05

must_haves:
  truths:
    - "AGENTS.md section 9 references bin/validate-op.sh as enforcement layer with batch validation rule"
    - "AGENTS.md section 12 documents structured operation log entry format per D-20/D-21"
    - "Section 9 includes per-operation preconditions and postconditions"
    - "Section 12 log format distinguishes workflow-level from structured operation entries"
  artifacts:
    - path: "AGENTS.md"
      provides: "Updated sections 9 and 12 with validator reference, per-operation semantics, and operation log format"
      contains: "bin/validate-op.sh"
  key_links:
    - from: "AGENTS.md section 9"
      to: "bin/validate-op.sh"
      via: "Executor model references validator script as enforcement layer"
      pattern: "bin/validate-op.sh"
    - from: "AGENTS.md section 12"
      to: "wiki/log.md"
      via: "Log format specification governs operation log entries"
      pattern: "OPERATION.*target_page"
---

<objective>
Update AGENTS.md executor model (section 9) with validator enforcement reference and per-operation semantics, and update log format (section 12) with structured operation log entries.

Purpose: Completes the structured operations specification by adding deterministic enforcement via bin/validate-op.sh (D-16, D-18), per-operation preconditions/postconditions that prevent ambiguous interpretation, and the structured operation logging format (D-20/D-21). Split from the query workflow plan to reduce semantic load per review feedback.
Output: AGENTS.md sections 9 and 12 updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-query-structured-operations/04-CONTEXT.md
@.planning/phases/04-query-structured-operations/04-RESEARCH.md

<interfaces>
<!-- Current AGENTS.md section 9 executor model (lines 806-818): -->
```
### Executor Model
The LLM proposes operations. Before applying any operation, it MUST validate:
1. Target exists
2. Both pages exist and are distinct (MERGE)
3. Provenance resolves
4. Frontmatter is valid
5. Privacy is respected
```

<!-- Current AGENTS.md section 12 log.md format (lines 1037-1052): -->
```
### log.md (Activity Log)
- Entry format: ## [YYYY-MM-DD] <operation_type> | <description>
- Valid operation types: ingest, query, lint, reflect, update, merge, supersede, archive
```

<!-- AGENTS.md section 13 privacy inheritance rule (line 1096): -->
```
When a wiki page cites sources with mixed privacy tiers, the wiki page inherits 
local_only -- the strictest tier among its contributing sources.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AGENTS.md section 9 with validator enforcement and per-operation semantics</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md (lines 754-818 for section 9)
    - .planning/phases/04-query-structured-operations/04-CONTEXT.md (D-16, D-17, D-18, D-19)
    - bin/validate-op.sh (the script created in Plan 03 -- read to confirm invocation pattern)
  </read_first>
  <action>
After the existing executor model validation rules list and the "If validation fails, the LLM MUST NOT apply the operation..." paragraph (around line 816), add two new subsections:

**Subsection 1: Deterministic Enforcement**

```markdown
#### Deterministic Enforcement

In addition to LLM self-validation, a deterministic bash validator provides mechanical enforcement:

```
bin/validate-op.sh <OPERATION> <target_path> [<second_path>]
```

The LLM MUST run `bin/validate-op.sh` before applying any operation. The validator performs the same 5 checks listed above using file system inspection and YAML parsing -- no LLM judgment involved. If the validator returns FAIL, the operation MUST NOT be applied.

**Batch validation:** When a workflow proposes multiple operations (e.g., an ingest that UPDATEs several pages), validate ALL operations before applying ANY. If any single validation fails, abort the entire batch. This prevents partial application of interdependent changes.
```

**Subsection 2: Per-Operation Semantics (addresses review concern: operation-specific rules prevent drift)**

```markdown
#### Per-Operation Preconditions and Postconditions

Each operation type has specific rules beyond the 5 global checks:

**UPDATE**
- Precondition: Target page exists and has `status: active` (do not UPDATE archived or superseded pages -- un-archive or un-supersede first).
- Postcondition: `updated_at` field is set to today's date. `sources` list includes any new source IDs. Provenance markers are added for new claims.
- Privacy: If new content derives from `local_only` sources but target is `cloud_safe`, STOP -- see Section 13 and query workflow Section 11.2 privacy rules.

**MERGE**
- Precondition: Both pages exist, are distinct, and both have `status: active`.
- Postcondition: One surviving page contains the combined content. The other page has `status: superseded` and `superseded_by` set to the surviving page's ID. `sources` lists from both pages are merged (union). All provenance markers from both pages are preserved.
- Privacy: If either source page is `local_only`, the surviving page MUST be `local_only`.

**SUPERSEDE**
- Precondition: Target page exists, has `status: active`, and `superseded_by` is empty/null.
- Postcondition: Target page has `status: superseded` and `superseded_by` set to the replacing page's ID. The replacing page has `supersedes` set to the target's ID.
- Note: The replacing page must already exist or be created in the same batch.

**ARCHIVE**
- Precondition: Target page exists and has `status: active` (do not archive already-archived pages).
- Postcondition: Target page has `status: archived`. `updated_at` set to today's date. Page remains in its directory but is excluded from active index queries.
- Note: Archive is reversible -- change `status` back to `active` to un-archive.
```

IMPORTANT: Do not modify sections 8 or 10. The new subsections go inside section 9 only.
  </action>
  <verify>
    <automated>grep -q "bin/validate-op.sh" AGENTS.md && grep -q "Per-Operation Preconditions" AGENTS.md && echo "PASS: section 9 updates present" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md section 9 contains "#### Deterministic Enforcement" subsection
    - AGENTS.md section 9 contains `bin/validate-op.sh <OPERATION> <target_path>`
    - AGENTS.md section 9 contains "Batch validation" paragraph per D-18
    - AGENTS.md section 9 contains "#### Per-Operation Preconditions and Postconditions" subsection
    - AGENTS.md section 9 contains precondition/postcondition blocks for all four operations: UPDATE, MERGE, SUPERSEDE, ARCHIVE
    - AGENTS.md UPDATE postcondition includes `updated_at` field requirement
    - AGENTS.md MERGE postcondition includes "sources lists from both pages are merged"
    - AGENTS.md MERGE privacy rule states "If either source page is `local_only`, the surviving page MUST be `local_only`"
    - AGENTS.md section 10 is unchanged
  </acceptance_criteria>
  <done>AGENTS.md section 9 references bin/validate-op.sh as enforcement layer with batch validation rule, and documents per-operation preconditions and postconditions for all four structured operations</done>
</task>

<task type="auto">
  <name>Task 2: Update AGENTS.md section 12 with structured operation log entry format</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md (lines 1025-1052 for section 12)
    - .planning/phases/04-query-structured-operations/04-CONTEXT.md (D-20, D-21)
  </read_first>
  <action>
After the existing log.md section content (around line 1052), add a new subsection:

```markdown
#### Structured Operation Log Entries

When logging individual structured operations (UPDATE, MERGE, SUPERSEDE, ARCHIVE), use this extended format:

```markdown
## [YYYY-MM-DD] OPERATION | target_page

source: source_id
result: what changed (e.g., "added 3 claims, refreshed TL;DR")
reason: one-line rationale
```

This format extends the base log entry format with structured sub-fields for machine-parseability. It applies to individual operations, NOT to workflow-level entries. Workflow-level entries (ingest, query, lint, reflect) use the base format with their own sub-fields as defined in Section 11.

**Examples:**

```markdown
## [2026-04-15] UPDATE | prospect-theory

source: src-2026-04-15-new-article
result: added 2 claims on probability weighting, refreshed TL;DR
reason: new source provides empirical evidence for probability distortion parameters
```

```markdown
## [2026-04-15] MERGE | cognitive-biases

source: n/a (structural reorganization)
result: merged anchoring-bias into cognitive-biases, added anchoring subsection
reason: anchoring-bias page had <3 claims, better as subsection of parent concept
```

```markdown
## [2026-04-15] SUPERSEDE | old-kahneman-summary

source: src-2026-04-15-comprehensive-biography
result: marked old-kahneman-summary as superseded by daniel-kahneman
reason: new comprehensive source makes old summary redundant
```
```

Also update the "Valid operation types" line to clarify the two formats:
Change: `- Valid operation types: \`ingest\`, \`query\`, \`lint\`, \`reflect\`, \`update\`, \`merge\`, \`supersede\`, \`archive\`.`
To: `- Valid operation types: workflow-level (\`ingest\`, \`query\`, \`lint\`, \`reflect\`) and structured operations (\`UPDATE\`, \`MERGE\`, \`SUPERSEDE\`, \`ARCHIVE\`). Structured operations use the extended format below.`

IMPORTANT: Do not modify section 13 (Privacy Routing) which follows section 12.
  </action>
  <verify>
    <automated>grep -q "Structured Operation Log Entries" AGENTS.md && grep -q "OPERATION | target_page" AGENTS.md && echo "PASS: section 12 updates present" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md section 12 contains "#### Structured Operation Log Entries" subsection
    - AGENTS.md section 12 contains the format `## [YYYY-MM-DD] OPERATION | target_page` with source/result/reason fields per D-21
    - AGENTS.md section 12 contains at least two worked examples showing the format (UPDATE and MERGE at minimum)
    - AGENTS.md section 12 valid operation types line distinguishes "workflow-level" from "structured operations"
    - AGENTS.md section 13 (Privacy Routing) is unchanged
  </acceptance_criteria>
  <done>AGENTS.md section 12 documents structured operation log entry format with sub-fields, three worked examples (UPDATE, MERGE, SUPERSEDE), and clear distinction between workflow-level and structured operation entries</done>
</task>

</tasks>

<verification>
- AGENTS.md section 9 references bin/validate-op.sh and documents batch validation and per-operation semantics
- AGENTS.md section 12 has structured operation log entry format with examples
- Sections 8, 10, 11, and 13 are unchanged
</verification>

<success_criteria>
- Executor model fully specified: LLM agents know both the policy (AGENTS.md rules) and enforcement (bin/validate-op.sh), plus per-operation preconditions/postconditions
- Operation logging format supports both workflow-level and individual operation entries with examples
</success_criteria>

<output>
After completion, create `.planning/phases/04-query-structured-operations/04-05-SUMMARY.md`
</output>
