---
phase: 04-query-structured-operations
plan: 04
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - AGENTS.md
autonomous: true
requirements:
  - QURY-03
  - QURY-04
  - QURY-05
  - SOPS-01
  - SOPS-02
  - SOPS-03
  - SOPS-04
  - SOPS-05

must_haves:
  truths:
    - "AGENTS.md section 11.2 is a complete rewritten query workflow with write-back decision rules and delta compilation"
    - "AGENTS.md section 9 references bin/validate-op.sh as enforcement layer"
    - "AGENTS.md section 12 documents structured operation log entry format per D-20/D-21"
    - "Write-back triggers and skip reasons are explicitly documented per D-03/D-04"
    - "Delta compilation uses compilation_status fields from section 5 (added in Plan 01)"
  artifacts:
    - path: "AGENTS.md"
      provides: "Updated sections 9, 11.2, and 12 with complete query workflow, validator reference, and operation log format"
      contains: "write-back"
  key_links:
    - from: "AGENTS.md section 11.2"
      to: "AGENTS.md section 5"
      via: "Query workflow references compilation_status fields for delta compilation"
      pattern: "compilation_status"
    - from: "AGENTS.md section 11.2"
      to: "AGENTS.md section 9"
      via: "Write-back uses structured operations (UPDATE) validated by executor"
      pattern: "UPDATE"
    - from: "AGENTS.md section 9"
      to: "bin/validate-op.sh"
      via: "Executor model references validator script as enforcement layer"
      pattern: "bin/validate-op.sh"
    - from: "AGENTS.md section 12"
      to: "wiki/log.md"
      via: "Log format specification governs operation log entries"
      pattern: "OPERATION.*target_page"
---

<objective>
Rewrite AGENTS.md query workflow (section 11.2), update executor model (section 9), and update log format (section 12) to encode all Phase 4 decisions.

Purpose: Makes the query workflow fully operational with write-back decision rules (D-01 through D-05), delta compilation mechanics (D-06 through D-10), validator enforcement reference (D-16), and structured operation logging (D-20/D-21). This is the core specification work that makes the wiki's interaction layer complete.
Output: AGENTS.md sections 9, 11.2, and 12 updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-query-structured-operations/04-CONTEXT.md
@.planning/phases/04-query-structured-operations/04-RESEARCH.md
@.planning/phases/04-query-structured-operations/04-01-SUMMARY.md

<interfaces>
<!-- Current AGENTS.md section 11.2 query workflow (lines 950-973): -->
```
### 11.2 Query Workflow
Trigger:  User asks a question about the wiki contents
Inputs:   User question (natural language)
Outputs:  Answer with citations, optionally new/updated wiki pages, updated index/log
Commit:   query(<topic>): <one-line summary>

Steps:
1. Read wiki/index.md to find pages relevant to the question.
2. Read TL;DR and Key Facts sections of relevant pages.
3. Read Detail sections only where shallow content is insufficient.
4. Synthesize answer with citations.
5. Mandatory write-back (if novel synthesis).
6. Delta compilation check.
7. Update wiki/index.md if new pages were created.
8. Append entry to wiki/log.md.
9. Commit.
```

<!-- Current AGENTS.md section 9 executor model (lines 806-818): -->
```
### Executor Model
The LLM proposes operations. Before applying any operation, it MUST validate:
1. Target exists
2. Both pages exist and are distinct (MERGE)
3. Provenance resolves
4. Frontmatter is valid
5. Privacy is respected
```

<!-- Current AGENTS.md section 12 log.md format (lines 1037-1052): -->
```
### log.md (Activity Log)
- Entry format: ## [YYYY-MM-DD] <operation_type> | <description>
- Valid operation types: ingest, query, lint, reflect, update, merge, supersede, archive
```

<!-- Compilation status fields added in Plan 01 (AGENTS.md section 5): -->
```yaml
compilation_status: pending | partial | compiled | stale
compiled_against_hash: ""
compiled_targets: []
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite AGENTS.md section 11.2 query workflow with write-back rules and delta compilation</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md (lines 950-973 for current section 11.2; lines 473-548 for section 5 compilation fields added in Plan 01)
    - .planning/phases/04-query-structured-operations/04-CONTEXT.md (D-01 through D-11)
    - .planning/phases/04-query-structured-operations/04-RESEARCH.md (pitfalls 3 and 4: write-back circular refs, privacy violation)
    - .planning/phases/04-query-structured-operations/04-01-SUMMARY.md (compilation status fields now exist)
  </read_first>
  <action>
Replace the entire section 11.2 (from `### 11.2 Query Workflow` to the line before `### 11.3 Lint Workflow`) with a complete rewrite. The new section must be self-contained — an LLM agent reading only this section should be able to execute the full query workflow.

**New section 11.2 structure:**

```markdown
### 11.2 Query Workflow

\`\`\`
Trigger:  User asks a question about the wiki contents
Inputs:   User question (natural language)
Outputs:  Cited answer, optionally new/updated wiki pages, updated index/log
Commit:   query(<topic>): <one-line summary>
\`\`\`

**Steps:**

1. **Search** — Read `wiki/index.md` to find pages relevant to the question. Optionally use `bin/search.sh` to identify candidates.
2. **Shallow read** — Read TL;DR and Key Facts sections of relevant pages (progressive disclosure — shallow first).
3. **Deep read** — Read Detail sections only where shallow content is insufficient to answer the question.
4. **Synthesize** — Compose answer with citations to specific wiki pages and inline provenance markers.
5. **Write-back decision** — Determine whether the answer should be written back to the wiki (see Write-Back Rules below).
6. **Delta compilation** — Check for uncompiled or stale sources relevant to this query (see Delta Compilation below).
7. **Apply write-back** — If write-back is triggered, apply using structured operations (Section 9). Run `bin/validate-op.sh` before applying each operation.
8. Update `wiki/index.md` if new pages were created or existing pages were significantly modified.
9. Append entry to `wiki/log.md` (see Log Entry Format below).
10. Commit (only if wiki was modified): `query(<topic>): <one-line summary>`

#### Write-Back Rules

Write-back is **mandatory** when the answer produces novel or durable synthesis. It is NOT optional — queries that produce reusable knowledge MUST contribute back to the wiki.

**Write back when the answer produces at least one of:**
- A new claim not already captured in the wiki
- A new connection between existing pages or sources
- A meaningful reframing or synthesis of existing material
- A reusable artifact (comparison, overview, decision note)
- A correction to an existing page's framing or status

**Do NOT write back for:**
- Pure lookups of facts already present in the wiki
- Reformatted restatements of a single existing page
- Transient conversational answers with no durable value

**Page targeting:** Use page ownership, not query origin.
- If an existing page clearly owns the topic being synthesized, UPDATE that page.
- If no single page cleanly owns the synthesis, or the output is a distinct reusable artifact (comparison, overview, reflection), CREATE a new page.
- New pages are typed by semantic role (entity, concept, comparison, overview) — NEVER by workflow origin. There is no "query result" page type.

**Privacy inheritance:** If ANY source contributing to the synthesis has `privacy: local_only`, the write-back target MUST be `local_only` (Section 13 — stricter setting always wins).

**Ordering:** The workflow executes linearly: (1) answer with citations, (2) delta compile if needed, (3) write back results. Write-back happens ONCE at the end, not recursively.

#### Delta Compilation

Before or during answer synthesis, check whether relevant sources have uncompiled material:

1. Read source summary pages referenced by or related to the query topic.
2. Check `compilation_status` field (Section 5): if `pending`, `partial`, or `stale`, the source has uncompiled material.
3. **Query-scoped compilation (default):** Compile only claims from uncompiled sources that are relevant to the current question. Log remaining uncompiled material for later pickup.
4. **Full-source compilation (exception):** Only when the source is central to many pages, query-scoped extraction would be wasteful, or the user explicitly requests a fuller refresh.
5. After compiling, update the source summary page: set `compilation_status` to `compiled` (or `partial` if not all claims were compiled), update `compiled_against_hash`, and extend `compiled_targets`.

**Detecting uncompiled material:** Primary mechanism is the `compilation_status` field on source summary pages. Secondary verification: check whether source claims actually appear in target topic pages via provenance markers.

#### Query Log Entry Format

Append to `wiki/log.md`:

\`\`\`markdown
## [YYYY-MM-DD] query | <question summary>

answer: <one-line summary of the answer>
write_back: <WRITE-BACK: trigger met -> UPDATE/CREATE page_id> OR <NO-WRITE-BACK: reason>
delta_compiled: <source_ids compiled, or "none">
pages_affected: <list of page IDs modified or created>
\`\`\`

The write-back decision MUST be logged — structured and terse, stating which trigger was met or why write-back was skipped. This enables auditing.

**Abort conditions:**

- No relevant pages exist AND no sources exist on the topic. Inform the user that the wiki has no information on this topic rather than hallucinating an answer. Log the knowledge gap in `wiki/log.md` so the lint workflow can track it.
```

IMPORTANT: Preserve the exact line `### 11.3 Lint Workflow` that follows — do not modify anything after the end of section 11.2.
  </action>
  <verify>
    <automated>grep -c "write-back\|write_back\|WRITE-BACK\|NO-WRITE-BACK" AGENTS.md | grep -q "[5-9]\|[1-9][0-9]" && echo "PASS: write-back rules present in AGENTS.md" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md section 11.2 contains "Write-Back Rules" subsection
    - AGENTS.md section 11.2 contains "Delta Compilation" subsection
    - AGENTS.md section 11.2 contains "Query Log Entry Format" subsection
    - AGENTS.md contains the string "A new claim not already captured in the wiki"
    - AGENTS.md contains the string "Pure lookups of facts already present"
    - AGENTS.md contains the string "typed by semantic role"
    - AGENTS.md contains the string "compilation_status" in section 11.2 (referencing section 5 fields)
    - AGENTS.md contains "WRITE-BACK:" and "NO-WRITE-BACK:" in the log format example
    - AGENTS.md section 11.2 contains "bin/validate-op.sh" reference in step 7
    - AGENTS.md section 11.2 contains "Privacy inheritance" paragraph referencing Section 13
    - AGENTS.md section 11.3 (Lint Workflow) is unchanged
  </acceptance_criteria>
  <done>AGENTS.md section 11.2 is a complete, self-contained query workflow specification with write-back decision rules per D-01/D-03/D-04/D-05, delta compilation per D-06/D-09/D-10, privacy inheritance, and structured log format</done>
</task>

<task type="auto">
  <name>Task 2: Update AGENTS.md section 9 (executor model) and section 12 (log format)</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md (lines 754-818 for section 9; lines 1025-1052 for section 12)
    - .planning/phases/04-query-structured-operations/04-CONTEXT.md (D-16, D-18, D-20, D-21)
  </read_first>
  <action>
**Section 9 update — Executor Model (around lines 806-818):**

After the existing paragraph "If validation fails, the LLM MUST NOT apply the operation..." (line 816), add a new subsection:

```markdown
### Deterministic Enforcement

In addition to LLM self-validation, a deterministic bash validator provides mechanical enforcement:

```
bin/validate-op.sh <OPERATION> <target_path> [<second_path>]
```

The LLM MUST run `bin/validate-op.sh` before applying any operation. The validator performs the same 5 checks listed above using file system inspection and YAML parsing — no LLM judgment involved. If the validator returns FAIL, the operation MUST NOT be applied.

**Batch validation:** When a workflow proposes multiple operations (e.g., an ingest that UPDATEs several pages), validate ALL operations before applying ANY. If any single validation fails, abort the entire batch. This prevents partial application of interdependent changes.
```

**Section 12 update — log.md format (around lines 1037-1052):**

After the existing log.md section content, add a new subsection:

```markdown
### Structured Operation Log Entries

When logging individual structured operations (UPDATE, MERGE, SUPERSEDE, ARCHIVE), use this extended format:

\`\`\`markdown
## [YYYY-MM-DD] OPERATION | target_page

source: source_id
result: what changed (e.g., "added 3 claims, refreshed TL;DR")
reason: one-line rationale
\`\`\`

This format extends the base log entry format with structured sub-fields for machine-parseability. It applies to individual operations, NOT to workflow-level entries. Workflow-level entries (ingest, query, lint, reflect) use the base format with their own sub-fields as defined in Section 11.

**Example:**

\`\`\`markdown
## [2026-04-15] UPDATE | prospect-theory

source: src-2026-04-15-new-article
result: added 2 claims on probability weighting, refreshed TL;DR
reason: new source provides empirical evidence for probability distortion parameters
\`\`\`
```

Also update the "Valid operation types" line to clarify the two formats:
Change: `- Valid operation types: \`ingest\`, \`query\`, \`lint\`, \`reflect\`, \`update\`, \`merge\`, \`supersede\`, \`archive\`.`
To: `- Valid operation types: workflow-level (\`ingest\`, \`query\`, \`lint\`, \`reflect\`) and structured operations (\`UPDATE\`, \`MERGE\`, \`SUPERSEDE\`, \`ARCHIVE\`). Structured operations use the extended format below.`
  </action>
  <verify>
    <automated>grep -q "bin/validate-op.sh" AGENTS.md && grep -q "Structured Operation Log Entries" AGENTS.md && echo "PASS: section 9 and 12 updates present" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md section 9 contains "### Deterministic Enforcement" subsection
    - AGENTS.md section 9 contains `bin/validate-op.sh <OPERATION> <target_path>`
    - AGENTS.md section 9 contains "Batch validation" paragraph per D-18
    - AGENTS.md section 12 contains "### Structured Operation Log Entries" subsection
    - AGENTS.md section 12 contains the format `## [YYYY-MM-DD] OPERATION | target_page` with source/result/reason fields per D-21
    - AGENTS.md section 12 contains a worked example showing the format
    - AGENTS.md section 12 valid operation types line distinguishes workflow-level from structured operations
    - AGENTS.md section 13 (Privacy Routing) is unchanged
  </acceptance_criteria>
  <done>AGENTS.md section 9 references bin/validate-op.sh as enforcement layer with batch validation rule; section 12 documents structured operation log entry format with sub-fields and worked example</done>
</task>

</tasks>

<verification>
- AGENTS.md section 11.2 is a complete query workflow with write-back rules, delta compilation, and log format
- AGENTS.md section 9 references bin/validate-op.sh and documents batch validation
- AGENTS.md section 12 has structured operation log entry format with example
- Sections 11.1, 11.3, 11.4, and 13 are unchanged
- grep -c "## " AGENTS.md shows same total section count (no sections added/removed at top level)
</verification>

<success_criteria>
- Query workflow is fully specified: any LLM agent can follow section 11.2 to answer questions, decide on write-back, perform delta compilation, and log the result
- Executor model references deterministic validator
- Operation logging format supports both workflow-level and individual operation entries
</success_criteria>

<output>
After completion, create `.planning/phases/04-query-structured-operations/04-04-SUMMARY.md`
</output>
