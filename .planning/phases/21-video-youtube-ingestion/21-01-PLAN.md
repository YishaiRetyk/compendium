---
phase: 21-video-youtube-ingestion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - schema/reference/video-ingestion.md
  - schema/reference/source-types.md
  - schema/reference/frontmatter.md
  - schema/workflows/ingest.md
  - AGENTS.md
  - CLAUDE.md
  - schema/AGENTS.template.md
  - schema/fixtures/canonical-AGENTS.md
autonomous: true
requirements: [VID-01, VID-02, VID-03]
user_setup: []

must_haves:
  truths:
    - "D-01: the convention specifies the [H:MM:SS] SPEAKER: text line grammar (timestamp first, speaker prefix part of the line text), which the existing audit TS_RE resolves with zero resolver change"
    - "D-02: the convention mandates per-segment timestamp density — one [H:MM:SS] line per STT segment (~2–5s), as the tool emits, with no paragraph-clustering/merge step — for finest #t anchor precision with zero post-processing"
    - "D-03: speaker labels appear ONLY for multi-speaker videos; single-speaker videos omit labels entirely"
    - "D-04: video-ingestion.md defines a tool-generic acquisition contract (any yt-dlp-or-equivalent + timestamped STT emitting the D-01 grammar) and ships NO bin/ script and NO named worked instance in the template-public doc"
    - "D-05: video is transcript-only, a single .md file — no bundle dir, no --asset mechanism (no committable video binary)"
    - "D-06: the convention states the link-rot drift stance — the committed transcript + frontmatter is the durable archive; url is a courtesy pointer that may rot; no drift machinery (VID-03)"
    - "D-07: extraction_tool/extraction_model/extraction_date are convention-only and explicitly NOT lint-enforced (no mechanical signal distinguishes an STT transcript from one built off official captions)"
    - "D-08: the convention permits an OPTIONAL ## Description section between frontmatter and transcript — included case-by-case at ingest when the video description carries substance (reference links, chapters, named sources), skipped for pure promo; agent/human judgment, no enforcement"
    - "D-09: clean spoken-word audio keeps transcript's sourced default + MANDATED claim-level hedging on the STT failure surface; degraded audio gets page-level tentative + N=3 spot-verify"
    - "D-10: spot-verification target is the live url re-watched at the segment's #t timestamp (no local asset exists); a rotted video stays tentative"
    - "D-11: claims from video sources keep support_type: direct — transcription risk is carried by epistemic_status, NEVER by support type"
    - "D-14: source-types.md section-4 video row is finalized as a sub-case of transcript (Acquisition always; Epistemic Default degraded-audio only) with a Convention Doc pointer to video-ingestion.md"
    - "VID-02: frontmatter.md documents the five video fields (url, channel, title, publish_date, duration); claims use the existing #t<start>-<end> locators (provenance.md, not redefined)"
    - "AGENTS.md and CLAUDE.md both carry a routing-table row for video-ingestion.md and are byte-identical"
  artifacts:
    - path: "schema/reference/video-ingestion.md"
      provides: "Authoritative video-as-sub-case-of-transcript convention + tool-generic acquisition runbook (lazy-loaded)"
      contains: "support_type: direct"
      min_lines: 55
    - path: "schema/reference/source-types.md"
      provides: "Finalized section-4 video registry row (sub-case of transcript + Convention Doc pointer)"
      contains: "video-ingestion.md"
    - path: "schema/reference/frontmatter.md"
      provides: "The five video sub-case fields documented in the Source Summary block (neutral placeholders)"
      contains: "publish_date"
    - path: "schema/workflows/ingest.md"
      provides: "Pass-0 video sub-bullet: classify to transcript, then layer the video convention"
      contains: "video-ingestion.md"
    - path: "AGENTS.md"
      provides: "Routing-table row pointing at video-ingestion.md"
      contains: "video-ingestion.md"
    - path: "schema/AGENTS.template.md"
      provides: "Wizard-source routing row for video-ingestion.md"
      contains: "video-ingestion.md"
    - path: "schema/fixtures/canonical-AGENTS.md"
      provides: "Regenerated wizard-render fixture in sync with the template edit (setup-parity byte-equality gate)"
      contains: "video-ingestion.md"
  key_links:
    - from: "schema/reference/source-types.md"
      to: "schema/reference/video-ingestion.md"
      via: "Convention Doc column pointer in the section-4 video row"
      pattern: "video-ingestion.md"
    - from: "AGENTS.md"
      to: "schema/reference/video-ingestion.md"
      via: "routing-table row"
      pattern: "video-ingestion.md"
    - from: "CLAUDE.md"
      to: "AGENTS.md"
      via: "byte-equality (bin/sync-claude.sh)"
      pattern: "video-ingestion.md"
---

<objective>
Author the authoritative video convention doc `schema/reference/video-ingestion.md` (video-as-sub-case-of-`transcript` convention + tool-generic acquisition runbook) and wire it into the schema machinery exactly as Phase 20 wired `pdf-ingestion.md`: finalize the `source-types.md` section-4 `video` registry row, document the five VID-02 frontmatter fields in `frontmatter.md`, add a Pass-0 video pointer to `ingest.md`, and add a routing-table row to AGENTS.md + byte-synced CLAUDE.md + the template + regenerated fixture.

Purpose: video is a **sub-case of `transcript`** (D-14), NOT a new `source_type` enum value. The convention is lazy-loaded: not needed at Pass-0 classification time, so it lives in its own file with only a one-row pointer in source-types.md. This is a deliberately LIGHTER re-skin of Phase 20 — it DROPS the lint mandate (D-07), DROPS the `--asset` mechanism (D-05), DROPS the named worked instance + `bin/` script from the runbook (D-04), and re-aims spot-verification at the live `url` (D-10). This plan covers VID-01 (doc half), VID-02 (convention + fields), and VID-03 (drift stance + epistemic policy).
Output: one new authoritative file + three in-place edits + the byte-synced CLAUDE.md mirror + template + regenerated fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-video-youtube-ingestion/21-CONTEXT.md
@.planning/phases/21-video-youtube-ingestion/21-PATTERNS.md

<interfaces>
Structural model to mirror EXACTLY (it is the #1 re-skin analog): schema/reference/pdf-ingestion.md — its 8-section skeleton (header banner → Classification → Frontmatter fields → locator usage → Tiered epistemic policy → Acquisition runbook → Ingest checklist → See Also). Swap PDF→video specifics and DROP the four divergences below.

THE FOUR DELIBERATE DIVERGENCES FROM pdf-ingestion.md (non-negotiable, locked decisions):
  1. (D-07) DROP the lint mandate. pdf-ingestion.md §2 L39 says "Lint **requires** all four fields when original_asset points at a *.pdf." video-ingestion.md states the OPPOSITE: extraction_* fields are convention-only, NOT lint-enforced.
  2. (D-05) DROP the --asset step. pdf-ingestion.md §6 step 2 runs "bin/ingest.sh --asset". video-ingestion.md commits a single .md file, no bundle, no --asset.
  3. (D-04) DROP the named worked instance + bin/ script from the runbook. pdf-ingestion.md §5 names olmOCR/Ollama/bin/pdf-extract.sh (legal — public tools). video-ingestion.md names ONLY a generic contract (yt-dlp-or-equivalent + a timestamped STT) and ships no script and no worked-instance name (the personal STT tool is NOT nameable on a template-public surface).
  4. (D-10) RE-AIM spot-verification. pdf-ingestion.md §4 reads against a co-located original_asset. video-ingestion.md re-watches the live url at the segment's #t timestamp (no local asset exists).

source-types.md section-4 registry — CURRENT provisional video row (L50):
  | `video` | sub-case of `transcript` (provisional) | Acquisition (provisional) | (Phase 21) | Timestamp locators (`#t`) already exist; Phase 21 walk-through finalizes verdict and dimension assessment |
source-types.md section-4 registry — FINALIZE to (drop "provisional", fill Convention Doc, rewrite Notes — mirror the finalized pdf row directly above it at L49):
  | `video` | sub-case of `transcript` | Acquisition (always); Epistemic Default (degraded audio only) | `schema/reference/video-ingestion.md` | Timestamp locators (`#t`) + the `[H:MM:SS]` line grammar already exist; download + STT replaces recorded acquisition. Claims stay `support_type: direct`; degraded audio gets `tentative` + N=3 spot-verification (D-09). |

The #t<start>-<end> locator grammar is ALREADY fully specified in schema/reference/provenance.md L31 (`#t00:12:10-00:12:48`, with a worked transcript example at L78). DO NOT edit provenance.md and DO NOT redefine the grammar — reference it.

frontmatter.md Source Summary Additional Fields block (the `type: source` YAML at L62-80) — the PDF sub-case block at L69-74 is the exact placement to parallel. `url` is already a base source-summary field at L64 — reuse it, do not duplicate.

AGENTS.md routing table (the "Resolvable references" table L45-64) — add ONE row adjacent to the pdf-ingestion.md row at L51 (both are acquisition sub-case docs). AGENTS.md L7 carries `<!-- inclusion-audit: N lines @ DATE -->` — re-record after adding the row.

The audit TS_RE (bin/audit-claims.sh L392): `^\s*\[?(\d{1,2}:\d{2}(?::\d{2})?)\]?` — anchors at line start, allows 1-2 digit hour, treats everything after the timestamp (including a `SPEAKER:` prefix) as opaque line text. This is why D-01's grammar + D-03's SPEAKER: prefix need ZERO resolver change, and why `[H:MM:SS]`, `[HH:MM:SS]`, and `[MM:SS]` all resolve (pick one hour-digit form in the convention — Claude's Discretion).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author schema/reference/video-ingestion.md (convention + tool-generic runbook)</name>
  <files>schema/reference/video-ingestion.md</files>
  <read_first>
    - schema/reference/pdf-ingestion.md (the FULL 89-line file — the #1 structural analog; mirror its 8-section skeleton, then apply the four divergences)
    - schema/reference/source-types.md (section 5 "research-report — Worked Secondary Instance" L55-104 + section 6 Ingest Checklist — secondary structural model; header style L1-6)
    - schema/reference/provenance.md (L31 #t locator row + L78 worked transcript example — REFERENCE, do NOT redefine)
    - sources/2026/2026-05/2026-05-03-is-this-the-only-skill-left.md (the de-facto committed transcript: `[H:MM:SS] text` grammar, single-speaker no-labels — the exemplar D-01/D-03 formalize)
    - .planning/phases/21-video-youtube-ingestion/21-CONTEXT.md (D-01..D-14 — the locked decisions this doc encodes)
    - .planning/phases/21-video-youtube-ingestion/21-PATTERNS.md ("schema/reference/video-ingestion.md" section — the exact per-section re-skin shapes + verbatim-in-shape text blocks)
    - CLAUDE.md (section 3 "What Agents Must NOT Do" — the neutrality placeholder rule for template-public files)
  </read_first>
  <action>
    Create `schema/reference/video-ingestion.md` as a self-contained authoritative reference. Mirror the pdf-ingestion.md header + 8-section structure, retitled for video, applying the FOUR divergences. Required sections, in order:

    1. Header (copy pdf-ingestion.md L1-6 style): an h1 "# Video Ingestion", then a blockquote "> Agent-authoritative reference for the video-as-sub-case-of-transcript convention and the YouTube acquisition runbook." and "> The AGENTS.md routing table points here. If you find a discrepancy between this file and AGENTS.md, this file wins." Then a one-line "Use this file when acquiring a YouTube/video source (running the acquisition runbook), authoring the source summary for a video-acquired transcript, or deciding the epistemic status of claims drawn from STT-extracted spoken text." sentence.

    2. Classification — video is a sub-case of `transcript` (D-14). At Pass 0 the content classifies to the `transcript` parent type; the video convention layers on top. Walk the 5 dimensions explicitly in a table (mirror pdf-ingestion.md §1 table shape):
       | Dimension | Effect of the video format |
       | **Acquisition** | Changes (download via a yt-dlp-equivalent + timestamped STT vs. a locally-recorded transcript). |
       | **Locator** | **Unchanged** — `#t<start>-<end>` already exists (`schema/reference/provenance.md`); the STT emits the `[H:MM:SS]` lines it resolves against. |
       | **Extraction Granularity** | **Unchanged** — inherited from `transcript` (utterance/paragraph clusters). |
       | **Drift** | **Unchanged** — static after recording; the committed transcript is the durable archive, link-rot of `url` is the only concern (D-06). |
       | **Epistemic Default** | Changes **conditionally** — only for degraded audio. Clean spoken-word keeps `transcript`'s `sourced` default. See the Tiered Epistemic Policy section. |
       Conclusion sentence (mirror pdf's "does NOT earn a new enum value"): video is a sub-case of `transcript`, not a new `source_type` — only Acquisition changes unconditionally (and Epistemic Default conditionally). Format-orthogonal framing carried forward from Phase 20 D-05.

    3. Frontmatter Fields — the five VID-02 fields as a flat snake_case table (mirror pdf §2 table shape BUT WITHOUT the lint mandate):
       | Field | Meaning |
       | `url` | The video URL — a courtesy pointer that may rot (D-06). Base source-summary field, reused (already in frontmatter.md). |
       | `channel` | Publishing channel / author. |
       | `title` | Video title. |
       | `publish_date` | ISO 8601 video publication date. |
       | `duration` | Runtime (e.g. `~12 min`). |
       Then the convention-only extraction fields: extraction_tool / extraction_model / extraction_date — recorded on STT-extracted transcripts but explicitly NOT lint-enforced (D-07). State the reason plainly: unlike PDF's `original_asset → *.pdf` trigger, there is NO mechanical signal distinguishing an STT-built transcript from one built off official captions, so a lint rule would guess. **CRITICAL DIVERGENCE (D-07):** do NOT copy pdf-ingestion.md §2 L39 ("Lint **requires** all four fields ...") — write the opposite. The literal string "Lint requires" must NOT appear in this section in a mandate sense; state these fields are convention-only.

    4. `#t` Locator Usage (mirror pdf §3) — REFERENCE provenance.md, do NOT redefine: claims from video sources use the existing `#t<start>-<end>` timestamp locators, which resolve against the `[H:MM:SS]` lines the STT emits — see `schema/reference/provenance.md`. Then the support-type paragraph (re-skin pdf §3 L45, swap #p→#t, OCR→STT): claims keep `support_type: direct` (D-11). A video is a PRIMARY transcript source: STT is *extraction*, not *derivation*. Transcription risk is carried by the page's `epistemic_status`, NEVER by the support type. Using the derived support type here is an epistemic-laundering error. (Write "the derived support type" in prose — do NOT write the literal frontmatter string `support_type: derived` anywhere in this file, so the negative acceptance grep stays meaningful.)

    5. Tiered Epistemic Policy (D-09, VID-03 failure surface) — adapt pdf §4 to audio:
       - **Clean spoken-word audio** → the `transcript` type's normal `sourced` default. The convention MANDATES claim-level hedging on the STT failure surface: proper nouns / named entities, technical terms, numbers & statistics, and anything the speaker themselves hedges (D-09 — formalizes what the existing two transcripts already do inline).
       - **Degraded audio** (music bed, heavy crosstalk, poor mic, heavy accent) → page-level `epistemic_status: tentative` + a mandatory spot-verification of **N = 3 sampled segments** (D-09, PDF-symmetric). Pick an N-segment selection method (first/middle/last by analogy to PDF, or another — Claude's Discretion; state which).
       **Spot-verification target DIVERGES from PDF (D-10):** there is NO local asset (D-05), so verification re-watches the live `url` at the segment's `#t` timestamp (confirm the transcript matches the audio). Convention caveat: if the video is already rotted, the segment stays `tentative` (cannot verify what is gone). Read-only; reuse `bin/audit-claims.sh` for `#t` resolution where useful (NO new audit machinery). Re-aim the pdf "confident hallucination" failure-mode paragraph at the STT failure surface (one or two sentences of generic prose: STT can emit plausible-but-wrong tokens on degraded audio — wrong proper nouns, mangled numbers).

    6. Acquisition Runbook — TOOL-GENERIC CONTRACT ONLY (D-04, the highest-neutrality-risk section). Write the contract as: "**Generic contract (D-04):** any pipeline of a video downloader (`yt-dlp` or equivalent) + a timestamped speech-to-text engine that emits the `[H:MM:SS] SPEAKER: text` line grammar (D-01) satisfies this convention. No repo script is shipped; the convention is tool-generic." `yt-dlp` is a public tool name and is PERMITTED. **DO NOT name a worked instance** (contrast pdf-ingestion.md §5 L62 which named bin/pdf-extract.sh) — the personal STT tool name, its asr/diarize module names, and any private model tag must NOT appear here. There is NO `bin/` script for video. Note the `[H:MM:SS]` timestamp stays FIRST on every line so the audit resolver matches at line start, and the optional `SPEAKER:` prefix (multi-speaker only, D-03) is opaque line text needing zero resolver change. Note hour-digit normalization is tolerated by the resolver either way — pick one form for the convention.

    7. Ingest Checklist (mirror pdf §6, DROP the --asset line — D-05):
       1. Run the (tool-generic) URL → timestamped-transcript pipeline.
       2. Commit the transcript as a single `.md` file (no bundle dir — D-05), rich frontmatter + `[H:MM:SS]` lines. Optional `## Description` section between frontmatter and transcript when the video description carries substance — reference links, chapters, named sources (D-08); skipped when pure promo.
       3. Author the source summary with `source_type: transcript` + the five video frontmatter fields.
       4. Claims use `#t<start>-<end>` with `support_type: direct`; mandate claim-level hedging on the STT failure surface.
       5. If audio is degraded → page-level `tentative` + spot-verify N=3 segments against the live `url`; otherwise → `sourced`.

    8. See Also — bare path-stub pointers to source-types.md, frontmatter.md, provenance.md, schema/workflows/ingest.md (Phase 16 bare-pointer pattern).

    NEUTRALITY (MUST — template-public file): use abstract placeholders <video-title>, <channel-name>, <YYYY-MM-DD-slug>, <stt-tool>, <asr-model> for any example value. The public tool name `yt-dlp` is PERMITTED. The personal STT tool name and any private vault slug are FORBIDDEN here.
  </action>
  <verify>
    <automated>cd /home/yishai/Documents/compendium && test -f schema/reference/video-ingestion.md && grep -q 'support_type: direct' schema/reference/video-ingestion.md && ! grep -q 'support_type: derived' schema/reference/video-ingestion.md && grep -qi 'tentative' schema/reference/video-ingestion.md && grep -q 'yt-dlp' schema/reference/video-ingestion.md && bash bin/check-neutrality.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -f schema/reference/video-ingestion.md` succeeds and `[ "$(wc -l < schema/reference/video-ingestion.md)" -ge 55 ]`
    - All five VID-02 fields present: `grep -q '`url`'` and `grep -q channel` and `grep -q '`title`'` and `grep -q publish_date` and `grep -q duration` against the file all succeed
    - `grep -q 'sub-case of `transcript`' schema/reference/video-ingestion.md` succeeds (D-14 verdict)
    - Tiered policy present: `grep -qi tentative` and `grep -qi 'spot-verif'` and `grep -qi 'degraded'` against the file all succeed (D-09)
    - `grep -qE 'N = 3|N=3' schema/reference/video-ingestion.md` succeeds (D-09 three-segment spot-verify)
    - `grep -q 'support_type: direct' schema/reference/video-ingestion.md` succeeds AND `! grep -q 'support_type: derived' schema/reference/video-ingestion.md` (D-11)
    - `grep -q 'yt-dlp' schema/reference/video-ingestion.md` succeeds (generic-contract runbook present)
    - DIVERGENCE D-04 — no bin/ script named: `! grep -q 'bin/video-extract' schema/reference/video-ingestion.md` AND `! grep -q 'bin/pdf-extract' schema/reference/video-ingestion.md`
    - DIVERGENCE D-05 — no --asset step: `! grep -q -- '--asset' schema/reference/video-ingestion.md`
    - DIVERGENCE D-07 — convention-only, no lint mandate: `! grep -qi 'lint requires' schema/reference/video-ingestion.md` AND the file states extraction fields are convention-only (`grep -qi 'convention-only'`)
    - DIVERGENCE D-06 — link-rot drift stance: `grep -qi 'link-rot' schema/reference/video-ingestion.md` AND `grep -qi 'durable archive' schema/reference/video-ingestion.md`
    - `bash bin/check-neutrality.sh` exits 0 (no personal STT tool name or vault slug leaked into the template-public doc)
  </acceptance_criteria>
  <done>video-ingestion.md exists as an authoritative file covering classification (sub-case of transcript), the five frontmatter fields + convention-only extraction fields, #t locator usage with support_type direct, the tiered epistemic policy (clean/degraded with live-url spot-verify), the tool-generic acquisition runbook (no worked-instance name, no bin/ script), the single-file ingest checklist (no --asset), and a See Also block. The four PDF divergences (D-04/D-05/D-06/D-07/D-10) are all applied. Passes neutrality.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize source-types.md video row + frontmatter.md fields + ingest.md pointer</name>
  <files>schema/reference/source-types.md, schema/reference/frontmatter.md, schema/workflows/ingest.md</files>
  <read_first>
    - schema/reference/source-types.md (section 3 Retro-fit Table L33-41 for the transcript row vocabulary; section 4 Evaluated Candidates registry L47-53, the provisional video row at L50, the finalized pdf row at L49 to mirror)
    - schema/reference/frontmatter.md (Source Summary Additional Fields block L58-80 — the PDF sub-case block L69-74 is the placement to parallel; `url` already at L64)
    - schema/workflows/ingest.md (Pass-0 step 3 — the research-report + PDF sub-bullets are the precedent to parallel; grep for `pdf-ingestion.md` to find the PDF sub-bullet)
    - .planning/phases/21-video-youtube-ingestion/21-PATTERNS.md (source-types.md, frontmatter.md sections — exact edit shapes + the neutral video frontmatter block)
  </read_first>
  <action>
    Three in-place edits:

    (a) schema/reference/source-types.md section 4 — replace the provisional video row (L50). The new row's five columns are: Candidate = "`video`"; Verdict = "sub-case of `transcript`"; Dimensions That Change = "Acquisition (always); Epistemic Default (degraded audio only)"; Convention Doc = "`schema/reference/video-ingestion.md`"; Notes = "Timestamp locators (`#t`) + the `[H:MM:SS]` line grammar already exist; download + STT replaces recorded acquisition. Claims stay `support_type: direct`; degraded audio gets `tentative` + N=3 spot-verification (D-09)." Drop "(provisional)" from both the Verdict and Dimensions cells. Do NOT add `video` to the section-3 Retro-fit Table (that table is the 7 enum values; video is absorbed by the existing `transcript` row at L37 — leave that row untouched). Do NOT touch the `pdf` or `repository` rows.

    (b) schema/reference/frontmatter.md — in the Source Summary Additional Fields YAML block, add the video sub-case fields parallel to the PDF block (after the PDF block at L69-74, before the `# Compilation tracking` comment). Insert a blank line, then a comment "# Video sub-case fields (transcript parent) — present on video-acquired transcript sources.", then a comment "# extraction_* are convention-only (NOT lint-enforced — see schema/reference/video-ingestion.md).", then these field lines with NEUTRAL placeholder example values:
        channel: "<channel-name>"
        publish_date: YYYY-MM-DD
        duration: "~12 min"
        extraction_tool: "<stt-tool>"
        extraction_model: "<asr-model>"
        extraction_date: YYYY-MM-DD
        Do NOT re-add `url` (it already exists at L64 — reuse it; the convention doc notes url is the reused base field). Do NOT re-add `title` (it is a base field). Keep the existing flat snake_case style; do NOT add a nested YAML block. **NEUTRALITY DIVERGENCE from the PDF block:** unlike the PDF block which names a public model tag (`richardyoung/olmocr2:7b-q8`), the video extraction_tool/extraction_model values MUST be placeholders (`<stt-tool>`, `<asr-model>`) — this file is template-public and the personal STT tool name is forbidden. If the file documents source-summary fields in a Field Descriptions table, add a one-row description for `channel`, `publish_date`, `duration` matching that table's style (marked video-sub-case / conditional); otherwise the inline YAML comments suffice.

    (c) schema/workflows/ingest.md — after the PDF sub-bullet under the Pass-0 step (find it by grepping `pdf-ingestion.md`), add a parallel video sub-bullet (one line, indented to match): "   - **Video / YouTube sources:** a video is a sub-case of `transcript`, NOT a new source_type. Classify to `transcript` as normal; then layer the video convention (the five frontmatter fields, `#t` locators, tiered epistemic policy, link-rot drift stance). See `schema/reference/video-ingestion.md`." Keep it a one-line pointer (lazy-load discipline). Do NOT introduce a `video` source_type.
  </action>
  <verify>
    <automated>cd /home/yishai/Documents/compendium && grep -q 'video-ingestion.md' schema/reference/source-types.md && grep -q 'publish_date' schema/reference/frontmatter.md && grep -q 'video-ingestion.md' schema/workflows/ingest.md && bash bin/lint.sh --ci --dry-run --category yaml >/dev/null 2>&1 && bash bin/check-neutrality.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'video-ingestion.md' schema/reference/source-types.md` succeeds (Convention Doc pointer in the section-4 video row)
    - The video REGISTRY ROW no longer says provisional — anchor the negative grep to the table row, not the whole file: `! grep -E '^\| .video.' schema/reference/source-types.md | grep -q provisional` (no line beginning with the video table cell still contains "provisional")
    - `grep -qE '^\| .video. \| sub-case of .transcript.' schema/reference/source-types.md` succeeds (the finalized verdict cell)
    - The section-3 Retro-fit Table still has exactly 7 type rows: `[ "$(grep -cE '^\| \`(article|paper|transcript|journal|data|image|research-report)\`' schema/reference/source-types.md)" -eq 7 ]`
    - Video fields present in frontmatter.md: `grep -q channel` and `grep -q publish_date` and `grep -q duration` against schema/reference/frontmatter.md all succeed
    - frontmatter.md video block uses neutral placeholders, not the personal tool: `grep -q '<stt-tool>' schema/reference/frontmatter.md` succeeds (and the personal tool name is absent — covered by bin/check-neutrality.sh)
    - `grep -q 'video-ingestion.md' schema/workflows/ingest.md` succeeds (Pass-0 one-line pointer)
    - `bash bin/lint.sh --ci --dry-run --category yaml` exits 0 (scope the gate to the touched category; --dry-run keeps the verification run from rewriting lint-report.md/log.md; no source page carries the video extraction fields yet so nothing new fires)
    - `bash bin/check-neutrality.sh` exits 0
  </acceptance_criteria>
  <done>The source-types.md video row is finalized (sub-case-of-transcript verdict + Convention Doc pointer, no longer provisional), the five video fields are documented in frontmatter.md with neutral placeholders, and ingest.md Pass-0 has a one-line video pointer. Lint (yaml) stays green; the 7-value source_type enum and the transcript Retro-fit row are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Add the video-ingestion.md routing-table row to AGENTS.md + byte-sync CLAUDE.md + template + regenerated fixture</name>
  <files>AGENTS.md, CLAUDE.md, schema/AGENTS.template.md, schema/fixtures/canonical-AGENTS.md</files>
  <read_first>
    - AGENTS.md (the "Resolvable references" routing table L45-64 — the row format to copy; the pdf-ingestion.md row at L51; the inclusion-audit comment at L7)
    - schema/AGENTS.template.md (the wizard-source routing table — the pdf-ingestion.md row at L52 is the placement to parallel)
    - schema/fixtures/canonical-AGENTS.md (the wizard-render fixture — the pdf-ingestion.md row at L52; this file is regenerated, never hand-edited)
    - schema/fixtures/README.md (the Regeneration Rule — whenever schema/AGENTS.template.md changes, canonical-AGENTS.md MUST be regenerated in the same commit)
    - bin/sync-claude.sh (the cp+cmp byte-equality sync; CLAUDE.md is NEVER hand-edited)
    - .planning/phases/20-pdf-ingestion/20-02-PLAN.md (Task 3 — the exact routing+template+fixture recipe this task parallels)
  </read_first>
  <action>
    1. Edit AGENTS.md only (it is the source of truth). In the "Resolvable references" routing table, add ONE new row immediately AFTER the existing "| Ingesting a PDF source (acquisition runbook + sub-case convention) | `schema/reference/pdf-ingestion.md` |" row (topical grouping — both are acquisition sub-case docs): "> | Ingesting a video/YouTube source (acquisition runbook + sub-case convention) | `schema/reference/video-ingestion.md` |". Match the EXACT "> | ... | ... |" blockquote-table cell format of the surrounding rows (note the leading "> " on each routing-table row).

    2. Update the AGENTS.md inclusion-audit baseline (the resident-core header at AGENTS.md L7 requires every added line to be justified and the baseline re-recorded): the new row passes the inclusion test as `dispatch` (a routing-table pointer). Update the `<!-- inclusion-audit: N lines @ DATE -->` comment at AGENTS.md L7 so N equals the new actual line count (`wc -l < AGENTS.md` AFTER the row is added) and the date is today's ISO date (2026-06-14).

    3. Do NOT hand-edit CLAUDE.md. Regenerate it from AGENTS.md: run `bash bin/sync-claude.sh` then `git add CLAUDE.md`. The Phase 09.1-02 precedent: `bash bin/sync-claude.sh && git add CLAUDE.md` BEFORE committing collapses the 2-attempt path to 1.

    4. Mirror the routing row into `schema/AGENTS.template.md` (the wizard source consumed by `bin/init-wizard.sh`; routers generated for new vaults would otherwise miss video-ingestion.md). Add ONE row immediately after the template's pdf-ingestion.md row (L52), matching the SAME "> | ... | ... |" blockquote row format (the template's routing rows carry the same leading "> " blockquote prefix as AGENTS.md — do NOT drop it): "> | Ingesting a video/YouTube source (acquisition runbook + sub-case convention) | `schema/reference/video-ingestion.md` |". Do NOT touch the template's `{{...}}` placeholders or any other section.

    5. Regenerate `schema/fixtures/canonical-AGENTS.md` (the template edit in step 4 changes the wizard render, and `tests/phase-08/test_canonical_byte_equality.sh` + the `.github/workflows/setup-parity.yml` CI job assert byte-equality of the render against this fixture — a template edit WITHOUT the regenerated fixture in the same commit fails CI). Follow the schema/fixtures/README.md Regeneration Rule exactly: `bash bin/init-wizard.sh --answers-file schema/fixtures/canonical-answers.yaml --render-to /tmp/wz-regen && cp /tmp/wz-regen/AGENTS.md schema/fixtures/canonical-AGENTS.md`. Then confirm with `bash tests/phase-08/test_canonical_byte_equality.sh` (exit 0).

    6. The video-ingestion.md file MUST already exist (Task 1) so the `routing` lint category validates the new row points at a real file in the same commit.

    7. Pre-commit gates that must pass on this commit: `bin/sync-claude.sh --check` (byte-equality), `bin/check-neutrality.sh`, `bin/gen-skills.sh --check`, `bash tests/phase-08/test_canonical_byte_equality.sh` (fixture parity). Run them before committing.

    8. COMMIT SHAPE: Tasks 1-3 land as ONE `schema(21):` commit — one logical operation (CLAUDE.md §3). This also makes two atomicity requirements trivial: the routing row and the file it points at land together (step 6), and the template edit and the regenerated fixture land together (the setup-parity CI gate fails if they are split across commits).
  </action>
  <verify>
    <automated>cd /home/yishai/Documents/compendium && grep -q 'video-ingestion.md' AGENTS.md && grep -q 'video-ingestion.md' CLAUDE.md && grep -q 'video-ingestion.md' schema/AGENTS.template.md && grep -q 'video-ingestion.md' schema/fixtures/canonical-AGENTS.md && bash bin/sync-claude.sh --check && bash tests/phase-08/test_canonical_byte_equality.sh >/dev/null 2>&1 && bash bin/lint.sh --ci --dry-run --category routing >/dev/null 2>&1 && bash bin/check-neutrality.sh && bash bin/gen-skills.sh --check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'video-ingestion.md' AGENTS.md` succeeds (routing row added)
    - `grep -q 'video-ingestion.md' CLAUDE.md` succeeds (mirror synced)
    - `grep -q 'video-ingestion.md' schema/AGENTS.template.md` succeeds (wizard-source routing propagated)
    - `bash bin/sync-claude.sh --check` exits 0 (AGENTS.md and CLAUDE.md byte-identical)
    - `cmp -s AGENTS.md CLAUDE.md` exits 0 (direct byte-equality confirmation)
    - `bash bin/lint.sh --ci --dry-run --category routing` exits 0 (the new row points at an existing file; --ci makes the exit code meaningful, --dry-run keeps the verification run from rewriting lint-report.md/log.md)
    - Inclusion-audit baseline updated: `[ "$(grep -oP 'inclusion-audit: \K[0-9]+' AGENTS.md)" -eq "$(wc -l < AGENTS.md)" ]`
    - `grep -q 'video-ingestion.md' schema/fixtures/canonical-AGENTS.md` succeeds (fixture regenerated from the edited template)
    - `bash tests/phase-08/test_canonical_byte_equality.sh` exits 0 (wizard render byte-equal to the regenerated fixture — the setup-parity CI gate this commit must survive)
    - `bash bin/check-neutrality.sh` exits 0
    - `bash bin/gen-skills.sh --check` exits 0 (skills overlay not drifted)
  </acceptance_criteria>
  <done>AGENTS.md carries a routing-table row for video-ingestion.md; CLAUDE.md is byte-synced via bin/sync-claude.sh; schema/AGENTS.template.md mirrors the row; schema/fixtures/canonical-AGENTS.md is regenerated in the same commit and the byte-equality test passes; routing lint validates the pointer; neutrality and skills gates pass. Tasks 1-3 land as one schema(21) commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a markdown documentation/convention phase. No executable code is added (D-04 ships no new script), no user input is handled, no network service or auth surface is introduced. This plan edits template-public schema/reference + workflow docs and the router (AGENTS.md/CLAUDE.md/template/fixture). The single realistic trust boundary is the **template-public surface**: a real vault slug or the personal STT tool name leaking into published documentation.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Information Disclosure | video-ingestion.md / frontmatter.md / source-types.md / ingest.md / AGENTS.md / template / fixture (all template-public) | mitigate | Use abstract placeholders (<video-title>, <channel-name>, <YYYY-MM-DD-slug>, <stt-tool>, <asr-model>) for all examples; only the public tool name `yt-dlp` is permitted. The personal STT tool name + asr/diarize module names belong on `.neutrality-denylist.txt` and must NOT appear in any PUBLIC_PATHS surface. `bin/check-neutrality.sh` runs as a pre-commit backstop on every edited file — prevent the leak at write time, the gate is the backstop not the design. |
| T-21-02 | Tampering | CLAUDE.md byte-drift from AGENTS.md | mitigate | Never hand-edit CLAUDE.md; regenerate via bin/sync-claude.sh and verify with --check (cmp -s). The pre-commit hook + CI block a drifted commit. |
| T-21-03 | Tampering | routing row points at a non-existent file, or template edit lands without the regenerated fixture | mitigate | video-ingestion.md is authored in Task 1 within the same plan/commit; bin/lint.sh --category routing validates forward-path existence; the template edit + regenerated canonical-AGENTS.md land in the same commit so the setup-parity byte-equality CI gate stays green. |
</threat_model>

<verification>
- schema/reference/video-ingestion.md exists, >= 55 lines, passes neutrality, covers all 8 required sections, applies all four PDF divergences (D-04 no script/no worked instance; D-05 no --asset; D-06 link-rot stance; D-07 no lint mandate; D-10 live-url spot-verify).
- source-types.md video row finalized (sub-case-of-transcript + Convention Doc pointer, no "provisional"); the 7-value source_type enum and transcript Retro-fit row untouched.
- frontmatter.md documents the five video fields with neutral placeholders; url reused (not duplicated).
- ingest.md Pass-0 has the one-line video pointer.
- AGENTS.md routing row added with inclusion-audit baseline updated; cmp -s AGENTS.md CLAUDE.md exits 0; bash bin/lint.sh --ci --dry-run --category routing exits 0.
- schema/AGENTS.template.md carries the video-ingestion.md routing row; schema/fixtures/canonical-AGENTS.md regenerated in the same commit; bash tests/phase-08/test_canonical_byte_equality.sh exits 0. Tasks 1-3 land as ONE schema(21) commit.
- bash bin/lint.sh --ci --dry-run --category yaml exits 0; bash bin/check-neutrality.sh exits 0; bash bin/gen-skills.sh --check exits 0.
- No personal STT tool name or real vault slug in any template-public file (abstract placeholders only; only yt-dlp permitted).
</verification>

<success_criteria>
- VID-01 (doc half): the tool-generic video acquisition pipeline (yt-dlp-equivalent + timestamped STT) is documented in an authoritative schema/ file an agent can find via the routing table; no personal tool name leaks (worked instance stays in .planning/ notes only).
- VID-02 (convention): the five video frontmatter fields (url, channel, title, publish_date, duration) are documented; claims use the existing #t<start>-<end> locators; the source-types.md video row is finalized as a sub-case of transcript.
- VID-03: the convention documents the drift stance (immutable once published; link-rot is the only concern; no drift machinery) AND the tiered epistemic policy (clean → sourced + mandated hedging; degraded → tentative + N=3 live-url spot-verify); support_type stays direct.
</success_criteria>

<output>
After completion, create `.planning/phases/21-video-youtube-ingestion/21-01-SUMMARY.md`.
</output>
