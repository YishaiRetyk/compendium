---
phase: 21-video-youtube-ingestion
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - sources/
  - wiki-cloud/sources/
  - wiki-cloud/entities/
  - wiki-cloud/concepts/
  - wiki-cloud/overviews/
  - wiki-cloud/comparisons/
  - wiki-cloud/index.md
  - wiki-cloud/log.md
  - wiki-cloud/decisions/
  - wiki-local/maintenance/
autonomous: false
requirements: [VID-04]
user_setup: []

must_haves:
  truths:
    - "D-13: stt runs end-to-end on a YouTube URL LOCALLY on this workstation (~/.local/bin/stt, uv venv) — confirmed before the validation checkpoint; no SSH tunnel needed (diverges from Phase 20 remote OCR)"
    - "D-12: one clean, cloud-safe, multi-speaker (2+) YouTube video (~5-20 min) is supplied by the user at a checkpoint:human-action, acquired via the tool-generic pipeline, and ingested"
    - "D-12: the video is multi-speaker specifically to exercise the SPEAKER: labeling path (D-03) — the one piece of genuinely-new convention surface with zero existing coverage"
    - "D-03: the committed transcript carries >=2 DISTINCT speaker labels — if diarization yields a wrong count, duplicated/inconsistent labels, or a single label, the executor applies a fallback (rerun stt with adjusted --speakers, mend labels at ingest, or reject the video back to the Task 2 checkpoint) BEFORE ingesting; no single-label or mislabeled transcript is silently ingested"
    - "VID-02: the four video frontmatter fields (title, channel, publish_date, duration) are sourced from the yt-dlp metadata the acquisition pipeline already pulls (info-json / --print) and manually cross-checked against the YouTube watch page; publish_date is normalized to ISO YYYY-MM-DD"
    - "D-05: the transcript is committed as a SINGLE .md file in sources/ — no bundle dir, no co-located asset, no --asset flag"
    - "D-01/D-03: transcript lines use the [H:MM:SS] SPEAKER: text grammar (timestamp first, SPEAKER: prefix because multi-speaker); the audit TS_RE resolves them unchanged"
    - "D-11: claims use #t<start>-<end> locators with support_type: direct; page-level epistemic_status sourced for clean audio with claim-level hedging on the STT failure surface (D-09)"
    - "VID-04: the ingested wiki pages live in cloud-safe sources/ + wiki-cloud/ with timestamp-anchored provenance; bin/audit-claims.sh resolves the #t locators"
  artifacts:
    - path: "sources/"
      provides: "Single-file raw transcript (no bundle) with YouTube frontmatter + [H:MM:SS] SPEAKER: lines, >=2 distinct speaker labels"
      contains: "source_type: transcript"
    - path: "wiki-cloud/sources/"
      provides: "Source summary page with the five video fields (title/channel/publish_date/duration from yt-dlp metadata, ISO date) + #t-anchored direct claims + claim-level hedging"
      contains: "url"
    - path: "wiki-cloud/index.md"
      provides: "Index entries for the new + modified pages"
      contains: "ingest"
  key_links:
    - from: "wiki-cloud/sources/<source-id>.md"
      to: "sources/.../<slug>.md [H:MM:SS] lines"
      via: "#t<start>-<end> locators in claim provenance"
      pattern: "#t[0-9]"
    - from: "wiki-cloud/sources/<source-id>.md"
      to: "the live video"
      via: "url frontmatter field (courtesy pointer)"
      pattern: "url:"
---

<objective>
Exercise the entire video pipeline end-to-end on one real, user-supplied YouTube video (VID-04): confirm the local `stt` worked instance runs URL → speaker-labeled timestamped transcript, collect a clean multi-speaker cloud-safe video URL from the user at a `checkpoint:human-action`, acquire the transcript, ingest it as a SINGLE .md file (no bundle, no asset — D-05), author the source summary + topic pages with `#t`-anchored `direct` provenance and the five video frontmatter fields, verify with `bin/audit-claims.sh`, and (optionally) author the schema-update decision record.

Purpose: this is the only task in the phase that runs real STT inference and proves the convention works in anger, not just at the convention level. It validates SC-4 (one real YouTube video acquired → ingested → wiki pages with timestamp-anchored provenance) and deliberately uses a multi-speaker video to exercise the `SPEAKER:` labeling path (D-03) — the single piece of genuinely-new convention surface with no existing in-repo coverage (the two committed transcripts are both single-speaker).
Output: one new single-file transcript in sources/, a source summary + topic pages in wiki-cloud/, updated index.md + log.md, and optionally a decision record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-video-youtube-ingestion/21-CONTEXT.md
@.planning/phases/21-video-youtube-ingestion/21-PATTERNS.md

<interfaces>
The authoritative convention to FOLLOW (authored in Plan 01): schema/reference/video-ingestion.md — its Ingest Checklist is the script for this plan.
The tools: the local `stt` CLI (worked instance — `.planning`-notes-only; NEVER named in any file written to a template-public path) for acquisition; bin/ingest.sh (single-file path, NO --asset — D-05) for ingest bookkeeping; bin/audit-claims.sh (read-only #t resolution) for verification.
The ingest workflow: schema/workflows/ingest.md — the full classify/diff/extract/merge/lint/log/commit procedure. Video classifies to `source_type: transcript` (D-14), NOT a `video` source_type.

stt CLI (confirmed runnable at ~/.local/bin/stt — VERIFIED `stt --help`): `stt <YouTube-URL> --speakers <N> --output-dir <dir>` downloads the audio and emits a timestamped transcript next to the downloaded mp3. `--speakers N` improves diarization accuracy when the speaker count is known (multi-speaker → SPEAKER: labels); OMITTING `--speakers` lets the tool auto-detect the count. `--lang en` if needed. It runs in its own uv venv; yt-dlp/faster-whisper are not on the global PATH but stt wraps them — this is expected, not a blocker. The stt instance wraps yt-dlp for acquisition, so the yt-dlp video metadata (title/channel/upload_date/duration) is pulled during the download step — capture it via `yt-dlp --print` or the `.info.json` the downloader writes (these are public yt-dlp field names; yt-dlp is nameable, the personal stt wrapper name is not).

De-facto committed format exemplars (READ for the exact shape — sources/ uses REAL slugs, NOT template-public):
  - sources/2026/2026-05/2026-05-03-is-this-the-only-skill-left.md — YouTube frontmatter (title/author/publication: YouTube/url/date/source_type: transcript/privacy: cloud_safe) + `[H:MM:SS] text` lines (single-speaker, no labels). The validation video DIVERGES: multi-speaker → each line carries a `SPEAKER:` prefix AFTER the timestamp: `[0:00:00] ALICE: ...`.
  - wiki-cloud/sources/src-2026-05-03-is-this-the-only-skill-left.md — source-summary shape: base frontmatter + path/url/content_hash/ingested_at/source_type: transcript/compilation_status, page-level epistemic_status: sourced, inline claim-level hedging on proper nouns/numbers (the de-facto pattern D-09 formalizes).
  - wiki-cloud/concepts/jagged-frontier.md — live `#t`-anchored direct claims authored from a YouTube transcript (`...#t00:04:56-00:05:21|direct|...`).

DR analog (optional Task 3): wiki-cloud/decisions/dr-2026-06-11-pdf-ingestion.md — same trigger_type: schema-update, domains: [wiki-infrastructure], affected_pages: []. The PDF DR L81 forward-references this phase.

PRIVACY: sources/ is cloud-safe-only (D-12). The validation video MUST be cloud-safe (no private content — the transcript is committed to the cloud-safe sources/ tree). Verify before ingest. The ingested wiki pages use REAL slugs (sources/ + wiki-cloud/ are NOT template-public — real terms, real channel/title are fine here). The personal STT tool name MAY appear in the real sources/ + wiki-cloud/sources/ pages (extraction_tool field) — neutrality binds template-public surfaces only (D-07), and these pages are NOT template-public.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm stt runs end-to-end on a YouTube URL (local, .planning-notes-safe)</name>
  <files>(none — a runnable-confirmation probe; writes nothing to the repo)</files>
  <read_first>
    - .planning/phases/21-video-youtube-ingestion/21-CONTEXT.md (D-13 — stt runs LOCALLY on this workstation, ~/.local/bin/stt, uv-managed at ~/transcript; the planner-confirm-before-checkpoint requirement)
  </read_first>
  <action>
    Confirm the local `stt` worked instance is runnable end-to-end on a YouTube URL BEFORE pausing the user at the Task 2 checkpoint (D-13 requires this confirmation up front so the human is not asked for a URL the pipeline cannot process). This is a pre-flight, not the validation ingest. Steps:

    1. Confirm the binary resolves: `command -v stt` returns `~/.local/bin/stt` (a uv-tool symlink). If absent, STOP and report — the worked instance is not installed and Task 2 cannot run locally (D-13).
    2. Confirm the CLI contract: `stt --help` prints the URL-accepting usage (it accepts `INPUTS...` = audio files or YouTube URLs, with `--speakers`, `--lang`, `--output-dir`). VERIFIED at plan time: the contract is `stt <URL> --speakers <N> --output-dir <dir>`.
    3. OPTIONAL smoke (if a fast confirmation is wanted before the human picks the real video): run `stt <short-public-youtube-url> --speakers 2 --output-dir /tmp/stt-smoke` on any short cloud-safe public clip and confirm it produces a transcript file with `[H:MM:SS]`-prefixed lines (and SPEAKER: prefixes for multi-speaker). Delete /tmp/stt-smoke after. This is a throwaway confirmation — do NOT ingest the smoke transcript.
    4. NOTE for the executor: keep ALL stt-tool-name details in this task's SUMMARY / .planning notes only. This task writes NOTHING to a template-public path. The real ingest in Task 2 writes the tool name only into the real sources/ + wiki-cloud/sources/ pages (NOT template-public — D-07 permits it there).
  </action>
  <verify>
    <automated>command -v stt >/dev/null 2>&1 && stt --help >/dev/null 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `command -v stt` exits 0 and resolves to a real path (worked instance installed — D-13)
    - `stt --help` exits 0 and its output mentions accepting a URL / YouTube input (the CLI contract is confirmed before the human checkpoint)
    - Nothing was written to any template-public path by this task (no repo file modified — this is a pre-flight probe)
  </acceptance_criteria>
  <done>The local stt worked instance is confirmed runnable end-to-end on a YouTube URL (binary resolves, URL-accepting CLI contract verified, optionally smoke-tested on a throwaway clip). The Task 2 checkpoint can safely ask the user for a real URL. No template-public surface touched.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Obtain the validation YouTube URL from the user</name>
  <files>(none — collects a user-supplied URL)</files>
  <action>
    Pause and request the validation video URL from the user. Ask for ONE YouTube video meeting all D-12 criteria: clean audio (clear spoken word — NOT a music-bed/heavy-crosstalk clip, so it earns the `sourced` tier rather than the degraded `tentative` tier); MULTI-SPEAKER (2 or more speakers — deliberately chosen to exercise the SPEAKER: labeling path D-03, the one piece of new convention surface); cloud-safe (no private/local-only content — the transcript is committed to the cloud-safe sources/ tree); ~5-20 minutes (local faster-whisper practicality). When the user supplies the URL, confirm it is a reachable YouTube video and ask the user to confirm the content is cloud-safe and multi-speaker. Also ask the user for the EXACT speaker count if they know it (it feeds `--speakers <N>` in Task 3 and the diarization fallback); if they are unsure, note that Task 3 will auto-detect by omitting the flag. Do NOT proceed to Task 3 until a valid URL is confirmed (or the user says "skip" to defer VID-04).
  </action>
  <what-built>The full video pipeline (the tool-generic convention authored in Plan 01, the confirmed local stt worked instance, the single-file ingest path) is ready. This checkpoint collects the one real artifact needed to exercise it.</what-built>
  <how-to-verify>
    Ask the user to supply ONE YouTube video URL meeting all of these (D-12):
    1. Clean audio (clear spoken word — a degraded clip would need the `tentative` + spot-verify tier, which is out of scope for this validation).
    2. MULTI-SPEAKER (2+ speakers — required to exercise the SPEAKER: labeling / diarization path D-03; this is WHY a multi-speaker video was chosen).
    3. Cloud-safe (no private/local-only content — the transcript will be committed to the cloud-safe sources/ tree).
    4. ~5-20 minutes (local STT practicality on this workstation's GPU).
    Have the user paste the URL. Confirm: the URL is a reachable YouTube watch URL; ask the user to confirm the content is cloud-safe and has 2+ speakers before proceeding; ask for the exact speaker count if known (else Task 3 auto-detects).
  </how-to-verify>
  <resume-signal>Provide the YouTube URL of a clean, multi-speaker (2+), cloud-safe video (~5-20 min) — plus the exact speaker count if you know it — or say "skip" to defer VID-04 validation.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Acquire + ingest the video as a single-file transcript, author pages with #t provenance, verify, log</name>
  <files>sources/, wiki-cloud/sources/, wiki-cloud/entities/, wiki-cloud/concepts/, wiki-cloud/overviews/, wiki-cloud/index.md, wiki-cloud/log.md</files>
  <read_first>
    - schema/reference/video-ingestion.md (the Ingest Checklist authored in Plan 01 — this is the exact procedure to follow)
    - schema/workflows/ingest.md (the full classify/diff/extract/merge/lint/log/commit procedure; the video Pass-0 sub-bullet added in Plan 01)
    - schema/reference/provenance.md (L31 #t locator + L78 worked transcript example — for authoring claim provenance)
    - schema/reference/frontmatter.md (Source Summary Additional Fields + the video sub-case fields added in Plan 01)
    - schema/reference/page-types.md (section order + authoring conventions for the source summary and the topic pages created in the merge pass)
    - schema/reference/log-format.md (the authoritative index entry + log entry shapes)
    - schema/reference/privacy.md (tier routing — confirms the cloud placement of every page this ingest writes)
    - sources/2026/2026-05/2026-05-03-is-this-the-only-skill-left.md (the de-facto single-file transcript shape — frontmatter + [H:MM:SS] lines)
    - wiki-cloud/sources/src-2026-05-03-is-this-the-only-skill-left.md (the source-summary shape + the de-facto claim-level hedging on proper nouns/numbers)
    - wiki-cloud/concepts/jagged-frontier.md (live #t-anchored direct claims authored from a YouTube transcript)
    - wiki-cloud/index.md and wiki-cloud/log.md (the navigation artifacts to update; the log entry format)
    - bin/ingest.sh, bin/audit-claims.sh (the tools to invoke — note: NO --asset for video, D-05)
  </read_first>
  <action>
    Follow the video-ingestion.md Ingest Checklist with the user-supplied URL at <VIDEO_URL>:

    0. BASELINE (capture FIRST, before any content mutation — a sequential executor reaching the lint step has no clean "before" state once pages are authored):
       - `PRE_REF=$(git rev-parse HEAD)` (consumed by the post-commit audit).
       - `CROSSREF_BEFORE=$(bash bin/lint.sh --ci --dry-run --category crossref 2>&1 | grep -c ERROR || true)` (consumed by the lint delta check; do NOT hardcode a count — capture it live).

    1. ACQUIRE: run the local worked instance — `stt <VIDEO_URL> --speakers <N> --output-dir /tmp/vid-extract` (use the actual speaker count the user confirmed for <N>; if the user did NOT give an exact count, OMIT `--speakers` and let the tool auto-detect; add `--lang en` if the video is English and auto-detect is unreliable). This downloads the audio and emits a timestamped transcript. Confirm the transcript exists in /tmp/vid-extract and its lines start with `[H:MM:SS]` (the D-01 grammar). For this MULTI-SPEAKER video, confirm lines carry a `SPEAKER:` prefix after the timestamp (D-03 — `[0:00:00] SPEAKER_00: ...` or mapped names). Spot-check 2-3 segments against the video audio for gross STT sanity (the "tune on real artifact" step). KEEP all stt-tool-command details in the SUMMARY / .planning notes; do NOT echo the tool name into any template-public file.

    1a. DIARIZATION GATE + FALLBACK (D-03 — speaker labels are the one genuinely-new convention surface, so this is where the validation can fail): count the DISTINCT speaker labels in the emitted transcript before treating acquisition as done. Concretely, extract the labels that follow the timestamp on each line and count uniques, e.g.:
       `DISTINCT=$(grep -oE '\] +[A-Z][A-Z0-9_]*:' /tmp/vid-extract/<transcript>.md | sed -E 's/^\] +//; s/:$//' | sort -u | wc -l)`
       This is a MULTI-SPEAKER validation video (D-12), so the transcript MUST carry `DISTINCT >= 2`. If `DISTINCT < 2`, OR the labels are obviously wrong (a count that disagrees with the user-confirmed speaker count, duplicated/aliased labels for the same voice, or alternating/inconsistent labels mid-utterance), apply ONE of these three fallback branches BEFORE proceeding to PREPARE — do NOT silently ingest a single-label or mislabeled transcript:
       (a) RERUN `stt` with an adjusted `--speakers <N>`: set <N> to the user-confirmed exact count if you have it, OR OMIT the `--speakers` flag entirely to let the tool auto-detect — use this when the label count is wrong or labels are duplicated/inconsistent because the supplied <N> was off. Re-run into a fresh `/tmp/vid-extract` and re-check `DISTINCT`.
       (b) MEND AT INGEST: if diarization is mostly-right but messy (correct number of voices, ugly raw labels), manually map/mend the raw `SPEAKER_NN` labels to consistent labels in the transcript body — D-03 explicitly permits mapping raw diarization labels to meaningful names at ingest. Use this when (a) does not improve the count but the segmentation is sound. After mending, re-check `DISTINCT >= 2` on the mended file.
       (c) REJECT THE VIDEO: if diarization is unusable (the tool cannot produce >=2 distinct, consistent labels even after (a), and the segmentation is too broken to mend by hand under (b)), STOP and RETURN to the Task 2 `checkpoint:human-action` to request a DIFFERENT multi-speaker video. Do not ingest an unusable transcript to satisfy the gate.
       The gate (`DISTINCT >= 2` on the file that will actually be committed) is a hard precondition for PREPARE — the multi-speaker SPEAKER: path is the whole point of choosing this video (D-12).

    2. PREPARE the raw transcript .md: build the single source file body — YouTube frontmatter (title, channel/author, `publication: "YouTube"`, url: <VIDEO_URL>, date: <publish_date>, source_type: transcript, privacy: cloud_safe) + the `[H:MM:SS] SPEAKER: text` lines from the STT output. Multi-speaker: keep the SPEAKER: prefix; optionally map raw diarization labels (SPEAKER_00) to meaningful names at ingest (D-03, agent/human judgment).
       METADATA SOURCE (VID-02 — the four video fields come from the yt-dlp metadata the acquisition step already pulls; the stt instance wraps yt-dlp, so the download already fetched this — capture it via `yt-dlp --print` on <VIDEO_URL> or read the `.info.json` the downloader wrote next to the audio). Encode this EXACT field→source mapping (yt-dlp is a public tool, its field names are nameable; do NOT name the personal stt wrapper):
         - `title`        ← yt-dlp `title`        (info-json `title`        / `--print title`)
         - `channel`      ← yt-dlp `channel`      (info-json `channel` or `uploader` / `--print channel`)
         - `publish_date` ← yt-dlp `upload_date`  (info-json `upload_date`, format `YYYYMMDD`) — NORMALIZE to ISO `YYYY-MM-DD` per CLAUDE.md §3 date rule (e.g. `20260514` → `2026-05-14`)
         - `duration`     ← yt-dlp `duration`     (info-json `duration`, seconds) — format human-readable `H:MM:SS`, OR use yt-dlp `duration_string` directly
       MANUAL CROSS-CHECK: open <VIDEO_URL> in the browser (the YouTube watch page) and confirm the four captured values (title, channel, publish date, duration) MATCH the page before authoring frontmatter — yt-dlp metadata is authoritative but a quick human cross-check catches a stale/wrong info-json. If a field is missing from the yt-dlp metadata, read it off the watch page manually and record that it was manually sourced.
       Optional `## Description` section between frontmatter and transcript ONLY if the video description carries substance — reference links/chapters/named sources (D-08); skip for pure promo. The `[H:MM:SS]` timestamp MUST stay FIRST on every line so the audit TS_RE matches at line start (D-01). Pick one hour-digit form consistent with the convention doc.

    3. CLASSIFY (Pass 0): `source_type: transcript` (D-14 — video is a sub-case of transcript, NOT a `video` source_type). Choose a real kebab-case slug from the video's actual title (real slugs are fine — sources/ is NOT template-public).

    4. INGEST as a single file (NO --asset — D-05): run `bin/ingest.sh --slug <slug> /tmp/vid-extract/<transcript>.md`. This creates the single-file source path `sources/YYYY/YYYY-MM/YYYY-MM-DD-<slug>.md` (NO bundle dir, NO co-located asset — a video binary is too large and is not committed; the committed transcript IS the durable record, D-05/D-06). Confirm exactly one .md file was created and NO bundle directory was made.

    5. EXTRACT + author the source summary at `wiki-cloud/sources/src-YYYY-MM-DD-<slug>.md`:
       - Full base frontmatter (id, title, type: source, status: active, summary, created_at, updated_at, sources, epistemic_status, tags, domains, supersedes/superseded_by null, aliases, has_contradictions: false, knowledge_domain).
       - Source extra fields: path (to the single-file source.md path), url (<VIDEO_URL> — the courtesy pointer, D-06), content_hash, ingested_at, source_type: transcript, compilation_status.
       - The five video fields (VID-02): url (the courtesy pointer), title, channel (or author), publish_date (ISO YYYY-MM-DD from yt-dlp `upload_date`), duration (H:MM:SS from yt-dlp `duration`/`duration_string`) — all four metadata fields carry the yt-dlp-sourced, watch-page-cross-checked values from step 2. The convention-only extraction fields (extraction_tool / extraction_model / extraction_date) MAY name the real local STT tool + asr model here (NOT template-public — D-07 permits real names on real vault pages).
       - epistemic_status: sourced (clean-audio tier — D-09; no spot-verification mandate for clean input). MANDATE claim-level hedging on the STT failure surface: proper nouns / named entities, technical terms, numbers & statistics, and anything the speaker hedges get an `[epistemic:: tentative]` inline marker (the de-facto pattern the exemplar already shows — D-09).
       - Claims with `[prov:src-YYYY-MM-DD-<slug>#t<start>-<end>|direct]` provenance — support_type: direct (D-11), `#t<start>-<end>` timestamp locators pointing at the correct transcript segments.

    6. MERGE (Pass 3): create/update entity/concept/overview topic pages from the extracted claims, each claim carrying its `#t<start>-<end>|direct` provenance. Wikilinks on first mention (`[[id|Title]]`). Update the source summary compilation tracking fields (compilation_status, compiled_against_hash, compiled_targets) per ingest.md step 6a.

    7. LINT: run `bin/lint.sh --ci --dry-run --category yaml` — it MUST exit 0 (scope the gate to the touched category; the UNSCOPED `--ci` gate may be red from pre-existing crossref backlog out of this phase's scope). NOTE: there is NO new video lint rule (D-07 — extraction fields are convention-only, no mechanical trigger), so unlike Phase 20 nothing video-specific fires here; the yaml category just validates the new pages' frontmatter. Additionally assert the ingest introduced no NEW crossref errors (delta, not absolute): re-run the step-0 capture to get `CROSSREF_AFTER` and require `CROSSREF_AFTER <= CROSSREF_BEFORE`. New pages sharing domains/tags with existing pages can create NEW missing-cross-reference errors resolvable only by adding backlinks to EXISTING pages — that is a normal part of the merge pass, not scope creep; finish the merge. Reserve ONE non-dry-run `bin/lint.sh` invocation for the run whose lint-report.md/log.md updates are intentionally part of the ingest commit; all other verification runs use `--dry-run`.

    7b. CLOUD-SAFETY GUARD: run `bash bin/check-sources-cloud-safe.sh` post-ingest, pre-commit — the fail-closed PRIV-03 guard. SCOPE: it checks exactly two things — no raw source carries `privacy: local_only` frontmatter, and no `sources/local-only/` dir exists. Human confirmation (Task 2) stays the primary content-level check; this is the mechanical backstop.

    8. NAV: update `wiki-cloud/index.md` with the new + modified pages; append an `## [YYYY-MM-DD] ingest | <video title>` entry to `wiki-cloud/log.md` listing affected pages + rationale.

    9. COMMIT: one commit, `ingest(<slug>): <one-line summary>` (one logical operation even if it touches many files — CLAUDE.md §3).

    10. AUDIT (POST-COMMIT — the recency selector diffs committed changes only, so run AFTER step 9): assert over FINDINGS (each carries source_id + verdict):
        - `FINDINGS=$(bash bin/audit-claims.sh --since "$PRE_REF" --sample 100 --format json 2>/dev/null)` — the generous --sample + --since guarantees the new source's claims are selected.
        - Selection assertion (non-vacuous): `echo "$FINDINGS" | jq -e --arg sid "<source-id>" '[.[] | select(.source_id == $sid)] | length > 0'`.
        - Resolution assertion: `echo "$FINDINGS" | jq -e --arg sid "<source-id>" '[.[] | select(.source_id == $sid and .verdict == "insufficient-locator")] | length == 0'`. Together these read "the new source's #t claims were selected AND every #t locator resolved against the committed transcript" — non-vacuously, proving VID-04's timestamp-anchored provenance works.
        - Do NOT use a global `! grep -q insufficient-locator` over unscoped output (it can never pass while the Phase 19 #sec: residue exists — tracked in 19-REVIEW.md).
        - SIDE EFFECT: every audit mode rewrites `wiki-local/maintenance/audit-report.md` + advances `audit-state.md`. This is by design (D-15 checkpoint), not read-only. Both are wiki-local control-plane (never pushed) — leave them as uncommitted local state; do NOT fold them into the cloud-facing ingest commit. If a push is planned, the ordering constraint is commit-then-audit, not push-then-audit.

    PRIVACY: verify the video + transcript content are cloud-safe before committing to sources/ (D-12). If the content turns out local-only, STOP — do NOT ingest it anywhere (a wiki-local/ ingest would be privacy-safe but FAILS VID-04, which requires the artifact in cloud-safe sources/). Return to the Task 2 checkpoint and request a replacement cloud-safe video instead.
  </action>
  <verify>
    <automated>cd /home/yishai/Documents/compendium && bash bin/lint.sh --ci --dry-run --category yaml && bash bin/check-sources-cloud-safe.sh && FINDINGS=$(bash bin/audit-claims.sh --since "$PRE_REF" --sample 100 --format json 2>/dev/null) && echo "$FINDINGS" | jq -e --arg sid "<source-id>" '[.[] | select(.source_id == $sid)] | length > 0' && echo "$FINDINGS" | jq -e --arg sid "<source-id>" '[.[] | select(.source_id == $sid and .verdict == "insufficient-locator")] | length == 0'</automated>
    <!-- The audit assertions MUST run AFTER the step-9 ingest commit (the recency selector diffs committed changes only). -->
    <!-- SUBSTITUTION REQUIRED: this block is NOT runnable verbatim. Before running: set $PRE_REF (the step-0 capture, or re-derive as the parent of the ingest commit) and replace BOTH <source-id> occurrences with the real source id. -->
  </verify>
  <acceptance_criteria>
    - A new SINGLE-FILE transcript exists under sources/ (NOT a bundle dir — D-05): `find sources -name '*<slug>*.md' -not -path '*/source.md'` returns the single-file path, AND no bundle directory `sources/**/YYYY-MM-DD-<slug>/` was created
    - The raw transcript carries [H:MM:SS] lines with SPEAKER: prefixes (multi-speaker, D-03): the source file has lines matching `^\[?[0-9]+:[0-9]{2}` AND at least one line carries a `SPEAKER`-style label after the timestamp
    - DIARIZATION GATE (D-03/D-12): the committed transcript carries >=2 DISTINCT speaker labels — `grep -oE '\] +[A-Z][A-Z0-9_]*:' <source-file> | sed -E 's/^\] +//; s/:$//' | sort -u | wc -l` returns a count `>= 2`. If acquisition first yielded `< 2` (or wrong/duplicated/inconsistent labels), the executor MUST have applied fallback branch (a) rerun-with-adjusted/omitted-`--speakers`, (b) mend-raw-labels-at-ingest, or (c) reject-and-return-to-Task-2 BEFORE this file was committed — a single-label or mislabeled transcript is never silently ingested
    - The source file frontmatter has `source_type: transcript` (NOT a video source_type): `grep -q 'source_type: transcript'` against the new source file AND `! grep -q 'source_type: video'`
    - A source summary page exists in wiki-cloud/sources/ with the five video fields: grep the new summary for `url`, `channel` (or author), `title`, `publish_date`, `duration`
    - METADATA PROVENANCE (VID-02): the four video fields are populated from yt-dlp metadata and cross-checked against the YouTube watch page — `title`, `channel`, `publish_date`, `duration` are all NON-EMPTY in the source summary frontmatter, AND `publish_date` is ISO `YYYY-MM-DD` (`grep -qE 'publish_date: *[0-9]{4}-[0-9]{2}-[0-9]{2}'` succeeds — i.e. the yt-dlp `upload_date` YYYYMMDD was normalized), AND `duration` is non-empty (e.g. `H:MM:SS` or yt-dlp `duration_string`)
    - At least one claim uses a #t locator with direct support: `grep -qE '#t[0-9]'` and a `|direct` marker against the new wiki pages
    - Claim-level hedging present on the STT failure surface (D-09): at least one `[epistemic:: tentative]` inline marker on a proper-noun / number claim in the new pages
    - `bash bin/lint.sh --ci --dry-run --category yaml` exits 0 (the new pages' frontmatter validates; NO video-specific lint rule fires — D-07)
    - The crossref error count did not increase vs the pre-ingest baseline captured at step 0 (delta check — no NEW crossref errors introduced)
    - `bash bin/check-sources-cloud-safe.sh` exits 0 (mechanical D-12 backstop)
    - The source-scoped FINDINGS assertions (run POST-COMMIT) pass BOTH ways: the new source's #t claims WERE selected (length > 0 — no vacuous pass) AND none carry `verdict: insufficient-locator` (jq filter on source_id, NOT a global grep) — proving every #t locator resolves against the committed transcript (VID-04)
    - `wiki-cloud/index.md` and `wiki-cloud/log.md` reference the new source
    - `wiki-local/maintenance/audit-report.md` + `audit-state.md` mutations from the audit run are expected control-plane writes, left as uncommitted local state — not folded into the ingest commit
  </acceptance_criteria>
  <done>One real multi-speaker YouTube video is acquired via the local stt worked instance, committed as a SINGLE .md transcript (no bundle, no asset — D-05) carrying >=2 distinct speaker labels (a diarization-fallback branch applied if acquisition first under-labeled), source summary + topic pages authored with #t|direct provenance, the five video fields (title/channel/publish_date/duration sourced from yt-dlp metadata and cross-checked against the YouTube page, publish_date normalized to ISO), and claim-level hedging on the STT failure surface, committed as one ingest, and the POST-COMMIT source-scoped audit findings show the source selected with every #t locator resolving. Scoped lint (yaml) green with no new crossref errors; cloud-safe guard green; index + log updated.</done>
</task>

<task type="auto">
  <name>Task 4: Author the schema-update decision record (optional — Claude's Discretion per CONTEXT D)</name>
  <files>wiki-cloud/decisions/dr-2026-06-14-video-ingestion.md, wiki-cloud/index.md, wiki-cloud/log.md</files>
  <read_first>
    - wiki-cloud/decisions/dr-2026-06-11-pdf-ingestion.md (the direct Phase-20 parallel — frontmatter + body section order to mirror; its L81 forward-references this phase)
    - wiki-cloud/decisions/dr-2026-06-10-source-type-contract.md (the other schema-update DR analog)
    - schema/reference/page-types.md (decision page section order + required fields, if needed)
    - .planning/phases/21-video-youtube-ingestion/21-CONTEXT.md (the decisions D-01..D-14 to summarize; the `<deferred>` block L111 — declined options for Alternatives Considered)
  </read_first>
  <action>
    Author this DR ONLY if the schema edits warrant it (Phase 18/19/20 precedent; Claude's Discretion per CONTEXT.md L50 — the Phase-20 schema work DID warrant one, and this phase's four deliberate divergences are a non-trivial design statement, so authoring it is the expected choice). Create `wiki-cloud/decisions/dr-2026-06-14-video-ingestion.md` mirroring the Phase-20 pdf-ingestion DR.

    Frontmatter (mirror dr-2026-06-11 L1-24): id dr-2026-06-14-video-ingestion; title in the "X as Y + Z" shape, e.g. "Video as Sub-Case of Transcript + Tool-Generic Acquisition (No Repo Script)"; type decision; status active; summary one sentence; created_at/updated_at 2026-06-14; sources []; epistemic_status sourced; tags [meta, schema]; domains [wiki-infrastructure]; supersedes/superseded_by null; aliases [dr-2026-06-14-video-ingestion]; has_contradictions false; knowledge_domain software; trigger_type schema-update; affected_pages [] (infrastructure DR).

    Body section order to mirror (dr-2026-06-11): `## TL;DR` → `## Decision` (numbered coupled deliverables) → `## Why` → `## Alternatives Considered` → `## Consequences` → `## Affected Pages` (`None.` — infrastructure DR) → `## Sources`. Capture the load-bearing decisions:
      (1) video is a sub-case of `transcript`, NOT a new source_type — only Acquisition changes (always) + Epistemic Default (degraded audio only), per the 5-dimension walk-through (D-14);
      (2) lazy-loaded authoritative video-ingestion.md + lean registry-row pointer in source-types.md (carried from Phase 20 D-01/D-02);
      (3) tool-generic acquisition contract, NO repo script (D-04) — diverges from Phase 20's bin/pdf-extract.sh because the worked STT instance is not nameable on a template-public surface; the convention names only a generic yt-dlp-or-equivalent + timestamped STT contract;
      (4) transcript-only single .md file, no bundle/no --asset (D-05) — a video binary is too large and is not committed;
      (5) link-rot drift stance (D-06) — the committed transcript + frontmatter is the durable archive; url is a courtesy pointer that may rot; no drift machinery (VID-03);
      (6) convention-only extraction fields, NOT lint-enforced (D-07) — diverges from Phase 20's lint-enforced PDF fields because no mechanical signal distinguishes an STT transcript from one built off official captions;
      (7) tiered epistemic policy with claim-level hedging mandate + live-url spot-verify for degraded audio (D-09/D-10); support_type stays direct (D-11).
    `## Alternatives Considered` maps cleanly to the declined options preserved in CONTEXT.md `<deferred>` L111: a yt-dlp .info.json/thumbnail bundle for richer post-rot metadata (declined for transcript-only D-05); an archive_url/Wayback field + url_dead: rot-annotation convention (declined for the plain transcript-is-the-archive stance D-06); lint-enforcing the extraction fields (declined as unmechanizable D-07). Cite the Phase-20 pdf-ingestion DR (its L81 forward-references this phase, inheriting the same format-orthogonal sub-case pattern).

    NAV: add the DR to `wiki-cloud/index.md` (decisions listing) and append a log entry to `wiki-cloud/log.md` per log-format — a new decision page is navigable content.

    NEUTRALITY: `wiki-cloud/decisions/` IS a template-public surface (it is in bin/check-neutrality.sh PUBLIC_PATHS). Keep the personal STT tool name OUT — describe it generically ("a timestamped STT engine"). Public tool names (yt-dlp) permitted.

    Lint: trigger_type must be `schema-update` (valid); affected_pages must be a YAML list. Run `bin/lint.sh --ci --dry-run --category yaml` after.

    Commit: `reflect(video-ingestion): record video sub-case schema decision` — a standalone reflect commit, NOT rolled under the ingest commit (keeps the ingest commit focused and independently auditable).
  </action>
  <verify>
    <automated>cd /home/yishai/Documents/compendium && test -f wiki-cloud/decisions/dr-2026-06-14-video-ingestion.md && grep -q 'trigger_type: schema-update' wiki-cloud/decisions/dr-2026-06-14-video-ingestion.md && grep -q 'type: decision' wiki-cloud/decisions/dr-2026-06-14-video-ingestion.md && bash bin/lint.sh --ci --dry-run --category yaml >/dev/null 2>&1 && bash bin/check-neutrality.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -f wiki-cloud/decisions/dr-2026-06-14-video-ingestion.md` succeeds
    - `grep -q 'trigger_type: schema-update' wiki-cloud/decisions/dr-2026-06-14-video-ingestion.md` succeeds
    - `grep -q 'type: decision' wiki-cloud/decisions/dr-2026-06-14-video-ingestion.md` succeeds
    - The DR body has the mirrored section order: `grep -q '## TL;DR'` and `grep -q '## Decision'` and `grep -q '## Alternatives Considered'` and `grep -q '## Consequences'` against the file
    - `grep -q 'dr-2026-06-14-video-ingestion' wiki-cloud/index.md` succeeds (DR registered in the index)
    - `bash bin/check-neutrality.sh` exits 0 (the personal STT tool name is NOT in this template-public DR — described generically)
    - `bash bin/lint.sh --ci --dry-run --category yaml` exits 0 (decision-page frontmatter validates)
  </acceptance_criteria>
  <done>A schema-update decision record captures video-as-sub-case-of-transcript + the four deliberate divergences (no repo script, single-file no-asset, link-rot stance, convention-only extraction fields), with Alternatives Considered mapping the declined options, mirroring the Phase-20 pdf DR. Registered in index.md + log.md. Passes neutrality (no personal tool name) and lint. Committed as a standalone reflect commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan ingests a real YouTube video into the cloud-safe sources/ + wiki-cloud/ tree. No new executable code is added (acquisition uses the external local stt worked instance, not a repo script — D-04). Two realistic trust boundaries: (1) the **privacy tier** — a non-cloud-safe video's transcript landing in cloud-safe sources/ (a privacy-tier violation); (2) the **template-public surface** — the personal STT tool name leaking into the decision record (which IS in PUBLIC_PATHS), even though it is permitted on the real sources/ + wiki-cloud/sources/ pages.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Information Disclosure | a non-cloud-safe video transcript committed to cloud-safe sources/ | mitigate | Human confirms cloud-safety at the Task 2 checkpoint (primary content-level check); `bin/check-sources-cloud-safe.sh` runs post-ingest pre-commit as the fail-closed PRIV-03 mechanical backstop (no `privacy: local_only` source, no `sources/local-only/` dir). If content turns out local-only, STOP — do not ingest anywhere (a wiki-local ingest fails VID-04); request a replacement. |
| T-21-05 | Information Disclosure | personal STT tool name leaking into the template-public decision record (wiki-cloud/decisions/ is in PUBLIC_PATHS) | mitigate | The DR describes the tool generically ("a timestamped STT engine"); only yt-dlp is named. The personal tool name belongs on .neutrality-denylist.txt. `bin/check-neutrality.sh` runs as the pre-commit backstop on the DR. The real tool name is permitted ONLY on the real sources/ + wiki-cloud/sources/ pages (NOT template-public — D-07). |
| T-21-06 | Tampering | #t locators that do not resolve against the committed transcript (broken provenance) | mitigate | The POST-COMMIT source-scoped audit (bin/audit-claims.sh --since PRE_REF --format json) asserts every #t locator on the new source resolves (no verdict: insufficient-locator); the [H:MM:SS] grammar (D-01) is exactly what the existing TS_RE resolves, so no resolver change is needed. |
| T-21-07 | Spoofing | wrong/duplicated/missing diarization labels producing a single-label or mislabeled transcript that defeats the SPEAKER: validation (D-03/D-12) | mitigate | The Task 3 step-1a diarization gate counts distinct labels and requires `>= 2` on the file that will be committed; if acquisition under-labels, one of three explicit fallbacks (rerun with adjusted/omitted `--speakers`, mend raw labels at ingest, or reject the video back to the Task 2 checkpoint) MUST run before PREPARE — no single-label/mislabeled transcript is silently ingested. |
| T-21-08 | Tampering | inaccurate VID-02 metadata (title/channel/publish_date/duration) authored from guesswork instead of the canonical source | mitigate | The four fields are sourced from the yt-dlp metadata the acquisition step already pulls (info-json / `--print`) with a fixed field→source mapping, `upload_date` normalized to ISO `YYYY-MM-DD`, and a manual cross-check against the YouTube watch page before authoring; acceptance criteria require all four non-empty and `publish_date` ISO-formatted. |
</threat_model>

<verification>
- The local stt worked instance is confirmed runnable on a YouTube URL before the human checkpoint (Task 1 — D-13).
- One real multi-speaker cloud-safe video (~5-20 min) is collected at the Task 2 checkpoint:human-action (D-12).
- The transcript is committed as a SINGLE .md file in sources/ (no bundle dir, no --asset — D-05) with [H:MM:SS] SPEAKER: lines (D-01/D-03), carrying >=2 distinct speaker labels (the step-1a diarization gate; a fallback branch applied if acquisition first under-labeled).
- The four VID-02 metadata fields (title/channel/publish_date/duration) are sourced from the yt-dlp metadata the acquisition step already pulls, cross-checked against the YouTube watch page, with publish_date normalized to ISO YYYY-MM-DD (VID-02).
- source_type: transcript (NOT video — D-14); claims use #t<start>-<end> + support_type: direct (D-11); epistemic_status sourced for clean audio with claim-level hedging on the STT failure surface (D-09).
- bash bin/lint.sh --ci --dry-run --category yaml exits 0 (no new video lint rule — D-07); no NEW crossref errors vs the step-0 baseline; bash bin/check-sources-cloud-safe.sh exits 0.
- POST-COMMIT source-scoped audit findings show the source selected with every #t locator resolving (VID-04 timestamp-anchored provenance proven non-vacuously).
- index.md + log.md updated; wiki-local audit control-plane left as uncommitted local state.
- (Task 4) A schema-update DR is authored, registered in index.md/log.md, passes neutrality (no personal tool name in the template-public DR) and lint.
</verification>

<success_criteria>
- VID-04: one real YouTube video has been acquired via the (tool-generic, locally-run stt worked instance) pipeline, ingested as a single-file transcript sub-case of transcript, and its wiki pages live in cloud-safe sources/ + wiki-cloud/ with timestamp-anchored (#t) provenance that the audit resolves.
- The SPEAKER: multi-speaker labeling path (D-03) — the one piece of genuinely-new convention surface — is exercised end-to-end with >=2 distinct speaker labels on the committed transcript (a diarization-fallback branch applied if acquisition first under-labeled).
- The four VID-02 metadata fields are populated from yt-dlp metadata and cross-checked against the YouTube page, with publish_date in ISO YYYY-MM-DD.
- The drift stance is honored in practice: the committed transcript is the durable record; the url is a courtesy pointer; no drift machinery is invoked.
</success_criteria>

<output>
After completion, create `.planning/phases/21-video-youtube-ingestion/21-02-SUMMARY.md`.
</output>
