---
phase: 05-lint-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AGENTS.md
  - wiki/concepts/cognitive-biases.md
  - wiki/concepts/loss-aversion.md
  - wiki/concepts/prospect-theory.md
  - wiki/entities/daniel-kahneman.md
  - wiki/comparisons/system-1-vs-system-2.md
  - wiki/overviews/decision-making.md
  - wiki/overviews/personal-decision-patterns.md
  - wiki/sources/src-2026-04-09-thinking-fast-and-slow-part1.md
  - wiki/sources/src-2026-04-10-kahneman-prospect-theory.md
  - wiki/sources/src-2026-04-10-personal-decision-journal.md
autonomous: true
requirements:
  - STALE-03
  - STALE-04
  - CNTR-03
  - LINT-07

must_haves:
  truths:
    - "AGENTS.md section 6 contains a domain-based decay rate table with at least 4 domains and numeric thresholds"
    - "AGENTS.md section 5 documents has_contradictions and knowledge_domain as frontmatter fields"
    - "AGENTS.md section 11.3 contains severity tiers (error/warning/info) with explicit definitions"
    - "AGENTS.md section 11.3 documents auto-fix boundary with mechanical-only rule"
    - "AGENTS.md section 6 documents contradiction inline syntax"
    - "All existing wiki pages have knowledge_domain field in frontmatter"
  artifacts:
    - path: "AGENTS.md"
      provides: "Schema extensions for decay rates, contradiction syntax, severity tiers, auto-fix boundary, new frontmatter fields"
      contains: "knowledge_domain"
  key_links:
    - from: "AGENTS.md section 6"
      to: "AGENTS.md section 5"
      via: "knowledge_domain field referenced in decay rate table"
      pattern: "knowledge_domain"
    - from: "AGENTS.md section 11.3"
      to: "AGENTS.md section 6"
      via: "staleness checks reference decay rate table"
      pattern: "decay_rate\\|decay rate"
---

<objective>
Extend AGENTS.md with all Phase 5 schema definitions: domain-based decay rate table, contradiction inline syntax, severity tiers, auto-fix boundary, new frontmatter fields (has_contradictions, knowledge_domain). Backfill knowledge_domain on all existing wiki pages. This is the foundation that all lint rules will enforce.

Purpose: The lint script mechanically enforces schema rules. The schema must be complete and authoritative before implementation begins.
Output: Extended AGENTS.md sections 5, 6, and 11.3. All existing wiki pages with knowledge_domain frontmatter field.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-lint-quality/05-CONTEXT.md
@.planning/phases/05-lint-quality/05-RESEARCH.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend AGENTS.md sections 5, 6, and 11.3 with Phase 5 schema definitions</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md (full file -- sections 5, 6, and 11.3 are the modification targets)
    - .planning/phases/05-lint-quality/05-CONTEXT.md (locked decisions D-01 through D-23)
    - .planning/phases/05-lint-quality/05-RESEARCH.md (code examples for decay rates, contradiction syntax)
  </read_first>
  <action>
**Section 5 (Frontmatter Schema) -- add two new fields to the base fields block and Field Descriptions table:**

After the `aliases` field in the base fields YAML block, add:
```yaml
has_contradictions: false       # true when page contains contradiction-flagged claims
knowledge_domain: ""            # Primary knowledge domain for decay rate calculation
```

Add rows to the Field Descriptions table:
| `has_contradictions` | boolean | `true` when any claim on the page has a `[contradiction:...]` marker. Independent of `epistemic_status` -- a `sourced` page can have contradictions. Set by lint workflow. |
| `knowledge_domain` | string | Primary knowledge domain for staleness decay rate calculation. Maps to the decay rate table in Section 6. One of: `software`, `science`, `biography`, `personal-goals`, or a custom domain. Empty string if not yet classified. |

Update the Frontmatter Validation Checklist (add item 13):
13. `has_contradictions` is a boolean (`true` or `false`)
14. `knowledge_domain` is a non-empty string for pages with provenance-backed claims

**Section 6 (Provenance, Epistemics, and Staleness) -- add three new subsections after "Mixed Inline Grammar":**

Add subsection "### Domain-Based Decay Rate Table" with this content:

Claims inherit temporal relevance from their source publication dates. Different knowledge domains decay at different rates. The lint workflow uses this table to flag stale claims mechanically.

| Domain | Base Decay Period | Rationale |
|--------|-------------------|-----------|
| `software` | 180 days (6 months) | Libraries, APIs, and tooling change rapidly |
| `science` | 730 days (2 years) | Replication and meta-analysis cycles |
| `biography` | 1825 days (5 years) | Biographical facts change slowly |
| `personal-goals` | 90 days (3 months) | Goals evolve with life circumstances |
| (default) | 365 days (1 year) | Fallback for unclassified domains |

**Epistemic status modifiers** (per D-08): Tentative and inferred claims decay faster than their domain default. Multiply the base decay period by the modifier:

| Epistemic Status | Modifier | Effect |
|------------------|----------|--------|
| `sourced` | 1.0 | Base rate |
| `mixed` | 0.85 | 15% faster decay |
| `inferred` | 0.75 | 33% faster decay |
| `tentative` | 0.5 | Twice as fast decay |

**Hash override** (per D-09): If a source page's `content_hash` differs from `compiled_against_hash`, ALL claims linked to that source via `[prov:]` markers are immediately stale regardless of decay window.

**Date fallback chain** for staleness calculation: When `checked_at` is missing from a provenance marker, use (in order): (1) the source page's `ingested_at` date, (2) the wiki page's `updated_at` date.

Add subsection "### Contradiction Inline Syntax" with this content:

When two different sources assert conflicting claims about the same subject/attribute (per D-01), the contradiction is flagged inline on the affected claim(s):

```
[contradiction:source_a_id#locator vs source_b_id#locator]
```

Example:
```markdown
Loss aversion coefficient is approximately 2.0 [prov:src-2026-04-10-kahneman-prospect-theory#sec:core-findings|direct|2026-04-10] [contradiction:src-2026-04-10-kahneman-prospect-theory#sec:core-findings vs src-2026-05-01-newer-study#sec:results]
```

Rules:
- Contradiction markers sit alongside provenance markers on the affected claim
- Both source references in the contradiction marker MUST resolve to known sources in `wiki/sources/`
- The lint does NOT decide which source is correct -- it surfaces the disagreement
- Pages with any contradiction marker must have `has_contradictions: true` in frontmatter
- Contradictions are severity: **warning** (source disagreement is expected in scholarship)
- Semantic conflicts without provenance grounding are out of scope for v1 (per D-02)

Add subsection "### Staleness Auto-Fix Rules" with this content:

The lint workflow applies mechanical staleness fixes (per D-12):

**Claim-level auto-fix:** When a claim's provenance date exceeds its domain decay threshold (adjusted by epistemic modifier), the lint adds or updates `[epistemic:: stale]` on that claim. This is deterministic and reversible.

**Page-level status:** The lint only auto-updates page-level `epistemic_status` to `stale` when the rollup clearly warrants it (per D-13): all material claims are stale, OR the TL;DR/Key Facts section contains materially stale claims. Default: do NOT auto-change page-level status.

**Logging:** All auto-fix staleness changes are logged in `wiki/maintenance/lint-report.md` and `wiki/log.md` (per D-14).

**Section 11.3 (Lint Workflow) -- replace the existing 12-step skeleton with fully operational version:**

Keep the existing trigger/inputs/outputs/commit block. Replace the Steps section with:

**Severity Tiers** (per D-17):
| Severity | Meaning | Examples |
|----------|---------|----------|
| `error` | Must fix -- broken references, invalid structure | Broken provenance refs, missing source pages, YAML parse failures, invalid frontmatter enum values |
| `warning` | Should fix -- quality degradation | Stale claims, orphan pages, contradictions, missing cross-references |
| `info` | Nice to know -- improvement opportunities | Knowledge gaps, sparse coverage, suggested questions |

**Auto-Fix Boundary** (per D-18, D-19):
Auto-fix (mechanical, deterministic, reversible): adding missing cross-references where link target exists, updating stale claim markers per decay table, fixing broken provenance refs where the correct source is unambiguous.
Report-only (no auto-fix): contradictions, knowledge gaps, orphan pages, page restructuring, any fix requiring judgment.

Then the 12 steps (enhanced from skeleton):
1. Read `wiki/index.md` for full page inventory. Build resolution map: filename -> id, title, aliases.
2. **Orphan detection:** Find pages with no inbound wikilinks from other wiki pages (using resolution map for alias-aware matching). Severity: warning. Report-only (orphan may be intentional).
3. **Missing cross-references:** Identify pages sharing 2+ domains AND 2+ tags that lack mutual wikilinks, AND whose source pages' `compiled_targets` overlap. Severity: warning. Auto-fix: add wikilink if target page exists.
4. **Stale claims:** Compute staleness using domain decay rate table (Section 6), epistemic modifier, and hash override. Date fallback chain: `checked_at` -> `ingested_at` -> `updated_at`. Severity: warning. Auto-fix: add/update `[epistemic:: stale]` markers on affected claims. Log all changes.
5. **Contradiction detection:** Flag wiki page sections where claims have `[prov:]` markers from 2+ different source_ids. Mark as potential contradictions for agent review. Severity: warning. Report-only.
6. **Knowledge gaps (red links):** Collect unresolved wikilinks. Flag when: appears on 2+ distinct pages, OR appears in TL;DR/Key Facts section of any page (per D-20). Severity: info. Report-only.
7. **Source coverage gaps:** Compare domain source counts. Flag domains with materially fewer sources than median. Only run when wiki has 5+ domains with 3+ having 2+ sources (maturity guardrail per D-22). Severity: info. Report-only.
8. **Provenance validation:** Verify all `[prov:]` references resolve to known source IDs. Verify locator syntax matches defined patterns. Severity: error. Auto-fix: correct source_id if unambiguous (e.g., typo with Levenshtein distance 1 from a known source).
9. **YAML frontmatter validation:** Parse all page frontmatter, check required fields, validate enum values against Section 5 schema. Severity: error. Report-only (structural issues need human judgment).
10. Compile findings into `wiki/maintenance/lint-report.md` organized by severity then category. Include total counts and per-category breakdowns. Suggest investigative questions for gaps (per D-23, LINT-06). Example: "Health domain has 1 source vs 8 in psychology. Consider: What are your current health goals?"
11. Append entry to `wiki/log.md`: `## [YYYY-MM-DD] lint | <scope>` with summary of findings counts and auto-fixes applied.
12. Commit: `lint(<scope>): <one-line summary of findings and fixes>`

Keep the existing abort conditions.
  </action>
  <verify>
    <automated>grep -c 'knowledge_domain' AGENTS.md && grep -c 'has_contradictions' AGENTS.md && grep -c 'decay_rate\|Base Decay Period' AGENTS.md && grep -c '\[contradiction:' AGENTS.md && grep -c 'error.*Must fix\|error.*must fix' AGENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md section 5 contains `has_contradictions: false` in the base fields YAML block
    - AGENTS.md section 5 contains `knowledge_domain: ""` in the base fields YAML block
    - AGENTS.md section 5 Field Descriptions table has rows for `has_contradictions` and `knowledge_domain`
    - AGENTS.md section 5 validation checklist has items 13 and 14 for the new fields
    - AGENTS.md section 6 contains "### Domain-Based Decay Rate Table" heading
    - AGENTS.md section 6 decay table has rows for `software` (180), `science` (730), `biography` (1825), `personal-goals` (90), default (365)
    - AGENTS.md section 6 contains epistemic modifier table with `sourced` (1.0), `mixed` (0.85), `inferred` (0.75), `tentative` (0.5)
    - AGENTS.md section 6 contains "### Contradiction Inline Syntax" heading with `[contradiction:` pattern
    - AGENTS.md section 6 contains "### Staleness Auto-Fix Rules" heading
    - AGENTS.md section 11.3 contains severity tier table with error/warning/info rows
    - AGENTS.md section 11.3 contains "Auto-Fix Boundary" subsection
    - AGENTS.md section 11.3 step 4 references "domain decay rate table (Section 6)"
    - AGENTS.md section 11.3 step 6 references "2+ distinct pages" red link rule
    - AGENTS.md section 11.3 step 7 references "5+ domains" maturity guardrail
  </acceptance_criteria>
  <done>AGENTS.md sections 5, 6, and 11.3 fully extended with all Phase 5 schema definitions per decisions D-01 through D-23. Decay rate table, contradiction syntax, severity tiers, auto-fix boundary, and new frontmatter fields are documented and authoritative.</done>
</task>

<task type="auto">
  <name>Task 2: Backfill knowledge_domain on all existing wiki pages</name>
  <files>
    wiki/concepts/cognitive-biases.md
    wiki/concepts/loss-aversion.md
    wiki/concepts/prospect-theory.md
    wiki/entities/daniel-kahneman.md
    wiki/comparisons/system-1-vs-system-2.md
    wiki/overviews/decision-making.md
    wiki/overviews/personal-decision-patterns.md
    wiki/sources/src-2026-04-09-thinking-fast-and-slow-part1.md
    wiki/sources/src-2026-04-10-kahneman-prospect-theory.md
    wiki/sources/src-2026-04-10-personal-decision-journal.md
  </files>
  <read_first>
    - wiki/concepts/cognitive-biases.md (frontmatter -- check existing domains field)
    - wiki/concepts/loss-aversion.md (frontmatter)
    - wiki/concepts/prospect-theory.md (frontmatter)
    - wiki/entities/daniel-kahneman.md (frontmatter)
    - wiki/comparisons/system-1-vs-system-2.md (frontmatter)
    - wiki/overviews/decision-making.md (frontmatter)
    - wiki/overviews/personal-decision-patterns.md (frontmatter)
    - wiki/sources/src-2026-04-09-thinking-fast-and-slow-part1.md (frontmatter)
    - wiki/sources/src-2026-04-10-kahneman-prospect-theory.md (frontmatter)
    - wiki/sources/src-2026-04-10-personal-decision-journal.md (frontmatter)
  </read_first>
  <action>
Add `knowledge_domain` and `has_contradictions` frontmatter fields to every existing wiki page. Place them after the `aliases` field (last of the existing base fields).

Domain assignments based on page content:
- `wiki/concepts/cognitive-biases.md` -> `knowledge_domain: science` (psychology research), `has_contradictions: false`
- `wiki/concepts/loss-aversion.md` -> `knowledge_domain: science`, `has_contradictions: false`
- `wiki/concepts/prospect-theory.md` -> `knowledge_domain: science`, `has_contradictions: false`
- `wiki/entities/daniel-kahneman.md` -> `knowledge_domain: biography`, `has_contradictions: false`
- `wiki/comparisons/system-1-vs-system-2.md` -> `knowledge_domain: science`, `has_contradictions: false`
- `wiki/overviews/decision-making.md` -> `knowledge_domain: science`, `has_contradictions: false`
- `wiki/overviews/personal-decision-patterns.md` -> `knowledge_domain: personal-goals`, `has_contradictions: false`
- `wiki/sources/src-2026-04-09-thinking-fast-and-slow-part1.md` -> `knowledge_domain: science`, `has_contradictions: false`
- `wiki/sources/src-2026-04-10-kahneman-prospect-theory.md` -> `knowledge_domain: science`, `has_contradictions: false`
- `wiki/sources/src-2026-04-10-personal-decision-journal.md` -> `knowledge_domain: personal-goals`, `has_contradictions: false`

For each file, add the two lines in the YAML frontmatter block (between `---` markers), after `aliases:` and its list entries. Do not modify any other content.
  </action>
  <verify>
    <automated>for f in wiki/concepts/*.md wiki/entities/*.md wiki/comparisons/*.md wiki/overviews/*.md wiki/sources/*.md; do grep -q 'knowledge_domain:' "$f" && grep -q 'has_contradictions:' "$f" || echo "MISSING in $f"; done; echo "CHECK COMPLETE"</automated>
  </verify>
  <acceptance_criteria>
    - Every .md file in wiki/concepts/, wiki/entities/, wiki/comparisons/, wiki/overviews/, wiki/sources/ contains `knowledge_domain:` in frontmatter
    - Every .md file in those directories contains `has_contradictions: false` in frontmatter
    - daniel-kahneman.md contains `knowledge_domain: biography`
    - personal-decision-patterns.md contains `knowledge_domain: personal-goals`
    - src-2026-04-10-personal-decision-journal.md contains `knowledge_domain: personal-goals`
    - All other wiki pages contain `knowledge_domain: science`
    - No page content (body text below frontmatter) is modified
  </acceptance_criteria>
  <done>All 10 existing wiki pages have knowledge_domain and has_contradictions frontmatter fields. Domain assignments reflect content type: biography for entity pages, personal-goals for personal content, science for psychology/research content.</done>
</task>

</tasks>

<verification>
- `grep -c 'knowledge_domain' AGENTS.md` returns at least 5 (field definition, table references, validation checklist)
- `grep -c 'has_contradictions' AGENTS.md` returns at least 3
- `grep -c 'Base Decay Period' AGENTS.md` returns 1
- `grep 'knowledge_domain:' wiki/concepts/cognitive-biases.md` returns `knowledge_domain: science`
- `grep 'knowledge_domain:' wiki/entities/daniel-kahneman.md` returns `knowledge_domain: biography`
- `grep 'has_contradictions:' wiki/overviews/decision-making.md` returns `has_contradictions: false`
</verification>

<success_criteria>
AGENTS.md contains complete schema definitions for domain-based decay rates, contradiction inline syntax, severity tiers, auto-fix boundary, and two new frontmatter fields. All 10 existing wiki pages have knowledge_domain and has_contradictions fields. The lint implementation (Plan 02+) can reference these schema definitions as authoritative.
</success_criteria>

<output>
After completion, create `.planning/phases/05-lint-quality/05-01-SUMMARY.md`
</output>
