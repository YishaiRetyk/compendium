---
phase: 13-claim-faithfulness-audit
plan: 05
type: execute
wave: 3
depends_on: [13-01, 13-02, 13-03, 13-04]
files_modified:
  - .planning/phases/13-claim-faithfulness-audit/13-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [FAITH-01, FAITH-02, FAITH-03, FAITH-04]
user_setup: []

must_haves:
  truths:
    - "13-VERIFICATION.md follows the Phase 12.2 mirror format: frontmatter (phase/verified/status/score), Goal Achievement truths→evidence table, Required Artifacts table, Behavioral Spot-Checks with verbatim CLI evidence, Requirements Coverage, and per-REQ-ID `- [x] **FAITH-0X**: Complete` lines."
    - "Every FAITH-01..04 evidence row cites concrete file paths + the passing `bash tests/phase-13/<test>.sh` commands (no subjective claims)."
    - "REQUIREMENTS.md FAITH-01..04 checkboxes are flipped to [x] with the `**Status:** Complete (Phase 13). See ...VERIFICATION.md` note, and the traceability matrix rows flip to Complete."
    - "bin/requirements-sync.sh --strict --phase 13 exits 0 (zero drift) and --require-complete --phase 13 exits 0 (all 4 in-scope REQ-IDs Complete)."
  artifacts:
    - path: ".planning/phases/13-claim-faithfulness-audit/13-VERIFICATION.md"
      provides: "Phase 13 verification evidence in the 12.2 mirror format"
      contains: "FAITH-01"
  key_links:
    - from: "13-VERIFICATION.md"
      to: "REQUIREMENTS.md FAITH-01..04"
      via: "bin/requirements-sync.sh REQ_STATUS parser"
      pattern: "FAITH-0"
    - from: "REQUIREMENTS.md FAITH rows"
      to: "13-VERIFICATION.md"
      via: "**Status:** Complete (Phase 13). See ... note"
      pattern: "Phase 13"
---

<objective>
Close Phase 13: author `13-VERIFICATION.md` in the Phase 12.2 mirror format with concrete file-path + passing-test evidence for every FAITH-01..04 requirement, flip the four FAITH checkboxes and traceability rows in REQUIREMENTS.md, and prove the green-light gate (`bin/requirements-sync.sh --strict --phase 13` exit 0 + `--require-complete --phase 13` exit 0). This is the requirements-sync consumer that the v1.1 closure gate (CLOSE-01) will later audit.

Purpose: the mechanical traceability handshake that marks FAITH-01..04 done and unblocks Phase 13.1/13.2.
Output: `13-VERIFICATION.md` + the REQUIREMENTS.md status flip, with verbatim CLI evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.2-local-wiki-write-gate/12.2-VERIFICATION.md
@.planning/phases/13-01-SUMMARY.md
@.planning/phases/13-02-SUMMARY.md
@.planning/phases/13-03-SUMMARY.md
@.planning/phases/13-04-SUMMARY.md

<interfaces>
<!-- 12.2-VERIFICATION.md is the format template (read it fully). Required sections:
       - frontmatter: phase, verified (ISO datetime), status: passed, score, phase_base_sha (+ anchor cmd)
       - ## Goal Achievement: Observable Truths table (ROADMAP SC-1..6 → evidence)
       - ## Required Artifacts table (Artifact | Exists | Substantive | Wired | Status)
       - ## Behavioral Spot-Checks table + verbatim CLI evidence fenced blocks
       - ## Requirements Coverage table (REQ | Source Plan | Status | Evidence)
       - ## REQ-ID Verification: per-REQ `- [x] **FAITH-0X**: Complete` + Truth/Evidence/Acceptance bullets
         (this is the block bin/requirements-sync.sh parses — REQ_STATUS regex, mirror 12.2:208-242). -->

<!-- requirements-sync green-light (the phase gate, from 13-VALIDATION.md Sampling Rate):
       bash bin/requirements-sync.sh --strict --phase 13         → exit 0, 0 drift / 4
       bash bin/requirements-sync.sh --require-complete --phase 13 → exit 0, 0 incomplete / 4 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 13-VERIFICATION.md (12.2 mirror format) with concrete per-requirement evidence</name>
  <read_first>
    - .planning/phases/12.2-local-wiki-write-gate/12.2-VERIFICATION.md (the exact mirror format to follow — frontmatter, Goal Achievement truths table, Required Artifacts, Behavioral Spot-Checks with verbatim CLI fenced blocks, Requirements Coverage, the REQ-ID Verification block requirements-sync parses)
    - .planning/ROADMAP.md (Phase 13 entry lines ~226-244 — SC-1..6 + non-goals, the truths to map evidence against)
    - .planning/REQUIREMENTS.md (lines ~135-141 — the FAITH-01..04 statements verbatim)
    - .planning/phases/13-01-SUMMARY.md, 13-02-SUMMARY.md, 13-03-SUMMARY.md, 13-04-SUMMARY.md (the actual artifacts + test names produced — cite their real file paths and passing tests)
    - bin/requirements-sync.sh (the REQ_STATUS / LEADING_JUNK parser — confirm the `- [x] **FAITH-0X**: Complete` line shape it consumes)
  </read_first>
  <action>
Create `.planning/phases/13-claim-faithfulness-audit/13-VERIFICATION.md` mirroring `12.2-VERIFICATION.md` exactly. Sections:

1. **Frontmatter:** `phase: 13-claim-faithfulness-audit`, `verified: <ISO datetime>`, `status: passed`, `score: 6/6 success criteria verified (4/4 FAITH requirements satisfied)`, and a `phase_base_sha` anchored to the CONTEXT commit (`git log --format=%H -n 1 -- .planning/phases/13-claim-faithfulness-audit/13-CONTEXT.md`) with the anchor command documented, exactly as 12.2 does.

2. **## Goal Achievement — Observable Truths:** a table mapping each ROADMAP SC-1..6 to evidence:
   - SC-1 (sampling): `bin/audit-claims.sh` exists with four selectors; cite `test_select_*.sh` + `test_sampling_cap_logging.sh` PASS.
   - SC-2 (locator resolution + 4-way verdict): cite `resolve_locator` + `test_resolve_*.sh` + `test_verdict_paths.sh` PASS.
   - SC-3 (privacy): cite `bin/lib/privacy_resolve.py` + `test_privacy_partition_fail_closed.sh` + `test_skipped_privacy.sh` PASS.
   - SC-4 (structured 6-field findings): cite the 9-key dict + `test_output_schema.sh` PASS.
   - SC-5 (review-only / no auto-fix / no default gate): cite `test_no_page_mutation.sh` PASS + the never-error severity map + no CI wiring.
   - SC-6 (machine-readable, lint-consumable): cite `test_json_lint_compat.sh` PASS.

3. **## Required Artifacts** table: `bin/audit-claims.sh`, `bin/lib/privacy_resolve.py`, `wiki/maintenance/audit-report.md`, `wiki/maintenance/audit-state.md`, the AGENTS.md §6 + Audit-workflow edits (+ CLAUDE.md + schema/AGENTS.template.md mirrors), `tests/phase-13/` (run.sh + lib.sh + ~22 test_*.sh), and this file — each with Exists/Substantive/Wired/Status.

4. **## Behavioral Spot-Checks** + verbatim CLI evidence fenced blocks: paste the actual output of `bash tests/phase-13/run.sh` (the `PHASE 13 TESTS: N/N` banner), `bash bin/audit-claims.sh --version`, `bash bin/check-neutrality.sh` (exit 0), `bash bin/sync-claude.sh --check` (exit 0), and `bash bin/lint.sh --category yaml` over the new control-plane files (exit 0).

5. **## Requirements Coverage** table + **## REQ-ID Verification** block: emit the per-REQ `- [x] **FAITH-01**: Complete` … `- [x] **FAITH-04**: Complete` lines with Truth/Evidence/Acceptance bullets, mirroring 12.2:208-242 exactly (this is what requirements-sync parses). Cite the source plan for each: FAITH-01→13-02; FAITH-02→13-02 (det) + 13-03 (verdict); FAITH-03→13-02; FAITH-04→13-03.

Every evidence cell MUST be a concrete file path or a `bash tests/phase-13/<test>.sh` command — no subjective language.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-claim-faithfulness-audit/13-VERIFICATION.md && grep -c '\[x\] \*\*FAITH-0' .planning/phases/13-claim-faithfulness-audit/13-VERIFICATION.md && bash tests/phase-13/run.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/13-claim-faithfulness-audit/13-VERIFICATION.md`.
    - `grep -c '\[x\] \*\*FAITH-0' 13-VERIFICATION.md` returns 4 (one per requirement in the REQ-ID Verification block).
    - Frontmatter has `status: passed` and a `score:` line; `grep -q 'phase_base_sha' 13-VERIFICATION.md`.
    - The Behavioral Spot-Checks section contains a verbatim `PHASE 13 TESTS:` fenced block and a `bin/check-neutrality.sh` exit-0 line.
    - No subjective evidence: every Requirements Coverage / REQ-ID evidence bullet cites a file path or a `bash tests/phase-13/` command.
  </acceptance_criteria>
  <done>13-VERIFICATION.md exists in the 12.2 mirror format, maps SC-1..6 and FAITH-01..04 to concrete file-path + passing-test evidence, and carries the verbatim CLI evidence blocks.</done>
</task>

<task type="auto">
  <name>Task 2: Flip FAITH-01..04 in REQUIREMENTS.md and prove the requirements-sync green-light gate</name>
  <read_first>
    - .planning/REQUIREMENTS.md (lines ~135-141 — the four FAITH bullets to flip; and the traceability matrix near the bottom — find the FAITH rows in the per-requirement status table)
    - .planning/phases/12.2-local-wiki-write-gate/12.2-VERIFICATION.md (the "REQUIREMENTS.md Diff" section — the exact `**Status:** Complete (Phase 12.2). See ...` note format to mirror, and the matrix-row flip)
    - bin/requirements-sync.sh (the --strict and --require-complete --phase semantics; the parser that cross-checks REQUIREMENTS.md against 13-VERIFICATION.md)
  </read_first>
  <action>
Edit `.planning/REQUIREMENTS.md`:
1. Flip each FAITH-01..04 bullet from `- [ ]` to `- [x]` and append `**Status:** Complete (Phase 13). See `.planning/phases/13-claim-faithfulness-audit/13-VERIFICATION.md`.` to each (mirror the 12.2 note format exactly).
2. In the traceability matrix near the bottom of REQUIREMENTS.md, flip the FAITH-01..04 status cells from Pending to Complete.

Then prove the gate (paste the verbatim output into the 13-VERIFICATION.md Behavioral Spot-Checks if not already there, or into the SUMMARY):
- `bash bin/requirements-sync.sh --strict --phase 13` → must exit 0, 0 drift of 4.
- `bash bin/requirements-sync.sh --require-complete --phase 13` → must exit 0, 0 incomplete of 4.

If either exits non-zero, the drift is between the REQUIREMENTS.md status and the 13-VERIFICATION.md REQ-ID block — reconcile (usually a REQ-ID typo or a missing `- [x] **FAITH-0X**: Complete` line) and re-run until both exit 0.
  </action>
  <verify>
    <automated>bash bin/requirements-sync.sh --strict --phase 13; echo "strict=$?"; bash bin/requirements-sync.sh --require-complete --phase 13; echo "complete=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\[x\] \*\*FAITH-0' .planning/REQUIREMENTS.md` returns 4 (all four flipped) AND each carries `Phase 13` in its Status note.
    - `bash bin/requirements-sync.sh --strict --phase 13` exits 0 with `0 drift row(s) of 4`.
    - `bash bin/requirements-sync.sh --require-complete --phase 13` exits 0 with `all 4 in-scope REQ-IDs are Complete`.
    - The FAITH rows in the traceability matrix read Complete (no remaining Pending for FAITH-*).
  </acceptance_criteria>
  <done>REQUIREMENTS.md FAITH-01..04 are flipped to Complete with the Phase 13 note + matrix rows updated, and both requirements-sync modes exit 0 — the mechanical green-light gate for Phase 13.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 13-VERIFICATION.md REQ-ID block → requirements-sync parser | A malformed REQ-ID line silently drops a requirement from the strict check |
| REQUIREMENTS.md status ↔ VERIFICATION.md status | Drift between the two is exactly what the gate exists to catch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-15 | Repudiation | a requirement marked Complete without real evidence | mitigate | Every REQ-ID Verification bullet cites a concrete file path or a passing `bash tests/phase-13/` command; acceptance forbids subjective evidence; `--require-complete` exit 0 confirms all 4 in-scope IDs are Complete on both sides |
| T-13-16 | Tampering | REQUIREMENTS.md ↔ VERIFICATION.md drift slipping past closure | mitigate | `requirements-sync --strict --phase 13` exit 0 (0 drift) is a hard acceptance criterion; this is the same gate CLOSE-01 re-runs at milestone close |
</threat_model>

<verification>
- `bash bin/requirements-sync.sh --strict --phase 13` exit 0; `--require-complete --phase 13` exit 0.
- `grep -c '\[x\] \*\*FAITH-0' .planning/REQUIREMENTS.md` == 4.
- `bash tests/phase-13/run.sh` still green (no regression from the doc edits).
</verification>

<success_criteria>
- FAITH-01..04 are verified with concrete evidence in `13-VERIFICATION.md` (12.2 mirror format) and flipped to Complete in REQUIREMENTS.md (bullets + matrix).
- The mechanical green-light gate holds: both `requirements-sync` modes exit 0 for phase 13.
- Phase 13 is closed and ready for Phase 13.1.
</success_criteria>

<output>
After completion, create `.planning/phases/13-claim-faithfulness-audit/13-05-SUMMARY.md`.
</output>
