---
phase: 13-claim-faithfulness-audit
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/phase-13/run.sh
  - tests/phase-13/lib.sh
  - tests/phase-13/fixtures/README.md
autonomous: true
requirements: [FAITH-01, FAITH-02, FAITH-03, FAITH-04]
user_setup: []

must_haves:
  truths:
    - "Running `bash tests/phase-13/run.sh` executes a RED aggregator that fails because bin/audit-claims.sh does not yet exist."
    - "lib.sh provides make_bare_repo / write_page / assert_exit_code / cleanup_fixture_repo (copied verbatim from phase-12.2) plus make_fake_verifier and make_recording_verifier."
    - "make_fake_verifier honors the D-01 contract: reads {claim,passage,support_type} on stdin, emits {verdict,rationale,sub_claims} on stdout."
    - "make_recording_verifier appends every stdin passage it receives to a sentinel log so the fail-closed partition can be asserted by absence."
    - "Self-contained fixtures write BOTH the wiki/sources/<id>.md summary AND the raw source file at its path:, with real ## headings, blank-line paragraphs, and <!-- page: N --> markers."
  artifacts:
    - path: "tests/phase-13/run.sh"
      provides: "Per-phase aggregator emitting 'PHASE 13 TESTS: N/M'"
      contains: "PHASE 13 TESTS"
    - path: "tests/phase-13/lib.sh"
      provides: "Fixture + verifier helper library"
      contains: "make_fake_verifier"
    - path: "tests/phase-13/fixtures/README.md"
      provides: "Documents the self-contained fixture contract"
  key_links:
    - from: "tests/phase-13/run.sh"
      to: "tests/phase-13/test_*.sh"
      via: "nullglob glob loop"
      pattern: "test_\\*.sh"
    - from: "tests/phase-13/lib.sh"
      to: "make_fake_verifier / make_recording_verifier"
      via: "export -f"
      pattern: "export -f"
---

<objective>
Land the Wave 0 test harness for Phase 13 so every downstream plan has `bash tests/phase-13/test_*.sh` to verify against. This is a RED-first harness: the aggregator and helpers exist, but the per-scenario `test_*.sh` files are authored by the plans that implement the behavior they test (a thin set of canary/skeleton tests ship here so the aggregator is non-empty and demonstrably RED).

The load-bearing deliverable is the **fake/recording verifier insight** (from 13-VALIDATION.md): the verdict step is non-deterministic (LLM), but everything else in the pipeline is deterministic. A fake `--verifier` makes the whole pipeline reproducible; a recording `--verifier` proves the fail-closed privacy partition by negative assertion (the sentinel log never contains a local_only source's text).

Purpose: De-risk the rest of the phase — Plans 02/03/04 author their own `test_*.sh` against these helpers and run them per-commit.
Output: `tests/phase-13/run.sh`, `tests/phase-13/lib.sh` (copied 4 helpers + 2 new verifier helpers), `tests/phase-13/fixtures/README.md` documenting the self-contained fixture contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-claim-faithfulness-audit/13-VALIDATION.md
@.planning/phases/13-claim-faithfulness-audit/13-RESEARCH.md
@.planning/phases/13-claim-faithfulness-audit/13-PATTERNS.md

<interfaces>
<!-- The D-01 verifier subprocess contract these helpers must honor (from 13-CONTEXT.md D-01, 13-RESEARCH.md):
     stdin (one JSON object per claim, or a JSON array — caller's choice in Plan 02/03):
       {"claim": "...", "passage": "...", "support_type": "direct|inferred|tentative|derived"}
     stdout:
       {"verdict": "supports|weak|contradicts|insufficient", "rationale": "...", "sub_claims": [...]}
     The fake verifier echoes a canned {verdict,rationale}; the recording verifier additionally
     appends its received stdin to "$repo/verifier-saw.log". -->

<!-- Copy-verbatim source: tests/phase-12.2/lib.sh lines 15-69 (make_bare_repo, write_page,
     assert_exit_code, cleanup_fixture_repo) and tests/phase-12.2/run.sh lines 27-40 (aggregator). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy the phase-12.2 harness shell + add the two verifier helpers</name>
  <read_first>
    - tests/phase-12.2/run.sh (the aggregator to copy near-verbatim — nullglob loop, PASS/FAIL tally, non-zero exit on any failure)
    - tests/phase-12.2/lib.sh (copy make_bare_repo / write_page / assert_exit_code / cleanup_fixture_repo verbatim; note the REPO_ROOT derivation and the `export -f` line)
    - .planning/phases/13-claim-faithfulness-audit/13-PATTERNS.md (the `tests/phase-13/run.sh` and `tests/phase-13/lib.sh` pattern-assignment sections — exact copy-vs-change notes + the make_fake_verifier shape)
    - .planning/phases/13-claim-faithfulness-audit/13-RESEARCH.md (§ Validation Architecture — make_fake_verifier verified shape; § "How to test the load-bearing fail-closed partition" — recording verifier requirement)
  </read_first>
  <action>
Create `tests/phase-13/run.sh` by copying `tests/phase-12.2/run.sh` and changing only: the aggregator banner from `PHASE 12.2 TESTS:` to `PHASE 13 TESTS:`. Keep the `SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"` derivation, `shopt -s nullglob`, the `for t in "$SCRIPT_DIR"/test_*.sh` loop, the PASS/FAIL/TOTAL tally, `FAILED_TESTS` array, and `if [ "$FAIL" -gt 0 ]; then ... exit 1; fi; exit 0`.

Create `tests/phase-13/lib.sh` by copying these helpers VERBATIM from `tests/phase-12.2/lib.sh`: `make_bare_repo` (bump the mktemp prefix to `phase13-`), `write_page` (writes body from stdin via heredoc; used to write BOTH wiki/sources summaries AND raw sources/** files), `assert_exit_code`, `cleanup_fixture_repo`. Keep the `REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"` derivation so tests can invoke `$REPO_ROOT/bin/audit-claims.sh`.

ADD two new helpers honoring the D-01 contract:

```bash
# make_fake_verifier <repo> <verdict> <rationale> -> echoes path to the stub
# Reads the D-01 contract on stdin, emits a canned {verdict,rationale,sub_claims}.
make_fake_verifier() {
    cat > "$1/fake-verifier.sh" <<EOF
#!/usr/bin/env bash
read -r _input
printf '{"verdict":"%s","rationale":"%s","sub_claims":[]}\n' "$2" "$3"
EOF
    chmod +x "$1/fake-verifier.sh"; echo "$1/fake-verifier.sh"
}

# make_recording_verifier <repo> <verdict> -> echoes path to a verifier that
# APPENDS every stdin payload to "$repo/verifier-saw.log" before replying.
# Used by the load-bearing fail-closed partition negative test (D-02): assert
# the log NEVER contains the local_only source's distinctive passage text.
make_recording_verifier() {
    cat > "$1/recording-verifier.sh" <<EOF
#!/usr/bin/env bash
_input="\$(cat)"
printf '%s\n' "\$_input" >> "$1/verifier-saw.log"
printf '{"verdict":"%s","rationale":"recorded","sub_claims":[]}\n' "$2"
EOF
    chmod +x "$1/recording-verifier.sh"; echo "$1/recording-verifier.sh"
}
```

Add both new helpers to the `export -f` line alongside the four copied helpers. Note the `\$_input`/`\$(cat)` escaping: the recording verifier must read the FULL stdin payload (not a single `read -r` line) so a multi-line JSON passage is captured intact in the sentinel log — getting this escaping right inside the heredoc is the one subtle part.
  </action>
  <verify>
    <automated>test -x tests/phase-13/run.sh || bash -n tests/phase-13/run.sh; bash -n tests/phase-13/lib.sh && grep -q 'PHASE 13 TESTS' tests/phase-13/run.sh && grep -q 'make_fake_verifier' tests/phase-13/lib.sh && grep -q 'make_recording_verifier' tests/phase-13/lib.sh && grep -q 'verifier-saw.log' tests/phase-13/lib.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n tests/phase-13/run.sh` exits 0 (valid bash) AND `grep -q 'PHASE 13 TESTS' tests/phase-13/run.sh`.
    - `bash -n tests/phase-13/lib.sh` exits 0.
    - `grep -c 'make_bare_repo\|write_page\|assert_exit_code\|cleanup_fixture_repo' tests/phase-13/lib.sh` ≥ 4 (all four copied helpers present).
    - `grep -q 'make_fake_verifier' tests/phase-13/lib.sh` AND `grep -q 'make_recording_verifier' tests/phase-13/lib.sh` AND `grep -q 'verifier-saw.log' tests/phase-13/lib.sh`.
    - `grep -q 'export -f' tests/phase-13/lib.sh` and that line names all six helpers.
    - `grep -q 'phase13-' tests/phase-13/lib.sh` (mktemp prefix bumped).
  </acceptance_criteria>
  <done>run.sh and lib.sh exist, are valid bash, and lib.sh exports the four copied helpers plus the two new verifier helpers honoring the D-01 contract; the recording verifier captures full multi-line stdin to verifier-saw.log.</done>
</task>

<task type="auto">
  <name>Task 2: Document the self-contained fixture contract + ship one RED canary so the aggregator is non-empty and demonstrably RED</name>
  <read_first>
    - .planning/phases/13-claim-faithfulness-audit/13-VALIDATION.md (§ Wave 0 Requirements — the self-contained fixture requirement: each fixture writes BOTH the wiki/sources/<id>.md summary AND the raw file at its path:; the fixture-realism note that examples/kahneman/ summaries point at raw files NOT on disk)
    - .planning/phases/13-claim-faithfulness-audit/13-RESEARCH.md (§ Fixture realism note — every current wiki/sources/*.md is local_only; the D-03 local-heavy default is the real production state)
    - tests/phase-12.2/test_staged_passes_concept_with_prov.sh (representative per-scenario test shape: source lib.sh → make_bare_repo → trap cleanup → write_page → set +e/rc=$?/set -e → assert_exit_code → echo PASS)
    - CLAUDE.md §6 (the [prov:source_id#locator] grammar + locator types the fixtures must use); CLAUDE.md §5 (source-summary required fields: path, content_hash, ingested_at, source_type, compiled_against_hash)
  </read_first>
  <action>
Create `tests/phase-13/fixtures/README.md` documenting the **self-contained fixture contract** that every downstream `test_*.sh` follows (fixtures are built inline via `write_page`, not copied from a fixtures dir, per 13-PATTERNS.md "No Analog Found"). Document concretely, using ABSTRACT placeholders only (no real vault terms — CLAUDE.md §3 neutrality rule, since `tests/` is not a public path but keep the discipline):

1. Each fixture writes BOTH artifacts:
   - the wiki/sources/<source-id>.md SUMMARY page (frontmatter `type: source` with `id`, `path: sources/YYYY/YYYY-MM/<YYYY-MM-DD-slug>/source.md`, `content_hash`, `compiled_against_hash`, `ingested_at`, `source_type`, `privacy`).
   - the RAW source file at that exact `path:` (so the D-11 resolver reads the raw file, never the summary's `## Extracted Claims`).
2. Raw sources carry real `## headings` (for `#sec:`), blank-line-delimited paragraphs (for `#para`), `<!-- page: N -->` markers (for `#p` against a MARKED source per D-05), and at least one UNMARKED paginated raw source (for the `#p` → `insufficient-locator` degrade path per D-06).
3. Fixture variants needed across the phase: cloud_safe + local_only privacy; marked + unmarked pagination; an `#img` claim; a missing-raw-file case (summary present, raw absent → graceful `insufficient-locator`).
4. The stale-source fixture sets `content_hash != compiled_against_hash` (FAITH-01 stale selector); the high-fanout fixture seeds ≥N inbound wikilinks; the recency fixture commits then modifies a page (git-diff selector).
5. State explicitly: every current production `wiki/sources/*.md` is `local_only`, so the D-03 `skipped-privacy`-by-default behavior is the realistic production state — high skipped-privacy counts are NOT a test failure (D-03).

Then ship ONE RED canary test `tests/phase-13/test_harness_red_canary.sh` that asserts the aggregator is wired and currently RED: it asserts `bin/audit-claims.sh` does NOT yet exist (so this test PASSES today, proving the harness runs), and emits a `PASS:` line. This keeps the aggregator non-empty without pretending the audit script exists. (Downstream plans DELETE this canary when they add their real tests, or it flips to assert the script DOES exist — Plan 02 will replace it. Per the Rule-3 prior-phase-test obsolescence precedent, Plan 02 may relax/remove it.)

```bash
#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/lib.sh"
if [ -f "$REPO_ROOT/bin/audit-claims.sh" ]; then
    echo "INFO: bin/audit-claims.sh now exists — Plan 02 should remove this Wave-0 canary." >&2
fi
echo "PASS: phase-13 harness wired (Wave 0 canary)"
```
  </action>
  <verify>
    <automated>bash tests/phase-13/run.sh; echo "rc=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/phase-13/fixtures/README.md` AND `grep -q 'content_hash' tests/phase-13/fixtures/README.md` AND `grep -q 'page: N' tests/phase-13/fixtures/README.md` AND `grep -q 'local_only' tests/phase-13/fixtures/README.md`.
    - `bash tests/phase-13/run.sh` exits 0 and prints `PHASE 13 TESTS: 1/1` (the canary passes; the aggregator is wired and non-empty).
    - `grep -q 'self-contained' tests/phase-13/fixtures/README.md` (the contract is named).
    - The fixtures README enumerates all variants: cloud_safe, local_only, marked, unmarked, img, missing-raw, stale-source, high-fanout, recency (`grep -c` of these tokens ≥ 8 across the file, excluding comment-only lines).
  </acceptance_criteria>
  <done>fixtures/README.md documents the self-contained BOTH-artifacts contract with all required variants; `bash tests/phase-13/run.sh` runs the canary green (1/1), proving the harness is wired and ready for downstream tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → audit-claims.sh under test | Fixtures are inert input; no untrusted external input crosses into this plan |
| fake/recording verifier ← audit-claims.sh stdin | The verifier helpers receive passage text on stdin; the recording verifier writes it to a sentinel log within the throwaway fixture repo only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | `cleanup_fixture_repo` rm -rf | mitigate | Copy the phase-12.2 guard verbatim: only `rm -rf` when path is non-empty, a dir, AND matches `/tmp/*` — prevents accidental deletion outside the temp tree |
| T-13-02 | Information Disclosure | recording verifier sentinel log | accept | The sentinel log lives inside the throwaway `mktemp -d` fixture repo and is deleted by cleanup; it only ever contains fixture passage text, never real vault content |
| T-13-03 | Denial of Service | heredoc-escaped recording verifier reading unbounded stdin | accept | Fixture passages are tiny test strings; no real-world payload size; `$(cat)` is bounded by the fixture |
</threat_model>

<verification>
- `bash tests/phase-13/run.sh` → `PHASE 13 TESTS: 1/1`, exit 0.
- `bash -n tests/phase-13/run.sh` and `bash -n tests/phase-13/lib.sh` both exit 0.
- lib.sh exports six helpers (four copied + two new) via `export -f`.
</verification>

<success_criteria>
- The Wave 0 harness (`run.sh` + `lib.sh` + fixture contract) exists and is demonstrably RED-ready: the aggregator runs green on the canary today and is ready to absorb downstream `test_*.sh`.
- The D-01 verifier contract is encoded in `make_fake_verifier` (canned verdict) and `make_recording_verifier` (sentinel-log passages for the FAITH-04 negative test).
- The self-contained fixture contract (BOTH summary AND raw file) is documented so no downstream test points at a raw file that is not on disk.
</success_criteria>

<output>
After completion, create `.planning/phases/13-claim-faithfulness-audit/13-01-SUMMARY.md`.
</output>
