---
phase: 13-claim-faithfulness-audit
plan: 04
type: execute
wave: 1
depends_on: [13-01]
files_modified:
  - AGENTS.md
  - CLAUDE.md
  - schema/AGENTS.template.md
  - schema/fixtures/canonical-AGENTS.md
  - tests/phase-13/test_page_marker_convention.sh
  - tests/phase-13/test_agents_claude_mirror.sh
autonomous: true
requirements: [FAITH-02, FAITH-04]
user_setup: []

must_haves:
  truths:
    - "AGENTS.md §6 documents the <!-- page: N --> page-marker convention immediately after the Locator Types table, using ABSTRACT placeholders only (no real vault terms)."
    - "The convention states: HTML comment at page boundaries, Obsidian-invisible, grep-able, NO change to the [prov:] grammar; #p8 slices page:8→page:9, #p12-14 spans page:12→page:15 (exclusive); optional, absent → insufficient-locator (D-06)."
    - "AGENTS.md documents the Audit as a review-only workflow/operation (NOT a 5th top-level operation; the four-operation framing is preserved), including the D-10 human-approved contradicts→marker handoff and the D-08 non-binding reflect-tier 'audit recommended' suggestion."
    - "AGENTS.md describes the audit privacy gate as EFFECTIVE CLAIM privacy — the strictest of {claim-page privacy, source-summary privacy, raw-source privacy, enclosing-dir signal, fail-closed local_only default} — NOT 'source privacy' alone (MEDIUM 13-04, mirroring the cycle-2 HIGH-A implementation contract in Plan 03). The doc also notes local-only skipped-privacy metadata is redacted to a bare count on cloud-facing --emit-worklist stdout (REVIEW HIGH-C). Mirrored byte-equal to CLAUDE.md and into schema/AGENTS.template.md."
    - "CLAUDE.md is byte-equal to AGENTS.md (auto-synced by .githooks/pre-commit / bin/sync-claude.sh)."
    - "schema/AGENTS.template.md carries the same §6 + workflow additions (manual mirror, wizard {{placeholders}} preserved); the §6 page-marker subsection is byte-identical to AGENTS.md's modulo {{PLACEHOLDER}} tokens."
    - "schema/fixtures/canonical-AGENTS.md is regenerated from the edited template so tests/phase-08/test_canonical_byte_equality.sh (and its phase-09.1 wrapper) stay green."
    - "bin/check-neutrality.sh passes (no real vault terms in the template-public edits)."
  artifacts:
    - path: "AGENTS.md"
      provides: "§6 page-marker convention + Audit workflow documentation"
      contains: "<!-- page:"
    - path: "CLAUDE.md"
      provides: "Byte-equal mirror of AGENTS.md"
      contains: "<!-- page:"
    - path: "schema/AGENTS.template.md"
      provides: "Wizard-source mirror of the §6 + workflow additions"
      contains: "<!-- page:"
    - path: "schema/fixtures/canonical-AGENTS.md"
      provides: "Regenerated wizard-render fixture matching the edited template (byte-equality gate)"
      contains: "<!-- page:"
  key_links:
    - from: "AGENTS.md §6"
      to: "CLAUDE.md"
      via: "bin/sync-claude.sh byte-copy (hook-enforced)"
      pattern: "page:"
    - from: "schema/AGENTS.template.md"
      to: "schema/fixtures/canonical-AGENTS.md"
      via: "bin/init-wizard.sh render (canonical byte-equality fixture)"
      pattern: "page:"
    - from: "AGENTS.md Audit workflow"
      to: "§6 [contradiction:] + [epistemic:: tentative] markers"
      via: "D-10 human-approved handoff documentation"
      pattern: "contradicts"
---

<objective>
Land the schema-surface and workflow-documentation half of Phase 13: the `<!-- page: N -->` page-marker convention in AGENTS.md §6 (the D-04 deliberate v1 inclusion — the one override of the design-note v1.2 deferral) and the documentation of the Audit as a review-only workflow with the D-10 contradicts→marker handoff and the D-08 reflect-tier suggestion. This is a 3-way mirror edit (AGENTS.md → CLAUDE.md byte-equal via sync-claude → schema/AGENTS.template.md manual mirror) on TEMPLATE-PUBLIC files, so abstract placeholders are mandatory (CLAUDE.md §3 neutrality rule; `bin/check-neutrality.sh` is the backstop, not the defense). Editing the template ALSO requires regenerating the canonical wizard-render fixture so the existing byte-equality CI gate stays green.

This plan is the read-side contract the audit resolver consumes: the `#p` slice semantics documented here are exactly what `resolve_locator` (Plan 02) implements. It runs in parallel with Plan 02 (disjoint file set) — they agree on the D-05 slice semantics by both citing 13-CONTEXT.md D-05.

Purpose: ship the optional, additive, dependency-free locator-precision convention (D-04/D-05/D-06/D-07) and frame Audit in the workflow surface without churning the four-operation model.
Output: §6 convention + Audit workflow docs mirrored across all three files (+ regenerated fixture), plus two mirror/neutrality guard tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-claim-faithfulness-audit/13-PATTERNS.md
@.planning/phases/13-claim-faithfulness-audit/13-RESEARCH.md
@.planning/phases/13-01-SUMMARY.md

<interfaces>
<!-- D-05 page-marker convention to document in §6 (abstract placeholders ONLY):
       Marker:  <!-- page: N -->  inserted in the RAW source at each page boundary.
       Properties: HTML comment (Obsidian-invisible), grep-able, NO [prov:] grammar change (#p already exists).
       Slice:   #p8     resolves from the `<!-- page: 8 -->` marker to the line before `<!-- page: 9 -->`.
                #p12-14 spans the `<!-- page: 12 -->` marker to the `<!-- page: 15 -->` marker (exclusive upper).
       Optional: never required; absent markers → #p degrades to the insufficient-locator audit verdict (D-06).
       Document-now / helper-later (D-07): the curator hand-marks sources; auto-insertion deferred to v1.2.
     Use placeholders like <source-id>, <YYYY-MM-DD-slug>, N — NEVER real vault terms (CLAUDE.md §3). -->

<!-- 3-way mirror mechanism (13-PATTERNS.md):
     1. Edit AGENTS.md.
     2. bin/sync-claude.sh byte-copies AGENTS.md → CLAUDE.md; .githooks/pre-commit auto-syncs + re-stages on drift.
        Do NOT hand-edit CLAUDE.md — run `bash bin/sync-claude.sh` (or let the hook) propagate.
     3. schema/AGENTS.template.md is the wizard source with {{PLACEHOLDER}} tokens — mirror the §6 + workflow
        additions MANUALLY (it is not byte-copied; it carries wizard placeholders).
     4. Editing the template breaks the canonical byte-equality fixture — regenerate
        schema/fixtures/canonical-AGENTS.md from the edited template (see Task 1 step 3). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the §6 <!-- page: N --> page-marker convention to AGENTS.md, mirror to CLAUDE.md + schema/AGENTS.template.md, regenerate the canonical fixture</name>
  <read_first>
    - AGENTS.md §6 (the "### Locator Types" table — the exact insertion point; the surrounding Provenance/Epistemics/Staleness section; the existing #p/#sec:/#para/#t/#img rows)
    - CLAUDE.md §6 (confirm it currently byte-mirrors AGENTS.md §6 so the edit propagates cleanly)
    - schema/AGENTS.template.md §6 (the wizard-source mirror with {{PLACEHOLDER}} tokens — the manual-mirror target; the "### Locator Types" table is the same insertion point)
    - bin/sync-claude.sh (the byte-copy + cmp -s check) and .githooks/pre-commit (the auto-sync + re-stage on drift)
    - bin/check-neutrality.sh (the denylist backstop — NOTE: PUBLIC_PATHS does NOT include schema/, so the template mirror is NOT covered by this gate; see step 2 mitigation)
    - tests/phase-08/test_canonical_byte_equality.sh and tests/phase-09.1/test_byte_equality.sh (the byte-equality gate that editing schema/AGENTS.template.md WILL break — and the documented fixture-regenerate recipe inside test_canonical_byte_equality.sh, which step 3 reproduces verbatim)
    - schema/fixtures/canonical-answers.yaml and schema/fixtures/README.md (the frozen answers + regeneration procedure)
    - .planning/phases/13-claim-faithfulness-audit/13-PATTERNS.md (the AGENTS.md §6 + 3-way mirror pattern-assignment section — insertion point + mirror mechanism + placeholder discipline)
    - .planning/phases/13-claim-faithfulness-audit/13-RESEARCH.md (Open Q3 — recommended placement: a "Page-marker convention" subsection right after the Locator Types table, abstract placeholders only)
  </read_first>
  <action>
Add a "### Page-marker convention" subsection to AGENTS.md §6 immediately AFTER the "### Locator Types" table. Document, with ABSTRACT placeholders only:

- Markdown-native raw sources lose page boundaries, so `#p<n>` locators have nothing to bound against. The OPTIONAL `<!-- page: N -->` HTML-comment marker, hand-inserted in the raw source at each page break, makes `#p` resolvable where authors opt in. It is Obsidian-invisible (does not render), grep-able, and requires NO change to the `[prov:]` grammar (`#p` already exists in the Locator Types table).
- Slice semantics: `#p8` resolves from the `<!-- page: 8 -->` marker to the line before `<!-- page: 9 -->`; `#p12-14` spans the `<!-- page: 12 -->` marker to the `<!-- page: 15 -->` marker (exclusive upper bound).
- Optional, with a documented fallback: markers are never required. When present, `#p` resolves to a bounded passage; when absent, a `#p` locator degrades to the audit's first-class `insufficient-locator` verdict (NOT an error) — this nudges authors toward `#sec:`/`#para` locators. Unmarked paginated sources are not errors (D-06).
- Document-now / helper-later (D-07): the curator hand-marks sources today; any auto-insertion helper (e.g. PDF→md page-break detection at ingest) is deferred to a later version. The audit consumes the markers read-only; ingest gains zero new logic.

Use placeholders such as `<source-id>`, `<YYYY-MM-DD-slug>`, and literal `N` in any example. Do NOT use any real vault slug, page title, or term (CLAUDE.md §3 neutrality rule — template-public file).

Mirror (all three files + fixture):
1. Run `bash bin/sync-claude.sh` to byte-copy AGENTS.md → CLAUDE.md (or rely on the pre-commit hook; either way CLAUDE.md must end byte-equal — do NOT hand-edit it).
2. Manually mirror the identical "### Page-marker convention" subsection into `schema/AGENTS.template.md` §6 at the same insertion point (immediately after that file's "### Locator Types" table), preserving its `{{PLACEHOLDER}}` wizard tokens elsewhere in the section. **NEUTRALITY MITIGATION (T-13-12, mandatory):** `bin/check-neutrality.sh` PUBLIC_PATHS does NOT include `schema/`, so the template mirror is NOT covered by the denylist gate. Therefore copy the new "### Page-marker convention" subsection into the template VERBATIM (byte-for-byte) from the already-neutrality-checked AGENTS.md subsection — introduce ZERO schema-only divergence in this subsection. The only legitimate differences anywhere in the template are the pre-existing `{{PLACEHOLDER}}` wizard tokens, and the page-marker subsection MUST contain none of them. The acceptance criteria assert the extracted subsection is byte-identical between AGENTS.md and schema/AGENTS.template.md.
3. **Regenerate the canonical fixture (mandatory — editing the template deterministically breaks the byte-equality gate).** Editing `schema/AGENTS.template.md` invalidates `schema/fixtures/canonical-AGENTS.md`, failing `tests/phase-08/test_canonical_byte_equality.sh` and its wrapper `tests/phase-09.1/test_byte_equality.sh` (and the `setup-parity.yml` CI gate). Regenerate via the wizard render routine — the exact recipe documented inside `test_canonical_byte_equality.sh` (verify the paths/flags against that file before running; they are confirmed current as of planning):

       bash bin/init-wizard.sh --answers-file schema/fixtures/canonical-answers.yaml --render-to /tmp/wz-regen
       cp /tmp/wz-regen/AGENTS.md schema/fixtures/canonical-AGENTS.md

   (No `WIZARD_GENERATED_AT`/`WIZARD_TEMPLATE_SHA` env vars are needed for the AGENTS.md fixture — those values land only in `.wizard-answers.yaml`, which is not part of the fixture file; confirm the rendered `canonical-AGENTS.md` contains no embedded timestamp/SHA before relying on this.)
4. Run `bash bin/check-neutrality.sh` — must exit 0.

Test `test_page_marker_convention.sh`: assert AGENTS.md §6 contains a `<!-- page:` example, the `insufficient-locator` fallback mention, and the `exclusive` slice wording; assert CLAUDE.md and schema/AGENTS.template.md contain the same `<!-- page:` marker text (mirror coverage); assert the extracted "### Page-marker convention" subsection is byte-identical between AGENTS.md and schema/AGENTS.template.md (neutrality-by-verbatim-copy guard for the schema mirror that check-neutrality.sh does not cover). Extract the subsection with the flag-based awk `awk 'f&&/^#{2,3} /{exit} /^### Page-marker convention$/{f=1} f' <file>` (NOT the range form `/start/,/^### |^## /`, which self-closes on the heading and makes the diff a tautology that passes even when bodies diverge).
  </action>
  <verify>
    <automated>grep -q '<!-- page:' AGENTS.md && grep -q '<!-- page:' CLAUDE.md && grep -q '<!-- page:' schema/AGENTS.template.md && bash bin/sync-claude.sh --check && bash bin/check-neutrality.sh && bash tests/phase-08/test_canonical_byte_equality.sh && bash tests/phase-09.1/test_byte_equality.sh && bash tests/phase-13/test_page_marker_convention.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '<!-- page:' AGENTS.md` AND the §6 subsection mentions `insufficient-locator` AND `exclusive` (slice semantics) AND `Obsidian` (invisible property).
    - `bash bin/sync-claude.sh --check` exits 0 (`OK: AGENTS.md == CLAUDE.md`) — CLAUDE.md byte-equal.
    - `grep -q '<!-- page:' schema/AGENTS.template.md` (manual template mirror present).
    - The "### Page-marker convention" subsection extracted from AGENTS.md is byte-identical to the same subsection extracted from schema/AGENTS.template.md (use a flag-based extractor that excludes the heading line from the terminator so the range does not self-close: `diff <(awk 'f&&/^#{2,3} /{exit} /^### Page-marker convention$/{f=1} f' AGENTS.md) <(awk 'f&&/^#{2,3} /{exit} /^### Page-marker convention$/{f=1} f' schema/AGENTS.template.md)` is empty) — schema-mirror neutrality guard (PUBLIC_PATHS excludes schema/). NOTE: do NOT use the awk *range* form `'/^### Page-marker convention$/,/^### |^## /'` — its start line also matches the end pattern, so it emits only the heading and the diff is a tautology that passes even when bodies diverge.
    - `bash bin/check-neutrality.sh` exits 0 (no real vault terms in the AGENTS.md/CLAUDE.md edits).
    - `bash tests/phase-08/test_canonical_byte_equality.sh` exits 0 — the canonical fixture was regenerated and matches the edited template (this is the gate the template edit would otherwise break).
    - `bash tests/phase-09.1/test_byte_equality.sh` exits 0 (the wrapper covering AGENTS.md≡CLAUDE.md + canonical-fixture parity).
    - `bash tests/phase-13/test_page_marker_convention.sh` exits 0.
  </acceptance_criteria>
  <done>The §6 page-marker convention is documented in AGENTS.md with abstract placeholders, byte-mirrored to CLAUDE.md and manually mirrored VERBATIM to schema/AGENTS.template.md, the canonical-AGENTS.md fixture is regenerated so the byte-equality gate stays green, neutrality passes (incl. the schema-mirror verbatim guard), and the slice semantics match what Plan 02's resolve_locator implements.</done>
</task>

<task type="auto">
  <name>Task 2: Document the Audit as a review-only workflow + the D-10 handoff + the D-08 reflect suggestion</name>
  <read_first>
    - AGENTS.md §11 (the Workflows section — §11.1 Ingest / §11.2 Query / §11.3 Lint / §11.4 Reflect / §11.5 Brownfield / §11.6 Release; decide placement — a new §11.x subsection vs folding into reflect/lint, per 13-CONTEXT.md Integration Points)
    - AGENTS.md §11.4 (the Tier-2 `reflect recommended: ...` recommendation pattern — the precedent for the D-08 `audit recommended: ...` note; and the reflect-state.md checkpoint mechanics that audit-state.md mirrors)
    - AGENTS.md §6 (the `[contradiction:source_a vs source_b]` + `[epistemic:: tentative]` markers — the D-10 handoff targets) and §9 (UPDATE/MERGE/SUPERSEDE/ARCHIVE operations)
    - AGENTS.md §1 (the "four operations" framing — Ingest/Query/Lint/Reflect — which this plan PRESERVES; Audit is documented as a workflow, NOT elevated to a 5th operation, per 13-CONTEXT.md Deferred + Integration Points default)
    - .planning/phases/13-claim-faithfulness-audit/13-CONTEXT.md (D-08 cadence on-demand + reflect-tier; D-10 human-approved handoff; the Integration Points note that Audit does NOT add a wiki page type and keeps the four-operation framing)
    - .planning/phases/13-claim-faithfulness-audit/13-RESEARCH.md (Open Q2 — recommended host for the audit-recommended suggestion: emit from the lint workflow where the high-risk signals are already computed, non-binding log note)
  </read_first>
  <action>
Document the Audit in the AGENTS.md workflow surface. Default placement (planner's call, made here): a new `### 11.7 Audit Workflow` subsection (or fold a concise "Audit" procedure into the existing surface) — whichever keeps the four-operation framing intact. Do NOT add a wiki page type. Do NOT elevate Audit to a 5th top-level operation; in §1 keep "four operations" but note the Audit is a review-only diagnostic workflow layered on top (analogous to how Lint is a workflow, not one of the four mutation operations). Document:

1. **What it is:** `bin/audit-claims.sh` is a source-grounded, review-only audit (FAITH-01..04). It samples high-risk claims, resolves each `[prov:]` to the cited RAW source passage, and emits supports/weak/contradicts/insufficient (+ operational insufficient-locator/skipped-privacy/skipped-nontext) findings to `wiki/maintenance/audit-report.md`. It NEVER mutates a wiki page (SC-5), never gates by default, and runs on-demand.
2. **Cadence (D-08):** primary path is operator-invoked. Additionally, the lint workflow MAY emit a non-binding `audit recommended: <reason>` note (the §11.4 Tier-2 recommendation pattern) when high-risk-claim counts cross a threshold — lint already computes the stale/epistemic/orphan signals, so it is the cheapest host (research Open Q2). Document that this note is informational only — it does not run the audit, and the audit is never a CI gate in v1.
3. **Privacy (FAITH-04):** the audit resolves each claim's **EFFECTIVE claim privacy** via §13 — the STRICTEST of {claim-page privacy, source-summary privacy, raw-source privacy, enclosing-dir signal, fail-closed `local_only` default} (REVIEW HIGH-A / MEDIUM 13-04 — NOT "source privacy" alone; the worklist payload carries the wiki page's own claim text, so a `privacy: local_only` page citing a `cloud_safe` source must still be withheld). It withholds `local_only`-effective claims (their claim text AND passage) from BOTH the verifier subprocess AND the `--emit-worklist` stdout (fail-closed; the partition gates every passage-bearing egress surface, not only the subprocess — REVIEW HIGH-2). `local_only` claims emit `skipped-privacy` by default — acceptable, expected UX on a local-heavy vault (D-03); on cloud-facing `--emit-worklist` stdout the withheld claim's `skipped-privacy` metadata (`source_id`/`path`/`locator`) is redacted to a bare aggregate count, with full per-record detail written only to the `privacy: local_only` `audit-report.md` (REVIEW HIGH-C). Document the verifier-locality model explicitly (REVIEW HIGH-1): every `--verifier <cmd>` is treated as cloud/egress by default; a `local_only` passage is admitted ONLY via an explicit `--allow-local` flag (with `--local-verifier <cmd>` documented as sugar for `--verifier <cmd> --allow-local`). State plainly that the audit NEVER infers a verifier's locality from its command — locality is an operator assertion via the flag, never a guess. This is the load-bearing FAITH-04 contract; the script enforces it mechanically (Plan 03), and the docs must not describe a weaker "local verifier auto-detected" behavior.
4. **The contradicts→marker handoff (D-10):** document the connection to existing §6/§9 machinery. The audit stays strictly report-only; a human or agent MAY, as a SEPARATE explicit operation, add `[epistemic:: tentative]` or a `[contradiction:source_a vs source_b]` marker (§6) to a claim with a confirmed `contradicts` verdict, then sync `has_contradictions` per the lint mechanics. Never automatic — the audit produces a finding; a subsequent decision promotes it.
5. **Checkpoint:** `wiki/maintenance/audit-state.md` (control-plane, not indexed) mirrors `reflect-state.md`; advances even on a no-finding run (D-15).

If a `### 11.7` subsection is added, note that §11.6 Release Workflow precedes it (preserve existing ordering; do not renumber existing subsections). Re-run `bash bin/sync-claude.sh` and mirror to `schema/AGENTS.template.md` (same VERBATIM-copy discipline as Task 1 for any template-public prose — the schema mirror is outside check-neutrality.sh PUBLIC_PATHS). Regenerate `schema/fixtures/canonical-AGENTS.md` again (same recipe as Task 1 step 3) since the template changed again. Re-run `bash bin/check-neutrality.sh`.

**MEDIUM 13-04 fixture-regen scoping (verify which):** the "effective claim privacy" wording (item 3) is RENDERED prose inside the §11.7 audit-workflow body — it is NOT an HTML-comment / commentary line, so it lands in the wizard-rendered `canonical-AGENTS.md`. Therefore the fixture regeneration mandated above (the second regen, triggered by the §11.7 audit-workflow template edit) ALREADY covers this wording change — no SEPARATE fixture regen is required for the wording fix on top of the §11.7 regen. CONFIRM this by checking the wording appears in the regenerated `schema/fixtures/canonical-AGENTS.md` after the regen step (`grep -qi 'effective claim privacy' schema/fixtures/canonical-AGENTS.md`); if for any reason the wording were placed only inside an HTML-comment block that the wizard strips, no regen would be needed and that grep would correctly be skipped — but the planned placement is rendered body prose, so the grep MUST pass.

Before adding cross-references, CONFIRM the cited schema sections exist in-repo (LOW concern reconciliation): `grep` for the Privacy Routing (§13) and Workflows (§11) headings in AGENTS.md; the Audit docs reference them, so if a heading text differs, cite the actual heading rather than a bare "§13"/"§11" token.

Test `test_agents_claude_mirror.sh`: assert the Audit workflow text mentions `review-only`, the `audit recommended` reflect-tier note, the `contradicts` handoff, `skipped-privacy`, the explicit verifier-locality model (`allow-local` + cloud-by-default prose — REVIEW HIGH-1), the partitioned `emit-worklist` (REVIEW HIGH-2), AND describes the gate as **effective claim privacy** — the strictest of claim-page / source-summary / raw-source / dir / fail-closed — NOT bare "source privacy" (MEDIUM 13-04: `grep -qi 'effective claim privacy' AGENTS.md` AND the §6/§11 audit prose does NOT say the audit gates on "source privacy" alone); assert CLAUDE.md byte-mirrors (sync-claude --check exit 0) and schema/AGENTS.template.md contains the same Audit-workflow heading; assert the canonical byte-equality gate still passes after the second template edit + fixture regenerate.
  </action>
  <verify>
    <automated>grep -qi 'audit recommended' AGENTS.md && grep -qi 'review-only' AGENTS.md && grep -qi 'allow-local' AGENTS.md && grep -qi 'emit-worklist' AGENTS.md && bash bin/sync-claude.sh --check && grep -qi 'audit' schema/AGENTS.template.md && bash bin/check-neutrality.sh && bash tests/phase-08/test_canonical_byte_equality.sh && bash tests/phase-13/test_agents_claude_mirror.sh</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md documents the Audit workflow: `grep -qi 'review-only' AGENTS.md` AND `grep -qi 'audit recommended' AGENTS.md` (D-08 reflect-tier note) AND `grep -qi 'contradicts' AGENTS.md` near the handoff text AND `grep -qi 'skipped-privacy' AGENTS.md`.
    - The Audit privacy docs state the explicit verifier-locality model (REVIEW HIGH-1) and the worklist egress (REVIEW HIGH-2): `grep -qi 'allow-local' AGENTS.md` AND the prose says verifiers are cloud-by-default / locality is never inferred (`grep -qiE 'cloud.*default|never infer|treated as cloud' AGENTS.md`) AND mentions `--emit-worklist` is partitioned like the verifier (`grep -qi 'emit-worklist' AGENTS.md`).
    - The Audit privacy docs describe the gate as EFFECTIVE CLAIM privacy, not bare "source privacy" (MEDIUM 13-04, mirrors the HIGH-A impl contract): `grep -qi 'effective claim privacy' AGENTS.md` AND the prose names the strictest-of set (`grep -qiE 'strictest of|claim-page.*source-summary.*raw-source|raw-source' AGENTS.md`). The audit-workflow prose must NOT describe the gate as gating on "source privacy" alone. Mirrored to CLAUDE.md (sync) and schema/AGENTS.template.md.
    - The §13 and §11 sections the Audit docs cite exist in AGENTS.md (LOW concern reconciliation): `grep -qiE '^#+.*Privacy Routing|§13' AGENTS.md` AND `grep -qiE '^#+.*Workflows|§11' AGENTS.md` both succeed before adding cross-references.
    - §1 still says "four operations" (the framing is preserved): `grep -qi 'four operations' AGENTS.md`.
    - `bash bin/sync-claude.sh --check` exits 0 (CLAUDE.md byte-equal).
    - `grep -qi 'audit' schema/AGENTS.template.md` (template mirror present).
    - `bash bin/check-neutrality.sh` exits 0.
    - `bash tests/phase-08/test_canonical_byte_equality.sh` exits 0 — fixture regenerated after the second template edit (byte-equality gate stays green).
    - `bash tests/phase-13/test_agents_claude_mirror.sh` exits 0; `bash tests/phase-13/run.sh` exits 0.
  </acceptance_criteria>
  <done>The Audit is documented as a review-only workflow (four-operation framing preserved, no new page type), with the D-08 reflect-tier suggestion (hosted in lint), the D-10 human-approved contradicts→marker handoff, and the D-15 checkpoint; the privacy gate is described as EFFECTIVE CLAIM privacy (strictest of claim-page/source-summary/raw-source/dir/default, NOT bare "source privacy" — MEDIUM 13-04) with the HIGH-C metadata-redaction note; all three mirror files agree, the canonical fixture is regenerated, and neutrality passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author hand-editing template-public files (AGENTS.md/CLAUDE.md/template) | The leak risk is a real vault term (private slug/title) entering a published template file |
| AGENTS.md → CLAUDE.md sync | A drift would publish an inconsistent spec; the hook enforces byte-equality |
| schema/AGENTS.template.md → canonical-AGENTS.md fixture | A template edit without fixture regenerate publishes a red byte-equality gate to the whole repo suite + CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-12 | Information Disclosure | real vault term leaking into a template-public §6/§11 edit | mitigate | Abstract placeholders only at write-time (CLAUDE.md §3 — `<source-id>`, `<YYYY-MM-DD-slug>`, `N`); `bin/check-neutrality.sh` denylist gate as the backstop for AGENTS.md/CLAUDE.md (exits 0). NOTE: PUBLIC_PATHS excludes `schema/`, so the template mirror is NOT covered — mitigated by copying the new subsections VERBATIM from the neutrality-checked AGENTS.md and asserting byte-identity (Task 1 acceptance criterion), so no schema-only term can diverge into the uncovered mirror |
| T-13-13 | Tampering | AGENTS.md ↔ CLAUDE.md drift publishing an inconsistent spec | mitigate | `bin/sync-claude.sh --check` exits 0 in acceptance; `.githooks/pre-commit` auto-syncs + re-stages on drift; do not hand-edit CLAUDE.md |
| T-13-15 | Tampering | template edit leaving schema/fixtures/canonical-AGENTS.md stale → repo suite + setup-parity CI red | mitigate | Mandatory fixture regenerate in Task 1/2 (`bin/init-wizard.sh … && cp … canonical-AGENTS.md`); `tests/phase-08/test_canonical_byte_equality.sh` + `tests/phase-09.1/test_byte_equality.sh` are IN this plan's own verify gate and acceptance criteria |
| T-13-14 | Repudiation | the documented `#p` slice semantics diverging from the implemented resolver | accept | Both this plan and Plan 02 cite 13-CONTEXT.md D-05 as the single source; a divergence would surface in `test_resolve_p_marked.sh` (Plan 02) failing — caught by the suite |
</threat_model>

<verification>
- `bash bin/sync-claude.sh --check` exits 0 (CLAUDE.md byte-equal to AGENTS.md).
- `bash bin/check-neutrality.sh` exits 0; the §6 page-marker subsection is byte-identical between AGENTS.md and schema/AGENTS.template.md (schema-mirror neutrality guard, since PUBLIC_PATHS excludes schema/).
- `bash tests/phase-08/test_canonical_byte_equality.sh` and `bash tests/phase-09.1/test_byte_equality.sh` exit 0 (canonical fixture regenerated after the template edits).
- `bash tests/phase-13/test_page_marker_convention.sh` and `test_agents_claude_mirror.sh` pass.
- The §6 `#p` slice semantics documented here match `resolve_locator` in Plan 02 (both cite D-05).
</verification>

<success_criteria>
- The optional `<!-- page: N -->` convention (D-04/D-05/D-06/D-07) is documented in §6 across all three mirror files with abstract placeholders, additive and dependency-free, with the insufficient-locator fallback intact.
- The Audit is framed as a review-only workflow (four-operation model preserved, no new page type) with the D-08 reflect-tier suggestion, the D-10 human-approved handoff, and the D-15 checkpoint.
- The audit privacy gate is documented as EFFECTIVE CLAIM privacy (strictest of claim-page / source-summary / raw-source / dir / fail-closed default — NOT bare "source privacy"; MEDIUM 13-04 mirrors the Plan 03 HIGH-A contract), with the HIGH-C local-only skipped-privacy metadata-redaction note; the wording is mirrored byte-equal to CLAUDE.md and into schema/AGENTS.template.md, and the canonical fixture is regenerated only if the wording lands in rendered template body (verify per the no-fixture-regen-for-commentary note below).
- Neutrality and the AGENTS↔CLAUDE byte-equality both hold; the schema-mirror page-marker subsection is verbatim-identical to AGENTS.md (covering the schema/ gap in check-neutrality.sh).
- The canonical-AGENTS.md fixture is regenerated so the repo-wide byte-equality gate (phase-08 + phase-09.1 + setup-parity CI) stays green.
</success_criteria>

<output>
After completion, create `.planning/phases/13-claim-faithfulness-audit/13-04-SUMMARY.md`.
</output>
</output>
