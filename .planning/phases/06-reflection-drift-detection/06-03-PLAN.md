---
phase: 06-reflection-drift-detection
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - AGENTS.md
  - wiki/maintenance/reflect-state.md
autonomous: false
requirements: [DCSN-03]

must_haves:
  truths:
    - "AGENTS.md section 11.4 documents the three-tier reflect model (inline creation, workflow recommendations, periodic reflect)"
    - "AGENTS.md section 9 includes inline decision record creation hooks for MERGE and SUPERSEDE operations"
    - "AGENTS.md section 11.3 includes drift detection steps in the lint workflow"
    - "A reflect checkpoint state file exists at wiki/maintenance/reflect-state.md with machine-readable fields"
    - "The periodic reflect procedure is documented as a deterministic sequence: read checkpoint, scan log, inspect git, create records, advance checkpoint"
  artifacts:
    - path: "AGENTS.md"
      provides: "Complete reflect workflow documentation with three-tier model"
      contains: "Three-Tier Reflect Model"
    - path: "wiki/maintenance/reflect-state.md"
      provides: "Reflect checkpoint state file"
      contains: "last_reflect_log_entry"
  key_links:
    - from: "AGENTS.md section 11.4"
      to: "AGENTS.md section 9"
      via: "inline creation hooks reference"
      pattern: "Section 9.*inline"
    - from: "AGENTS.md section 11.4"
      to: "wiki/maintenance/reflect-state.md"
      via: "checkpoint state file reference"
      pattern: "reflect-state.md"
    - from: "AGENTS.md section 11.3"
      to: "bin/lint.sh"
      via: "drift check steps"
      pattern: "drift"
---

<objective>
Rewrite AGENTS.md section 11.4 (Reflect Workflow) with the three-tier model, add inline decision record creation hooks to section 9 (MERGE/SUPERSEDE), add drift detection steps to section 11.3 (Lint Workflow), and create the reflect checkpoint state file. Human verification at end.

Purpose: The reflect workflow is the mechanism that creates decision records. This plan documents when and how they are created (inline during operations, on recommendation from workflows, or during periodic sweeps), and integrates drift detection into the lint workflow documentation.
Output: Expanded AGENTS.md sections 9, 11.3, 11.4; reflect-state.md checkpoint file
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-reflection-drift-detection/06-CONTEXT.md
@.planning/phases/06-reflection-drift-detection/06-RESEARCH.md
@.planning/phases/06-reflection-drift-detection/06-01-SUMMARY.md
@.planning/phases/06-reflection-drift-detection/06-02-SUMMARY.md

<interfaces>
<!-- From AGENTS.md section 11.4 (current, to be replaced) -->
Current: 6-step skeleton, decision records stored as overview pages in wiki/overviews/
New: Three-tier model (D-06), both log.md and git log (D-07), explicit checkpoint (D-08), periodic procedure (D-09)

<!-- From AGENTS.md section 9 (to be extended) -->
MERGE step 7 currently: Log entry
SUPERSEDE step 6 currently: Log entry
Both need: "7a. If this operation warrants a decision record (see Section 11.4 trigger types), create one inline as part of this commit."

<!-- From AGENTS.md section 11.3 (to be extended with drift steps) -->
Current steps: 1-10 (checks) + 11 (compile report) + 12 (log entry) + 13 (commit)
New: Insert drift checks as step 11 (renumber compile/log/commit to 12/13/14)
Steps for drift: source/wiki mismatch, missing source files, content-hash drift, index coverage, vault awareness

<!-- From wiki/maintenance/ — existing maintenance files pattern -->
lint-report.md lives here. reflect-state.md joins it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite AGENTS.md section 11.4 (Reflect Workflow) with three-tier model, extend sections 9 and 11.3</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md lines 1247-1268 (current section 11.4 — full text to replace)
    - AGENTS.md lines 858-959 (section 9 — MERGE and SUPERSEDE operation definitions, need insertion points)
    - AGENTS.md lines 1204-1246 (section 11.3 — lint workflow steps, need drift step insertion)
    - .planning/phases/06-reflection-drift-detection/06-CONTEXT.md (D-06 through D-09, D-16 through D-21)
    - .planning/phases/06-reflection-drift-detection/06-RESEARCH.md lines 136-170 (three-tier model and checkpoint mechanism)
  </read_first>
  <action>
    **A. Rewrite AGENTS.md Section 11.4 — Reflect Workflow**

    Replace the entire section 11.4 (lines 1247-1268) with the following expanded content:

    ```markdown
    ### 11.4 Reflect Workflow

    The reflect workflow creates decision records (see Section 4.6) that capture why structural changes were made to the wiki. It operates through three tiers, from automatic to manual.

    ```
    Trigger:  After structural operations, on workflow recommendation, or periodically
    Inputs:   Recent changes (from log.md and git history), reflect checkpoint state
    Outputs:  Decision record page(s) in wiki/decisions/, updated index/log, advanced checkpoint
    Commit:   reflect(<scope>): <one-line summary>
    ```

    #### Three-Tier Reflect Model

    **Tier 1 — Inline creation:** MERGE, SUPERSEDE, splits, domain reorganization, schema updates, and recognized reframings produce decision records as part of the operation commit. No separate reflect pass needed. The agent creating the structural change also creates the decision record in the same commit. See Section 9 operation definitions for inline hooks.

    **Tier 2 — Workflow recommendations:** Ingest, query, and lint workflows emit a structured recommendation when they detect ambiguous signals that may warrant a decision record but require judgment:

    ```
    reflect recommended: [trigger_type] -- [reason]
    ```

    Signals that trigger recommendations:
    - Material framing shifts during ingest (a new source substantially reframes an existing concept)
    - Contradiction resolution choices during query write-back
    - Novel synthesis frames created during query compilation
    - Accumulated structural drift detected during lint (multiple related drift findings)

    This is a log message appended to the workflow's log entry, not an automatic action. The agent or human decides whether to act on it in a subsequent reflect pass.

    **Tier 3 — Manual/periodic reflect:** A safety-net pass that scans recent activity and backfills missed decision records. Run periodically (e.g., after several ingests) or when the operator suspects structural decisions went unrecorded.

    #### Reflect Checkpoint

    The reflect checkpoint lives at `wiki/maintenance/reflect-state.md`. It tracks where the last reflect pass ended so subsequent passes resume from the correct position, even when a pass produces no decision records.

    Fields (in frontmatter):
    - `last_reflect_log_entry`: The full heading line of the last log entry scanned (e.g., `"## [2026-04-14] lint | wiki health check"`)
    - `last_reflect_commit`: The short SHA of the last git commit inspected
    - `last_reflect_at`: ISO 8601 date of the last reflect pass

    #### Periodic Reflect Procedure (Tier 3)

    1. Read the reflect checkpoint from `wiki/maintenance/reflect-state.md`.
    2. Scan `wiki/log.md` for entries after `last_reflect_log_entry`. Identify structural operations (MERGE, SUPERSEDE, ARCHIVE, schema changes) and workflow recommendations (`reflect recommended:` lines).
    3. Inspect `git log --oneline` for commits after `last_reflect_commit`. Look for structural file changes: new/deleted/renamed pages, template modifications, AGENTS.md updates, directory reorganizations.
    4. For each identified structural change that lacks a corresponding decision record: create a decision record page in `wiki/decisions/` using the decision template (Section 4.6). Set `trigger_type` to the most appropriate value. Set `affected_pages` to the IDs of pages touched by the change. Add the decision record ID to `decision_history` on each affected page's frontmatter (if meaningful).
    5. Update `wiki/index.md` with any new decision record entries under the Decisions category.
    6. Append entry to `wiki/log.md`: `## [YYYY-MM-DD] reflect | <scope>` with a summary of how many decision records were created and what they cover.
    7. Advance the reflect checkpoint: update `last_reflect_log_entry` to the most recent log entry, `last_reflect_commit` to HEAD, `last_reflect_at` to today's date. **A reflect run that produces no decision records still advances the checkpoint.**
    8. Commit: `reflect(<scope>): <one-line summary>`

    #### Abort Conditions

    - No structural changes detected since the last checkpoint AND no pending workflow recommendations. Advance the checkpoint (step 7) and skip record creation. Log: `## [YYYY-MM-DD] reflect | no structural changes detected`.

    #### Unifying Principle

    Create a decision record when future-you would reasonably ask "why is the wiki shaped this way?" When in doubt, record. A redundant record is retrievable; a missing record is lost context.
    ```

    **B. Extend AGENTS.md Section 9 — Inline Decision Record Hooks**

    In the MERGE operation definition, after step 7 ("Log: ..."), add step 7a:

    ```
    7a. **Decision record (inline):** If this merge represents a significant structural choice (not a trivial cleanup), create a decision record page in `wiki/decisions/` with `trigger_type: merge` and `affected_pages` listing both merged page IDs and the new page ID. Add the new decision record to `wiki/index.md` under Decisions. This is Tier 1 of the reflect model (Section 11.4) — the record is part of this commit, not a separate pass.
    ```

    In the SUPERSEDE operation definition, after step 6 ("Log: ..."), add step 6a:

    ```
    6a. **Decision record (inline):** If this supersession replaces a key page or represents a significant editorial judgment (not a routine stale-claim update), create a decision record page in `wiki/decisions/` with `trigger_type: reframing` (if the new page reframes the concept) or `trigger_type: merge` (if consolidating). Set `affected_pages` to include both old and new page IDs. Add to `wiki/index.md` under Decisions. This is Tier 1 of the reflect model (Section 11.4).
    ```

    **C. Extend AGENTS.md Section 11.3 — Drift Detection Steps**

    In the lint workflow steps, insert a new step 11 after step 10 (source coverage gaps) and renumber the remaining steps (compile report becomes 12, log entry becomes 13, commit becomes 14):

    ```
    11. **Drift detection:** Run cross-system drift checks. These detect misalignment between the wiki layer and its dependencies. Category: `drift`.
        - **Unrepresented sources (DRFT-01):** Walk `sources/` directory for `.md` files, check each has a corresponding wiki source summary page (matching the `path` field). Severity: warning.
        - **Missing source files (DRFT-02):** For each source summary page, verify the raw source file at the `path` field exists on disk. Severity: error.
        - **Content-hash drift:** Recompute SHA-256 of the raw source file at `path`, compare against `content_hash` in source summary frontmatter. If mismatch: report drift finding (severity: warning) and auto-fix `compilation_status` to `stale`. See Section 10 compilation status transitions.
        - **Index coverage:** Verify every wiki page (excluding index.md, log.md, maintenance pages) has a wikilink entry in `wiki/index.md`. Severity: warning.
        - **Obsidian vault awareness (DRFT-03):** Verify `.obsidian/` directory exists (info if missing). Check for non-markdown files in `wiki/` that may cause Obsidian issues (severity: info).
    ```

    Also update the --category documentation in the lint workflow intro to include `drift`:
    After the existing categories list, ensure `drift` is documented as a valid --category value.

    Renumber existing step 11 (compile report) to step 12, step 12 (log entry) to step 13, step 13 (commit) to step 14.

    In the renumbered step 12 (compile report), update:
    "organized by severity then category" — this is already correct and covers drift findings naturally.
  </action>
  <verify>
    <automated>grep -c "Three-Tier Reflect Model" AGENTS.md && grep -c "Tier 1" AGENTS.md && grep -c "reflect-state.md" AGENTS.md && grep -c "7a\." AGENTS.md && grep -c "6a\." AGENTS.md && grep -c "DRFT-01" AGENTS.md && grep -c "Periodic Reflect Procedure" AGENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md section 11.4 contains `#### Three-Tier Reflect Model`
    - AGENTS.md section 11.4 contains `**Tier 1 — Inline creation:**`
    - AGENTS.md section 11.4 contains `**Tier 2 — Workflow recommendations:**`
    - AGENTS.md section 11.4 contains `**Tier 3 — Manual/periodic reflect:**`
    - AGENTS.md section 11.4 contains `#### Reflect Checkpoint`
    - AGENTS.md section 11.4 contains `wiki/maintenance/reflect-state.md`
    - AGENTS.md section 11.4 contains `#### Periodic Reflect Procedure (Tier 3)` with 8 numbered steps
    - AGENTS.md section 11.4 contains "A reflect run that produces no decision records still advances the checkpoint"
    - AGENTS.md section 9 MERGE definition contains step `7a.` with `trigger_type: merge`
    - AGENTS.md section 9 SUPERSEDE definition contains step `6a.` with decision record creation
    - AGENTS.md section 11.3 contains step 11 with `Drift detection` heading
    - AGENTS.md section 11.3 step 11 mentions DRFT-01, DRFT-02, DRFT-03
    - AGENTS.md section 11.3 steps renumbered: compile report is step 12, log entry is step 13, commit is step 14
    - AGENTS.md section 11.3 lists `drift` as a valid --category value
  </acceptance_criteria>
  <done>AGENTS.md fully documents the three-tier reflect model in section 11.4, inline decision record hooks in section 9 for MERGE and SUPERSEDE, and drift detection as lint step 11 in section 11.3.</done>
</task>

<task type="auto">
  <name>Task 2: Create reflect checkpoint state file</name>
  <files>wiki/maintenance/reflect-state.md</files>
  <read_first>
    - wiki/maintenance/lint-report.md (lines 1-20, established maintenance file pattern)
    - .planning/phases/06-reflection-drift-detection/06-RESEARCH.md lines 149-170 (checkpoint mechanism pattern)
    - .planning/phases/06-reflection-drift-detection/06-CONTEXT.md (D-08, D-09)
  </read_first>
  <action>
    Create `wiki/maintenance/reflect-state.md` — the reflect checkpoint state file per D-08.

    This file uses AGENTS.md section 5 compliant frontmatter (type: overview since it's a maintenance/control-plane file, consistent with lint-report.md). The frontmatter includes the three checkpoint fields plus standard base fields.

    Content:
    ```yaml
    ---
    id: reflect-state
    title: Reflect State
    type: overview
    status: active
    summary: "Checkpoint state for periodic reflect workflow (AGENTS.md section 11.4)."
    created_at: 2026-04-14
    updated_at: 2026-04-14
    sources: []
    epistemic_status: sourced
    tags:
      - meta
      - maintenance
    domains: []
    supersedes:
    superseded_by:
    privacy: cloud_safe
    aliases:
      - Reflect State
    has_contradictions: false
    knowledge_domain: ""
    last_reflect_log_entry: ""
    last_reflect_commit: ""
    last_reflect_at: 2026-04-14
    ---

    # Reflect State

    Control-plane checkpoint for the periodic reflect workflow (AGENTS.md section 11.4).
    This file tracks where the last reflect pass ended so subsequent passes resume from the correct position.

    ## Fields

    - **last_reflect_log_entry:** The full heading line of the last log.md entry scanned. Empty string means no prior reflect pass.
    - **last_reflect_commit:** The short SHA of the last git commit inspected. Empty string means scan from beginning.
    - **last_reflect_at:** ISO 8601 date of the last reflect pass.

    ## Usage

    Read this file at the start of each periodic reflect pass (Tier 3). After completing the pass — whether or not decision records were created — update all three fields and commit.
    ```

    Set `last_reflect_at` to today's date (2026-04-14) as the initial checkpoint. Leave `last_reflect_log_entry` and `last_reflect_commit` as empty strings to indicate no prior reflect pass has run.
  </action>
  <verify>
    <automated>test -f wiki/maintenance/reflect-state.md && grep -c "last_reflect_log_entry" wiki/maintenance/reflect-state.md && grep -c "last_reflect_commit" wiki/maintenance/reflect-state.md && grep -c "last_reflect_at" wiki/maintenance/reflect-state.md</automated>
  </verify>
  <acceptance_criteria>
    - wiki/maintenance/reflect-state.md exists
    - File contains valid YAML frontmatter with `id: reflect-state`
    - File contains `type: overview` (consistent with lint-report.md)
    - File contains `last_reflect_log_entry: ""`
    - File contains `last_reflect_commit: ""`
    - File contains `last_reflect_at: 2026-04-14`
    - File contains all 16 base frontmatter fields
    - File body explains the three checkpoint fields and usage
    - `bin/lint.sh --dry-run --category yaml wiki/` reports no errors for this file
  </acceptance_criteria>
  <done>Reflect checkpoint state file exists at wiki/maintenance/reflect-state.md with machine-readable checkpoint fields and AGENTS.md-compliant frontmatter.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of Phase 6 deliverables</name>
  <what-built>
    Complete Phase 6 deliverables across all three plans:
    1. Decision record page type (template, example page, AGENTS.md sections 4.6/5/12, index category)
    2. Drift detection integrated into bin/lint.sh (5 checks, category-labeled report, --category drift)
    3. Three-tier reflect workflow documented in AGENTS.md section 11.4, inline hooks in section 9, drift steps in section 11.3
    4. Reflect checkpoint state file at wiki/maintenance/reflect-state.md
  </what-built>
  <how-to-verify>
    1. Run `bin/lint.sh --dry-run wiki/` — should complete with 0 errors. Any warnings/info are expected drift findings.
    2. Run `bin/lint.sh --dry-run --category drift wiki/` — should show drift-specific findings only.
    3. Check `wiki/decisions/dr-2026-04-14-phase6-decision-type.md` — should have complete content with all 7 sections.
    4. Check `wiki/index.md` — should have a "## Decisions" category with the example record.
    5. Open AGENTS.md — verify section 4.6 (Decision page type), section 5 (decision in type enum and validation checklist), section 9 (steps 7a and 6a), section 11.3 (step 11 drift checks), section 11.4 (three-tier model with checkpoint).
    6. Check `wiki/maintenance/reflect-state.md` — should have checkpoint fields in frontmatter.
    7. Optionally: Open vault in Obsidian and verify the decision record appears in graph view linked via wikilinks.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `bin/lint.sh --dry-run wiki/` exits 0 with no error-level findings on Phase 6 files
- `grep "Three-Tier Reflect Model" AGENTS.md` returns a match
- `grep "7a\." AGENTS.md` returns a match (inline MERGE hook)
- `grep "DRFT-01" AGENTS.md` returns a match (drift in lint workflow)
- `test -f wiki/maintenance/reflect-state.md` succeeds
- All 7 requirements (DCSN-01 through DCSN-03, DRFT-01 through DRFT-04) have corresponding implementations
</verification>

<success_criteria>
The reflect workflow is fully documented with three-tier model (inline, recommendations, periodic), inline hooks exist in MERGE and SUPERSEDE operations, drift detection is documented in the lint workflow, and the reflect checkpoint enables deterministic periodic passes. An agent reading AGENTS.md knows exactly when and how to create decision records and run drift checks.
</success_criteria>

<output>
After completion, create `.planning/phases/06-reflection-drift-detection/06-03-SUMMARY.md`
</output>
