---
phase: 06-reflection-drift-detection
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - AGENTS.md
  - wiki/maintenance/reflect-state.md
autonomous: false
requirements: [DCSN-03]

must_haves:
  truths:
    - "AGENTS.md section 11.4 documents the three-tier reflect model (inline creation, workflow recommendations, periodic reflect)"
    - "AGENTS.md section 9 includes inline decision record creation hooks for MERGE and SUPERSEDE operations with explicit trigger criteria"
    - "AGENTS.md section 11.3 includes drift detection steps in the lint workflow"
    - "A reflect checkpoint state file exists at wiki/maintenance/reflect-state.md with machine-readable fields"
    - "The periodic reflect procedure is documented as a deterministic sequence: read checkpoint, scan log, inspect git, create records, advance checkpoint"
    - "Deduplication rules between log.md and git log are documented"
  artifacts:
    - path: "AGENTS.md"
      provides: "Complete reflect workflow documentation with three-tier model"
      contains: "Three-Tier Reflect Model"
    - path: "wiki/maintenance/reflect-state.md"
      provides: "Reflect checkpoint state file"
      contains: "last_reflect_log_entry"
  key_links:
    - from: "AGENTS.md section 11.4"
      to: "AGENTS.md section 9"
      via: "inline creation hooks reference"
      pattern: "Section 9"
    - from: "AGENTS.md section 11.4"
      to: "AGENTS.md section 4.6"
      via: "decision template reference"
      pattern: "Section 4.6"
    - from: "AGENTS.md section 11.4"
      to: "wiki/maintenance/reflect-state.md"
      via: "checkpoint state file reference"
      pattern: "reflect-state.md"
    - from: "AGENTS.md section 11.3"
      to: "bin/lint.sh"
      via: "drift check steps"
      pattern: "drift"
---

<objective>
Rewrite AGENTS.md section 11.4 (Reflect Workflow) with the three-tier model, add inline decision record creation hooks to section 9 (MERGE/SUPERSEDE) with explicit trigger criteria, add drift detection steps to section 11.3 (Lint Workflow), create the reflect checkpoint state file, and human-verify all Phase 6 deliverables.

Purpose: The reflect workflow is the mechanism that creates decision records. This plan documents when and how they are created (inline during operations, on recommendation from workflows, or during periodic sweeps), specifies the operational execution path with concrete trigger criteria (not just documentation), integrates drift detection into the lint workflow documentation, and verifies all Phase 6 deliverables.
Output: Expanded AGENTS.md sections 9, 11.3, 11.4; reflect-state.md checkpoint file
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-reflection-drift-detection/06-CONTEXT.md
@.planning/phases/06-reflection-drift-detection/06-RESEARCH.md
@.planning/phases/06-reflection-drift-detection/06-01-SUMMARY.md

<interfaces>
<!-- From AGENTS.md section 11.4 (current, to be replaced) -->
Current: 6-step skeleton, decision records stored as overview pages in wiki/overviews/
New: Three-tier model (D-06), both log.md and git log (D-07), explicit checkpoint (D-08), periodic procedure (D-09)

<!-- From AGENTS.md section 9 (to be extended) -->
MERGE currently ends with: Log entry step
SUPERSEDE currently ends with: Log entry step
Both need inline decision record creation hooks with EXPLICIT TRIGGER CRITERIA:
- MERGE: "if this merge represents a significant structural choice (not a trivial cleanup)"
- SUPERSEDE: "if this supersession replaces a key page or represents a significant editorial judgment"
These criteria address the review concern that "not every merge or supersede necessarily warrants a decision record."

<!-- From AGENTS.md section 11.3 (to be extended with drift steps) -->
Current steps: 1-10 (checks) + 11 (compile report) + 12 (log entry) + 13 (commit)
New: Insert drift checks as step 11 (renumber compile/log/commit to 12/13/14)

<!-- From AGENTS.md section 4.6 (created in Plan 01) -->
The decision page type, trigger_types, and section structure are already documented.
Section 11.4 references Section 4.6 for the template, not duplicating it.

<!-- Deduplication rule (addresses review concern about log.md vs git log overlap): -->
log.md is authoritative for workflow INTENT (what operation was attempted and why).
git log is authoritative for actual FILE CHANGES (what files were added/modified/deleted).
If both show the same structural event, the log.md entry is the primary trigger for decision record creation.
If git shows file changes with no log entry (e.g., manual edit), that is a signal of unrecorded structural work.
If log shows an operation but git shows no corresponding file change, it was a no-op and does not warrant a record.

<!-- wiki/maintenance/ directory -->
Already sanctioned: lint-report.md lives here. reflect-state.md joins it.
Maintenance pages are EXCLUDED from index.md (they are control-plane, not content).
This is already the pattern: lint-report.md is not in the index either.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite AGENTS.md section 11.4 (Reflect Workflow) with three-tier model, extend sections 9 and 11.3</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md lines 1247-1268 (current section 11.4 -- full text to replace)
    - AGENTS.md lines 858-959 (section 9 -- MERGE and SUPERSEDE operation definitions, need insertion points for steps 7a and 6a)
    - AGENTS.md lines 1204-1246 (section 11.3 -- lint workflow steps, need drift step insertion point after step 10)
    - AGENTS.md lines 123-180 (section 4.6 -- decision page type already documented by Plan 01, to reference not duplicate)
    - .planning/phases/06-reflection-drift-detection/06-CONTEXT.md (D-06 through D-09, D-16 through D-21)
    - .planning/phases/06-reflection-drift-detection/06-RESEARCH.md lines 136-170 (three-tier model and checkpoint mechanism)
  </read_first>
  <action>
    **A. Rewrite AGENTS.md Section 11.4 -- Reflect Workflow**

    Replace the entire section 11.4 content (lines 1247-1268) with the following expanded content. The old content references overview pages and a 6-step skeleton -- both are replaced.

    ```markdown
    ### 11.4 Reflect Workflow

    The reflect workflow creates decision records (see Section 4.6) that capture why structural changes were made to the wiki. It operates through three tiers, from automatic to manual.

    ```
    Trigger:  After structural operations, on workflow recommendation, or periodically
    Inputs:   Recent changes (from log.md and git history), reflect checkpoint state
    Outputs:  Decision record page(s) in wiki/decisions/, updated index/log, advanced checkpoint
    Commit:   reflect(<scope>): <one-line summary>
    ```

    #### Three-Tier Reflect Model

    **Tier 1 -- Inline creation:** MERGE, SUPERSEDE, splits, domain reorganization, schema updates, and recognized reframings produce decision records as part of the operation commit. No separate reflect pass needed. The agent creating the structural change also creates the decision record in the same commit. See Section 9 operation definitions for inline hooks (steps 7a and 6a).

    Inline creation is for operations where the "why" is obvious because the agent is actively making the structural choice. The decision record is a natural byproduct, not extra work.

    **Tier 2 -- Workflow recommendations:** Ingest, query, and lint workflows emit a structured recommendation when they detect ambiguous signals that may warrant a decision record but require judgment:

    ```
    reflect recommended: [trigger_type] -- [reason]
    ```

    Signals that trigger recommendations:
    - Material framing shifts during ingest (a new source substantially reframes an existing concept)
    - Contradiction resolution choices during query write-back (choosing one framing over another)
    - Novel synthesis frames created during query compilation (new overview page creates a novel organizing principle)
    - Accumulated structural drift detected during lint (3+ related drift findings suggest a systemic issue)

    This message is appended to the workflow's log entry in `wiki/log.md`. It is NOT an automatic action. The agent or human decides whether to act on it in a subsequent reflect pass or immediately.

    **Tier 3 -- Manual/periodic reflect:** A safety-net pass that scans recent activity and backfills missed decision records. Run periodically (e.g., after several ingests or a batch of structural changes) or when the operator suspects structural decisions went unrecorded.

    #### Reflect Checkpoint

    The reflect checkpoint lives at `wiki/maintenance/reflect-state.md`. It tracks where the last reflect pass ended so subsequent passes resume from the correct position, even when a pass produces no decision records.

    Fields (in frontmatter):
    - `last_reflect_log_entry`: The full heading line of the last log entry scanned (e.g., `"## [2026-04-14] lint | wiki health check"`)
    - `last_reflect_commit`: The short SHA of the last git commit inspected (e.g., `"abc1234"`)
    - `last_reflect_at`: ISO 8601 date of the last reflect pass (e.g., `2026-04-14`)

    `wiki/maintenance/` is a control-plane directory. Files here (lint-report.md, reflect-state.md) are NOT listed in wiki/index.md -- they are infrastructure, not content.

    #### Periodic Reflect Procedure (Tier 3)

    1. Read the reflect checkpoint from `wiki/maintenance/reflect-state.md`.
    2. Scan `wiki/log.md` for entries after `last_reflect_log_entry`. Identify:
       - Structural operations: MERGE, SUPERSEDE, ARCHIVE entries
       - Schema changes: entries referencing AGENTS.md modifications
       - Workflow recommendations: lines matching `reflect recommended: [trigger_type] -- [reason]`
    3. Inspect `git log --oneline` for commits after `last_reflect_commit`. Look for structural file changes: new/deleted/renamed pages, template modifications, AGENTS.md updates, directory reorganizations. **Deduplication rule:** If both log.md and git show the same event, use the log.md entry as the primary trigger (it has intent). Git-only changes (no log entry) indicate unrecorded structural work and should be investigated.
    4. For each identified structural change that lacks a corresponding decision record:
       a. Create a decision record page in `wiki/decisions/` using the decision template (Section 4.6).
       b. Set `trigger_type` to the most appropriate value from the six allowed types.
       c. Set `affected_pages` to the IDs of pages touched by the change.
       d. Fill all 7 required sections with real content (not placeholders). The "Why" section must state what framing was adopted and what it replaced. "Alternatives Considered" must list at least one alternative.
       e. Add the decision record ID to `decision_history` on each affected page's frontmatter (only when meaningful per D-05).
    5. Update `wiki/index.md` with new decision record entries under the Decisions category.
    6. Append entry to `wiki/log.md`: `## [YYYY-MM-DD] reflect | <scope>` with a summary of how many decision records were created, or "no structural changes detected" if none.
    7. Advance the reflect checkpoint: update `last_reflect_log_entry` to the most recent log entry heading, `last_reflect_commit` to current HEAD short SHA, `last_reflect_at` to today's date. **A reflect run that produces no decision records still advances the checkpoint.**
    8. Commit: `reflect(<scope>): <one-line summary>`

    #### Abort Conditions

    - No structural changes detected since the last checkpoint AND no pending workflow recommendations. Advance the checkpoint (step 7) and skip record creation. Log: `## [YYYY-MM-DD] reflect | no structural changes detected`.
    - Log entry for a structural operation already has a corresponding decision record in `wiki/decisions/` (check by date + scope match). Skip that event -- already recorded.

    #### Unifying Principle

    Create a decision record when future-you would reasonably ask "why is the wiki shaped this way?" When in doubt, record. A redundant record is retrievable; a missing record is lost context.
    ```

    **B. Extend AGENTS.md Section 9 -- Inline Decision Record Hooks**

    In the MERGE operation definition, find the last numbered step (the "Log: ..." step). Add step 7a immediately after:

    ```
    7a. **Decision record (inline -- Tier 1):** If this merge represents a significant structural choice -- combining two established pages, resolving a long-standing organizational ambiguity, or eliminating a redundant page that multiple other pages linked to -- create a decision record page in `wiki/decisions/` with `trigger_type: merge` and `affected_pages` listing both original page IDs and the new merged page ID. Add the new decision record to `wiki/index.md` under Decisions. Commit the decision record as part of this same commit. **Skip for trivial cleanup merges** (e.g., merging a stub into its parent when the stub has no unique claims). See Section 11.4, Tier 1.
    ```

    In the SUPERSEDE operation definition, find the last numbered step (the "Log: ..." step). Add step 6a immediately after:

    ```
    6a. **Decision record (inline -- Tier 1):** If this supersession replaces a key page or represents a significant editorial judgment -- the new page substantially reframes the concept, or the superseded page was widely linked -- create a decision record page in `wiki/decisions/` with `trigger_type: reframing` (if the new page reframes the concept) or `trigger_type: merge` (if consolidating). Set `affected_pages` to include both old and new page IDs. Add to `wiki/index.md` under Decisions. Commit as part of this same commit. **Skip for routine stale-claim supersessions** (e.g., updating a fact to a newer version without reframing). See Section 11.4, Tier 1.
    ```

    **C. Extend AGENTS.md Section 11.3 -- Drift Detection Steps**

    In the lint workflow steps, insert a new step 11 after step 10 (source coverage gaps). Renumber the remaining steps:
    - Current step 11 (compile report) becomes step 12
    - Current step 12 (log entry) becomes step 13
    - Current step 13 (commit) becomes step 14

    New step 11:
    ```
    11. **Drift detection (category: `drift`):** Run cross-system drift checks. These detect misalignment between the wiki layer and its dependencies.
        - **Unrepresented sources (DRFT-01):** Walk `sources/` directory for `.md` files, check each has a corresponding wiki source summary page (matching the `path` field in source page frontmatter). Severity: warning.
        - **Missing source files (DRFT-02):** For each source summary page, verify the raw source file at the `path` frontmatter field exists on disk. Severity: error.
        - **Content-hash drift:** Recompute SHA-256 of the raw source file, compare against `content_hash` in source summary frontmatter. If mismatch: report finding (severity: warning). When `--fix` is passed, auto-fix `compilation_status` to `stale` on the affected source page. See Section 10 compilation status transitions.
        - **Index coverage:** Verify every wiki page (excluding index.md, log.md, and maintenance/ pages) has a wikilink entry in `wiki/index.md`. Severity: warning.
        - **Obsidian vault awareness (DRFT-03):** Verify `.obsidian/` directory exists (info if missing). Check for non-markdown files in `wiki/` subdirectories (severity: info).
    ```

    Also update the --category documentation in the lint workflow intro paragraph or step list to include `drift` as a valid category alongside `orphan`, `crossref`, `stale`, `contradiction`, `gap`, `provenance`, `yaml`.

    In the renumbered step 12 (compile report), ensure the description mentions that findings are organized by severity with category subsections (e.g., "### Drift" within "## Warnings").
  </action>
  <verify>
    <automated>grep -c "Three-Tier Reflect Model" AGENTS.md && grep -c "Tier 1 -- Inline" AGENTS.md && grep -c "Tier 2 -- Workflow" AGENTS.md && grep -c "Tier 3 -- Manual" AGENTS.md && grep -c "reflect-state.md" AGENTS.md && grep -c "7a\." AGENTS.md && grep -c "6a\." AGENTS.md && grep -c "DRFT-01" AGENTS.md && grep -c "Periodic Reflect Procedure" AGENTS.md && grep -c "Deduplication rule" AGENTS.md && grep "Skip for trivial" AGENTS.md | head -1 && grep "Skip for routine" AGENTS.md | head -1</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md section 11.4 contains `#### Three-Tier Reflect Model`
    - AGENTS.md section 11.4 contains `**Tier 1 -- Inline creation:**`
    - AGENTS.md section 11.4 contains `**Tier 2 -- Workflow recommendations:**`
    - AGENTS.md section 11.4 contains `**Tier 3 -- Manual/periodic reflect:**`
    - AGENTS.md section 11.4 contains `#### Reflect Checkpoint` with `wiki/maintenance/reflect-state.md`
    - AGENTS.md section 11.4 contains `#### Periodic Reflect Procedure (Tier 3)` with 8 numbered steps
    - AGENTS.md section 11.4 step 3 contains `**Deduplication rule:**` explaining log.md vs git log authority
    - AGENTS.md section 11.4 contains "A reflect run that produces no decision records still advances the checkpoint"
    - AGENTS.md section 11.4 contains `#### Abort Conditions`
    - AGENTS.md section 9 MERGE contains step `7a.` with `trigger_type: merge` and explicit "Skip for trivial cleanup merges" criteria
    - AGENTS.md section 9 SUPERSEDE contains step `6a.` with decision record creation and explicit "Skip for routine stale-claim supersessions" criteria
    - AGENTS.md section 11.3 contains step 11 `**Drift detection (category: \`drift\`):**`
    - AGENTS.md section 11.3 step 11 mentions DRFT-01, DRFT-02, DRFT-03
    - AGENTS.md section 11.3 steps renumbered: compile report is step 12, log entry is step 13, commit is step 14
    - AGENTS.md section 11.3 lists `drift` as a valid --category value
    - No references to the old pattern (decision records as overview pages in wiki/overviews/) remain in section 11.4
  </acceptance_criteria>
  <done>AGENTS.md fully documents the three-tier reflect model in section 11.4 (with deduplication rules and abort conditions), inline decision record hooks with explicit skip criteria in section 9 for MERGE (7a) and SUPERSEDE (6a), and drift detection as lint step 11 in section 11.3 with category documentation.</done>
</task>

<task type="auto">
  <name>Task 2: Create reflect checkpoint state file</name>
  <files>wiki/maintenance/reflect-state.md</files>
  <read_first>
    - wiki/maintenance/lint-report.md (lines 1-20, established maintenance file pattern)
    - .planning/phases/06-reflection-drift-detection/06-RESEARCH.md lines 149-170 (checkpoint mechanism pattern)
    - .planning/phases/06-reflection-drift-detection/06-CONTEXT.md (D-08, D-09)
  </read_first>
  <action>
    Create `wiki/maintenance/reflect-state.md` -- the reflect checkpoint state file per D-08.

    This file uses AGENTS.md section 5 compliant frontmatter. Use `type: overview` consistent with lint-report.md (both are maintenance/control-plane files). The frontmatter includes the three checkpoint fields plus standard base fields.

    Exact file content:
    ```yaml
    ---
    id: reflect-state
    title: Reflect State
    type: overview
    status: active
    summary: "Checkpoint state for periodic reflect workflow (AGENTS.md section 11.4)."
    created_at: 2026-04-14
    updated_at: 2026-04-14
    sources: []
    epistemic_status: sourced
    tags:
      - meta
      - maintenance
    domains: []
    supersedes:
    superseded_by:
    privacy: cloud_safe
    aliases:
      - Reflect State
    has_contradictions: false
    knowledge_domain: ""
    last_reflect_log_entry: ""
    last_reflect_commit: ""
    last_reflect_at: 2026-04-14
    ---

    # Reflect State

    Control-plane checkpoint for the periodic reflect workflow (AGENTS.md section 11.4).
    This file tracks where the last reflect pass ended so subsequent passes resume from the correct position.

    ## Fields

    - **last_reflect_log_entry:** The full heading line of the last log.md entry scanned. Empty string means no prior reflect pass has scanned log entries.
    - **last_reflect_commit:** The short SHA of the last git commit inspected. Empty string means scan from the beginning of git history.
    - **last_reflect_at:** ISO 8601 date of the last reflect pass.

    ## Usage

    Read this file at the start of each periodic reflect pass (Tier 3, Section 11.4). After completing the pass -- whether or not decision records were created -- update all three checkpoint fields and commit. A reflect pass that produces no records still advances the checkpoint to prevent re-scanning.
    ```

    Initial values:
    - `last_reflect_log_entry: ""` (no prior reflect pass)
    - `last_reflect_commit: ""` (scan from beginning)
    - `last_reflect_at: 2026-04-14` (file creation date)

    Note: This file is intentionally NOT added to wiki/index.md. Maintenance files are control-plane infrastructure, not wiki content. This matches the pattern of lint-report.md (also in maintenance/, also not indexed).
  </action>
  <verify>
    <automated>test -f wiki/maintenance/reflect-state.md && grep -c "last_reflect_log_entry" wiki/maintenance/reflect-state.md && grep -c "last_reflect_commit" wiki/maintenance/reflect-state.md && grep -c "last_reflect_at" wiki/maintenance/reflect-state.md && grep "id: reflect-state" wiki/maintenance/reflect-state.md && grep "type: overview" wiki/maintenance/reflect-state.md</automated>
  </verify>
  <acceptance_criteria>
    - wiki/maintenance/reflect-state.md exists
    - File contains valid YAML frontmatter with `id: reflect-state`
    - File contains `type: overview` (consistent with lint-report.md)
    - File contains `last_reflect_log_entry: ""`
    - File contains `last_reflect_commit: ""`
    - File contains `last_reflect_at: 2026-04-14`
    - File contains all 16 base frontmatter fields (id, title, type, status, summary, created_at, updated_at, sources, epistemic_status, tags, domains, supersedes, superseded_by, privacy, aliases, has_contradictions, knowledge_domain)
    - File body contains `## Fields` section explaining all three checkpoint fields
    - File body contains `## Usage` section with instructions
    - `bin/lint.sh --dry-run --category yaml wiki/` reports no errors for this file
  </acceptance_criteria>
  <done>Reflect checkpoint state file exists at wiki/maintenance/reflect-state.md with machine-readable checkpoint fields, AGENTS.md-compliant frontmatter, and documentation of field semantics and usage.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of all Phase 6 deliverables</name>
  <files>AGENTS.md, bin/lint.sh, schema/templates/decision.md, wiki/decisions/dr-2026-04-14-phase6-decision-type.md, wiki/index.md, wiki/maintenance/reflect-state.md</files>
  <action>
    Complete Phase 6 deliverables across all three plans:

    **Plan 01 -- Decision Record Page Type:**
    1. schema/templates/decision.md -- template with 7 required sections
    2. wiki/decisions/dr-2026-04-14-phase6-decision-type.md -- bootstrapping example with real prose
    3. AGENTS.md section 4.6 -- page type definition with directory rule, naming convention, ID convention
    4. AGENTS.md section 5 -- type enum updated, validation checklist items 15-17
    5. AGENTS.md section 12 -- Decisions in index category list
    6. wiki/index.md -- Decisions category with example entry

    **Plan 02 -- Drift Detection in Lint:**
    7. bin/lint.sh -- VALID_TYPES includes 'decision'
    8. bin/lint.sh -- Decision-specific YAML validation (trigger_type, affected_pages, decision_history)
    9. bin/lint.sh -- 5 drift checks (DRFT-01 through DRFT-03 + hash + index coverage)
    10. bin/lint.sh -- Auto-fix for content-hash drift gated behind --fix flag
    11. bin/lint.sh -- Category-labeled subsections in lint report format
    12. bin/lint.sh -- --category drift filtering works

    **Plan 03 -- Reflect Workflow:**
    13. AGENTS.md section 11.4 -- Three-tier reflect model with checkpoint, deduplication rules, abort conditions
    14. AGENTS.md section 9 -- Inline hooks (7a for MERGE, 6a for SUPERSEDE) with explicit skip criteria
    15. AGENTS.md section 11.3 -- Drift detection as step 11, steps renumbered to 14
    16. wiki/maintenance/reflect-state.md -- Checkpoint state file


    **Automated checks (run these first):**
    1. `bin/lint.sh --dry-run wiki/` -- should exit 0 with no error-level findings on Phase 6 files
    2. `bin/lint.sh --dry-run --category drift wiki/` -- should show drift-specific findings only
    3. `bin/lint.sh --dry-run --category yaml wiki/decisions/` -- should show 0 errors for decision page

    **Manual review:**
    4. Open `wiki/decisions/dr-2026-04-14-phase6-decision-type.md` -- verify all 7 sections have real prose (not just comments), "Why" section states adopted and replaced framing
    5. Open `wiki/index.md` -- verify "## Decisions" category exists with the example entry
    6. Open AGENTS.md section 4.6 -- verify directory rule (`wiki/decisions/`), naming convention (`dr-YYYY-MM-DD-slug.md`), ID convention, and worked example
    7. Open AGENTS.md section 5 -- verify `decision` in type enum, items 15-17 in validation checklist
    8. Open AGENTS.md section 9 -- verify steps 7a (MERGE) and 6a (SUPERSEDE) have explicit "skip for..." criteria
    9. Open AGENTS.md section 11.3 -- verify step 11 (drift detection) with DRFT-01/02/03, steps renumbered through 14
    10. Open AGENTS.md section 11.4 -- verify three-tier model, checkpoint mechanism, periodic procedure (8 steps), deduplication rule, abort conditions
    11. Open `wiki/maintenance/reflect-state.md` -- verify checkpoint fields
    12. Optionally: Open vault in Obsidian and verify decision record appears in graph view
  </action>
  <verify>
    <automated>bin/lint.sh --dry-run wiki/ 2>&1 | tail -10 && bin/lint.sh --dry-run --category drift wiki/ 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - bin/lint.sh --dry-run wiki/ exits 0
    - bin/lint.sh --dry-run --category drift wiki/ exits 0
    - Human confirms decision record example has real prose in all 7 sections
    - Human confirms AGENTS.md sections 4.6, 5, 9, 11.3, 11.4 are complete
  </acceptance_criteria>
  <done>All Phase 6 deliverables verified: decision page type, drift detection, reflect workflow, checkpoint state. Human approved.</done>
</task>

</tasks>

<verification>
- `bin/lint.sh --dry-run wiki/` exits 0 with no error-level findings on Phase 6 files
- `grep "Three-Tier Reflect Model" AGENTS.md` returns a match
- `grep "7a\." AGENTS.md` returns a match (inline MERGE hook)
- `grep "6a\." AGENTS.md` returns a match (inline SUPERSEDE hook)
- `grep "DRFT-01" AGENTS.md` returns a match (drift in lint workflow)
- `grep "Deduplication rule" AGENTS.md` returns a match
- `test -f wiki/maintenance/reflect-state.md` succeeds
- All 7 requirements (DCSN-01 through DCSN-03, DRFT-01 through DRFT-04) have corresponding implementations
</verification>

<success_criteria>
The reflect workflow is fully documented with three-tier model (inline with explicit trigger criteria, recommendations with concrete signal list, periodic with deduplication rules), inline hooks exist in MERGE and SUPERSEDE operations with skip criteria for trivial cases, drift detection is documented in the lint workflow as step 11, and the reflect checkpoint enables deterministic periodic passes. An agent reading AGENTS.md knows exactly when to create decision records, when to skip, and how to run drift checks.
</success_criteria>

<output>
After completion, create `.planning/phases/06-reflection-drift-detection/06-03-SUMMARY.md`
</output>
