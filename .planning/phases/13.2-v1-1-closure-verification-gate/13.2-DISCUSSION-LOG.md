# Phase 13.2: v1.1 Closure Verification Gate - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-01
**Phase:** 13.2-v1-1-closure-verification-gate
**Areas discussed:** Render disposition, Codex parity disposition, Check mechanism, Closure output

---

## DEBT-01 Obsidian render disposition (CLOSE-02)

| Option | Description | Selected |
|--------|-------------|----------|
| You render now, then close | User runs the 5 fixture Dataview queries + graph/wikilink checks, fills the capture slot; gate closes on real render evidence. | ✓ |
| Audit-and-defer | Gate audits the prepared checklist, records render as deferred, closes without the GUI step. | |
| Render now if quick, else defer | Decide at the gate. | |

**User's choice:** You render now, then close.
**Notes:** Takes the "re-run" branch of CLOSE-02 (not "audit"). Implies a user-in-the-loop checkpoint in execution. The 13.1 checklist already resolved the `.obsidianignore` KNOWN TENSION, so the render is expected to just work; expected counts 3/2/2/2/5.

---

## DEBT-02 Codex agent-parity disposition (CLOSE-02)

| Option | Description | Selected |
|--------|-------------|----------|
| Accept structural-diff audit | Use the completed Claude-vs-golden-subset diff as parity evidence; record Codex as environment-blocked; no re-run, no sudo. | ✓ |
| Remediate sandbox & re-run | Lift `apparmor_restrict_unprivileged_userns` (sudo) or find a bubblewrap-bypass flag, then genuinely re-run. | |
| Try alt invocation, fall back to audit | Bounded non-sudo attempts, then fall back. | |

**User's choice:** Accept structural-diff audit.
**Notes:** Root cause confirmed live this session: `kernel.apparmor_restrict_unprivileged_userns = 1` blocks Codex's bubblewrap user-namespace creation. Recorded as host-environment limitation, not an agent-parity failure.

---

## CLOSE-03 docs-consistency + CLOSE-04 scope-leak mechanism

| Option | Description | Selected |
|--------|-------------|----------|
| Manual audit, edit-on-drift | One-time agent audit vs. v1.1 surface + forbidden-scope list; fix inline; no new script. | ✓ |
| Build reusable bin/ closure-check | Author `check-scope-leak.sh` + committed denylist for repeatable closure. | |
| Manual audit, report-only | Audit and report without editing; user fixes later. | |

**User's choice:** Manual audit, edit-on-drift.
**Notes:** Matches Phase 13.1's accuracy-pass style. Reuses existing `requirements-sync.sh` / `check-neutrality.sh` / `check-privacy.sh` for the mechanical parts; only scope-leak + narrative consistency are the new manual audit.

---

## Closure deliverable + milestone disposition (CLOSE-01)

| Option | Description | Selected |
|--------|-------------|----------|
| Full gate + auto-archive | VERIFICATION + independent VERIFIER-REPORT, flip CLOSE-01..04 Complete, resolve untracked 12.2 report + author SC1 reframing DR, then run `/gsd-complete-milestone`. | ✓ |
| Full gate, archive separately | Same artifacts but stop before archiving; user archives. | |
| Gate only, no DR/cleanup | Just artifacts + CLOSE-01..04; leave 12.2 report + SC1 DR as follow-ups. | |

**User's choice:** Full gate + auto-archive.
**Notes:** Folds in two scouted cleanup items: commit the untracked `12.2-VERIFIER-REPORT.md`, and author the SC1 reframing decision record (`trigger_type: reframing`) flagged by 13.1.

## Claude's Discretion

- Plan/wave decomposition across CLOSE-01..04.
- Ordering of mechanical gates vs. manual audit (subject to render checkpoint + auto-archive bracketing).
- Whether docs-consistency and scope-leak are one plan or two.

## Deferred Ideas

- Reusable `bin/check-scope-leak.sh` closure script — rejected for this one-time gate; seed for a future tooling phase.
- Codex sandbox remediation / non-bubblewrap multi-agent harness — rejected for v1.1; path for a future parity milestone.
- All Phase 999.x backlog items remain backlog (CLOSE-04 boundary).
