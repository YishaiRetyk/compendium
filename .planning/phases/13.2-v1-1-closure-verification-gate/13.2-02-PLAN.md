---
phase: 13.2-v1-1-closure-verification-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .planning/ROADMAP.md
  - docs/reference/three-layer-model.md
  - wiki/decisions/dr-2026-05-01-complementary-systems-boundary.md
autonomous: true
requirements: [CLOSE-03, CLOSE-04]
must_haves:
  truths:
    - "README, docs/, decision records, and ROADMAP.md consistently describe the final v1.1 feature surface, the complementary-system boundary, and explicitly deferred work — drift fixed inline (D-03)."
    - "A single-pass scope-leak audit confirms NO task engine, reminder/calendar layer, high-frequency event ingest, premature scaling tier, or canonical GTD dashboard entered v1.1 — any leak fixed inline."
    - "bin/check-neutrality.sh and bin/check-privacy.sh stay exit 0 (reused as-is, no new script)."
  artifacts:
    - path: "README.md"
      provides: "Final v1.1 feature surface + deferred-work narrative consistent with PROJECT.md"
    - path: "docs/reference/three-layer-model.md"
      provides: "Complementary-system boundary + anti-features list consistent with the DR"
  key_links:
    - from: "README.md / docs/ / ROADMAP.md narrative"
      to: ".planning/PROJECT.md feature surface + Deferred-to-backlog list"
      via: "edit-on-drift accuracy pass"
      pattern: "Deferred"
---

<objective>
Execute CLOSE-03 (docs/README/DR/roadmap consistency) and CLOSE-04 (scope-leak audit) as a single combined manual edit-on-drift pass (D-03 — no new bin/ script).

Purpose: prove the canonical shipped surface describes the FINAL v1.1 feature set, the complementary-system boundary, and explicitly deferred work consistently, and that no forbidden GTD/task-engine scope crept in. This mirrors Phase 13.1 Plan 05's accuracy-pass style: read source-of-truth, edit only where drift exists, leave byte-unchanged otherwise.

Output: drift fixed inline across README / docs / DR / ROADMAP (or confirmed byte-unchanged); scope-leak audit recorded in SUMMARY; neutrality + privacy gates stay exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.2-v1-1-closure-verification-gate/13.2-CONTEXT.md
@README.md
@docs/reference/three-layer-model.md
@wiki/decisions/dr-2026-05-01-complementary-systems-boundary.md

<facts>
<!-- Established from live inspection during planning. -->

SOURCE-OF-TRUTH for the v1.1 feature surface + deferred work (PROJECT.md, verified live):
- PROJECT.md line 25 "Deferred to backlog:" Obsidian plugin distribution, one-command installer (`curl|bash`), hosted docs site, brownfield `--apply` mode for judgment-heavy operations (→ v1.2).
- PROJECT.md §"Current Milestone: v1.1 Shareability" carries the shipped feature list (BOUND-01/02/03 complementary-systems boundary already validated in Phase 12).

CLOSE-04 FORBIDDEN-SCOPE SET (CONTEXT.md §domain, LOCKED): task engine, reminder/calendar layer, high-frequency event ingest, premature scaling tier, canonical GTD dashboard. The complementary-systems boundary DR (`wiki/decisions/dr-2026-05-01-complementary-systems-boundary.md`) + `docs/reference/three-layer-model.md` already state the anti-features list; CLOSE-04 confirms NO product code/docs contradict that boundary by introducing any forbidden capability.

EDIT-ON-DRIFT DISCIPLINE (Phase 13.1 Plan 05 precedent): leave files byte-unchanged where they already match source-of-truth. Only edit where narrative has drifted (stale feature claims, wrong deferred-work list, scope-leak language). Record per-file NO-DRIFT vs EDITED in the SUMMARY.

The mechanical guards `bin/check-neutrality.sh` and `bin/check-privacy.sh` both exit 0 on the current tree — they MUST stay exit 0 after any edit.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLOSE-03 — docs/README/DR/ROADMAP consistency accuracy pass (edit-on-drift)</name>
  <read_first>
    - .planning/PROJECT.md (the authoritative v1.1 feature surface + "Deferred to backlog" list, line 25)
    - README.md (feature/overview + any deferred-work or roadmap narrative)
    - .planning/ROADMAP.md (v1.1 phase summaries + Backlog section)
    - docs/reference/three-layer-model.md (complementary-system boundary + anti-features list)
    - wiki/decisions/dr-2026-05-01-complementary-systems-boundary.md (canonical boundary DR)
  </read_first>
  <action>
    Perform a single-pass accuracy review of each file against PROJECT.md as source-of-truth (Phase 13.1 Plan 05 edit-on-drift model):
    1. README.md — verify the described feature surface matches the FINAL v1.1 set (template foundation, neutrality/privacy gates, wizard+manual setup, collaborative PR/CI lint gate, brownfield scan/bootstrap/suggest/verify, local write gate, claim-faithfulness audit, Obsidian starter, complementary-systems boundary). Verify any "deferred"/"future"/"roadmap" narrative matches PROJECT.md line 25 (Obsidian plugin distribution, one-command installer, hosted docs site, brownfield `--apply`). Fix only drifted lines.
    2. ROADMAP.md — verify the v1.1 phase list and Backlog section consistently describe shipped vs deferred. Fix drift only.
    3. docs/reference/three-layer-model.md + the boundary DR — verify the complementary-system boundary + anti-features list are consistent with each other and with PROJECT.md. Fix drift only.
    Leave every non-drifted file byte-unchanged. For each file, record NO-DRIFT or EDITED (with the specific line corrected) in the SUMMARY.
    Apply CLAUDE.md §3 neutrality on every edit to a template-public file (README.md, docs/, wiki/ scaffolding) — abstract placeholders only, no real vault terms.
  </action>
  <verify>
    <automated>bash bin/check-neutrality.sh >/dev/null 2>&1; echo "neutrality exit: $?"; bash bin/check-privacy.sh >/dev/null 2>&1; echo "privacy exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `bash bin/check-neutrality.sh` exits 0.
    - `bash bin/check-privacy.sh` exits 0.
    - `grep -c "Deferred to backlog\|Deferred\|deferred" README.md` — README's deferred-work narrative (if present) names items consistent with PROJECT.md line 25; SUMMARY documents the cross-check.
    - SUMMARY lists every reviewed file as NO-DRIFT or EDITED (with corrected line cited). No file edited without a documented drift reason.
    - `git diff --stat` shows edits ONLY to files in this plan's files_modified (no scope-creep edits).
  </acceptance_criteria>
  <done>README / docs / DR / ROADMAP consistently describe the final v1.1 feature surface, boundary, and deferred work; non-drifted files left byte-unchanged; neutrality + privacy exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: CLOSE-04 — scope-leak audit against the forbidden set (single-pass, edit-on-drift)</name>
  <read_first>
    - .planning/phases/13.2-v1-1-closure-verification-gate/13.2-CONTEXT.md (§domain forbidden set; D-03 manual audit, edit-on-drift, no new bin/ script)
    - wiki/decisions/dr-2026-05-01-complementary-systems-boundary.md (the anti-features the boundary forbids)
    - docs/reference/three-layer-model.md (anti-features list)
    - .planning/PROJECT.md (shipped surface — confirm nothing forbidden shipped)
  </read_first>
  <action>
    Audit the v1.1 shipped surface for the LOCKED forbidden-scope set: (1) task engine, (2) reminder/calendar layer, (3) high-frequency event ingest, (4) premature scaling tier (e.g. SQLite/DB-backed metadata shipped prematurely — note AGENTS.md §14 documents these as FUTURE tiers, which is allowed; a forbidden leak would be a SHIPPED implementation), (5) canonical GTD dashboard.
    Method (D-03 — manual single-pass, no reusable bin/check-scope-leak.sh):
    1. Grep the shipped surface for leak indicators, e.g.:
       `grep -rniE "task engine|reminder|calendar|cron|scheduler|gtd dashboard|sqlite|database-backed|next action" README.md docs/ wiki/ AGENTS.md CLAUDE.md bin/ schema/ 2>/dev/null`
       — then classify each hit: ALLOWED (documents the boundary / names an anti-feature / describes a FUTURE scaling tier) vs LEAK (an actually-shipped forbidden capability).
    2. If any hit is a genuine LEAK, fix it inline (remove the leaked capability/doc, or restate it as explicitly-deferred per the boundary DR). The expectation (per CONTEXT) is ZERO leaks — the boundary DR already forecloses these — so most/all hits should classify as ALLOWED boundary/anti-feature mentions.
    3. Record the full hit list + per-hit ALLOWED/LEAK classification in the SUMMARY (this IS the CLOSE-04 audit evidence Plan 03 cites).
    Do NOT create bin/check-scope-leak.sh (explicitly deferred — CONTEXT Deferred Ideas).
  </action>
  <verify>
    <automated>grep -rniE "task engine|reminder|calendar|cron|scheduler|gtd dashboard" README.md docs/ AGENTS.md 2>/dev/null | grep -viE "boundary|anti-feature|complementary|does not|do not|NOT |own[s]?|deferred|out of scope" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY contains a scope-leak hit table with every grep hit classified ALLOWED or LEAK.
    - Zero hits classified LEAK remain unaddressed (if any LEAK found, it is fixed inline and re-verified).
    - `bash bin/check-neutrality.sh` exits 0 and `bash bin/check-privacy.sh` exits 0 after any fix.
    - No `bin/check-scope-leak.sh` created: `test ! -f bin/check-scope-leak.sh` (must be true).
  </acceptance_criteria>
  <done>Scope-leak audit complete: forbidden set confirmed absent (or any leak fixed inline); audit hit-table recorded in SUMMARY; no reusable script created; gates exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Docs/narrative audit + edit-on-drift. No runtime code path, no network/auth/input handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.2-02 | I (Information disclosure) | edits to template-public files (README.md, docs/, wiki/ scaffolding) | mitigate | `bin/check-neutrality.sh` + `bin/check-privacy.sh` stay exit 0 (both task acceptances); CLAUDE.md §3 abstract-placeholder discipline on every edit. |

No other STRIDE categories apply: read/audit existing artifacts and edit docs only. No new attack surface.
</threat_model>

<verification>
- `bash bin/check-neutrality.sh` exits 0.
- `bash bin/check-privacy.sh` exits 0.
- Scope-leak hit-table recorded; zero unaddressed LEAKs.
- No bin/check-scope-leak.sh created.
- Edits confined to files_modified.
</verification>

<success_criteria>
CLOSE-03 and CLOSE-04 are satisfied: the canonical shipped surface consistently describes the final v1.1 feature set, boundary, and deferred work (drift fixed inline), and the forbidden-scope set is confirmed absent — all via a single manual edit-on-drift pass reusing existing mechanical guards, with no new tooling.
</success_criteria>

<output>
After completion, create `.planning/phases/13.2-v1-1-closure-verification-gate/13.2-02-SUMMARY.md`
</output>
