---
phase: 13.2-v1-1-closure-verification-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - docs/reference/agent-parity.md
autonomous: false
requirements: [CLOSE-02]
must_haves:
  truths:
    - "The Obsidian render is re-run by the user and the five fixture-scoped Dataview counts are recorded into 13.1-VERIFICATION.md (3/2/2/2/5)."
    - "DEBT-01 is flipped from Pending/awaiting to Complete in BOTH REQUIREMENTS.md (bullet + matrix) and 13.1-VERIFICATION.md, clearing the single milestone-wide drift row."
    - "The DEBT-02 Codex column is resolved in docs/reference/agent-parity.md from 'pending — deferred to 13.2' to 'blocked-on-host-runtime (AppArmor apparmor_restrict_unprivileged_userns=1)'."
    - "The DEBT-04 genuine write-back evidence in 13.1-VERIFICATION.md is audited and confirmed present."
  artifacts:
    - path: ".planning/phases/13.1-docs-finalization-obsidian-starter/13.1-VERIFICATION.md"
      provides: "Filled DEBT-01 capture slot + DEBT-01 flipped to Complete"
      contains: "active-entities"
    - path: "docs/reference/agent-parity.md"
      provides: "Codex column resolved to blocked-on-host-runtime"
      contains: "blocked-on-host-runtime"
  key_links:
    - from: ".planning/REQUIREMENTS.md DEBT-01 row"
      to: "bin/requirements-sync.sh --strict zero-drift"
      via: "DEBT-01 flipped Complete in both bullet and matrix"
      pattern: "DEBT-01.*Complete"
---

<objective>
Execute CLOSE-02: re-run / explicitly audit the three carried-over verifications from Phase 13.1 (DEBT-01 Obsidian render, DEBT-02 Codex agent-parity, DEBT-04 write-back), and clear the single milestone-wide requirements-sync drift row by flipping DEBT-01 to Complete.

Purpose: CLOSE-02 is the verification-evidence half of the v1.1 closure gate. The user takes the "re-run" branch (D-01) for the Obsidian render — this plan contains the single human-in-the-loop GUI checkpoint of the whole phase. DEBT-02 is accepted as blocked-on-host-runtime (D-02, no remediation). DEBT-04 is audit-only.

Output: filled DEBT-01 capture slot + DEBT-01 flipped to Complete (REQUIREMENTS.md bullet + matrix + 13.1-VERIFICATION.md), Codex column resolved in agent-parity.md, DEBT-04 confirmed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.2-v1-1-closure-verification-gate/13.2-CONTEXT.md
@.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-VERIFICATION.md
@docs/reference/examples.md
@docs/reference/agent-parity.md

<facts>
<!-- Established from live inspection during planning — executor needs these, no re-discovery required. -->

CURRENT DRIFT STATE (verified live during planning):
- `bash bin/requirements-sync.sh --strict` exits 2 with exactly ONE drift row:
  `| DEBT-01 | Pending (awaiting user render; Phase 13.2 audits) | Awaiting user render | DRIFT |`
- This is the ONLY drift in the entire 97-requirement ledger. Clearing it (flipping DEBT-01 → Complete in both REQUIREMENTS.md and 13.1-VERIFICATION.md) is what makes CLOSE-01's zero-drift assertion (Plan 03) achievable.

DEBT-01 LOCATIONS:
- REQUIREMENTS.md line 111 (bullet): `- [ ] **DEBT-01**: Obsidian render verification — ...`
- REQUIREMENTS.md line 276 (matrix): `| DEBT-01 | Phase 13.1 | Pending (awaiting user render; Phase 13.2 audits) |`
- 13.1-VERIFICATION.md: capture slot at lines ~182-196; matrix row ~206; REQ-ID block line ~214 (`- [ ] **DEBT-01**: Awaiting user render`).

EXPECTED RENDER COUNTS (from docs/reference/examples.md, queries FROM "examples/dataview-fixtures"):
- active-entities = 3
- sources-by-domain(alpha) = 2
- stale-pages = 2
- missing-privacy = 2
- pages-in-domain(alpha, active) = 5

DEBT-02 CODEX COLUMN (docs/reference/agent-parity.md): the `pending — deferred to 13.2` literal occurs in SIX places — the line-~74 "how to read the table" prose sentence above the diff table, FIVE Codex-column cells in the table (lines ~78-82), AND a line-~84 prose promise paragraph ("...blocked-on-runtime and is re-run/confirmed in Phase 13.2"). Root cause confirmed live: AppArmor `kernel.apparmor_restrict_unprivileged_userns = 1` blocks Codex's bubblewrap sandbox from creating user namespaces. The Claude-vs-golden-subset structural diff (EXACT MATCH on all 6 `#sec:` targets) already carries the parity verdict.
</facts>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: User renders the wiki in Obsidian and reports the five Dataview counts (D-01)</name>
  <read_first>
    - .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-VERIFICATION.md (the DEBT-01 render checklist + capture slot, lines ~166-196)
    - docs/reference/examples.md (the five fixture-scoped Dataview query blocks + expected counts)
  </read_first>
  <what-built>
    Phase 13.1 already shipped the precise DEBT-01 render checklist and an empty "awaiting user render" capture slot in 13.1-VERIFICATION.md (lines ~182-196). This checkpoint presents that checklist to the user and pauses for the GUI render — the single human step in the entire closure gate (D-01a).
  </what-built>
  <how-to-verify>
    1. Open the wiki vault in Obsidian (the repo root is the vault).
    2. DO NOT add `examples/` or `examples/dataview-fixtures/` to Settings → Files & Links → Excluded files for this render pass — the fixtures MUST stay indexed for Dataview (`.obsidianignore` is a release-manifest/graph-hygiene convention, NOT Obsidian-enforced; live `.obsidian/app.json` is `{}` so the fixtures ARE Dataview-indexed).
    3. Run the five fixture-scoped Dataview queries (all `FROM "examples/dataview-fixtures"`) documented in `docs/reference/examples.md` and read off each row count. Expected:
       - active-entities = 3
       - sources-by-domain(alpha) = 2
       - stale-pages = 2
       - missing-privacy = 2
       - pages-in-domain(alpha, active) = 5
    4. Confirm wikilinks resolve (no unexpected red links among the fixtures) and that `examples/` forms a VISUALLY ISOLABLE sub-graph (reframed SC1 — see Plan 03 DR; a one-time graph-only Excluded-files pass is acceptable, then remove it so Dataview still indexes the fixtures).
    5. Report the five observed counts back, plus the date rendered and the Obsidian + Dataview versions if convenient.
  </how-to-verify>
  <resume-signal>Report the five counts (e.g. "active-entities=3, sources-by-domain(alpha)=2, stale-pages=2, missing-privacy=2, pages-in-domain(alpha,active)=5") plus the render date, or describe any mismatch.</resume-signal>
  <acceptance_criteria>
    - User has supplied five numeric counts and a render date.
    - If any count diverges from the expected 3/2/2/2/5, the executor records the observed value verbatim AND flags the divergence in the SUMMARY (do not silently "correct" to expected).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Record render counts into 13.1-VERIFICATION.md and flip DEBT-01 → Complete in all three locations</name>
  <read_first>
    - .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-VERIFICATION.md (capture slot ~182-196; matrix row ~206; REQ-ID block ~214; frontmatter `score:` line 5)
    - .planning/REQUIREMENTS.md (DEBT-01 bullet line 111; matrix row line 276)
  </read_first>
  <action>
    Using the five counts reported in Task 1, edit `13.1-VERIFICATION.md`:
    1. Fill the capture slot (lines ~182-196): replace each `____` blank with the reported count, and fill `Date rendered:` with the reported date. Preserve the surrounding code-fence and "(expected N)" annotations.
    2. Flip the DEBT-01 REQ-ID block (line ~214) from `- [ ] **DEBT-01**: Awaiting user render` to `- [x] **DEBT-01**: Complete` and update its evidence sub-bullets to state the render was executed with the recorded counts (cite the capture slot in this same file). Mirror the FAITH-0X REQ-ID block style from `.planning/phases/13-claim-faithfulness-audit/13-VERIFICATION.md` lines ~213-247.
    3. Flip the DEBT-01 matrix row (line ~206) status cell from `AWAITING USER RENDER` to `Complete`.
    4. Update the frontmatter `score:` line 5 from `5/6 ... (DEBT-01 render = awaiting user GUI render ...)` to `6/6 success criteria verified` and the `status:` accordingly.

    Then edit `.planning/REQUIREMENTS.md`:
    5. Line 111 bullet: change `- [ ] **DEBT-01**:` to `- [x] **DEBT-01**:` and append ` **Status:** Complete (Phase 13.2). See \`.planning/phases/13.1-docs-finalization-obsidian-starter/13.1-VERIFICATION.md\`.` (mirror the FAITH bullets' Status-suffix convention).
    6. Line 276 matrix: change `| DEBT-01 | Phase 13.1 | Pending (awaiting user render; Phase 13.2 audits) |` to `| DEBT-01 | Phase 13.1 | Complete |`.

    Do NOT touch CLOSE-01..04 rows here — Plan 03 owns those.
  </action>
  <verify>
    <automated>bash bin/requirements-sync.sh --strict ; echo "EXIT=$?"  # AUTHORITATIVE: must print EXIT=0. The grep below is INFORMATIONAL only (the sync table prints a 'Drift' column header that can echo): bash bin/requirements-sync.sh --strict 2>&1 | grep -c "DRIFT"</automated>
  </verify>
  <acceptance_criteria>
    - PRIMARY PROOF: `bash bin/requirements-sync.sh --strict; echo $?` prints `0` (was exit 2 before this task). Exit-0 is the authoritative signal that the DEBT-01 drift row is cleared across the full 97-req ledger. Do NOT rely on the `grep -c "DRIFT"` count as the acceptance signal — it is fragile because the requirements-sync table prints a `Drift` column header that can echo into the count; treat any grep output as informational diagnostics only.
    - `grep -n "DEBT-01" .planning/REQUIREMENTS.md` shows line 111 starting with `- [x] **DEBT-01**` AND the matrix row containing `Complete` (not `Pending`).
    - `grep -A2 "active-entities count" .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-VERIFICATION.md` shows the reported numeric value (no remaining `____`).
    - `grep -c "____" .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-VERIFICATION.md` returns 0 within the DEBT-01 capture slot region.
    - `grep "DEBT-01" .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-VERIFICATION.md` shows the REQ-ID block as `- [x] **DEBT-01**: Complete`.
  </acceptance_criteria>
  <done>DEBT-01 is Complete in all three locations; `requirements-sync.sh --strict` exits 0 (zero drift across the full 97-req ledger) — exit code is the authoritative proof.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve the DEBT-02 Codex column + audit DEBT-04 write-back evidence</name>
  <read_first>
    - docs/reference/agent-parity.md (diff table region lines ~72-84; the SIX `pending — deferred to 13.2` occurrences — line ~74 prose, 5 cells lines ~78-82, line ~84 prose promise)
    - .planning/phases/13.1-docs-finalization-obsidian-starter/13.1-VERIFICATION.md (DEBT-04 write-back evidence section; DEBT-02 structural-diff record)
    - .planning/phases/13.2-v1-1-closure-verification-gate/13.2-CONTEXT.md (D-02 / D-02a — accept Codex as blocked-on-host-runtime; NO sudo, NO sandbox remediation, NO alt-agent attempt)
  </read_first>
  <action>
    DEBT-02 (D-02 — accept, do NOT re-run, do NOT remediate the sandbox). The `pending — deferred to 13.2` literal occurs in SIX places in `docs/reference/agent-parity.md`; the Task-3 acceptance grep (`grep -c "pending — deferred to 13.2" ... returns 0`) covers ALL six, so EVERY occurrence below must be edited:
    1. **Line-~74 "how to read the table" prose sentence** (directly above the diff table): it currently reads `the Codex column is rendered \`pending — deferred to 13.2 (Codex auth/runtime unavailable)\``. Rewrite this sentence so the Codex column is described as `blocked-on-host-runtime (AppArmor apparmor_restrict_unprivileged_userns=1)` — a host runtime-environment limitation recorded honestly, never fabricated as a measured MATCH. Remove the `pending — deferred to 13.2` literal from this sentence.
    2. **The FIVE Codex-column cells in the diff table (lines ~78-82)**: change each from `pending — deferred to 13.2 (Codex runtime unavailable)` / `pending — deferred to 13.2` to `blocked-on-host-runtime (AppArmor apparmor_restrict_unprivileged_userns=1)`. Keep the Claude-vs-golden-subset column UNCHANGED (it already reads MATCH / EXACT MATCH and carries the parity verdict). The `Claude vs Codex` column's `pending — Codex produced 0 pages` cells may stay as-is (they do not contain the `pending — deferred to 13.2` literal) OR be aligned to `blocked-on-host-runtime` phrasing — but at minimum every `pending — deferred to 13.2` literal must be gone.
    3. **The line-~84 prose promise paragraph ("Recorded outcome")**: it currently ends `The Codex column is blocked-on-runtime and is re-run/confirmed in Phase 13.2; ...`. Post-closure this is a FALSE promise (no Codex re-run was or will be performed per D-02). REWRITE the phrase "is re-run/confirmed in Phase 13.2" to the post-closure truth: the Codex column is recorded as **blocked-on-host-runtime via the live AppArmor `apparmor_restrict_unprivileged_userns=1` user-namespace restriction**; the Claude-vs-golden-subset EXACT structural-diff carries the parity verdict; NO Codex re-run was performed (per D-02, the runtime block is accepted, not remediated). Do NOT merely flip the table cells while leaving this promise intact — the sentence itself must be rewritten so the doc tells the post-closure truth.
    4. Across all three edits: do NOT claim any new Codex run occurred. Do NOT mention sudo/remediation as a next step (it is explicitly deferred per CONTEXT D-02 / Deferred Ideas). Record the outcome as a HOST-ENVIRONMENT limitation NOT an agent-parity failure (SAFETY-GUARD honesty).

    DEBT-04 (audit-only — D per CLOSE-02):
    5. Confirm the DEBT-04 genuine cross-source write-back evidence already recorded in 13.1-VERIFICATION.md is present and self-consistent (the UPDATE to `wiki/concepts/progressive-disclosure.md` citing ≥2 distinct cloud_safe source_ids, validate-op UPDATE PASS, the new WRITE-BACK log line). DEBT-04 is already Complete in REQUIREMENTS.md — this is a read-only audit confirming the evidence stands. Record the audit outcome in the SUMMARY (do not edit REQUIREMENTS.md for DEBT-04).
  </action>
  <verify>
    <automated>grep -c "blocked-on-host-runtime" docs/reference/agent-parity.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pending — deferred to 13.2" docs/reference/agent-parity.md` returns 0 (ALL SIX occurrences resolved — the line-~74 prose sentence, the five table cells lines ~78-82, AND the line-~84 prose promise).
    - `grep -c "re-run/confirmed in Phase 13.2" docs/reference/agent-parity.md` returns 0 (the false post-closure promise is rewritten, not just the cells flipped).
    - `grep -c "blocked-on-host-runtime" docs/reference/agent-parity.md` returns >= 1.
    - `grep -c "apparmor_restrict_unprivileged_userns" docs/reference/agent-parity.md` returns >= 1 (root cause named).
    - The Claude-vs-golden-subset column still contains `EXACT MATCH` (parity verdict preserved): `grep -c "EXACT MATCH" docs/reference/agent-parity.md` returns >= 1.
    - `bash bin/check-neutrality.sh` exits 0 (agent-parity.md is a template-public doc).
    - SUMMARY records DEBT-04 audit outcome (evidence present, self-consistent).
  </acceptance_criteria>
  <done>All six `pending — deferred to 13.2` occurrences resolved (prose sentence above the table, five cells, and the line-~84 prose promise rewritten so no false re-run promise remains); Codex column reads blocked-on-host-runtime with root cause named; Claude-vs-golden parity verdict preserved; DEBT-04 evidence audited and confirmed; neutrality clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds NO runtime code path, NO network/auth/input handling. It records a human-reported GUI render result and edits planning/doc markdown. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.2-01 | I (Information disclosure) | edits to template-public `docs/reference/agent-parity.md` | mitigate | `bin/check-neutrality.sh` must stay exit 0 (Task 3 acceptance); abstract placeholders only per CLAUDE.md §3 — no real vault terms introduced. |

No other STRIDE categories apply: the gate only reads/audits existing artifacts and edits docs. No new attack surface.
</threat_model>

<verification>
- `bash bin/requirements-sync.sh --strict` exits 0 (DEBT-01 drift cleared) — exit code is the authoritative drift signal.
- `bash bin/check-neutrality.sh` exits 0.
- DEBT-01 Complete in REQUIREMENTS.md (bullet + matrix) and 13.1-VERIFICATION.md.
- agent-parity.md Codex column resolved (all six `pending — deferred to 13.2` occurrences gone, no false re-run promise); Claude-vs-golden EXACT MATCH preserved.
</verification>

<success_criteria>
CLOSE-02 verification evidence is in place: the Obsidian render was re-run with recorded counts, DEBT-01 is Complete everywhere (clearing the sole milestone drift row), the Codex column is honestly resolved as blocked-on-host-runtime with no false re-run promise remaining, and DEBT-04 write-back evidence is confirmed.
</success_criteria>

<output>
After completion, create `.planning/phases/13.2-v1-1-closure-verification-gate/13.2-01-SUMMARY.md`
</output>
