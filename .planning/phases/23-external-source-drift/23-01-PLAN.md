---
phase: 23-external-source-drift
plan: "01"
wave: 1
autonomous: true
requirements: [DRIFT-01, DRIFT-02, DRIFT-03-partial]
must_haves:
  truths:
    - "bin/lint.sh --network runs repository HEAD-drift (git ls-remote vs commit_sha), URL reachability (curl), and citation-registry link-rot checks as EXTERNAL:-prefixed drift findings; without the flag, output is byte-identical to pre-phase behavior; --ci still default-skips drift-external."
    - "Videos (transcript + channel field) are skipped by the URL check; missing tools degrade to one info finding; severities never exceed warning."
    - "tests/phase-23/ is network-free (file:// fixture repo for ls-remote, PATH-injected curl stub, byte-identity test) and green; LINT_VERSION 1.12.0."
  artifacts: ["bin/lint.sh", "tests/phase-23/{run.sh,test_network_flag_identity.sh,test_repo_head_drift.sh,test_url_reachability.sh,test_registry_linkrot.sh}"]
---
# Plan 23-01 — --network drift checks (TDD)
RED harness first (fixture wiki + file:// upstream repo + curl stub), then the three
check families inside the drift section of lint.sh, gated on a NETWORK env passed
from the new --network flag. Commit: feat(23-01).
