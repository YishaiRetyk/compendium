---
phase: 01-schema-structure-conventions
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified: []
autonomous: false
requirements:
  - SCHM-01
  - SCHM-02
  - SCHM-03
  - SCHM-04
  - SCHM-05
  - DIRS-01
  - DIRS-02
  - DIRS-03
  - DIRS-04
  - OBSD-01
  - OBSD-02
  - OBSD-03
  - OBSD-04
  - PROG-01
  - PROG-02
  - PROG-03
  - BNDY-01
  - BNDY-02
  - BNDY-03

must_haves:
  truths:
    - "An LLM agent can read AGENTS.md and understand all conventions and workflows without additional instruction"
    - "The directory structure exists with sources/, wiki/ (with subdirs), and schema/"
    - "Opening the vault in Obsidian shows valid frontmatter in index.md and log.md"
    - "All 19 Phase 1 requirements are addressed in AGENTS.md or the directory structure"
  artifacts:
    - path: "AGENTS.md"
      provides: "Complete 16-section agent-agnostic schema"
      min_lines: 800
    - path: "wiki/index.md"
      provides: "Content index with valid frontmatter"
    - path: "wiki/log.md"
      provides: "Activity log with valid frontmatter"
  key_links: []
---

<objective>
Verify the complete Phase 1 deliverables: AGENTS.md schema document and directory skeleton.

Purpose: Human confirms that the schema reads well, the directory structure makes sense in Obsidian, and the specification is complete enough to drive all downstream phases.
Output: Confirmation that Phase 1 is ready for verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-schema-structure-conventions/01-CONTEXT.md
@.planning/phases/01-schema-structure-conventions/01-01-SUMMARY.md
@.planning/phases/01-schema-structure-conventions/01-02-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated validation of all Phase 1 requirements</name>
  <files></files>
  <read_first>
    AGENTS.md,
    wiki/index.md,
    wiki/log.md
  </read_first>
  <action>
    Run automated checks against all 19 Phase 1 requirements. For each requirement, verify the condition and report pass/fail:

    **Directory checks:**
    - DIRS-01: `test -d sources/` (sources directory exists)
    - DIRS-02: `test -d wiki/entities && test -d wiki/concepts && test -d wiki/sources && test -d wiki/comparisons && test -d wiki/overviews` (wiki subdirs exist)
    - DIRS-03: `ls -d sources/ wiki/` (both exist as siblings)
    - DIRS-04: `git log --oneline -1` (git-tracked repository)

    **Schema content checks (grep against AGENTS.md):**
    - SCHM-01: File exists and has 800+ lines (`wc -l AGENTS.md`)
    - SCHM-02: Contains all four workflow names: `grep -c "Ingest Workflow\|Query Workflow\|Lint Workflow\|Reflect Workflow" AGENTS.md` >= 4
    - SCHM-03: Contains all five page types: grep for `entity`, `concept`, `source`, `comparison`, `overview` each with type definition
    - SCHM-04: Contains all 15 base frontmatter fields from D-10: `id`, `title`, `type`, `status`, `summary`, `created_at`, `updated_at`, `sources`, `epistemic_status`, `tags`, `domains`, `supersedes`, `superseded_by`, `privacy`, `aliases`
    - SCHM-05: Contains all four operations: `UPDATE`, `MERGE`, `SUPERSEDE`, `ARCHIVE`

    **Obsidian compatibility checks:**
    - OBSD-01: Contains `[[` wikilink syntax examples
    - OBSD-02: Contains `snake_case` field naming rule and ISO 8601 date format
    - OBSD-03: Contains first-mention linking rule
    - OBSD-04: Contains Dataview query examples with `FROM "wiki/`

    **Progressive disclosure checks:**
    - PROG-01: Contains `TL;DR` section convention
    - PROG-02: Contains per-type section orderings (at least `Key Facts`, `Detail`, `Sources`)
    - PROG-03: Contains explicit instruction to read index first and scan shallow sections

    **Boundary checks:**
    - BNDY-01: Contains scaling tiers distinguishing markdown-first from upgrades
    - BNDY-02: Contains `local_only` and `cloud_safe` privacy tiers
    - BNDY-03: Contains word "heuristic" in scaling section

    Print a summary table of all 19 requirements with PASS/FAIL status.
    If any FAIL, document what's missing so it can be fixed before the human review.
  </action>
  <verify>
    <automated>test -f AGENTS.md && test -d sources && test -d wiki/entities && test -d wiki/concepts && test -d wiki/sources && test -d wiki/comparisons && test -d wiki/overviews && test -f wiki/index.md && test -f wiki/log.md && grep -q "UPDATE" AGENTS.md && grep -q "MERGE" AGENTS.md && grep -q "SUPERSEDE" AGENTS.md && grep -q "ARCHIVE" AGENTS.md && grep -q "local_only" AGENTS.md && grep -q "heuristic" AGENTS.md && echo "ALL CHECKS PASS"</automated>
  </verify>
  <acceptance_criteria>
    - All 19 requirement checks report PASS
    - No missing directories or files
    - AGENTS.md contains all required sections and conventions
  </acceptance_criteria>
  <done>All 19 Phase 1 requirements validated via automated checks. Summary table printed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review of AGENTS.md and vault structure</name>
  <files>AGENTS.md, wiki/index.md, wiki/log.md</files>
  <action>
    Present the complete Phase 1 deliverables for human review:
    1. AGENTS.md -- 16-section agent-agnostic schema document (~800-1200 lines) at repo root
    2. Directory skeleton: sources/, wiki/ (with entities/, concepts/, sources/, comparisons/, overviews/, index.md, log.md), schema/templates/
    3. Git-tracked repository with commit conventions documented

    Human should verify in Obsidian:
    1. Open the repository in Obsidian (File -> Open vault -> select the `life/` directory)
    2. Check that `wiki/index.md` and `wiki/log.md` render with valid Properties in the sidebar
    3. Check that the directory structure appears as expected in Obsidian's file explorer
    4. Read through AGENTS.md -- confirm it reads as a complete, coherent specification:
       - Does the schema make sense as instructions for an LLM?
       - Are the workflows prescriptive enough (step-by-step)?
       - Do the frontmatter conventions look right?
       - Does the provenance syntax make sense?
       - Are the scaling boundaries described as heuristics (not hard rules)?
       - Is the privacy routing fail-closed?
    5. Check that Dataview query examples in Appendix A look syntactically correct
    6. Confirm the commit convention examples match what you want
  </action>
  <verify>Human types "approved" or describes issues</verify>
  <done>Human has reviewed and approved the complete Phase 1 deliverables</done>
</task>

</tasks>

<verification>
1. Automated validation passes all 19 requirement checks
2. Human confirms AGENTS.md reads as a coherent, complete specification
3. Human confirms directory structure renders correctly in Obsidian
4. Human confirms frontmatter on index.md and log.md is valid
</verification>

<success_criteria>
- All 19 Phase 1 requirements pass automated validation
- Human approves AGENTS.md as the authoritative schema for the wiki
- No missing conventions, workflows, or structural elements
- Phase 1 is ready for formal verification
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-structure-conventions/01-03-SUMMARY.md`
</output>
