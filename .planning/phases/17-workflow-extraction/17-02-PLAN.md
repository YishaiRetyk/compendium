---
phase: 17-workflow-extraction
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - schema/workflows/ingest.md
  - schema/workflows/query.md
  - CLAUDE.md
  - AGENTS.md
  - schema/AGENTS.template.md
autonomous: true
requirements: [WF-03, WF-04]
must_haves:
  truths:
    - "schema/workflows/ingest.md holds the full §11.1 ingest workflow procedure ABOVE the Plan-01 folded §10 blocks"
    - "schema/workflows/query.md holds the §11.2 query workflow body verbatim (write-back rules, privacy tier, delta compilation, query log format, worked example)"
    - "core §11.1 and §11.2 are removed from the §11 region; core §11 keeps ONLY the write-back-mandatory residue + routing dispatch"
    - "every cross-file §N / Section N reference inside both relocated bodies is rewritten to a path"
    - "the bare per-workflow log-entry format stays INLINE in each workflow file (ingest step 9 header, query log entry format)"
    - "AGENTS.md byte-identical to CLAUDE.md; stub edits mirrored to schema/AGENTS.template.md"
  artifacts:
    - path: "schema/workflows/ingest.md"
      provides: "Full ingest workflow procedure + folded §10 blocks"
      contains: "Ingest Workflow"
    - path: "schema/workflows/query.md"
      provides: "Extracted §11.2 query workflow"
      contains: "Write-Back Rules"
  key_links:
    - from: "schema/workflows/query.md write-back apply step"
      to: "schema/workflows/structured-operations.md"
      via: "structured-operations cross-link (was 'Section 9')"
      pattern: "schema/workflows/structured-operations.md"
    - from: "CLAUDE.md §11 stub"
      to: "schema/workflows/ingest.md"
      via: "routing dispatch"
      pattern: "schema/workflows/ingest.md"
---

<objective>
Extract the §11.1 ingest workflow body into `schema/workflows/ingest.md` (prepended ABOVE the §10 blocks
Plan 01 folded in), and extract the §11.2 query workflow into `schema/workflows/query.md`. Reduce the core
§11 region: §11.1 and §11.2 bodies move out; the core §11 stub keeps ONLY the write-back-mandatory line
(the unscriptable-judgment residue) plus routing dispatch to the per-workflow files.

Purpose: WF-03 (ingest → ingest.md + already-folded §10 claim-granularity) and WF-04 (query → query.md;
core keeps only write-back-mandatory line).
Output: completed ingest.md, new query.md, reduced core §11.1/§11.2, mirrored to AGENTS.md + template.

Wave 2 — depends on Plan 01 (ingest.md must already exist with the folded §10 blocks; query.md cross-links
structured-operations.md created in Plan 01). Shares CLAUDE.md with Plans 01/03 → sequential.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-workflow-extraction/17-CONTEXT.md
@.planning/phases/17-workflow-extraction/17-PATTERNS.md

<interfaces>
<!-- ingest.md ALREADY EXISTS from Plan 01 with the two §10 blocks + a bridging note. -->
<!-- This plan PREPENDS the ingest workflow procedure above those blocks and removes the bridging note. -->
<!-- Extracted-file skeleton + See Also footer pattern: see schema/reference/provenance.md L1-4 / L186-190. -->
<!-- The core §11 stub keeps ONLY the write-back-mandatory residue — see 17-PATTERNS.md "§11 stub". -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete schema/workflows/ingest.md with the §11.1 ingest workflow procedure</name>
  <files>schema/workflows/ingest.md</files>
  <read_first>
    - schema/workflows/ingest.md (the Plan-01 seed: header + the two folded §10 blocks + bridging note)
    - CLAUDE.md lines 396–432 (the full §11.1 ingest workflow body being relocated)
    - schema/reference/provenance.md lines 186–190 (See Also footer analog)
    - 17-PATTERNS.md "Per-workflow bare log-format inline (WF-07)" rule
  </read_first>
  <action>
    Open the existing `schema/workflows/ingest.md` (created by Plan 01). REMOVE the Plan-01 bridging note
    ("> **Note:** The step-by-step ingest procedure is added by Plan 02...").

    Insert the full §11.1 ingest workflow procedure BETWEEN the header and the `## Claim Granularity Rules`
    block. Relocate VERBATIM from current CLAUDE.md L396–432:
    - The fenced `Trigger/Inputs/Outputs/Commit` block (L398–403)
    - The `**Steps:**` list, steps 1–10 including the 6a compilation-tracking sub-bullets and the 9a
      contributor-attribution sub-block (L405–426)
    - `**Abort conditions:**` (L428–432)

    Keep the bare inline log-entry format in step 9 (`## [YYYY-MM-DD] ingest | <source title>`) INLINE here
    (WF-07: bare format stays per-workflow; the consolidated reference goes to log-format.md in Plan 03).

    §N→path conversions inside the relocated body (D-05):
    - step 5 "claim granularity rules from Section 10 Pass 2" → "claim granularity rules in this file (see Claim Granularity Rules below)" (now intra-file, since the block is folded into this same file)
    - step 5 "(or `wiki-local/sources/` if content derives from a local source -- §13)" → "(or `wiki-local/sources/` if content derives from a local source -- see `schema/reference/privacy.md`)"
    - step 6 "using UPDATE operations (Section 9)" → "using UPDATE operations (see `schema/workflows/structured-operations.md`)"
    - step 6 "append-then-synthesize policy (Section 10 Pass 3)" → "append-then-synthesize policy (see Incremental Update Policy below)" (intra-file)
    - step 6 "Generate wikilinks on first mention" — if it cites §8, convert to `schema/reference/wikilinks.md`
    - step 9a "(see §12)" → "(see `schema/reference/log-format.md`)"
    - Grep the finished file for `§[0-9]` / `Section [0-9]`; convert every cross-file hit. Intra-file
      forward-references ("see Claim Granularity Rules below") are fine.

    Ensure the file ends with the See Also footer Plan 01 already added (keep it; update if needed so it
    references structured-operations.md, provenance.md, log-format.md).
  </action>
  <verify>
    <automated>grep -q '# Ingest Workflow' schema/workflows/ingest.md && grep -q 'Trigger:  User places a new source' schema/workflows/ingest.md && grep -q 'Abort conditions' schema/workflows/ingest.md && grep -q 'Claim Granularity Rules' schema/workflows/ingest.md && ! grep -qE '§[0-9]|Section [0-9]' schema/workflows/ingest.md && ! grep -q 'procedure is added by Plan 02' schema/workflows/ingest.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Trigger:  User places a new source document' schema/workflows/ingest.md` returns 1
    - `grep -q '## \[YYYY-MM-DD\] ingest | <source title>' schema/workflows/ingest.md` (bare log format stays inline)
    - `grep -q 'Contributor attribution (COLAB-03' schema/workflows/ingest.md` (9a sub-block relocated)
    - `grep -c 'procedure is added by Plan 02' schema/workflows/ingest.md` returns 0 (bridging note removed)
    - Both `## Claim Granularity Rules` and `## Incremental Update Policy` still present (Plan-01 folds intact)
    - `grep -qE '§[0-9]|Section [0-9]' schema/workflows/ingest.md` returns non-zero (no cross-file §N)
  </acceptance_criteria>
  <done>ingest.md is the complete ingest workflow: procedure + folded §10 blocks, bridging note gone, §N converted.</done>
</task>

<task type="auto">
  <name>Task 2: Extract §11.2 → schema/workflows/query.md (verbatim body, §N→path)</name>
  <files>schema/workflows/query.md</files>
  <read_first>
    - CLAUDE.md lines 434–544 (the full §11.2 query workflow body — steps, write-back rules, privacy tier, delta compilation, query log format, ordering, abort, worked example)
    - schema/reference/provenance.md lines 1–4, 186–190 (skeleton)
  </read_first>
  <action>
    Create `schema/workflows/query.md` (NO YAML frontmatter).

    Header:
    ```markdown
    # Query Workflow

    > Agent-authoritative reference for the query workflow: searching the wiki, synthesizing a cited answer, the mandatory write-back rules, privacy-tier routing, and delta compilation.
    > AGENTS.md §11.2 points here. If you find a discrepancy between this file and AGENTS.md, this file wins.
    ```

    Body: relocate VERBATIM from current CLAUDE.md L436–544:
    - the `Trigger/Inputs/Outputs/Commit` fenced block + `**Steps:**` 1–10
    - `#### Write-Back Rules`, `#### Privacy Tier for Write-Back`, `#### Delta Compilation`,
      `#### Query Log Entry Format` (keep the bare query-log format block INLINE here per WF-07),
      `**Ordering:**`, `**Abort conditions:**`, and `#### Worked Example` (including the
      "See: examples/kahneman/..." pointer line at L544 — that path ref is already correct, keep it).

    §N→path conversions (D-05):
    - step 7 "apply using structured operations (Section 9)" → "(see `schema/workflows/structured-operations.md`)"
    - Privacy Tier heading "(§13 asymmetric model)" → "(see `schema/reference/privacy.md` asymmetric model)"
    - Delta Compilation step 2 "`compilation_status` field (Section 5)" → "(see `schema/reference/frontmatter.md`)"
    - Any "Section 11.x" workflow cross-ref → its `schema/workflows/*.md` path
    - Grep finished file for `§[0-9]` / `Section [0-9]`; convert every cross-file hit.

    Footer:
    ```markdown
    ## See Also

    - [AGENTS.md](../../AGENTS.md) — §11 stub (write-back-mandatory residue + routing table).
    - `schema/workflows/structured-operations.md` — the UPDATE/CREATE operations write-back invokes.
    - `schema/reference/privacy.md` — the asymmetric tier rule governing write-back placement.
    - `schema/reference/log-format.md` — consolidated log entry format.
    ```
  </action>
  <verify>
    <automated>test -f schema/workflows/query.md && grep -q 'Write-Back Rules' schema/workflows/query.md && grep -q 'Privacy Tier for Write-Back' schema/workflows/query.md && grep -q 'Delta Compilation' schema/workflows/query.md && ! grep -qE '§[0-9]|Section [0-9]' schema/workflows/query.md && ! head -1 schema/workflows/query.md | grep -q '^---'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f schema/workflows/query.md` exits 0; line 1 is `# Query Workflow`
    - `grep -c 'Write-Back Rules' schema/workflows/query.md` returns 1
    - `grep -c 'Privacy Tier for Write-Back' schema/workflows/query.md` returns 1
    - `grep -q '## \[YYYY-MM-DD\] query | <question summary>' schema/workflows/query.md` (bare log format inline)
    - `grep -q 'schema/workflows/structured-operations.md' schema/workflows/query.md` (write-back §9 ref converted)
    - `grep -qE '§[0-9]|Section [0-9]' schema/workflows/query.md` returns non-zero
  </acceptance_criteria>
  <done>query.md holds the relocated §11.2 body with §N converted and a See Also footer.</done>
</task>

<task type="auto">
  <name>Task 3: Reduce core §11.1/§11.2 to routing dispatch + write-back-mandatory residue; mirror</name>
  <files>CLAUDE.md, AGENTS.md, schema/AGENTS.template.md</files>
  <read_first>
    - CLAUDE.md lines 392–545 (the §11 intro + §11.1 + §11.2 region being reduced)
    - CLAUDE.md lines 34–60 (the routing table — its "Workflows STILL INLINE until Phase 17" scaffold block; this plan begins promoting workflow rows but Plan 03 finishes the table cleanup)
    - 17-PATTERNS.md "§11 stub" + "Routing table" adaptation bullets
  </read_first>
  <action>
    In CLAUDE.md, within the `## 11. Workflows` region, REPLACE the `### 11.1 Ingest Workflow` body
    (L396–432) and the `### 11.2 Query Workflow` body (L434–544) with bare-pointer stubs. Keep the §11
    intro line (L392–394) and the `### 11.1` / `### 11.2` headers.

    §11.1 stub body:
    ```markdown
    ### 11.1 Ingest Workflow

    → See `schema/workflows/ingest.md` for the full ingest procedure (classify, diff, extract with provenance, merge, lint, log, commit) and the claim-granularity rules.
    ```

    §11.2 stub body — this is where the ONE resident write-back-mandatory line lives (WF-04: "core keeps
    ONLY the write-back-mandatory line"):
    ```markdown
    ### 11.2 Query Workflow

    **Write-back is mandatory** when a query produces novel or durable synthesis — a new claim, a new connection, a meaningful reframing, or a reusable artifact MUST be compiled back into the wiki (it is not optional).

    → See `schema/workflows/query.md` for the full query procedure, write-back rules, privacy-tier routing, and delta compilation.
    ```

    Routing table (L34–60): promote the Ingest and Query rows out of the "Workflows — STILL INLINE in §11
    until Phase 17" 2-column scaffold table into the main "Resolvable references" table (L41–50 form,
    keyed by operation + topic). Add rows:
    `| Ingesting a new source (classify → extract → merge) | \`schema/workflows/ingest.md\` |`
    `| Answering a question + write-back rules | \`schema/workflows/query.md\` |`
    Remove the Ingest and Query rows from the scaffold block. Leave the Lint and Reflect scaffold rows for
    Plan 03 to promote (Plan 03 deletes the entire scaffold block once all four are promoted). Do NOT delete
    the scaffold block yet — Lint/Reflect still resolve to inline §11.3/§11.4 until Plan 03 runs.

    Mirror: run `bash bin/sync-claude.sh` (CLAUDE.md → AGENTS.md) and apply the SAME edits to
    `schema/AGENTS.template.md`. Verify `bash bin/sync-claude.sh --check` exits 0.
  </action>
  <verify>
    <automated>bash bin/sync-claude.sh --check && grep -q 'schema/workflows/ingest.md' CLAUDE.md && grep -q 'schema/workflows/query.md' CLAUDE.md && grep -q 'Write-back is mandatory' CLAUDE.md && ! grep -q 'Trigger:  User places a new source document' CLAUDE.md && grep -q 'schema/workflows/query.md' schema/AGENTS.template.md</automated>
  </verify>
  <acceptance_criteria>
    - `bash bin/sync-claude.sh --check` exits 0 (byte-equality)
    - `grep -c 'Trigger:  User places a new source document' CLAUDE.md` returns 0 (§11.1 body moved out)
    - `grep -c '#### Write-Back Rules' CLAUDE.md` returns 0 (§11.2 body moved out)
    - `grep -q 'Write-back is mandatory' CLAUDE.md` (the one resident write-back line present)
    - Routing table "Resolvable references" section contains an `schema/workflows/ingest.md` row and a `schema/workflows/query.md` row
    - The scaffold "STILL INLINE in §11 until Phase 17" block no longer lists Ingest or Query (Lint/Reflect remain)
    - `grep -q 'schema/workflows/query.md' schema/AGENTS.template.md` (template mirrored)
  </acceptance_criteria>
  <done>Core §11.1/§11.2 are stubs; the one write-back-mandatory line is resident; ingest/query promoted into the routing table; all three files synced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relocated workflow text → template-public schema/workflows/ | Extracted ingest/query bodies are scanned by check-neutrality.sh; must stay neutral |
| CLAUDE.md ↔ AGENTS.md | Byte-equality gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Information disclosure | query.md worked example (cites examples/kahneman path) | accept | The worked example uses `<PLACEHOLDER>` tokens + the sanctioned `examples/kahneman/` path (path-only ref is exempt per established neutrality precedent); check-neutrality.sh is the backstop |
| T-17-05 | Tampering | CLAUDE.md vs AGENTS.md byte-equality | mitigate | Task 3 runs sync-claude --check (exit 0) before done |
| T-17-06 | Spoofing | partial routing-table promotion leaving a dangling/duplicated row | accept (caught downstream) | Plan 04 routing category catches orphan/dangling routing entries; Plan 03 finishes the table cleanup |
</threat_model>

<verification>
- `bash bin/sync-claude.sh --check` exits 0
- `bash bin/check-neutrality.sh` exits 0 over new schema/workflows files
- query.md and ingest.md both start with `# H1` (no frontmatter)
- No cross-file §N survives in either workflow file
- Core §11.2 retains exactly one resident write-back-mandatory statement
</verification>

<success_criteria>
- WF-03: ingest.md complete (procedure + folded §10 claim-granularity); core §11.1 is a pointer stub
- WF-04: query.md complete; core §11.2 keeps ONLY the write-back-mandatory line + a pointer
- Routing table now dispatches Ingest + Query to their files; CLAUDE.md ≡ AGENTS.md ≡ template
</success_criteria>

<output>
After completion, create `.planning/phases/17-workflow-extraction/17-02-SUMMARY.md` recording line ranges
relocated, §N→path conversions, routing-table rows added, and that sync-claude --check + check-neutrality exit 0.
</output>
