---
phase: 17-workflow-extraction
plan: 03
type: execute
wave: 3
depends_on: [17-02]
files_modified:
  - schema/workflows/lint.md
  - schema/workflows/reflect.md
  - schema/workflows/brownfield.md
  - schema/workflows/release.md
  - schema/workflows/audit.md
  - schema/reference/log-format.md
  - CLAUDE.md
  - AGENTS.md
  - schema/AGENTS.template.md
  - docs/reference/ci.md
  - CONTRIBUTING.md
  - docs/reference/agent-parity.md
autonomous: true
requirements: [WF-05, WF-06, WF-07]
must_haves:
  truths:
    - "schema/workflows/lint.md holds the §11.3 lint-workflow body merged AFTER the existing Phase-16 decay/staleness seed, with the 'source of truth for CI contracts' framing preserved verbatim"
    - "schema/workflows/brownfield.md holds the §11.5 brownfield-workflow body verbatim (the 182-line file: all five subcommands, bootstrap_stage lifecycle, applied.log shapes, architectural-boundary table)"
    - "schema/workflows/reflect.md, release.md, audit.md each hold their §11.4/11.6/11.7 body (audit.md also folds the §1 audit-framing paragraph)"
    - "schema/reference/log-format.md holds the consolidated §12 index/log format reference, INCLUDING the reciprocal note (review HIGH #3) flagging the compact core structured-op log form as a dispatch summary of this file's canonical multi-line entry"
    - "core §11.3/11.4/11.5/11.6/11.7 and §12 reduced to bare-pointer stubs; routing table scaffold block deleted (all four workflows promoted)"
    - "the six EXISTING Phase-16 schema files (page-types/frontmatter/provenance/wikilinks/privacy + lint.md) have their §N backlinks repointed to dispatch/path wording (Task 0 — the prerequisite for Plan 04's routing guard)"
    - "external referrers (docs/reference/ci.md, CONTRIBUTING.md, docs/reference/agent-parity.md) have their §N refs repointed to schema/ paths (D-08 one-shot)"
    - "the post-extraction CORE (AGENTS.md/CLAUDE.md) is itself §N-clean — the residual resident-core cross-file §N refs (§1 L30/L32, §2 L67/L98) are converted to paths/prose; zero §N glyph-refs or 'Section N' prose refs survive in core (section HEADINGS exempt)"
    - "every cross-file §N inside the relocated bodies is rewritten to a path; the 'Source of truth for Phase 9 / Phase 12.2 CI' phrase appears EXACTLY ONCE in lint.md (banner paraphrased); AGENTS.md byte-identical; template mirrored"
  artifacts:
    - path: "schema/workflows/lint.md"
      provides: "Full lint workflow (decay seed + §11.3 body + CI-contract source-of-truth framing)"
      contains: "Source of truth for"
    - path: "schema/workflows/brownfield.md"
      provides: "Extracted §11.5 brownfield workflow (182 lines)"
      contains: "Brownfield Workflow"
    - path: "schema/reference/log-format.md"
      provides: "Consolidated §12 index/log format"
      contains: "Structured Operation Log Entries"
  key_links:
    - from: "docs/reference/ci.md"
      to: "schema/workflows/lint.md"
      via: "repointed source-of-truth link (was AGENTS.md §11.3)"
      pattern: "schema/workflows/lint.md"
    - from: "CLAUDE.md §11.3 stub"
      to: "schema/workflows/lint.md"
      via: "routing dispatch"
      pattern: "schema/workflows/lint.md"
---

<objective>
The heaviest extraction plan. (1) MERGE the §11.3 lint-workflow body INTO the existing
`schema/workflows/lint.md` seed (WF-05), preserving its "source of truth for CI contracts" framing.
(2) Extract §11.4 reflect, §11.5 brownfield (the 182-line file), §11.6 release, §11.7 audit into
`schema/workflows/*.md` (WF-06). (3) Extract §12 → `schema/reference/log-format.md` (WF-07). (4) Reduce all
remaining core §11.x + §12 to stubs and DELETE the routing-table scaffold block (all four workflows now
promoted). (5) One-shot repoint the external referrers (ci.md, CONTRIBUTING.md, agent-parity.md) from
`§11.3`/`§N` to extracted paths (D-08).

Purpose: WF-05, WF-06, WF-07 + the D-08 external repoint that lets Plan 04's `routing` error-category go green.
Output: 4 workflow files (1 extended, 3 new) + log-format.md + reduced core + repointed external docs.

Wave 3 — depends on Plan 02 (routing table half-promoted; this plan finishes it). Final shared-core edit
before the routing guard is built in Plan 04. After this plan, ZERO cross-file §N may remain in schema/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-workflow-extraction/17-CONTEXT.md
@.planning/phases/17-workflow-extraction/17-PATTERNS.md

<interfaces>
<!-- schema/workflows/lint.md ALREADY EXISTS (Phase-16 seed: decay table + staleness auto-fix only). -->
<!-- WF-05 MERGES the §11.3 body INTO it — do NOT create a second lint file. -->
<!-- The §11.3 "CI mode" subsection carries the load-bearing framing that ci.md/CONTRIBUTING.md/lint.yml link to: -->
<!--   "Source of truth for Phase 9 / Phase 12.2 CI + local-gate contracts. ... Other docs ... MUST link here -->
<!--    rather than restating the policy." — this MUST survive verbatim. -->
<!-- External referrers needing §N→path repoint (verified via grep -rln '§[0-9]'): -->
<!--   docs/reference/ci.md, CONTRIBUTING.md, docs/reference/agent-parity.md -->
<!--   (.github/workflows/lint.yml has NO §N refs — confirmed; no change needed there). -->
<!--
  UNIFIED §N POLICY (review HIGH #1 — §N policy inconsistency + unrepointed Phase-16 backlinks):
  After this plan, ZERO cross-file §N / Section N may survive ANYWHERE in the routing corpus
  (AGENTS.md + schema/reference/*.md + schema/workflows/*.md). That includes the EXISTING Phase-16 files,
  which today carry 14 §N backlinks in their headers/footers (and 2 in frontmatter.md body prose). Plan 04's
  routing guard rejects ALL of them — so a NEW Task 0 below repoints every one of them BEFORE the guard is
  built. The header/footer backlink form is "The AGENTS.md routing table points here" / "routing-table stub"
  (dispatch wording, NO §N), matching Plans 01/02. There is NO {1,2,3} resident-section exemption — the
  resident core sections (§1/§2/§3) live in AGENTS.md and are never referenced cross-file from schema/ files;
  any §N in a schema/ file is a forbidden cross-file ref, full stop. (This supersedes the earlier draft note
  that distinguished resident §1/§2/§3 refs — there are none in the schema tree to preserve.)

  VERIFIED EXISTING §N WORKLIST (grep -rnE '§[0-9]|Section [0-9]' schema/reference/ schema/workflows/):
    schema/reference/page-types.md  L4 (header §4), L152 (footer §4 stub)
    schema/reference/frontmatter.md L4 (header §5), L29 ("Section 6 decay table"), L55 ("see §6 PROV-01..05" + "§11.5 Brownfield Workflow"), L148 (footer §5 stub)
    schema/reference/provenance.md  L4 (header §6), L188 (footer §6 stub)
    schema/reference/wikilinks.md   L4 (header §8 + §3 Red Links), L61 (footer §8 stub)
    schema/reference/privacy.md     L4 (header §13), L24 (footer §13 stub)
    schema/workflows/lint.md        L5 (header §6), L49 (footer §6 stub)
-->
<!--
  DUPLICATE SOURCE-OF-TRUTH STRING (review HIGH #2): the phrase "Source of truth for Phase 9 / Phase 12.2 CI"
  appears EXACTLY ONCE in CLAUDE.md today (L571). Part A relocates that L571 blockquote verbatim into lint.md
  AND the new banner restates it → 2 occurrences → the `== 1` acceptance FAILS. FIX: keep the phrase exactly
  once. Strategy below: the banner uses a PARAPHRASE (no verbatim "Source of truth for Phase 9 / Phase 12.2 CI"
  string), and the relocated L571 body block keeps the verbatim phrase. Acceptance asserts count == 1.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Repoint the existing Phase-16 §N backlinks in the schema/ tree (review HIGH #1 — unblocks Plan 04's routing guard)</name>
  <files>schema/reference/page-types.md, schema/reference/frontmatter.md, schema/reference/provenance.md, schema/reference/wikilinks.md, schema/reference/privacy.md, schema/workflows/lint.md</files>
  <read_first>
    - schema/reference/page-types.md lines 1–5 and 150–155 (header §4 + footer §4 stub)
    - schema/reference/frontmatter.md lines 1–5, 29, 55, 145–150 (header §5, body "Section 6", body "§6 PROV"/"§11.5 Brownfield", footer §5 stub)
    - schema/reference/provenance.md lines 1–5 and 186–190 (header §6 + footer §6 stub)
    - schema/reference/wikilinks.md lines 1–5 and 58–62 (header §8/§3 + footer §8 stub)
    - schema/reference/privacy.md lines 1–5 and 22–26 (header §13 + footer §13 stub)
    - schema/workflows/lint.md lines 1–6 and 47–50 (header §6 + footer §6 stub)
  </read_first>
  <action>
    These six Phase-16 files predate this phase's unified §N policy and still carry `§N`-in-backlink form.
    Plan 04's `routing` guard (Wave 4) rejects EVERY cross-file `§N` in the schema tree — so they MUST be
    repointed now, BEFORE the guard is built. This is a NON-NEGOTIABLE prerequisite: skipping it makes
    `bash bin/lint.sh --category routing` impossible to bring to exit 0 (review Consensus Concern 1).

    Apply these EXACT conversions (header/footer → dispatch wording; body prose → path):

    page-types.md:
    - L4  `> AGENTS.md §4 points here. If you find a discrepancy...` → `> The AGENTS.md routing table points here. If you find a discrepancy...`
    - L152 `- [AGENTS.md](../../AGENTS.md) -- §4 stub (dispatch pointer to this file).` → `- [AGENTS.md](../../AGENTS.md) -- routing-table stub (dispatch pointer to this file).`

    frontmatter.md:
    - L4  `> AGENTS.md §5 points here.` → `> The AGENTS.md routing table points here.`
    - L29 inline comment `(maps to Section 6 decay table)` → `(maps to the decay table in \`schema/workflows/lint.md\`)`
    - L55 `**NOT a substitute for claim-level provenance (see §6 PROV-01..05)**` → `**NOT a substitute for claim-level provenance (see \`schema/reference/provenance.md\` PROV-01..05)**`; and in the same cell `See \`§11.5 Brownfield Workflow\` (populated in Phase 11).` → `See \`schema/workflows/brownfield.md\` (populated in Phase 11).`
    - L148 `-- §5 stub (pointer to this file).` → `-- routing-table stub (pointer to this file).`

    provenance.md:
    - L4  `> AGENTS.md §6 (syntax/epistemics portion) points here.` → `> The AGENTS.md routing table points here (syntax/epistemics portion).`
    - L188 `— §6 stub (pointer to this file and to lint.md for decay math).` → `— routing-table stub (pointer to this file and to \`schema/workflows/lint.md\` for decay math).`

    wikilinks.md:
    - L4  `> AGENTS.md §8 and §3 Red Links point here. This file carries...` → `> The AGENTS.md routing table (wikilinks + Red Links) points here. This file carries...`
    - L61 `— §8 stub (pointer to this file).` → `— routing-table stub (pointer to this file).`

    privacy.md:
    - L4  `> AGENTS.md §13 points here.` → `> The AGENTS.md routing table points here.`
    - L24 `— §13 stub (pointer to this file).` → `— routing-table stub (pointer to this file).`

    lint.md (Task 1 Part A overwrites this banner anyway, but repoint the footer now so this task leaves the
    file §N-clean independently):
    - L5  `> AGENTS.md §6 decay/staleness content points here.` → `> The AGENTS.md routing table (decay/staleness) points here.`
    - L49 `— §6 stub (decay/staleness pointer to this file).` → `— routing-table stub (decay/staleness pointer to this file).`

    After editing, confirm the schema tree is §N-free EXCEPT for lint.md (which Task 1 then extends with the
    §11.3 body and re-cleans): `grep -rnE '§[0-9]|Section [0-9]' schema/reference/` MUST return zero lines.
    (lint.md is checked at the end of Task 1, not here, because Task 1 appends new body that itself needs conversion.)
  </action>
  <verify>
    <automated>[ "$(grep -rnE '§[0-9]|Section [0-9]' schema/reference/ | wc -l)" -eq 0 ] && ! grep -qE '§[0-9]|Section [0-9]' schema/reference/page-types.md && ! grep -qE '§[0-9]|Section [0-9]' schema/reference/frontmatter.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE '§[0-9]|Section [0-9]' schema/reference/` returns zero lines (all 5 reference files §N-free)
    - `grep -q 'The AGENTS.md routing table points here' schema/reference/page-types.md` (header repointed to dispatch wording)
    - `grep -q 'schema/workflows/brownfield.md' schema/reference/frontmatter.md` (the `§11.5 Brownfield Workflow` body ref converted to a path)
    - `grep -q 'schema/workflows/lint.md' schema/reference/frontmatter.md` (the `Section 6 decay table` body ref converted to a path)
    - `grep -c 'routing-table stub' schema/reference/privacy.md` returns 1 (footer repointed)
  </acceptance_criteria>
  <done>All six Phase-16 schema files repointed: zero §N in schema/reference/; lint.md footer repointed (its body is re-cleaned in Task 1). Plan 04's routing guard can now reach exit 0.</done>
</task>

<task type="auto">
  <name>Task 1: WF-05 — Merge §11.3 lint body INTO schema/workflows/lint.md; repoint external referrers</name>
  <files>schema/workflows/lint.md, docs/reference/ci.md, CONTRIBUTING.md</files>
  <read_first>
    - schema/workflows/lint.md (the full Phase-16 seed — decay table, epistemic modifiers, hash override, staleness auto-fix, current See Also at L47-50)
    - CLAUDE.md lines 546–651 (the §11.3 lint workflow body: Severity Tiers, Auto-Fix Boundary, CI mode block, escape-hatch syntax, staged-mode rules, 15 numbered steps, Categories, Abort)
    - CLAUDE.md line 571 (the "Source of truth for Phase 9 / Phase 12.2 CI + local-gate contracts" framing line — preserve verbatim)
    - docs/reference/ci.md (lines 17, 26, 46, 93, 111, 112, 122, 130, 300 — the §N referrers)
    - CONTRIBUTING.md (lines 12, 28, 56, 114, 132 — the §N referrers)
  </read_first>
  <action>
    PART A — Merge the §11.3 body into lint.md (do NOT create a new file):
    Update the lint.md banner (L1–5): drop the "added in Phase 17" reservation note. The banner uses
    DISPATCH wording (NO `§N` token) and a PARAPHRASE of the CI-contract framing — it MUST NOT contain the
    verbatim string "Source of truth for Phase 9 / Phase 12.2 CI" (that exact phrase is reserved for the
    relocated L571 body blockquote, which keeps it verbatim — review HIGH #2: keep the phrase exactly once).
    New banner:
    ```markdown
    # Lint Workflow

    > Agent-authoritative reference for the lint workflow: the decay/staleness model, severity tiers, the `bin/lint.sh` CI-mode contract, and the 15-step lint procedure.
    > This file is the authoritative specification for the CI + local-gate contracts (the severity-remap table, `--format json` schema, escape-hatch markers, `--require-version`, and the `--staged` write-gate). `docs/reference/ci.md`, `CONTRIBUTING.md`, and `.github/workflows/lint.yml` link here rather than restating the policy.
    > The AGENTS.md routing table (decay/staleness + lint workflow) points here. If you find a discrepancy between this file and AGENTS.md, this file wins.
    ```
    (Note the banner says "This file is the authoritative specification for the CI + local-gate contracts" —
    a PARAPHRASE — NOT the verbatim "Source of truth for Phase 9 / Phase 12.2 CI" string. The verbatim string
    lives ONCE, in the relocated L571 blockquote below.)

    KEEP the existing decay/staleness body (Domain-Based Decay Rate Table through Staleness Auto-Fix Rules).
    AFTER it, append the relocated §11.3 body VERBATIM from CLAUDE.md L555–651: Severity Tiers table,
    Auto-Fix Boundary, the `#### CI mode (Phase 9)` block (INCLUDING the L571 source-of-truth blockquote —
    verbatim, the load-bearing framing; this is the SINGLE occurrence of the "Source of truth for Phase 9 /
    Phase 12.2 CI" phrase in the finished file), the flags table, escape-hatch marker syntax, CI workflow
    reference, Staged-mode rules, Failure UX, the 15 numbered Steps, Categories list, Abort conditions.

    §N→path conversions inside the relocated §11.3 body (D-05):
    - Severity Tiers / step 2 "Section 5 schema" → "`schema/reference/frontmatter.md`"
    - step 6 "domain decay rate table (Section 6)" → "the decay table above (this file)" (intra-file now)
    - step 3 / step 7 references to provenance/contradiction "Section 6" → "`schema/reference/provenance.md`"
    - DRFT-04 "§11.2 Query Log Entry Format" → "`schema/workflows/query.md` Query Log Entry Format"
    - any "Section 9" / "Section 11.4" → the respective `schema/workflows/*.md` path
    - Grep the finished lint.md for `§[0-9]` / `Section [0-9]`; convert every cross-file hit. (The CI-mode
      block's own self-references like "see Staged-mode rules below" are intra-file — leave them.)

    Rewrite the existing See Also (L47–50) to use dispatch wording — the single backlink line becomes
    `- [AGENTS.md](../../AGENTS.md) — routing-table stub (decay/staleness + lint workflow pointer to this file).`
    (NO `§6`/`§11.3` token — the lint workflow is now reached via the routing table, not a section number).

    PART B — One-shot external repoint (D-08): in `docs/reference/ci.md` and `CONTRIBUTING.md`, replace every
    `AGENTS.md §11.3` link with `schema/workflows/lint.md` (drop the §-anchor — D-05 abolishes cross-file
    §-refs), keeping the "source of truth ... if you find a discrepancy, X wins" framing but swapping the
    target. Also repoint the other §N refs found by grep in those two files:
    - ci.md L26 `§6` → `schema/reference/provenance.md`; L93/L300 `§13` → `schema/reference/privacy.md`;
      L111 `§4.6` / L112 `§4.3/§4.6` → `schema/reference/page-types.md` + `schema/reference/frontmatter.md`.
    - CONTRIBUTING.md L12 `§11.1` → `schema/workflows/ingest.md`; L56 `§13` → `schema/reference/privacy.md`.
    Use the relative path form those files already use for links (e.g. `[schema/workflows/lint.md](../../schema/workflows/lint.md)` from docs/reference/; `schema/workflows/lint.md` bare-path from CONTRIBUTING.md at repo root).
    Confirm with `grep -rn '§[0-9]' docs/reference/ci.md CONTRIBUTING.md` returning zero lines.
  </action>
  <verify>
    <automated>[ "$(grep -c 'Source of truth for Phase 9 / Phase 12.2 CI' schema/workflows/lint.md)" -eq 1 ] && grep -q 'CI mode' schema/workflows/lint.md && grep -q 'Staged-mode rules' schema/workflows/lint.md && grep -q 'Domain-Based Decay Rate Table' schema/workflows/lint.md && ! grep -qE '§[0-9]|Section [0-9]' schema/workflows/lint.md && [ "$(grep -rc '§[0-9]' docs/reference/ci.md CONTRIBUTING.md | grep -v ':0$' | wc -l)" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Source of truth for Phase 9 / Phase 12.2 CI' schema/workflows/lint.md` returns EXACTLY 1 (review HIGH #2: phrase preserved verbatim in the relocated L571 body block, paraphrased in the banner — not duplicated)
    - `grep -c 'Domain-Based Decay Rate Table' schema/workflows/lint.md` returns 1 (Phase-16 seed retained — single file, no second lint file)
    - `grep -q 'Staged-mode rules' schema/workflows/lint.md` and `grep -q 'Escape-hatch marker syntax' schema/workflows/lint.md`
    - `ls schema/workflows/lint-workflow.md 2>/dev/null` fails (no second lint file created)
    - `grep -qE '§[0-9]|Section [0-9]' schema/workflows/lint.md` returns non-zero (no cross-file §N)
    - `grep -rn '§[0-9]' docs/reference/ci.md CONTRIBUTING.md` returns zero matching lines
    - `grep -q 'schema/workflows/lint.md' docs/reference/ci.md` and `grep -q 'schema/workflows/lint.md' CONTRIBUTING.md`
  </acceptance_criteria>
  <done>lint.md is the single merged lint workflow (seed + §11.3 body + CI-contract framing); ci.md/CONTRIBUTING.md repointed to paths; no §N anywhere in these files.</done>
</task>

<task type="auto">
  <name>Task 2: WF-06 — Extract §11.4 reflect, §11.5 brownfield, §11.6 release, §11.7 audit → schema/workflows/*.md</name>
  <files>schema/workflows/reflect.md, schema/workflows/brownfield.md, schema/workflows/release.md, schema/workflows/audit.md</files>
  <read_first>
    - CLAUDE.md lines 653–724 (§11.4 reflect: three-tier model, checkpoint, periodic procedure, abort, unifying principle)
    - CLAUDE.md lines 725–906 (§11.5 brownfield — the full 182-line workflow)
    - CLAUDE.md lines 907–910 (§11.6 release — already a near-pointer)
    - CLAUDE.md lines 911–938 (§11.7 audit) + line 32 (the §1 audit-framing sentence: "These four operations are the wiki's mutation vocabulary. Layered on top is the **Audit** ... four-operation framing is preserved." — L23-31 above it is the four-operations table, NOT the audit framing; fold ONLY the L32 sentence-set)
    - schema/reference/provenance.md lines 1–4, 186–190 (skeleton)
  </read_first>
  <action>
    Create four files, each NO YAML frontmatter, each with the standard header + See Also footer.
    ALL headers/footers use DISPATCH wording ("The AGENTS.md routing table points here" / "routing-table
    stub"), NO `§N` token — per the unified §N policy. ALL body cross-file §N → paths.

    `schema/workflows/reflect.md`:
    - Header: `# Reflect Workflow` / "> Agent-authoritative reference for the reflect workflow: the three-tier decision-record model, the reflect checkpoint, and the periodic backfill procedure." / "> The AGENTS.md routing table points here. If you find a discrepancy between this file and AGENTS.md, this file wins."
    - Body: relocate §11.4 verbatim (L655–724). §N conversions: "decision records (see Section 4.6)" → "`schema/reference/page-types.md` (decision page type)"; "decision template (Section 4.6)" → same; "See Section 9 operation definitions" → "`schema/workflows/structured-operations.md`"; "inline hooks (steps 7a and 6a)" stays (refers to the structured-operations file content — reword to "(see the MERGE/SUPERSEDE inline-DR hooks in `schema/workflows/structured-operations.md`)"). Grep + convert all cross-file §N.
    - See Also (dispatch wording): routing-table stub; structured-operations.md (inline DR hooks), page-types.md (decision type).

    `schema/workflows/brownfield.md` (the 182-line miss):
    - Header: `# Brownfield Workflow` / scope line / "> The AGENTS.md routing table points here. ... this file wins."
    - Body: relocate §11.5 verbatim (L725–906) — all five subcommands, bootstrap_stage lifecycle, applied.log
      shapes, the architectural-boundary table. §N conversions: any "Section N" / "§N" → path (e.g. "§4" type
      enum → `schema/reference/page-types.md`; "§13" → `schema/reference/privacy.md`; "Phase 9" historical
      labels are NOT cross-file refs — leave them). Grep + convert. NOTE: the brownfield body contains intra-
      file references like "(§11.5.1 suggest)" pointing at its OWN sub-headings — these are intra-FILE now
      (the whole §11.5 lives in this one file); reword them to the section name ("see the suggest subcommand
      below") so no `§N` token survives the negative grep.
    - See Also (dispatch wording): routing-table stub; `schema/brownfield/migrations/README.md`; `docs/reference/brownfield.md`.

    `schema/workflows/release.md`:
    - Header: `# Release Workflow (Orphan-Branch Publish)` / scope / "> The AGENTS.md routing table points here. ... this file wins."
    - Body: relocate §11.6 verbatim (L909): the one paragraph. It already points to `docs/reference/release.md`
      (keep that path). No §N present.
    - See Also (dispatch wording): routing-table stub; `docs/reference/release.md`.

    `schema/workflows/audit.md`:
    - Header: `# Audit Workflow` / scope / "> The AGENTS.md routing table points here. ... this file wins."
    - Body: FIRST fold in the §1 audit-framing sentence (CLAUDE.md L32, the "Layered on top is the Audit
      ... four-operation framing is preserved" sentence-set — relocate the audit-specific framing, NOT the
      whole §1 four-operations table at L23-31) as an intro, THEN relocate §11.7 verbatim (L913–937). §N conversions:
      "(Section 1)" → reword to "(preserving the four mutation operations: Ingest / Query / Lint / Reflect)" — descriptive prose, NO `§1` token (unified policy: zero §N in schema files); "Section 11.4 Tier-2" → "`schema/workflows/reflect.md` Tier-2"; "§13 structural predicate" → "`schema/reference/privacy.md` structural predicate"; "Section 6" marker refs → "`schema/reference/provenance.md`"; "Lint mechanics (Section 11.3)" → "`schema/workflows/lint.md`". Grep + convert all.
    - See Also (dispatch wording): routing-table stub; lint.md (twin report pattern); privacy.md (FAITH-04); provenance.md (markers).
  </action>
  <verify>
    <automated>for f in reflect release audit; do test -f schema/workflows/$f.md || { echo "MISSING $f"; exit 1; }; done && test -f schema/workflows/brownfield.md && grep -q 'Three-Tier Reflect Model' schema/workflows/reflect.md && grep -q 'bootstrap_stage Lifecycle' schema/workflows/brownfield.md && grep -q 'review-only' schema/workflows/audit.md && for f in reflect brownfield release audit; do ! grep -qE '§[0-9]|Section [0-9]' schema/workflows/$f.md || { echo "STRAY §N in $f"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist; each line 1 is its `# <Title>` (no `---`)
    - `grep -c 'Three-Tier Reflect Model' schema/workflows/reflect.md` returns 1
    - brownfield.md is substantively complete — assert by REQUIRED SUBCOMMAND HEADINGS, not a tight line count (review MEDIUM: header/footer + §N→path conversions shift the count): all five of `grep -q '#### 11.5.1 suggest\|## .*suggest' …`, plus `grep -q 'bootstrap_stage Lifecycle'`, `grep -q 'review-typing'`, `grep -q 'verify \[--promote\]'`, `grep -q 'applied.log'`, and `grep -q 'architectural boundary\|apply class vs advisory' schema/workflows/brownfield.md` succeed. (Soft sanity: `wc -l` ≥ 175 — a floor, not a tight window.)
    - `grep -c 'bootstrap_stage Lifecycle' schema/workflows/brownfield.md` returns 1
    - `grep -q 'review-only' schema/workflows/audit.md` and `grep -q 'FAITH-04' schema/workflows/audit.md`
    - For each of reflect/brownfield/release/audit: `grep -qE '§[0-9]|Section [0-9]'` returns non-zero (no cross-file §N)
  </acceptance_criteria>
  <done>reflect/brownfield/release/audit.md created with verbatim §11.4–11.7 bodies (audit folds the §1 L32 framing sentence), all §N converted.</done>
</task>

<task type="auto">
  <name>Task 3: WF-07 — Extract §12 → schema/reference/log-format.md</name>
  <files>schema/reference/log-format.md</files>
  <read_first>
    - CLAUDE.md lines 939–1037 (the full §12 Index and Log section: index.md format, log.md format, structured-operation log entries + examples, contributor inline field, Dataview block)
    - schema/reference/provenance.md lines 1–4, 186–190 (skeleton)
  </read_first>
  <action>
    Create `schema/reference/log-format.md` (NO YAML frontmatter; this is a `schema/reference/` sibling per WF-07).

    Header (dispatch wording, NO `§12` token):
    ```markdown
    # Index and Log Format

    > Agent-authoritative reference for `wiki-cloud/index.md` and `wiki-cloud/log.md` formats: the content-index entry shape, the chronological activity-log entry format, the structured-operation extended log entries, and the contributor inline field.
    > The AGENTS.md routing table points here. If you find a discrepancy between this file and AGENTS.md, this file wins.
    ```

    Body: relocate §12 verbatim (L941–1037): "### index.md (Content Index)", "### log.md (Activity Log)",
    "#### Structured Operation Log Entries" + the three examples, "#### Contributor inline field" + the
    Dataview block.

    RECIPROCAL LOG-SHAPE NOTE (review HIGH #3 — close the canonical/summary reconciliation): Plan 01 (its core
    stub at CLAUDE.md §9, L302) labels the compact 2-line core structured-op log form a "dispatch summary … NOT
    a second standard" and explicitly promises that THIS file's §12 extraction "adds a **reciprocal note** that
    the compact core shape is a summary of the canonical entry." Honor that promise: inside the "#### Structured
    Operation Log Entries" subsection of log-format.md, IMMEDIATELY above the canonical multi-line
    `## [YYYY-MM-DD] OPERATION | target_page` / `source:` / `result:` / `reason:` block, insert this exact
    reciprocal note as a blockquote:
    ```markdown
    > **Canonical shape.** This multi-line `source:` / `result:` / `reason:` form is the ONE canonical
    > structured-operation log entry. The compact one-line form shown in the core `## 9. Structured Operations`
    > dispatch stub (`## [date] OP | page` + a single combined `source: … | result: … | reason: …` line) is a
    > dispatch summary of THIS entry, not a competing standard — the two never diverge.
    ```
    This makes the reconciliation bidirectional: core says "the compact form is a summary of the canonical
    log-format.md entry," and log-format.md now says "the core compact form is a summary of this canonical
    entry." Neither surface claims a second canonical shape.

    §N conversions — UNIFIED POLICY (review HIGH #1): this is a schema/ file, so ZERO `§N` may survive,
    including refs to resident core sections. Convert EVERY cross-file §N to a path OR to plain descriptive
    prose (NO `AGENTS.md §N` form, even for §1/§2/§3 — the earlier draft's "keep resident-section §N" rule is
    SUPERSEDED; the routing guard rejects any `§N` in a schema file regardless of which section it names):
    - "(per Section 3 LLM Navigation Rule)" → "(read the index first when searching, per the LLM Navigation Rule in `AGENTS.md`)" — name the file, drop the §3 token.
    - "decision record pages (reflect workflow, Section 11.4)" → "`schema/workflows/reflect.md`"
    - "(see §11.1 step 9a)" → "`schema/workflows/ingest.md` step 9a"
    - "(query-workflow format, §11.2 Query Log Entry Format)" → "`schema/workflows/query.md` Query Log Entry Format"
    - "(§3 prohibition)" → "(the no-wikilinks-in-frontmatter prohibition in `AGENTS.md` Global Rules)" — name the rule, drop the §3 token.
    - "(per AGENTS.md §6 inline syntax precedent)" → "(per the inline-field syntax precedent in `schema/reference/provenance.md`)"
    Grep the finished file: `grep -nE '§[0-9]|Section [0-9]' schema/reference/log-format.md` MUST return zero lines.

    Footer See Also (dispatch wording): routing-table stub; reflect.md; the per-workflow files that inline their own bare log shapes (ingest.md, query.md, lint.md).
  </action>
  <verify>
    <automated>test -f schema/reference/log-format.md && grep -q 'Structured Operation Log Entries' schema/reference/log-format.md && grep -q 'Contributor inline field' schema/reference/log-format.md && grep -q 'dispatch summary of THIS entry' schema/reference/log-format.md && ! grep -qE '§[0-9]|Section [0-9]' schema/reference/log-format.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f schema/reference/log-format.md` exits 0; line 1 is `# Index and Log Format`
    - `grep -c 'Structured Operation Log Entries' schema/reference/log-format.md` returns 1
    - `grep -c 'Contributor inline field' schema/reference/log-format.md` returns 1
    - RECIPROCAL LOG-SHAPE NOTE (review HIGH #3): `grep -q 'dispatch summary of THIS entry' schema/reference/log-format.md` succeeds — the reciprocal note Plan 01 L302 promises is now present in log-format.md, making the canonical/summary reconciliation bidirectional (core points to this file as canonical; this file flags the core compact form as its summary)
    - ZERO `§N` survives (unified policy — schema files carry no section-number refs at all): `grep -qE '§[0-9]|Section [0-9]' schema/reference/log-format.md` returns non-zero
  </acceptance_criteria>
  <done>log-format.md holds the consolidated §12 reference; refs to extracted sections converted to paths; the reciprocal log-shape note (review HIGH #3) is present, closing the canonical/summary reconciliation Plan 01 L302 promised.</done>
</task>

<task type="auto">
  <name>Task 4: Reduce core §11.3–11.7 + §12 to stubs; delete routing scaffold; repoint agent-parity.md; mirror</name>
  <files>CLAUDE.md, AGENTS.md, schema/AGENTS.template.md, docs/reference/agent-parity.md</files>
  <read_first>
    - CLAUDE.md lines 546–1037 (the §11.3–§12 region being stubbed)
    - CLAUDE.md lines 34–60 (routing table — the scaffold block to delete now that all four workflows are extracted)
    - CLAUDE.md lines 168–187 (stub shape reference)
    - docs/reference/agent-parity.md (its §4/§5/§6/§11.1/§11.7/§13 banner refs to repoint)
  </read_first>
  <action>
    In CLAUDE.md, replace each remaining §11.x body and §12 with a bare-pointer stub (keep the `### N.N`
    headers — e.g. `### 11.3 Lint Workflow`, which is a HEADING with no `§` glyph, NOT a cross-file ref).
    NOTE: the `§11.3:` / `§12 (...)` labels below are pointers telling you WHICH heading's body to replace —
    do NOT write the `§` glyph into the stub. The stub BODY is exactly the backtick-fenced `→ See …` arrow
    (§-free). After this step, core headings remain as `### 11.3` style (titles), and no `§N` glyph or
    "Section N" prose ref survives anywhere in core (the CORE §N SWEEP below enforces this).
    - §11.3: `→ See \`schema/workflows/lint.md\` for the lint workflow, severity tiers, the CI-mode contract (source of truth for CI severity/JSON/escape-hatch/staged-gate policy), and the 15-step procedure.`
    - §11.4: `→ See \`schema/workflows/reflect.md\` for the three-tier decision-record model and periodic backfill procedure.`
    - §11.5: `→ See \`schema/workflows/brownfield.md\` for the brownfield onboarding workflow (scan / bootstrap / suggest / review-typing / verify) and the bootstrap_stage lifecycle.`
    - §11.6: `→ See \`schema/workflows/release.md\` for the orphan-branch publish runbook.`
    - §11.7: `→ See \`schema/workflows/audit.md\` for the review-only claim-faithfulness audit.`
    - §12 (`## 12. Index and Log`): `→ See \`schema/reference/log-format.md\` for index.md / log.md entry formats and the structured-operation + contributor log shapes.`

    Routing table cleanup (L34–60): DELETE the entire "Workflows — STILL INLINE in §11 until Phase 17 ..."
    scaffold block (the two-column "Where it lives NOW" table and its two bold warning lines). Promote the
    remaining Lint and Reflect rows into the main "Resolvable references" table:
    `| Lint workflow + CI severity/JSON contract | \`schema/workflows/lint.md\` |`
    `| Reflect workflow (decision records) | \`schema/workflows/reflect.md\` |`
    Also ADD rows for the files not yet in the table so the inverse-orphan guard (Plan 04) finds every
    extracted workflow reachable:
    `| Structured operations (UPDATE/MERGE/SUPERSEDE/ARCHIVE) | \`schema/workflows/structured-operations.md\` |`
    `| Brownfield vault onboarding | \`schema/workflows/brownfield.md\` |`
    `| Orphan-branch template release | \`schema/workflows/release.md\` |`
    `| Claim-faithfulness audit | \`schema/workflows/audit.md\` |`
    `| Index / log entry formats | \`schema/reference/log-format.md\` |`
    (Ingest + Query rows were added by Plan 02 — do not duplicate.) Every `schema/workflows/*.md` and
    `schema/reference/log-format.md` MUST have exactly one routing-table ROW pointing at it.

    ROUTING-REFERENCE DEFINITION (review NEW-HIGH-A/B — pick ONE definition of "routing reference"):
    The **routing-table ROW** is the canonical reachability anchor. A routing-table row is a blockquote-prefixed
    markdown table cell of the form `> | <when-you-need-this> | \`<path>\` |` (the routing table lives inside the
    `> ` blockquote that opens "Reference Routing Table"). A core **stub arrow** (`→ See \`<path>\` …`, e.g. the
    `### 11.3` stub body) is a CONVENIENCE POINTER and does **NOT** count toward the one-row invariant. This
    matters because after extraction EVERY workflow file is referenced TWICE in core — once by its routing-table
    row AND once by its §-stub arrow — so a raw `grep -c "<path>" CLAUDE.md` counts 2, not 1. The one-row
    assertion below therefore counts routing-TABLE rows specifically (blockquote table-cell form), NOT raw path
    occurrences. (Plan 04's orphan guard uses the SAME definition: reachability = a routing-table row, NOT a
    stub arrow — so deleting only the row orphans the file even though the stub arrow remains.)

    CORE §N SWEEP (review HIGH #1 — the routing guard scans AGENTS.md, so the CORE must be §N-clean too):
    After stubbing, the resident core (§1/§2/§3 + the §4-13 stub bodies) still contains a handful of cross-file
    `§N` refs that survive extraction. Plan 04's guard (corpus = AGENTS.md + schema/ tree) rejects every one.
    Convert these EXACT surviving core refs to paths or descriptive prose (verified via
    `grep -nE '§[0-9]|Section [0-9]' CLAUDE.md` over the resident regions AFTER stubbing):
    - §1 Overview L30 "defined in Section 11 of this document" → "defined in the workflow files under `schema/workflows/` (see the routing table)"
    - §1 Overview L32 "(`bin/audit-claims.sh`, Section 11.7)" → "(`bin/audit-claims.sh`, see `schema/workflows/audit.md`)"
    - §2 Directory Structure L67 code-comment "(cloud-safe-only; see §13)" → "(cloud-safe-only; see `schema/reference/privacy.md`)"
    - §2 L98 "(see `example: true` in Section 5)" → "(see `example: true` in `schema/reference/frontmatter.md`)"
    - Then `grep -nE '§[0-9]|Section [0-9]' CLAUDE.md` over the WHOLE file must return zero lines EXCEPT
      intra-file section HEADINGS (`## 9.`, `### 11.3`, etc.) and the bare numeric `§`-free heading numbers,
      which are NOT cross-file refs (a heading like `## 9. Structured Operations` is the section's own title,
      not a pointer). Concretely: zero `§[0-9]` tokens and zero "Section [0-9]" PROSE refs survive; section
      headings (`^#+ [0-9]`) are exempt because they are titles, not references. If any `§N`/`Section N` prose
      ref remains anywhere in core, convert it (resident concept → path of its extracted home; if it names a
      still-resident concept, use descriptive prose, never a `§N` token).

    External repoint (D-08) of `docs/reference/agent-parity.md`: repoint its banner §-refs (L6, L8: §4/§5/§6/§11.1;
    L29 "AGENTS.md §6"; any §11.7/§13) to the extracted paths (page-types.md, frontmatter.md, provenance.md,
    ingest.md, audit.md, privacy.md). Confirm `grep -rn '§[0-9]' docs/reference/agent-parity.md` returns zero.

    Mirror: `bash bin/sync-claude.sh` then apply identical edits to `schema/AGENTS.template.md`. Verify
    `bash bin/sync-claude.sh --check` exits 0.
  </action>
  <verify>
    <automated>bash bin/sync-claude.sh --check && ! grep -q 'STILL INLINE in §11 until Phase 17' CLAUDE.md && for f in structured-operations ingest query lint reflect brownfield release audit; do [ "$(grep -cE "^> \|.*\`schema/workflows/$f.md\` \|" CLAUDE.md)" -eq 1 ] || { echo "not exactly one routing-table row for $f"; exit 1; }; done && [ "$(grep -cE "^> \|.*\`schema/reference/log-format.md\` \|" CLAUDE.md)" -eq 1 ] && ! grep -q '### Three-Tier Reflect Model' CLAUDE.md && ! grep -qE '§[0-9]' CLAUDE.md && ! grep -qE '\bSection [0-9]' CLAUDE.md && [ "$(grep -rc '§[0-9]' docs/reference/agent-parity.md | grep -v ':0$' | wc -l)" -eq 0 ] && grep -q 'schema/workflows/brownfield.md' schema/AGENTS.template.md</automated>
  </verify>
  <acceptance_criteria>
    - `bash bin/sync-claude.sh --check` exits 0
    - `grep -c 'STILL INLINE in §11 until Phase 17' CLAUDE.md` returns 0 (scaffold block deleted)
    - Every workflow file has EXACTLY ONE routing-table ROW (review NEW-HIGH-A — count routing-TABLE rows specifically, NOT raw path occurrences; a raw `grep -c "<path>" CLAUDE.md` counts 2 because each file is referenced by both its routing-table row AND its §-stub arrow, making `-eq 1` impossible to satisfy). A routing-table row is the blockquote table-cell form `> | … | \`<path>\` |`. Assert one and only one ROW per file: for f in structured-operations ingest query lint reflect brownfield release audit, `[ "$(grep -cE "^> \|.*\`schema/workflows/$f.md\` \|" CLAUDE.md)" -eq 1 ]` (catches both a missing row AND an accidental duplicate row; the stub-arrow occurrence is correctly NOT counted because it is not a `> | … |` table cell), and `[ "$(grep -cE "^> \|.*\`schema/reference/log-format.md\` \|" CLAUDE.md)" -eq 1 ]`. (A loose `grep -q "<path>" CLAUDE.md` is still fine for mere presence — see the verify block — but the EXACTLY-ONE invariant must scope to routing-table rows.)
    - CORE §N SWEEP (review HIGH #1 — the routing guard scans AGENTS.md): `grep -qE '§[0-9]' CLAUDE.md` returns non-zero (no `§N` glyph-refs anywhere in core) AND `grep -qE '\bSection [0-9]' CLAUDE.md` returns non-zero (no "Section N" prose refs); section HEADINGS like `## 9.` are unaffected (they carry no `§` glyph and are not the word "Section")
    - `grep -c 'Three-Tier Reflect Model' CLAUDE.md` returns 0 (§11.4 body moved out)
    - `grep -rn '§[0-9]' docs/reference/agent-parity.md` returns zero lines
    - `grep -q 'schema/workflows/brownfield.md' schema/AGENTS.template.md` (template mirrored)
  </acceptance_criteria>
  <done>Core §11.3–11.7 and §12 are stubs; routing scaffold deleted; all eight workflow files reachable from the routing table; agent-parity.md repointed; all three core files synced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relocated brownfield/audit text → template-public schema/workflows/ | check-neutrality.sh scanned; must stay neutral |
| §11.3 CI-contract framing → external referrers | If the framing line is dropped on the move, ci.md/CONTRIBUTING.md links resolve to a target with no authority statement (silent contract loss) |
| CLAUDE.md ↔ AGENTS.md | Byte-equality gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Tampering | §11.3 "source of truth for CI contracts" framing line | mitigate | Task 1 acceptance criterion greps for the exact framing string in lint.md (count == 1); external links repointed in the same task so the contract end-to-end stays intact |
| T-17-08 | Information disclosure | brownfield.md / audit.md relocated bodies | accept | Source §11.5/§11.7 text is abstract schema/tooling prose (no vault terms); check-neutrality.sh over schema/ is the backstop |
| T-17-09 | Tampering | CLAUDE.md vs AGENTS.md byte-equality across a large multi-section edit | mitigate | Task 4 runs sync-claude --check (exit 0) before done |
| T-17-10 | Denial of service (false-green) | a workflow file left with no routing-table row → unreachable but undetected until Plan 04 | mitigate | Task 4 acceptance loops over all 8 workflow files asserting a routing row for each; Plan 04 inverse-orphan guard is the mechanical backstop |
</threat_model>

<verification>
- `bash bin/sync-claude.sh --check` exits 0
- `bash bin/check-neutrality.sh` exits 0 over all new schema/workflows + log-format.md
- WHOLE-CORPUS §N ABOLITION (the precondition for Plan 04's routing guard — review HIGH #1):
  `grep -rnE '§[0-9]|Section [0-9]' AGENTS.md CLAUDE.md schema/reference/ schema/workflows/` returns zero PROSE
  refs (section headings like `## 9.` carry no `§` glyph and are not the word "Section", so they don't match
  and are correctly exempt). This covers BOTH the schema tree AND the resident core AND the repointed Phase-16
  files (Task 0).
- `grep -rn '§[0-9]' docs/reference/ci.md CONTRIBUTING.md docs/reference/agent-parity.md` returns zero (external repoint complete)
- Every `schema/workflows/*.md` file + `schema/reference/log-format.md` has EXACTLY ONE routing-table row in core
- `grep -c 'Source of truth for Phase 9 / Phase 12.2 CI' schema/workflows/lint.md` returns exactly 1 (no duplicate)
</verification>

<success_criteria>
- WF-05: lint.md merged (seed + §11.3 body + CI-contract framing preserved); external referrers repointed to paths
- WF-06: reflect/brownfield/release/audit.md extracted (brownfield = the 182-line file); audit folds §1 framing
- WF-07: §12 → log-format.md; bare log formats stay inline per-workflow; core §12 resident ~0
- Routing scaffold deleted; all eight workflow files reachable; CLAUDE.md ≡ AGENTS.md ≡ template
</success_criteria>

<output>
After completion, create `.planning/phases/17-workflow-extraction/17-03-SUMMARY.md` recording: line ranges
relocated per file, the framing-line preservation confirmation, the full list of §N→path repoints (schema +
external), the final routing-table row inventory, and that sync-claude --check + check-neutrality exit 0.
This SUMMARY confirms ZERO cross-file §N remains in the schema/ tree (the gate for Plan 04).
</output>
