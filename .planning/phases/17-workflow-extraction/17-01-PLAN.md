---
phase: 17-workflow-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - schema/workflows/structured-operations.md
  - schema/workflows/ingest.md
  - CLAUDE.md
  - AGENTS.md
  - schema/AGENTS.template.md
autonomous: true
requirements: [WF-01, WF-02]
must_haves:
  truths:
    - "schema/workflows/structured-operations.md exists holding the §9 body verbatim (relocated, not rewritten)"
    - "schema/workflows/ingest.md exists holding the two folded §10 substantive blocks (claim-granularity + append-then-synthesize)"
    - "core §9 is a stub: ops-vocab table + validate-op.sh pointer + locked 2-line solo-op log shape + the NEW solo-op commit-prefix line (update:/merge:/supersede:/archive:)"
    - "core §10 is reduced to the 1-line pipeline diagram; pass-narrative deleted"
    - "AGENTS.md byte-identical to CLAUDE.md; both stub edits mirrored to schema/AGENTS.template.md"
    - "every cross-file §N / Section N reference inside the relocated bodies is rewritten to a path"
  artifacts:
    - path: "schema/workflows/structured-operations.md"
      provides: "Extracted §9 structured operations + executor model"
      contains: "Operations Vocabulary"
    - path: "schema/workflows/ingest.md"
      provides: "Folded §10 claim-granularity + append-then-synthesize blocks (ingest workflow body added in Plan 02)"
      contains: "Claim Granularity Rules"
    - path: "CLAUDE.md"
      provides: "§9 stub + §10 diagram-only core remnant"
      contains: "update:"
  key_links:
    - from: "CLAUDE.md §9 stub"
      to: "schema/workflows/structured-operations.md"
      via: "→ See arrow pointer"
      pattern: "schema/workflows/structured-operations.md"
    - from: "schema/workflows/structured-operations.md UPDATE definition"
      to: "schema/workflows/ingest.md"
      via: "append-then-synthesize cross-link (was 'Section 10 Pass 3')"
      pattern: "schema/workflows/ingest.md"
---

<objective>
Extract §9 (Structured Operations and Executor Model) into `schema/workflows/structured-operations.md`,
fold the two substantive §10 blocks (Claim Granularity Rules + Append-Then-Synthesize) into a new
`schema/workflows/ingest.md`, and reduce core §10 to its 1-line pipeline diagram. Replace core §9 with a
bare-pointer stub that keeps the ops-vocab table, the `validate-op.sh` pointer, the locked 2-line solo-op
log shape, and the NEW solo-op commit-prefix line that closes Open Q9 (WF-01/D-01).

Purpose: WF-01 (structured-ops extraction + close the solo-op commit-prefix spec hole) and WF-02 (§10
pipeline: diagram stays, NO pipeline.md, substantive blocks fold, pass-narrative deletes).
Output: two new `schema/workflows/*.md` files + reduced core §9/§10 stubs in CLAUDE.md ≡ AGENTS.md ≡ template.

This is Wave 1 of 4. It runs FIRST because it creates `schema/workflows/ingest.md` (which Plan 02 extends
with the ingest workflow body) and `structured-operations.md` (which Plan 02's query workflow cross-links).
All four core-editing plans (01–03) share CLAUDE.md/AGENTS.md and MUST run sequentially.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-workflow-extraction/17-CONTEXT.md
@.planning/phases/17-workflow-extraction/17-PATTERNS.md

<interfaces>
<!-- The extracted-file skeleton ALL new schema/workflows/*.md files follow. -->
<!-- Source: schema/reference/provenance.md (header L1-4, footer L186-190). NO YAML frontmatter. -->

Header (use the "this file wins" variant from page-types.md — workflow files are procedural authority):
```markdown
# <Title>

> Agent-authoritative reference for <scope>.
> AGENTS.md §<N> points here. If you find a discrepancy between this file and AGENTS.md, this file wins.

<body — verbatim relocated §-content, every cross-file §N rewritten to a path>

## See Also

- [AGENTS.md](../../AGENTS.md) — §<N> stub (pointer to this file).
- `schema/<dir>/<sibling>.md` — <what the sibling owns>.
```

Existing core stub shape to copy (CLAUDE.md L168-187 — bare-pointer, zero reproduced body):
```markdown
## 6. Provenance, Epistemics, and Staleness

Every factual claim MUST have an inline provenance marker `[prov:source_id#locator]`.

→ Syntax, locator types, epistemic markers, and contradiction markers: `schema/reference/provenance.md`.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract §9 → schema/workflows/structured-operations.md (verbatim body, §N→path)</name>
  <files>schema/workflows/structured-operations.md</files>
  <read_first>
    - CLAUDE.md lines 189–292 (the full §9 body being relocated — read every line)
    - schema/reference/provenance.md lines 1–4 and 186–190 (extracted-file header + See Also footer analog)
    - schema/reference/page-types.md lines 1–4 (the stronger "this file wins" banner variant to use)
    - .planning/phases/17-workflow-extraction/17-PATTERNS.md (the extracted-file skeleton + abolish-§N rule sections)
  </read_first>
  <action>
    Create `schema/workflows/structured-operations.md` (NO YAML frontmatter — it would make the file parse
    as a wiki page and trip lint/neutrality).

    Header (verbatim):
    ```markdown
    # Structured Operations and Executor Model

    > Agent-authoritative reference for the four mutation operations (UPDATE / MERGE / SUPERSEDE / ARCHIVE), the executor validation model, and deterministic enforcement.
    > AGENTS.md §9 points here. If you find a discrepancy between this file and AGENTS.md, this file wins.
    ```

    Body: relocate the §9 body from current CLAUDE.md L202–292 VERBATIM (from "### Operation Definitions"
    through the end of the "Per-Operation Preconditions and Postconditions" ARCHIVE block at L291). This is
    text RELOCATION, not a rewrite — preserve wording byte-for-byte EXCEPT the mandatory §N→path conversions
    below. Do NOT relocate the "### Operations Vocabulary" table (L193–200) — that table stays resident in
    the core stub (Task 3).

    MANDATORY §N→path conversions (D-05 abolish-§N — every cross-FILE reference becomes a path):
    - L213 "see Section 10 Pass 3 (Append-Then-Synthesize)" → "see `schema/workflows/ingest.md` (Append-Then-Synthesize)"
    - L224 / L234 "See Section 11.4, Tier 1." → "See `schema/workflows/reflect.md`, Tier 1."
    - L250 "(see Section 5 validation checklist)" → "(see `schema/reference/frontmatter.md` validation checklist)"
    - L251 "(§13 asymmetric model)" → "(see `schema/reference/privacy.md` asymmetric model)"
    - L255 "See Section 12 for log format." → "See `schema/reference/log-format.md` for log format."
    - L276 "see Section 13 and query workflow Section 11.2 privacy rules" → "see `schema/reference/privacy.md` and the query workflow at `schema/workflows/query.md` privacy rules"
    - Any other `(Section N)` / `§N` inside the relocated body → its extracted path. Grep the finished file
      for `§[0-9]` and `Section [0-9]` and convert every cross-file hit. Self-referential intra-file phrasing
      like "the 5 checks listed above" stays as-is (not a cross-file ref).

    Footer:
    ```markdown
    ## See Also

    - [AGENTS.md](../../AGENTS.md) — §9 stub (ops-vocab table + validate-op.sh pointer + solo-op log/commit shapes).
    - `schema/workflows/ingest.md` — the Append-Then-Synthesize incremental-update policy invoked by UPDATE.
    - `schema/workflows/reflect.md` — Tier-1 inline decision-record hooks invoked by MERGE/SUPERSEDE.
    - `schema/reference/log-format.md` — structured-operation log entry format.
    ```
  </action>
  <verify>
    <automated>test -f schema/workflows/structured-operations.md && grep -q "Operation Definitions" schema/workflows/structured-operations.md && ! grep -qE '§[0-9]|Section [0-9]' schema/workflows/structured-operations.md && ! head -1 schema/workflows/structured-operations.md | grep -q '^---'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f schema/workflows/structured-operations.md` exits 0
    - File line 1 is `# Structured Operations and Executor Model` (NOT `---`)
    - `grep -c 'Operation Definitions' schema/workflows/structured-operations.md` returns 1
    - `grep -c 'Per-Operation Preconditions' schema/workflows/structured-operations.md` returns 1
    - `grep -qE '§[0-9]|Section [0-9]' schema/workflows/structured-operations.md` returns non-zero (NO cross-file §N survives)
    - `grep -q 'schema/workflows/ingest.md' schema/workflows/structured-operations.md` (append-then-synthesize cross-link converted)
    - File ends with a `## See Also` section back-linking `../../AGENTS.md`
  </acceptance_criteria>
  <done>structured-operations.md holds the relocated §9 body with every cross-file §N converted to a path and a See Also footer.</done>
</task>

<task type="auto">
  <name>Task 2: Fold §10 substantive blocks → schema/workflows/ingest.md (Claim Granularity + Append-Then-Synthesize)</name>
  <files>schema/workflows/ingest.md</files>
  <read_first>
    - CLAUDE.md lines 293–391 (the full §10 body — identify the two substantive blocks vs the deletable pass-narrative)
    - schema/reference/provenance.md lines 1–4, 186–190 (skeleton)
    - .planning/phases/17-workflow-extraction/17-PATTERNS.md (WF-02 §10 fold rules)
  </read_first>
  <action>
    Create `schema/workflows/ingest.md`. This plan seeds it with ONLY the two folded §10 blocks; Plan 02
    (Wave 2) prepends the full ingest workflow procedure ABOVE these blocks. To make that clean, structure
    this file so the §10 folds live under clearly-named `##` sections that Plan 02 will reference.

    Header (verbatim):
    ```markdown
    # Ingest Workflow

    > Agent-authoritative reference for the ingest workflow: classifying a source, extracting claims with provenance, and merging into the wiki.
    > AGENTS.md §11.1 points here. If you find a discrepancy between this file and AGENTS.md, this file wins.
    ```

    Then add a short bridging line:
    ```markdown
    > **Note:** The step-by-step ingest procedure is added by Plan 02. This file currently holds the two reference blocks folded in from the former §10 pipeline (claim-granularity + incremental-update policy).
    ```
    (Plan 02 will REMOVE this bridging note when it adds the procedure — acceptable, it's a scaffold line.)

    Relocate VERBATIM from current CLAUDE.md:
    1. The "#### Claim Granularity Rules" block (L329–342: the heuristic line + the 4-row granularity table +
       the "Bias toward atomic" guidance). Promote it to a top-level `## Claim Granularity Rules`.
    2. The "#### Incremental Update Policy: Append-Then-Synthesize" block (L357–370: the 4 numbered policy
       points + Exceptions + Mantra). Promote it to a top-level `## Incremental Update Policy: Append-Then-Synthesize`.

    DELETE (do not relocate): the §10 pass-narrative — "## 10. Compiler Pipeline" intro prose (L293–301
    EXCEPT the diagram, which stays in core per Task 3), Pass 0/1/2/3/4 narrative scaffolding (the
    "### Pass N" headers and their bullet prose at L303–327, L344–355, L372–390), and "Optional Follow-On
    Passes" (L384–390). Only the two substantive blocks survive.

    §N→path conversions inside the two folded blocks:
    - In Append-Then-Synthesize point 2: "(see Section 6)" → "(see `schema/reference/provenance.md`)"
    - point 4: "(see Section 11.4)" → "(see `schema/workflows/reflect.md`)"
    - Grep the file for `§[0-9]` / `Section [0-9]` and convert any remaining cross-file hit.

    Footer:
    ```markdown
    ## See Also

    - [AGENTS.md](../../AGENTS.md) — §11 stub (write-back-mandatory residue + routing table).
    - `schema/workflows/structured-operations.md` — the UPDATE/MERGE operations the merge step invokes.
    - `schema/reference/provenance.md` — provenance marker syntax used during extraction.
    ```
  </action>
  <verify>
    <automated>test -f schema/workflows/ingest.md && grep -q "Claim Granularity Rules" schema/workflows/ingest.md && grep -q "Append-Then-Synthesize" schema/workflows/ingest.md && ! grep -qE '§[0-9]|Section [0-9]' schema/workflows/ingest.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f schema/workflows/ingest.md` exits 0; line 1 is `# Ingest Workflow`
    - `grep -c '## Claim Granularity Rules' schema/workflows/ingest.md` returns 1
    - `grep -c '## Incremental Update Policy: Append-Then-Synthesize' schema/workflows/ingest.md` returns 1
    - `grep -q 'smallest unit that preserves meaningful provenance' schema/workflows/ingest.md` (granularity heuristic relocated verbatim)
    - `grep -q 'Append in the detail layer, synthesize in the summary layer' schema/workflows/ingest.md` (Mantra relocated)
    - `grep -qE '§[0-9]|Section [0-9]' schema/workflows/ingest.md` returns non-zero (no cross-file §N)
    - `grep -c '### Pass 0' schema/workflows/ingest.md` returns 0 (pass-narrative NOT relocated)
  </acceptance_criteria>
  <done>ingest.md seeded with the two folded §10 blocks (verbatim), pass-narrative not carried over, §N converted.</done>
</task>

<task type="auto">
  <name>Task 3: Reduce core §9 to stub + §10 to diagram; mirror to AGENTS.md + template</name>
  <files>CLAUDE.md, AGENTS.md, schema/AGENTS.template.md</files>
  <read_first>
    - CLAUDE.md lines 189–391 (the §9 + §10 region being replaced)
    - CLAUDE.md lines 168–187 (existing Phase-16 stub shapes to copy exactly)
    - 17-CONTEXT.md decisions D-01, D-02 (solo-op commit prefix vocabulary)
    - 17-PATTERNS.md "§9 stub" adaptation bullet (resident remnants list)
  </read_first>
  <action>
    In CLAUDE.md, REPLACE the entire region from `## 9. Structured Operations and Executor Model` (L189)
    through the end of §10 (the last line before `## 11. Workflows` at L392) with the two stubs below.

    §9 stub (keeps ops-vocab table + validate-op.sh pointer + locked 2-line solo-op log shape + the NEW
    WF-01/D-01 solo-op commit-prefix line). Write EXACTLY:
    ```markdown
    ## 9. Structured Operations and Executor Model

    All wiki mutations use a formal operations vocabulary. Raw file rewrites are prohibited -- every change goes through one of these four operations with mandatory logging.

    | Operation    | Verb    | What It Does                                       |
    |-------------|---------|---------------------------------------------------|
    | **UPDATE**  | Modify  | Add new information to an existing page            |
    | **MERGE**   | Combine | Unify two pages covering the same concept          |
    | **SUPERSEDE** | Replace | Mark a page/claim as replaced by newer information |
    | **ARCHIVE** | Retire  | Move outdated content out of active wiki           |

    Before applying ANY operation, run `bin/validate-op.sh <OPERATION> <target_path> [<second_path>]` — it mechanically enforces the 5 executor checks (target exists, both-pages-distinct for MERGE, provenance resolves, frontmatter valid, privacy respected). If it returns FAIL, do NOT apply the operation.

    **Solo-op log shape.** A standalone structured op (not wrapped in a workflow) logs to `wiki-cloud/log.md` as:
    ```
    ## [YYYY-MM-DD] OPERATION | target_page

    source: source_id | result: what changed | reason: one-line rationale
    ```

    **Solo-op commit prefix (Open Q9 / D-01).** A *solo* structured op gets its own lowercased per-op commit prefix, symmetric with the workflow prefixes: `update(<page>): …` / `merge(<page>): …` / `supersede(<page>): …` / `archive(<page>): …`. Within a workflow, ops roll up under the workflow prefix instead (D-02) — only a standalone structural action gets its own per-op prefix.

    → Full operation definitions, executor model, batch validation, and per-op preconditions/postconditions: `schema/workflows/structured-operations.md`.

    ## 10. Compiler Pipeline (Conceptual Model)

    ```
    Source -> [Classify] -> [Diff] -> [Extract] -> [Merge] -> [Lint] -> Wiki
    ```

    → Claim-granularity rules and the Append-Then-Synthesize incremental-update policy live in `schema/workflows/ingest.md`. Each pass is implemented by the corresponding workflow (see the routing table).
    ```

    NOTE on the locked 2-line solo-op log shape: the brief locks a "2-line" shape. The original §12 renders
    source/result/reason on three lines; collapse to the locked compact two-line shape shown above
    (`## [date] OP | page` + one combined `source: … | result: … | reason: …` line). The full multi-line
    reference form stays in `schema/reference/log-format.md` (Plan 03).

    Then run `bash bin/sync-claude.sh` to mirror CLAUDE.md → AGENTS.md (or apply the identical edit to
    AGENTS.md directly), and apply the SAME two stubs to `schema/AGENTS.template.md` at its corresponding
    §9/§10 region (the template tracks core stubs verbatim, minus {{PLACEHOLDER}} substitutions — there are
    no placeholders in this region, so it is a verbatim copy).

    Verify byte-equality with `bash bin/sync-claude.sh --check` (must exit 0).
  </action>
  <verify>
    <automated>bash bin/sync-claude.sh --check && grep -q 'update(<page>):' CLAUDE.md && grep -q 'Source -> \[Classify\]' CLAUDE.md && grep -q 'schema/workflows/structured-operations.md' CLAUDE.md && grep -q 'update(<page>):' schema/AGENTS.template.md && ! grep -q '### Operation Definitions' CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `bash bin/sync-claude.sh --check` exits 0 (CLAUDE.md ≡ AGENTS.md byte-identical)
    - `grep -q 'update(<page>): …' CLAUDE.md` and the same for merge/supersede/archive prefixes (D-01 line present)
    - `grep -q 'Source -> \[Classify\] -> \[Diff\]' CLAUDE.md` (1-line diagram retained)
    - `grep -c '### Operation Definitions' CLAUDE.md` returns 0 (§9 body moved out)
    - `grep -c '### Pass 0' CLAUDE.md` returns 0 (pass-narrative deleted from core)
    - `grep -q 'schema/workflows/structured-operations.md' CLAUDE.md` (§9 arrow present)
    - `grep -q 'update(<page>):' schema/AGENTS.template.md` (template mirrored)
  </acceptance_criteria>
  <done>Core §9 is a stub (vocab table + validate-op pointer + solo-op log + commit-prefix line + arrow); §10 is diagram + 1 arrow; all three files in sync.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relocated text → template-public schema/ files | Extracted bodies land in `schema/workflows/*.md` which is a template-public path (scanned by check-neutrality.sh); real vault terms must never appear |
| CLAUDE.md ↔ AGENTS.md | Byte-equality gate; a drift silently bypasses a gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Information disclosure | §9/§10 bodies relocated into `schema/workflows/*.md` | mitigate | §9/§10 are abstract schema prose with `<UPPERCASE>`/`<slug>` placeholders only (no real vault terms); `bin/check-neutrality.sh` over `schema/` is the backstop. Task verification greps for absence of §N, not vault content, but the source text is already neutral. |
| T-17-02 | Tampering | CLAUDE.md vs AGENTS.md byte-equality | mitigate | Task 3 runs `bin/sync-claude.sh --check` (exit 0 required) before done; pre-commit hook re-enforces |
| T-17-03 | Spoofing | dangling §N pointer left after partial conversion | accept (caught downstream) | Plan 04's `routing` lint category mechanically catches any surviving cross-file §N; this plan's grep acceptance criteria catch it earlier |
</threat_model>

<verification>
- `bash bin/sync-claude.sh --check` exits 0 (byte-equality preserved)
- `bash bin/check-neutrality.sh` exits 0 over the new schema/workflows files
- Both new files start with `# H1`, not `---` (no YAML frontmatter)
- No cross-file `§N` / `Section N` survives in either new file
- Core §9 retains the ops-vocab table and gains the D-01 solo-op commit-prefix line; §10 is diagram-only
</verification>

<success_criteria>
- WF-01: §9 extracted; core keeps vocab + validate-op pointer + 2-line solo-op log shape + the NEW solo-op commit prefix (Open Q9 closed)
- WF-02: §10 diagram (1 line) stays resident; claim-granularity + append-then-synthesize folded into ingest.md; pass-narrative deleted; NO pipeline.md created
- CLAUDE.md ≡ AGENTS.md ≡ template (stubs mirrored)
</success_criteria>

<output>
After completion, create `.planning/phases/17-workflow-extraction/17-01-SUMMARY.md` recording: exact line
ranges relocated, the §N→path conversions made, and confirmation that sync-claude --check and check-neutrality
both exit 0.
</output>
