---
phase: 17-workflow-extraction
plan: 04
type: execute
wave: 4
depends_on: [17-03]
files_modified:
  - bin/lint.sh
  - CLAUDE.md
  - AGENTS.md
  - schema/AGENTS.template.md
  - docs/reference/agent-parity.md
  - wiki-cloud/decisions/dr-2026-06-05-workflow-extraction.md
  - wiki-cloud/index.md
autonomous: true
requirements: [WF-08, WF-09]
must_haves:
  truths:
    - "bin/lint.sh has a NEW `routing` category: forward (dangling cross-file path / surviving cross-file §N) → error; inverse (extracted schema/workflows file with no routing-table row) → warning"
    - "the routing scan walks the schema/ tree + AGENTS.md rooted at LINT_REPO_ROOT, independent of WIKI_DIR (bypasses the empty-wiki workflow abort)"
    - "bin/lint.sh has a WF-08 inclusion-audit drift `info` check reading the core header comment and firing when core grows > +20%/+25 lines"
    - "AGENTS.md/CLAUDE.md carry the inclusion-audit header comment with N = the actual post-extraction core line count (D-12 baseline)"
    - "`bash bin/lint.sh --category routing` exits 0 (no dangling refs, no orphan files) over the fully-extracted tree"
    - "docs/reference/agent-parity.md has the WF-09 scoped desk-check trace (gating floor) + empirical-run record (non-gating)"
    - "a schema-update decision record for the workflow extraction is authored and indexed"
  artifacts:
    - path: "bin/lint.sh"
      provides: "routing category + WF-08 drift info check"
      contains: "routing"
    - path: "docs/reference/agent-parity.md"
      provides: "WF-09 three-tier evidence (desk-check + empirical record)"
      contains: "Desk-Check"
    - path: "wiki-cloud/decisions/dr-2026-06-05-workflow-extraction.md"
      provides: "schema-update DR for the extraction"
      contains: "trigger_type: schema-update"
  key_links:
    - from: "bin/lint.sh routing category"
      to: "schema/ tree + AGENTS.md"
      via: "LINT_REPO_ROOT-rooted walk, WIKI_DIR-independent"
      pattern: "routing"
    - from: "AGENTS.md inclusion-audit header"
      to: "bin/lint.sh WF-08 drift check"
      via: "machine-readable comment parsed by the info check"
      pattern: "inclusion-audit:"
---

<objective>
The close-gating wave — built LAST because the `routing` error-category can only go green AFTER every
cross-file §N has been converted to a path (Plans 01-03). Deliver: (1) the NEW `routing` lint category —
bidirectional resolvability guard over the schema/ tree (forward dangling-ref → error; inverse orphan-file →
warning), bypassing the empty-wiki abort (D-03..D-07). (2) The WF-08 inclusion-audit drift `info` check +
the machine-readable core header comment baseline (D-09..D-12). (3) The WF-08 section-by-section inclusion
re-audit recording the real baseline line count + per-resident-section justification. (4) The WF-09
agent-parity three-tier evidence in agent-parity.md (D-13..D-15). (5) The schema-update DR.

Purpose: WF-08 (inclusion-test verification + tripwire) and WF-09 (agent-parity evidence) + the Phase-16
D-07/D-08 `--check-tree` carry-over realized as the `routing` lint category.
Output: extended bin/lint.sh (LINT_VERSION 1.7.0 → 1.8.0), the inclusion-audit header in core, the WF-09
evidence sections, the DR. This wave GATES the phase close.

Wave 4 — depends on Plan 03 (ZERO cross-file §N must remain in schema/ before the routing error-check can
pass green).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-workflow-extraction/17-CONTEXT.md
@.planning/phases/17-workflow-extraction/17-PATTERNS.md

<interfaces>
<!-- bin/lint.sh EXACT insertion points (verified against the live file): -->
<!--   LINT_VERSION at L13 (="1.7.0") -> bump to "1.8.0" (MINOR: new non-breaking category) -->
<!--   CI_SEVERITY_REMAP dict at L316-331 -> add 'routing': 'error'. The inverse (orphan) + WF-08 (info) -->
<!--     findings must NOT be promoted to error: use the proven drift/drift-external two-token trick — -->
<!--     emit inverse findings under category 'routing' with a message PREFIX the remap leaves alone, -->
<!--     guarded the same way matches_skip() handles 'EXTERNAL: ' at L348. -->
<!--   should_run(cat) gate at L463-464 -> wrap the new scan in: if should_run('routing'): -->
<!--   add_finding(severity, category, path, message) at L442 -> the emit API (4-tuple) -->
<!--   REPO_ROOT at L292 (os.environ LINT_REPO_ROOT) -> root the schema/ walk here, NOT at wiki_dir -->
<!--   --category parse: routing flows through automatically once should_run('routing') exists; add to usage doc L26-29 -->
<!--   skip-subtract + CI remap application at ~L2393-2398; routing errors flow to has_error exit-1 automatically -->
<!-- Forward analog: the linkres known-set/resolve block (~L1887). Inverse analog: the orphan block (~L1184). -->
<!-- D-07: --staged stays provenance-only; do NOT wire routing into --staged. -->
<!-- Current live core is 1051 lines PRE-extraction; after Plans 01-03 it is materially smaller — the WF-08 -->
<!--   audit RECORDS that actual number as the baseline (D-12: drop the guessed ~145). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the `routing` lint category to bin/lint.sh (forward error + inverse warning, schema/-rooted)</name>
  <files>bin/lint.sh</files>
  <read_first>
    - bin/lint.sh lines 10-50 (LINT_VERSION + usage doc category list)
    - bin/lint.sh lines 316-350 (CI_SEVERITY_REMAP + matches_skip — the drift/drift-external two-token template)
    - bin/lint.sh lines 442-464 (add_finding + should_run)
    - bin/lint.sh lines 266-292 (REPO_ROOT / LINT_REPO_ROOT)
    - bin/lint.sh lines 1184-1262 (the orphan inbound-link block — inverse analog)
    - bin/lint.sh lines 1887-1971 (the linkres known-set/resolve block — forward analog)
    - bin/lint.sh lines 2382-2400 (skip-subtract + CI remap application)
    - 17-PATTERNS.md "bin/lint.sh — NEW routing category" section (the 6 exact insertion points + the bidirectional-severity reconciliation flag)
  </read_first>
  <action>
    Add a NEW `routing` category to bin/lint.sh. It walks the schema/ tree + AGENTS.md (rooted at
    LINT_REPO_ROOT, INDEPENDENT of WIKI_DIR — so a fresh template clone with a near-empty wiki but a full
    schema tree is still checked; this is the empty-wiki-abort bypass, D-06).

    Corpus: AGENTS.md (core) + every schema/reference/*.md + every schema/workflows/*.md.

    FORWARD check (dangling pointer -> add_finding('error', 'routing', <referrer>, ...)):
    1. Build the known-set = the set of existing files in the schema/ tree (relative paths like
       schema/workflows/ingest.md) plus AGENTS.md (mirror the linkres known-set pattern at L1894-1904).
    2. In each corpus file, find every cross-file path reference of the form schema/reference/<x>.md,
       schema/workflows/<x>.md, docs/reference/<x>.md. For each, if the target file does NOT exist on disk
       -> add_finding('error', 'routing', <referrer_rel>, 'dangling routing reference: <target> does not exist').
    3. PATTERN-PROHIBITION (D-05): in each corpus file, flag any surviving cross-file section reference —
       regex matching the section-symbol followed by a digit, or "Section " followed by a digit — as
       add_finding('error', 'routing', <referrer_rel>, 'cross-file section-number reference forbidden (D-05); use a schema/ path instead').
       EXCEPTION: references to the RESIDENT core sections that still exist in AGENTS.md are permitted.
       Implement narrowly: a section-number token is ALLOWED only when N is in {1,2,3} (the resident-core
       sections). All other section-number refs in the schema tree -> error. (Rationale: §4/5/6/8/9/10/11/12/13
       are all extracted; their numbers no longer name anything.)

    INVERSE check (orphan file -> add_finding('warning', 'routing', <orphan_file>, ...), NOT promoted to error):
    4. For each schema/workflows/*.md and schema/reference/log-format.md, check whether AGENTS.md's routing
       table (or any core stub arrow) references it by path. If NO core reference points to it ->
       add_finding('warning', 'routing', <orphan_rel>, 'ORPHAN: extracted file unreachable from core routing table').
       To keep this finding at warning under --ci (where routing->error would promote it), use the
       drift/drift-external two-token trick: the message PREFIX is 'ORPHAN: ', and add a remap-time guard
       mirroring matches_skip() at L348 so a 'routing' finding whose message starts with 'ORPHAN: ' is left
       at warning (not promoted to error). Document the choice in a code comment next to CI_SEVERITY_REMAP.

    Wire-in (the 6 exact points from 17-PATTERNS.md):
    1. Wrap the whole routing scan in: if should_run('routing'): (L463 gate).
    2. CI_SEVERITY_REMAP (L316-331): add the line  'routing': 'error',
    3. Usage doc (L26-29): add routing to the --category list.
    4. LINT_VERSION (L13): bump "1.7.0" -> "1.8.0" (MINOR — new non-breaking category).
    5. Empty-wiki bypass: root the routing walk at REPO_ROOT/schema (NOT wiki_dir); do not gate it on
       all_pages being non-empty.
    6. JSON/exit: no change — routing findings flow through the 4-tuple emitter and has_error exit automatically.

    Do NOT wire routing into --staged (D-07: --staged stays provenance-only).

    After implementing, run: bash bin/lint.sh --category routing — it MUST exit 0 (Plans 01-03 left zero
    dangling refs and zero forbidden section-refs, and every workflow file has a routing row). If it reports
    findings, they are REAL extraction defects — fix the underlying schema/core file (do NOT loosen the check
    to make it pass).
  </action>
  <verify>
    <automated>bash bin/lint.sh --version | grep -q '1.8.0' && bash bin/lint.sh --category routing && grep -q "'routing'" bin/lint.sh && grep -q "should_run('routing')" bin/lint.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash bin/lint.sh --version` prints `1.8.0`
    - `bash bin/lint.sh --category routing` exits 0 over the fully-extracted tree (no dangling refs, no orphan files, no stray section-refs)
    - `grep -qE "'routing': *'error'" bin/lint.sh` (severity remap entry present)
    - `grep -q "should_run('routing')" bin/lint.sh` (category gated)
    - Negative test: `printf 'see schema/workflows/nonexistent.md\n' >> schema/workflows/release.md; bash bin/lint.sh --category routing --format json | grep -q dangling; git checkout schema/workflows/release.md` — the dangling finding fires, then the file is restored
    - `bash bin/lint.sh --category routing` still runs (no abort) even when the wiki is near-empty (empty-wiki bypass)
  </acceptance_criteria>
  <done>routing category live: forward dangling/section-ref → error, inverse orphan → warning, schema/-rooted, --staged untouched, LINT_VERSION 1.8.0; passes green on the extracted tree.</done>
</task>

<task type="auto">
  <name>Task 2: WF-08 — inclusion-audit header comment baseline + drift info check</name>
  <files>bin/lint.sh, CLAUDE.md, AGENTS.md, schema/AGENTS.template.md</files>
  <read_first>
    - bin/lint.sh (the routing block added in Task 1 — the WF-08 info check rides alongside it)
    - bin/lint.sh lines ~950, ~2339 (info-severity add_finding patterns for the drift/brownfield summary)
    - CLAUDE.md lines 1-6 (top of core — where the inclusion-audit header comment lands)
    - 17-CONTEXT.md decisions D-09, D-10, D-11, D-12 (delta-from-baseline tripwire mechanics)
    - 17-PATTERNS.md "bin/lint.sh — WF-08 inclusion-audit drift info check" section
  </read_first>
  <action>
    PART A — Record the baseline (D-12). Count the ACTUAL current core line count via: wc -l < AGENTS.md.
    Insert a machine-readable header comment immediately after the top router blockquote in CLAUDE.md
    (and mirror to AGENTS.md + template), EXACT format (D-10):
      <!-- inclusion-audit: <N> lines @ 2026-06-05 -->
    where <N> is the post-extraction wc -l of AGENTS.md AT THE MOMENT of this task. Add one resident comment
    just below it (the preventive surface):
      <!-- Resident core: justify any added line against the inclusion test (ambient / unscriptable-AND-unacceptable-miss / dispatch) or extract it. Re-run WF-08 and update this baseline. -->

    PART B — The drift info check in bin/lint.sh (D-11). Inside the routing scan (gated by should_run('routing')),
    parse the inclusion-audit comment from AGENTS.md to get N and DATE, compute current line count
    len(open(AGENTS.md).readlines()), and if current - N > max(25, ceil(0.20 * N)) then
      add_finding('info', 'routing', 'AGENTS.md', 'AUDIT: core drifted +<delta> lines since last inclusion audit (<DATE>) — re-run WF-08')
    Severity info (non-blocking). Prefix the message with 'AUDIT: ' and leave it at info under --ci the same
    way the inverse 'ORPHAN: ' findings are left at warning (guard in the remap step so a 'routing' finding
    whose message starts with 'AUDIT: ' is NOT promoted to error). Document the +20%/+25 threshold in a comment.

    After Part A the freshly-written baseline equals current size, so delta == 0 -> the info check is SILENT.
    Verify: bash bin/lint.sh --category routing --format json emits NO 'core drifted' finding immediately
    after baseline recording. Mirror the header via bash bin/sync-claude.sh; confirm --check exits 0.
  </action>
  <verify>
    <automated>grep -qE '<!-- inclusion-audit: [0-9]+ lines @ 2026-06-05 -->' CLAUDE.md && grep -qE '<!-- inclusion-audit: [0-9]+ lines @ 2026-06-05 -->' AGENTS.md && grep -qE '<!-- inclusion-audit: [0-9]+ lines @ 2026-06-05 -->' schema/AGENTS.template.md && bash bin/sync-claude.sh --check && grep -q 'inclusion-audit' bin/lint.sh && ! bash bin/lint.sh --category routing --format json | grep -q 'core drifted'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -oE '<!-- inclusion-audit: [0-9]+ lines @ 2026-06-05 -->' AGENTS.md` matches, and the `[0-9]+` equals `wc -l < AGENTS.md` (baseline == actual)
    - The same header comment is present in CLAUDE.md and schema/AGENTS.template.md (mirrored)
    - `bash bin/sync-claude.sh --check` exits 0
    - `grep -q 'inclusion-audit' bin/lint.sh` (the check parses the comment)
    - Immediately post-baseline, `bash bin/lint.sh --category routing --format json | grep 'core drifted'` returns nothing (delta == 0, silent)
  </acceptance_criteria>
  <done>Core carries the inclusion-audit baseline header (N = real post-extraction line count); the drift info check is wired and silent at delta 0.</done>
</task>

<task type="auto">
  <name>Task 3: WF-08 inclusion re-audit (section justification) + WF-09 agent-parity evidence</name>
  <files>docs/reference/agent-parity.md, CLAUDE.md, AGENTS.md</files>
  <read_first>
    - CLAUDE.md (the full post-extraction core — every resident section)
    - docs/reference/agent-parity.md (the full file: DEBT-02 rubric, golden inventory, Codex re-run procedure, banner)
    - 17-CONTEXT.md decisions D-13, D-14, D-15 (three-tier reachability stack; desk-check = gating floor, empirical = non-gating)
    - .planning/milestones/v1.2-MILESTONE-BRIEF.md (the inclusion test 3 clauses)
  </read_first>
  <action>
    PART A — WF-08 section-by-section justification. For EACH resident core section (§1 Overview, §2 Directory
    Structure, §3 Global Rules, the routing table, §9 stub, §10 diagram, §11 write-back line, the
    §4-8/§12-13 pointer stubs), record a one-line justification citing its inclusion-test clause: `ambient`
    (loaded every turn, no dispatch), `unscriptable-AND-unacceptable-miss` (judgment a script can't enforce
    and silent failure is costly), or `dispatch` (the routing table). Write the full justification table into
    the SUMMARY (it is verification evidence). Confirm the recorded baseline N from Task 2 matches the audited
    core. Planner's discretion: optionally add the per-section one-line justifications as HTML comments
    adjacent to each resident core section header in CLAUDE.md/AGENTS.md (if added, mirror + sync-check).

    PART B — WF-09 agent-parity evidence in docs/reference/agent-parity.md (D-13/D-14/D-15). Add two sections,
    keeping the file's existing "re-run manual" framing:

    1. "## Routing Desk-Check (gating floor)" — the scoped, re-runnable desk-check covering ONLY the
       non-mechanized judgment dims (D-14): (a) routing-table prominence/unambiguity — is the IMPORTANT
       routing table positioned/worded such that a foreign agent given only AGENTS.md would FOLLOW it to
       schema/workflows/ingest.md; (b) content self-sufficiency — does schema/workflows/ingest.md contain
       everything needed to ingest WITHOUT reading the monolith. CITE the new routing lint guard for
       resolvability (bash bin/lint.sh --category routing exits 0 — link-resolution is mechanized; do NOT
       re-prove it by hand). Record the desk-check verdict as PASS/FAIL with the trace.

    2. "## Empirical Agent Run (best-effort, non-gating)" — record a Codex/Cursor run on the prospect-theory
       seed for behavioral discoverability. If the tool is available, run it and record the result VERBATIM
       (including failure: "agent did not follow the router to ingest.md" is the Vercel-56% finding caught in
       the wild — a signal to strengthen the IMPORTANT cue, NOT a red to suppress, per D-15). If the tool is
       unavailable, record blocked-on-host-runtime honestly (the v1.1 "Codex blocked-on-host-runtime"
       precedent) and close on the desk-check floor. Mark this section explicitly NON-GATING.

    Re-verify zero residual section-refs in agent-parity.md (Plan 03 repointed them; confirm).
  </action>
  <verify>
    <automated>grep -q 'Routing Desk-Check' docs/reference/agent-parity.md && grep -q 'Empirical Agent Run' docs/reference/agent-parity.md && grep -qi 'non-gating' docs/reference/agent-parity.md && grep -q 'bin/lint.sh --category routing' docs/reference/agent-parity.md && [ "$(grep -rc '§[0-9]' docs/reference/agent-parity.md | grep -v ':0$' | wc -l)" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'Routing Desk-Check' docs/reference/agent-parity.md` (gating-floor section present)
    - `grep -q 'Empirical Agent Run' docs/reference/agent-parity.md` and `grep -qi 'non-gating' docs/reference/agent-parity.md`
    - `grep -q 'bin/lint.sh --category routing' docs/reference/agent-parity.md` (cites the mechanized guard for resolvability)
    - `grep -rn '§[0-9]' docs/reference/agent-parity.md` returns zero lines
    - The SUMMARY contains a per-resident-section inclusion-test justification table whose section count matches the resident core
  </acceptance_criteria>
  <done>WF-08 section justification recorded (baseline matches); agent-parity.md carries the desk-check floor + non-gating empirical record citing the routing guard.</done>
</task>

<task type="auto">
  <name>Task 4: Author the schema-update decision record; index it; run the full CI gate suite</name>
  <files>wiki-cloud/decisions/dr-2026-06-05-workflow-extraction.md, wiki-cloud/index.md</files>
  <read_first>
    - wiki-cloud/decisions/ (an existing schema-update DR for format — e.g. the Phase-16 REF-10 extraction DR or dr-…-privacy-asymmetric-two-dir)
    - schema/reference/page-types.md (decision page type: the 7 required sections + frontmatter)
    - wiki-cloud/index.md (the Decisions category listing format)
    - 17-CONTEXT.md "DR-at-execution precedent" note
  </read_first>
  <action>
    Author wiki-cloud/decisions/dr-2026-06-05-workflow-extraction.md (trigger_type: schema-update) recording
    the Phase 17 extraction: §9/§10/§11.1-11.7/§12 -> schema/workflows/*.md + schema/reference/log-format.md;
    the solo-op commit-prefix decision (D-01 closing Open Q9); the abolish-§N rule realized as the routing
    lint category (D-05); the delta-from-baseline inclusion tripwire (D-09..D-12). Use the decision page-type
    template: full frontmatter (id, type: decision, trigger_type: schema-update, affected_pages, status,
    created_at, updated_at, aliases, supersedes/superseded_by, has_contradictions) + all 7 required sections.
    The "Why" section states what framing was adopted (markdown-authoritative, router-core, path-refs-not-§N)
    and what it replaced. "Alternatives Considered" lists at least one (e.g. a standalone bin/check-routing.sh
    or extending sync-claude --check-tree — both rejected per D-06). Set affected_pages to the workflow/reference
    file IDs created this phase.

    Neutrality: this DR lands under wiki-cloud/decisions/ (creator-vault content, not template-public) so it
    may reference real schema filenames; but keep it free of private vault terms regardless. Add provenance
    markers if the decision page-type requires them (decision records are prov-exempt per the strict-mode
    rules — confirm against page-types.md; if exempt, no [prov:] needed).

    Add the DR to wiki-cloud/index.md under the Decisions category using the established
    `- [[id|Title]] -- <one-line summary> (<epistemic_status>, <updated_at>)` format.

    Then run the FULL CI gate suite and confirm all green (behavior unchanged + new routing green):
    - bash bin/sync-claude.sh --check  (byte-equality)
    - bash bin/lint.sh --ci --format json  (whole suite incl. new routing category; exit 0)
    - bash bin/lint.sh --category routing  (exit 0)
    - bash bin/check-neutrality.sh  (exit 0 over schema/ tree)
    - bash bin/check-privacy.sh  (exit 0)
    - bash bin/init-wizard.sh --dry-run  (wizard render still works over the mirrored template) — if the
      flag/script name differs, use the project's documented dry-run invocation.
    Record every command's exit status in the SUMMARY. If any gate fails, the failure is a REAL regression —
    fix the underlying file, do not suppress.
  </action>
  <verify>
    <automated>test -f wiki-cloud/decisions/dr-2026-06-05-workflow-extraction.md && grep -q 'trigger_type: schema-update' wiki-cloud/decisions/dr-2026-06-05-workflow-extraction.md && grep -q 'dr-2026-06-05-workflow-extraction' wiki-cloud/index.md && bash bin/sync-claude.sh --check && bash bin/lint.sh --ci --format json >/dev/null && bash bin/lint.sh --category routing && bash bin/check-neutrality.sh && bash bin/check-privacy.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -f wiki-cloud/decisions/dr-2026-06-05-workflow-extraction.md` exits 0
    - `grep -q 'trigger_type: schema-update' …/dr-2026-06-05-workflow-extraction.md` and the file has all 7 decision sections
    - `grep -q 'dr-2026-06-05-workflow-extraction' wiki-cloud/index.md` (indexed under Decisions)
    - `bash bin/sync-claude.sh --check` exits 0
    - `bash bin/lint.sh --ci --format json` exits 0 (whole suite green, routing included)
    - `bash bin/lint.sh --category routing` exits 0
    - `bash bin/check-neutrality.sh` exits 0 and `bash bin/check-privacy.sh` exits 0
  </acceptance_criteria>
  <done>DR authored + indexed; full CI gate suite (sync, lint --ci, routing, neutrality, privacy, wizard dry-run) all green; phase ready to close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| routing scan corpus | The scan must walk schema/ + AGENTS.md only; a mis-scoped walk into wiki-local/ would read local-only content into a CI surface |
| CLAUDE.md ↔ AGENTS.md | Byte-equality gate (header comment must mirror) |
| DR under wiki-cloud/decisions/ | Creator-vault content path; must stay free of private terms even though path-public |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-11 | Information disclosure | routing scan corpus walk | mitigate | Corpus is explicitly rooted at REPO_ROOT/schema + AGENTS.md (Task 1); it never enters wiki-local/ or wiki-cloud/. Verify the walk path list in the code excludes wiki dirs. |
| T-17-12 | Elevation of privilege (false-green) | inverse-orphan / WF-08 info findings promoted to error by the routing→error remap, OR error findings silently downgraded | mitigate | The 'ORPHAN: '/'AUDIT: ' message-prefix guard (mirroring drift-external) is explicitly tested: forward dangling stays error (negative test in Task 1), inverse stays warning, audit stays info |
| T-17-13 | Tampering | inclusion-audit baseline mirrored across 3 files | mitigate | Task 2 runs sync-claude --check (exit 0); the comment is byte-identical in all three |
| T-17-14 | Repudiation | a dishonest baseline bump without re-auditing | accept (documented residual) | Per D-12 honest-residual: the baseline update is a WF-08 deliverable tied to this DR's commit trail; a bare number-bump with no audit artifact is review-visible. Same trust model as any self-reported check. |
</threat_model>

<verification>
- `bash bin/lint.sh --version` == 1.8.0; `bash bin/lint.sh --category routing` exits 0
- `bash bin/lint.sh --ci --format json` exits 0 (whole suite, routing included, no behavioral regression)
- `bash bin/sync-claude.sh --check`, `bash bin/check-neutrality.sh`, `bash bin/check-privacy.sh` all exit 0
- inclusion-audit header present + mirrored in CLAUDE.md/AGENTS.md/template; N == actual core line count
- agent-parity.md has the desk-check floor + non-gating empirical record citing the routing guard
- DR authored + indexed; wizard dry-run renders over the mirrored template
</verification>

<success_criteria>
- WF-08: routing category (the --check-tree carry-over) + inclusion-audit baseline header + drift info check; core verified section-by-section against the inclusion test; baseline = real post-extraction count (no guessed 145)
- WF-09: three-tier evidence in agent-parity.md — mechanical (routing guard), documented desk-check (gating floor), empirical run (best-effort, non-gating)
- schema-update DR authored + indexed; full CI gate suite green
</success_criteria>

<output>
After completion, create `.planning/phases/17-workflow-extraction/17-04-SUMMARY.md` recording: the routing
category implementation (forward/inverse/severity handling + empty-wiki bypass), the recorded baseline N, the
full per-resident-section inclusion-test justification table, the WF-09 desk-check verdict + empirical-run
outcome (or blocked-on-host-runtime), the DR id, and the exit status of every CI gate command. This SUMMARY
is the WF-08/WF-09 verification evidence for phase close.
</output>
