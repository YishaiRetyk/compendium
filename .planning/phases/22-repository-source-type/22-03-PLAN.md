---
phase: 22-repository-source-type
plan: "03"
wave: 3
autonomous: true
requirements: [REPO-06]
must_haves:
  truths:
    - "One real repository (D-10: gsd-build/get-shit-done; fallback github/spec-kit) acquired via bin/repo-snapshot.sh into sources/2026/2026-07/2026-07-03-<slug>/source.md with a populated ## Excerpts registry."
    - "Source summary at wiki-cloud/sources/src-2026-07-03-<slug>.md with source_type: repository + required fields; existing entity page updated via UPDATE op with #path:-anchored direct claims; README capability claims hedged [epistemic:: tentative] (D-08 exercised)."
    - "Source-scoped audit run resolves the #path locators (0 insufficient-locator on excerpt-backed claims) — non-vacuous proof."
    - "Decision record dr-2026-07-03-repository-source-type (trigger_type: schema-update) records D-01..D-12; index.md + log.md updated; full lint gate exits 0."
  artifacts:
    - "sources/2026/2026-07/2026-07-03-<slug>/source.md (bundle)"
    - "wiki-cloud/sources/src-2026-07-03-<slug>.md, updated entity/concept pages"
    - "wiki-cloud/decisions/dr-2026-07-03-repository-source-type.md"
---

# Plan 22-03 — End-to-end validation ingest + decision record

1. Acquire: run `bin/repo-snapshot.sh https://github.com/gsd-build/get-shit-done` →
   bundle under sources/2026/2026-07/. Curate: keep README core sections, add 3–5
   excerpts (files that back the claims worth making) to `## Excerpts`.
2. Ingest per schema/workflows/ingest.md: Pass 0 → repository; source summary with
   full frontmatter (repo_url, commit_sha, default_branch, license, primary_language);
   merge into `wiki-cloud/entities/gsd.md` (UPDATE op, append-then-synthesize;
   validate-op.sh first) — code-anchored claims `direct`+`sourced`, README
   self-descriptions hedged; wikilinks piped form; compilation tracking fields set.
3. Audit: `bin/audit-claims.sh --select recency --sample 40` scoped to the new source
   (or full run) — assert every #path claim resolves.
4. DR + index + log; final combined gate: lint --dry-run, sync-claude --check,
   check-neutrality, gen-skills --check. Commit: `ingest(<slug>): validate repository
   source type end-to-end` + `reflect(schema): DR for repository source type` (or one
   commit if the DR is authored within the ingest operation — follow one-commit-per-
   logical-op; the DR is part of the schema-validation operation → ONE commit).
