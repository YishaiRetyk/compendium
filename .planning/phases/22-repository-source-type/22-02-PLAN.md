---
phase: 22-repository-source-type
plan: "02"
wave: 2
autonomous: true
requirements: [REPO-02, REPO-03, REPO-05]
must_haves:
  truths:
    - "bin/lint.sh accepts source_type: repository (D-09 enum) and conditionally REQUIRES repo_url + commit_sha + default_branch on repository sources; LINT_VERSION 1.10.1 -> 1.11.0 (MINOR)."
    - "bin/lint.sh provenance locator-syntax validation accepts #path:<file>[:L<n>[-L<m>]] and #commit:<sha> as valid locator forms (no false 'invalid locator' errors)."
    - "bin/audit-claims.sh resolves #path: against the ## Excerpts registry (exact path; contained line-ranges) and #commit: against the snapshot metadata (prefix match vs commit_sha); missing -> insufficient-locator; dispatch order puts #path:/#commit: BEFORE #para/#p (D-11 regression test)."
    - "bin/repo-snapshot.sh exists: <repo-url> [--dest <dir>] -> shallow clone to tmp, harvest SHA/branch/license/language, emit snapshot source.md skeleton; mechanical-only; no network in tests (local fixture git repo)."
    - "tests/phase-22/ harness green; live-tree lint stays clean; phase-09 lint tests unregressed."
  artifacts:
    - "bin/lint.sh, bin/audit-claims.sh, bin/repo-snapshot.sh (new)"
    - "tests/phase-22/{run.sh,test_lint_repository_fields.sh,test_lint_locator_syntax.sh,test_audit_path_resolver.sh,test_repo_snapshot.sh}"
---

# Plan 22-02 — Tooling enforcement (TDD)

RED first: build tests/phase-22 fixtures — a minimal wiki with one repository source
summary (valid + field-missing variants), a snapshot raw source with an `## Excerpts`
registry, and a tiny local git repo built in $TMP for the snapshot script. Then:

1. lint: extend VALID_SOURCE_TYPES; add the conditional-fields check next to the D-07
   PDF block (same pattern, keyed on source_type == 'repository'); verify/extend the
   provenance locator-syntax pattern for the two new forms; bump LINT_VERSION.
2. audit: `_resolve_path` (Excerpts headings `### <path>` / `### <path>:L<a>-L<b>`;
   containment for sub-ranges; passage = heading + fenced excerpt), `_resolve_commit`
   (metadata section passage on prefix-match), dispatch-order fix + D-11 test.
3. repo-snapshot.sh: `git clone --depth 1` to mktemp dir; SHA = `git rev-parse HEAD`;
   default branch from `git symbolic-ref`; license/language heuristics; emit skeleton.
   Anchor to repo root; bash strict mode; placeholders in help text.

Gates: tests/phase-22 all green; tests/phase-09 run.sh no new failures vs pre-plan
baseline; `bin/lint.sh --dry-run` on live tree exits 0. Commit:
`feat(22-02): repository lint gates + #path/#commit audit resolvers + repo-snapshot glue`.
