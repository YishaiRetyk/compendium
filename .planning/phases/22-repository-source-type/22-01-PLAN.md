---
phase: 22-repository-source-type
plan: "01"
wave: 1
autonomous: true
requirements: [REPO-01, REPO-02-partial, REPO-03-partial, REPO-04]
must_haves:
  truths:
    - "schema/reference/repository-ingestion.md exists, agent-authoritative, defining classification (new primary type via the contract), snapshot bundle + Excerpts registry, locators, epistemic split, drift stance, acquisition runbook, ingest checklist."
    - "source-types.md: retro-fit table gains the `repository` row (8 types); the sub-case-registry `repository | pending` row is finalized with the NEW-TYPE verdict and dimension assessment."
    - "frontmatter.md: enum gains `repository`; repository-source fields block documented (required vs recommended)."
    - "provenance.md: locator table gains `#path:` and `#commit:` rows + an example; validation-rule text untouched otherwise."
    - "ingest.md: Pass-0 gains the repository bullet (first-class enum value, NOT a sub-case — D-12); claim-granularity table gains the repository row."
    - "Routing table row added; AGENTS.md ≡ CLAUDE.md byte-equal; schema/AGENTS.template.md mirrored; wizard canonical fixture regenerated if it embeds the routing table."
  artifacts:
    - "schema/reference/repository-ingestion.md (new)"
    - "schema/reference/{source-types,frontmatter,provenance}.md, schema/workflows/ingest.md (edited)"
    - "AGENTS.md + CLAUDE.md + schema/AGENTS.template.md routing rows"
---

# Plan 22-01 — Repository convention (schema surfaces)

Structure `repository-ingestion.md` mirroring `video-ingestion.md`: (1) Classification —
walk the 5 dimensions, verdict NEW PRIMARY TYPE (D-01) — this section is the inverse of
the PDF/video sub-case argument and must say why explicitly; (2) Frontmatter fields
(D-06); (3) Locator usage `#path:`/`#commit:` + Excerpts-registry resolution semantics
(D-02/D-04/D-05); (4) Epistemic split (D-08); (5) Drift stance (D-07 — fields now,
machinery in Phase 23); (6) Acquisition runbook (tool-generic contract: any VCS
snapshot that captures README+docs+SHA satisfies it; `bin/repo-snapshot.sh` is the
worked instance, landing in 22-02); (7) Ingest checklist. Template-public: neutral
placeholders only (`<owner>/<repo>`, `<path/to/file.py>`).

Gates: `bin/sync-claude.sh --check`, `bin/check-neutrality.sh`, `bin/lint.sh --dry-run
--category routing` all exit 0. Commit: `schema(22-01): repository source type — convention,
locators, contract row, routing`.
