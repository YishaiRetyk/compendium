---
quick_id: 260501-g5n
description: requirements-sync strict-mode completion check
created: 2026-05-01
status: in-progress
backlog_origin: ROADMAP.md Phase 999.7 (BACKLOG)
must_haves:
  truths:
    - bin/requirements-sync.sh accepts a new --require-complete flag
    - --require-complete fails (exit 2) when any in-scope REQ-ID has status != Complete in REQUIREMENTS.md
    - --require-complete composes with --phase N (phase-scope closure)
    - default behavior of --strict (drift-only) is unchanged
    - --help text documents the new flag
    - test_requirements_sync.sh covers a Pending-fail case and an all-Complete pass case
  artifacts:
    - bin/requirements-sync.sh (modified)
    - tests/phase-07/test_requirements_sync.sh (extended)
    - .planning/ROADMAP.md (mark Phase 999.7 DELIVERED via quick task 260501-g5n)
  key_links:
    - .planning/ROADMAP.md:353 (Phase 999.7 backlog entry)
    - bin/requirements-sync.sh
    - tests/phase-07/test_requirements_sync.sh
    - tests/phase-07/fixtures/requirements-sync/REQUIREMENTS.md
---

# Quick Task 260501-g5n: requirements-sync strict-mode completion check

## Context

Promotes ROADMAP.md Phase 999.7 (`requirements-sync Strict-Mode Completion Check`) directly via `/gsd-quick`, skipping a standalone phase. Origin captured 2026-05-01 while reconciling Phase 12.1 — the existing `--strict` mode reports `0 drift / NEUT-08 Pending / not found / ok` and exits 0, so closure gates that lean on `--strict` cannot detect incomplete work, only inconsistent work.

Per architectural separation: Phase 13.2 stays pure verification — it will *consume* the new flag, not build it.

## Task 1: Add --require-complete flag to bin/requirements-sync.sh

**Files:**
- bin/requirements-sync.sh

**Action:**
1. Add `REQUIRE_COMPLETE=0` initializer in the argparse block.
2. Add `--require-complete) REQUIRE_COMPLETE=1; shift ;;` case branch.
3. Pass `REQUIRE_COMPLETE` to the python heredoc as an env var.
4. In python: after building `findings`, count in-scope REQ-IDs whose `requirements_md` status is not `Complete`. Emit an `incomplete_count` to a second sentinel file.
5. After the python heredoc, if `REQUIRE_COMPLETE=1` and incomplete_count > 0, exit 2 (regardless of strict drift logic).
6. Update `usage()` `--help` text: document the new flag, its exit semantics, and its composability with `--phase`.

**Verify:**
- `bash bin/requirements-sync.sh --help | grep -q -- --require-complete` → match
- `bash bin/requirements-sync.sh --require-complete; echo $?` against the live tree → exit 2 (NEUT-08 is Pending)
- `bash bin/requirements-sync.sh --strict; echo $?` against the live tree → exit 0 (no drift; existing behavior preserved)

**Done when:**
- New flag parses, executes, and exits 2 on the live tree (NEUT-08 still Pending).
- `--strict` semantics unchanged.
- `--help` text documents the flag.

## Task 2: Extend test_requirements_sync.sh with Pending-fail and all-Complete pass cases

**Files:**
- tests/phase-07/test_requirements_sync.sh

**Action:**
1. Add `t8_require_complete_fails_on_pending`: copy the existing fixture (which has Pending statuses), run with `--require-complete`, assert exit 2.
2. Add `t9_require_complete_passes_when_all_complete`: write a small inline REQUIREMENTS.md heredoc with all Complete statuses, run with `--require-complete`, assert exit 0.
3. Add `t10_require_complete_phase_scoped`: inline fixture with one phase all-Complete and another phase Pending; assert `--require-complete --phase <complete-phase>` exits 0; assert `--require-complete --phase <pending-phase>` exits 2.
4. Wire the three new functions into the test runner block at the bottom.

**Verify:**
- `bash tests/phase-07/test_requirements_sync.sh` → all 10 tests pass.

**Done when:**
- 3 new test functions added, all green.
- No regression in t1–t7.

## Task 3: Mark Phase 999.7 DELIVERED in ROADMAP.md

**Files:**
- .planning/ROADMAP.md

**Action:**
- Update the Phase 999.7 header: append `(DELIVERED via quick task 260501-g5n on 2026-05-01)`.
- Add a one-line Status note in the body: `**Status:** Delivered 2026-05-01 via quick task — see .planning/quick/260501-g5n-requirements-sync-strict-mode-completion/. Promoted directly from backlog without a standalone phase since the work was scope-trivial (one CLI flag + 3 tests).`

**Verify:**
- `grep "DELIVERED via quick task 260501-g5n" .planning/ROADMAP.md` → match.

**Done when:**
- Phase 999.7 entry reflects delivery; future readers understand the work happened.
